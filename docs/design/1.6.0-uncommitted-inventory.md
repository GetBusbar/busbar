# 1.6.0 — UNCOMMITTED INVENTORY

**Census taken 2026-09-22 18:23 PDT.** Main checkout `/Users/matthew/Developer/GetBusbar/busbar`,
branch `consolidated/1.6.0`.

**THE TREE MOVED FOUR TIMES DURING THIS CENSUS.** HEAD went `c6951e1f8` → `f74d1e4f0` →
`66627c65a` in twenty minutes; `crates/busbar-contract/src/zz_fee_plant.rs` and
`crates/busbar-kernel/src/handlers/mcp.rs` were born and died inside single one-minute windows;
`plane_abi_rider.rs` grew from 708 to 715 lines while it was being read. Every count below is a
snapshot, and three of the five untracked files in the main checkout are transient by their own
admission. The *findings* are stable; the *numbers* are not. Re-run before acting on a count.

This file is an inventory and a risk call. **Nothing was committed on anyone's behalf, nothing was
deleted, nothing was restored.** Each owner decides their own.

---

## 1. CENSUS

### 1.1 Main checkout — 26 entries

| class | count | note |
| --- | --- | --- |
| untracked (`??`) | 5 | 1 real, 3 self-declared temporary probes, 1 empty dir |
| staged, index ≠ HEAD | 8 | **4 of these are index-only — see §4.2** |
| worktree-modified | 19 | |

### 1.2 All 26 worktrees

| entries | untracked | deleted | worktree | branch / HEAD | last edit |
| --- | --- | --- | --- | --- | --- |
| 26 | 4 | 0 | `/Users/matthew/Developer/GetBusbar/busbar` | `consolidated/1.6.0` | **live** |
| **1789** | 0 | **1789** | `.claude/worktrees/agent-a7d9b8383fe96aa7e` | `land/conformance-fapi2-complete` | — (see §2.1) |
| 77 | 1 | 0 | `.claude/worktrees/agent-aac1be5378e631a75` | `worktree-agent-aac1be5378e631a75` | 09-17 08:18 |
| 28 | 0 | 0 | `.claude/worktrees/agent-btki` | detached `7e51f473e` | 09-12 20:35 |
| 19 | 0 | 0 | `/private/tmp/pb103-wt` | detached `1ca3d5481` | 09-22 14:25 |
| 11 | 1 | 2 | `/private/tmp/bb-admin-face` | detached `a9a71b961` | 09-22 12:38 |
| 8 | 5 | 0 | `.claude/worktrees/config-seam-work` | `kind-iso-streaming-edge` | 09-17 11:03 |
| 4 | 0 | 0 | `/Users/matthew/Developer/GetBusbar/busbar-gatefix` | `fix/bounded-gate-cleanup` | 09-21 16:58 |
| 4 | 0 | 0 | `.claude/worktrees/agent-a7c7ec0bc73d06da6` | `worktree-agent-a7c7ec0bc73d06da6` | 09-17 08:17 |
| 2 | 0 | 0 | `.claude/worktrees/agent-a7815c8955eda764b` | `land/config-stage1-registry-lift` | 09-19 13:51 |
| 2 | 0 | 0 | `/private/tmp/fixchk` | detached `a90b9255b` | 09-22 16:58 |
| 2 | 0 | 0 | `/private/tmp/oapi-head` | detached `ee930eac8` | 09-22 18:13 |
| 1 | 0 | 0 | `.claude/worktrees/agent-a0ba853f92322eb96` | `worktree-agent-a0ba853f92322eb96` | 09-17 08:17 |
| 1 | 0 | 0 | `.claude/worktrees/agent-aaca33d2ba7534846` | `worktree-agent-aaca33d2ba7534846` | 09-17 09:34 |
| 1 | 0 | 0 | `.claude/worktrees/agent-a20735a44164f185d` | `streaming-switchover-s1` | 09-17 09:50 |
| 1 | 0 | 0 | `.claude/worktrees/agent-a462eadc5dc838a37` | `worktree-agent-a462eadc5dc838a37` | 09-17 10:01 |
| 1 | 0 | 0 | `/private/tmp/headcheck`, `/private/tmp/headchk2`, `/private/tmp/xtchk`, `/private/tmp/busbar-head` | detached | 09-22 |
| 0 | 0 | 0 | 5 clean worktrees | `agent-a073d906b8dfbcbfb`, `agent-a55b127e315e34e25`, `agent-a7318063ab6eec351`, `agent-aa483259d9e300b38`, `/private/tmp/{busbar-oracle-clean,done-snap,ki-base}` | — |

**Of the single-entry worktrees, six carry only a `Cargo.lock` churn** (`headcheck`, `headchk2`,
`xtchk`, `agent-a20735a44164f185d`, `agent-a462eadc5dc838a37`, `busbar-head`). Regenerable; zero risk.

**Nine of nine local agent branches checked are ABSENT from `origin`.** Their *committed* work is in
the local object store — safe from `git clean`, not safe from disk loss.

### 1.3 The prior report's examples are STALE — all of them landed

`crates/busbar-transport-{grpc,sse,stdio,ws,http,tcp,tls}/src/{claims,meta}.rs` and
`crates/busbar-transport-sse/src/transport.rs` are **committed** at `465d9f207`.
`xtask/src/gates/construction/teller_paths.rs` is **committed** at `897074b56`.
`crates/busbar/src/root/tests/transports.rs` — the file named in this morning's HEAD breakage — is
now **tracked**, with its `pub mod transports;` committed at `root/mod.rs:77`. **That incident is
repaired.** Re-derivation was required and is the reason this section exists.

---

## 2. THE 1,789-FILE WORKTREE — IT IS NOT 1,789 FILES OF WORK

`.claude/worktrees/agent-a7d9b8383fe96aa7e`, branch `land/conformance-fapi2-complete`.

**All 1,789 entries are ` D` — deleted-in-worktree, unstaged. Zero untracked. Zero modified.**
1,559 are `.rs`. `crates/` (1,776 entries), `Cargo.toml`, `docker/`, `bin/`, `.githooks/`,
`LICENSE`, `CHANGELOG.md`, `deny.toml`, `codecov.yml`, `rustfmt.toml` are physically absent from
disk. The branch tracks 3,706 files, so roughly half the checkout was removed.

**The answer that changes everything: it is leaked *deletion*, not leaked work, and nothing is at
risk.**

```
land/conformance-fapi2-complete          aba990469671347c2647b7f069ccff841777c974
origin/land/conformance-fapi2-complete   aba990469671347c2647b7f069ccff841777c974
```

Identical SHAs. Every byte is in the local object store **and** on the remote. Nothing here can be
lost by `git clean`, by deleting the worktree, or by anything short of losing both. The tip is real
work — `feat(oauth2): FAPI2 as config-gated capability` — and it is safely pushed.

What it costs: the worktree cannot build, and it looks like a catastrophe to anyone who runs
`git status` in it. A `git -C <wt> checkout -- .` would restore it. **I have not run it** — per the
destroy-nothing rule, a "restore" is still a write to someone else's checkout.

---

## 3. UNTRACKED SOURCE FILES — ALL 11, EVERYWHERE

| # | file | worktree | lines | referenced by | build-critical? | finished? |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `crates/busbar-kernel/tests/plane_abi_rider.rs` | main | 715 | **uncommitted only** — `plugin-loader/src/plane.rs` doc + `.github/workflows/ci.yml` comment, both by path | No (cargo auto-discovers `tests/`) | **Yes** — 7 tests, no TODO/FIXME, all imports resolve against HEAD |
| 2 | `crates/busbar-plane-streaming/src/lib.rs` | `agent-aac1be5378e631a75` | 24 | `Cargo.toml` `[lib] path = "src/lib.rs"` (**committed on that branch**) | **YES — hard break on that branch** | Yes, but superseded (§5.4) |
| 3 | `crates/busbar/src/root/units_admin/tests/admin_path_without_plane_face.rs` | `bb-admin-face` | 455 | `admin_mount.rs:698` `#[path=…] mod` — **modified-uncommitted** | **Would be, if `admin_mount.rs` lands first** | Yes |
| 4 | `xtask/src/gates/hot_path_perf.rs` | `config-seam-work` | ~9.5 KB | `xtask/src/gates/mod.rs` `pub mod` + `Registration` — **modified-uncommitted** | **Would be, if `mod.rs` lands first** | Yes |
| 5 | `xtask/src/gates/hot_path_alloc.rs` | `config-seam-work` | ~7.3 KB | same as #4 | same as #4 | Yes |
| 6 | `crates/busbar-core/benches/plane_host_vtable_perf.rs` | `config-seam-work` | ~10 KB | `crates/busbar-core/Cargo.toml` `[[bench]]` — **modified-uncommitted** | **Would be, if `Cargo.toml` lands first** | Yes |
| 7 | `crates/busbar-core/benches/plane_host_vtable_alloc.rs` | `config-seam-work` | ~7.6 KB | same as #6 | same as #6 | Yes |
| 8 | `xtask/tests/hot_path_gates.rs` | `config-seam-work` | ~1.7 KB | cited by name in `xtask/src/full_gate.rs` `REGISTRY_NOT_IN_CI` — **modified-uncommitted** | No (auto-discovered) | Yes |
| 9 | `crates/busbar-kernel/tests/zz_audit_probe_auth.rs` | main | 8.5 KB | nothing | No | **Transient** — `// TEMPORARY AUDIT PROBE — delete after the run.` |
| 10 | `crates/busbar-kernel/tests/zz_audit_probe_argguard.rs` | main | 1.8 KB | nothing | No | **Transient** — same header |
| 11 | `crates/busbar-kernel-egress/tests/zz_audit_probe_egress.rs` | main | 5.1 KB | nothing | No | **Transient** — same header |

Plus `crates/busbar-llm-codec/tests/` — an empty directory, no content.

**Observed transient churn:** `crates/busbar-contract/src/zz_fee_plant.rs` and
`crates/busbar-kernel/src/handlers/mcp.rs` both appeared in one census and were gone from the next,
under a minute apart. A live audit agent is planting red-proof probes and cleaning up after itself.
Rows 9–11 are that agent's; they are **not** at risk and should **not** be committed.

**No source file is hidden by `.gitignore`.** All 15 ignored entries are build/venv/report
directories plus `crates/busbar/busbar-signing.key`. Nothing was missed there.

---

## 4. MODIFIED-TRACKED FILES (main checkout)

### 4.1 The §11a plane-ABI rider unit — one change in six parts, five tracked, **one not**

This is the largest coherent body of uncommitted work in the tree and it is **finished**.

| part | state | size |
| --- | --- | --- |
| `crates/busbar-kernel/tests/plane_abi_rider.rs` | **UNTRACKED** | 715 lines / 34 KB |
| `crates/plugin-loader/src/tests/plane_conformance_tests.rs` | modified | +639 |
| `crates/plane-example/src/lib.rs` | modified | +249 |
| `crates/plugin-loader/src/plane.rs` | modified | +28/−14 |
| `crates/plugin-loader/src/tests/export_conformance_tests.rs` | modified | +23 |
| `crates/plugin-loader/src/registry.rs` | modified | +4/−4 |
| `.github/workflows/ci.yml` | modified | +9 (`-p busbar-plugin-example-plane`) |

The parts *name each other*. `plane.rs`'s new doc prose cites
`crates/busbar-kernel/tests/plane_abi_rider.rs` by path; `ci.yml`'s new comment cites it by path and
explains that the added build line exists to keep the rider's seven tests from panicking. The rider
itself cites `plane_conformance_tests.rs` as "the other half". Every import resolves against
committed code (`busbar_kernel::plane_host::with_dispatch_scope`,
`busbar_kernel::test_support::TestApp`, `busbar_plugin_loader::{DynPlane, plugin_library_filename,
registry::{scan_and_validate, open_plane}}`), and `busbar-plugin` + `busbar-plugin-loader` are both
already committed dev-dependencies of `busbar-kernel`. **It compiles as written.**

**Two-part flag, benign direction:** if `ci.yml` lands without the rider, CI spends ~30 s building a
cdylib that nothing loads. If `plugin-loader/src/plane.rs` lands without the rider, its doc names a
path that does not exist — a `doc-links`/prose gate candidate. Neither breaks the build. **The
damage is not a red; it is the silent loss of the artifact in §5.1.**

### 4.2 FOUR INDEX-ONLY ENTRIES — invisible to `git diff HEAD`, and they hold *superseded* content

For these, **worktree == HEAD but index ≠ HEAD**. They do not appear in `git diff HEAD`. They are
not work-in-progress; the index is holding an **older** version that HEAD has since improved.

| file | what the stale index would restore |
| --- | --- |
| `crates/busbar-a2a/src/a2a/meter.rs` | the module-wide `#![cfg_attr(not(test), allow(dead_code))]` that HEAD's re-measurement deliberately narrowed to three items — "a silencer that would absorb the fourth" |
| `crates/busbar-plane-mcp/src/codec.rs` | a doc comment naming `busbar_substrate_values::ingress::jsonrpc` directly, which HEAD replaced precisely because the `kind-isolation:matrix` scanner counts a doc comment like a `use` |
| `qa/kind-isolation.toml` | a smart-quote, a nested-quote `cite` string, and two wrong cell counts (`busbar-kernel`/`transport` 2212 → HEAD's 4) |
| `docs/design/1.6.0-engine-split-plan.md` | 141 lines of plan prose that HEAD has since rewritten |

**A bare `git commit` in this checkout silently reverts four committed improvements.** Anyone
committing here must use `--only <their own paths>`.

### 4.3 THE STAGED REGRESSION — three files pointing at a deleted crate

`scripts/release-build.sh`, `scripts/release-key-guard.sh`, `.github/workflows/plugin-ci.yml`.
Staged **and** present in the worktree (`M ` — index == worktree), mtime **09-17**, untouched for
five days. All three rewrite `crates/plugin-loader/src/sign.rs` into `crates/plugin-sign`:

```
-# `option_env!("BUSBAR_RELEASE_PUBKEY")` (crates/plugin-loader/src/sign.rs) is a COMPILE-time read
+# `option_env!("BUSBAR_RELEASE_PUBKEY")` (crates/plugin-sign) is a COMPILE-time read
```

`crates/plugin-sign` **does not exist** — `busbar-plugin-sign` was folded into `busbar-plugin-loader`
at `d81a76f7f`, and `crates/plugin-loader/src/sign.rs` is the live tracked file. HEAD already carries
the correct path, put there by `672535a64` *"fold(#33): repoint what the two plugin folds left
pointing at deleted paths."*

**These three are the pre-fix text, staged before the fix landed and left sitting. Committing the
index re-introduces exactly the defect `672535a64` repaired**, into the release build script, the
release key guard, and plugin CI. This is the highest-probability harm in the tree: it needs no
mistake beyond a `git commit` without `--only`.

### 4.4 Live, healthy, in-flight (no action)

| file | change | state |
| --- | --- | --- |
| `qa/construction.toml` | +160/−34 — re-scoping the voice glob after the codec fold | active, 18:07 |
| `scripts/proto-deletion-gate.sh` | +205/−… | active, 18:06 |
| `xtask/src/gates/construction/rules2.rs` | +68 | active, 18:07 |
| `crates/busbar-core-admin/src/v1/json/openapi.json{,.gz}` | +23 — regenerated, both halves moved together | consistent |
| `crates/busbar/src/root/tests/transports.rs` | +118 | active, tracked, mod decl committed |
| `docs/design/1.6.0-{commit-residue,duplication-census,oracle-coverage,secret-hygiene}.md` | docs | active |

The `openapi.json` / `openapi.json.gz` pair is the one generated-artifact pair that *could* drift —
both are modified together and `/private/tmp/oapi-head` holds the same pair. Consistent.

---

## 5. AT RISK — ranked by "if this vanished, would anything break and would anyone notice"

### 5.1 `crates/busbar-kernel/tests/plane_abi_rider.rs` — **715 lines, nothing else holds it**

*Breaks:* nothing. *Notices:* **yes, loudly, and not for days.**

The only proof that BUSBAR-1.6.0 §11a's "no 0-caller ABI" is satisfied. Seven tests that dlopen a
real cdylib over the hot-tier C ABI and drive it against the **real** host vtable — per-slot call
counts, load-bearing-ness by withdrawal, the host's own clock read back, a metering lease opened
plugin-side and settled in core's registry, and pricing-blindness asserted off the wire. Hand-written
`unsafe` FFI test code with a documented safety contract per block. It cannot be re-derived from the
other five parts; it *is* the part that crosses the seam, and its sibling `plane_conformance_tests.rs`
exists only because `busbar-plugin-loader` may not name `busbar-kernel` and therefore **cannot** do
what this file does.

Its five siblings are tracked, so `git clean` would leave a §11a claim asserted by prose in
`plane.rs`, a CI line building a cdylib nothing loads, and no proof. **It is actively being edited
right now** (708 → 715 lines mid-census), so its author is live — this is a "commit it before you
finish for the day" item, not an orphan.

> **Owner action:** commit with `--only crates/busbar-kernel/tests/plane_abi_rider.rs` **together
> with** the five tracked siblings and `ci.yml`. Committing any subset is the failure mode that broke
> HEAD twice today.

### 5.2 The staged `crates/plugin-sign` regression — **the only item that causes harm by inaction**

*Breaks:* the release build script, the key guard, and plugin CI all pointing at a deleted crate.
*Notices:* whoever next reads them to find the compile-time key read — i.e. someone debugging an
unsigned release binary, at the worst possible moment.

Everything above is "work that could be lost". This is the inverse: **staged content that will be
gained by accident.** It is five days stale, it is already in the working tree, and it survives every
`git commit -a` and every `git commit` without `--only`. It is ranked second only because it destroys
nothing — it just quietly re-breaks something already fixed.

> **Owner action:** unstage and revert these three to HEAD's text. *I have not done so* — it is a
> write to three files I do not own.

### 5.3 `config-seam-work` — 5 untracked files, ~36 KB, **the largest genuinely orphaned work**

*Breaks:* nothing on trunk. *Notices:* **nobody. Ever.**

A complete, self-consistent §8 hot-path gate: two criterion benches (vtable crossing < 1 µs at p50
**and** p99; zero global allocations across an isolated POD host-call batch), two xtask gates that
read them, an xtask test driving both through the dispatcher, plus three modified files that wire it
all up — `gates/mod.rs` declares and registers both, `busbar-core/Cargo.toml` declares both
`[[bench]]`, `full_gate.rs` files both `REGISTRY_NOT_IN_CI` excuses with written justifications.
**Eight pieces, all uncommitted, five of them untracked.**

Triply orphaned: untouched since **09-17 11:03** (5 days); branch `kind-iso-streaming-edge` is not on
`origin`; and it targets `crates/busbar-core/benches/` — **`busbar-core` has 0 tracked files on
trunk**, absorbed into `busbar-kernel` at `673ecdaaa`. The benches would need relocating to
`busbar-kernel` to apply at all.

This is the one entry where the honest answer is *valuable, finished, and nobody is coming back for
it.* If §8's perf/alloc gates are wanted for 1.6.0, this is a day of work already done, sitting one
`git clean` from gone.

### 5.4 `agent-aac1be5378e631a75` — `busbar-plane-streaming/src/lib.rs`, build-critical but superseded

*Breaks:* **that branch, hard.** `Cargo.toml` declares `[lib] path = "src/lib.rs"`; 68 files are
staged under `src/voice/`; without the crate root there is no crate. *Notices:* nobody.

The keystone of a staged 68-file three-crate collapse (`busbar-voice` + `busbar-voice-codec` +
`busbar-plane-voice` → `busbar-plane-streaming`, per DECISIONS #6/#9/#12/#18). **The 68 renames are
staged — their blobs are in the object store. Only the 24-line crate root is not.**

But trunk took a different route: trunk's `busbar-plane-streaming` (21 tracked files) is the renamed
*plane-voice adapter*, and `busbar-voice` (35 files) and `busbar-voice-codec` (16 files) still stand.
Untouched since 09-17 08:18; branch not on `origin`. **Ranked here only for completeness: the file is
24 lines of `pub mod voice;` and doc prose, re-derivable in two minutes, for an approach trunk did
not take.**

### 5.5 `bb-admin-face` — the two-part pattern, armed

*Breaks:* `cargo test -p busbar` on a fresh clone — **if `admin_mount.rs` is committed and the test
file is not.** *Notices:* the next person to clone.

`crates/busbar/src/root/units_admin/tests/admin_path_without_plane_face.rs` (455 lines) is required
by `#[path = "tests/admin_path_without_plane_face.rs"] mod admin_path_without_plane_face;` at
`admin_mount.rs:698-699`. Right now both halves are uncommitted, so the tree is self-consistent.
**The exact configuration that broke HEAD twice today is one `git add admin_mount.rs` away.**

It is the proof that removing `impl Plane for AdminPlane` cost nothing — a real admin verb still
travels the whole chain and the admin units still meter and audit explicitly. It belongs to a live
refactor that also **deletes** `busbar-core-admin/src/admin_codec/codec.rs`. Worktree is on a
**detached HEAD** (`a9a71b961`) in `/private/tmp` — no branch holds it, and `/private/tmp` is not a
durable location on macOS. Last touched 12:38 today.

> **Owner action:** commit both halves together, or neither. And move this off `/private/tmp`.

### 5.6 Below the risk line

- **`agent-btki`** (28 modified, **09-12**, 10 days stale) — touches `crates/busbar-substrate`
  (0 files on trunk, deleted at `5fa320208`) and `busbar-voice-codec`. Includes three staged
  test-file renames. Orphaned and largely unapplicable.
- **`agent-a7815c8955eda764b`** (2 modified, 09-19) — `crates/busbar-core/src/config/` — **crate
  deleted on trunk.** Orphaned.
- **`pb103-wt`** (19 modified, +846/−312, 09-22 14:25) — substantial live money/governance work,
  all **tracked**. Not at risk, but on a detached HEAD in `/private/tmp` with no branch.
- **`agent-a7c7ec0bc73d06da6`**, **`agent-aaca33d2ba7534846`**, **`agent-a0ba853f92322eb96`**,
  **`busbar-gatefix`**, **`fixchk`** — small tracked-only edits. Recoverable from the index/HEAD.
- **The `zz_audit_probe_*` files** — self-labelled "delete after use". Do not commit.

---

## 6. THE ONE-LINE ANSWER

**Exactly one file in this checkout is both valuable and held nowhere else:**
`crates/busbar-kernel/tests/plane_abi_rider.rs` — 715 lines, the sole proof of a release gate, five
tracked siblings already waiting on it, author still typing.

**Exactly one body of finished work is genuinely orphaned:** `config-seam-work`'s five untracked
files, the §8 hot-path perf/alloc gates, five days cold on an unpushed branch against a deleted crate.

**And the 1,789 are a red herring** — every byte of that worktree is on `origin`.
