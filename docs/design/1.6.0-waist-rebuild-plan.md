# 1.6.0 waist rebuild — the Teller loop as new code, oracle-green at every PR

Status: proposal, 2026-09-04. Companion to the migration plan (Phase 1.6 "the attempt seam", Phase 2 "the Teller loop as a shell around the existing waist", "Build-quality track"). Behaviour authority: `testing/shadow-oracle` (780 owed, 0 unaccepted) + `testing/llm-conformance`. Construction authority: `scripts/construction-gate.sh` over `qa/construction.toml`. Design compliance: `qa/DESIGN-BINDINGS.md`.

## 0. What the waist is today (file:line, as measured)

The request path for one LLM unit, in call order:

1. `crates/busbar-core/src/router.rs:654 apply_common_layers` → `crates/busbar-core/src/ingress/dispatch.rs:26 protocol_dispatch` → body/path arrival tables (`busbar_llm::BODY_INGRESS` / `PATH_INGRESS`, installed at `crates/busbar/src/main.rs:617,635`).
2. `crates/busbar-llm/src/native_ingress.rs:39 operation_ingress_inner` (body-model) or `:496 ingress_path_model_inner` (gemini/bedrock): handler lookup, `LazyBody::parse`, model extraction; every failure exits through `host.finish_rejected` (`:55,:70,:102,:137,:525,:564,:598,:620`).
3. `native_ingress.rs:360 run` builds `GauntletRequest { correlation_id: 0 … }` and calls `busbar_substrate::plane_host::run_gauntlet` (`crates/busbar-substrate/src/plane_host/mod.rs:259`), which runs `admit_open` (`:240`) = `NativePlane::verify_destination` (`native_ingress.rs:202` → `AdmissionHost::destination_guard`, substrate `:1042`, core `crates/busbar-core/src/ingress/mod.rs:379`) then `NativePlane::drive` (`native_ingress.rs:221`).
4. `drive`: `host.admission_door` (`substrate :1061` → core `ingress/mod.rs:443` → `admit_check :135` → `governance/state.rs:1752 try_admit`), candidate resolution (`:256-287`; model-miss 404 through `finish_admitted` with `charged`), `usage_sink` (`:439`), `forward_with_pool_parsed` (`crates/busbar-llm/src/engine/pipeline.rs:214`), then `host.finish_admitted` (`:348`).
5. `forward_with_pool_parsed` (`pipeline.rs:214-335`): stamps `host.next_request_id()` once (`:252`), captures the response-tap shape, calls the first twin, fires the completion tap (`:316`).
6. Twin A `forward_with_pool_parsed_inner` (`pipeline.rs:828-2930`, 2103 lines): op-support filter `:881`, stream/usage flags `:923-955`, global+pool rewrite pass `:957-1050`, IR `:1061`, request taps `:1078`, gemini array shim `:1105`, affinity hash `:1123`, failover cfg `:1141`, breaker cfg `:1158`, `RequestCtx` `:1161`, exclusions `:1174`, decision gates `:1197-1409`, base policy `:1411-1615`, candidate taps `:1625-1662`, **the loop** `:1681` (deadline `:1683`, `pick_among` `:1698`, probe guard `:1761`, exclude `:1776`, routing tap `:1781`, translate `:1837-1948`, include_usage injection `:1974`, creds/target/auth/headers `:2003-2143`, send `:2196-2212`, classify `:2265-2664`, success bookkeeping `:2676-2714`, buffered translate `:2759`, `FirstByteBody` `:2836`), exhaustion `:2915`.
7. Twin B `forward_once` (`crates/busbar-llm/src/engine/walk.rs:413-1098`, 686 lines): the same attempt for the degraded paths, called from `handle_queue :303`, `handle_fallback_pool :1232`, `handle_least_bad :1333`.
8. Meter: `UsageSink` (`engine/response_body.rs:12`), stream-end tap in `FirstByteBody` (`response_body.rs:267` poll, `:762` Drop), buffered tap in `translate_response_cross_protocol` (`pipeline.rs:411`), accrual through `engine/usage.rs:95 ledger_and_meter` → `EngineHost::meter_ledger/meter_series`.
9. Audit: `finish_inner` (`ingress/mod.rs:586`), reached only via `finish`/`finish_admitted`/`finish_rejected` (`:497/:529/:563`); refund at `:686-690` via `governance/state.rs:2035 refund_request`.

Construction today (`target/construction/report.md`): one-attempt-seam FAIL (send at `walk.rs:694` outside `forward_with_pool_parsed_inner`), request-path-fn-size FAIL (2103 / 686 / 411 vs 200), no-uninstalled-seam FAIL (`install_stream_translator_factory`, `crates/busbar-substrate/src/proto.rs:1529`, only `crates/busbar-llm/src/testkit.rs:41` installs it), duplicate-dispatch WARN 153 lines.

## 1. Target module layout

### 1.1 Kernel side — `crates/busbar-substrate/src/teller/` (new, neutral, names no plane or dialect)

| file | contents | lines (est.) |
|---|---|---|
| `teller/mod.rs` | `pub mod tokens; pub mod steps; pub mod unit; mod run;` re-exports; the `Step` sealed trait + the nine ZST step markers `Arrival, Decode, Authenticate, Verify, Approve, Admit, Route, Meter, Audit` | 80 |
| `teller/tokens.rs` | `UnitToken<S>` (private `pub(super) fn mint()`), `Decision<S>` (`#[repr(transparent)] #[must_use]`, built only via `Decision::proceed(&UnitToken<S>, S::Facts)` / `Decision::refuse(&UnitToken<S>, Refusal)`), `Hold` (`#[repr(transparent)] #[must_use]`, no `Drop`, wraps `HoldInner { admit: Option<AdmitHandle>, downgraded: Option<String>, charged: bool }` until Phase 3), `Posted` (`#[repr(transparent)]`, minted only by `AuditToken`). All `!Clone + !Copy`. Compile-fail doctests: a `UnitToken<Meter>` cannot build `Decision<Admit>`; a plane cannot construct `Hold` or `Posted` | 220 |
| `teller/steps.rs` | `pub trait TellerPlane` — one method per step, each taking `&UnitToken<S>` and the previous step's facts and returning `Decision<S>`; `route` and `meter` are `async`. Also `Refusal { resp: Response, after_admit: bool }` | 160 |
| `teller/unit.rs` | `Unit<'a> { gov: &'a PlaneRequestCtx, destination: &'a str, started: Instant, charged_at: u64, correlation: Cell<u64> }` (successor of `GauntletRequest`); `UnitEnd` (Phase 2: `Completed | Refused(StepName)`), `StepName` enum | 120 |
| `teller/run.rs` | the ONE loop `pub async fn run_unit<P: TellerPlane>(plane: P, unit: Unit<'_>) -> Response`. Order fixed by types: `arrival → decode → authenticate → verify → approve → admit(→Hold) → route(&Hold) → meter → audit(Hold) → Posted`. A `Refuse` before Admit goes to `plane.audit_refused` (= `finish_rejected`, PB-26); a `Refuse` at/after Route goes to `plane.audit(hold, …)` (= `finish_admitted`, charged). No `?`, no early `return` of a `Response` except through `audit` | 150 |
| `plane_host/mod.rs:240-289` | `admit_open`/`run_gauntlet`/`run_gauntlet_session` become thin adapters over `run_unit` with a 2-step `GauntletAdapter` (Verify = `verify_destination`, Route = `drive`, every other step `proceed`) so mcp/a2a/voice compile unchanged until Phase 4 | −20/+60 |

### 1.2 Plane side — `crates/busbar-llm/src/unit/` (new; one file per Teller step)

| file | body = existing code, called unchanged | lines |
|---|---|---|
| `unit/mod.rs` | `pub struct LlmUnit<'a>` (owns what `NativePlane` owns today: `host, proto, operation, op_handler, headers, body, parsed, caller_token, model_not_found_message`), `impl TellerPlane for LlmUnit`, `pub async fn run(...)` = the two public entry points `operation_ingress` / `ingress_path_model` (same fn-pointer signatures as `native_ingress.rs:713/:737`) | 180 |
| `unit/arrival.rs` | Arrival facts the plane sees after `apply_common_layers` ran in core: content-type read (`native_ingress.rs:85-88`), `LazyBody::parse` (`:95-119`) or the path-model parse+inject (`:514-612`). Refusals: parse 400 / non-object 400 / reserialize 400 (the same three `ingress_error` literals) | 150 |
| `unit/decode.rs` | `request_handler(proto)` + `operation_handler(operation)` (`:54-83`, `:617-633`), model extraction incl. `multipart_model` (`:26`, `:120-151`). Refusals: 404 protocol/endpoint, 400 missing model | 120 |
| `unit/authenticate.rs` | reads `unit.gov.key` (auth middleware already ran); `Decision::proceed` with `Principal { key: Option<Arc<VirtualKey>> }` | 40 |
| `unit/verify.rs` | `host.destination_guard(gov, proto, model, started, charged_at)` (`native_ingress.rs:202-219`) — pool ACL → fallback ACL → no-rate 400, in that order. **Not** candidate resolution (see hazard H-3) | 60 |
| `unit/approve.rs` | no-op seat; comment cites PB-46: migrated hooks seat `After(Admit)` | 25 |
| `unit/admit.rs` | `host.admission_door(...)` (`native_ingress.rs:244-253`) → `Hold`; downgrade re-pool; `usage_sink` construction (`native_ingress.rs:439-473`) moved here because the sink IS the hold's meter half (it carries `AdmitHandle`) | 110 |
| `unit/route.rs` | candidate resolution (`native_ingress.rs:256-287`; model-miss 404 is a post-admit `Refuse`), affinity header (`:299`), `correlation = host.next_request_id()` (moved from `pipeline.rs:252`, exactly once, only on units that reach Route), completion-shape capture (`pipeline.rs:263-288`), `engine::route::dispatch(...)` (the failover loop, §1.3), completion tap (`pipeline.rs:308-331`) | 190 |
| `unit/meter.rs` | Phase 2: the stream-end tap already lives in `FirstByteBody`/`translate_response_cross_protocol`; this step records `MeterFacts { status, charged }` for Audit and owns the `Posted` hand-off. Phase 3 grows the ledger dual-write here | 60 |
| `unit/audit.rs` | THE plane's only fn that returns an `axum::Response` to the loop: `finish_admitted` / `finish_rejected` via `AdmissionHost` (`native_ingress.rs:278-286`, `:348-356`), `host.pool_label`, the `POOL_LABEL_UNRESOLVED` rule for pre-route refusals | 90 |

### 1.3 The single attempt and the Route internals — `crates/busbar-llm/src/engine/`

| file | contents | lines |
|---|---|---|
| `engine/attempt.rs` | **the ONE `pub(crate) async fn attempt(a: AttemptInput<'_>) -> AttemptOutcome`** — the only non-test site of `.client().get().request(` in the workspace. Inputs: `host, rt, lane i, permit, probe_epoch, pool_cell: &str, cands: &[WeightedLane], body: &Bytes, hop_v: Option<Value>, req_content_type, ingress_protocol, op, wants_stream, client_include_usage, client_has_stream_options, gemini_json_array, caller_token, resolved_gov_key, remaining_secs, breaker_cfg: &Arc<BreakerCfg>, client_fwd, usage_sink, chosen_policy_name, metric_pool: &str`. Outputs: `AttemptOutcome::Response(Response)` (delivered 2xx body, passthrough 40x, ClientFault relay, HardDown-auth envelope), `AttemptOutcome::Failed { disposition: Disposition, class: StatusClass, err_type: &'static str, relay: Option<Response> }` (transport error, attempt timeout, TransientUpstream, HardDown-billing, ContextLength — the caller decides failover vs relay), `AttemptOutcome::Bail(Response)` (pre-dispatch internal errors, `pipeline.rs:1865/:1914/:1944/:2039/:2111`). Breaker recording, trip emit, `record_client_fault`, `record_success_in`, latency, `spend_budget`, `BudgetSpendGuard`, `ProbeGuard` all live here, once | 700 → split to `attempt/mod.rs` (loop body ≤200), `attempt/assemble.rs` (translate + creds + auth + headers + include_usage, `pipeline.rs:1837-2143`), `attempt/send.rs` (deadlines + `SendOutcome`, `:2156-2252`), `attempt/classify.rs` (`:2296-2664`), `attempt/respond.rs` (success bookkeeping + `FirstByteBody` build, `:2676-2908`), `attempt/buffered.rs` (today's `translate_response_cross_protocol` `pipeline.rs:411-827`, split ≤200 each) |
| `engine/route/mod.rs` | `pub(crate) async fn dispatch(r: RouteInput) -> Response`: `prepare()` then the loop `for attempt in 0..=max_cap` (`pipeline.rs:1681-2911` re-expressed over `attempt()`), routing-stage taps per attempt (PB-95), context-length exclusion, `last_failure`, exhaustion hand-off | 180 |
| `engine/route/prepare.rs` | op-support filter, stream/usage flags, failover+breaker cfg, `RequestCtx`, exclusions (`pipeline.rs:881-1180`) → `Prepared` struct | 170 |
| `engine/route/rewrite.rs` | global + pool rewrite pass (`pipeline.rs:957-1050`) | 110 |
| `engine/route/taps.rs` | IR ensure + request taps + candidate/routing shape (`:1061-1093`, `:1625-1662`) | 90 |
| `engine/route/gates.rs` | decision gates reconcile 1/2/3 (`:1197-1409`) | 190 |
| `engine/route/policy.rs` | base routing policy (`:1411-1615`) | 180 |
| `engine/exhaustion.rs` | `handle_exhaustion_for_pool`, `handle_status_503`, `retry_after_secs`, `handle_queue`, `handle_fallback_pool`, `handle_least_bad` (walk.rs `:86-384`, `:1106-1368`) rewritten to call `attempt()` and to map `AttemptOutcome::Failed` → "try next / relay" exactly as `forward_once`'s `Err(())`/`Ok(resp)` did | 480 → `exhaustion/{mod,queue,fallback,least_bad}.rs` |

### 1.4 What stays as parts, unchanged

- Dialect codecs: `crates/busbar-llm/src/{anthropic,bedrock,cohere,gemini,openai_chat,openai_responses}/`, `proto_codec.rs`, `proto_stream.rs` (`new_stream_translator :1107`), `ir/`, `ir_encode.rs`, `leaf_codec.rs`.
- `engine/select.rs` — `pick_among :280`, `RequestCtx :22`, `ProbeGuard`, `SwrrOrder`; `busbar_substrate::failover::walk_with` (the one selection loop, PB-57).
- `engine/wire.rs` — `translate_request_cross_protocol :344`, `shape_cross_protocol_error :193`, `read_capped_body :633`, error literals.
- `engine/egress.rs`, `engine/tables.rs`, `engine/usage.rs`, `engine/response_body.rs` (`UsageSink`, `FirstByteBody`), `engine/hooks.rs` (`apply_global_rewrites :373`, `decide_policy_order :490`, `capture_stage_shape :1005`, `fire_stage_taps`), `engine/health.rs`, `engine/build_runtime.rs`.
- Breaker/lane store: `busbar_substrate::store::LaneRuntime` verbs (`record_transient_in :691`, `record_rate_limit_in :707`, `record_hard_down_all_cells :735`, `record_success_in :654`, `spend_budget :767`, `refund_budget :775`, `try_admit :555`, `try_admit_breaker :579`, `try_acquire :761`).
- Admission door and terminal in core: `ingress/mod.rs` `admit_check :135`, `governance_guard :349` (kept for its test callers), `destination_guard :379`, `admission_door :443`, `finish_admitted :529`, `finish_rejected :563`, `finish_inner :586`; `governance/state.rs` `try_admit :1752`, `refund_request :2035`, `flush_metering :916`; substrate ports `AdmissionHost :968`, `MeteringHost :485`, `JournalHost :613`, `CompletionHost :1114`.
- Arrival/Decode in core: `router.rs:654 apply_common_layers`, `ingress/dispatch.rs:26 protocol_dispatch`, `arrival.rs` (gemini/bedrock URL parsing).

### 1.5 What is deleted at the end

`pipeline.rs:214-335` (`forward_with_pool_parsed`), `:828-2930` (`_inner`), `:2938-3040` injectors move to `attempt/assemble.rs`, `:3042` `fire_global_taps` moves to `route/taps.rs`; `walk.rs` whole file; `native_ingress.rs:170-438` (`NativePlane`, `run`, `operation_resolved`), `:39-169`, `:496-652` (become `unit/{arrival,decode}.rs`); the `GauntletAdapter` for llm. `synthesize_completion` (`native_ingress.rs:668`) and `operation_ingress`/`ingress_path_model` keep their names and move to `unit/mod.rs` (the composition root and `CompletionHost` name them).

## 2. PR order (oracle green at every step)

Switch mechanism during coexistence: a cargo feature `teller-waist` on `busbar-llm` and `busbar` (default off in PR-4, default on in PR-5, removed in PR-6). Both paths always compile and are always unit-tested; the feature only chooses which fn-pointer tables the composition root installs at `crates/busbar/src/main.rs:617/:635/:644`: `busbar_llm::{BODY_INGRESS, PATH_INGRESS}` (legacy) vs `busbar_llm::unit::{BODY_INGRESS, PATH_INGRESS}` (Teller). No config key, no env var, no boot line (PB-17/40/41 neutral). The `shadow-oracle` CI job (`.github/workflows/ci.yml:1336`) gains a `waist: [legacy, teller]` matrix for PRs 4–5 only.

| PR | title | families that must be 0-diff | construction-gate row it moves |
|---|---|---|---|
| **PR-0** | Instruments first: missing cells/tests, gate rules, translator installer | full set (no behaviour change) | `no-uninstalled-seam` → PASS (one line in `main.rs` next to `:643`: `busbar_substrate::proto::install_stream_translator_factory(busbar_llm::proto_stream::new_stream_translator)`); adds rules §4.2 (red by design) |
| **PR-1** | `busbar-substrate::teller` tokens, steps, `run_unit`; `run_gauntlet` re-expressed over it | full set | `token-sealed` → PASS; `teller-step-order` → PASS |
| **PR-2** | `engine/attempt.rs` (new) + `engine/exhaustion.rs`; degraded paths call `attempt()`; delete `walk.rs`; `walk_vs_pipeline_attempt_identity` (attempt vs `forward_once` vs `_inner` loop body) | `route.failover` (all 14, esp. `fb\|member-down`, `fb\|all-down`, `fo\|all-down`, `fo\|all-down-stream`), `billing\|key-usage\|after-upstream-down`, `llm\|*\|*\|request\|upstream_down` | `duplicate-dispatch` → 0 (retire); `request-path-fn-size` worst drops 686 → still FAIL on 2103 |
| **PR-3** | `engine/route/*` (new Route internals over `attempt()`), exposed as `engine::route::forward_teller(...)` with `forward_with_pool_parsed`'s exact signature; identity test extended to whole-forward level; not wired | none touched (unwired); identity + unit tests only | `request-path-fn-size` unchanged (twin A still present) |
| **PR-4** | `busbar-llm/src/unit/*` (nine step files) + `TellerPlane` impl + feature `teller-waist` + composition-root tables + CI matrix | ALL, on both matrix legs: `llm.wire` 126, `llm.stream-cut`, `route.failover`, `billing`, `hooks`, `ops.scrape`, `http.crosscut`, `admin.ops`, `boot.*` | `terminal-doors-in-audit-step` → PASS; `no-response-escapes-audit` → PASS |
| **PR-5** | default `teller-waist = on`; matrix leg `legacy` kept one cycle | ALL | — |
| **PR-6** | delete twin A + `NativePlane` + feature + adapter; `qa/construction.toml`: `allowed_function = "attempt"`, `files +=` the new files, retire `duplicate-dispatch` | ALL | `one-attempt-seam` → PASS; `request-path-fn-size` → PASS (every request-path fn ≤ 200) |
| **PR-7** (Phase 2.3/2.4) | mcp/voice `GauntletPlane` impls (`crates/busbar-mcp/src/mcp/method.rs:1195`, `crates/busbar-voice/src/topology/mod.rs:227`) onto `TellerPlane`; a2a (`crates/busbar-a2a/src/a2a/receive.rs:768`) LAST, after the agents editing `busbar-a2a` land | mcp/a2a conformance rigs; `capability_equality.rs`, `plane_isomorphism.rs`, `plane_meter_seam_reachability.rs` (`crates/busbar/tests/`) | delete `run_gauntlet` adapter |

Coexistence detail (PR-4/5): the two paths share every part in §1.4 and differ only in the shell; `finish_inner` remains the single terminal for both; `next_request_id` is called exactly once per routed unit on both paths (legacy at `pipeline.rs:252`, Teller in `unit/route.rs`) so request-id sequences are identical run-to-run.

Deletion points: `forward_once` dies in PR-2; `forward_with_pool_parsed(_inner)`, `NativePlane`, `run`, `operation_resolved` die in PR-6; `run_gauntlet`/`admit_open` die in PR-7.

## 3. Behaviour hazards per PR → oracle family / binding / cell

Legend: **cell** = `testing/shadow-oracle/cells.json` id; **test** = existing Rust test named in `qa/DESIGN-BINDINGS.md`; **NEW** = must be added in PR-0 because no cell/test exists today.

### PR-0 — add before anything moves

- **PB-46 (unmapped)**: NEW test `migrated_request_hook_seats_after_admit_before_candidate` (`crates/busbar-llm/src/engine/tests/hook_seat_order_tests.rs`, new file): with a rewrite gate + a candidate tap configured, assert the order admission_door → rewrite → taps → gates → candidate tap, and that a gate reject refunds `billable_requests` but keeps `requests` (PB-6).
- **PB-8 conflict**: NEW test `route_deadline_503_carries_detail_request_timeout` asserting `DETAIL_REQUEST_TIMEOUT` (`engine/wire.rs:711`) on the pre-attempt deadline check, and that the `pick_among` `None` path lands on `on_exhausted` (`KIND_OVERLOADED` + `Retry-After`) — the ledger says no test asserts the literal today.
- **PB-25 (unmapped)**: NEW test `stream_without_usage_frame_bills_zero_on_every_dialect` (six dialects × recorded stream with no usage frame → `meter_ledger` never called, `meter_series` once with `None`).
- **PB-29 (unmapped)**: NEW test pinning the four 503 literals (`pipeline.rs:1296`, `:1365`, `:1483`, `:1594`) by gate vs base policy.
- **PB-13/15/17/41 (unmapped, no cell)**: NEW oracle cells `boot.warning|data_dir-unset|corpus-<n>` (warning count equals golden on every `tests/migration-corpus/` config) and `ops.scrape|metrics|no-ledger-series` (no series matching `busbar_(ledger|journal|hold|wal)_` on a 1.5.5 config); NEW test `no_ledger_series_and_no_keyset_lines_without_data_dir` in `crates/busbar/tests/`. These are cheap now (nothing exists) and become the tripwire for Phase 3.
- **PB-84 conflict** (`completion_tap_fires_synthetic_rejected_by_auth` asserts a tap on an auth denial; the binding says pre-forward refusals never tap): owner decision required before PR-4, because the Audit step must know whether a pre-Route refusal fires the completion tap. Recommendation: keep the code (tap fires; oracle `hooks|hooked-pool|unauth` pins it) and amend the binding.
- Installer for `install_stream_translator_factory`: zero wire effect (both engine sites name `crate::proto_stream::new_stream_translator` directly, `pipeline.rs:2798`, `walk.rs:1003`); cell `llm|*|*|request|ok_stream` (cross-dialect) proves nothing changed.

### PR-1 — tokens + loop

- H-1 `run_gauntlet` order: Verify strictly before Admit/drive. Cells `llm|*|*|request|out_of_scope` (pool ACL 403 charges nothing), `billing|key-usage|noscope-after-403`; tests `test_governance_pool_acl_403_*`, `test_fallback_pool_acl_denies_key_not_allowed_on_fallback_target` (PB-26).
- H-2 voice `run_gauntlet_session` (`plane_host/mod.rs:284`) keeps returning `Admitted` without driving: `crates/busbar-voice` topology tests; `gauntlet_session_tests.rs`.

### PR-2 — the attempt seam on the degraded path

The twins are not identical. Every difference below is either preserved by an `AttemptInput` field/caller decision or must be registered in `testing/shadow-oracle/accepted-differences.json` as `improvement` with owner sign-off before merge:

| # | difference (`_inner` vs `forward_once`) | treatment | catching cell / test |
|---|---|---|---|
| D1 | non-2xx TransientUpstream: hot records per class with `retry_after` and fails over (`pipeline.rs:2471-2521`); degraded records `ERR_DEGRADED_NON2XX` without `retry_after` and relays (`walk.rs:837-851`, `:895-911`) | breaker recording unified in `attempt/classify.rs` (hot rule); failover-vs-relay is the caller's map of `AttemptOutcome::Failed` | `route.failover|fb|member-down` (effects.metrics, weight 1), `test_forward_once_fallback_5xx_fault_trips_and_releases_probe`, PB-80 `route.failover|fo|primary-429` |
| D2 | HardDown on degraded path is never recorded (`record_hard_down_all_cells` only at `pipeline.rs:2556`) | unify (record) — `improvement` (breaker correctness) | `test_record_hard_down_all_cells_trips_default_and_every_pool` (PB-83); NEW cell `route.failover|fb|member-401` |
| D3 | `record_client_fault(i)` only on hot path (`pipeline.rs:2428`) | unify | `test_forward_once_fallback_client_4xx_does_not_trip_breaker` |
| D4 | `stream_options.include_usage` injection only on hot path (`pipeline.rs:1974-1987`) — a fallback stream to an OpenAI-chat lane bills zero tokens today | unify — money-affecting; register `improvement` ONLY with owner sign-off (bar: "billing figures identical" vs "maximum spec compliance") | `billing|key-usage|after-*` after a fallback stream — NEW cell `route.failover|fb|member-down-stream-openai` (effects.usage, weight 10) |
| D5 | attempt-timeout member override (`effective_attempt_timeout_ms`, `usage.rs:191`) only on hot path | preserve via `cands` in `AttemptInput` (degraded callers pass the fallback pool's cands) — the override becomes honoured on fallback: `improvement` | `test_attempt_cap_budget_floor`, `attempt_timeout_precedence_tests.rs` |
| D6 | `wants_stream`: `op.wants_stream(probe)` vs raw `v.get("stream")`; `gemini_json_array` gated on `op.streaming()` only on hot path | preserve hot-path rule (identical for chat; non-chat ops never reached the degraded path with `stream:true` in any cell) | `llm|gemini|*|request|ok` (no `ok_stream` for gemini ingress in cells — NEW cell `llm|gemini|gemini|request|ok_stream_array`) |
| D7 | huge-body `spawn_blocking` offload only on hot path | preserve (perf only) | `translate_offload_tests.rs` |
| D8 | `metric_pool_label` vs raw `pool` for telemetry; `maybe_attach_route_policy` header only on hot path | preserve via `metric_pool` and `chosen_policy_name` inputs | `ops.scrape|metrics|key`, `route_policy_headers_*` tests (PB-73) |
| D9 | transport-error telemetry (`telemetry_upstream_failure/failover`) only on hot path (`pipeline.rs:2287-2288`) | unify — `improvement` (metrics only) | `route.failover|fb|member-down` effects.metrics |

Also for PR-2: probe ownership (`ProbeGuard` armed only when `probe_epoch.is_some()`, disarmed at `record_success_in`) — `test_forward_once_fallback_2xx_closes_pool_cell_not_default`, `test_forward_once_fallback_transport_error_opens_pool_cell`, `probe_guard_tests.rs`, `probe_release_owner_tests.rs`; least-bad passes `None` (`walk.rs:1352`). Budget refunds PB-16/27/91: `BudgetSpendGuard` disarm points (`pipeline.rs:2825`, `walk.rs:1013`) and `translate_response_cross_protocol`'s cap arm — `test_forward_once_untranslatable_2xx_refunds_budget_and_trips_breaker`, `test_truncated_body_does_not_refund_budget`, cells `route.failover|fo|primary-cut-body`, `|fo|primary-cut-stream`. Queue mode `handle_queue` (`walk.rs:197`) deadline: `queue_times_out_to_503_when_capacity_never_frees`, `queue_won_permit_but_breaker_now_open_never_dispatches` (PB-2/4).

### PR-3 — Route internals (unwired; identity test is the gate)

- PB-5/57 pick order: `affinity_key_hash` derived before any mutation of `v` (`pipeline.rs:1123`); `policy_order` computed once before the loop; exclusions removed from `cands` not seeded into `request_ctx.excluded` (`:1174`). Tests `sticky_affinity_never_selects_zero_weight_drained_member`, `order_last_in_chain_wins`, `stale_order_filtered_against_post_restrict_set`, `test_zero_weight_member_is_never_selected`, `a_saturated_primary_is_passed_over_inside_the_one_loop_and_the_twin_serves`; cell `route.failover|fo|all-up`, `|lb|up`.
- PB-6/46/84/95 seats: rewrite → IR → request taps → gates → base policy → candidate tap → per-attempt routing tap, all after Admit. Tests `global_gate_reject_short_circuits_the_request`, `global_request_stage_tap_fires_on_a_real_dispatched_request`, `attempt_tap_carries_attempt_story`, `route_tap_reports_surviving_candidates`, `substrate_fire_stage_taps_honors_group_scope_via_host_seam`, PR-0's seat-order test; cells `hooks|hooked-pool|ok`, `ok_stream`.
- PB-8: `max_hops + 1` (`for attempt in 0..=max_cap`), deadline checked before every attempt incl. streaming; `test_saturated_lane_respects_deadline_no_infinite_spin`, PR-0's `DETAIL_REQUEST_TIMEOUT` test; cells `route.failover|fo|primary-slow`, `|fo|primary-down`.
- Context-length exclusion rule (`pipeline.rs:2631-2645`): `test_context_length_failover_no_penalty`, `test_prefers_larger_context_max`.
- Gate/policy 503 literals (PB-29 test from PR-0).

### PR-4 — the nine steps wired behind the feature

- H-3 **charge point (PB-22/26)**: candidate resolution and the model-miss 404 stay in `unit/route.rs` AFTER `admission_door` (as `native_ingress.rs:256-287` today) and finish through `finish_admitted(charged)`; Verify is `destination_guard` only. Moving resolution into Verify would turn a charged 404 into an uncharged one and change `requests` counts. Tests `test_pre_routing_failure_does_not_refund_prior_charge`, `finish_admitted_does_not_refund_an_uncharged_admit`, `test_unpriced_passthrough_model_rejected_when_rate_card_present`; cells `llm|*|*|request|over_budget`, `out_of_scope`, `http.crosscut|unknown-path|openai-suffix`, `billing|key-usage|fresh/after-1/after-3`.
- H-4 **pre-route refusals** (parse 400, non-object 400, missing model 400, 404 handler, 404 op) route through `finish_rejected` with `POOL_LABEL_UNRESOLVED` (PB-99): cells `llm|*|*|request|malformed`, `ops.scrape|metrics|key` (`busbar_requests_total{pool="unresolved"}`), tests in `reqlog_dispatch_tests.rs`, `ingress_reject_response_tests.rs`.
- H-5 **`charged_at` and `started`** captured once at Arrival (`native_ingress.rs:49-50`, `:509-511`) and threaded — window identity for fee + tokens: `test_metering_accumulates_split_per_key_model_and_bucket`, cells `billing|*` (effects.usage weight 10), `admin.ops|GetAudit|ok`.
- H-6 **request id** minted once at Route entry, never for refusals before Route; `hooks|hooked-pool|*` (join key in tap payload), `usage_tap_tests.rs`.
- H-7 **completion tap outcome** (`rejected_by_gate` marker via `GateRejected` extension, `pipeline.rs:309`): `completion_tap_fires_synthetic_rejected_by_gate`, `completion_tap_reports_ok_outcome`; plus the PB-84 owner decision from PR-0.
- H-8 **`synthesize_completion`** (`native_ingress.rs:668`, the MCP sampling re-entry) must go through the same `unit::run` — mcp conformance rig `sampling/complete` rows; `CompletionHost::synthesize_completion` tests.
- H-9 **path-model arrivals** (gemini/bedrock: `arrival.rs:78`, `:346`) hand `LazyBody::from_value` eagerly; the pristine head check (`head_provably_pristine`) must see the same DOM: `head_pristine_matches_translate_output`, `pristine_same_proto_is_byte_identical_url_model` (PB-97); cells `llm|gemini|*`, `llm|bedrock|*`.
- H-10 neutrality: no new INFO boot line, series, config key (PB-17/40/41/70): `config-stability-gate.sh`, PR-0's neutrality cells, `boot.warning` family.
- H-11 no `Response` escapes the loop except via Audit: gate rule `no-response-escapes-audit` (§4.2) + the type signature (`Decision<Route>` carries the response).

### PR-5/6 — flip and delete

- The oracle full set on the flipped default; after PR-6 the legacy leg is gone, so PR-6 must also carry the diff of the last legacy recording vs the Teller recording as an artifact (zero rows).
- Deleting `walk.rs`/`pipeline.rs` changes `engine_never_branches_on_operation_identity` (`engine_identity_witness_tests.rs:17`, an `include_str!` witness) — update its file list to the new files in the same PR.
- `ports-only-tests:busbar-llm` ceiling: every new test file adds zero `busbar_core::` lines (use `crate::test_support` and `crate::engine::test_host_rt`, `tables.rs:376`).

## 4. Tests and gate rules the rebuild adds

### 4.1 `walk_vs_pipeline_attempt_identity` (PR-2, extended PR-3)

File: `crates/busbar-llm/src/engine/tests/attempt_identity_tests.rs` (new; registered from `engine/attempt.rs` via `#[cfg(test)] #[path = "tests/attempt_identity_tests.rs"]` so no edit to `engine/mod.rs`'s test list).

Shape: a table-driven harness over `crate::test_support::{MockServer, MockResponse, TestApp}` — the fixtures `forward_once_pool_cell_tests.rs` already uses. For each scripted upstream (2xx json same-proto, 2xx SSE same-proto, 2xx json cross-proto, 2xx SSE cross-proto, 401 passthrough, 400 client-fault, 429 with `Retry-After`, 503, billing 402, context-length 400, connect refused, headers-timeout, body-cut-after-first-byte, untranslatable 2xx, over-cap 2xx) and for each of the six ingress dialects:

1. Build three identical `TestApp`s (own lane store each).
2. Drive (a) `attempt()` directly with `Posture`-equivalent inputs, (b) the legacy `forward_once` (PR-2 only, until deleted), (c) the legacy `forward_with_pool_parsed_inner` single-hop (PR-3: (c) becomes `forward_with_pool_parsed` vs `route::forward_teller` whole-forward).
3. Assert equality of: client status + headers (minus `date`) + full body bytes (streams drained); `store.breaker_state_in(pool, lane)`, `cooldown_remaining_in`, `probe_in_flight`, `budget_remaining(lane)`; the recorded `meter_ledger`/`meter_series` calls (a capturing `EngineHost` wrapper, `test_support`); `telemetry_*` call list; `next_request_id` call count.
4. PR-2's expected-difference list (§3 D1–D9) is encoded as an explicit `Divergence` allow-table keyed by (fixture, field) with the `accepted-differences.json` id — any unlisted divergence fails; any listed divergence that stops occurring also fails (so the table cannot go stale).

Registered as `attempt-identity` in `scripts/verify-1.6.0-done.sh` (after the DESIGN group) and as a required check from PR-2.

### 4.2 New `qa/construction.toml` rules (implemented in `scripts/construction-gate/rules.py`, each with a `plant.py` self-test case)

| rule | invariant | PR it turns green |
|---|---|---|
| `token-sealed` | in non-test code, `Decision::proceed(`/`Decision::refuse(`/`Hold::`/`Posted(`/`UnitToken::mint(` appear only under `crates/busbar-substrate/src/teller/` | PR-1 |
| `teller-step-order` | `teller/run.rs` contains the nine step calls in the canonical order (ordered regex over the fn body) and exactly one `fn run_unit` | PR-1 |
| `one-teller-loop` | exactly one production caller of `run_unit(` outside `teller/` per plane crate (llm from PR-4; mcp/a2a/voice from PR-7) and `run_gauntlet(` call sites → 0 after PR-7 | PR-4 / PR-7 |
| `no-response-escapes-audit` | in `crates/busbar-llm/src/unit/`, functions returning `Response`/`axum::response::Response` exist only in `audit.rs` | PR-4 |
| `terminal-doors-in-audit-step` | `finish_admitted(`/`finish_rejected(` call sites in busbar-llm non-test code only in `unit/audit.rs` | PR-4 |
| `one-pick-site` | `pick_among(` production call sites ≤ 2 (`route/mod.rs` loop + `exhaustion/fallback.rs` re-entry) | PR-3 |
| existing `one-attempt-seam` | `allowed_function = "attempt"` (temporarily 2 sites during PR-2..6 — set `max_extra_sites = 1` in PR-2 with the reason in the commit, back to 0 in PR-6) | PR-6 |
| existing `request-path-fn-size` | `files +=` `crates/busbar-llm/src/unit/*.rs`, `engine/attempt/*.rs`, `engine/route/*.rs`, `engine/exhaustion/*.rs`, `crates/busbar-substrate/src/teller/*.rs` (globs — `rules.py` currently takes exact paths; extend) | PR-6 |
| existing `duplicate-dispatch` | retire in PR-2 | PR-2 |

Design-bindings ledger: PR-0 adds the new tests/cells to `qa/design-bindings.json` for PB-8, PB-13, PB-15, PB-17, PB-25, PB-29, PB-41, PB-46 (unmapped count 25 → 17).

## 5. Size, dependencies, parallelism

| PR | files | lines (+/−) | depends on | parallel with |
|---|---|---|---|---|
| PR-0 | ~9 (5 new test files, `cells.json`, `construction.toml`, `rules.py`, `plant.py`, `main.rs` 1 line, `design-bindings.json`) | +900 / −0 | — | everything |
| PR-1 | 5 new in `busbar-substrate/src/teller/`, `plane_host/mod.rs`, `lib.rs`, 1 new test file | +750 / −30 | — | PR-2 |
| PR-2 | `engine/attempt/*.rs` (5 new), `engine/exhaustion/*.rs` (4 new), `engine/mod.rs` (mod lines), delete `walk.rs`, identity test, `accepted-differences.json`, `construction.toml` | +1,900 / −1,370 | PR-0 (cells D2/D4/D6) | PR-1 |
| PR-3 | `engine/route/*.rs` (6 new), identity test extension | +1,100 / −0 | PR-2 | PR-1 if not yet merged |
| PR-4 | `busbar-llm/src/unit/*.rs` (10 new), `lib.rs`, `Cargo.toml` ×2, `main.rs`, `ci.yml` matrix, new `unit/tests/*.rs` | +1,300 / −20 | PR-1, PR-3, PB-84 decision | — |
| PR-5 | `Cargo.toml` ×2, `ci.yml` | +5 / −5 | PR-4 + one full green oracle cycle on both legs | — |
| PR-6 | delete `pipeline.rs` bulk, `native_ingress.rs` bulk, adapter; `construction.toml`; witness test list | +80 / −4,600 | PR-5 | — |
| PR-7 | mcp, voice, then a2a; delete `run_gauntlet` | +400 / −150 | PR-6; a2a after the current busbar-a2a work lands | mcp ∥ voice |

Critical path: PR-0 → PR-2 → PR-3 → PR-4 → PR-5 → PR-6 (PR-1 rides alongside PR-2/3). Estimated 6–8 working days for PR-0..6 with the oracle in CI; PR-7 2–3 days.

Coordination: every test this plan adds is a NEW file registered from a NEW module (`engine/attempt.rs`, `unit/mod.rs`, `teller/mod.rs`), so no PR here edits `engine/mod.rs`'s existing `#[path]` list or any existing `tests/*.rs`.
