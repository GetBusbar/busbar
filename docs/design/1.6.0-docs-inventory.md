<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- Copyright (C) 2026 Busbar Inc and contributors -->

# 1.6.0 DOCS INVENTORY — what steers, what lies, and what nothing reads

**Measured 2026-09-22, branch `consolidated/1.6.0`.** This document is an INVENTORY and a VERDICT.
**Nothing was deleted, and nothing outside this file was edited.** The "safe to delete" section at
the end is a RECOMMENDATION with its proof attached; acting on it is a separate, owner-gated pass.

**Scope and count, measured at write time:** `docs/**/*.md` = **196**, repo-root `*.md` = **7**,
**203 in scope**. There is no top-level `playbook/`; the playbook is `docs/design/playbook/` (**38**
files) and is counted inside `docs/`. Sub-counts: `docs/design/*.md` **90**, `docs/design/inventory/`
**8**, `docs/*.md` (public) **34**, `docs/security/advisories/` **8**, `docs/ci/` **5**,
`docs/advisories/1.6.0/` **5**, `docs/adr/` **5**, `docs/security/` **2**, `docs/proof/` **1**.
The count MOVED during this pass — it was 198 at the start and 203 at the end — because several
agents are writing new `docs/design/1.6.0-*.md` files concurrently. Treat the totals as a
measurement with a timestamp, not a constant.

> **Note on quoted vocabulary.** This file quotes banned deferral words ("defer", "1.7.x",
> "DEFERRED", "out of scope") as EVIDENCE of what stale documents say. They are citations of a
> defect, never this document's own posture.

---

## THE GOVERNING RULE, restated from the source

The spec is ONE file: **`docs/design/BUSBAR-1.6.0.md`**. Row #14 says so in its own words
(`:331`): the five predecessors — `VISION-1.6.0.md`, `1.6.0-plane-extraction-LOCKED.md`,
`DECISIONS.md`, `1.6.0-PLAN.md`, `1.6.0-plane-abi-taxonomy.md` — were absorbed VERBATIM into Parts
1/3/2/5/4 and DELETED in `49ab4aca2`. Authority inside it: **Part 1 (Laws) > Part 3 (plane seam) >
Part 2 (the locked decisions) > Part 5 (waves)**.

### Ground truth this inventory was measured against (each re-verified, not taken on trust)

| Fact | Measured value | Where measured |
|---|---|---|
| Locked decisions in Part 2 | **85**, ids 1–85 contiguous | `BUSBAR-1.6.0.md:305` heading + row count |
| Plane roster | **5** — `llm`, `mcp`, `a2a`, **`streaming`**, `decision(s)`. Voice is a DIALECT inside streaming | #18 / #48; `scripts/verify-1.6.0-done.sh:488-500` |
| Plane crates on disk | exactly the 5: `busbar-plane-{llm,mcp,a2a,streaming,decision}`. **`busbar-plane-voice` does NOT exist** | `ls crates/busbar-plane-*` |
| Plugin kinds | **7** — store, secret, auth, hook, export, plane, transport. `auth` is ONE kind; `control`/`dialect`/`unit` are NOT kinds | #3 / #4 / #5 |
| Kinds the loader actually accepts today | **6** (`store`, `secret`, `auth`, `hook`, `export`, `plane`) — `transport` has no ABI-floor row yet | `crates/plugin-loader/src/registry.rs:53-89`, `:458` |
| D36 / D37 | **REJECTED — do not resurrect** | `BUSBAR-1.6.0.md:70-71` ("Rejected, do not resurrect") |
| Trunk | **`consolidated/1.6.0`** | `BUSBAR-1.6.0.md:1184` |
| Branch roster (#67) | `predev` (permanent WIP) / `dev` (release-train-write-only) / `qa` / `main`; **"Never push `dev`, `qa` or `main` by hand"** | `BUSBAR-1.6.0.md:1025-1036`, #67 |
| Done-oracle groups | **23** defined, **23** declared | `grep -cE '^begin_group "' scripts/verify-1.6.0-done.sh`; `DONE_GROUPS_DECLARED=23` |
| Crates in the workspace | **49** under `crates/` | `Cargo.toml` `[workspace] members` |
| Money | LEDGER + RATECARD, two systems; `money = f(ledger, ratecard)` derived at READ time; nothing stores a price; unitless, NO currency/symbol (#66); a mid-stream cut BILLS what streamed (#62); cards are a DATED HISTORY (#79); counts are EXACT fixed-point DECIMAL at scale 6, not integers (#81) | Part 0 + #62/#66/#79/#81 |
| CI | **Latchkey**, not EC2. The EC2 fleet is retired and torn down (#56 beats #29) | `BUSBAR-1.6.0.md:1050-1056` |

### Three numbers in the brief that this pass corrects

1. **"78 locked decisions" → 85.** Part 2's heading reads `# PART 2 — THE 85 LOCKED DECISIONS` and
   the table carries ids 1–85 with no gaps.
2. **"24 done-oracle groups" → 23.** The 24 is an off-by-one from `grep -c '^begin_group'` WITHOUT
   the trailing space, which also counts line 93, the function *definition*
   (`begin_group() { CUR_GROUP="$1"; ... }`). The script's own count is
   `awk '/^begin_group /{n++}'` = 23, matching `DONE_GROUPS_DECLARED=23`. This was independently
   re-derived this session in `docs/design/1.6.0-done-readout.md:28-42`.
3. **"~15 dead references" is not the repo-wide figure.** That count belongs to the specific source
   `1.6.0-map-proof.md` re-measures (its §S10). Repo-wide, backtick-quoted path citations that do
   not resolve number **538 distinct, across 103 of the 203 files**. See §4.

---

## 1. THE READERS, ENUMERATED FIRST — what actually opens a document

This was derived from the gates, scripts, workflows and crates themselves, before any document was
judged. A *citation* in a comment is not a reader; only a file that is **opened** (or whose bytes
are asserted) is listed as GATE-READ.

### 1a. Files a gate, test or build OPENS — touching these breaks something

| Document | The reader, exactly | What happens if it moves |
|---|---|---|
| `docs/design/ARCHITECTURE.md` | `xtask/src/gates/design_bindings.rs:88` `cx.read(ARCH_REL)`; const at `design_bindings/build.rs:22`. Appendix B is parsed into `qa/design-bindings.json` + `qa/DESIGN-BINDINGS.md` | `design-bindings` gate RED; also the REGEN-CLEAN check at `.github/workflows/ci.yml:3488` |
| `docs/design/inventory/1.5.5-*.md` (8) | `xtask/src/gates/inventory_coverage.rs` walks the directory; `inventory_ref.rs:60-78` names all 8 by path; `scripts/documented-claims-check.py` reads the ops file | `inventory-coverage` + `inventory-ref` gates RED; done-oracle group INVENTORY-COVERAGE RED |
| `docs/design/1.5.5-BEHAVIOUR.md` | `inventory_coverage.rs:57` `BEHAVIOUR_MD` — **read AND rewritten** (the 40-row matrix between markers) | gate RED, and a hand-edit inside the markers is overwritten |
| `docs/design/BUSBAR-1.6.0.md` | `xtask/src/gates/plane_abi_neutrality.rs:49` `TAXONOMY_DOC`, read at `:258`; the gate parses its `banned set …` line | `plane-abi-neutrality` RED: *"the mandate document is missing at …"* |
| `docs/design/1.6.0-TRACKER.md` | `xtask/src/gates/no_deferral.rs:60` `TRACKER`, read at `:310`; every waiver must name a TRACKER row as its expiry | `no-deferral` RED: *"the tracker … could not be read, so no waiver's expiry can be checked"* |
| `docs/design/AUDIT-STATUS.md` | **GENERATED** by `cargo xtask ledger status` — `xtask/src/audit.rs:50` `REPORT_REL`; consumed by `scripts/verify-1.6.0-done.sh:758` | regenerated; hand-edits are lost |
| `CHANGELOG.md` | `xtask/src/gates/changelog.rs:102`, `changelog_register.rs:80/429/849`; `.github/scripts/roll_changelog.py`; `release-stage.yml:663` | `changelog` + `changelog-register` gates RED; release notes empty |
| `README.md` | `xtask/src/gates/conformance_sync/render.rs:39` `README_PATH`; the badge span between `BADGE_BEGIN`/`BADGE_END` is generated and drift-checked | `conformance-sync` RED (fatal drift gate) |
| `docs/admin-api.md` | **COMPILE-TIME**: `include_str!("../../../../docs/admin-api.md")` at `crates/busbar-core-admin/src/tests/tests.rs:12969` and `:13035` | **the build breaks** |
| `docs/diagnostics.md` + `docs/diagnostics.json` | `crates/busbar-substrate-values/src/diagnostics/mod.rs:3857-3860`; asserted byte-equal to a fresh render at `diagnostics/tests.rs:133-168` (*"docs/diagnostics.md is stale — regenerate with UPDATE_DIAGNOSTICS=1"*) | test failure |
| `docs/diagnostics-a2a.md` | `crates/busbar-a2a/src/tests/diagnostics_tests.rs:12` `COMMITTED_MD` | test failure |
| `docs/diagnostics-mcp.md` | `crates/busbar-mcp/src/tests/diagnostics_tests.rs:12` `COMMITTED_MD` | test failure |
| `docs/getting-started.md` | its minimal `config.yaml` quickstart is **EXECUTED**: `.github/workflows/verify-deploy.yml:1681-1791`, `scripts/verify-artifact.py:358`, `scripts/release-check.sh:1217`, `scripts/release-gate/docker-checks.sh:321` | release verification RED: *"the quickstart printed in the README and docs/getting-started.md does NOT work"* |
| **every `.md` under `docs/` EXCEPT `docs/design/`, plus root `*.md`** | `scripts/public-hygiene-lint.py` — `SKIP_DESIGN = (^\|/)docs/design/` is the ONLY doc exclusion; wired at `scripts/verify-1.6.0-done.sh:942-944`, group PUBLIC-HYGIENE | done-oracle group PUBLIC-HYGIENE RED on content, not on absence |

Also gate-read but outside this inventory's scope: `qa/DESIGN-BINDINGS.md` (generated,
`design_bindings/build.rs:25`), `qa/WAIVERS.md` (`scripts/pin-missing-cells.py:39`),
`qa/reachability-evidence.md` (`xtask/src/gates/reachability.rs:127`).

### 1b. Named in a gate ARTIFACT or in shipped source — not opened, but not free either

Deleting one of these leaves a gate ledger or a shipped comment citing a file that is not there.

| Document | Named by |
|---|---|
| `docs/design/playbook/00-MASTER-PLAN.md` | `scripts/verify-1.6.0-done.sh:6` — cited as the source of *"DONE = scripts/verify-1.6.0-done.sh green"* |
| `docs/design/playbook/gate-isomorphism.md` | `crates/busbar/tests/plane_isomorphism.rs:5,29`; `scripts/plane-config-noun-gate.sh:8`; **`qa/plane-hook-isomorphism.allow:5`** (an allowlist row) |
| `docs/design/playbook/gate-no-deferral.md` | `scripts/no-deferral.waivers:10`; `crates/busbar/tests/plane_isomorphism.rs:29` |
| `docs/design/playbook/neutrality-findings-and-prevention.md` | `crates/busbar/tests/plane_host_universal_purity.rs:5` |
| `docs/design/plane4-duplex-session.md` | ~10 sites in `crates/busbar-voice/` and `crates/busbar-voice-codec/` |
| `docs/design/plane4-voice-dialect-landscape.md` | `crates/busbar-plane-streaming/src/twilio.rs` |
| `docs/design/PLUGIN-TREE.md` | `qa/design-bindings.json`; `crates/busbar-transport-sse/src/{claims,meta}.rs`; the kind-isolation gate quotes its §3/§4 in refusal text |
| `docs/design/1.6.0-contract-gaps.md` | `qa/design-bindings.json`; `xtask/src/denylist.rs:2,86` (CG-59/CG-60 are the gate's stated reason to exist) |
| `docs/design/rate-card-history.md` | `qa/design-bindings.json`; `testing/shadow-oracle/accepted-differences.json`; `crates/busbar/src/root/units_admin/mod.rs` |
| `docs/design/xtask-gates.md` | `xtask/src/main.rs:2`; `gates/plane_purity/freeze.rs:2`; `gates/structure_lint/translate.rs:25,81` |
| `docs/design/store-qa-cycle.md` | `scripts/verify-1.6.0-done.sh:952`; `testing/fleet-fixtures/*` |
| `docs/design/plane-extraction-design.md` | `scripts/plane-delete-test.sh:7`; `crates/busbar-llm/Cargo.toml:86` |
| `docs/design/1.6.0-proof-dashboard.md` | `scripts/proof-manifest.py:5`; `.github/workflows/ci.yml:2769` |
| `docs/design/1.6.0-secret-hygiene.md` | `scripts/secret-hygiene-gate.sh:7`; `qa/segments.toml`; `ci.yml:1429` |
| `docs/design/1.6.0-security-posture.md` | `scripts/proto-deletion-gate.sh:109,269` |
| `docs/design/1.6.0-engine-split-plan.md` | `qa/construction.toml`; `crates/busbar-a2a/src/a2a/meter.rs` |
| `docs/design/1.6.0-duplex-plane-and-realtime.md` | `scripts/no-deferral.waivers` |
| `docs/design/contract-notes.md` | `crates/busbar-contract/src/{kinds,lib,plane}.rs` |
| `docs/design/billing-unified.md`, `billing-usage-units.md` | `crates/busbar-llm-codec/src/{gemini/mod.rs,ir/types.rs,gemini/tests/usage_identity_tests.rs}` |
| `docs/code-layout.md`, `docs/testing.md`, `docs/observability.md`, `docs/plugins.md`, `docs/ci/actions-pins.md`, `docs/ci/self-hosted-runners.md`, `docs/ci/gate-integrity.md` | structure-lint / tracing / release-order / plugin-loader / CI-script comments cite them as the rule they encode |

### 1c. Corrections to the assumed keep-set

The stated keep-set was directionally right but **both over- and under-inclusive**. Verified:

* **`docs/adr/**`, `docs/advisories/**`, `docs/security/**` are NOT gate-read.** The only hits are a
  doc-comment citation (`crates/busbar-llm-codec/src/ir/mod.rs:6` → `adr/0005-ir-fidelity.md`) and
  `scripts/history-rewrite-empty-bodies.sh:37`, which names `docs/security/rewrite-bodies.txt` —
  a `.txt`, not a `.md`. They are content-scanned by `public-hygiene-lint` like the rest of `docs/`
  outside `design/`, and that is all.
* **`docs/ci/**` is not read either** — and `public-hygiene-lint.py:174` actually *exempts*
  `docs/ci/` from one of its rules, so it is less gated than its neighbours.
* **`docs/diagnostics.*` — correct, and stronger than stated.** All three (`diagnostics.md`,
  `-a2a`, `-mcp`) are asserted byte-equal to a live render by unit tests.
* **MISSING from the keep-set, and each is genuinely gate-read:** `docs/design/BUSBAR-1.6.0.md`,
  **`docs/design/1.6.0-TRACKER.md`**, `docs/design/1.5.5-BEHAVIOUR.md`, `docs/admin-api.md`
  (compile-time), `docs/getting-started.md` (executed by CI).
* `docs/roadmap.md` has **no reader at all** beyond the hygiene scan; the 21 apparent references
  are comments inside `tests/migration-corpus/*/providers.yaml`, i.e. frozen historical fixtures.

---

## 2. STALE-DANGEROUS — the bucket that matters, most dangerous first

Every claim below was read in the file and quoted verbatim, with its line. "STEERING" marks a
document that opens by telling a reader what to DO; those are the ones that turn a stale sentence
into a commit.

### T1. `docs/design/playbook/00-MASTER-PLAN.md` — STEERING, and the done-script cites it

The single most dangerous file in the tree, because `scripts/verify-1.6.0-done.sh:6` names it as
the source of *"DONE = scripts/verify-1.6.0-done.sh green"*. A reader who follows that pointer
lands on this:

* `:3-4` — *"Working branch: `integration/config-seam-stage1-rebased` (on `origin/dev` 7004d8a7).
  Dev-only pushes."* Trunk is `consolidated/1.6.0`; #67 makes `predev` the WIP branch and Part 6
  says **"Never push `dev`, `qa` or `main` by hand."** This one line names the wrong branch AND
  instructs the one push the spec forbids.
* `:21` — *"**M5 boot** — Cargo `plane-voice` feature (off-default)…"* — #18 bans `voice` as a
  feature name outright.
* `:40` — *"`plane-voice` must NOT enter `default` (would change config-schema snapshot)."* It is
  in `default` today (`crates/busbar/Cargo.toml:177`), and #17 says the knob is `plane-streaming`.
* `:26`/`:47` — *"delete-test all four planes"*. The roster is 5, and
  `scripts/verify-1.6.0-done.sh:488` refuses that exact set by name.
* `:38` — names `plane-noun-gate` / `plane-grep-gate`; `:8` item names `no-deferral-gate.sh`.
  **None of those three scripts exists.**

### T2. `docs/design/1.6.0-TRACKER.md` — GATE-READ *and* carrying two CANCELLED rows

This file is read by `xtask/src/gates/no_deferral.rs:310` (every waiver must name a row in it as
its expiry) and is cited as the requirement source inside a gate artifact
(`qa/teller-steps.json:3`) and shipped source (`crates/plugin-loader/src/plane.rs:35`). It is
therefore the checklist agents actually tick. It carries:

* `:123` — `- [ ] D36 dialects are plane-internal, not crates and not a kind …` — an **unchecked
  box for work the owner REJECTED** (`BUSBAR-1.6.0.md:70` lists D36 under *"Rejected, do not
  resurrect"*). Its own body then compounds the error: *"codec logic is the per-plane path-dep
  `busbar-<plane>-codec`"* and *"`busbar-{llm,mcp,a2a}-codec` stay per-plane"* — #6/#39 say there
  is **no** separate codec crate.
* `:125` — `- [ ] D37 admin and oauth2 are compiled-in cleanliness crates, NOT a plugin kind …` —
  the second REJECTED row, also unchecked.
* `:173` — `## K. Voice plane: from scaffold to shipped` — names voice as a plane.
* `:97` — lists `busbar-plane-voice, busbar-plane-admin` as landed crates; **neither exists**.
* `:111` — presents `busbar-mcp-codec` / `busbar-a2a-codec` as landed; both are dissolved
  (`qa/construction.toml:135`: *"MCP HAS NO ROW HERE ANY MORE."*).
* `:123`/`:125` cite `DECISIONS #4`, `#5`, `#6`, `#18` — four citations to a **deleted file**.

**This is the measured harm the brief describes.** A measurement that reports D36/D37 as todo is
reading these two lines.

### T3. The voice-plane program — 46 files that all teach a retired architecture

`docs/design/playbook/**` (38) and `docs/design/plane4-*.md` (8) are one corpus, written
2026-09-02/03, and **all 46 are STALE-DANGEROUS**. Their common root is
`plane4-duplex-session.md:1` — *"Plane 4 — The Duplex/Session Plane (authoritative design)"* — and
`plane4-duplex-session-1.6.0-plan.md:3` — *"This is the plan 1.6.0 is built from."* Both are
self-declared authorities that #14 says cannot exist. The most actionable:

| File | The instruction that is wrong now |
|---|---|
| `playbook/voice-arming-blueprint.md:14-16` | *"Voice ships default-ON… `plane-voice = ["dep:busbar-voice", "busbar-voice?/runtime", "busbar-core/plane-voice"]`"*; `:333` `vec!["llm", "mcp", "a2a", "voice"]` — the banned feature name, through a deleted crate, with the banned roster |
| `playbook/m5-voice-boot.md:4,26-40` | *"Booting the voice plane (Plane 4)"* + copy-pasteable `plane-voice = [...]` and `register_planes` wiring |
| `playbook/de-skeleton-plan.md:26,34-45` | a replacement-text table to *polish* `crates/busbar-voice`, a crate #19 **deletes** |
| `playbook/gate-isomorphism.md:4,7` | *"The 4-plane BEHAVIOURAL ISOMORPHISM gate … across `{llm, mcp, a2a, voice}`"* — and this doc is named by `crates/busbar/tests/plane_isomorphism.rs`, `scripts/plane-config-noun-gate.sh:8` and the allowlist `qa/plane-hook-isomorphism.allow:5`. A gate's design doc carrying the exact roster the done-script refuses by name. |
| `playbook/gate-no-deferral.md:39` | names `plane4-duplex-session-1.6.0-plan.md` *"AUTHORITATIVE"* — a second-spec claim #14 forbids; `:161-172` makes *"busbar-voice MUST clear its SKELETON markers"* a build-blocking law for a crate slated for deletion |
| `playbook/PROGRESS-REPORT.md:3,19,106` | *"pushed to **origin/dev**"* (nothing is pushed), *"Voice plane booted (DoD #2)"*, *"Done-oracle readout — 10/11 GREEN"* (the count is 23) |
| `playbook/voice-dod-finish-checklist.md:6-7,90,107` | *"Dev is green at 10/11"*, *"add `plane-voice` to default"*, *"verify-1.6.0-done.sh → 11/11 groups GREEN"* |
| `playbook/t1-media-path.md:39-45` | directs *"100% new code in busbar-voice"* — new code into the doomed crate instead of `busbar-plane-streaming` |
| `playbook/neutrality-findings-and-prevention.md:153,165,217` | three findings marked *"deferred"* / *"DEFERRED"* — banned by #15; and this file is named by `crates/busbar/tests/plane_host_universal_purity.rs:5` |

Wrong done-oracle counts in this corpus: **10/11** and **11/11**. The number is **23**.

### T4. `docs/voice.md` — public, and stale against the LAW while true to the CODE

This is the subtlest and therefore worst case. `:27` *"declares the voice plane's one posture"*,
`:44-46` *"**Voice ships default-on.** `plane-voice` is in the `busbar` binary's default feature
set"*. **That is accurate to the tree today** — `crates/busbar/Cargo.toml:177` reads
`default = [… "plane-voice", "root-admin", "root-mcp", "root-a2a", "root-voice", "root-llm"]`.
But #17 locks the end-state default to
`["auth-admin-tokens","hook-ranking","plane-llm","plane-mcp","plane-a2a","plane-streaming","plane-decision"]`
with **the whole `root-*` family gone**, and #18 bans `voice` as a feature name. So an agent using
this page as the target state will faithfully preserve a name the law deletes. It also carries two
genuinely broken citations: `:64`/`:129` → `crates/busbar-core/src/config/mod.rs:5040-5053` (crate
deleted; the successor file is 2,796 lines, so even the range is impossible) and `:224` →
`crates/busbar-voice/src/topology/twilio.rs` (it is in `busbar-voice-codec`).

### T5. Six "AUTHORITATIVE execution plan" docs whose premise is already gone

| File | The dead premise |
|---|---|
| `1.6.0-llm-plane-abi-purity.md:3,20` | *"Status: AUTHORITATIVE execution plan"* to repoint **1602** `busbar_core::` references. `grep -rn 'busbar_core::' crates --include='*.rs'` returns **1 hit, and it is a comment** (`busbar-kernel/src/lib.rs:26`). The whole plan is moot. |
| `1.6.0-money-path-relocation.md:3,44` | *"AUTHORITATIVE execution plan"*; its gate command is `cargo build -p busbar-core` — **no such package**. |
| `1.6.0-money-path-phase34.md:59` | describes Commit-C as still to run; it landed with busbar-core's deletion. |
| `1.6.0-mcp-engine.md:133,343,354,385,388` | a §7 "ORDERED LANDING PLAN" that routes work into `crates/busbar-mcp-codec/…` — dissolved under #39. Following it **resurrects a banned crate**. |
| `1.6.0-composition-root-plan.md:385,431,685` | *"Order: `admin` → `mcp` → `a2a` → `voice` → `llm`"*, *"matrix is `llm`, `mcp`, `a2a`, `voice`"*, *"the root serves four planes"* — a live cutover-order plan on the retired roster. |
| `1.6.0-technical-changes.md:19,199-200` | the **external, plugin-author-facing** porting guide. It tells third parties to depend on `busbar-mcp` / `busbar-a2a` / `busbar-llm-codec` and never mentions the `busbar-plane-*` crates that already exist. |

### T6. `docs/design/plane-extraction-design.md` — declares 1.6.0 finished, and a gate script cites it

`:3` *"Target: 1.7.x"*; `:327` *"**This is 1.7.x work.** 1.6.0 is complete and
frozen-pending-promotion; nothing here touches it."* 1.6.0 is not complete (6 of 23 groups green),
and #15 bans that framing. `scripts/plane-delete-test.sh:7` cites this file as *"WHY THIS EXISTS"*
— and the script then has to correct it inline: *"busbar-core and busbar-substrate NO LONGER EXIST
BY THOSE NAMES"*. The gate had to work around its own design doc.

### T7. Gate-read docs carrying the retired roster

* **`docs/design/ARCHITECTURE.md`** (read by `design_bindings.rs:88`) — `:17` and `:1177`:
  *"three additions: the `mcp`, `a2a` and `voice` planes"*, while the **same file** says at `:205`
  *"The plane key `streams` (formerly `voice`)"* and `:1681` *"**The voice plane is renamed
  `streams`**"*. Self-contradictory, in the part a parity claim leans on.
* **`docs/design/1.5.5-BEHAVIOUR.md`** (read AND rewritten by `inventory_coverage.rs:57`) — `:5`
  carries that identical sentence, so the error is mirrored in two gate-read files.
* **`docs/design/PLUGIN-TREE.md`** (cited "normative" by the tracker; named in
  `qa/design-bindings.json`) — `:264` renames to **`busbar-plane-streams`** while `:269` says
  `busbar-plane-streaming`; the crate on disk is `busbar-plane-streaming`. `:258` renames
  `busbar-kernel` → `busbar-core-kernel`, contradicting #19 and the on-disk roster. `:266`/`:269`
  keep a **separate per-plane `-codec` crate**, which #6/#39 forbid, and cite `DECISIONS #4/#6/#18`.
* **`qa/teller-steps.json`** is not a document, but it is the same defect in a gate's DATA: its
  plane matrix is `llm, mcp, a2a, voice, admin` — the retired roster plus a cleanliness crate.

### T8. Money-law contradictions

* **`docs/design/rate-card-history.md`** (named in `qa/design-bindings.json` and
  `testing/shadow-oracle/accepted-differences.json`) gets #79 right, but `:184`
  `pub struct CurrencyCode([u8; 3]);` and `:198` `CellPrices(BTreeMap<CurrencyCode, u64>)` build
  native multi-currency pricing — #66 says *"no currency type/symbol anywhere in the money path"*.
  The type exists in code too (`crates/busbar-kernel-ledger/src/cost/currency.rs:29` `pub const USD`).
* **`docs/admin-api.md`** (COMPILE-TIME dependency) `:213` `"currency": "USD"` and `:223` *"the
  response's top-level **`currency`** field … currently `"USD"`"*. Accurate to the shipped
  endpoint, contrary to #66's direction. **Byte-identity (#9) protects this field**, so this is a
  contradiction to route to the owner, not a doc to "fix".
* **`docs/design/billing-unified.md:12,97`** — *"every plane — LLM, MCP, A2A, voice, and dlopen
  FFI"* and `pub plane: Plane, // CLOSED enum: Llm | Mcp | A2a | Voice | Ffi`. A closed plane enum
  in the money path is both the wrong roster and a #1 violation.
* **`docs/design/billing-usage-units.md:453,477`** — *"Tier multiplier: DEFERRED to 1.7"*, *"may
  defer to 1.7"*. Banned by #15.
* **Inside the spec itself:** Part 0 `:115` says *"Integer-only, unitless, no floats"*, but #81
  (OWNER-LOCKED **2026-09-22**, `:425`) rules counts are **exact fixed-point DECIMAL at scale 6** —
  *"`27.5` is 27.5"*. Part 0 is one ruling behind its own Part 2.

### T9. CI / infrastructure docs presenting a torn-down fleet as live

* **`docs/ci/self-hosted-runners.md`** — 555 lines of an operational 10-box EC2 fleet: sizing
  arithmetic, a $/hr cost table, `ci-runners-*.sh` runbooks, a "the fleet is at zero" incident
  playbook. #56 retires it: *"zero instances, zero volumes, zero NAT gateways, zero elastic IPs."*
  **The scripts it tells a reader to run still exist in the repo**, and
  `scripts/ci-runners-lib.sh:42` still cites this doc as the source of its defaults.
* **`docs/ci/gate-integrity.md:131`** — *"fanned across 24 shards of the `busbar-xl` fleet"* —
  `busbar-xl` is the retired EC2 label.
* **`docs/ci/feature-sets.md`** — its ~26-row coverage table is keyed to `busbar-core/*` cargo
  features; the crate is deleted, so the table measures a phantom.

### T10. Public docs teaching a three-plane product

* **`docs/architecture.md:8`** *"## Three planes, one core"*, `:394` *"Circuit-breaker state, on all
  three planes"*, `:476` *"All three planes are in the one binary"*; `:34-35` hedges *"A fourth
  plane is new…"* without naming streaming, and never mentions decision. Plus 14 dead
  `crates/busbar-core/...` citations, including `:464` `crates/busbar-core/src/proxy/engine/mod.rs`,
  which exists nowhere in the tree under any name.
* **`docs/mcp.md:7`** and **`docs/a2a.md:7`** repeat *"the one FSM, on all three planes"*, with 33
  and 23 dead `busbar-core` citations respectively.
* **`docs/migration-1.6.md:118`** *"The live-voice plane"*; `:274` *"Want MCP, A2A or voice?"*
* **`docs/plugins.md:122`** — *"`kind` is `store`, `secret`, `auth`, `hook`, or `export`."* Five.
  The loader itself accepts **`plane`** (`crates/plugin-loader/src/registry.rs:89`, `:458`), and #3
  locks seven. Whether the omission is deliberate scoping or drift, a plugin author reading this
  concludes planes are not droppable — which is exactly what #11 puts in the cut.
* **`THREAT_MODEL.md:114`** — *"A dynamic store/auth/hook plugin is native code loaded with
  `dlopen`"*. Three kinds, in a **security** document, where the loader takes six and the spec
  seven. This understates the dlopen trust surface.

### T11. `RELEASE.md` and `SECURITY.md` — the hand-push instruction

* `RELEASE.md:3` *"The branch model is **dev → qa → main**"*; `:36` *"Prepare the version (on
  `dev`)"*; `:49` `git push origin dev:qa`. **`predev` appears zero times.** #67 and Part 6 say WIP
  lands on `predev` and *"Never push `dev`, `qa` or `main` by hand."*
* `SECURITY.md:69,87` *"Busbar follows a `dev` → `qa` → `main` release flow"* / *"**Fix on `dev`
  first, always**"*. Defensible as public policy; the hazard is that internal advisories quote it
  as an operational directive — e.g.
  `docs/security/advisories/2026-09-10-mutation-rate-limiter-clock-regression.md:5`: *"per
  SECURITY.md, the fix must land on `dev` first."*

### T12. A currently-RED gate, verified by running it

`python3 scripts/public-hygiene-lint.py --root . --quiet` **fails right now**:
`3468 public file(s) scanned against 11 rules — 81 hit(s), 20 allowed` →
`public-hygiene-lint FAILED`. Done-oracle group **PUBLIC-HYGIENE is RED**, which matches the spec's
own Part 7 list. `docs/security/openssf-best-practices.md:84,143,150` ("mutation testing",
"killed mutants") is among the offending files.

Separately, five shipped files leak the private spec's filename and numbering past the
`private-doc-reference` rule, whose bare-`§` pattern is unconditionally exempt in `.md`:
`docs/ci/gate-integrity.md:64`, `docs/migration-1.6.md:226`,
`docs/security/openssf-best-practices.md:16,66,92,119,120,141`, `docs/proof/README.md:3`, and
`docs/security/advisories/2026-09-10-admin-unit-scope-port-drift.md:103` ("DECISIONS #19/#37").

---

## 3. CONTRADICTION SWEEP — by axis, with file and line

### 3a. Plane roster (truth: 5 — llm, mcp, a2a, **streaming**, decision; voice is a dialect, #18/#48)

| File:line | What it says |
|---|---|
| `docs/design/ARCHITECTURE.md:17`, `:1177` | *"three additions: the `mcp`, `a2a` and `voice` planes"* |
| `docs/design/1.5.5-BEHAVIOUR.md:5` | same sentence, verbatim |
| `docs/design/PLUGIN-TREE.md:264` | `busbar-plane-voice` → **`busbar-plane-streams`** (wrong target; `:269` says `-streaming`) |
| `docs/design/D33-legacy-retirement.md:224` | *"reads **five planes and no separate codec crates** — admin, llm, mcp, a2a, voice"* (admin is not a plane; voice is not a plane; streaming/decision absent) |
| `docs/design/1.6.0-composition-root-plan.md:385`, `:431`, `:685`, `:797` | *"`admin` → `mcp` → `a2a` → `voice` → `llm`"*; *"the root serves four planes"* |
| `docs/design/1.6.0-duplex-plane-and-realtime.md:756-758` | *"a plane of 1.6.0's four, not a fifth one"* |
| `docs/design/1.6.0-second-teller-loop-readers.md:108` | *"`Units` implementation for five planes"* then lists admin + voice among them |
| `docs/design/1.6.0-proof-dashboard.md:65` | names 3 of the 5 planes on a public-facing proof page |
| `docs/design/1.6.0-map-proof.md:1030` | *"There are **four** planes (#18)"* — #48 amends #18 to five. **Another agent owns this file; reported, not edited.** |
| `docs/design/billing-unified.md:12`, `:97` | *"LLM, MCP, A2A, voice, and dlopen FFI"*; `CLOSED enum: Llm \| Mcp \| A2a \| Voice \| Ffi` |
| `docs/design/playbook/gate-isomorphism.md:4,7,170,230` | *"The 4-plane … gate … across `{llm, mcp, a2a, voice}`"* |
| `docs/design/playbook/integration-order.md:37,177` | `busbar-{llm,mcp,a2a,voice}`; *"isomorphism (4 planes)"* |
| `docs/design/playbook/voice-arming-blueprint.md:333` | `vec!["llm", "mcp", "a2a", "voice"]` |
| `docs/design/plane4-duplex-session.md:1,4-6` | *"Plane 4 … busbar's fourth plane kind"* |
| `docs/design/plane4-duplex-session-1.6.0-plan.md:157-169,359,438-440` | *"the delete test iterates four planes"* |
| `docs/architecture.md:8,394,476` | *"Three planes, one core"* / *"All three planes are in the one binary"* |
| `docs/mcp.md:7`, `docs/a2a.md:7,434` | *"the one FSM, on all three planes"* |
| `docs/voice.md:27,44` | *"the voice plane"*, *"`plane-voice` is in the … default feature set"* |
| `docs/migration-1.6.md:118,274` | *"The live-voice plane"*; *"Want MCP, A2A or voice?"* |
| `docs/design/1.6.0-TRACKER.md:97,173` | `busbar-plane-voice`; *"## K. Voice plane: from scaffold to shipped"* |
| all 38 `docs/design/playbook/*.md` + 8 `plane4-*.md` | the roster is wrong in effectively every file; see §2 T3 |

**Also inside the spec:** `BUSBAR-1.6.0.md:743` *"There are four planes, not three"* and `:2732`,
`:2736`, `:3204` all say "four planes" — Part 4 text that #48 supersedes.

### 3b. Plugin kinds (truth: 7 — store, secret, auth, hook, export, plane, transport)

| File:line | What it says | Verdict |
|---|---|---|
| `docs/plugins.md:122` | *"`kind` is `store`, `secret`, `auth`, `hook`, or `export`."* | **5 named; the loader takes `plane` too** (`plugin-loader/src/registry.rs:89`) |
| `THREAT_MODEL.md:114` | *"A dynamic store/auth/hook plugin"* | 3 named, in the security doc |
| `docs/design/PLUGIN-TREE.md:266,269` | keeps a separate `busbar-<plane>-codec` path-dep crate | contradicts #6/#39 |
| `docs/design/1.6.0-TRACKER.md:123` (D36) | *"codec logic is the per-plane path-dep `busbar-<plane>-codec`"* | same, in a gate-read file |
| `docs/design/1.6.0-codeaudit-tracker.md:28-31` | lists `busbar-mcp-codec`, `busbar-a2a-codec`, `busbar-streaming-codec` as audit targets | none of the three exists |
| `docs/design/1.6.0-register-audit.md:830`, `1.6.0-owner-decisions.md:815` | *"these four crates **ARE the dialect kind**, unnamed"* | `dialect` is not a kind (#4) — both are framing a measurement, not proposing one, so LOW |
| `docs/design/1.6.0-commit-residue.md:521` | correctly records *"the `control` kind the owner CANCELLED"* | **LIVE — this is the right reading** |

*Not a contradiction, checked:* `crates/busbar-contract/src/plugin.rs:20-37` declares **8** `Kind`
variants (`Plane, Transport, Auth, EgressAuth, Store, Secret, Hook, Export`). #3 pre-reconciles this
in its own words — *"the code's `auth`/`egress-auth` split is an ABI detail inside one kind"* — so
8 variants over 7 kinds is expected, not drift.

### 3c. Trunk and branch (truth: trunk `consolidated/1.6.0`; #67 `predev`/`dev`/`qa`/`main`; never hand-push `dev`/`qa`/`main`)

| File:line | What it says |
|---|---|
| `docs/design/playbook/00-MASTER-PLAN.md:3-4` | *"Working branch: `integration/config-seam-stage1-rebased` (on `origin/dev` 7004d8a7). Dev-only pushes."* |
| `docs/design/playbook/PROGRESS-REPORT.md:3-4` | *"Branch `integration/config-seam-stage1-rebased`, pushed to **origin/dev**. All pushes dev-only."* |
| `docs/design/playbook/integration-order.md:7-9` | *"…land on `integration/config-seam-stage1-rebased` (base `origin/dev@7004d8a7`)"* |
| `docs/design/playbook/voice-money-path-FINAL.md:3` | *"Base: `integration/config-seam-stage1-rebased` (ae85025d — HAS busbar-voice)"* |
| `docs/design/playbook/t1-transport-ws.md:2-4,189` | same branch, cited as HEAD |
| `docs/design/playbook/{gate-no-deferral:18, session-drop-arrival:3, t1-session-scope:3-4, handle-engine-audit-c:4}` | evidence pinned to `integration/plane-extraction` |
| `docs/design/plane4-seam-audit-{A,B}*.md` | pinned to commit `e393b9e6` |
| `docs/design/1.6.0-duplex-plane-and-realtime.md:3` | *"Working branch: `integration/plane-extraction`"* |
| `RELEASE.md:3,36,49` | *"dev → qa → main"*; `--ref dev`; `git push origin dev:qa`. **No `predev`.** |
| `SECURITY.md:69,87` | *"`dev` → `qa` → `main`"*; *"Fix on `dev` first, always"* |
| `docs/design/qa-promote-release.md` | *"The three branches after the split"* — `predev` absent |
| `docs/design/release-path-i5.md:19` | *"Prepare the version (on `dev`)"* |

### 3d. Done-oracle group count (truth: **23** defined, 23 declared)

| File:line | Says |
|---|---|
| `docs/design/BUSBAR-1.6.0.md:86` | *"exits 0 across its **22** groups"* |
| `docs/design/BUSBAR-1.6.0.md:1178` | *"**6 / 22 groups GREEN**"* |
| `docs/design/BUSBAR-1.6.0.md:1212` | *"### 3. The 16 red done-oracle groups"* (6 + 16 = 22) |
| `docs/design/playbook/PROGRESS-REPORT.md:106` | *"Done-oracle readout — 10/11 GREEN"* |
| `docs/design/playbook/voice-dod-finish-checklist.md:6-7,107` | *"Dev is green at 10/11"*, *"11/11 groups GREEN"* |
| `docs/design/playbook/neutrality-findings-and-prevention.md:228` | *"Fired at 11/11 dev-green"* |
| `scripts/verify-1.6.0-done.sh:134` (comment) | *"This script defines TWENTY begin_group/end_group pairs"* — the script's own comment is three behind its own code |

**Correct today:** `docs/design/1.6.0-done-readout.md:28-42` and `1.6.0-map-proof.md` §8.1 both
land on 23.

### 3e. Money law

| File:line | Says | Against |
|---|---|---|
| `docs/design/rate-card-history.md:184,198,364-365` | `pub struct CurrencyCode([u8; 3])`, `CellPrices(BTreeMap<CurrencyCode, u64>)`, native multi-currency cells | #66 — no currency type/symbol |
| `docs/admin-api.md:213,223` | `"currency": "USD"`, *"the response's top-level `currency` field … currently `"USD"`"* | #66 — but **protected by byte-identity #9**; owner call, not a doc fix |
| `docs/design/billing-unified.md:97` | a CLOSED `Plane` enum inside the money record | #1 (core names zero plane types) + #77(1) |
| `docs/design/billing-usage-units.md:453,477` | *"Tier multiplier: DEFERRED to 1.7"*, *"may defer to 1.7"* | #15 |
| `docs/design/rate-card-epochs.md:264` | *"defer C's verb to 1.6.x"* | #15 (doc is already superseded by `rate-card-history.md`) |
| `docs/design/BUSBAR-1.6.0.md:115` | *"Integer-only, unitless, no floats"* | its own #81 (`:425`): exact fixed-point **DECIMAL** at scale 6 |

*Checked and CORRECT:* `docs/configuration.md:634` (*"Currency, symbols, and FX are display
concerns owned by your dashboard"*), `docs/why-busbar.md:29` (*"attaches no currency and does pure
integer math"*), `docs/design/1.6.0-money-cut.md:84` (bills the delivered quantity, never refunds —
matches #62), `docs/design/1.6.0-money-one-function.md:95` (card resolution by `arrived_ms`, #79).

### 3f. Deleted-predecessor citations (#14: these five filenames are BROKEN citations)

Only four files name one as a live path, and three of them are correctly describing the deletion:

| File:line | Context |
|---|---|
| `docs/design/1.6.0-TRACKER.md:123,125` | **live citations** — `DECISIONS #4`, `#5`, `#6`, `#18` used as authority |
| `docs/design/PLUGIN-TREE.md:266,269,336` | **live citations** — `(DECISIONS #4/#6)`, `(DECISIONS #18)` |
| `docs/design/1.6.0-owner-decisions.md:1067` | correctly reports the deletion — LIVE |
| `docs/design/1.6.0-map-proof.md:813-827,854` | correctly audits the deletion — LIVE |
| `docs/security/advisories/…-admin-unit-scope-port-drift.md:103` | *"DECISIONS #19/#37"* in a **public** file — a leak as well as a broken citation |

The 46-file playbook/plane4 corpus names **none** of the five — it predates them; its second-spec
problem is `plane4-duplex-session*.md` instead.

---

## 4. DEAD REFERENCES — documents citing paths that do not exist

**Method:** every backtick-quoted token in all 203 files that looks like a repo path (rooted at
`crates/`, `xtask/`, `scripts/`, `testing/`, `conformance/`, `qa/`, `docs/`, `.github/`, `tests/`,
`examples/`, `fuzz/`, `bin/`, `docker/`, `plugins/`), de-duplicated per file, tested with
`os.path.exists`. Globs and bare directories were dropped.

**Result: 538 distinct dead path citations across 103 of the 203 files.**

> The brief's *"known count of ~15"* is the figure for one specific source that
> `1.6.0-map-proof.md` §S10 re-measures ("15 cited-but-absent docs"). **That agent owns that number
> and this pass did not touch `1.6.0-map-proof.md`.** The 538 above is the repo-wide figure over a
> different and much wider population — the two are not in conflict and should not be reconciled
> into one number.

### 4a. The three root causes, which account for most of the 538

1. **`crates/busbar-core/...`** — the crate is gone (absorbed into `busbar-kernel` at `673ecdaaa`).
2. **`crates/busbar-substrate/...`** — gone (engine half into `busbar-kernel` at `5fa320208`; the
   pure half split to `busbar-substrate-values` at `06132b0b1`).
3. **`crates/busbar-plane-voice/...`, `crates/busbar-mcp-codec/...`, `crates/busbar-a2a-codec/...`,
   `crates/busbar-plane-admin/...`** — never existed or already dissolved.

### 4b. Highest-density files

| Dead refs | File | Note |
|---|---|---|
| 55 | `docs/design/AUDIT-STATUS.md` | **GENERATED** — the ledger's scope rows still name `busbar-core/src`, `busbar-plane-voice/src`, `busbar-a2a-codec/src`, `busbar-mcp-codec/src`. The generator is behind the tree, so this is a `qa/audit-ledger.json` defect surfacing in a gate-consumed report |
| 36 / 31 / 29 / 28 / 24 / 18 / 14 | `docs/design/inventory/1.5.5-*.md` (all 8) | **EXPECTED, not a defect.** These freeze the **1.5.5** layout on purpose (`crates/busbar/src/...`, `crates/plugin-abi/...`). They are the parity baseline; "fixing" them would destroy the thing they exist to preserve |
| 33 / 23 | `docs/mcp.md`, `docs/a2a.md` | public plane references, all `busbar-core` |
| 17 / 14 | `docs/architecture.md` | incl. `:464` `crates/busbar-core/src/proxy/engine/mod.rs`, which exists nowhere under any name |
| 13 | `docs/design/1.6.0-composition-root-plan.md` | |
| 11 | `docs/design/playbook/t2-voice-ir.md` | 11 files under `crates/busbar-voice/src/ir/…` |
| 10 | `docs/design/SEAMS.md`, `docs/design/1.6.0-proof-dashboard.md` | |
| 9 | `docs/design/1.6.0-ratchet-census.md` | |
| 8 | `docs/testing.md`, `docs/design/xtask-gates.md`, `docs/design/1.6.0-secret-hygiene.md` | |
| 7 | **`docs/design/BUSBAR-1.6.0.md`** | the ONE SPEC itself: `:424` `testing/shadow-oracle/cells/__init__.py`, `crates/busbar-release-corpus`, `scripts/apply-deferred-decisions.py`; `:429` `crates/busbar/src/proxy/tests/hook_opt_in_projection_tests.rs`; `:1840` `scripts/loc-surface.py`; `:2450` `crates/busbar-release-oracle/src/normalize.rs`; `:3340` `crates/busbar-unit-verbs` |

### 4c. Dead *script* citations — commands a reader would try to run

| File:line | Cited command | State |
|---|---|---|
| `docs/design/playbook/00-MASTER-PLAN.md:8` (item) | `no-deferral-gate.sh` | absent — retired into `xtask::gates::no_deferral` |
| `docs/design/playbook/gate-no-deferral.md:4,11,12` | `scripts/no-deferral-gate.sh`, `scripts/plane-abi-neutrality.sh`, `scripts/plane-purity-lint.sh` | all absent (the doc self-corrects on the first, not the other two) |
| `docs/design/playbook/gate-isomorphism.md:213`, `qa-promote-release.md:365` | `scripts/full-gate.sh` | absent |
| `docs/design/xtask-gates.md:1163`, `rate-card-epochs.md:316` | `scripts/inventory-coverage.sh`, `scripts/changelog-register-check.sh` | absent |
| `docs/design/qa-promote-release.md:283`, `release-path-i5.md:391`, `docs/proof/README.md:23` | `scripts/release-order-lint.py` | absent |
| `docs/design/playbook/stage-a-{design:26,audit-sonnet:122}.md` | `scripts/config-schema.py` | absent |
| `docs/design/store-qa-cycle.md:588,710,714,715` | `scripts/store-upgrade.sh`, `.github/workflows/{store-matrix,monthly-refresh}.yml` | absent |
| `docs/design/1.6.0-TRACKER.md` (A28) | `scripts/inventory-coverage.py` | absent |
| `docs/design/playbook/m5-voice-boot.md:266`, `docs/mcp.md:539` | `.github/workflows/{voice-conformance,mcp-conformance}.yml` | absent |
| `docs/design/xtask-gates.md:490` | `qa/no-deferral.waivers` | it is `scripts/no-deferral.waivers` |

### 4d. Dead *document* citations — one doc pointing at another that is gone

`docs/design/playbook/gate-no-deferral.md:108` and `1.6.0-map-proof.md:654` →
`docs/design/1.6.0-protocols-as-plugins.md`; `1.6.0-map-proof.md:51,765,766` →
`1.6.0-map.md`, `1.6.0-STATUS.md`, `1.6.0-owner-decisions-queue.md`;
`1.6.0-owner-decisions.md:36` → `1.6.0-STATUS.md`;
`rate-card-history.md:846` → `1.6.0-rate-card-history-ARCHITECTURE-amendment.md`;
`scripts/proto-deletion-gate.sh:5` → `design/one-core-mcp-a2a-as-protocols.md`.
(The `1.6.0-map-proof.md` rows are that agent's to re-measure; listed here for completeness only.)

---

## 5. THE FULL TABLE

**A moving target, stated honestly.** The in-scope set was **198** when this pass began, **203**
mid-pass, and **212** (`docs/**` 205 + root 7) when the table was written — other agents are
committing new `docs/design/1.6.0-*.md` files continuously. Rows below are the state at
classification time; the 13 files that landed after the classification pass are listed at the end
of §5 as **NOT ASSESSED** rather than guessed at.

Classifications: **GATE-READ** (a gate/test/build opens it) · **CITED** (named in a gate artifact
or shipped source, not opened) · **LIVE** · **STALE-DANGEROUS** (with HIGH/MED/LOW) ·
**SUPERSEDED**. A file can be GATE-READ *and* stale; both are shown.

### 5a. Repo root (7)

| Path | Class | Reader | False claims |
|---|---|---|---|
| `README.md` | **GATE-READ**, LIVE | `conformance_sync/render.rs:39`; badge span generated + drift-gated | none. The `Voice_*` badges are **dialect** suite names from `conformance/registry.toml`, which #18 permits |
| `CHANGELOG.md` | **GATE-READ**, LIVE | `changelog.rs:102`, `changelog_register.rs:849`, `roll_changelog.py`, `release-stage.yml:663` | none found |
| `RELEASE.md` | **STALE-DANGEROUS (MED)** | — | `:3` *"dev → qa → main"*; `:36` prepare on `dev`; `:49` `git push origin dev:qa`. `predev` never appears; #67 + Part 6 forbid hand-pushing `dev`/`qa`/`main` |
| `SECURITY.md` | **STALE-DANGEROUS (MED)**, hygiene-scanned | `public-hygiene-lint` | `:69` *"`dev` → `qa` → `main` release flow"*, `:87` *"Fix on `dev` first, always"* — quoted as an operational directive by internal advisories |
| `THREAT_MODEL.md` | **STALE-DANGEROUS (MED)** | `public-hygiene-lint` | `:114` *"A dynamic store/auth/hook plugin"* — 3 kinds where the loader takes 6 and #3 locks 7; understates the dlopen trust surface in a security doc. Oldest file in the repo (2026-08-05) |
| `CONTRIBUTING.md` | LIVE | `public-hygiene-lint` | none found |
| `CODE_OF_CONDUCT.md` | LIVE | `public-hygiene-lint` | none |

### 5b. Public `docs/*.md` (34) — all content-gated by `public-hygiene-lint`

| Path | Class | Reader | False claims |
|---|---|---|---|
| `docs/voice.md` | **STALE-DANGEROUS (HIGH)** | hygiene | `:27` *"the voice plane's one posture"*; `:44` *"`plane-voice` is in the … default feature set"* — true of the code, false against #17/#18. `:64,:129` dead `busbar-core/src/config/mod.rs:5040-5053`; `:224` wrong crate for `twilio.rs` |
| `docs/architecture.md` | **STALE-DANGEROUS (HIGH)** | hygiene | `:8` *"Three planes, one core"*; `:394`,`:476` same; `:34-35` hedges a 4th without naming it. 14 dead `busbar-core` refs incl. `:464` a path that exists nowhere |
| `docs/mcp.md` | **STALE-DANGEROUS (MED)** | hygiene | `:7` *"on all three planes"*; 33 dead `busbar-core` refs; `:539` dead workflow |
| `docs/a2a.md` | **STALE-DANGEROUS (MED)** | hygiene | `:7`,`:434` *"all three planes"*; 23 dead `busbar-core` refs |
| `docs/storage-model.md` | **STALE-DANGEROUS (MED)** | hygiene | 4 dead `busbar-core` refs (`:54`,`:83`,`:106`,`:108`) |
| `docs/tool-and-agent-trust.md` | **STALE-DANGEROUS (LOW)** | hygiene | `:77` dead `crates/busbar-core/src/trust/verify.rs` |
| `docs/plugins.md` | **STALE-DANGEROUS (LOW)** | hygiene; cited by `plugin-loader/src/{sign,highwater}.rs`, `ci.yml:794` | `:122` *"`kind` is `store`, `secret`, `auth`, `hook`, or `export`"* — the loader also accepts `plane` (`registry.rs:89`) |
| `docs/migration-1.6.md` | **STALE-DANGEROUS (LOW)** | hygiene | `:118` *"The live-voice plane"*; `:274` *"Want MCP, A2A or voice?"*; `:226` cites the private spec by path |
| `docs/admin-api.md` | **GATE-READ** + STALE (LOW) | `include_str!` at `busbar-core-admin/src/tests/tests.rs:12969,13035` — **compile-time** | `:213`,`:223` `"currency": "USD"` vs #66. Byte-identity #9 protects the field; owner call |
| `docs/getting-started.md` | **GATE-READ**, LIVE | quickstart EXECUTED by `verify-deploy.yml:1681-1791`, `verify-artifact.py:358`, `release-check.sh:1217` | none |
| `docs/diagnostics.md` | **GATE-READ**, LIVE | byte-asserted, `busbar-substrate-values/src/diagnostics/{mod.rs:3857,tests.rs:133-168}` | none |
| `docs/diagnostics-a2a.md` | **GATE-READ**, LIVE | `busbar-a2a/src/tests/diagnostics_tests.rs:12` | none |
| `docs/diagnostics-mcp.md` | **GATE-READ**, LIVE | `busbar-mcp/src/tests/diagnostics_tests.rs:12` | none |
| `docs/code-layout.md` | LIVE (LOW note) | hygiene; cited by `structure_lint/{hybrid.rs:5,translate.rs:65}` | `:30` calls `crates/busbar/` "the engine + binary"; the engine is `busbar-kernel` now |
| `docs/testing.md` | LIVE | hygiene; cited by `structure_lint/choke_points.rs:5` | 8 dead `crates/busbar/src/...` refs (1.5.5-era) |
| `docs/observability.md` | LIVE | hygiene; cited by `gates/tracing.rs:11` | `:194` one dead ref |
| `docs/internals.md` | LIVE | hygiene | 6 dead 1.5.5-era `crates/busbar/src/...` refs |
| `docs/development.md` | LIVE | hygiene | 3 dead 1.5.5-era refs |
| `docs/roadmap.md` | LIVE | hygiene only — **no other reader exists** | 2 dead 1.5.5-era refs |
| `docs/protocols.md` | LIVE | hygiene | `:313` one dead ref |
| `docs/configuration.md` | LIVE | hygiene | none; `:634` is **correct** on currency |
| `docs/why-busbar.md` | LIVE | hygiene | none; `:29` **correct** on unitless money |
| `docs/audit-chain-digest-v1.md` | LIVE | hygiene | `:155`,`:305` `currency` — per-posting `CurrencyCode`, flagged for awareness only |
| `docs/circuit-breaker.md`, `docs/config-at-a-glance.md`, `docs/failover.md`, `docs/hooks.md`, `docs/migration-1.3.md`, `docs/migration-1.5.md`, `docs/operations.md`, `docs/pools.md`, `docs/providers.md`, `docs/reliability.md`, `docs/token-exchange.md` | LIVE | hygiene | none found |

### 5c. `docs/{adr,advisories,ci,security,proof}` (26)

| Path | Class | Reader | False claims |
|---|---|---|---|
| `docs/ci/self-hosted-runners.md` | **STALE-DANGEROUS (HIGH)** / SUPERSEDED | hygiene (`docs/ci/` partly exempt); cited by `scripts/ci-runners-lib.sh:42` | 555 lines presenting a live 10-box EC2 fleet — cost table, scale scripts, incident playbook — against #56's *"zero instances, zero volumes, zero NAT gateways, zero elastic IPs"*. The `ci-runners-*.sh` it tells you to run still exist |
| `docs/ci/feature-sets.md` | **STALE-DANGEROUS (HIGH)** | cited by `ci.yml:381,1788,2162` | `:169-234` ~26 rows keyed to `busbar-core/*` cargo features; crate deleted. `:88,:175,:200,:221` assert `plane-voice` as the live default |
| `docs/ci/gate-integrity.md` | **STALE-DANGEROUS (MED)** | cited by `scripts/gate-mutants.sh:19` | `:131` *"24 shards of the `busbar-xl` fleet"* — retired EC2 label. `:64` cites the private spec's Part/§ from a shipped file (hygiene leak) |
| `docs/security/openssf-best-practices.md` | **STALE-DANGEROUS (HIGH)** — live gate red | hygiene | `:84,:143,:150` "mutation testing"/"killed mutants" trip the `mutation-testing` rule; the gate **FAILS today** (81 hits). `:16,66,92,119,120,141` name the private `1.6.0-security-posture.md` |
| `docs/advisories/1.6.0/refund-across-window-unrecordable.md` | **STALE-DANGEROUS (MED)** | hygiene | 6 dead `busbar-core` refs (`:34,:37,:69,:71,:72,:74`) |
| `docs/security/advisories/…-mutation-rate-limiter-clock-regression.md` | **STALE-DANGEROUS (LOW)** | hygiene | 7 dead refs; `:5` *"per SECURITY.md, the fix must land on `dev` first"* |
| `docs/security/advisories/…-plugin-abi-enum-discriminant-ub.md` | **STALE-DANGEROUS (LOW)** | hygiene | `:14,:15,:59` dead `busbar-core/src/plane_host/*` |
| `docs/advisories/1.6.0/mysql-store-split-flush-request-count-loss.md` | **STALE-DANGEROUS (LOW)** | hygiene | `:91` dead ref |
| `docs/security/advisories/…-admin-unit-scope-port-drift.md` | LIVE (hygiene note) | hygiene | `:103` *"DECISIONS #19/#37"* — a deleted-file citation **and** a leak, in a public file; `:107,:108` 2 dead refs |
| `docs/ci/latchkey-migration.md` | **LIVE** | cited by `ci.yml:3,12,81` | none — matches #56 exactly and names `self-hosted-runners.md` as superseded |
| `docs/ci/actions-pins.md` | LIVE | cited by `gates/release_order.rs:663` | none |
| `docs/proof/README.md` | LIVE (hygiene note) | cited by `ci.yml:2794`; `proof-manifest.py` writes its siblings | `:3` names the private `1.6.0-proof-dashboard.md`; `:8,:23` 3 dead refs |
| `docs/adr/*.md` (5) | LIVE | **no gate reader** (only a comment cite at `busbar-llm-codec/src/ir/mod.rs:6`) | none |
| `docs/advisories/1.6.0/{README,upstream-429-answered-as-503,voice-session-fee-draws-once-per-session}.md` | LIVE | hygiene | none — the voice-fee advisory uses **current** paths and is well maintained |
| `docs/security/{HISTORY-REWRITE.md}` + 5 clean advisories | LIVE | hygiene | none |

### 5d. `docs/design/*.md` — not `1.6.0-*`, not `plane4-*` (35) + `inventory/` (8)

| Path (under `docs/design/`) | Class | Reader | False claims |
|---|---|---|---|
| `BUSBAR-1.6.0.md` | **GATE-READ**, LIVE (the spec) | `plane_abi_neutrality.rs:49,258` | Behind itself: `:86`,`:1178`,`:1212` say **22** groups (it is 23); `:115` *"Integer-only"* vs its own #81 at `:425`; `:743`,`:2732`,`:2736`,`:3204` say *"four planes"* vs #48; `:1181`,`:1188` say **57** crates (workspace has 49); 7 dead path refs |
| `ARCHITECTURE.md` | **GATE-READ** + **STALE-DANGEROUS (HIGH)** | `design_bindings.rs:88`; construction gate | `:17`,`:1177` *"three additions: the `mcp`, `a2a` and `voice` planes"* while `:205`,`:1681` say the plane is renamed `streams`. Self-contradictory |
| `1.5.5-BEHAVIOUR.md` | **GATE-READ** (read **and rewritten**) + STALE (MED) | `inventory_coverage.rs:57` | `:5` the same *"mcp, a2a and voice planes"* sentence |
| `AUDIT-STATUS.md` | **GATE-READ** (GENERATED) | `audit.rs:50`; `verify-1.6.0-done.sh:758` | 55 dead scope paths (`busbar-core/src`, `busbar-plane-voice/src`, `busbar-{mcp,a2a}-codec/src`) — a `qa/audit-ledger.json` defect surfacing in the report; fix the ledger |
| `PLUGIN-TREE.md` | **CITED** + **STALE-DANGEROUS (HIGH)** | `qa/design-bindings.json`; `busbar-transport-sse/src/{claims,meta}.rs`; quoted in kind-isolation refusals | `:264` `busbar-plane-streams` (should be `-streaming`; `:269` disagrees); `:258` `busbar-kernel`→`busbar-core-kernel`; `:266`,`:269` keep a per-plane `-codec` crate (#6/#39); cites deleted `DECISIONS` |
| `D33-legacy-retirement.md` | **STALE-DANGEROUS (HIGH)** | — | `:224` *"five planes … admin, llm, mcp, a2a, voice"*; `:147`,`:212` target `busbar-plane-voice`/`busbar-voice-codec` for open work |
| `billing-unified.md` | **CITED** + **STALE-DANGEROUS (HIGH)** | `busbar-llm-codec/src/{gemini/mod,ir/types}.rs` | `:12` *"LLM, MCP, A2A, voice, and dlopen FFI"*; `:97` `CLOSED enum: Llm \| Mcp \| A2a \| Voice \| Ffi`; routes money through deleted `busbar-core` |
| `billing-usage-units.md` | **CITED** + **STALE-DANGEROUS (HIGH)** | same | `:453` *"Tier multiplier: DEFERRED to 1.7"*; `:477` *"may defer to 1.7"* (#15); same roster |
| `rate-card-history.md` | **CITED** + **STALE-DANGEROUS (HIGH)** | `qa/design-bindings.json`; `accepted-differences.json`; `root/units_admin/mod.rs` | `:184` `CurrencyCode([u8;3])`, `:198` `CellPrices(BTreeMap<CurrencyCode,u64>)`, `:364-365` multi-currency cells vs #66. Its #79 content **is** correct |
| `plane-extraction-design.md` | **CITED** + **STALE-DANGEROUS (HIGH)** | `scripts/plane-delete-test.sh:7`; `busbar-llm/Cargo.toml:86` | `:3` *"Target: 1.7.x"*; `:327` *"1.6.0 is complete and frozen-pending-promotion"* |
| `foundation-abi-config.md` | STALE (MED) | — | uses "voice" as the plane-5 exemplar throughout; the mechanism is still sound |
| `qa-promote-release.md` | STALE (MED) | — | *"The three branches after the split"* — no `predev`; `:283`,`:365` 2 dead scripts |
| `release-path-i5.md` | STALE (LOW) | cited by `RELEASE.md` | `:19` *"Prepare the version (on `dev`)"*; no `predev`; 3 dead script refs |
| `voice-cross-dialect-mapping.md` | STALE (LOW) | — | `:3` *"the busbar voice plane bridges"*; the mapping DATA is still valid and wanted |
| `VT8-design-driver-socket.md` | LIVE (in progress) | — | `:384` `busbar-plane-streams-*` — the `streams`/`streaming` spelling gap |
| `xtask-gates.md` | **CITED**, LIVE | `xtask/src/main.rs:2`, `plane_purity/freeze.rs:2`, `structure_lint/translate.rs:25,81` | 8 dead refs (`:160,:264,:490,:673,:696,:1163,:1335`) |
| `store-qa-cycle.md` | **CITED**, LIVE | `verify-1.6.0-done.sh:952`; `testing/fleet-fixtures/*` | 6 dead refs (`:96,:501,:588,:710,:714,:715`) |
| `contract-notes.md` | **CITED**, LIVE | `busbar-contract/src/{kinds,lib,plane}.rs` | none |
| `handle-engine-retention-sweep.md` | **CITED**, LIVE | `busbar-kernel/src/plane/handle_engine.rs` | none |
| `gemini-usage-metadata-spec-discrepancy.md` | **CITED**, LIVE | `CHANGELOG.md`; `testing/llm-conformance/…/cells.json` | none |
| `ARCHITECTURE-AUDIT-LOG.md` | SUPERSEDED | — | self-declared `:1` *"history; NOT the design"* |
| `CORE-AND-PLUGIN-CONTRACT.md` | SUPERSEDED | — | self-declared `:1` *"v0.1 — SUPERSEDED"*, `:2` *"Do not hand this file to a builder"* |
| `CODE-ANALYSIS.md` | SUPERSEDED | — | `:15` old roster, framed as then-current analysis |
| `PARITY-BASELINE-2026-09-04.md` | SUPERSEDED | `testing/shadow-oracle/findings-2026-09-04.md` | `:18` *"mcp / a2a / voice planes"* — self-dated |
| `PARITY-BASELINE-2026-09-05.md` | SUPERSEDED | — | `:9` same phrasing — self-dated |
| `rate-card-epochs.md` | SUPERSEDED | — | `:264` *"defer C's verb to 1.6.x"* (#15); superseded by `rate-card-history.md` |
| `CI-HEALTH-2026-09-04.md` | LIVE (self-dated) | — | 2 dead refs |
| `admin-new-verbs-contract.md` | LIVE, STEERING | — | none — correctly treats admin as a cleanliness surface with no `control` kind |
| `AUDIT-1.6.0-HIGH-DRIVE.md` | LIVE (fix log) | — | `:79-80` `busbar-plane-voice` as a then-current path |
| `SEAMS.md` | LIVE (pinned measurement) | — | 10 dead refs; voice/`busbar-core` content explicitly marked *"dies with D33"* |
| `plane-decl-registry.md`, `loop-unify-map.md`, `llm-test-fixture-seam.md`, `kernel-group-leases.md`, `kernel-late-accrual.md`, `1.5.3-config-consolidation.md` | LIVE | — | none material |
| `inventory/1.5.5-*.md` (8) | **GATE-READ**, LIVE | `inventory_coverage.rs` (dir walk); `inventory_ref.rs:60-78`; `documented-claims-check.py` | Their ~180 "dead" paths are the **1.5.5 layout, frozen on purpose**. NOT a defect — do not "fix" |

### 5e. `docs/design/playbook/` (38) + `docs/design/plane4-*.md` (8) — **all 46 STALE-DANGEROUS**

Zero files in this corpus classify LIVE or SUPERSEDED. It is the ancestor of the "voice is Plane 4"
program that #18/#48 retired, written 2026-09-02/03 and never reconciled. **STEERING** marks a file
that opens by instructing.

| Path | Rank | The claim that would move an agent's hands |
|---|---|---|
| `playbook/00-MASTER-PLAN.md` | HIGH, STEERING, **cited by the done script** | see §2 T1 |
| `playbook/voice-arming-blueprint.md` | HIGH, STEERING | `:14-16` `plane-voice = ["dep:busbar-voice", …, "busbar-core/plane-voice"]`; `:333` `vec!["llm","mcp","a2a","voice"]` |
| `playbook/m5-voice-boot.md` | HIGH, STEERING | `:4` *"Booting the voice plane (Plane 4)"*; `:26-40` copy-pasteable Cargo + `register_planes` wiring |
| `playbook/gate-no-deferral.md` | HIGH, STEERING, **cited by `scripts/no-deferral.waivers:10`** | `:39` names a non-spec doc *"AUTHORITATIVE"*; `:161-172` makes finishing `busbar-voice` a build-blocking law; `:4,:11,:12` 3 dead scripts |
| `playbook/gate-isomorphism.md` | HIGH, STEERING, **cited by `plane_isomorphism.rs` + `qa/plane-hook-isomorphism.allow:5`** | `:4`,`:7`,`:170`,`:230` the `{llm, mcp, a2a, voice}` roster |
| `playbook/PROGRESS-REPORT.md` | HIGH, STEERING | `:3` *"pushed to origin/dev"*; `:19` *"Voice plane booted"*; `:106` *"10/11 GREEN"* |
| `playbook/integration-order.md` | HIGH, STEERING | `:1`,`:5` *"AUTHORITATIVE INTEGRATION PLAN"*; `:7-9` wrong branch; `:177` *"isomorphism (4 planes)"* |
| `playbook/de-skeleton-plan.md` | HIGH, STEERING | `:26`,`:34-45` a replacement-text table to polish `crates/busbar-voice`, which #19 deletes |
| `playbook/gauntlet-session.md` | HIGH, STEERING | `:1`,`:3` *"STOP-condition. BLOCKS M5 boot"*; `:233` *"M5 boots the voice/duplex plane"* |
| `playbook/usage-cost-fold.md` | HIGH, STEERING | title *"(voice hard-stop)"*; `:19-20` deleted `busbar-core/src/plane/cost.rs` |
| `playbook/money-path-refinements.md` | HIGH, STEERING | `:6` *"Apply BEFORE building"* against `busbar-voice/src/topology/mod.rs` |
| `playbook/neutrality-findings-and-prevention.md` | HIGH, **cited by `plane_host_universal_purity.rs:5`** | `:153`,`:165`,`:217` three *"deferred"* findings (#15); `:228` *"11/11 dev-green"*; `:33-36` voice keeps codecs in `busbar-voice` (#5) |
| `playbook/voice-dod-finish-checklist.md` | HIGH, STEERING | `:6-7` *"green at 10/11"*; `:90` *"add `plane-voice` to default"*; `:107` *"11/11 groups GREEN"* |
| `playbook/capability-equality-resolution.md` | HIGH | `:25`,`:147` instructions to add a *"voice column"* to the isomorphism ledger |
| `playbook/conformance-voice.md` | HIGH, STEERING | `:1` *"Voice (Plane 4) conformance battery"*; `:108-109` cites `plane4-*.md` as authority |
| `playbook/gap-census.md` | HIGH | `:4-5` DoD *"voice plane booted, four-noun config seam closed"* |
| `playbook/prod-composition.md` | HIGH, STEERING | `:115`,`:236-244` *"DEFERRABLE (post-1.6.0)"* (#15) |
| `playbook/stage-a-design.md` | HIGH, STEERING | `:12` deleted `busbar-core/src/config/named_map.rs`; `:73` *"Voice owns `streams:` via the seam"* |
| `playbook/t1-cost-lease.md` | HIGH, STEERING | `:11-12` *"ship the slots with the plane in 1.7.0"* (#15) |
| `playbook/t1-media-path.md` | HIGH, STEERING | `:39`,`:45`,`:150` *"100% new code in busbar-voice"* |
| `playbook/t1-session-scope.md` | HIGH | `:3-4` wrong branch; `:102`,`:133` deleted `busbar-core/src/plane_host/scope.rs` |
| `playbook/t1-transport-ws.md` | HIGH, STEERING | `:2-4` wrong branch as HEAD; `:21-27` *"All three legs live in busbar-substrate"*; `:182` *"the plane key is `"voice"`"*; `:197` *"deferred"* |
| `playbook/t2-gemini.md` | HIGH, STEERING | `:4-6`,`:40-43` `VOICE_WIRE_FORMATS` — a plane-level const for a plane that must not exist |
| `playbook/t2-runtime-session.md` | HIGH, STEERING | `:138-139` `PLANE_DECL … key:"voice", config_section:"streams"` |
| `playbook/t2-twilio.md` | HIGH, STEERING | `:8-10` *"T2 is the first consuming plugin, busbar-voice"* |
| `playbook/t2-voice-ir.md` | HIGH, STEERING | `:287` *"a P2 (1.7.0) blocker"* (#15); 11 dead `busbar-voice/src/ir/*` refs |
| `playbook/t2-webrtc.md` | HIGH, STEERING | title *"BUILD PLAYBOOK … (busbar-voice, behind `runtime`)"* |
| `playbook/ws-accept-seam-design.md` | HIGH, STEERING | `:83-84`,`:133` proposes `plane-voice = ["busbar-substrate/plane-voice", "duplex-ws"]` — the banned name through two deleted crates |
| `playbook/voice-money-path-FINAL.md` | HIGH, STEERING | `:3` *"Base: `integration/config-seam-stage1-rebased` … HAS busbar-voice"* |
| `playbook/session-drop-arrival.md` | MED | `:3` wrong branch; `:96` *"lives in busbar-voice + busbar-core cost_host"* |
| `playbook/handle-engine-audit-c.md` | MED | `:4` wrong branch; voice framed as a future plane consumer; locking content otherwise sound |
| `playbook/t1-pump-port.md` | MED | `:1`,`:3` target crate `busbar-substrate` does not exist; content otherwise plane-neutral |
| `playbook/gauntlet-session-audit-opus.md` | MED | `:26`,`:49`,`:62` `busbar-core/src/{ingress/dispatch,plane/cost}.rs` as live |
| `playbook/gauntlet-session-audit-sonnet.md` | MED | `:1-4` its target file did not exist; `:32`–`:128` pervasive `busbar-voice/src/topology/*` |
| `playbook/stage-a-audit-opus.md` | MED | `:34`,`:71-73`,`:113` deleted `busbar-core` as the live edit surface |
| `playbook/stage-a-audit-sonnet.md` | MED | `:5`,`:16-17`,`:121` deleted `busbar-core` + `busbar-substrate` |
| `playbook/usage-cost-fold-audit-opus.md` | MED | `:23-25` treats `busbar-core` as extant |
| `playbook/usage-cost-fold-audit-sonnet.md` | MED | `:5`,`:57-58` same |
| `plane4-duplex-session-1.6.0-plan.md` | HIGH, STEERING | `:1`,`:3` *"Plane 4 … This is the plan 1.6.0 is built from"*; `:35-36` *"the complete busbar-voice plane"*; `:157-169` *"the delete test iterates four planes"* |
| `plane4-duplex-session.md` | HIGH, STEERING, **cited by `busbar-voice*` source** | `:1` *"(authoritative design)"*; `:4-6` *"busbar's fourth plane kind"*; `:667-675` *"Name the plane crate busbar-voice"*; `:761-762` *"P2 (1.7.0)"* |
| `plane4-seam-audit-A-transport.md` | HIGH | `:4` pinned to `e393b9e6`; `:7` *"Plane 4 … busbar-voice"*; extensive `busbar-core` |
| `plane4-seam-audit-B-session.md` | HIGH | `:5` cites the plan doc as authoritative (#14); deleted SessionScope homes |
| `plane4-seam-audit-D-abi.md` | HIGH | `:3-5` the ABI judged against *"a fourth, DUPLEX plane (busbar-voice)"* |
| `plane4-seam-audit-E-ir.md` | MED-HIGH | `:139`,`:146` argues for a NEW parallel pump instead of folding into `busbar-plane-streaming` (#5) |
| `plane4-seam-audit-C-handles.md` | MED | `:8`,`:151-160` busbar-voice as the second consumer; concurrency substance sound |
| `plane4-voice-dialect-landscape.md` | MED, **cited by `busbar-plane-streaming/src/twilio.rs`** | `:1`,`:4` *"Plane 4 — Voice Dialect Landscape"*; the OpenAI/Gemini protocol survey underneath is still accurate and worth keeping |

### 5f. `docs/design/1.6.0-*.md` (55 at write time; 42 assessed)

| Path (under `docs/design/`) | Class | Reader | False claims |
|---|---|---|---|
| `1.6.0-TRACKER.md` | **GATE-READ** + **STALE-DANGEROUS (HIGH)** | `no_deferral.rs:60,310`; cited by `qa/teller-steps.json:3` and `plugin-loader/src/plane.rs:35` | see §2 T2 — `:123` D36 and `:125` D37 are unchecked boxes for **CANCELLED** work; `:97`,`:111`,`:173`; four `DECISIONS #n` citations |
| `1.6.0-technical-changes.md` | **STALE-DANGEROUS (HIGH)** | — | `:19`,`:67`,`:199-200` the **external plugin-author porting guide** points third parties at `busbar-mcp`/`busbar-a2a`/`busbar-llm-codec` and never mentions the `busbar-plane-*` crates that exist |
| `1.6.0-mcp-engine.md` | **STALE-DANGEROUS (HIGH)**, STEERING | — | `:133`,`:343`,`:354`,`:385`,`:388` a §7 "ORDERED LANDING PLAN" routing work into `busbar-mcp-codec`; `qa/construction.toml:135` says *"MCP HAS NO ROW HERE ANY MORE."* Following it resurrects a #39-banned crate |
| `1.6.0-composition-root-plan.md` | **STALE-DANGEROUS (HIGH)** | — | `:385` *"`admin` → `mcp` → `a2a` → `voice` → `llm`"*; `:431`,`:685`,`:744`,`:797` four-plane roster; 13 dead refs |
| `1.6.0-llm-plane-abi-purity.md` | **STALE-DANGEROUS (HIGH)**, STEERING | — | `:3` *"AUTHORITATIVE execution plan"* to repoint **1602** `busbar_core::` refs; the tree has **1**, and it is a comment |
| `1.6.0-money-path-relocation.md` | **STALE-DANGEROUS (HIGH)**, STEERING | — | `:3` *"AUTHORITATIVE execution plan"*; `:44` gate `cargo build -p busbar-core` — no such package |
| `1.6.0-money-path-phase34.md` | **STALE-DANGEROUS (HIGH)**, STEERING | — | `:59` Commit-C presented as still to run; it landed with busbar-core's deletion |
| `1.6.0-contract-gaps.md` | **CITED** + **STALE-DANGEROUS (HIGH)** | `qa/design-bindings.json`; `xtask/src/denylist.rs:2,86` | `:38`,`:39`,`:43`,`:48` cite `busbar-plane-voice/...`; `:116` (CG-58) and `:129` (CG-63) frame voice legs as live Phase-0.5 work. CG-49 is already struck, proving the rest was missed |
| `1.6.0-duplex-plane-and-realtime.md` | **CITED** + **STALE-DANGEROUS (HIGH)** | `scripts/no-deferral.waivers` | `:3` wrong branch; `:6` cites nonexistent `1.6.0-protocols-as-plugins.md`; `:511-516` reopens a settled question; `:756-758` *"a plane of 1.6.0's four, not a fifth one"* |
| `1.6.0-a2a-node.md` | STALE-DANGEROUS (MED), STEERING | — | `:101`,`:329`,`:419`,`:432` build their argument on `busbar-a2a-codec`, dissolved under #39 |
| `1.6.0-api-retirement.md` | STALE-DANGEROUS (MED), STEERING | — | `:20` *"Later agents work off this file, drive their count down"*; `:41`–`:170` list `busbar-core` in the crates column |
| `1.6.0-credential-signature-declared.md` | STALE-DANGEROUS (MED), STEERING | — | `:113` *"busbar-core's own `AuthMiddleware::extract_client_token` is the live 1.5.5 path"* |
| `1.6.0-handle-engine-extraction-notes.md` | STALE-DANGEROUS (MED) | — | `:1`,`:88`,`:136` destination `busbar-substrate`; the work shipped in `busbar-kernel/src/plane/handle_engine.rs` |
| `1.6.0-second-teller-loop-readers.md` | STALE-DANGEROUS (MED), STEERING | — | `:84` *"`plane-voice`, default on"*; `:108` *"`Units` implementation for five planes"* listing admin and voice |
| `1.6.0-per-unit-arena.md` | STALE-DANGEROUS (MED) | — | `:18`,`:54`,`:62` `plane-voice`/`VoiceUnit` under *"Every current implementor"* |
| `1.6.0-proof-dashboard.md` | **CITED** + STALE-DANGEROUS (MED) | `proof-manifest.py:5`; `ci.yml:2769` | `:65` names 3 of 5 planes on a public proof page; `:94`,`:98` evidence paths inside deleted crates |
| `1.6.0-waist-rebuild-plan.md` | STALE-DANGEROUS (MED) | — | `:3` *"Status: proposal, 2026-09-04"*, no historical banner; `:23` targets `crates/busbar-substrate/src/teller/` (the real successor is `busbar-kernel/src/teller.rs`) |
| `1.6.0-root-admin-switch-notes.md` | STALE-DANGEROUS (LOW) | — | `:30`,`:204` `busbar-core` paths as live |
| `1.6.0-codeaudit-tracker.md` | STALE-DANGEROUS (LOW) | — | `:28-31` lists `busbar-{mcp,a2a,streaming}-codec` as audit targets; none exists. Row 9 duplicated with conflicting status (`:17` vs `:20`) |
| `1.6.0-owner-decisions.md` | STALE-DANGEROUS (LOW), STEERING | — | `:103` *"four planes, both auth plugins"*, though the same file later rules the roster is five; `:36` dead `1.6.0-STATUS.md`. `:44`'s *"both files are GONE"* is **correct** |
| `1.6.0-map-proof.md` | LIVE — **ANOTHER AGENT OWNS IT** | `scripts/deferral-words.py` | `:1030` *"There are **four** planes (#18)"* — #48 amends to five. **Reported, not edited.** Otherwise self-correcting and confirms 23 groups at §8.1 |
| `1.6.0-secret-hygiene.md` | **CITED**, LIVE | `secret-hygiene-gate.sh:7`; `qa/segments.toml`; `ci.yml:1429` | none — self-flags its #54 conflict at `:236-261`; 8 dead refs |
| `1.6.0-security-posture.md` | **CITED**, LIVE | `proto-deletion-gate.sh:109,269` | none — re-verified 2026-09-22; 7 dead refs |
| `1.6.0-engine-split-plan.md` | **CITED**, LIVE | `qa/construction.toml`; `busbar-a2a/src/a2a/meter.rs` | none — records `busbar-voice → busbar-plane-streaming` correctly at `:224-225` |
| `1.6.0-done-readout.md` | LIVE | — | none — establishes the 23-group count and the 5-plane roster correctly |
| `1.6.0-commit-residue.md` | LIVE | — | none — `:521` correctly names *"the `control` kind the owner CANCELLED"*; `:502` confirms 23/23 |
| `1.6.0-exact-count-migration.md` | LIVE | — | carries a visible correction banner; residual llm/voice-codec tension tracked via TRACKER D36 |
| `1.6.0-gate-sweep.md` | LIVE | — | none — `:373` itself files `busbar-plane-voice` as a bug |
| `1.6.0-money-cut.md` | LIVE | — | none — `:84` matches #62 exactly; 4 dead refs |
| `1.6.0-money-one-function.md` | LIVE | — | none — `:76` *"the five planes"*; `:95` card resolution by `arrived_ms` (#79) |
| `1.6.0-oracle-coverage.md`, `1.6.0-duplication-census.md`, `1.6.0-instrument-audit.md`, `1.6.0-ratchet-census.md`, `1.6.0-register-audit.md`, `1.6.0-llm-switch-over.md`, `1.6.0-kernel-open-unit.md`, `1.6.0-one-face-per-kind.md`, `1.6.0-one-net-judge.md`, `1.6.0-codeaudit-findings-ledger.md`, `1.6.0-streams-deletion-list.md` | LIVE | — | none material; several actively self-correct |
| `1.6.0-wave-d-work-order.md` | SUPERSEDED | — | self-marked *"HISTORICAL — EXECUTED"*, redirects to live TRACKER rows |

**NOT ASSESSED (13)** — created by other agents *during* this pass, after the classification run.
Listed for completeness; their owners should classify them:
`1.6.0-branch-audit-ab.md`, `1.6.0-branch-audit-ac.md`, `1.6.0-branch-audit-ad.md`,
`1.6.0-branch-audit-ah.md`, `1.6.0-conformance-rig-audit.md`, `1.6.0-deferral-reconcile.md`,
`1.6.0-feature-plugin-census.md`, `1.6.0-LEDGER.md`, `1.6.0-money-census.md`,
`1.6.0-oracle-skip-audit.md`, `1.6.0-release-engine-audit.md`, `1.6.0-shadowed-guard-audit.md`,
`1.6.0-uncommitted-inventory.md`, `1.6.0-unreachable-audit.md`. (Plus this file.)

---

## 6. SAFE TO DELETE — a RECOMMENDATION ONLY. Nothing was deleted.

### 6a. The proof each candidate had to pass

A file is listed here only if **all four** hold:

1. **Not GATE-READ.** Its path appears in no `cx.read`, `include_str!`, `open()`, byte-assert or
   directory walk in `xtask/`, `scripts/`, `.github/`, or any crate. Established by the §1 sweep.
2. **Not CITED.** Its *basename* appears nowhere outside `docs/` in the whole tree. Measured by
   collecting every `*.md` basename referenced in `xtask/ scripts/ .github/ crates/ testing/
   conformance/ tests/ qa/ examples/ fuzz/ bin/ docker/ org-profile/` (**152 distinct names**) and
   subtracting. A file whose name is in that set is excluded even if nothing opens it, because
   deleting it strands a shipped comment or a gate ledger row.
3. **Under `docs/design/`.** Everything under `docs/` *outside* `design/` is the published product
   surface and is content-scanned by `public-hygiene-lint`; none of it is proposed for deletion
   here regardless of staleness. Stale public docs get **rewritten**, not removed.
4. **Not touched on 2026-09-22.** Excludes everything other agents are writing right now.

**76 files pass all four conditions.** The three tiers below name the **53** whose verdict is
clear-cut; the other 23 pass the mechanical test but are judgement calls an owner should make
one at a time. The full computed list is reproducible with:

```sh
# 1. every .md basename named anywhere outside docs/
grep -rhoE '[A-Za-z0-9_.-]+\.md' --exclude-dir={.git,target,.claude,docs,mutants-report,mutants-report-live} . | sort -u > /tmp/referenced.txt
# 2. design docs whose basename is absent from it, excluding today's work
for f in $(find docs/design -name '*.md'); do
  b=$(basename "$f"); d=$(git log -1 --format=%ad --date=short -- "$f")
  grep -qxF "$b" /tmp/referenced.txt || { [ "$d" = "2026-09-22" ] || echo "$d $f"; }
done | sort
```

### 6b. The recommendation, in three tiers

**Tier 1 — delete outright (40 files): the voice-plane program, minus its six anchors.**
All 38 `playbook/*.md` and all 8 `plane4-*.md` are STALE-DANGEROUS with no LIVE content, **except**
these five, which condition 2 excludes because shipped source or a gate ledger names them:
`playbook/00-MASTER-PLAN.md`, `playbook/gate-isomorphism.md`, `playbook/gate-no-deferral.md`,
`playbook/neutrality-findings-and-prevention.md`, `plane4-duplex-session.md`, plus
`plane4-voice-dialect-landscape.md` (named by `busbar-plane-streaming/src/twilio.rs`).
**Those six must be repointed or rewritten first, never deleted first** — the citing source has to
move in the same commit. The remaining **40** have no external citation at all.

> Before deleting `plane4-voice-dialect-landscape.md`, note its OpenAI-Realtime/Gemini-Live
> protocol survey is still factually good and is the only place that comparison exists. Fold it
> into the streaming plane's own docs rather than losing it.

**Tier 2 — delete as superseded (7 files), each self-declaring:**
`CORE-AND-PLUGIN-CONTRACT.md` (*"v0.1 — SUPERSEDED … Do not hand this file to a builder"*),
`CODE-ANALYSIS.md`, `PARITY-BASELINE-2026-09-05.md`, `CI-HEALTH-2026-09-04.md`,
`rate-card-epochs.md` (superseded by `rate-card-history.md`), `1.5.3-config-consolidation.md`,
`1.6.0-waist-rebuild-plan.md`. *(`PARITY-BASELINE-2026-09-04.md` is excluded — it is named by
`testing/shadow-oracle/findings-2026-09-04.md`.)*

**Tier 3 — dead execution plans whose premise has already shipped (6 files):**
`1.6.0-money-path-relocation.md`, `1.6.0-money-path-phase34.md`, `1.6.0-llm-plane-abi-purity.md`,
`1.6.0-handle-engine-extraction-notes.md`, `1.6.0-root-admin-switch-notes.md`,
`1.6.0-codeaudit-tracker.md`. Each targets `busbar-core`/`busbar-substrate`/a dissolved codec
crate; all six are uncited and unread.

### 6c. Files that look dead but are NOT safe to delete — the trap list

| File | Why it survives |
|---|---|
| `docs/design/1.6.0-TRACKER.md` | `no_deferral.rs:310` reads it; a missing file is a RED gate. **Fix the D36/D37 rows in place.** |
| `docs/design/1.5.5-BEHAVIOUR.md` | `inventory_coverage.rs:57` reads **and rewrites** it |
| `docs/design/inventory/*.md` (8) | the inventory-coverage/ref gates walk the directory; their "dead" paths are the frozen 1.5.5 baseline |
| `docs/design/AUDIT-STATUS.md` | generated; deleting it just makes the next `ledger status` recreate it |
| `docs/admin-api.md` | `include_str!` — **deleting it breaks the build** |
| `docs/diagnostics{,-a2a,-mcp}.md` | byte-asserted against a live render by unit tests |
| `docs/getting-started.md` | its quickstart is executed by release CI |
| `docs/roadmap.md` | zero readers, but it is **published product surface**; rewrite, don't remove |
| `docs/ci/self-hosted-runners.md` | superseded in substance, but `scripts/ci-runners-lib.sh:42` cites it as the source of two live defaults — repoint the script first |
| `docs/design/plane-extraction-design.md` | `scripts/plane-delete-test.sh:7` + `busbar-llm/Cargo.toml:86` cite it |
| `docs/design/{PLUGIN-TREE,rate-card-history,1.6.0-contract-gaps}.md` | named inside `qa/design-bindings.json`, a committed gate artifact |
| `docs/design/{billing-unified,billing-usage-units,contract-notes,handle-engine-retention-sweep}.md` | named in shipped crate source |
| everything under `docs/` outside `design/` | `public-hygiene-lint` scans it as the published surface |

---

## 7. WHAT TO ACT ON FIRST (ordered; none of it done here)

1. **`docs/design/1.6.0-TRACKER.md:123,125`** — strike the two CANCELLED rows. This is the single
   line-level edit that stops a measurement reporting D36/D37 as outstanding. It is gate-read, so
   it must be edited in place, never removed.
2. **`docs/design/playbook/00-MASTER-PLAN.md`** — either reconcile it or repoint
   `scripts/verify-1.6.0-done.sh:6` away from it. Today the definition-of-done script points at a
   file naming the wrong branch, the wrong plane count, and a banned feature name.
3. **`qa/teller-steps.json`** — its plane matrix is `llm, mcp, a2a, voice, admin`. A gate is
   measuring the retired roster. (Data, not a doc, but it is the same defect.)
4. **`docs/design/ARCHITECTURE.md:17,1177`** and **`1.5.5-BEHAVIOUR.md:5`** — one sentence,
   duplicated across two gate-read files, that ARCHITECTURE.md itself contradicts 1,600 lines later.
5. **`docs/design/BUSBAR-1.6.0.md`** — the spec's own drift: `22` groups → 23 (`:86`, `:1178`,
   `:1212`), `57` crates → 49 (`:1181`, `:1188`), *"Integer-only"* vs #81 (`:115`), *"four planes"*
   in Part 4 vs #48 (`:743`, `:2732`, `:2736`, `:3204`).
6. **`RELEASE.md`** — add `predev` and stop instructing hand-pushes to `dev`/`qa`/`main`.
7. **PUBLIC-HYGIENE is RED right now** (81 hits, verified by running the lint). Three real
   offenders plus the five private-spec leaks listed in §2 T12.
8. **`docs/voice.md`, `docs/architecture.md`, `docs/plugins.md`, `THREAT_MODEL.md`** — the public
   surface still teaches three planes, a `voice` plane, five plugin kinds and a three-kind dlopen
   trust surface.
9. **The 46-file voice-plane corpus** — §6b Tier 1. Nothing in it is right, and six of its files
   are load-bearing citations that must be repointed in the same commit.

---

*Produced by the docs-inventory pass, 2026-09-22. Read-only over every file except this one.
No document was deleted, and `1.6.0-map-proof.md` was read but not edited — its dead-reference
count belongs to the agent that owns it.*
