# 1.6.0 — COMMIT-BODY RESIDUE: the 242 findings that live only in a commit message

> **What this is.** `docs/design/1.6.0-map-proof.md` §5 read **all 307 commit bodies** in the
> 36-hour window and extracted **242 residue findings** — facts, defects, owed work and
> corrections that exist in a commit message and nowhere else. It measured the master list at
> **≤ 45 rows** and declared Δ ≥ 197 the single largest unreconciled source in the tree. It did
> not triage them. **This document is the triage.**
>
> **The mechanism, stated once.** An agent lands a fix and records three more findings in the
> body — prominently, in good faith, because they could not fix them. Nobody reads 307 bodies.
> The finding is recorded, never actioned, and *looks handled because it is written down
> somewhere*. There is no gate that fails when a body says "owed" and no map row appears.
>
> **Read §2 first.** It is the OPEN-actionable list, severity-ordered, and it is the point.

---

## 0. THE CORPUS, AND HOW TO REPRODUCE IT

```sh
git log --since='2026-09-21 05:00' --oneline a197ad2f5 | wc -l          # -> 307
git log --since='2026-09-21 05:00' --reverse --pretty=format:'===COMMIT %h%n%s%n%b' a197ad2f5
#   first: 4241fb520   last: a197ad2f5   9,905 lines / 574 KB
```

All 307 bodies were read end to end for this triage. Every row below cites a SHA and the
sentence. **DONE rows were verified against the tree, not taken from the body that claimed
them** — a false DONE closes a real item invisibly, which is strictly worse than an OPEN.
Three claims did not survive that check and are recorded as such (§7).

### 0.1 THE FIVE COUNTS

| class | count | meaning |
|---|---:|---|
| **DONE** | **117** | a later commit closed it; SHA cited and verified against the tree. §5 |
| **OPEN, actionable** | **68** | real work nobody owns. **This is the deliverable.** §2 |
| **OPEN, blocked** | **6** | needs an owner ruling and is **not** already one of `1.6.0-owner-decisions.md`'s 21. §3 |
| **SUPERSEDED** | **26** | a later decision or measurement made it moot; the later one wins. §4 |
| **NOT A FINDING** | **20** | narration, a granted exemption with its reason, or a restatement of a mapped row. §6 |
| | **237** | **distinct items** |

**§10 is a SECOND, SEPARATE corpus of 31** — the LOST register `9b14f4f5a` wrote and `a07a7591e`
deleted. It is appended rather than merged: those 31 are **not** among the 237, and §7.3(2) said
recovering them was owed. Its counts are **21 DONE / 8 OPEN / 2 NOT A FINDING**.

**242 residue STATEMENTS in → 237 distinct ITEMS out.** Those are two different measurements of
one corpus and they are close by coincidence, not by derivation — **§7 says so explicitly and
refuses to fake the identity.** 32 statements collapsed into 12 items (§7.1) and six bodies carry
several findings apiece where the tally counts one (§7.2). A further **13 blocked questions are
already rows in `1.6.0-owner-decisions.md`** and are named in §3.1 rather than duplicated.

---

## 1. THE HEADLINE — AND A CORRECTION MADE IN THE HOUR IT TOOK TO WRITE THIS

**As first measured for this triage, `HEAD` did not compile, and the reason was a security fix
left uncommitted in the shared checkout. It was closed at `56e62a635` while this file was being
written. The finding is kept, corrected in place rather than quietly deleted, because the
correction is the best evidence this document has for its own thesis.**

### 1.1 WHAT WAS TRUE

`crates/busbar/src/main.rs:577-578` called two items that did not exist at `HEAD`:

```sh
git show 37951e062:crates/busbar/src/main.rs | sed -n '577,578p'
#   let secrets = root::transports::ConfiguredSecrets::new(resolver, refs);
#   let journal = root::transports::BookAccessJournal::new(book, &durability_token);

git show 37951e062:crates/busbar/src/root/transports.rs \
  | grep -c 'ConfiguredSecrets\|BookAccessJournal'      # -> 0
```

The definitions existed only in the working tree, where `git status` had read
`` M crates/busbar/src/root/transports.rs`` all session. They are the fix for `6d3bda8a3` —
*"**every TLS private-key read in production is unaudited** … This contradicts an explicit owner
ruling — that plugins cannot touch secrets because the kernel is in charge of auditing them."*
`233ca7a3e` had already named the hazard in its own body: *"ConfiguredSecrets is not at HEAD. It
arrives with another agent's in-flight work on `crates/busbar/src/{main.rs,root/transports.rs}` in
this shared checkout."* Nobody acted on that sentence for the rest of the session — which is the
mechanism this whole document is about.

### 1.2 WHAT IS TRUE NOW

```sh
git log --oneline -1 -- crates/busbar/src/root/transports.rs
#   56e62a635 root: commit the half of 2bb1e8531 I left behind — HEAD did not build

git show HEAD:crates/busbar/src/root/transports.rs \
  | grep -c 'ConfiguredSecrets\|BookAccessJournal'      # -> 8
git show HEAD:crates/busbar/src/root/transports.rs | grep -n 'impl AccessJournal for'
#   264:impl AccessJournal for BookAccessJournal<'_> {
```

`56e62a635` is *after* the pin this triage's input was taken at (`a197ad2f5` / `a0b808b79`), so it
is a post-pin closure like every other in §5. `main.rs` hands the journal to `provision_servers`,
which calls `record_access` per cert / key / client-CA. **The build break is closed and the TLS
private-key audit seam is live on the production path.** The row is `D117`.

### 1.3 WHY IT STAYS IN THE FILE

Three reasons, and the third is the point.

1. **A correction is recorded, never quietly fixed.** That rule is this tree's own — `74b8640dd`,
   `0a82aa4ec`, `027f8ae31`, `f3d15e652`, `6a0d55ffc`, `5b3ea85b0` and `df3c19c8a` all exist
   because an author wrote down that they had been wrong. Deleting the row would leave a reader
   unable to tell a finding that was never real from one that was closed.
2. **The class recurs and is not fixed.** `56e62a635`'s own subject — *"commit the half of
   `2bb1e8531` I left behind — HEAD did not build"* — is the third recorded instance in this
   window of a caller landing without its callee in a shared checkout, after `1b6c9d4ba` /
   `99dd34503` (a blanket `git add` sweeping three agents' work) and `1059d3c36` / `2573c9a98`
   (a commit staged from a tree that predated another and reverted it). `0ce782715` documented
   the hazard; nothing enforces it. There is no gate that fails when a symbol is referenced at
   `HEAD` and defined only in a worktree.
3. **This triage measured a shared tree and the tree moved under it — exactly as
   `1.6.0-map-proof.md` §0.1 warned it would.** That proof pinned every figure at
   `016422be7` for this reason. This file pins its *input* at `a197ad2f5` and checks its DONE rows
   against a moving `HEAD`, which is the honest trade for a triage whose whole job is to ask
   "is this still true?" — and the cost of that trade is one row that had to be corrected within
   the hour. It is recorded here in full rather than absorbed.

---

### 1.4 A SECOND CORRECTION — THREE OF THE SEVEN "BANK AUDITOR" ROWS WERE MISREAD, AND THE LEAD ROW WAS BACKWARDS

**§8.2's seven money rows were re-verified against the CODE AND ITS COMMENTS rather than against
the body that reported them. Four survive exactly as written. Three moved, and the one that moved
furthest was the one leading the list.**

| row | as written | re-verified verdict |
|---|---|---|
| **A2** | *"a card-less deployment serves the call and records NO ledger row"* | **REAL, STATED BACKWARDS.** The ledger accrual is NOT gated. What is gated is the METERING-SERIES row. Still OPEN, still **M**, and still a defect — but for the opposite reason to the one given. |
| **A3** | two admin reads price off the current card | **SURVIVES, exactly.** The tree's own test asserts the residual: `MONEY_READS` (`service_tests.rs:2827-2852`) carries two `resolves: false` rows — `get_group_usage` and `GET /keys/{id}/usage`, both reading `UsageLedger`, whose rows carry no price instant — beside `get_usage`'s `resolves: true`. |
| **A4** | a fourth unpinned `NANOS_PER_CENT` | **SURVIVES, exactly.** `busbar-kernel/src/cost.rs:58` `const NANOS_PER_CENT: u128 = 10_000_000;` — private, not imported, no assert. The other three ARE pinned (`units_voice.rs:164` and `units_a2a.rs:1216` are `const _: () = assert!(..)`; `kernel-budget/src/price.rs:24` is a `pub use` re-export). It is used at `cost.rs:745` inside `derive_spend_cents`, a live budget-gate path. Drift HAZARD, not a live wrong answer — the value agrees today. |
| **A5** | `units_voice.rs:1846/:1950` write `rate_card_version: 0` | **SURVIVES, exactly, and it is the hardest of the seven.** Both are DURABLE: `:1846` is the `PostingStamp` on `settle()`, `:1950` is `Amount` on the audit-chain `seal()`. Every sibling resolves it (`units_mcp.rs:1408,:1612` `card.rate_card_version()`; `units_a2a.rs:916,:1185`; `units_llm.rs:1610` `card_in_force(card, arrived.ms())`), and those siblings' own comments say a literal zero here *"was `HistorySeq::OPENING` — a real entry"* (`units_a2a.rs:913`, `units_mcp.rs:1405`). A confident wrong answer sealed into a hash chain. |
| **A6** | five billed reads in `busbar-voice-codec` silently zero | **CLOSED — a FALSE OPEN.** All five route through `read_count_u64` at HEAD, closed by `5cfe127d9` **12:16 on 2026-09-22 — before this triage's own pin**. Two smaller items survive (no guard test; five stale gate allowances). |
| **A7** | honesty tests in two of seven transports | **SURVIVES, stale by one.** It is **three** of seven at HEAD (http, sse, tcp); four uncovered (grpc, stdio, tls, ws). |
| **A8** | two `/metrics*` endpoints, two content-types | **SURVIVES, exactly, and the two ARE distinct served endpoints.** `GET /metrics` is the prometheus export sink (`export/prometheus.rs:22 METRICS_PATH`, `:128` → `metrics.rs:1029 "text/plain; version=0.0.4"`); `GET /metrics/hooks` is `hooks/scrape.rs:374,:387` → `"text/plain; version=0.0.4; charset=utf-8"`. The charset-bearing form is the Prometheus-canonical one, so the bare `/metrics` is the non-canonical of the pair. Cosmetic in consequence, not money — it is here on the *served byte* half of Tier 0, not the *money* half. |

**WHY A2 MATTERS BEYOND A2.** The sources it quotes were precise and the row was not. Every one of
them says **metering row**: `295435456` — *"`plane_host/govern.rs:226` wraps the **metering write**"*;
the rig — *"rate_card ABSENT → `GET /api/v1/admin/usage` → `by_model: []`, `total.requests: 0`"*;
`govern.rs`'s own comment — *"emits no **metering-series** row"*. The row paraphrased **metering row**
as **ledger row**, and the two are the tree's central distinction. Measured at HEAD, three lines
above the branch:

```sh
sed -n '89,100p' crates/busbar-llm/src/engine/usage.rs   # host.meter_ledger(..) — UNCONDITIONAL
sed -n '113,121p' crates/busbar-llm/src/engine/usage.rs  # if host.cost_pricing_enabled(..) { host.meter_series(..) }
grep -n 'requests\|tokens_\|priced_from_ms' crates/busbar-contract/src/records.rs | sed -n '1,9p'
#   MeteringRow carries COUNTS and a price INSTANT. It stores no money.
```

**And the defect survives the correction.** A gated money surface with the ledger still accruing
would be the design; a gated **count-bearing record** is not, and the owner ruled so on 2026-09-22.
`MeteringRow` stores no money — it stores tokens, requests and `priced_from_ms`, and `spend_micros`
is derived at read time. So the branch conditions *what happened* on *how it is priced*, which is
the inversion #43/#71/#77(3) exist to forbid. **#42 is untouched by this** and the ruling says so
outright: presence IS the billing switch, because billing is the READ.

**THE TRAP, NAMED.** The two gate sites carry long comments asserting the model the ruling
overturned, and those comments are the OLDER document (`c3da6d3f12`, 2026-09-20 vs `295435456`,
2026-09-22). Reading the code and believing its comment is exactly how a live, gating-RED,
owner-ruled defect reads as settled design. That is now row **A2a** rather than a footnote,
because it will mislead the next reader the same way.

**SCORE: six of the seven are real open work.** A6's headline claim was closed four hours before
this triage's own pin and was carried as OPEN anyway. One row had its mechanism inverted (A2), two
had a measured figure move (A6, A7), and four survive byte-for-byte (A3, A4, A5, A8). **§0.1's five
counts are unchanged**: no row left the OPEN column — A6 stays, at **XS + XS** instead of **S**, for
two residuals that are real and much smaller than the row that carried them. The list is shorter in
consequence and more accurate in fact, which is the outcome worth having.

---


## 2. OPEN, ACTIONABLE — 68 items, severity-ordered

Severity order is money · auth · secrets · a served byte first, regardless of when the sentence
was written. Sizes: **XS** < 1h · **S** a session · **M** a day · **L** multi-day · **XL** a wave.

### 2.1 TIER 0 — a served byte, and money a bank auditor would fail

> **A1 is not here.** It was the build break in §1 and it closed at `56e62a635` while this file
> was being written. It is **D117**. The item numbers below are unchanged so that a reference to
> `A7` means the same row tomorrow as today. **`A2`, `A6` and `A7` were corrected in place on
> re-verification (§1.4) and `A2a` was added beside `A2` rather than renumbering anything.**

| # | statement | evidence | size |
|---|---|---|---|
| **A2** | ⚠ **CORRECTED IN PLACE — see §1.4. The gated write is the METERING SERIES, not the ledger.** `plane_host/govern.rs:239` wraps `gov.record_metering(..)` in `if state.app.cost.pricing_enabled()`; `busbar-llm/src/engine/usage.rs:113` wraps `host.meter_series(..)` in `if host.cost_pricing_enabled(..)`. **The budget/ledger accrual is NOT gated at either site** — `usage.rs:93 host.meter_ledger(..)` runs unconditionally three lines above the branch (and drives the real `record_usage` on the budget chain, `plane_host/mod.rs:962`), and on the plane path `GET /keys/<id>/usage` reads the budget cell `try_admit` bumps one step before the meter, on a path with no card gate (`37b5576ce`, measured). **What goes quiet is `MeteringRow`, and that row carries COUNTS, not money**: `busbar-contract/src/records.rs:885-906` is `tokens_input/output/cache_read/cache_write`, `requests`, `billable_requests`, plus `priced_from_ms` so the READ resolves the dated card. That is precisely what makes it a defect rather than #42 working — the card may decide whether a read turns counts into money; it may not decide whether the counts are RECORDED. The ruling is made; the fix is not. Moves oracle cells → park, never bless. | `295435456` *"RULED 2026-09-22 … `plane_host/govern.rs:226` wraps the metering write in `if cost.pricing_enabled()` … **and that is the defect**"*, owner: *"planes always ledger"*, *"its a kernal default all planes run though. no tunring things on or off by plane"*, and *"**THE FIX IS NOT HERE.** It belongs in `busbar-kernel/src/plane_host/`"* · `eb3c54ed9` *"`engine/usage.rs`'s `if host.cost_pricing_enabled(..)`, **which is the literal branch #87 outlaws**"* · `37b5576ce`, `245cb1962` (the `h2-ledger-unconditional` rig is RED per plane, gating at `ci.yml:2149-2152`, no `continue-on-error`) · **#42 is untouched** — presence IS the billing switch, because billing is the READ (#43/#71 pricing-blind, #77(3) price is never stored) | **M** |
| **A2a** | **NEW, additive — the code comment at both gate sites asserts the model the owner ruled AGAINST, and it is the older document.** `usage.rs:105-112` says *"BILLING OFF ⇒ NO METERING ROW (DECISION #42) … the metering series is the billing-facing usage ledger, and #42 says an unbilled plane emits none … only the money surface goes quiet"*; `govern.rs:234-238` says the same (*"emits no metering-series row, exactly as the llm path does"*). Both were written `2026-09-20 20:49` (`c3da6d3f12`); the ruling that calls the branch a defect is `2026-09-22` (`295435456`). **A reader who trusts the comment concludes “ruled design” and closes a live item** — which is how A2 came to be stated backwards in the first place. Whatever the fix does to the branch, the two comments must not outlive the ruling that contradicts them. | `git blame -L 100,120 crates/busbar-llm/src/engine/usage.rs` → `c3da6d3f12` (2026-09-20) · `295435456` (2026-09-22) · the two gating rigs and #87's gate are both RED on the branch the comments defend | **XS** |
| **A3** | **`GET /keys/{id}/usage` and `GET /groups/{group}/usage` still derive off the CURRENT card, so after a rate-card edit two admin reads answer DIFFERENT MONEY for the same window.** Root cause: `UsageLedger`/`ModelTokens` carry no price instant. Owner ruled the fix (`spend_cents` removed outright); the work is unstarted. Old rows must still LOAD. | recorded **three times, three commits**: `5a385c5ef` *"after a card change **the two admin reads can answer different money for the same window**"* · `af7d2329b` *"STILL DIVERGENT, AND NAMED RATHER THAN HIDDEN"* · `2d74302ea` *"a ledger that cannot distinguish two price eras IS a wrong ledger"* · ruling `2fea27e6e`(2) · residual tracked at `service_tests.rs:2827 MONEY_READS` with `resolves=false` | **L** |
| **A4** | **`crates/busbar-kernel/src/cost.rs:58` holds a fourth, unpinned copy of `NANOS_PER_CENT`.** The other three agree by compile-time assertion; this one is neither imported nor asserted. Closing it is a dependency ruling (kernel→ledger edge, or hoist to `busbar-contract` under #40) — not a refactor. | `489b63ab1` *"busbar-kernel's cost.rs copy is NOT addressed here … Left for that move, not silently patched"* · `94594ad1d` *"One genuinely unpinned copy remains, `busbar-kernel/src/cost.rs:58`"* · measured at HEAD: `const NANOS_PER_CENT: u128 = 10_000_000;` | **S** + 1 ruling |
| **A5** | **`crates/busbar/src/root/units_voice.rs:1846` and `:1950` still write `rate_card_version: 0`.** Zero is `HistorySeq::OPENING`, a real entry number the audit chain digests — a confident wrong answer, not a missing one. a2a, mcp and llm were fixed; voice was skipped for the fold, and the fold has landed. | `aa81f48b0` *"The two voice sites (`units_voice.rs:1840`, `:1944`) carry the identical defect and are LATENT … Left alone on purpose: the streaming fold is mid-rename in that file."* · parent `0f7766c13` *"hardcoded to zero at all seven live posting sites … the hash chain attests a placeholder"* | **S** |
| **A6** | ⚠ **CORRECTED IN PLACE — see §1.4. The headline claim is CLOSED; carrying it as OPEN was a false OPEN.** ~~Five billed count reads in `busbar-voice-codec` still default to a silent zero~~ — closed by `5cfe127d9` (2026-09-22 **12:16**, i.e. *before* this triage's `a197ad2f5` pin, and 39 minutes before `bfe513714` 12:55, whose body this row quoted). Measured at HEAD: all five route through `crate::ir::usage::read_count_u64` — `ir/codec/mod.rs:815,:820`, `ir/codec/gemini/mod.rs:369,:391,:417` — which accepts a float-spelled integer (`ir/usage.rs:112-126`). **Two smaller things are genuinely open.** (i) `busbar-voice-codec` STILL has no float-count guard test, while the llm twin has five dedicated files (`busbar-llm-codec/src/{gemini,bedrock,anthropic,openai_responses,openai_chat}/tests/*float*_tests.rs`) — the "NO guard test at all" half of `bfe513714` survives. (ii) The five `PendingConversion` allowances in `xtask/src/gates/no_float_money.rs:373-403` are now STALE: their `why` still reads *"every billed read in it defaults to zero"*, which is false, and one needle (`fn u64_at`, `ir/codec/mod.rs:441`) no longer names a billed read at all — its four call sites are `content_index` and audio-mark millisecond fields (`:649,:650,:689,:693`), while the billed duration counter `audio_ms_in` is derived in `busbar-plane-streaming/src/plane.rs:332,:376`. **A stale allowance on a money gate reads as live debt and suppresses a needle nobody is watching — exactly A28's class.** The residual silent zero the seam's own doc names (*"every caller in this crate currently ends in `unwrap_or_default()`… Decision #81 rules that a refused count is a REFUSAL, never a zero"*) is **A10's**, not this row's. | `5cfe127d9` *"Sixteen billed leaf-op reads that still used the bare PATH form … are routed onto the seam — 11 in the llm handlers, **5 in the voice codecs, which gain the seam the crate lacked**"* · `bfe513714` *"the audio copy carries NO guard test at all — measured, zero"* (still true) | **XS** (test) + **XS** (allowance drain) |
| **A7** | ⚠ **CORRECTED IN PLACE — see §1.4. Stale by one transport: it is THREE of seven, not two.** `FrameMeta.bytes` is a billing figure (`busbar-contract/src/transport/wire.rs:159`) and a frame-meta-honesty test exists in **three** transports at HEAD — `busbar-transport-http/src/tests/mod.rs:2016` (`frame_meta_is_honest_on_the_frames_this_transport_emits`), `busbar-transport-sse/src/tests/mod.rs:624` and `busbar-transport-tcp/src/tests/mod.rs:457` (`frame_meta_honesty_catches_inflating_and_deflating_fixtures`). **FOUR transports carry a money quantity with no red-capable test: `grpc` (constructs `FrameMeta` at `server.rs:210,:257`, two test references, neither an honesty test), `stdio`, `tls`, `ws` (zero `FrameMeta` test references each).** The item survives; only its count moved. | `b10be6807` *"**frame-meta-honesty tests exist for only two of seven transports, and `FrameMeta.bytes` is a billing figure**"* — that figure was right when written and is stale now; **measured at HEAD: 3 of 7** | **M** |
| **A8** | **Two `/metrics*` endpoints serve different content-types.** Measured: `busbar-kernel/src/hooks/scrape.rs:387` = `"text/plain; version=0.0.4; charset=utf-8"`; `busbar-kernel/src/metrics.rs:1029 PROMETHEUS_CONTENT_TYPE` = `"text/plain; version=0.0.4"`. A served byte, caused by the 141-line hand-rolled second Prometheus renderer. | `df3c19c8a` *"two `/metrics*` endpoints answer with **DIFFERENT content-types**, because the hand-rolled second Prometheus renderer re-spells the literal"* · `1ca3d5481` (the renderer is one of the KIND TEST's eight indicted sites) | **S** |

### 2.2 TIER 1 — the money model (planned, written down, HELD)

| # | statement | evidence | size |
|---|---|---|---|
| **A9** | **One `price(ledger_slice, card_history) -> Money` called by the admin reads AND the budget gate.** Today `f` is implemented ~20 times across 5 crates; two reads disagreed today *for exactly that reason*. The plan exists (`docs/design/1.6.0-money-one-function.md`) and is HELD behind the streaming fold. ~5,700 unreached lines fall out as a consequence, not a goal. | `86ebf3044` *"Two reads cannot disagree when there is one function; **today they disagree because `f` is implemented ~20 times in 5 crates**"* · `f1616c530` (plan landed, HELD) · `1ddc2b16a` *"**plane crates carry 9 to 41 money references each where #43 says they carry none**, and every `root/units_*.rs` reimplements accrual, provenance and keying separately"* · `42bccd097` | **XL** |
| **A10** | **Perform the exact-count conversion wave (#81/#81a).** The type, its text parser and the gate landed additively; **no call site is converted.** 13 real counts are owed, the caps+rates change is written out as six exact hunks, and counts/caps/rates must rescale in ONE commit or every limit is wrong by 1e6. | `b40fe4056` *"**13 real counts owed to the conversion wave**"* and *"The conversion wave is specified in `1.6.0-exact-count-migration.md` and **deliberately not performed here**"* · `bfe513714` (§3a, six hunks, HELD) · `166e6f4f7` *"counts, caps and rates rescale in ONE commit"* | **L** |
| **A11** | **The budget gate must price on the hot path, behind a memo keyed on (ledger-slice identity, card-history epoch) — in memory, never a field on a persisted row.** Measure before building the memo. | `2fea27e6e`(1) *"a stale figure on a cap admits a request that should have been refused"* · `86ebf3044` *"The honest cost: the budget gate must price on the hot path."* | **M** |
| **A12** | **`RawTierRates`'s four `f64` tier fields and `blended_per_mtok()` sit on the billing carrier where `no-float-money` cannot reach them.** Extending the gate needs the type relocated to a boundary file first, the way #44's card-build conversion already is. | `21c048e51` *"extending `no-float-money`'s float scan to `billing.rs` (which I tried, and backed out) flags FIVE MORE floats on the carrier"* | **S** |
| **A13** | **B13: `main.rs` still calls `flush_budgets`/`flush_metering` a second, ungated time at two sites** (the mcp-stdio exit path, which never signals shutdown, and the listener shutdown tail). A narrow interleaving double-counts an in-flight portion. Parked because routing them through the shared gate changes shutdown durability on a money path. | `5e024c0b3` *"Narrow but real, and it is money, so it is parked rather than fixed"* | **S** |
| **A14** | **`one-pricing-site:fee-fields` is crate-scoped, so a second fee read elsewhere in `busbar-core-admin` is invisible.** Proven by a plant that was not caught. | `42bccd097` *"A second plant in another file of busbar-core-admin was NOT caught -- the fee-fields check is crate-scoped, a pre-existing limitation now live again"* | **S** |
| **A15** | **The DECLARED meter-class vocabulary and the EMITTED one are disjoint sets and no gate compares them.** `f1616c530` bound declare→emit to symbols in two planes; the cross-check does not exist. | `a7f5f913f` *"what IS live is that the declared and emitted vocabularies are disjoint sets no gate compares"* | **S** |

### 2.3 TIER 2 — audit and evidence integrity

| # | statement | evidence | size |
|---|---|---|---|
| **A16** | **`Book::retain_from` has zero non-test callers — the audit book grows unbounded — and the audit Book is never replayed at boot, breaking the reconciliation identity the durability module doc states as a release requirement.** Measured at HEAD: `busbar-kernel-ledger/src/totals.rs:264` plus test callers only. Masked until now by the restart bug that zeroed it anyway. | `76455a049` *"the audit Book is never replayed at boot … and `Book::retain_from` has zero non-test callers so the book grows unbounded"* | **M** |
| **A17** | **99 audit scopes are OPEN at HIGH/MEDIUM, and 33 register records name paths that are gone — each a `cargo xtask ledger move` owed by whoever owns that fold.** Deleting them is the same laundering pointed backwards. | `d37821b59` *"`ledger --check` reports the SAME **99 scopes open at HIGH/MEDIUM**"* · *"The **33 records for paths that are gone** are left ALONE … Each is a `ledger move` for whoever owns that fold"* · `f6ceb7d54` *"One `sync --write` DELETES 26 scopes' worth of open HIGH/MEDIUM audit findings and exits 0"* | **M**(moves) / **L**(the 99) |
| **A18** | **`audit-ledger` still has no rule for code that MOVED somewhere now unaudited.** `8eb528979` gave the tree the `ledger move` verb; it did not give the gate the ability to *detect* the laundering. | `433300ea6` *"The gate only checks for missing scopes. It has **no rule for code that moved somewhere now unaudited** … so the collapse can **zero the tree's audit coverage one honest-looking green at a time**."* | **M** |
| **A19** | **`docs/design/AUDIT-STATUS.md` differs from a regeneration off the UNCHANGED register by 354 lines.** | `d37821b59` *"Regenerating it from the UNCHANGED register already differs from the committed copy by 354 lines"* | **XS** |

### 2.4 TIER 3 — the plugin ABI, #11 and #84

| # | statement | evidence | size |
|---|---|---|---|
| **A20** | **Build the zero-busbar-import reference plugin. None has ever existed** — all six in-tree ABI fixtures depend on `busbar-plugin-sdk`, so none can be #84's witness. A seventh is owed. | `4617d8b79`/`6d6a9c62a` *"**no plugin built this way has ever existed.** All six in-tree plugins … depend on the SDK. The zero-import path the ABI was designed for has **never been demonstrated**."* · `a9a71b961` *"A seventh is owed."* | **M** |
| **A21** | **`busbar_plugin_sdk::dispatch_auth_enveloped` is OWED and is THE BLOCKER: `export` has the only enveloped dispatch, so `plane` and `export` are the tree's entire #11 evidence base — 2 of 7 kinds.** Carries `fn open` → `pub fn open` and `pub fn dispatch_compiled_in` as trailing work. | `14a49131d` *"`busbar_plugin_sdk::dispatch_auth_enveloped` **OWED (SDK) — THE BLOCKER**"* · *"So `plane` and `export` remain **the tree's entire #11 evidence base**"* | **M** |
| **A22** | **The four export sinks are four missing subsystems, not four crates.** Four prerequisites, each measured: (a) a host-side load/registry seam — `open_export` has zero callers outside loader tests and `ExportDispatch` is never constructed; (b) a delivery driver — nothing routes a delivery to a loaded sink; (c) a host-owned destination capability on the cold ABI; (d) for otlp, a span→traces producer. Four roster slots have no crate. | `18858591f` *"I ruled past my own spec"* (all four enumerated) · `1059d3c36` *"`DynExport::deliver` has zero production callers"* · `ee8e721ef` *"Four roster slots have no crate: `busbar-export-{prometheus,webhook,file}` exist only inside `busbar-kernel/src/export/`, and otlp has no implementation anywhere"* | **XL** |
| **A23** | **Rename `arena` out of the plugin-facing ABI, on a quiet tree, BEFORE the cut.** Measured at HEAD: `busbar-contract/src/unit.rs:538 pub fn arena(&self)`, plus `arena:` parameters at `spans.rs:35`, `unit.rs:482/494`, `transport/mod.rs:226`; 108 files name it. Brand-new in 1.6.0 (`v1.5.5` has no `fn arena` and no `PlaneAlloc`), so it costs no compatibility — today. | `4551d52d6` *"Once 1.6.0 ships, third-party plugins are written against `ctx.arena()` and a concept the architecture has explicitly retired becomes permanent in the contract. **The free window closes at the cut.**"* | **M**, TIME-BOXED |
| **A24** | **Retire `busbar-api` (#35/W5.b/#84 "the villain").** Three slices have landed (`40a47a8aa` records, `2b5017321` UpstreamCreds, `0209e13c0` Redacted+signal). The rest of its plugin-facing half is owed; the `("api","contract")` grant is marked TRANSITIONAL and disappears WITH the crate rather than being drained. | `4617d8b79` *"busbar-api is the villain. It is the one crate dragging the ledger onto the plugin path"* · `0ce782715`(2) · `6cf5790f6`, `250cda583` | **L** |
| **A25** | **`CallerToken` and `Principal` are each DEFINED TWICE — `busbar-api/src/auth.rs:69` and `busbar-kernel-identity/src/carrier.rs:42` — with two independent hand-written redacting `Debug`s and nothing forcing agreement.** Redact one and the other still prints the caller's bearer token. | `7d85fa5bc`(2) *"Independent definitions, not re-exports … the same shape as the verb table joined by `_ => \"\"`"* | **S** |
| **A26** | **#85's envelope is plugin→host, one way: the HOST sheds a sink's batch, and the sink is never called — so it cannot report the drop, cannot reconcile its counters against what shipped, and cannot tell a quiet period from a shed one.** An ABI gap; where the notification is delivered, how it is ordered, and what a plugin may do about it are all unsettled. | `2b5727416` *"That is an ABI gap, not a bug in the code below it, and closing it is a separate design … Recorded rather than invented."* | **M** |

### 2.5 TIER 4 — secrets

| # | statement | evidence | size |
|---|---|---|---|
| **A27** | **The AWS secret's real carrier is an unnamed tuple element, which NO needle set can reach — a SHAPE blindness the spelling fix cannot close.** `StoreResult<(VirtualKey, String, String, String)>` (`governance/state.rs:313`) destructured straight into a `json!` body (`core-admin/keys.rs:1042`). Exposure today is NIL and was checked, not assumed. The owed work is type debt: seven bare-credential structs and a `CallerToken` newtype `.0.clone()`d into a bare `Option<String>` at six sites. | `7d85fa5bc`(1) *"A tuple element HAS NO NAME TO SPELL, so no needle set of any width reaches it. The spelling gap is closed; the shape gap cannot be closed by that method at all."* · `9ffbf93fb` | **M** |
| **A28** | **`scripts/secret-hygiene-gate.sh` has no "did this row suppress anything?" check.** `check_allowlist_paths` asks only whether the path exists. Measured at HEAD: the gap is documented in the script's own header (line 155) and `access_token\|…schema.rs\|` still suppresses nothing (line 137). | `7d85fa5bc`(3) *"The weak half of staleness cannot catch a row whose subject moved out of a file that stayed. Only \"did this row suppress anything?\" catches it."* | **S** |

### 2.6 TIER 5 — instruments

| # | statement | evidence | size |
|---|---|---|---|
| **A29** | **Every gate owes a positive control.** Almost none has one — a gate run against a tree it was tuned on passes because it was shaped until it did. This is the cheapest defence against the failure mode behind every instrument defect this tree found. | `eb36d1695` *"**Almost no gate here has one** … **a gate that cannot be shown failing is not evidence**."* · the technique: *"KEEP ONE KNOWN INSTANCE OUT OF THE INSTRUMENT'S REACH AND CHECK THE INSTRUMENT AGAINST IT."* | **L** |
| **A30** | **Every source-scanning gate is blind to an act assembled from variables, and the two failure modes are in tension with no threshold between them.** `instance-noun-neutrality`, `plane-pricing-blindness`, `plane-purity`, the money-path scans and the LOC censuses are ALL source scans. The resolution is reachability over pattern, and testing BYTES where a rule must hold of bytes. | `959700d86` *"**ALL source scans, and every one is blind** to an act assembled from variables rather than spelled as a literal"* · `e542adfaf` *"they are in **TENSION** … **No threshold separates them**"* · `7d85fa5bc` | **L** |
| **A31** | **`construction:manifest-allowlist` reads DECLARED deps rather than the closure, despite its own text.** The crate-enrolment half of this finding is closed (`2b2de9712`, `ab5bcb45c`); this half is not. | `16892f965` *"`manifest-allowlist` is green because the violating crate is not in its list, **and it reads declared deps rather than the closure despite its own text**"* · `aad3cdd15` *"**NOT FIXED HERE, AND IT SHOULD BE.**"* | **S** |
| **A32** | **`plane-pricing-blindness` (#87) is RED on 15 findings across 10 files and its baseline ships EMPTY on purpose.** Every burndown row must say whether its violating line EXECUTES and cite the trace. | `eb3c54ed9` *"it reds on **16 findings across 11 files**"* · `8b5ba2acc` *"Price row 9 -> 8 files; 15 findings across 10 files remain, all real."* | **M** |
| **A33** | **`reachability` is RED on four undeclared rows: `unit-path:mcp` (declares no `impl .. Units for` at all), `unit-path:a2a`, `unit-path:streaming`, and `root-module` (`money_book.rs` and `vocabulary.rs` reached by nothing from `fn main()`).** Measured: `qa/reachability.toml` carries exactly one declared row. All four are blocked behind A9's fold. | `6c84dd01c`, `7e27cdc11` *"The other four findings stay RED and undeclared. All four are under active fold"* · `adda017ae` | **M** |
| **A34** | **454 broken rustdoc intra-doc links remain.** Tree-wide measurement was 463 unresolved + 12 ambiguous; nine were closed. | `18db4c42e` *"rustdoc `-D broken_intra_doc_links`, tree-wide: **463 unresolved + 12 ambiguous**. These nine are the ones this pass could close"* | **M** |
| **A35** | **`kind-isolation` debt: 25 dead-dep-edge findings owned by in-flight folds; `kind-isolation-ship` RED on 77 findings over 152 shipped edges; `kind-isolation:matrix` carrying 35,069 hits over 282 cells.** | `94cba7be6` *"`kind-isolation:deps` stays FAIL: 29 dead-dep-edge findings, of which 4 were mine … the other 25 belong to other in-flight folds"* · `aad3cdd15` *"still RED for unrelated reasons (**77 findings over 152 shipped edges**)"* · `e15bda24e` *"pre-existing vocabulary-matrix drift (35069 hits over 282 cells)"* | **L** |
| **A36** | **The `instance-noun-neutrality` per-noun census IS the plane-extraction burndown and it is RED by design: 28 rows, tree-wide counts of mcp 93 files / a2a 54 / llm 71 / streaming 87.** | `be22a9c9d` *"The per-noun census rows remain the tracked plane-extraction burndown (red by design)"* · `897abb424` (27→28, the `jev` row) · `236a68c45` *"that predates this fold and is the cross-plugin cleanup wave's"* | **XL** |
| **A37** | **`structure-lint:inline-tests`: the real backlog is 18 blocks / 851 lines and the gate names 3.** The largest single payload — `busbar-substrate-values/src/diagnostics/mod.rs`'s 3,945 `#[cfg(test)]` lines — is deliberately untouched pending the #83a split. | `feceea99a` *"the real backlog in `crates/` is 18 blocks / 851 lines, and the gate that already owns this rule names 3 of them"* · `07bf370af` · `e690b0834` (1 cleared) | **M** |
| **A38** | **`cargo fmt --all --check` is red on 11 files.** The four-batch sweep stopped at files other agents were editing. | `f4a7edb90` *"down from 248 hunks / 106 files to 6 hunks / 4 files, and all four remaining are files another agent is editing right now"* · measured now: 11 | **XS** |
| **A39** | **`plane-purity-strict`'s per-plane test-reach CEILING rows iterate the legacy four planes — a ratchet gap, not a ban gap.** The only real residue of a claim that was otherwise measured false. | `5b3ea85b0` *"The only real residue is plane-purity-strict's per-plane test-reach CEILING rows, which iterate the legacy four"* | **XS** |
| **A40** | **`construction:one-pick-site` is red, pre-existing, and has never been diagnosed.** | `2abb00dcd` *"one-pick-site, pre-existing and not yet diagnosed"* · `e15bda24e` | **S** |
| **A41** | **`kind-isolation:testkit` reports `not-run` for `busbar-store-example-plugin`**, because its conformance copy is at `src/tests/store_conformance.rs` and the rule matches a `{dir}/tests/` prefix. | `2846dcff5` *"The rule was left alone deliberately: widening its detection to make this commit pass would be weakening a gate to fit a change."* | **XS** |
| **A42** | **Two stale `instance-noun-neutrality` baseline rows and four `secret-hygiene` allowlist rows that are vacuous for a reason other than a dead path.** Both sets are named in-file and read as live. | `8c0d64e3a` *"The remaining 2 stale rows belong to other landings"* · `afe12ec00` *"the four that are vacuous for a different reason … are named in the file rather than left looking live"* | **XS** |
| **A43** | **Seven `kind-isolation` cell ceilings were STATED, not stamped, at the a2a-codec fold; a ceiling drain owns them under one declaration.** | `0e41eeb9e` *"They are NOT set here. Four of the seven were already unequal at HEAD … A ceiling drain owns them, with one declaration covering the whole drift."* | **S** |
| **A44** | **A roster SHRINK has no declaration form, the way `[gate.ceiling_raises]` gives a growth one.** Today the only mechanism is "the commit that owns the move names the cause in prose". | `236a68c45` *"A roster SHRINK has no declaration form … that gap is reported rather than guessed at."* | **S** |

### 2.7 TIER 6 — conformance and the release engine

| # | statement | evidence | size |
|---|---|---|---|
| **A45** | **`conformance check --musts` is RED on 17 rows: 10 STALE (a pass is honoured only at the release sha, so they run once on the frozen candidate) and 7 that have NEVER run — `ws`, `tls`, `h2spec`, `slsa-verifier`, `oidf-oauth2`, `jev`.** Any plan listing "make conformance green" as a mid-flight task is wrong by construction. | `21e1659f2` *"The other seven have genuinely never run and ARE buildable now … Those are the real conformance work."* | **L** |
| **A46** | **busbar has no WebSocket verdict. No Autobahn run has ever happened**, the in-tree subject is now buildable but Docker is absent and the Python `autobahntestsuite` is broken under Python 3. | `1124acb0f` *"**No Autobahn run happened** … busbar has **no WebSocket verdict yet**"* · `21e1659f2` *"`ws` is the worst of them — **a release whose fourth plane is streaming, carrying a WebSocket transport, with zero Autobahn coverage**"* · `b10be6807` | **M** |
| **A47** | **`TURNSTILE_DISPATCH_TOKEN` is still not provisioned**, so no candidate on this side has ever reached the turnstile. The workflow fails loudly by name rather than silently not dispatching. | `1536d3aae` *"`TURNSTILE_DISPATCH_TOKEN` is **still not provisioned**."* | **XS** (owner action) |
| **A48** | **A real container load of the plugin drop-in path is still owed (the dlopen fix is proven STATICALLY only), and two image gaps stay open: the documented `/etc/busbar/plugins` does not exist and `default_plugins_dir` is relative so it lands on `/plugins`; and the image runs as 65532 while an auto-created volume is root-owned.** | `7c0a381d1` *"this is labelled static proof, not a run"* · `55489d981` *"Two further gaps stay open and are written down rather than guessed at"* | **M** |
| **A49** | **Nine dependency bumps touching serialization/compression/TLS (`sonic-rs`, `flate2`, `rustls`, `tokio-rustls`, `rcgen`) MUST pass the byte-identity oracle before main; four more (`crc32fast`, `smallvec`, `indexmap`, `hyper`) are *believed* inert and unverified by the oracle either.** | `da69e9c65` *"MUST pass the byte-identity oracle before this reaches main; … believed inert … but unverified by the oracle either"* | **S** |
| **A50** | **The oracle engine owes three changes before any new-plane baseline is real: a cell with no prior golden must report UNBASELINED never PASS; gaps must reach the verdict; `accepted-differences` must stop refusing `missing.golden`.** Cross-repo (`busbar-release`). Until then an authored money cell is a silent no-op that can never fail. | `9a5b26d68` *"the oracle cannot be made to help, for three independent reasons in the engine"* · `b65f01613` (the standing rule that requires them) | **M** |
| **A51** | **Record a 1.6.0 golden at the release cut, turning 1,402 cells that structurally cannot fail into real coverage.** Honest ceiling: it cannot prove 1.5.5 parity; it locks today's behaviour so tomorrow's change is visible. | `2fea27e6e`(4) · `b65f01613` *"a cell is unbaselined exactly once, in the release that introduces it … The gap bites once per feature instead of forever."* | **L** |
| **A52** | **Un-normalise `as_of`/`start`/`end` in the oracle's `normalize.rs`, so a wrong billing window or rate-card epoch stops being structurally invisible.** Today's two-clocks defect was caught only because it ALSO moved a spend value; the next one that moves only the window passes green. | `2fea27e6e`(3) · `b98a6ddcf` *"A wrong billing window or rate-card epoch is structurally invisible."* | **S** |
| **A53** | **Corpus cells owed for 1.5.5 surfaces that LOST coverage: `plugins.load\|export-*` has no cell at all; 15 of 81 admin operations (every `agents`/`tools` verb) have none; 403 and 500 are declared on all 81 and recorded on none; only 6 of 20 served (dialect, op) pairs are covered, refusals only on the 6 diagonal pairs.** The export cells are blocked on a signed v2 artifact minted into `plugin-digests.tsv` with its digest pinned — corpus surgery under a pin. | `f7136b377` *"What the corpus IS owed is cells for surfaces that existed in 1.5.5 and lost coverage"* · `85da183c2` · `03791e495`/`18858591f` *"**No export plugin has ever been published** … So the claim **would have shipped on an assertion**"* | **L** |
| **A54** | **The witness is owed to the KIND, not the instance: one leg set, parameterised by plane, run against all five.** Measured: 5 planes, 2 have a rig at all, 10 leg names written twice once per instance, and 2 legs exist for ONE plane only. A witness hand-written per instance cannot make its instances uniform. | `be72bf69a` *"A plane that cannot pass it is not a plane — which is the only way \"a plugin is a plugin\" is checkable rather than aspirational."* | **L** |
| **A55** | **Fix `fetch-golden`'s `pinned()` reader upstream in `busbar-release` (skip short/comment lines) and bump the pin.** The in-tree de-commented copy is wiring, not the fix. | `e0ba287bf` *"The proper fix is in busbar-release … plus a pin bump; done here as wiring so the cutover proceeds without a cross-repo change."* | **XS** (cross-repo) |
| **A56** | **The done-oracle's TELLER-STEPS group is an honest `absent_step` naming an upstream gap: `rigs-ledger` exists nowhere as a CLI arm (proven three ways) and xtask deliberately has no equivalent, because driving the oracle's code is what the segregation gate forbids the gate runner from doing.** | `6aaed1398` *"So no entry point exists anywhere in this tree."* · `3caebd906` | **S** (cross-repo) |

### 2.8 TIER 7 — roster, size and the collapse residue

| # | statement | evidence | size |
|---|---|---|---|
| **A57** | **The #83a substrate split is roughly a third done.** `profile` moved byte-identically to the kernel; `breaker`, `eventstream`, `json`, `lossless`, `media`, `sigv4`, `transport`, `diagnostics` and `handlers` have not, and neither has the value-leaf half (`billing`, `media`, `wire`, `ir/*`) into `busbar-contract`. The kernel still carries `pub use` shims over the crate it is not allowed to depend on. | `07bf370af`/`bd6fba160` (the one module that could move today) · `f9b409941` *"#37's tier rule and the crate as it stands are mutually unsatisfiable"* · `084a5a4f6` *"**the tier rule is violated**: `busbar-kernel` takes a normal dependency on `busbar-substrate-values` … names it across 23 files"* · `ee8e721ef` (`json.rs` must NOT go to contract) | **XL** |
| **A58** | **SigV4 exists twice, completely, and the second copy has no production caller so nobody reviews it.** Measured at HEAD: `crates/busbar-substrate-values/src/sigv4.rs` and `crates/busbar-kernel-identity/src/egress_auth/sigv4/mod.rs`. | `df3c19c8a` *"**SigV4 exists twice, completely**, and the second copy has no production caller so nobody reviews it."* | **M** |
| **A59** | **`EgressAuthScheme` has no in-tree implementor outside test harnesses while core implements four schemes itself; the two auth-kind crates hold 237 lines and auth-kind code outside them is 12× larger, across 18 independent implementations.** | `df3c19c8a` | **L** |
| **A60** | **`AdminPlane` is still the public struct name of a crate the architecture says is not a plane.** The `impl Plane` face was removed (`ae4f22210`); the name was not. Measured: `crates/busbar-core-admin/src/admin_codec/mod.rs:93`. | `a39ec8633` *"The crate's public struct is still AdminPlane, which is the same category error one level down. Left deliberately"* | **XS** |
| **A61** | **`ARCHITECTURE.md` 1.4 and the kind-isolation gate still call `control` a kind the owner cancelled.** Measured: 10 narrow matches in `xtask/src/gates/kind_isolation.rs`. A checklist that outlives the decision it encodes will quietly re-litigate it. | `0bdaf5604` *"`ARCHITECTURE.md` 1.4 and **roughly twenty references in the kind-isolation gate still call `control` a kind**"* | **S** |
| **A62** | **The `busbar-voice` engine is still retiring into the streaming plane; the composition root still carries `plane-voice`/`root-voice` features and `mount_root_voice`; and `PLANE_ALIASES`'s `("voice","streams","busbar-plane-voice")` row is reported `alias-retired` and left standing because striking it changes how the gate reads the live engine's own `"voice"` plane key.** | `2bb1e8531` *"The rename is owed separately."* · `94cba7be6` *"it is left with the finding standing rather than guessed at"* | **M** |
| **A63** | **#39's ONE shell, not two, is still owed: `forward_with_pool_parsed_inner` has two callers and `route.rs` COPIED the shell around the core rather than calling it.** A design doc once asserted this was "Landed" over code that did not exist. | `74e0b1770` *"The deeper de-duplication #39 and this doc both want -- ONE shell, not two -- is still owed and is now named as owed rather than claimed as done."* | **M** |
| **A64** | **Production is 2.44× v1.5.5 excluding the four new planes — ~91,000 excess lines — and the cause is unexplained.** Misplaced kind-work accounts for ~15% (19,716 raw / 11,730 code, 6.1% of production). The owner's own hypothesis — 54 crates and major duplication — is the first thing to test and has not been. A fourth bucket owes a reason item by item, never measured against a total. | `d63fcd55e` *"Against the owner's standard this is a failing number"* · `df3c19c8a` *"**AND THE HYPOTHESIS FAILS ON MAGNITUDE** … Recorded as a negative result rather than padded"* · `86ebf3044` (SIZE) | **XL** |
| **A65** | **THE KIND TEST indicts eight sites and they are unworked: all four export sinks, the 141-line second Prometheus renderer in `hooks/scrape.rs`, four private dial stacks inside plane crates, and `serve_listener`'s raw `TcpListener`+axum bind — core's own data and admin listener.** *"If core binds its own socket instead of acquiring a carrier through the transport kind, the kind is decorative."* | `1ca3d5481` · `7a9143018` (the seam's SHAPE is settled: kind-agnostic and role-agnostic; its EXISTENCE is owner item 7) | **XL** |
| **A66** | **`busbar-kernel-budget` still asserts a hydration guarantee its constructors do not provide** — measured at HEAD in `Cargo.toml:10`, `src/lib.rs:44` and `src/cells.rs:12`, with no hydrate function anywhere in the crate. **And one narrow question stays open: `units_voice` builds a `VoiceNode`'s own door EMPTY and voice is wired from `main`.** | `0a82aa4ec` *"`busbar-kernel-budget` has **no hydration function at all**, both constructors build the cells empty, and two places … **assert a hydration guarantee those constructors do not provide**"* · *"One narrow question stays open"* | **S** |
| **A67** | **Six pre-existing test reds in the `busbar` package and five in `cargo test -p xtask --lib` (`gates::feature_sets` ×3, `gates::inventory_ref` ×2).** Both counts were recorded as "pre-existing, not mine" and nobody owns them. | `47f70d241` *"the same six pre-existing reds"* · `d37821b59` *"The five left are `gates::feature_sets` x3 and `gates::inventory_ref` x2"* | **S** |
| **A68** | **`crates/plugin-sdk/src/pack.rs:537` is contested — two readings disagree on whether `hex`'s error renders seed bytes.** Deferred by the fold that moved it; "tracked elsewhere" names no tracker. | `38db9d48e` *"It is contested … and it is tracked elsewhere. A fold is not the place to settle it."* | **XS** |
| **A69** | **The remaining `construction:hold-escapes` findings are real production sites — unrelated pre-existing debt nobody owns.** | `1ee9fcbe8` *"the remaining hold-escapes findings are real production sites, unrelated pre-existing debt this task did not ask to clear"* | **S** |


---

## 3. OPEN, BLOCKED — 6 items that need an owner ruling and are NOT already on the list

`docs/design/1.6.0-owner-decisions.md` was cross-checked item by item. These six are absent from it. The 13 that are **not** absent are in §3.1.

| # | the question | evidence |
|---|---|---|
| **B1** | **Is `getrandom -> libc` an I/O reach?** The `cpufeatures` half was ruled and landed as an edge exemption; `getrandom` was explicitly carved out and keeps two per-crate waivers (`busbar-plane-llm`, `busbar-plane-streaming`). Deleting the exemption is not an option and the waivers are the only thing keeping two pure planes green. | `4f26fa73c` *"`getrandom->libc` is explicitly NOT ruled on — the llm and mcp waivers cover that separately and it keeps its per-crate review."* · `a197ad2f5` |
| **B2** | **Is Cohere's `billed_units` already cache-exclusive?** Reading `cached_tokens` made a billed count and a cache read coexist for the first time, and the meter SUMS the lanes: 11 reported became 15 charged. The fix nets them, conservatively, but no captured body carries both fields so the semantics are a guess. **Money: busbar must never bill more than the provider reported.** | `5cfe127d9` *"whether `billed_units` is already cache-exclusive is OWNER-PARKED; busbar never bills more than the provider reported."* |
| **B3** | **Relocate the two LLM-dialect bridges out of kernel-level code, or accept them?** `lean-core` is RED on three sites left deliberately: `cost.rs`'s `"Prompt"` (the operator/customer-facing billing-dimension display label) and `plane_host/mod.rs`'s two genuine dialect concepts. Real architectural coupling the rule is right to flag; a wording change would hide what it does. | `be7ed30f2` *"real architectural coupling this rule is right to flag … worth an owner decision on relocating the bridge rather than a wording change that would hide what it does."* |
| **B4** | **Fold `fleet-auto-scale` (PR #120, `infra/fleet-auto-scale`) or drop it?** Confirmed not an ancestor and no superseding automation exists on dev — nothing invokes `ci-runners-down.sh` on a schedule at all. Real unlanded value; touches live EC2/IAM fleet-control automation. | `da69e9c65` *"It has real unlanded value but touches live EC2/IAM fleet-control automation, so it's flagged for owner review rather than folded in here."* |
| **B5** | **Sign or refuse the 13 diverging oracle cells.** A full corpus record/replay against the committed 1.5.5 golden reports 916 owed / 13 diverging / 289 accepted. All 13 belong to other work in flight — the audit `scheme` discriminator, the 1.6.0-new `amend-rate-history` verb, the plugin-sign fold, the TLS listener warning, the rate-card history. No golden was touched and nothing is blessed. | `679931c58` *"The 13 are PARKED for the owner and belong to other work in flight."* |
| **B6** | **May `busbar-core-admin` keep its `PlaneMeta`/`Plugin` declarations?** The entry face was removed; the declarations stayed because the claim they carry is what makes a second plane claiming `/api/v1/admin/*` on `http` a BOOT REFUSAL. Pulling the registration quietly retires that guard. | `ae4f22210` *"Pulling the registration would quietly retire that guard, which is a behaviour change and a separate decision."* |

### 3.1 ALREADY ON `1.6.0-owner-decisions.md` — 13 rows sourced from commit bodies, NOT duplicated here

| owner row | the commit bodies that are its source |
|---|---|
| **1** Is `cpufeatures -> libc` an I/O reach? | `c6f69b6d4`, `c0debeb4b`, `383f23875` — **and it is RULED**, at `4f26fa73c`/`a197ad2f5`, exactly as `1.6.0-map-proof.md` §1.1(3) says. The row is stale; M4's live open count is 20, not 21. |
| **2** May a crate of kind `kernel` name a plane? | `c6f69b6d4` (the `("kernel","plane")` research: the ship gate goes red on a fold the plan mandates, 71,949 non-test lines riding on it) |
| **3** Consolidate into one era? | `c6f69b6d4` (`MeteringDelta::priced_from_ms` already ships the two-era shape; the enforcement book is the only book without it) |
| **4** Ratify `busbar-contract` at its measurement? | `ee8e721ef`, `a9a71b961`, `1d3d299fd` |
| **5** Revisit the +192-LOC `PlaneDeclaration` recut? | `16892f965` (`ae0622694`, archived in the branch collapse, never merged) |
| **6** Plane ABI: reshape the trait or build the machinery? | `250cda583`, `16892f965` |
| **7** Does `kind: transport` get built for 1.6.0? | `874029471`, `7a9143018`, `a7f5f913f`, `df3c19c8a` |
| **8** Add `ExportRequest::Status` to the cold ABI? | `1059d3c36` |
| **9** Uncarded node: flat fee 0 or 1? | `295435456` (#42 says 0, `card_from_config` says 1 — both figures parked in §13), `a7f5f913f` |
| **10** Who writes the 16 plane-pricing-blindness verdicts? | `eb3c54ed9` |
| **12** Approve the a2a billed-byte accrual semantics? | `3a5c137d9`, `47bfb9383` |
| **13** Eleven ABI version constants → one? | `8d938fc26` *"Recorded separately and NOT ruled"* |
| **15** / **16** Six homeless crates; crates-or-definitions for the ±3-5 band | `ee8e721ef` (12 HOMELESS), `874029471` (33 → 39 is outside the bound; 24 → 25 per definition), `7227f6ad3` |

---

## 4. SUPERSEDED — 26. A later decision or measurement made it moot; the later one wins.

Where a pair exists, **the earlier finding is VOID, not open.** That is stated for each.

| # | the earlier statement | what supersedes it |
|---|---|---|
| **S1** | `0df9c2b10` — the float fix will turn the money oracle red; clearing it needs an owner re-record. | **VOID.** `43439c04a` measured every cell and golden: no float-valued token-count field exists at all. *"So the fix is oracle-neutral."* |
| **S2** | `6a0d55ffc` — the corpus should GAIN a float-encoded cell; recommendation is add it and accept the divergence. | **VOID.** `f7136b377` ruled the corpus is the wrong place; `70ad5e5fc` reframed it — money is `f(ledger, rate_card)`, so witness the two inputs, not the output. The guard that shipped instead is `3c321a31e`'s source scan plus `b40fe4056`'s shape-matching gate over ten money areas. |
| **S3** | `3a5c137d9`/`9fae9ee5b`/`f40b96bb4` — the oracle is structurally blind to plane money and the corpus is owed authored money cells per post-1.5.5 plane. | **CORRECTED and SUPERSEDED.** `9a5b26d68`: *"My grep looked only under `testing/` and missed `scripts/`. Two claims fall — a2a and mcp DO assert positive money and gate in CI today."* Then `f7136b377` (rigs, not corpus) and `be72bf69a` (the witness is owed to the KIND). The residual owed work is A53/A54, not corpus expansion. |
| **S4** | The claim, repeated across a dozen messages, that three plane gates scan zero bytes of the five `busbar-plane-*` crates. | **VOID — MEASURED FALSE.** `5b3ea85b0`: *"`plane-purity` resolves its population through `kind_isolation::plane_kind_src_roots` … `plane-purity:scan-denominator` PASSES."* The gate had been repaired before the claim was made. Residue is A39 only. |
| **S5** | `ee8e721ef`/`979176bc3` — `busbar-plane-streaming` is the DEAD fork; keep voice. | **VOID.** `7ca14571b`: *"I inferred stale from unwired … it is the NEWER code."* Streaming carries three fixes voice lacks, one a money-byte fix. Both directions originally given would have reintroduced it. |
| **S6** | `f9b409941` — #18's voice→streaming rename is not byte-identical: five constants change value and an `OpClassId` is a metering and audit key. | **RULED SAFE.** `1cc110dbd`, `94cba7be6`: `git ls-tree -r --name-only v1.5.5 \| grep -ic voice` is **0**, so no shipped ledger or audit row carries the old values and the rename cannot move a billed byte. |
| **S7** | The crate roster at 16 (`#39`), ~30, 28 (`979176bc3`), 35 (`a1553b3d7`), 34 (`ee8e721ef`). | **SUPERSEDED.** `7227f6ad3` — **33, owner-locked**, with a stated ±3-5 drift bound; a proposal moving it further is evidence the DEFINITIONS are wrong. |
| **S8** | `1ddc2b16a` #87 "planes always ledger" as a decision row of its own; `8d938fc26` #86; `166e6f4f7` #81a. | **FOLDED.** `440959966`: #87 → #43, #86 → #85, #81a → #81. *"a measurement that proves an existing rule is being violated belongs IN that rule, not beside it as a new one."* The measurements survive inside #43; the row does not. |
| **S9** | `a1553b3d7` — #33's "there is NO testkit" clause over-claims; it rested on five expressions of dislike and no kill. | **RULED.** `2846dcff5`: *"OWNER RULING, 2026-09-22: 'delete it. wtf is a testkit.'"* Deleted, with the Store conformance suite re-homed to both backends. |
| **S10** | `959700d86` "the twelfth instrument defect" · `b157be816` "the THIRTEENTH". | **VOID.** `5b3ea85b0`: *"Also **dropping the 'twelve instrument defects' tally. It was never enumerated, so nobody could audit it**"* — and it contained at least one bad entry. |
| **S11** | `6d6a9c62a`'s earlier form — "the SDK closure is exactly `busbar-contract`". | **SUPERSEDED.** `83cf15503`: a plugin's busbar closure is exactly `busbar-plugin-sdk`; the SDK's own busbar closure is **empty**. Contract and plugin-sdk MERGE. |
| **S12** | `ee8e721ef` — #83 moves ~2,311 surface lines INTO contract; ceiling 7,012 → ~9,750, ~13,100 with the merge. | **REVERSED.** `a9a71b961`: #84 sends the non-plugin two-thirds OUT — caps 3,019 + transport 1,082 + grammar 795 = **4,896 lines leave**; contract lands near 4,800 and the ceiling stops being the binding constraint. |
| **S13** | `6cf5790f6`/`250cda583` — split `busbar-contract`. | **DEMOTED then REVERSED.** `4617d8b79` (*"Protection first, tidiness later"*), then `83cf15503` — they MERGE, they do not split. |
| **S14** | `ee8e721ef`/`a9a71b961` — `contract_caps` is breached 7,426 vs 7,012; the +414 repair is owed either way. | **SUPERSEDED.** `1d3d299fd` deleted the second counter, put the PAIR on one number (both read 5,652), and re-stated the gap as **6,659 against 5,652** — which is owner row 4, not a repair. |
| **S15** | The `r5-money-m3` branch fix should be re-landed. | **VOID.** `0a82aa4ec`: *"the r5-money-m3 fix must NOT be re-landed as written. Its `Carried/HydratedSpend` stores a settled cents figure at boot, which stores a price and violates the locked model. Trunk's reprice-on-read is correct."* A rare case where the unmerged branch is wrong. |
| **S16** | `ee7efa5a6` — root's A2A leg is a LIVE horizontal privilege escalation between tenants. | **CORRECTED then FIXED.** `74b8640dd`/`027f8ae31`: latent, not reachable — `units_a2a` is named from `main.rs` once, to install scope entries. Fixed anyway at `27bcaac9e`. The rule it earned: *"a missing check is not a vulnerability until you have shown the line executes."* |
| **S17** | `76455a049` — PARK CRITICAL, the budget cap is bypassed on every restart for all four planes. | **CORRECTED.** `0a82aa4ec`: the main admission path DOES hydrate — `GovState::hydrate_budgets` runs from `appbuild.rs` in production. *"Downgraded from critical to open-and-narrow. No demonstrated cap bypass on the customer path."* Residue is A66. |
| **S18** | `3c533603f`/`bfe513714` — `Billing::Duration`'s `f64` is a LIVE money-path float reaching the ledger through `ledger_and_meter`. | **CORRECTED.** `f3d15e652`: *"THAT WAS WRONG … Nothing has been mis-billed, and this is a latent defect, not a live leak. The reachability was asserted, not proven — which is the failure this document warns about, committed by its own author."* Fixed anyway at `21c048e51` because unpriced today is the cheapest it will ever be. |
| **S19** | `874029471`/`1ca3d5481` — "4 of 4 protocols wrote their own private dial stack", MCP's at 8,233 lines. | **VOID — CENSUS KILLED IT.** `df3c19c8a`: the HTTP leg is genuinely shared through `engine::build_client` at 7 call sites; MCP's real residue is **~527 code lines, not 8,233**. |
| **S20** | Misplaced kind-work explains the size growth. | **NEGATIVE RESULT.** `df3c19c8a`: 19,716 raw / 11,730 code — **~15% of the excess, 6.1% of production**. *"Recorded as a negative result rather than padded, because otherwise the next reader assumes the fold plus this census closes the size question. It does not."* Residue is A64. |
| **S21** | The size ratios 2.08× / 2.90× / 2.60× / 2.19×, and "357 files / 74,660 lines of inline tests". | **ALL FOUR WRONG.** `cd96e5a31` (four measurements, four errors in different directions), `1d3d299fd` (one counter, in Rust, parsing), `d63fcd55e` (**2.44× excluding the new planes; the baseline itself had been 64% too large**). The phantom backlog is really 18 blocks / 851 lines. |
| **S22** | `874029471` recorded *"IDENTICAL, NOT ANALOGOUS"* as an owner ruling. | **UN-RULED.** `6381e6539`: *"AND I PUT WORDS IN HIS MOUTH … He said SIMILAR, and has now handed the one-mechanism question back explicitly."* Then settled by analysis at `7a9143018`. |
| **S23** | `874029471` property 6 — an exporter requests a carrier "like a plane". | **FENCED.** `6381e6539`: *"exports are NOT planes, they dont go over the teller loop."* No admit, no meter, no settle, no per-request lifecycle. |
| **S24** | The export score is 3 of 4 and `otlp` is missing. | **CORRECTED.** `a7f5f913f`: otlp is **MISPLACED, not missing** (a tracing-subscriber layer in `init_logging`), and the score is **0 of 4**, since none of the four are crates. |
| **S25** | The rate-card gap is LIVE. | **CORRECTED to LATENT.** `a7f5f913f`: no non-LLM declared class is emitted on any live path. What IS live is A15. |
| **S26** | `16892f965`/`ec21d27f0` — `ceiling-rose` reports ~123 raises that are not raises / 147 real ceiling rises. | **FIXED, twice over.** `aa51937ab` (123 → 41 → **7**, of which 0 are undeclared raises; both the base and the array-row keying were bugs) and `1d3d299fd`/`687afdfe1` (both `[gate.ceiling_raises]` blocks struck, everything re-pinned to measurement, `ceiling-rose` and `ceiling-slack` green). |


---

## 5. DONE — a later commit closed it

Every row was checked against the tree, not against the body that claimed it. Where a claim did
not survive that check it is in §7, not here.

### 5.1 Security and correctness

| # | the finding | closed by |
|---|---|---|
| D1 | NAT64/RFC 6052 SSRF bypass at the ADDRESS predicates — `resolve_and_pin` pinned the IMDS literal re-encoded | `ec21d27f0` |
| D2 | …and the HOST-STRING half, which was the reachable one: six guards kept their own `v6.to_ipv4()` and never reached the fixed predicate, including `ssrf_blocked_host`, the pre-flight for OAuth token endpoints and MCP tool-call ARGUMENT hosts | `a6fe1018b` — *"The earlier NAT64 fix (`ec21d27f0`) was incomplete, and the gap it left was the reachable half."* |
| D3 | The MCP argguard NAT64 canary, which carried its own flip instruction | `59815645b` |
| D4 | TIME-OF-CHECK/TIME-OF-USE SSRF: `tasks/update` answers were merged in after the one screen and never re-screened | `59815645b` |
| D5 | MCP confused deputy — an upstream could name a caller-only method on the response leg and have it minted as a genuine unit under the ORIGINAL CALLER's identity, budget and approval | `489b63ab1` |
| D6 | `redeem_plane_token` trait default `Ok(true)` — a backend with no ledger approved every replay of a single-use token | `ec21d27f0` |
| D7 | Unbounded `file:` secret read — a fifo/device/huge file could OOM the process | `ec21d27f0` |
| D8 | `percent_decode` slicing a `&str` by byte index — a `%` before a multibyte char panicked an unauthenticated oauth2 path | `ec21d27f0` |
| D9 | `HostCtx` use-after-free: `repr(C){ptr,generation,kind}` + RAII + liveness registry; call sites in the loader and the two benches | `ec21d27f0`, `e25234635`, `b498a3f81` |
| D10 | `Registry::decl` protocol confusion — the pointer fast path had no length check, so `"root"` out of `"rooted"` short-circuited to the wrong declaration: wrong codec, wrong auth, wrong verbs | `ec21d27f0` |
| D11 | gemini-live dialect rate parse: `.contains("rate=16000")` matched `"rate=160000"` — a **10× billing error** | `ec21d27f0` |
| D12 | Usage fell back to zero when the modality breakdown was absent while totals were stated — a silently unbilled turn; and the OpenAI-Realtime twin that metered a totals-only turn at ZERO | `ec21d27f0`, `5cfe127d9` |
| D13 | Tool-call arguments corrupted when a nested sub-object landed on a token boundary | `ec21d27f0` |
| D14 | The A2A push route had no bound on VOLUME — one valid token could drive unbounded durable audit writes and outbound webhook deliveries, turning busbar into an amplifier pointed at a third party | `fb18e7a75` |
| D15 | `POST /keys/{id}/rotate` built its idempotency subkey as a bare colon join of two caller-controlled strings, and looked up BEFORE any existence check — a crafted `(id, header)` pair was served another caller's cached rotate response, **which carries secret token material** | `35cdedaa3` |
| D16 | Audit digest collision: one pipe separator let attacker-shaped content move a field boundary, so two materially different audit facts hashed identically | `10d66a79f` |
| D17 | A credential cache HIT re-put its entry, so a credential under steady traffic never expired and REVOKING it did nothing — the busier the credential, the longer revocation was ignored | `6d9d6e88b` |
| D18 | `CredentialSlab` derived `Debug` over its raw buffer; `clear()` truncated rather than zeroized; no `Drop`, so a freed slab handed the allocator every credential byte it held | `6d9d6e88b`, `6c35e81db` |
| D19 | `Journal::append_scoped` stamped `ts: 0`, so every scoped row looked infinitely old and any retention cutoff deleted it immediately; the WAL `ChainBreak` twin dated to epoch 0 — *"the one record that says entries were lost here was the first thing lost"* | `6d9d6e88b`, `d8efb8c0c` |
| D20 | A failed durable audit write was dropped on the floor | `6d9d6e88b` |
| D21 | Verify sealed a destination set and dropped it while Route re-derived its own — a verify-then-act gap in the one place the sealed-capability design exists to close; `InFlight::insert` overwrote a duplicate unit key, orphaning a `HoldCell` so budget stayed reserved forever; `transport_overlaps` compared SNI byte-wise so two overlapping claims were judged disjoint | `7b622be1d` |
| D22 | `units_a2a::approve()` authorized the OPERATION class and never the AGENT | `27bcaac9e` |
| D23 | grpc hardcoded a 4 MiB cap and ignored `limits.request_body_max_bytes`; the http egress forwarded a chunked-plus-Content-Length request smuggling primitive rather than refusing it; TLS mapped a mid-session drop to `Closed`, identical to an orderly shutdown | `cdef7a0f2` |
| D24 | Three attacker-controlled MCP growth vectors — the raw tool name opening a durable hash-chain row BEFORE any length check, an unbounded per-task answer map, and an uncapped/undeduped subscription list | `3ee41762d` |
| D25 | `AdminRequest` derived `Debug` over the operator's live admin token (twice); `jwt_bearer::read_credential` interpolated an RS256 SIGNING KEY into a reachable error; `ClientIdentity` freed a private key intact; `A2aDraft` derived `Debug` over the caller's bearer | `6c35e81db`, `21cfd3b40` |
| D26 | D03 — a blank `schema_hash` recorded an approval AT the empty string and `dispatch_digest` offered the empty string for "no approval", so the two MATCHED and a tool dispatched against an approval of NOTHING | `0092659d9` |
| D27 | D21 — an `ask_caller[].method` outside the closed three was silently DROPPED, so a typo did not break the confirmation gate, it deleted it | `b94dfa9bf` |
| D28 | D04/D16 — a committed rewrite busbar could not read back dispatched the ORIGINALS (a redaction hook reports applied and is silently undone), in MCP, A2A and voice; and an undecodable demotion row silently un-quarantined a drifted upstream across a restart | `2c9c36902`, `32c2da106`, `732844550`; the voice twin landed post-pin (`busbar-voice/src/mount.rs:854`) |
| D29 | `ssrf_blocked_host` declared its own six-name `METADATA_HOSTS` INSIDE its body, shadowing the two-name const the dialing path reads — config validation refused `metadata.tencentyun.com`, the socket did not | `b0d232ebd`, `14dbf65ac` |
| D30 | A `Challenge` round walked past the approve and admit seats; admin auth churn evicted the data plane's identities; admin-tokens terminally rejected a credential addressed to a later chain arm; revocation was asked where the chain identified nobody; `admin_required_scope`'s query was not cut, so `POST /config/validate?x=1` demanded `full` where 1.5.5 served it read-only | `679931c58` |
| D31 | An explicit `"id": null` was classified as a notification and never answered; the RFC 8693 token-exchange body had no byte cap; an SSRF-refused dispatch was recorded as dispatched | `c789ba023` |
| D32 | `append_plane_record` did a blind insert, so a second writer silently replaced the first and the chain forked with the store saying nothing | `961718c18` |
| D33 | A TLS `close_notify` parked forever on a held writer lock; a raw TCP connect had no bound at either site; all WebSocket closes went out as `Close(None)` | `86392c161` |
| D34 | An empty Twilio `streamSid` passed the anti-forgery check vacuously and admitted frames from anyone; the negotiated `mediaFormat` was parsed and never checked, so a mismatched format was decoded AND TIMED under an assumed G.711 and a call's billed duration rolled up from it | `dd3bfbda9` |
| D35 | `apply_mutation_to_slot` upserted the durable row then appended the chain event with a bare `?` — one write ahead of a chain that never recorded the transition | `28dcc5fe5` |
| D36 | The opening-balance migration keyed a metering pool with a bare delimiter, so `("gpt/4","openai")` and `("gpt","4/openai")` landed on ONE balance holding their SUM — two customers' money in one bucket | `5725f038c` |

### 5.2 Money

| # | the finding | closed by |
|---|---|---|
| D37 | **The float-encoded token count.** `as_u64()` returns `None` for `27.0`, and `as_u64().unwrap_or(0)` was the house idiom in every dialect, so a real count of 27 was recorded as 0. Shipped in v1.5.5. Fixed at the read, lifted to one canonical reader, repointed across anthropic/bedrock/gemini/openai_chat/openai_responses, turned into a REFUSAL rather than a zero, and guarded by a shape-matching gate over ten money areas. | `1b6c9d4ba`, `834dc1836`, `812370e09`, `d5d9a0a63`, `3c321a31e`, `3c533603f`, `b40fe4056` |
| D38 | Six cache-tier reading defects (bedrock `cacheDetails`, cohere `cached_tokens`, openai_responses and openai_chat `cache_write_tokens`, OpenAI-Realtime totals-only, cohere truncated recovery) plus the hazard the cohere fix exposed: 11 reported tokens ledgering as 15 charged | `5cfe127d9` |
| D39 | `NANOS_PER_CENT` declared five times; four now agree by compile-time assertion or re-export | `a1019f041`, `489b63ab1` |
| D40 | **The stated ship blocker.** `GET /admin/usage` derived every row flat off the CURRENT card while `CHANGELOG.md` presented dated rate-card history as a shipped breaking change. Now resolved per posting through the deployment's dated history at `arrived_ms`, with the history installed at boot, the row carrying its own `priced_from_ms`, `?as_of` honoured, and a seq above the head refused. | `fc7e8837c`, `9370836d3`, `af7d2329b`, `9ba9b2c11`, `d407263d7` — closes `ea94ce933` *"it cannot ship as it stands"* and `48ea84225`'s D-3 |
| D41 | The two-clocks defect: `record_metering` took `now` in SECONDS and a card apply recorded `now_ms()`, so a response served at T.900 priced against the card published at T.600. Measured through the real endpoint: 52,500,000 → 277,530,000. | `d407263d7`, `2d74302ea` |
| D42 | `rate_card_version: 0` at the a2a, mcp and llm posting sites — zero is `HistorySeq::OPENING`, a real entry number, so every posting claimed the opening card had priced it | `47bfb9383`, `dcbb2dc1f`, `aa81f48b0` (voice remains → **A5**) |
| D43 | `UnitKey::new(0)` filed every unit under one key, at a2a and at its mcp twin | `47bfb9383`, `dcbb2dc1f` |
| D44 | The a2a route step accrued a raw byte COUNT into an `AccrualMeter` denominated in nanos — a budget that never ran down | `47bfb9383` |
| D45 | `GET /admin/ledger/totals` published the BOOK'S BALANCE as a price, so an appended rate row could never reach it and an operator who had mispriced a lane had no read-time remedy | `4e01aebec` |
| D46 | `Billing::Duration { seconds: f64 }` — measured, the carrier held `12.099999999999999644728632119950` for 12.1 s; more than one product in eight already differed from exact | `21c048e51` |
| D47 | `scripts/{a2a,mcp}-subject/h2-lib.sh` wrote no `rate_card:` at all, so twelve already-gating rigs were billing-OFF by construction and a priced meter class was unreachable | `9a5b26d68` → `37b5576ce`, `30a2926b7`, `245cb1962` |
| D48 | `h2_validate_card` matched `*"config valid"*`, which is a substring of `"config validation failed"` — a false green on the only question that helper exists to ask | `7a217f250` |
| D49 | `root::durability::node_book()` hard-coded `data_dir: None` and a `NullShipper`, and no opening balance was ever sealed — every settlement measured its residual from a checkpoint that was never written | `3c57b7e7e` |
| D50 | The money model stated in the owner's own terms, and the rulings that fix its vocabulary: #79 dated rate cards, #80 the oracle cutover, #81/#81a exact fractional units, #82 core signs and exposes, #83 crate definitions, #84 the plugin closure, #85 the observability envelope, and the standing per-release baseline rule | `3080720e0`, `5caf5fea7`, `0d1043da1`, `166e6f4f7`, `ce5e0fd96`, `1cc110dbd`, `4617d8b79`, `83cf15503`, `8d938fc26`, `7227f6ad3`, `b65f01613` |

### 5.3 Audit chain and #82

| # | the finding | closed by |
|---|---|---|
| D51 | The chain was tamper-evident but UNSIGNED, so it proved only that a file the operator fully controls agrees with itself — and a signature cannot be added later | `834f62f49` |
| D52 | The three chain reads resolved on the closed table and then refused, falling past both executor branches into the legacy catch-all | `b1a448454`, `47f70d241` |
| D53 | The closed verb table was DECLARED in one crate and COMPLETED in another, joined by `verb_name(v) == row.verb` over a `_ => ""` arm — a row with no arm resolved to the empty string with no compile error. Now a `const` assertion in both directions. | `47f70d241` |
| D54 | `scripts/verify-audit-chain.py` deleted and the one property it bought ported to Rust — a check that reads ONLY the published artifacts and is forbidden from reaching the code it checks | `2bdd3c743` |
| D55 | `ledger sync --write` launders audited code into unaudited scopes — priced at **26 scopes' worth of open HIGH/MEDIUM findings deleted, exit 0** — and answered with `cargo xtask ledger move` + `hashed_as`, with the seven missing scopes classified from history first | `8eb528979`, `d37821b59`, `f6ceb7d54` (the GATE rule remains → **A18**) |

### 5.4 Instruments and gates

| # | the finding | closed by |
|---|---|---|
| D56 | `ceiling-rose` measured against a hardcoded branch that had been renamed away, then against one that had DIVERGED by 88 commits, and keyed array rows by ORDINAL so a struck row made every row below it compare against an unrelated one: 123 raises → 41 → **7**, none undeclared | `aa51937ab` |
| D57 | Three line counters, three answers. `scripts/loc-surface.py` billed **192,813 lines** of nested `src/*/tests/` as production surface — more than the tree's real production code — and dropped `#[cfg(not(test))]`, the arm that SHIPS. Replaced by one parsing counter in Rust. | `1d3d299fd`, `d63fcd55e` |
| D58 | Two `[gate.ceiling_raises]` blocks in one file, silently merged, the second overwriting the first on five shared keys — the reviewed text and the read text were different text | `1d3d299fd` |
| D59 | `surface-ceiling:grammar` named a retired CRATE and so summed nothing and passed; `arena` named two files that exist nowhere | `1d3d299fd` |
| D60 | Two ceiling rows no plant could ever red, because they asked the filesystem for a directory an overlay cannot create | `dacbe1464`, `687afdfe1` |
| D61 | **The fifth plane was in NO `[gate.plugin_kinds]` list**, so `source-denylist` and `forbid-unsafe` reported green over it — proven by a positive control, not inferred | `2b2de9712`, `7f2a7f77c`, `ab5bcb45c` |
| D62 | …and enrolling it surfaced a real violation: the only pure plane naming `busbar-api`. The edge was DELETED, not waived, by moving `UpstreamCreds` to the contract | `2b5017321`, `6ec0c2dff`, `c915dd264` |
| D63 | **The AUTH KIND was unscanned too** — `[rules.source-denylist].kinds` read `pure_auth`/`egress_auth`, names no `[gate.plugin_kinds]` key holds, so both resolved to an EMPTY glob and an empty population is the passing answer to every ban | `383f23875`, `ab5bcb45c` |
| D64 | `[rules.forbid-unsafe].forbid_kinds` carried the same two dead names, so neither auth crate was ever held to `#![forbid(unsafe_code)]` — measured by the absence of their rows, not inferred | `ab5bcb45c` |
| D65 | The `export` faces rule derived `busbar_contract::kinds::Export` from the kind word — a FALSE FRIEND (WAL/journal shipping) with zero implementors, so the trait a sink MUST implement went unchecked; and `export = ["crates/export-*"]` would never have matched `crates/busbar-export-*` | `2b5727416` |
| D66 | `cpufeatures -> libc` restated once per rediscovery as a per-crate waiver. Measured at `a197ad2f5^`: **six `[[allow]]` rows, all `dep = "libc"`.** Ruled once as a property of the EDGE, keyed on `(dep, via)` with an enumerated symbol list, and checked both ways | `4f26fa73c`, `a197ad2f5` |
| D67 | `construction:manifest-allowlist` never scanned the only crate violating #40, because `[gate.plugin_kinds].plane` did not list it; and the violation itself — 129 lines of provably dead `PLANE_DECL` — was deleted | `aad3cdd15`, `2b2de9712` (the declared-deps-not-closure half remains → **A31**) |
| D68 | The decision plane's `invariance.rs` and `purity.rs`, Part 7's named "#40's only automated witness, red by design", both flipped green | `aad3cdd15` |
| D69 | Nine xtask gates and one test scanned DELETED paths as zero files — which passes every ban silently — plus a second and third sweep for the same shape in scripts, workflows, `qa/` and `xtask/src` | `d12e1f858`, `afe12ec00`, `fe689c616`, `b106a13f2`, `42bccd097`, `672535a64`, `8c0d64e3a`, `d5b3e1723` |
| D70 | **`scripts/txn-fence.sh`'s PASS CONDITION IS A BUILD FAILURE**, and it ran `-p busbar-core` — a package that had not existed for days — so cargo's "did not match any packages" read as the fence holding, over a crate that was never compiled. A gate now derives the resolvable set and asserts it in three unambiguous executable positions. | `fe689c616`, `ef092719a`, `47eb26b7d` |
| D71 | `NEUTRAL_ROOTS` named two deleted crates, so the walk reached 10 files and the gate had been silent for 17 days about `busbar-substrate-values` and `busbar-contract`; its own non-vacuity floor is what finally said so | `b42a8922d` |
| D72 | `secret-hygiene-gate.sh` carried five allowlist rows at deleted paths, counting four documented intentional exceptions as violations; and Check 1 compared field names by EQUALITY, so the one field literally named "secret access key" scored zero behind a vendor prefix — the gate ran green over the field it was built to catch | `afe12ec00`, `9ffbf93fb`, `d5b3e1723` |
| D73 | `teller-step-order` (MONEY-SACRED) false-positived on `HoldCell::admit` colliding with the `Units::admit` step and on the `RouteAwait` indirection | `ca10018c4` → `770d99e12` |
| D74 | `conformance-sync` was registered and appeared in no workflow step — and that red was blocking the done-oracle's BUILD group, so the whole 22-group readout was gated behind it. Wired to CI, because what it guards is this repository asserting a conformance result it does not have. | `ae973f0ad` |
| D75 | Four done-oracle groups red or mute for HARNESS reasons: `TELLER-STEPS` claiming a path through `bin/oracle` that exists nowhere, `CONFIG_NOUN_FLOOR` at 16 with a comment claiming 19 (both wrong; the measured residual is 17), `KERNEL`'s count of 2 (correct, now explained), and `PLANE-DELETE` enumerating four planes against a locked five | `6aaed1398`, `3caebd906` |
| D76 | `verify-1.6.0-done.sh` could render NO VERDICT AT ALL — 22 `begin_group` blocks against `DONE_GROUPS_DECLARED=21` made `floor_is_honest` refuse before scoring anything — plus five un-gameability holes (`SKIP_BOOT_LEG` and four `BUSBAR_ORACLE_*` repointers) | `ec21d27f0`, `6c84dd01c` (23 declared, 23 defined) |
| D77 | `qa/reachability.toml` shipping EMPTY so the landing commit could not pre-suppress the gate's own findings; and the `decision` declaration lifted out of a shell comment into a row that EXPIRES the moment the plane is registered | `6c84dd01c`, `7e27cdc11` |
| D78 | The #87 gate flagged six lines of a `#[cfg(test)]`-declared money DOUBLE as production pricing — nothing about its path said so. Scope is now read from the module declaration's attribute. | `8b5ba2acc` |
| D79 | `workspace-deps` descended into `.claude/worktrees/` — **114 worktrees, ~14 GB** — ran past its 300 s ceiling at 9.3 GB resident and read as a hang; and silently cost every other gate sharing the walk | `5b8267b01` |
| D80 | The reaper could see neither runaways (two `target/debug/deps/cli-*` at ~590% CPU for 52 minutes while `pgrep rustc/cargo/release-busbar` all returned zero) nor abandonment (a python3 at 97% CPU, ppid 1, 63 core-hours, writing into a session that ended five days earlier) — **and it had never been SCHEDULED** | `13a6c434f`, `f48aeb1eb` |
| D81 | `ceiling-census` red on seven real census problems, every decrement now naming the fold or rename it belongs to | `ab5bcb45c` |

### 5.5 Oracle

| # | the finding | closed by |
|---|---|---|
| D82 | All 33 script-driven cells recorded NOTHING in the Rust re-record because every driver shelled out to a Python file the Rust tool dir never contained — and each driver's `fail` wrote `status: -1`, which the ledger scored as a NAMED GAP, so 33 harness failures read as corpus gaps and the ledger stayed green | `abe7dbc03`, `05ea02ef6` |
| D83 | The recorder FABRICATED MONEY DATA and scored it PASS under a port collision, ADOPTED another process's busbar (minting keys into someone else's admin API), and invented ~200 `effects.stderr` divergences by handing Python `re.sub` templates to Rust's regex | `f2efe6390`, `f3ef50e14` |
| D84 | 1,380 fabricated FAILs per local run from driverless cells routed to the LLM request builder — the first instrument defect that reports RED over nothing — and the crate's own inventory of what is NOT ported had nothing tying it to the code | `b157be816`, `fdce526ef` |
| D85 | `bin/oracle record --out <dir>` printed that the basename was honoured and wrote elsewhere; `--filter` was discarded by `into_config()`; the shim looked for the built engine at a hardcoded path that is wrong under any `CARGO_TARGET_DIR`; and a money-reconciliation test hunted a Python venv retired at the Phase C cutover | `0d3490bfb`, `b77bd60b4` |
| D86 | `testing/shadow-oracle/cells/__init__.py` — the 13th, uncounted Python file of a judge declared "100% Rust" and the authoritative generator of the 2,318-cell corpus — meant `cells --check` validated the committed file against itself: corpus drift was not being checked at all | `692ef8afd`, `524cfc58a` |
| D87 | The pinned Rust `fetch-golden` reader aborted on the pin table's first comment line and exited 3 (worked around in `bin/oracle`; the upstream fix is **A55**) | `e0ba287bf` |
| D88 | The cdylib the six demotion batteries dlopen was never built by `cargo test -p busbar-kernel`, so a verdict depended on whether somebody remembered a second command; and `newest_source_under` returned `None` from an unfinished walk | `c047964dc` |

### 5.6 Structure, docs and the release path

| # | the finding | closed by |
|---|---|---|
| D89 | The 61→49 crate collapse's landed half: both `-host` crates, `busbar-grammar`, `busbar-admin`+`busbar-plane-admin`, `busbar-plugin-sign`, `busbar-plugin-pack`, `busbar-plugin-testkit`, `busbar-plane-voice`, `busbar-mcp-codec`, `busbar-a2a-codec`, `busbar-core-{hooks,config}` — each with test counts measured in both directions and the audit record CARRIED, never laundered | `2a4d39c80`, `284f289a8`, `92e823cd5`, `a39ec8633`, `d81a76f7f`, `38db9d48e`, `2846dcff5`, `fbead1a31`, `5fbd891e0`, `0e41eeb9e`, `50d14fd44` |
| D90 | #40 violated transitively by `busbar-grammar` and by seven crates including the money one-book — a third-party store plugin transitively linked `busbar-kernel-ledger`. The record SHAPES moved to the contract; the closure is now 5 and the money book is not one of them. | `6ec735807`, `284f289a8`, `6cf5790f6`, `250cda583`, `40a47a8aa` |
| D91 | `busbar-core-admin` implemented the `Plane` entry face — a category error, not a homeless impl — and a dead `qa/` row steered the fix at the `control` kind the owner CANCELLED | `ae4f22210`, `0bdaf5604` |
| D92 | `kind-isolation:plane-steps` red on `busbar-plane-{a2a,mcp}-host` missing seven steps — **moot**: measured at HEAD, neither directory exists | `2a4d39c80`, `e15bda24e` |
| D93 | `core` PANICKED when no plane was registered, which #1/#3/#11 make a legal configuration; removing the ambient registration exposed 99 failing unit tests, taken to 0 without restoring it | `674831817`, `5f90985f3` |
| D94 | The #41 Arena→Scratch rename left `busbar-kernel` uncompilable in three places and the ws-conformance subject importing three types that do not exist — and the rename correctly STOPS at the wire | `1772c7470`, `a1019f041`, `a6fe1018b`, `1124acb0f` |
| D95 | `testing/ws-conformance/subject` was never a workspace member, so the exact command the runner invokes could never succeed, Docker or no Docker | `1124acb0f` |
| D96 | `docs/migration-1.6.md` §7 claimed `?as_of` on the legacy endpoint and two routes that are not routes; `ARCHITECTURE.md` Appendix B stopped at PB-102 while seven oracle cells and a register entry cite PB-103; `sched-oracle-store-cells.yml` invoked four files deleted on 2026-09-06 | `1b01e7753` |
| D97 | `1.6.0-llm-switch-over.md` asserted a fix as "Landed" over a test that exists nowhere while the regression was live — *"A document claiming 'Landed' over absent code is worse than no document"* | `74e0b1770` (the de-duplication itself remains → **A63**) |
| D98 | The duplication census was cited by the spec as "full evidence — do not re-derive these" and was UNTRACKED; and `1.6.0-STATUS.md` / `1.6.0-owner-decisions-queue.md` read as current by mtime while contradicting locked #19 | `446537ad9`; both stale docs measured ABSENT at HEAD (deleted post-pin at `0f22d6b61`) |
| D99 | `gate-mutants` was a REQUIRED status check on qa and main while being `workflow_dispatch`-only and `disabled_manually`, so neither branch could ever go green — and it was 72.86% of a measured $1,710.23 7-day CI bill | `1409ff911` |
| D100 | `release-stage.yml` and `prepare-release.yml` selected deleted packages for the OpenAPI emit — jobs that could only ever have failed at the moment of cutting a release | `fe689c616` |
| D101 | The plain Docker image could not `dlopen` a user-supplied plugin — for a release named Protocols as Plugins — because the three musl runtime libs landed only in the bundled variant; plus an orphaned per-run artifact upload | `7c0a381d1`, `55489d981` (a real container load remains → **A48**) |
| D102 | `hooks-ranking` was not forwarded to `busbar-admin` after the crate extraction, leaving `GetInfo /build/hook_plugins` permanently empty | `97f1caac0` |
| D103 | `qa-security.yml` carried `issues: write` purely so an action could self-file an issue per advisory — the exact anti-pattern `no-self-filed-issues` exists to refuse | `d12e1f858` |
| D104 | CI compute waste: `fmt` split out behind a preflight barrier, ONE shared release binary for three consumers (~30 xlarge-minutes/run), `max-parallel` on five matrices, the qa-push conformance burst serialised behind a green compile, and a single-job oracle-proof lane | `32f2c3297`, `f91310ed3`, `daf1664d9`, `04cb9624e`, `0ecaf2cd4`, `fe96539cc` |
| D105 | The turnstile firing step existed only in an unpushed local worktree, so no candidate on this side had EVER reached the turnstile | `1536d3aae` (the token remains → **A47**) |
| D106 | `oracle-rerecord.yml` defaulted its ref, so a force-updated branch would silently re-record the money reference golden against the wrong tree while still claiming to be the 1.5.5 golden | `3caebd906` |
| D107 | The MCP JSON-RPC error table and `_meta` keys, and the A2A error band and detail tags, each had two spellings in one crate once the dialect came home — and the tests guarding them had degenerated to `X == X` | `4ea7ddbdc`, `10b4aea68` |
| D108 | `busbar-plane-streaming` described itself as "a thin identity over the busbar-plane-voice duplex adapter" in PUBLISHED metadata, and stated a closure that included the crate it had deleted | `236a68c45` |
| D109 | `busbar-plane-llm` and `busbar-plane-streaming` re-spelled declared meter class names as string literals inside `meter()` — #71's obligation held by convention; and streaming lacked the `the_plane_names_no_kernel_side_crate` forbid test all four siblings carry | `f1616c530` |
| D110 | `secret_ref_coverage` red on `ConfiguredSecrets`, a type the inventory had never seen — established NOT a security gap before anything was written down | `233ca7a3e` |
| D111 | `structure-lint:oversized` on `busbar-core-admin/src/v1/service.rs` (2,723 vs a 2,500 cap): the `impl AdminService` block carved out as a `#[path]` CHILD, 1,570 lines with zero insertions, and the money register repointed to follow the reads. `crates/busbar/src/main.rs` now measures **2,035** and is under the cap. | `6ab0bab93`, `4115fc5fb`, `2bb1e8531` |
| D112 | `structure-lint:inline-tests` on `busbar-oauth2/src/routes.rs`, moved byte-identically with the SAME 35 test names before and after | `e690b0834`, `feceea99a` |
| D113 | Post-absorption gate drift: `config-stability`, `design-bindings`, `structure-lint`, `feature-sets`, `openapi-schema`, `kind-isolation`, `construction`, `instance-noun-neutrality` and `plane-abi-neutrality` all repointed at where the content actually moved, each verified RED before and GREEN after | `cdd36465c`, `39e443c82`, `6cc548e22`, `e94e7e176`, `56d9da97a`, `28cc77f08`, `ca10018c4`, `e15bda24e`, `4e3b2b9c8`, `f5a427d5b`, `49ab4aca2`, `be22a9c9d` |
| D114 | The `openapi-schema` feature build broken at HEAD (`PlaneDecl.openapi_schemas` missing from busbar-llm/-mcp/-a2a) and the `?as_of` row owed in `openapi.json`. **Verified at HEAD: `busbar-llm/src/lib.rs:262` carries the field, mcp and a2a carry it, and `as_of` appears 10 times in the committed `openapi.json`.** | `9ba9b2c11` + a post-pin regeneration |
| D115 | The shared-checkout commit hazards that cost three agents' work: `git add -A` taking a staged rename, `commit -o` taking worktree content, and the `commit-tree`+`update-ref` footgun that leaves the shared index holding an UNDO of the commit just made — all three written into `CONTRIBUTING.md` with detection and fix | `0ce782715`, from `1b6c9d4ba`, `99dd34503`, `2573c9a98` |
| D117 | **`HEAD` did not compile.** `crates/busbar/src/main.rs:577-578` referenced `ConfiguredSecrets` and `BookAccessJournal`, defined only in an uncommitted working tree — and those definitions are the audit seam for TLS private-key reads, the fix for `6d3bda8a3`'s contradiction of an owner ruling. Closed **during this triage**; see §1 for the before/after measurement and why the row was kept. | `56e62a635` — *"commit the half of `2bb1e8531` I left behind — HEAD did not build"*; the seam originates at `6d3bda8a3`, `6a8960498`, `233ca7a3e` |
| D116 | `docs/design/BUSBAR-1.6.0.md` as five competing authorities, absorbed VERBATIM so all 78 decision row numbers survive byte-exactly; and Part 7 cut from 1,362 lines to 345 with the rule "when an item is done, delete the row" | `49ab4aca2`, `a07a7591e` — **but see §7.3: the LOST register went with it** |


---

## 6. NOT A FINDING — 20

Narration, a granted exemption that carries its own reason, or a restatement of something
already on the map. Listed so that the next reader does not re-triage them.

| # | what it is | why it is not a finding |
|---|---|---|
| N1 | The six ceiling raises pinned with no headroom at `d886cdc94`, and the post-absorption recalibration at `ca10018c4` | Declared under `[gate.ceiling_raises]` and pinned to a measurement. The mechanism working. |
| N2 | `GRPC_LIBRARY_VOCAB` and `OFF_TREE_MANIFESTS` (`e15bda24e`) | Narrow exemptions matched on exact library spellings, with the reasoning in the file. |
| N3 | 43 site-level `known_sites` in `lean-core` (`be7ed30f2`) | Each of the 43 investigated individually; renaming any would desync a message from the wire key it names. The 3 left RED are **B3**. |
| N4 | The plane-streaming `KernelSeal` fixture (`a4c290478`) and the `lint_rules.rs` spec-data exclusion (`1ee9fcbe8`) | Reviewed debt written down rather than left an unreviewed RED. |
| N5 | `1d3d299fd`'s `teller.rs` held at 667, not re-pinned in either direction | Deliberate: the file moved 681 → 661 → 681 under measurement in one afternoon. |
| N6 | `ab5bcb45c`'s three remaining `ceiling-census` findings | The base comparison reporting the decrements the same commit declares. *"It EXPIRES BY ITSELF."* |
| N7 | `feceea99a`'s REFUSAL of every inline-test block it could not move safely | The discipline, not a gap. The backlog it measured is **A37**. |
| N8 | `adda017ae`/`7e27cdc11`'s statement of the reachability gate's two blind spots (macro expansion, simple-name over-approximation) | Limits written in the gate's own header beside the claim. |
| N9 | `f2efe6390`'s *"Anyone who read an `effects.stderr` divergence today read a fabricated one"* | A statement about readings already taken. The defect is D83. |
| N10 | `13a6c434f`/`14e1af564`'s reaper lessons (a count of four named things is not a count of what is running) | Encoded in the rules. The scheduling gap is D80. |
| N11 | `c915dd264`/`5df366873` — an explanation of a deleted edge is counted as the edge | A prose-hygiene rule learned and applied; the cell reads 2. |
| N12 | `0ce782715`'s CONTRIBUTING section and `233ca7a3e`'s attribution note | Process documentation. The live hazard it describes is **A1**. |
| N13 | `fa23e0488`'s copy of the adjudication record to `~/Downloads` | Ops. |
| N14 | `1409ff911`'s withdrawal of two of four promotion blockers (`ship-ready`/`construction` absent from qa and main) | *"That is not a deadlock"* — check runs attach to a sha and the first promotion carrying the trunk's `ci.yml` produces them. |
| N15 | `692ef8afd`/`524cfc58a` keeping `apply-deferred-decisions.py` | #80's own body names it the one permanent documented python3 boundary. |
| N16 | `32c2da106`'s `envelope.as_object_mut()` write-back left alone | The `None` arm is dead by construction — `ingress::jsonrpc::read` refuses a non-message body before admission. |
| N17 | `ab5bcb45c`'s decision not to repoint the `unit` glob at `crates/busbar-kernel-*` | Would run the unit kind's rules over the kernel rather than restore a count. |
| N18 | `5fbd891e0`/`0e41eeb9e` leaving the dissolved codec crates' audit-ledger scopes standing | *"the finding history is worth more than the tidiness"* — and they fall due for re-audit by themselves. The 33 records are **A17**. |
| N19 | `1.6.0-owner-decisions.md` rows restated in bodies | 13 of them; named in §3.1 rather than duplicated. |
| N20 | Every `PROOF` / `RED-BEFORE-GREEN` / `MEASURED` / `VERIFICATION` block, and the owner rulings #79–#87 themselves | Evidence and decisions, not owed work. Where a ruling left work owed, that work is in §2. |

---

## 7. THE ARITHMETIC, AND EVERY DEDUPLICATION

```
  242   residue STATEMENTS   1.6.0-map-proof.md §5, counted over all 307 bodies
  237   DISTINCT ITEMS       this triage
        =  117 DONE + 68 OPEN-actionable + 6 OPEN-blocked + 26 SUPERSEDED + 20 NOT-A-FINDING
```

**These two numbers are NOT derived from one another and this document will not pretend they
are.** A statement-level tally and an item-level triage measure different things, and forcing an
identity between them would be exactly the failure `5b3ea85b0` struck: *"An unenumerated count is
… a number reporting confidence without exposing what it measured."*

Two forces act in opposite directions and neither is a rounding:

* **DOWN — 32 statements collapsed into 12 items** (§7.1). The same defect gets recorded by three
  agents in three commits; it is one item here, citing all three.
* **UP — six bodies carry several findings apiece where the tally counts one** (§7.2), worth
  **+12** on those six alone, plus the single-clause *"also found, not fixed"* / *"NOT FIXED HERE"*
  riders that a statement-level sweep folds into their parent sentence. The proof said so itself:
  *"the S3 delta is a floor, not a ceiling."*

What IS auditable is the item list: **237 rows, each carrying a SHA and the sentence it came
from.** Count the rows:

```sh
F=docs/design/1.6.0-commit-residue.md
grep -cE '^\| \*\*A[0-9]+\*\* \|' $F   # 68  OPEN, actionable
grep -cE '^\| \*\*B[0-9]+\*\* \|' $F   #  6  OPEN, blocked
grep -cE '^\| \*\*S[0-9]+\*\* \|' $F   # 26  SUPERSEDED
grep -cE '^\| D[0-9]+ \|'            $F   # 117 DONE
grep -cE '^\| N[0-9]+ \|'            $F   # 20  NOT A FINDING
```

### 7.1 The 32 collapsed statements → 12 items

| item | mentions collapsed | the commits |
|---|---:|---|
| The float-encoded token count (D37) | **7** | `1b6c9d4ba`, `834dc1836`, `0df9c2b10`, `812370e09`, `d5d9a0a63`, `3c321a31e`, `3c533603f` |
| Two admin money reads disagreeing (**A3**) | **3** | `5a385c5ef`, `af7d2329b`, `2d74302ea` — *"recorded three separate times, three separate commits"* |
| `rate_card_version: 0` (D42 + **A5**) | **5** | `0f7766c13`, `47bfb9383`, `dcbb2dc1f`, `aa81f48b0`, `4e01aebec` |
| `NANOS_PER_CENT` drift (D39 + **A4**) | **3** | `a1019f041`, `489b63ab1`, `94594ad1d` |
| The shared-checkout `git add` (D115) | **4** | `1b6c9d4ba`, `99dd34503`, `2573c9a98`, `0ce782715` |
| A crate/kind in no list is in no gate (D61/D63/D64) | **3** | `2b2de9712`, `7f2a7f77c`, `383f23875` |
| Stale crate names in gates (D69/D70) | **4** | `d12e1f858`, `afe12ec00`, `fe689c616`, `ef092719a` |
| Oracle plane-money blindness (S3) | **3** | `3a5c137d9`, `9fae9ee5b`, `f40b96bb4` |
| `ledger sync --write` laundering (D55 + **A18**) | **3** | `433300ea6`, `d37821b59`, `f6ceb7d54` |
| The grep trap (**A30**) | **3** | `959700d86`, `e542adfaf`, `eb36d1695` |
| The size ratio (S21 + **A64**) | **3** | `cd96e5a31`, `d63fcd55e`, `1ca3d5481` |
| `cpufeatures -> libc` (D66) | **3** | `c6f69b6d4`, `c0debeb4b`, `4f26fa73c` |
| | **44 mentions over 12 items — 32 collapsed** | |

### 7.2 The bodies the tally counts as ONE statement

The proof said so itself: *"the S3 delta is a floor, not a ceiling."* Six bodies carry more
than one finding apiece:

| body | items in it |
|---|---:|
| `9b14f4f5a` — *"8 open security items, 10 audit/money integrity items, 7 durability items, 6 correctness/observability items … This is the next session's worklist."* | **31 named, and see §8.3** |
| `679931c58` — five findings, each with its own reachability verdict | **5** |
| `df3c19c8a` — the transports serving zero bytes, SigV4 twice, two `/metrics` content-types | **3** |
| `7d85fa5bc` — shape blindness, the redaction written twice, a stale exception passing an existence check | **3** |
| `55489d981` — the un-run container load, the drop-in path, the volume ownership | **3** |
| `6aaed1398` / `3caebd906` — four done-oracle groups | **4** |
| | **+12 over these six alone, excluding `9b14f4f5a`** |

### 7.3 THREE CLAIMS THAT DID NOT SURVIVE VERIFICATION

The brief warned that a commit body can be wrong and that a false DONE is worse than an OPEN.
Three checks changed a verdict:

1. **`6a8960498` reads as if the TLS-key audit were the last open item of a finished sweep —
   and at `37951e062` it was worse than open: the caller had landed and the callee had not, so
   `HEAD` did not compile.** Classified OPEN on that measurement, then **re-classified DONE**
   when `56e62a635` landed the missing half an hour later. Both states are recorded in §1, with
   the command that produces each. **This is the only verdict in this file that changed after it
   was written, and it changed in the safe direction — an OPEN becoming a DONE, never the
   reverse.**

2. **`a07a7591e` says what it deleted from Part 7 "was narrative … it is all still in git".**
   It was not all narrative. It took **THE LOST REGISTER** with it — the register `9b14f4f5a`
   had landed sixteen commits earlier as *"the next session's worklist"*, holding 8 open
   security items, 10 audit/money integrity items, 7 durability items and 6
   correctness/observability items, each *"evidence-backed against trunk rather than inferred
   from an absent symbol name"*. Measured:

   ```sh
   git show 9b14f4f5a:docs/design/BUSBAR-1.6.0.md | grep -c 'THE LOST REGISTER'   # -> 1
   grep -c 'THE LOST REGISTER' docs/design/BUSBAR-1.6.0.md                        # -> 0
   git log --oneline -S'THE LOST REGISTER' -- docs/design/BUSBAR-1.6.0.md
   #   a07a7591e docs: Part 7 becomes a MAP, not a log
   #   9b14f4f5a docs: record the LOST register in Part 7
   ```

   `docs/design/1.6.0-map-proof.md` is now the **only** file in the tree that mentions it, and
   it mentions it only as a count. **The single largest item in the S3 residue was written
   down, then deleted, by the document-hygiene pass — which is the S3 mechanism completing a
   full circle.** ~7 of the 31 are demonstrably closed (`6a8960498` scores seven); the other
   ~24 have no home. **Restoring that register from `9b14f4f5a` and triaging its 31 rows is
   owed, and it is not counted in this document's 237 — the bodies name the buckets, not the
   rows.**

   **✓ DONE — §10 is that restoration.** All 31 recovered from `9b14f4f5a` and triaged against
   `HEAD`: **21 DONE, 8 OPEN, 2 NOT A FINDING, 0 SUPERSEDED**. The *"~7 are demonstrably closed"*
   estimate above was a floor and a large one — the real figure is 21. Two of the 8 open are
   defects **this document had already rediscovered independently** (`A5`, `A13`), which is the
   price of the deletion measured in the only currency that counts: the same work found twice.

3. **The "six versus four `[[allow]]` rows" disagreement reconciles; neither body is wrong.**
   `c6f69b6d4` says *"`qa/denylist-allow.toml` concedes this exact sentence SIX times, not
   twice"*; `4f26fa73c` says *"four `[[allow]]` rows already restate this same reasoning"*.
   Measured at the pin:

   ```sh
   git show a197ad2f5^:qa/denylist-allow.toml | grep -c '^\[\[allow\]\]'   # -> 6
   git show a197ad2f5^:qa/denylist-allow.toml | grep -c 'dep = "libc"'     # -> 6
   ```

   Six rows, all `libc`. Four of the six restate the **cpufeatures** reasoning
   (`busbar-plane-a2a`, `busbar-plane-mcp`, `hook-test-plugin`, `hooks-ranking`); the other two
   (`busbar-plane-llm`, `busbar-plane-streaming`) reach `libc` through **getrandom** as well
   and keep their per-crate waiver, which is **B1**. Both numbers are true of different sets.
   Recorded here because the brief flagged the pair and a reader will meet it again.

---

## 8. WHAT THIS MEANS

### 8.1 The number

**68 OPEN, actionable items nobody owns**, against a master list of at most 45 rows of which
42 are distinct-and-genuinely-open. This document roughly triples the worklist, and it does it
from a source that was already fully written down.

### 8.2 The seven that would fail a bank auditor — RE-VERIFIED, AND SIX OF THEM DO

**⚠ This list was re-read against the code in §1.4. Three of its seven one-line descriptions were
wrong, including the one that led it. The corrected list:**

`A2` (**a served call writes its LEDGER row and does not write its METERING-SERIES row** — the
original description said the opposite; the defect is that a count-bearing record is conditioned on
a read-time lens, ruled `295435456`) · `A3` (two admin reads, two answers, same window — **stands,
and the tree's own `MONEY_READS` test asserts the residual**) · `A4` (a fourth unpinned money
constant — **stands**; a drift hazard on a live budget path, not a live wrong answer) · `A5` (two
DURABLE voice postings that confidently name the wrong rate card — **stands, and it is the hardest
of the seven: one is sealed into the audit chain**) · ~~`A6` (five billed reads that record
zero)~~ — **CLOSED at `5cfe127d9`, 2026-09-22 12:16, four hours BEFORE this document's own
`a197ad2f5` pin; it was a false OPEN, and what survives is a missing guard test plus five stale gate
allowances** · `A7` (a billing figure with no red-capable test in **four** of seven transports —
grpc, stdio, tls, ws; the figure was five when written) · `A8` (two served endpoints, two
content-types — **stands**, and it is the *served byte* half of Tier 0, not the money half).

**Six of the seven are real open work. One was already closed.** Every one of the six was recorded
by an agent who could not fix it, and **none has a master-list row.** The eighth was `A1`, and it
closed while this file was being written (§1).

**And the method, not just the item, took a correction.** A2 was wrong because it paraphrased its
own sources — all of which say *metering row* — as *ledger row*. A6 was wrong because it trusted a
body (`bfe513714`, 12:55) that was itself stale by 39 minutes against a fix that had already landed
(`5cfe127d9`, 12:16). Both are the same failure as the one this document was written to expose: **a
finding read from prose rather than from the tree.** §0's rule — *"DONE rows were verified against
the tree, not taken from the body that claimed them"* — was applied to the DONE column and not to
the OPEN one. It has now been applied to both, for these seven.

### 8.3 The mechanism, one more time

`9b14f4f5a` wrote 31 items into Part 7 and called them *"the next session's worklist"*.
Sixteen commits later a document-hygiene pass deleted Part 7's narrative and took them with
it, in good faith, saying *"it is all still in git"*. That is true, and it is exactly the
problem: **git is where findings go to be true and unread.**

The structural fix `1.6.0-map-proof.md` §12 names is the only one that survives this:
**a refusing counter.** Every source that reconciled had one. A gate that fails when a commit
body says *owed* / *still owes* / *NOT FIXED HERE* / *separate decision* and no row appears in
a tracked file would have prevented every item in §2 from becoming a discovery.

Until that exists, this file is the counter, and it is a manual one.

---

## 9. PROVENANCE

Every body in `git log --since='2026-09-21 05:00' --reverse a197ad2f5` (307 commits,
`4241fb520` … `a197ad2f5`) was read end to end for this triage. Every DONE row was checked
against the working tree or the object store with a command; the checks that changed a verdict
are printed in §7.3. Figures quoted from a body are marked as that body's claim; figures
marked **measured** were re-run here.

**The input is pinned; the verdicts are not, and cannot be.** The corpus is fixed at
`a197ad2f5`. A DONE verdict is a claim about a MOVING `HEAD` in a checkout ~13 agents are
committing to, and one verdict already changed inside the hour (§1). A row that reads OPEN here
may have closed since. Re-run its command before spending a day on it — every row carries one.
---

## 10. THE LOST REGISTER, RESTORED AND TRIAGED — the 31 items `9b14f4f5a` called "the next session's worklist"

> **These 31 are NOT among this document's 237.** §7.3(2) recorded the loss and said restoring the
> register was owed and uncounted. This section is that restoration. The corpus is a different one:
> §1–§9 triage 307 commit BODIES; this section triages a DELETED DOCUMENT SECTION.

### 10.0 WHAT WAS LOST, MEASURED

```sh
git show 9b14f4f5a --stat -- docs/design/BUSBAR-1.6.0.md   # -> 121 insertions, "THE LOST REGISTER"
git show --stat a07a7591e -- docs/design/BUSBAR-1.6.0.md   # -> 105 insertions, 1,122 deletions
grep -c 'LOST REGISTER' docs/design/BUSBAR-1.6.0.md        # -> 0   (at HEAD)
git rev-list --count 9b14f4f5a..a07a7591e                             # -> 71 commits
git rev-list --count 9b14f4f5a..a07a7591e -- docs/design/BUSBAR-1.6.0.md   # -> 36 touching the file
```

`9b14f4f5a` landed 121 lines carrying 31 evidence-backed items — 8 security, 10 audit/money
integrity, 7 durability, 6 correctness/observability — from a read of ~625 survivor branches across
12 slices, one question each: *does the FUNCTION this branch added exist in the current tree, under
any name?* Its own summary: **~75–80% RENAMED** — the rename waves carried the work, and the fear
that consolidation had dropped whole subsystems was mostly unfounded. These 31 are the minority that
did not survive.

`a07a7591e` removed them with the reasoning that the deletion *"was narrative … it is all still in
git."* **Both halves of that sentence are true and the conclusion does not follow.** Git is where a
worklist goes to be correct and unread — which is the thesis of §8.3, arrived at independently.

**A correction to §7.3(2) and §8.3, made here rather than quietly:** both say the register was
deleted *"sixteen commits later"*. That figure does not reproduce. Measured: **71 commits** between
the two SHAs, of which **36** touch `BUSBAR-1.6.0.md`. The deletion was **1,122 lines removed / 105
added**, not "1,227 lines removed" (1,227 is the total changed-line count). Neither correction
changes the finding; both are recorded because an unreproducible number in an evidence document is
the thing this file exists to catch.

### 10.1 THE COUNTS

| class | count | where |
|---|---:|---|
| **DONE** — a later commit closed it; SHA cited and **verified an ancestor of `HEAD`** | **21** | §10.2 ×7 · §10.3 ×6 · §10.4 ×6 · §10.5 ×2 |
| **OPEN** — real work nobody owns | **8** | of which **6 wholly open** (L8, L14, L16, L18, L28, L31) and **2 partially** (L21, L30 — most of each item closed) |
| **NOT A FINDING** | **2** | L15 (the refusal is at the pricing boundary, not the leaf accessor) · L29 (a ruled design, with the ruling quoted) |
| **SUPERSEDED** | **0** | no item was made moot by a later decision; every one either closed or stands |
| | **31** | |

**The register's own thesis held up. 21 of 31 closed, and most closed properly** — with a named
defect, a RED-before-GREEN test and a comment explaining the failure mode — in the ~200 commits
between `9b14f4f5a` and `HEAD`. **The deletion was not the problem the counts reveal; the problem is
the 8 that nobody would have found again.**

**Sizes on the open work:** L8 **M** · L14 **S** · L16 **XS** · L18 **S** · L21 **S** · L28 **S** ·
L30c **XS** · L31 **M** (four sub-items: S + XS + XS + S). Plus two residuals recorded on DONE rows:
**L1** ships with no regression test for the path it fixed, and **L26** closed the kernel half and
left the plane half (**S**).

**Two open items are the SAME defects this document already carries, reached from a different
corpus — which is corroboration, not duplication:**

| LOST-register item | this document's row | what the second reading adds |
|---|---|---|
| **L14** `rate_card_version: 0` in voice | **A5** | The other four sites' closing SHAs (`dcbb2dc1f`, `47bfb9383`, `aa81f48b0`) and the mitigation that `VoiceUnit::settle` has no non-test caller **yet** — so A5 is a gated surface, not a live mis-stamp. |
| **L18** durable spend double-counted on shutdown | **A13** | A13 calls it *"a narrow interleaving"*. It is not narrow: the listener-tail `flush_budgets` fires on the **same broadcast** that wakes the flusher's shutdown arm, so the two overlap **by construction**. The `flush_gate` that exists is a local inside `spawn_budget_flusher` and cannot see the `main.rs` call sites. |

And **L16** lands in the same file as **A4** — `busbar-kernel/src/cost.rs`, the unpinned kernel copy
of the money constants — which now carries **two** independent divergences from the canonical
ledger: an unasserted `NANOS_PER_CENT` (`:58`, A4) and an unclamped `RateNanos::from_raw`
(`:138-145`, L16). Whatever dependency ruling closes A4 should close both.

### 10.2 SECURITY — items 1-8: **7 DONE, 1 OPEN**

Every SHA below was checked with `git merge-base --is-ancestor <sha> HEAD` and its subject read.

| # | item, as written `9b14f4f5a` | verdict | evidence at HEAD | size |
|---|---|---|---|---|
| **L1** | MCP task-answer merge bypasses the argument guard — `merge_answers` inserts caller-supplied keys with no re-screen and the result goes straight to dispatch | **DONE** | `59815645b` *"fix(mcp): re-screen the merged task answers before dispatch"*. `crates/busbar-mcp/src/mcp/tasks.rs:804-831` re-runs `client::argguard::guard` on the POST-merge arguments under the `input_schema` pinned at create, with an absent schema walked as `{"type":"object"}` *"because skipping would make 'declare no schema' the way past the check"*. `merge_answers` has exactly one caller (`:802`) — no second door. **Residual, worth a row: `59815645b` shipped NO regression test for this path**; the only ask-merge re-screen test (`sep2243_ask_merge_tests.rs:91`) covers the synchronous `inputResponses` path, not the detached task runner. | — |
| **L2** | MCP server→busbar direction has no sender mirror (claimed as a SECOND instance) | **DONE** + the "second instance" is **NOT A FINDING** | `489b63ab1`. `crates/busbar-plane-mcp/src/plane.rs:633` (`decode_response` refuses any method that is not `Sender::Provider`) mirrors `:496` (`decode_ingress` refuses `Sender::Provider`). Those are the crate's only two `ops::row_for` sites, and the application-layer server→busbar door (`busbar-mcp/src/mcp/client/peer.rs:282`) is a closed four-entry allowlist. Conformance test iterates every client row (`busbar-plane-mcp/tests/conformance.rs:424`). | — |
| **L3** | Rotate-replay-key idempotency collision — colon-joined caller-controlled strings; a crafted pair serves a different key's freshly-rotated secret | **DONE** | `35cdedaa3` *"stop serving another key's rotate response to a crafted id"*. `busbar-core-admin/src/keys.rs:1498` now calls the framed sibling `verbs::rotate_replay_key`, `verbs.rs:94` = `format!("rotate:{}:{}:{}:{}", id.len(), id, header.len(), header)`. Length-framed, so `("a:b","c")` and `("a","b:c")` no longer collide. Regression tests at `src/tests/mint_wiring_tests.rs:130`. | — |
| **L4** | Auth-cache revocation bypass — `run_chain_cached` re-`put`s on a HIT, resetting TTL | **DONE** | `6d9d6e88b`. `busbar-kernel/src/auth/mod.rs:464` commits only on a MISS (`!was_hit`); its doc names the exact defect and says the `busbar-kernel-identity/src/chain.rs` sibling was **ported, not merely referenced**. The second walk `run_admin_chain` (`:952-974`) returns on a hit with no `put`. RED-proven: `plugin_chain_tests.rs:1133 a_credential_hit_within_ttl_does_not_extend_the_cache_entrys_lifetime`. | — |
| **L5** | Unauthenticated A2A push route has no rate limit | **DONE** | `fb18e7a75` *"bound the push route's write volume per task"*. `busbar-a2a/src/a2a/pushback.rs:413` — fixed window 60 req / 60 s, keyed per TASK (the token already scopes to one task, so a second connection cannot sidestep it), placed *"right after the token is proven to name a task and before ANY of the cost this endpoint exists to bound"*. `429` + `Retry-After`. | — |
| **L6** | Unbounded MCP tool name into the audit chain | **DONE** | `3ee41762d` *"bound three attacker-controlled growth vectors"*. `busbar-mcp/src/mcp/method.rs:1284` `MAX_TOOL_NAME_BYTES = 256`, checked at `:1308` **before** `CallLog::open` — the doc states the ordering IS the fix. Refused, never truncated (a truncated name is a different tool name). | — |
| **L7** | Credential plaintext in `Debug` + no zeroize on `arena.rs::CredentialSlab` | **DONE** (the file moved) | `6d9d6e88b` (redacting `Debug` + zeroizing `clear()`), `6c35e81db` (`Drop` wipe). `busbar-kernel/src/arena.rs` no longer exists — the fixed-cap `Arena` model was killed under #41, which the register's own DELIBERATE list predicted. `CredentialSlab` is now `busbar-kernel/src/mask.rs:216` (hand-written `Debug` redacting `buf`), `:326` (`self.buf.zeroize()`), `:360` (`Drop` → `wipe()`). The doc is honest about scope: *"defence in depth, not the closing of a live leak… no production caller in this tree yet."* | — |
| **L8** | mTLS material not retired on config reload — `plane_host/identity.rs` has no config-generation tag; `trust_anchor.rs` has no eviction at all | **OPEN** — and the register **under**-stated it | Both halves survive verbatim; **no commit has touched either file since `9b14f4f5a`**. `plane_host/identity.rs:21` still admits *"it carries no config-generation tag — so this module cannot tell a fresh hot-reload's registrations apart from an old one's"*; the FIFO-256 cap (`:46`) bounds MEMORY, not revocation. `trust_anchor.rs:37` is a bare `HashMap`, `register` is an unconditional `insert` (`:55`), no cap and no eviction. **Two refinements.** (a) `trust_anchor` is SMALLER than claimed: every caller is test-gated (`a2a/transport.rs:283`, `mcp/client/transport.rs:270`) and `PassThroughEgressTrust` is documented *"ADDITIVE AND DORMANT"* (`busbar/src/main.rs:641-648`) — latent, not live. (b) `identity` is **WORSE** than claimed, by a stronger path: `identity::register` IS live in production (`a2a/mod.rs:455` → `transport.rs:544` → `:277`), so the *"reached only by tests"* comment at `identity.rs:29-33` is stale and wrong — but it runs from `a2a_start`, a `PlaneDecl::start` hook driven **once per process** (`busbar/src/main.rs:1478`), and the transports are then carried across every config apply behind a `OnceLock` (`a2a/plane.rs:116`, whose own doc says *"an agent added by a later apply is verified on the boot-time transport"*). **A config reload that rotates or removes a `client_identity:` therefore has no effect at all — the revoked identity is live for the process lifetime and needs a restart.** | **M** |

**L8 sizing.** A generation tag plus `retire_generation()` on both registries is XS–S mechanically
(the `auth_cache::CacheGeneration` precedent exists). What makes it **M** is the behaviour change
that actually closes it — re-resolving a2a client identities on config APPLY rather than only at
boot — which touches the boot/apply seam and the `cards` `OnceLock`, and needs a ruling on whether a
resolution failure mid-apply refuses the apply the way it currently refuses boot.


### 10.3 INTEGRITY (audit / money) — items 9-18: **6 DONE, 3 OPEN, 1 NOT A FINDING**

| # | item, as written `9b14f4f5a` | verdict | evidence at HEAD | size |
|---|---|---|---|---|
| **L9** | Admin audit-chain digest collision — `legacy/chain.rs` joins fields with a raw unescaped `\|`, `resource` carries untrusted MCP tool names, so a REJECTED mutation's digest can collide with an APPLIED one's. PARKED, owner's call | **DONE — and the register's own recommended fix is what landed** | `10d66a79f` *"tag each record with its framing scheme so a digest cannot be forged"* (+ `6d9d6e88b`). Per-record `scheme` tag (`AUDIT_SCHEME_PIPE=1` / `AUDIT_SCHEME_LENGTH_PREFIXED=2`), `#[serde(default)]` to 1 so **nothing on disk moves**, `ChainedRecord::framing()` overridden per instance so `digest()` checks each record under the rules it was sealed with (`busbar-kernel-audit/src/legacy/entry.rs:95,:110-118,:229-239`; `legacy/chain.rs:211-215`; mirror `busbar-kernel/src/audit_ring.rs:138-161`). `link()` mints scheme 2 unconditionally and `AuditInput` carries no scheme field, so **no caller can choose it**. `digest_framing_tests.rs:513-548` still proves the collision pair under scheme 1 and that scheme 2 breaks it; `:562-580` pins scheme-1 bytes unchanged. | — |
| **L10** | Audit second-writer detection regressed — `append_plane_record` is a blind upsert where legacy `append_audit` refused a fork | **DONE** | `961718c18` *"refuse a forked plane-record chain"*. `store-memory/src/lib.rs:766-791` is now `append_audit`'s mirror: byte-identical retry → `Ok`, different record at an occupied `(kind,parent,seq)` → `Err("… the chain has forked")`. Its comment names the defect verbatim. Cross-implementation conformance at `store-example-plugin/src/tests/mutation_hardening.rs:150-175`. | — |
| **L11** | Audit rows carry `ts: 0`, and `purge_plane_records_before` deletes anything older than the cutoff | **DONE** | `6d9d6e88b`. `busbar-kernel/src/audit/journal.rs:550-554` is `ts: busbar_kernel::store::now()`, with the failure named in place: *"A hardcoded `ts: 0` here made every row look infinitely old to `purge_plane_records_before`'s `r.ts < before` check, so the FIRST retention sweep deleted the whole class."* The sibling instance (the WAL chain-break record dated at epoch 0) is also closed, `busbar-kernel-wal/src/journal.rs:918-936`. | — |
| **L12** | Transient audit-write failure drops the record; the old backfill watermark is gone | **DONE** | `6d9d6e88b`. A bounded FIFO `PendingAuditQueue` replaces the drop (`busbar-kernel/src/plane/auditlog.rs:591-679`): a failed durable write is queued, every emitter drains oldest-first before its own write, and a recovered row is pushed into `AUDIT_LOG` so it appears on `GET /audit`. **Overflow is not silent** — it bumps a `dropped` watermark and logs `gap_detected = true` (*"a DETECTED GAP in the audit trail, not a delayed recovery"*). Both the live chokepoint `emit_admin_hostless` (`:915,:942`) and `emit` (`:745,:755`) are wired. | — |
| **L13** | SSRF-blocked call recorded as `OUTCOME_DISPATCHED` | **DONE** | `c789ba023`. `busbar-mcp/src/mcp/client/issue.rs:211-224` — `TransportError::is_own_refusal()` (`Refused` = dispatch-time SSRF guard, `Supervision` = crash-loop quarantine) maps to `OUTCOME_REFUSED`; `Unreachable`/`Io` keep `OUTCOME_DISPATCHED` because busbar did attempt the hop (`client/wire.rs:88-106`). The comment states the register's own argument. | — |
| **L14** | `rate_card_version: 0` hardcoded at six sites across `units_mcp/voice/a2a` — violates #44 | **OPEN — 2 of 6 sites. THIS IS `A5`, REACHED FROM A SECOND CORPUS.** | Four closed: mcp `units_mcp.rs:1408,:1612` via `Provenance::rate_card_version` (`dcbb2dc1f`), a2a `units_a2a.rs:916,:1185` (`47bfb9383`), and the #79 stamp wave `aa81f48b0`; llm `units_llm.rs:1610` resolves via `card_in_force(card, arrived.ms())`. **Voice keeps two literal zeros with no comment ruling them: `units_voice.rs:1846` (`PostingStamp` on `settle`) and `:1950` (`Amount` on the audit-chain `seal`).** Fix is the same six lines and the input is reachable — `root::kernel::ROOT_CARD.pin()` is a process global already used that way at `root/migration.rs:118`. **Mitigating, and it is the only thing separating this from a live mis-stamp:** `VoiceUnit::settle` (`units_voice.rs:1828`) has no non-test caller, and `compose_voice_governed_calls` (`main.rs:460-466`) documents the voice node as *"the root's unbound posture … honest for exactly as long as this node serves no unit"*. **A gated surface that ships a false provenance stamp the moment the serving switch lands.** | **S** |
| **L15** | Flat fee silently zero — `per_request_fee()` returns `0` for a currency the card doesn't name a fee in; #42 says an unpriced class REFUSES | **NOT A FINDING** | `per_request_fee()` is a leaf accessor, not the decision point. The #42 refusal lives at BOTH pricing boundaries: `busbar-kernel-ledger/src/cost/posting.rs:304` `price_at_card` returns `Unpriceable::CurrencyNotPriced` **before any fee is read** — and that gate was already present at `9b14f4f5a` — and `cost/view.rs:367` returns `MoneyError::CurrencyNotPriced`. Structurally the miss cannot occur anyway: every `RateCard` constructor inserts the fee in the card's own currency and records that currency, and `root/kernel.rs:105-115` keeps `node_currency()` as ONE function for three call sites because *"a second spelling is how a node comes to price a card in one currency and read it in another and report the refusal as a zero."* | — |
| **L16** | Third un-clamped rate conversion — `busbar-kernel/src/cost.rs::RateNanos::from_raw` lacks the u64 overflow clamp its canonical sibling has | **OPEN — and it is the SECOND of two now, not the third of three** | Measured at HEAD: `busbar-kernel/src/cost.rs:138-145` `fn nanos` guards `v.is_finite() && v > 0.0`; the sibling `busbar-kernel-ledger/src/cost/rate.rs:42 nano_rate` guards `v.is_finite() && v > 0.0 && v <= u64::MAX as f64`, gained in `c6f19c493` (*"clamp a finite but u64-overflowing rate to zero, not the saturated maximum"*). **Rust's saturating float→int cast means a `1e300` config typo yields `u64::MAX` nanos/token on the ADMISSION side while the ledger card prices the same config at `0` — an admit-vs-bill divergence, the same class as the `NANOS_PER_CENT` duplication already fixed, and `from_raw`'s own doc claims byte-identical arithmetic.** Config validation only checks `is_finite() && >= 0.0` (`config_validate/mod.rs:1406`), so nothing upstream bounds it. **Same file as `A4`** — `busbar-kernel/src/cost.rs` is the unpinned kernel copy of the money constants, and it now carries two independent divergences from the canonical ledger. Fix is one clause plus a test. | **XS** |
| **L17** | Root ledger book hardcodes `NullShipper` — never persists, whatever the config says | **DONE** | `3c57b7e7e` *"the boot book reaches the configured store"*. `node_book()` (`root/durability.rs:701-709`) still constructs `NullShipper`, but it is now the **no-store fallback**, not the boot path: `open_boot_book` returns it only when `app.governance == None`, otherwise `compose_boot_book` builds over `adapter.shipper()` and `fleet_data_dir()` (`main.rs:882-911,:925-937`). The constructor's doc says so: *"**A DEPLOYMENT WITH A STORE DOES NOT COME THROUGH HERE** … This constructor hard-codes both answers, which is correct only because the one caller that reaches it has already established that there is no store."* Regression proof `busbar/src/tests/tests.rs:772-790,:1039-1093`, which name the old literal as the defect. | — |
| **L18** | Durable spend double-counted on shutdown — `main.rs` flush sites race the spawned flusher | **OPEN — THIS IS `A13`, REACHED FROM A SECOND CORPUS, AND IT IS LARGER THAN `A13` STATES** | A `flush_gate` was added, **but it is a local inside `spawn_budget_flusher`** (`busbar-kernel/src/governance/mod.rs:1239`), so it serializes only that task's periodic tick against its own shutdown arm. `main.rs` calls `gov.flush_budgets()` directly on the run task at `:1595` (listener tail) and `:1497` (mcp-stdio tail), **outside the gate** — and the listener-tail call fires on the SAME broadcast that wakes the flusher's shutdown arm (`governance/mod.rs:1266-1283`), so **the two overlap by construction rather than by a narrow interleaving.** The gate's own comment states the consequence: *"a second concurrent flush would snapshot against the SAME un-advanced baseline and re-send the first flush's still-in-flight delta — a durable DOUBLE-COUNT."* The window is real because `flush_budgets` (`state.rs:2124`) clears `dirty` under the shard lock, writes OFF the lock, then advances the baseline at `:2211-2227`; a settlement landing during the drain re-marks the cell dirty and the second flusher re-sends. **Scope is `flush_budgets` only** — `flush_metering` is documented gate-free-safe by `mem::take`-per-shard (`state.rs:933-937`). Fix: hoist the gate onto `GovState` (a `Mutex<()>` taken at the top of `flush_budgets`) so the inline tails serialize with the task. | **S** |
### 10.4 DURABILITY / AVAILABILITY — items 19-25: **6 DONE, 1 PARTIAL (one half open)**

| # | item, as written `9b14f4f5a` | verdict | evidence at HEAD | size |
|---|---|---|---|---|
| **L19** | TLS `close_notify` has no timeout; the 250 ms `CLOSE_NOTIFY_BUDGET` is gone | **DONE** | `86392c161` *"bound three unbounded waits and stop discarding the close reason"*. `busbar-transport-tls/src/lib.rs:92` `CLOSE_NOTIFY_BUDGET = 250ms`; `:899-914` `send_close_notify` wraps **both** the writer-lock acquisition and `shutdown()` in `tokio::time::timeout`. The doc at `:80-91` names both hang paths the register did. | — |
| **L20** | WS close reason discarded — always `Close(None)` | **DONE** | `86392c161`. `busbar-transport-ws/src/transport.rs:52-78` `close_code_for` maps all eight `CloseReason` variants to distinct RFC 6455 codes and explicitly excludes reserved 1005/1006/1015; `:812-814` sends `Close(Some(close_frame))`. The `CloseReason` enum is unchanged (`busbar-contract/src/transport/wire.rs:210`), exactly as the register predicted. | — |
| **L21** | Raw TCP connect unbounded; WS read pump has no close wakeup | **PARTIAL — the WS half is OPEN** | TCP/TLS half **DONE** (`86392c161`): `busbar-transport-tcp/src/lib.rs:71,:422` `DIAL_TIMEOUT`; `busbar-transport-tls/src/lib.rs:597` wraps its own connect in `handshake_timeout`. **WS half OPEN:** `busbar-transport-ws/src/conn.rs:83` is a bare `closed: AtomicBool` with no `Notify` (measured directly); the pump only POLLS it (`transport.rs:574,:590,:691`) and `close` at `:803-809` stores the flag and never wakes. A pump suspended in `reader.next().await` on a silent peer is never woken, so the task and the socket it holds the last clone of survive for the life of the process. **This tree already ruled the other way for the two siblings** — `busbar-transport-tcp/src/lib.rs:103` and `busbar-transport-tls/src/lib.rs:129` both carry `closing: tokio::sync::Notify` with the comment *"The flag alone would leave that pump — and the socket it holds the last clone of — alive for the life of the process,"* and the TLS test (`tests/mod.rs:551`) states it outright: *"So the close is a wakeup, not just a flag."* That landed in `e7d158a97`, whose diffstat touches **only tcp and tls**. The existing ws test (`tests/battery.rs:875`) passes only because a frame DOES arrive; it cannot see the silent-peer case. | **S** — port the established pattern: `closing: Notify` on `ConnState`, notify in `close()` (flag first, per the sibling's ordering comment), race the read against `closing.notified()`, mirror TLS's `close_ends_a_read_parked_on_a_silent_peer`. |
| **L22** | `"id": null` misread as a notification, violating JSON-RPC 2.0 §4 | **DONE** | `c789ba023` *"three protocol and audit-accuracy defects on the client leg"*. `busbar-mcp/src/mcp/client/peer.rs:235-263` — closed notification-method table first, then request-vs-notification by `obj.contains_key("id")`: **presence, never nullness**. The doc block at `:218-234` is a verbatim post-mortem of the register's finding. | — |
| **L23** | Cohere token counts silently zero — `.as_u64().unwrap_or(0)` on values Cohere specs as floats | **DONE** | Seam `busbar_llm_codec::usage_count::read_count_u64`; every Cohere token/billing read routes through it (18 sites in `cohere/reader.rs`, 2 in `cohere/handler.rs`). Remaining bare `as_u64` calls are non-billing only (`k`, `output_dimension`, `top_n`, `max_tokens_per_doc`, stream-frame `index`). Crate-wide regression gate at `busbar-llm-codec/src/tests/usage_count_tests.rs`; seam lifted crate-wide in `834dc1836` and hardened to a REFUSAL in `3c533603f`. **Audit-trail note, and it is the same class as §7.3: the fix BYTES were swept into `1772c7470`, whose subject is unrelated (*"refactor(#41): Encode::ArenaExhausted -> ScratchExhausted"*); `1b6c9d4ba` records the sweep.** | — |
| **L24** | `InFlight::insert` overwrites a duplicate key — orphans a `HoldCell`, inflates the count | **DONE** | `7b622be1d`, whose body says it: *"InFlight::insert overwrote a duplicate unit key. The displaced entry's HoldCell was orphaned, so its budget stayed reserved forever."* `busbar-kernel/src/inflight.rs:490-497` — `shard.contains_key` under the SAME shard lock that would write, before `claim_slot`, returning `Err(CapRefused{ reason: ReasonCode::InFlight, hold })` so the hold is handed back. Doc at `:471-489` names both consequences and records why it is a typed refusal rather than a panic. | — |
| **L25** | Plain container image cannot dlopen a plugin — a REGRESSION, the Dockerfile once carried the `COPY` | **DONE** | `7c0a381d1` *"ship the runtime libs a dropped-in plugin needs"*. `Dockerfile:61` `COPY plugins/${TARGETARCH}/lib/ /lib/`, and it is now plugin-AGNOSTIC rather than the old headroom-coupled shape: the three musl libs are extracted once from the pinned `rust:alpine` digest (`.github/workflows/docker.yml:550`) and staged for both arches (`:785-786`). The workflow comment at `:537` names the regression itself. **Caveat carried from the commit body, not invented here: the dependency closure was proven STATICALLY (registry-pulled bytes, NEEDED graph closed on amd64 and arm64); no runtime `dlopen` was demonstrated.** | — |

### 10.5 CORRECTNESS / OBSERVABILITY — items 26-31: **3 DONE, 1 NOT A FINDING, 2 OPEN (one compound, one wholly open)**

Items 30 and 31 are compound and are triaged per sub-claim; an item's verdict is the weakest of its parts.

| # | item, as written `9b14f4f5a` | verdict | evidence at HEAD | size |
|---|---|---|---|---|
| **L26** | Verify's sealed `VerifiedDestination` set never reaches Route/Meter — Route re-derives its own | **DONE**, with a named residual | `7b622be1d`. The sealed `Vec<VerifiedDestination>` is threaded `open_to_door` → `under_hold` → `route_leg`/`meter`; trait doc `busbar-kernel/src/teller.rs:561-571,:627-635`: *"a Route that re-derived its own set could dial a lane nothing verified, and the sealed set on the door would have priced a decision Route never actually made."* RED-before-GREEN test at `src/tests/teller_verified_destination_tests.rs:4-13`. **Residual (S), and it is in the commit's own body: *"the planes take the set but do not consume it yet."*** `busbar/src/root/units_llm.rs:1517` still binds `_destinations` and re-derives at `:1520` via `self.walk.effective_pool(..)`. The KERNEL-side gap the item named is closed; plane-side consumption is a real follow-on. | — |
| **L27** | SNI compared byte-wise, not case-insensitively — `registry.rs::transport_overlaps` can clear two genuinely overlapping claims | **DONE** | `7b622be1d` (third fix in the same commit). `busbar-kernel/src/registry.rs:495-517` now uses `eq_ignore_ascii_case` for SNI/ALPN, and the comment settles it: *"DNS names — which is what a TLS server name IS — are case-insensitive… Comparing them byte-wise let a boot's claim-disjointness proof clear that fight as 'disjoint' when it is not."* Not a blanket lowercase: client-cert subject, stream name and port keep exact comparison. | — |
| **L28** | Voice tool-call refusal silently dropped with no client notification | **OPEN** | `busbar-voice/src/runtime/session.rs:421` sets the bit (`Outbound::refused_reply`, declared `:44-50`) and the bit's own doc argues the client must be able to tell: *"a caller that could not tell that apart from a frame that was never sent would have no way to report a client answering calls it was never asked to make."* **There is no production consumer** — `refused_reply` appears only in `bin/voice-conform.rs:1526,:1554` and `runtime/tests.rs`. `UplinkForwarder::handle` (`session.rs:576-582`), the only production driver of `on_client_frame`, forwards `plan.upstream` and drops `plan.downlink`/`plan.refused_reply` entirely. The bit predates the register (`a73f487de`, 2026-09-06), so the register was already scoring it as unfinished — it still is. Fix: emit a downlink error event and a `tracing::warn!` from `:576-582`. | **S** |
| **L29** | `voice_build` mounts nothing instead of refusing boot when `public_url` is absent | **NOT A FINDING — a ruled design** | `busbar-voice/src/mount.rs:483-486` states the decision: *"None when there is no public_url: a deployment with no receiving origin fronts nothing, claims nothing and admits no one — the A2A delegation-only asymmetry, and what makes the plane bind no audience rather than mount a door that could only ever refuse."* It is not an unchecked omission: `voice_claims`/`voice_routes`/`voice_admission` all return empty/`None`, so ratchet **R2** (*claims a path ⇒ must bind an audience, else boot refuses*, `busbar-kernel/src/appbuild.rs:1801-1810`) holds **by construction**, and the A2A twin is pinned by `a_delegation_only_a2a_plane_mounts_nothing` (`busbar-kernel/tests/registry_cross_plane.rs:994-1025`). Refusing boot would break delegation-only deployments. **This is the one item of the 31 that was never a defect.** | — |
| **L30** | gRPC `MESSAGE_MAX_BYTES_KEY` unwired · HTTP egress doesn't refuse TE+Content-Length · SSE drops legal bare-field lines | **2 of 3 DONE; 30c OPEN** | **30a DONE** `cdef7a0f2` — `busbar-transport-grpc/src/transport.rs:51` (key), `:221` (read in `listen`), threaded via `message_cap()` to accept+dial. **30b DONE** `cdef7a0f2` — egress `busbar-transport-http/src/lib.rs:1475-1483` now mirrors the ingress twin at `:1653-1666`, same two-armed check, same `TransportError::Framing`: *"Two headers describing two framings of the same bytes… Refused rather than silently disambiguated."* **30c OPEN** — `busbar-transport-sse/src/proto.rs:110` `SSE_FIELDS` require a trailing `:` and `:127` matches `starts_with(field)`, so a bare `data` / `id` line — legal per WHATWG SSE (*a non-empty line with no colon is a field whose name is the whole line and whose value is empty*) — is dropped. The file's own comment argues AGAINST the shipped behaviour: *"this transport declares `DECODES_PAYLOAD = false`, so deciding that a field it does not itself read is uninteresting is not its call… dropping it silently loses an event nothing else will report."* An oversight, not a ruling. Fix is one match arm; `src/tests/mod.rs:731` pins the current row (`b"data"` → `false`) and must flip with it. | **XS** (30c) |
| **L31** | Four xtask/CI-tooling bugs: no child-process timeout · a YAML block-scalar panic on multibyte input · a TSV `splitn(4)` corrupting 6-column rows · an unreadable directory read as silently empty | **OPEN — all four** | **See §10.5.1.** | **M** total |

#### 10.5.1 L31 — and the finding underneath it: three real fixes exist and are not in the tree

All four sub-claims are OPEN at HEAD, and three of them have a **written, committed fix that never
reached this branch**. Measured:

```sh
for s in bc6f625eb bcf0b3083 c32cc8509; do git merge-base --is-ancestor $s HEAD || echo "NOT IN HEAD: $s"; done
#   NOT IN HEAD: bc6f625eb  xtask: a child process is bounded in time, because a runner …
#   NOT IN HEAD: bcf0b3083  xtask: a block scalar is dedented by columns of whitespace, …
#   NOT IN HEAD: c32cc8509  xtask: the ledger reader stops at the detail, and the header …
git branch -a --contains bc6f625eb
#   keep-a3-drain-survivors, remotes/origin/delete/keep-a3-drain-survivors
```

**All three live only on a branch whose remote name begins `delete/`.** The register was itself the
output of a branch harvest; these are three survivors the harvest named and nobody landed. That is
the §8.3 mechanism in its most literal form — not a finding lost in a commit body, a *fix* lost on a
branch scheduled for deletion.

| sub | verdict | evidence at HEAD | size |
|---|---|---|---|
| **31a** no child-process timeout | **OPEN** | `xtask/src/full_gate.rs:711` (`run_one`, unbounded `.output()`), `xtask/src/parity.rs:163,:277,:584`, `xtask/src/ctx.rs:692`. The gate runner's wall-clock ceiling (`execute_within`, `gates/mod.rs:1516-1545`, `24b2f1065`) bounds the GATE and leaks the wedged thread — it does not bound the child. `gitp::ask` designs the pipe deadlock out but still `wait()`s unbounded. `proc::output_with_timeout`, `LEGACY_LIMIT`, `GATE_LIMIT` are all absent; `xtask/src/proc.rs` does not exist. Lost fix `bc6f625eb`, whose audit line still reads true of HEAD: *"Grepped before landing: `run_checked`, `gitp`, `timeout`, `mpsc`, `thread::spawn` — NOTHING in this crate bounds a child today."* | **S** |
| **31b** YAML block-scalar panic on multibyte input | **OPEN** | `xtask/src/yaml_lite.rs:720-726` — `base = indent_of(&body[0])` is a BYTE offset, guarded by `l.len() >= base` rather than `is_char_boundary`, so `l[base..]` panics mid-character. Reachable: the body loop admits any line more indented than the PARENT key, so a line may sit between the key's indent and `base`. Blast radius is unchanged — `ci.yml` is dense with em-dashes inside `run: \|` blocks, and the failure is **exit 101**, neither "gate failed" (1) nor "gate could not run" (3), taking down every other gate in a `gate --all` batch. Fix is one expression: cut at `min(base, indent_of(l))`. Lost fix `bcf0b3083`. | **XS** |
| **31c** TSV `splitn(4)` corrupts 6-column rows | **OPEN** | `xtask/src/ledger.rs:114` `splitn(4, '\t')` against `scripts/release-gate/lib.sh:112`, which `printf`s **six** columns (`id\tstatus\ttitle\tdetail\tver\tsha`). `detail\tver\tsha` is welded into `detail`, and that value is what `parity::check` (`xtask/src/parity.rs:126` → `ledger::read_leg`) compares against a Rust gate's clean detail — **so every ledger-path parity run compares two different shapes.** `ledger.rs:1-3` still asserts the false interop claim (*"Rows in exactly the TSV shape `scripts/release-gate/lib.sh::record` writes"*). Lost fix `c32cc8509`. | **XS** |
| **31d** unreadable directory read as silently empty | **OPEN** | Nine sites of `let Ok(rd) = read_dir(..) else { return/continue }`. The load-bearing one is `xtask/src/ctx.rs:778` inside `collect()` — the walk behind `Ctx::walk`/`Ctx::list` that **every gate scans through**: an existing-but-unlistable directory yields `Ok(())` and zero files, and `WalkError` has `MissingRoot`/`BelowFloor`/`Io` but **no variant for a failed listing**. `WalkError::MissingRoot`'s own message states the principle this violates: *"zero is the passing answer to every ban — so a root that moved is an error here, never a narrower scan."* `BelowFloor` only catches it where a spec sets `min_files`, which defaults to 0. Live doc/code contradiction: `xtask/src/denylist.rs:188-190` asserts *"a `crates/` that will not list is the WalkError"* — false at HEAD. Satellites: `planes.rs:200,:222`, `denylist.rs:562`, `qa_names.rs:384`, `design_bindings/verify.rs:184`, `teller_steps.rs:382`, `construction/tree.rs:671,:713`. | **S** |

> **Measurement caveat, stated rather than hidden.** `xtask/src/full_gate.rs` and
> `xtask/src/gates/mod.rs` are DIRTY in this shared checkout. `run_one` is at `:699-711` at HEAD and
> `:813-827` on disk; **both are unbounded and the in-flight diff adds no child timeout**, so 31a
> holds either way. Every other citation in §10.5 is against a clean file.

### 10.6 DELIBERATE — do NOT "restore" these, and the block still holds

`9b14f4f5a` closed with a list of things that look like casualties and are not. **Re-measured at
`HEAD`, every one is still absent and still absent on purpose:**

| "missing" thing | why it is absent | measured |
|---|---|---|
| `contract-kinds` (an 8-kind proposal) | `PLUGIN-TREE.md:16` — *"There are **seven plugin kinds**"*, per #3 | the doc says seven |
| `Kind::Control` / `Kind::Dialect` (D36/D37) | `BUSBAR-1.6.0.md:68` — *"**Rejected, do not resurrect:** D36 … and D37"* | `grep -r 'Kind::Control\|Kind::Dialect' crates/` → 0 |
| `AdminSurface` | superseded shape | `grep -r AdminSurface crates/` → 0 |
| `units_*_leg.rs` / `plane_mount.rs` | #28 chose the gauntlet-kernel-rider design | no such file in `crates/busbar/src/root/` |
| the `Arena` fixed-cap model | #41 | `busbar-kernel/src/arena.rs` is gone (its `CredentialSlab` moved to `mask.rs` — see **L7**); what survives is `PlaneAllocBudget`, the bounded-allocation **error**, which is #41's outcome, not the retired model |

**One live consequence of that last row belongs beside `A23`:** the model was retired but the
plugin-facing NOUN was not. `A23` is the time-boxed rename of `arena` out of the ABI, and **L7 is
the evidence the rest of the concept really did go** — the file the register named no longer exists.

### 10.7 WHAT THIS RESTORATION ACTUALLY PROVES

Three things, and the third is why the section is here rather than in a commit body.

1. **"It is all still in git" is true and is not a filing system.** 21 of the 31 closed anyway —
   because they were *also* being worked by people who had read the tree, not because anyone
   re-read `9b14f4f5a`. The 8 that did not close are the ones no other path led back to. **The
   register's value was never the 21.**
2. **A deleted worklist is worse than an unwritten one**, because it leaves a citation. §7.3(2)
   could tell that 31 items existed and could not tell which — and two of them (L14/**A5**,
   L18/**A13**) were independently rediscovered *inside this same document*, at a cost no one
   billed. That is the measurable price of the deletion: **the same defect found twice.**
3. **The harvest's own output can be lost the same way.** §10.5.1's three fixes — a bounded child
   process, a YAML panic, a corrupting TSV reader — are **written, committed, and reachable only
   from `origin/delete/keep-a3-drain-survivors`.** Not findings in a body this time. Finished work,
   on a branch named for its own deletion.
