# 1.6.0 — THE RELEASE ENGINE: WHAT STANDS BETWEEN A GREEN TREE AND A PROMOTION

**Measured 2026-09-23** against `consolidated/1.6.0` at `27c2194642`, `origin/{dev,qa,main}` as
fetched that morning, the LIVE branch-protection objects on `GetBusbar/busbar`, the LIVE workflow
enable/disable state, and the sibling repo `GetBusbar/busbar-release` at `reconcile/main-final`
(one commit ahead of `origin/main`, dirty).

**This file is a LIST, not a fix.** Nothing here was repaired, no workflow was edited, no
protection was written, nothing was dispatched. Every row is a claim you can re-run.

The spec is `docs/design/BUSBAR-1.6.0.md`; **Part 6 (line 1019) is the release engine**. Where a
row contradicts Part 6, the row says so and shows the command that settles it. Part 6 was written
2026-09-21 and four of its Promotion claims have gone stale in two days.

---

## THE ANSWER FIRST

**No. This repository cannot promote today, and no quantity of green changes that without four
specific acts.** Not one of the four is a code change; three are a switch and one is a drain.

`qa` requires 4 status contexts. `main` requires 6. Of those, **one can never report at all**
(`record the staged digest (the promote's only input)`, because its workflow is
`disabled_manually`), **two do not exist as job names on the branches that require them**
(`ship-ready`, `construction gate (…)`), and **two are red by design on the current tree**
(`ci umbrella`, because the six money rigs are stated in `ci.yml` itself as "EXPECTED RED TODAY";
`ship-ready`, because its own standing-reds row refuses a `qa`/`main` posture while five
construction rows stand red — measured, see R-11).

Separately: the **entire release path is switched off at the GitHub level.** `Release stage`,
`Release`, `Docker`, `Verify deploy`, `tag-on-main` and `Prepare release (on dev)` are all
`disabled_manually`. A push to `qa` today runs `CI` and nothing else — no staging build, no
staged record, no image. A push to `main` today runs `CI` and nothing else — no tag, no release,
no container promotion. The release engine is not blocked by a bug; **it is turned off.**

The minimal set is at the bottom of this file.

---

## PART A — THE EIGHT KNOWN-BROKEN ITEMS, RE-VERIFIED

Each was re-measured against the current tree. Nothing was inherited.

### K-1 · `gate-mutants` required on `qa`+`main` while disabled — **FIXED**

`gh api repos/GetBusbar/busbar/branches/{qa,main}/protection` returns contexts that do **not**
include `gate-mutants`. `main` = 6 contexts, `qa` = 4. The workflow is still
`disabled_manually` and still `workflow_dispatch:`-only (`.github/workflows/gate-mutants.yml:35`),
which is now correct rather than deadlocking.

`scripts/ci-branch-protection.sh` no longer merely *claims* the removal: `RETIRED_CONTEXTS_JSON`
(line ~133) is a real subtraction applied after the union, with the ruling cited, and
`cmd_selftest` case (g) proves it. The comment that used to lie is now the comment that explains
why a union alone could never have done it.

**Blocks: nothing.** Closed.

### K-2 · `ship-ready` / `construction gate (…)` exist on some branches only — **STILL BROKEN**

Counts of the job name in each branch's own `ci.yml`:

| ref | `ci umbrella` | `structure lint` | `construction gate (…)` | `ship-ready` |
|---|---|---|---|---|
| `origin/dev` | present | present | **0** | **0** |
| `origin/qa` | present | present | **0** | **0** |
| `origin/main` | present | present | **0** | **0** |
| `origin/predev` | present | present | 1 | 1 |
| `origin/integration/1.6.0-dev-green` | present | present | 1 | 1 |
| `consolidated/1.6.0` (trunk) | present | present | 1 | 1 |

Part 6 calls this "REAL, but SELF-HEALING, not a deadlock", on the reasoning that check runs
attach to a SHA and qa→main is a fast-forward. **The reasoning is right for qa→main and wrong for
→qa.** A branch with required status checks and `enforce_admins: true` refuses a direct push whose
head SHA does not already carry every required context green. So the first promotion cannot
"produce those runs against that SHA" by landing on `qa` — the contexts must exist on the SHA
*before* `qa` moves to it.

That is satisfiable, and the path is narrow: the candidate SHA must live on a ref `ci.yml`
triggers on (`integration/**`, per `ci.yml:66`), **and** that run must be FULL tier. See S-1 —
a fast-tier green is a green about a smaller subject.

**Blocks: every push to `qa`, and therefore every promotion.**

### K-3 · `ceilings.rs` dangling `origin/integration/oracle-phase0` with a silent `HEAD~1` fallback — **FIXED, and the fix is the good kind**

The hardcoded constant is gone. `BASE_ENV` (`ceilings.rs:73`) is now an *override*, not the base;
`base_line` (`:478`) derives the base from the checkout's own line (`origin/<branch>`); and
`base_ref` (`:407`) returns `Err` — a red row — in four cases that were all previously silent
passes: detached HEAD with no override, a ref that does not resolve (`:412`, *"A base that cannot
be established is RED, never a silent fall-back to the last commit"*), an empty merge-base, and —
this is the new one — **a ref that resolves but has DIVERGED**. That fourth case is the
generalisation the first repair missed: `origin/predev` resolved fine and was 88 commits ahead of
a 178-commit-old fork point, so the row reported four days of eighteen agents' ceiling movement as
the work of one commit. Resolving is not the same as being establishable.

There is still a `HEAD~1` arm (`:426`), and it is not the old defect: it fires only when the
merge-base **is** HEAD (the checkout is at or behind its own line's tip), and it states its own
derivation in the row detail (`how: "HEAD~1, because HEAD is at or behind {r} ({why})"`). The
`Base` struct exists precisely so "which ref, how far back" is printed rather than assumed.

The workflow side matches: `ci.yml`'s `construction-gate` (`:3432`) and `ship-ready` (`:3547`)
both set `XTASK_CEILING_BASE` to the PR base SHA on `pull_request` and to empty on `push`, with
`fetch-depth: 0`, and the explicit `predev` fetch that used to stand in both is deleted.

**Measured:** `ship-ready:ceiling-rose` is **PASS** on the trunk today (R-11).

**Blocks: nothing.** Closed.

### K-4 · Unconditional `git fetch` of the dead ref, exit 128, inside `ship-ready` and `gate-mutants.yml` — **FIXED**

`grep -rn "git fetch" .github/workflows/` returns no `oracle-phase0` anywhere. `ship-ready` has no
fetch step at all. `gate-mutants.yml` fetches `predev` at `:91`, `:146`, `:238` and each one is
`|| echo "::warning::…"` — resilient, with the hard refusal pushed downstream into
`gate-mutants.sh::gm_base` (`:160-165`), which returns 2 rather than reporting an empty scope. A
failed fetch is a red, not a quiet "nothing changed". `origin/predev` currently resolves
(`e2194e4224c3`).

`docs/design/1.6.0-LEDGER.md:136` (RB3) still records this as live with *"the fix is in the
working tree and has reached no branch CI observes"*. **That ledger row is now stale** — the fix is
on the trunk and on `origin/predev`.

**Blocks: nothing.** Closed. (But see N-4 for what `gate-mutants.sh` now dangles on instead.)

### K-5 · turnstile `admit` runs a subcommand that does not exist — **WITHDRAWN, and here is the run**

`cargo xtask conformance check --musts` exists (`xtask/src/conformance_check/mod.rs:92`, usage at
`:55`) and was executed against the trunk with the prebuilt `target/debug/xtask`:

```
./target/debug/xtask conformance check --musts --format=tsv   →  exit 1, 17 FAIL rows
```

Ten are `pass is STALE` (verdict commit `67ee79103050` ≠ candidate sha) — a2a, mcp, the six
llm-* suites, both voice-*. Seven are `not a fresh pass` / `manifest status is not-run` — fapi2,
h2, jev, oidf-oauth2, slsa-verifier, tls, ws. Exit 2 (bad args) and exit 3 (unresolvable sha) are
distinct codes and neither fires.

The gate denies for real reasons. **The gate is working.**

**Blocks: `predev` → `dev` admission** (17 red suites), not the qa/main promotion. Real work, not a
broken instrument.

### K-6 · `turnstile-dispatch.yml` lives only in a worktree — **PARTLY FIXED; it is pushed and it is still unusable**

The file is committed on `origin/consolidated/1.6.0`. `git for-each-ref` over every remote branch
finds it on **exactly one**: the trunk. It is absent from `predev`, `dev`, `qa` and `main`.

It is `workflow_dispatch:`-only (no `on: push`), which is the correct shape and correctly argued
in its own header — predev carries no automatic CI by #67, and firing the full battery on every
unfinished predev commit is a false signal and a standing money leak.

**But a `workflow_dispatch` workflow must be present on the repository's DEFAULT branch to be
dispatchable at all.** The default branch is `main`, which does not carry it. Confirmation is
empirical, not theoretical: `gh api repos/GetBusbar/busbar/actions/workflows` lists 26 workflows
and `turnstile-dispatch.yml` is not one of them. GitHub has never seen the file. There is nothing
to dispatch.

The token is also still absent. `gh api repos/GetBusbar/busbar/actions/secrets` returns exactly
`BUSBAR_TRAIN_TOKEN`, `DISCORD_WEBHOOK_URL`, `PULLS_WRITE_SECRET`; the org set visible to the repo
is `BUSBAR_SIGN_KEY`, `CLOUDFLARE_*`, `CODECOV_TOKEN`, `DOCKERHUB_*`, `FLEET_CONTROL_GH_TOKEN`,
`LATCHKEY_API_TOKEN`, `RELEASE_DISPATCH_TOKEN`. **`TURNSTILE_DISPATCH_TOKEN` is in neither.** The
workflow fails loudly and by name when it is absent, which is right.

**Blocks: the cross-repo turnstile bridge, entirely.**

### K-7 · A hardcoded SHA described as "the current predev tip" in `oracle-rerecord.yml` — **STILL BROKEN ON THE REMOTE**

```
git -C busbar-release show origin/main:.github/workflows/oracle-rerecord.yml | sed -n '30,33p'
  description: "busbar ref/SHA to check out … Default: the current predev/integration tip."
  required: true
  type: string
  default: e2194e4224c3aebf1d7b3caf1bf71b6a73782c96
```

The corrected version — `required: true`, **no default**, and a description that says in as many
words that there is no "current tip" a workflow can assume — exists **only in the dirty working
tree** of `/Users/matthew/Developer/GetBusbar/busbar-release`, on branch `reconcile/main-final`,
**uncommitted**. `git log --all -- .github/workflows/oracle-rerecord.yml` shows no commit carrying
it. `origin/main` is what GitHub runs, and `oracle-rerecord.yml` is `active` there.

The sharpest detail: `e2194e4224c3` **is** `origin/predev`'s tip right now. The literal is
*currently correct*, which is exactly what makes it dangerous — predev is force-updated, the
default keeps resolving after it stops being true, and the artifact it produces still claims to be
the 1.5.5 golden. This is a **money-reference** input.

**Blocks: nothing mechanically** (`workflow_dispatch` only). **Risks: the money golden.**

### K-8 · #78's cost-watch — **BUILT, AND NOT ARMED. See Part E.**

---

## PART B — WHAT THE SWEEP FOUND BEYOND THE LIST

### N-1 · `record the staged digest (the promote's only input)` is required on `main` and its workflow is DISABLED — **the hardest blocker in this file**

```
gh api repos/GetBusbar/busbar/actions/workflows/.github%2Fworkflows%2Frelease-stage.yml
  → 345770557   disabled_manually   "Release stage"
```

That job name exists in exactly one place — `release-stage.yml:1523` (trunk), `:1170` on
`origin/qa` — and a `disabled_manually` workflow does not run on any trigger, on any branch.
`release-stage.yml` triggers on `push: branches: [qa]`; a push to `qa` produces **no run**, and
therefore no `record the staged digest` status on the SHA, ever.

This is K-1's exact shape — a required context whose only producer cannot report — and it is
**still live, on `main`, and it was not on the known list.**

It compounds: `release.yml`'s `resolve-staged` (`:530`) looks up the staged record by
`workflows/release-stage.yml/runs?head_sha=${SHA}&status=success` and refuses outright when there
is none, with *"There is NO fallback build here, deliberately."* That refusal is correct and it is
unavoidable while the workflow is off.

**Blocks: every qa→main promotion, absolutely.**

### N-2 · The whole release path is `disabled_manually`

| workflow | state | what its absence costs |
|---|---|---|
| `release-stage.yml` | **disabled** | no staging build, no staged record, no `staging-<sha12>` image, no `record the staged digest` context |
| `release.yml` | **disabled** | the promote itself does not run on a push to `main` — no tag, no published release, no `latest` |
| `docker.yml` | **disabled** | `release-stage`'s `stage-image` leg has no image builder |
| `verify-deploy.yml` | **disabled** | the staged-image verifier |
| `tag-on-main.yml` | **disabled** | — |
| `prepare-release.yml` | **disabled** | the version bump on `dev` |
| `plugin-ci.yml` | **disabled** | — |
| `ci.yml`, `qa-gate.yml`, `oracle-rebless.yml` | active | the only three things that run |

Nothing in the tree records *why* these are off or what turns them back on. A push to `main` today
is inert — which is a mercy given the rest of this file, and is also why "the tree is green" and
"the tree can ship" are currently unrelated statements.

### N-3 · Every renamed workflow on the trunk is invisible to GitHub

The trunk carries `sched-cost-watch.yml`, `sched-scorecard.yml`, `sched-ci-images-mirror.yml`,
`sched-monthly-refresh.yml`, `sched-oracle-store-cells.yml`, `qa-codeql.yml`, `qa-security.yml`,
`qa-fuzz.yml`, `qa-conformance-{a2a,mcp,voice}.yml`, `manual-keep-proof.yml`,
`manual-bolt-pass.yml`, `oracle-proof.yml`, `fleet-autoscaler.yml`, `turnstile-dispatch.yml`.

`gh api …/actions/workflows` knows **none** of them. It knows their pre-rename paths
(`codeql.yml`, `security.yml`, `keep-proof.yml`, `monthly-refresh.yml`, `ci-images-mirror.yml`,
`oracle-record-store-cells.yml`, `hang-hunt.yml`, `runner-probe.yml`, `oauth-conformance.yml`,
`streaming-conformance.yml`, `fleet-control.yml`), all `disabled_manually`.

Two GitHub rules make this a functional finding rather than cosmetics:

* **`schedule:` fires only from the default branch.** Seven trunk workflows carry a `cron:`. None
  of them is on `main`. **Every scheduled job in this repository is dormant.**
* **`workflow_dispatch:` requires the file on the default branch to be dispatchable.** So
  `turnstile-dispatch.yml`, `manual-keep-proof.yml`, `manual-bolt-pass.yml`, `oracle-proof.yml`
  and `fleet-autoscaler.yml` cannot be run by anyone, by any means, today.

### N-4 · `gate-mutants.sh` cites a Rust constant that no longer exists, and the two bases it warned about have already diverged

`scripts/gate-mutants.sh:49-58`:

```
# THE BASE. Spelled to match `xtask/src/gates/construction/ceilings.rs::INTEGRATION_REF`: a branch
# that is measured against one ref by the ceilings and another by the mutants can be green on both
# while being unheld on the difference between them.
# KEEP THIS IN SYNC WITH `INTEGRATION_REF` BY HAND -- there is no single source both files read,
# so a rename of one without the other is a branch measured against two different lines by its two
# gates, silently.
GM_DEFAULT_BASE="origin/predev"
```

`grep -rn INTEGRATION_REF` over the whole tree returns **three prose citations in this script and
one in `gate-mutants.yml:81`, and zero definitions.** K-3 deleted the constant. So:

1. the comment names a symbol that does not exist, and
2. the hazard it describes **has already happened**: `ceiling-rose` measures against the checkout's
   own line (K-3), `gate-mutants` measures against `origin/predev`. Two gates, two lines, silently
   — precisely the sentence the comment wrote about itself.

`gate-mutants.sh` does not fail open (`gm_base` returns 2 on an unresolvable base), so this is a
wrong-subject finding, not a fail-open one. `gate-mutants` is not a required check, so it blocks
no promotion.

### N-5 · `ship-ready` is required with `app_id: null`, and the protection script would widen the other five to match

`main`'s protection `checks` array:

```
{"context":"ci umbrella","app_id":15368}
{"context":"qa-gate umbrella","app_id":15368}
{"context":"record the staged digest (the promote's only input)","app_id":15368}
{"context":"structure lint","app_id":15368}
{"context":"construction gate (… BLOCKING, on its posture)","app_id":15368}
{"context":"ship-ready","app_id":null}          ← any app, or any commit status
```

`app_id: null` means the context is satisfied by a status from *any* source. `ship-ready` is the
**only** required check that encodes the ship criterion — twin at zero, no ceiling slack, no
ceiling rose, nothing standing red — and it is the one that can be satisfied by
`POST /repos/GetBusbar/busbar/statuses/<sha>` with `context=ship-ready&state=success` from any
token with write access. The five checks that are pinned are the five that matter least.

Worse, the pinning is **not** durable. `scripts/ci-branch-protection.sh::build_body` sends the
legacy `required_status_checks.contexts` array (line ~218) and never `checks`/`app_id`. The
script's whole design argument is read-modify-write so that *"running this script can never
regress a protection setting"* — and `app_id` is the field that argument does not cover: the GET
exposes it under `checks`, the PUT body discards it. **Running the protection-as-code script today
would silently unpin all six contexts from GitHub Actions.**

### N-6 · `ci umbrella` is required on `qa` and `main`, and it is RED by design on the current tree

`ci.yml:2150`, inside the `plane-rigs` job (`:2068`), about six steps at `:2164-2175`:

> **THEY GATE, AND THEY ARE EXPECTED RED TODAY.** No `continue-on-error`, deliberately: each one
> is a decision the tree does not yet keep […]

The six are `mcp.rig class-price`, `mcp.rig card-epoch`, `mcp.rig unpriced-refuses` and the three
a2a twins. `plane-rigs` is in `ci-umbrella`'s `needs` **and** is scored in its `RESULTS` block as
a `full` tier row (`ci.yml:3706`). On a push to `qa`, `FULL_TIER` is `true`, a skip is not
forgiven, and the umbrella exits 1.

So `ci umbrella` — the single most-required context in this repository — **cannot go green on a
qa push while the three money defects named in that comment stand** (class prices at a silent
zero; the meter classes are neither reportable nor priceable; `/admin/usage` reprices closed
windows off the current card). Those are money defects, which makes this binary.

### N-7 · `scripts/release-order-lint.py` is cited by four workflows as the enforcer of the stage/release split and does not exist

`release.yml:64`, `release-stage.yml:48`, `docker.yml:65`, `docker.yml:840`, `ci.yml:2826`,
`manual-bolt-pass.yml:26,42` all name it. `find . -name release-order-lint.py` → nothing. It was
deleted at `378572b3f` (2026-09-07).

**The enforcement is not lost** — it is `cargo xtask gate release-order`
(`xtask/src/gates/release_order.rs`), invoked at `ci.yml:347`, `:349`, `:406`, and
`xtask/src/full_gate.rs:534` documents the migration and the dead arm it killed. What is left is
seven prose citations of a file nobody can open, in the workflows that define the release split.
Dangling reference, correct behaviour.

### N-8 · `scripts/verify-1.6.0-done.sh` — the definition of done — has no CI caller

Part 6 names it as *"the definition of done"* and Part 7 §"How done is proven" leans on it.
`grep -rn "run:.*verify-1.6.0-done" .github/workflows/` returns nothing. Five workflow comments
*describe* what it runs; no workflow runs it. The DONE claim is a local script only.

### N-9 · The trunk that all work lands on runs no CI

`ci.yml:66` — `branches: ['integration/**', 'dev', 'qa', 'main', '!wip/**']`. `consolidated/1.6.0`
matches none of them.

```
gh api "repos/GetBusbar/busbar/actions/runs?branch=consolidated%2F1.6.0" --jq .total_count  →  0
```

Zero runs, ever, on the branch some eighteen agents commit directly onto. The most recent run
of *any* workflow in the repository is `2026-09-21T23:35:58Z`. **Nothing in this tree has been
CI-verified for two days**, which is the same two days in which the trunk took every commit in
this file's evidence. "A green tree" is currently an unmeasured claim.

(The block comment at `ci.yml:17-25` still says `branches: ['**']` and *"EVERY branch push now
runs CI"*. Part 6 already flags that comment as stale; it is worth saying that the staleness is
load-bearing, not cosmetic — it is why nobody noticed the trunk was dark.)

### N-10 · `busbar-release` has no secrets, so the turnstile can admit and cannot promote

`gh api repos/GetBusbar/busbar-release/actions/secrets` → **empty**. The org set visible to it is
the same nine listed in K-6 and contains neither `BUSBAR_TRAIN_TOKEN` nor
`BUSBAR_CHECKOUT_TOKEN`.

* `turnstile.yml`'s `Promote to dev on BOARD` step sets `GITHUB_TOKEN: ${{ secrets.BUSBAR_TRAIN_TOKEN }}`.
  Absent secret → **empty string**, not unset. `busbar-release-train/src/main.rs:182` guards with
  `std::env::var("GITHUB_TOKEN").map_err(|_| "GITHUB_TOKEN not set")` — `env::var` returns
  `Ok("")` for an empty value, so the named guard does not fire and the run dies on a 401 from the
  API instead of on the sentence that would have explained it. A small fail-open in the message,
  not in the outcome.
* `BUSBAR_CHECKOUT_TOKEN` is absent and the fallback to `github.token` **works**, because
  `GetBusbar/busbar` is `public`. Not a blocker.

`BUSBAR_TRAIN_TOKEN` does exist — **as a repo secret on `GetBusbar/busbar`, which is the wrong
repository.** `turnstile.yml` runs in `busbar-release` and cannot read it.

**Blocks: `predev` → `dev`, from the other side of the bridge that K-6 already blocks.**

---

## PART C — THE SILENT-FALLBACK REGISTER

This repository's signature defect: the instrument keeps running, produces a number, and the
number is about the wrong subject. Every one found, including the ones that turn out to be honest.

| # | instrument | the fallback | verdict |
|---|---|---|---|
| **S-1** | **`ci umbrella` tier-awareness** (`ci.yml:3617`, `FULL_TIER` at `:3690`) | On a push to any ref that is not `dev`/`qa`/`main`, thirteen full-tier jobs — `openapi-schema`, `migration-corpus`, `executable-config-lint`, `no-default-features`, `deletion-test-matrix`, `feature-sets`, **`plane-rigs`**, `no-plugins-gate`, `txn-guards`, `timing`, `windows`, `perf-build-gate`, `teller-steps` — may be `skipped` and the umbrella prints `skipped (full-tier job on a fast-tier run — allowed)` and exits **0**. | **LIVE AND LOAD-BEARING.** Branch protection reads only "is context X green on SHA S", not which run produced it. K-2 forces the candidate SHA's contexts to be minted on an `integration/**` push — i.e. **fast tier**. A `ci umbrella` green minted that way satisfies `qa`'s required check while having proven about half of CI, and it is the *only* configuration in which the promotion is mechanically possible. The escape hatch is real and must be used: `pull_request` and `workflow_dispatch` both force FULL tier (`ci.yml:139-142`). |
| **S-2** | **`secret-hygiene`** (`ci.yml:1466`) | `SECRET_GATE_REPORT_ONLY=1 scripts/secret-hygiene-gate.sh --report` → prints the violation count and **exits 0** (`secret-hygiene-gate.sh:51-52`, `:1440`, `:1452`). | **ADVISORY, AS DESIGNED AND AS WARNED.** The script's own line: *"Set `SECRET_GATE_REPORT_ONLY=0` to arm the hard gate."* Its findings block nothing. One thing redeems it: the debt-count row at `:846` fails **independently** of the flag, so the number cannot silently grow. |
| **S-3** | **`construction gate (…)`** (`ci.yml:3432`, `run: cargo xtask gate construction --posture`) | `--posture` is green while the gate is red on exactly the rows `gates::REPORT_ONLY` names, and red the instant an unnamed row reds **or a named row goes green and the list is stale**. | **HONEST.** This is the good version of the pattern, and it is worth saying so: `Excused` is *checked on the run*, a stale excuse excuses nothing, and `--all` prints the excuse it applied on every run. `continue-on-error` is gone and the job is scored in the umbrella (`ci.yml:3714`). It is advisory about a **named, finite, self-invalidating** list. |
| **S-4** | **`qa-names`** (`ci.yml:330`, `--posture`) | Same mechanism. | **HONEST AND NOW VACUOUS** — `QA_NAMES_STANDING_REDS` is `&[]` (`gates/mod.rs:245`), all 25 dead names drained, so every row is scored like any other. |
| **S-5** | **`ceiling-rose` base** (`ceilings.rs:407`) | *Formerly* "does not resolve → `HEAD~1`". | **FIXED** — see K-3. Four distinct reds where there used to be one silent pass, and the surviving `HEAD~1` arm prints its own derivation. This is the template the rest of the tree should copy. |
| **S-6** | **`ship-ready` posture** (`ship_ready.rs:88`, `posture_of_branch_output`) | *Formerly* `unwrap_or_default()` → `""` → `is_shipping("")` false → **PASS arm**. A git failure answered "this is the dev line, carry on" to a question it could not ask. | **FIXED, and the fix is exemplary.** `Posture::Unknown` is a third state; `"HEAD"` (detached) and `""` both fold into it rather than reading as a branch name. |
| **S-7** | **`ship-ready`'s standing-red row vs. the push that it gates** | `XTASK_SHIP_TARGET: ${{ github.ref_name }}` (`ci.yml:3600`). `ROW_STANDING` is red only for `SHIPPING_TARGETS = ["qa","main"]` (`ship_ready.rs:49`). The context that unblocks a push to `qa` is necessarily minted on a branch that is **not** `qa` (K-2), where `ref_name` is e.g. `integration/1.6.0-dev-green` and the row **passes by posture**. | **LIVE.** The one row whose entire job is to refuse a promotion is evaluated under the dev-line posture on the run that authorises the promotion, and under the shipping posture only on the run that happens *after* the push. Today this is masked — the other three rows are red anyway (R-11) — so it changes no outcome yet. It is recorded because the day the twin and the slack go green is the day this becomes the only thing between a standing red and `main`. |
| **S-8** | **`busbar-train` token guard** (`main.rs:182`) | `env::var("GITHUB_TOKEN").map_err(…)` — an Actions secret that does not exist arrives as `""`, not unset, so `Ok("")` passes the guard. | **LIVE, cosmetic.** The run still fails (401), but on the wrong sentence. See N-10. |
| **S-9** | **`gate-mutants` fetch** (`gate-mutants.yml:91,146,238`) | `git fetch … || echo "::warning::…"`. | **HONEST.** The warning is paired with a hard downstream refusal (`gm_base` → exit 2), so a failed fetch cannot read as an empty scope. Documented in the step's own comment. |
| **S-10** | **`qa-gate umbrella`** (`qa-gate.yml:377`) | `if: !cancelled()` and it **re-states** the trigger condition, because `!cancelled()` breaks the `needs`-skip cascade. Treats a `skipped` tier as **red** on purpose. | **HONEST**, and the comment explains the exact silent-dormancy bug it was built to kill (an auto-fired gate that ran ONE job and went green). |
| **S-11** | **`qa-gate` loads from the default branch** | `workflow_run` always loads the workflow file from `main`. | **CHECKED, NOT A FINDING.** `origin/main:.github/workflows/qa-gate.yml:306-307` does carry `umbrella: / name: qa-gate umbrella`. The context can report. It reports only when CI on `qa` concluded `success` — which N-6 currently prevents. |
| **S-12** | **`plugin-ci.yml:880`, `fleet-autoscaler.yml:314`, `manual-keep-proof.yml:774`** | `continue-on-error: true`. | **NOT GATING ANYTHING TODAY** — all three workflows are unregistered or disabled. Each carries a written reason. Listed for completeness. |

---

## PART D — MEASURED: WHAT `ship-ready` ACTUALLY SAYS (R-11)

Run on the trunk at `27c2194642`, `XTASK_SHIP_TARGET=qa`, against the prebuilt
`target/debug/xtask` — the exact posture a promotion carries:

```
FAIL  ship-ready:standing-reds   target `qa` is a shipping line and 5 construction row(s) are still
                                 standing red: hold-discipline:cancellation-before-await,
                                 one-pick-site, ports-only-tests:busbar-llm, request-path-fn-size,
                                 terminal-doors-in-audit-step
FAIL  ship-ready:ceiling-slack   loc-ceilings:kernel measures 52476 against ceiling 52950 (slack 474)
PASS  ship-ready:ceiling-rose    `ceiling-rose` is green
FAIL  ship-ready:ship-twin       kind-isolation-ship is not green: deps, test-deps, vocab, registry,
                                 matrix, faces, shape, testkit, legacy-drain, control-path
ship-ready: 4 row(s), RED
```

`CONSTRUCTION_STANDING_REDS` is `gates/mod.rs:227-236`, five entries, non-empty. The row's own
sentence is the right one to quote: *"Promoting it does not drain it — it promotes the breakage and
retires the record of it."*

`ceiling-slack` is the cheapest of the three: one number, and
`cargo xtask gate construction --write` re-pins downward.

---

## PART E — THE COST VERDICT (#78)

**The instrument is built. It has never run, and it cannot run.**

Built, and built well:

* `scripts/cost-watch.py` — 71 KB, with the four-band exit contract Part 6 specifies
  (`0` clear / `2` $50 soft-alarm / `3` $80 review / `1` $100 cap breach / `4` tool error) and a
  `--selftest` that proves all four bands on planted fixtures, offline.
* `.github/workflows/sched-cost-watch.yml` — weekly `cron: "0 12 * * 1"`, `latchkey-small`,
  10-minute ceiling, `permissions: contents:read + actions:read` and nothing writable, self-test
  before the real read, and a `case` that maps every one of the five exit codes — including an
  unrecognised one, which is treated as drift between the script and the step and fails.

Not armed:

1. **`sched-cost-watch.yml` is not on `main`.** `schedule:` fires only from the default branch.
2. **GitHub has never seen the file.** It is absent from `gh api …/actions/workflows`.
3. **Zero runs.** No `cost-watch` run exists in the repository's history.

So the acceptance criterion Part 6 states — *"cost-watch checks the LK Cost-Analysis each
period"* — is **not met**, and the owner's instruction stands unsatisfied on its own terms:
*"if you incur costs thats fine just dont let htem run blindly, monitor for unaways, poor
performing code, issues in ci that just waste money."* **Today, nothing notices a runaway but the
invoice.** The change is one file reaching `main`. Cost: zero.

Two further money rows:

* **The 24 → 8 shard lever was never applied.** Part 6 states the remedy for the $1,245.94 /
  72.86% `gate-mutants` finding as *"the default `shards` input drops 24 → 8 for routine
  dispatches"*. `.github/workflows/gate-mutants.yml:40` reads **`default: '24'`**. The measured
  shape of that spend was 23 dispatches in 36 hours, 22 failed or cancelled, ~$56 an attempt. The
  gate is disabled and unrequired, so the exposure is dormant rather than live — but the lever
  that makes a failing dispatch cheap is still not in the file, and the document says it is.
* **`release-stage.yml` on every qa push is a ~40-minute two-arch PGO build, by design**
  (`release-stage.yml:64-67`, *"Pushing to qa is the act of spending that"*). That cost is
  deliberate and correctly documented. It is named here only so that arming N-1 is understood as
  arming a real bill, and so that the cost-watch above is armed **at the same time**, not after
  the first month of it.

---

## PART F — THE MINIMAL SET THAT MAKES A PROMOTION POSSIBLE

Ordered by dependency. Nothing here is optional; each item is a precondition, not an improvement.

**Owner-only switches (three acts, zero code):**

1. **Enable `Release stage`.** Without it, `record the staged digest (the promote's only input)`
   — required on `main` — has no producer, and `release.yml`'s `resolve-staged` refuses by
   construction. (N-1)
2. **Enable `Release`, `Docker` and `Verify deploy`.** `Release` is the promote; `Docker` is
   `Release stage`'s image builder; `Verify deploy` is its verifier. `Release stage` green without
   `Docker` is a staging run with no staged image. (N-2)
3. **Land the trunk's `.github/workflows/` onto `main`.** This is what registers `sched-cost-watch`
   (arming #78 — item E, and the one with a dollar figure attached), `turnstile-dispatch` (K-6),
   and every other renamed workflow. Scheduled and `workflow_dispatch` workflows are dead until
   their files sit on the default branch. (N-3)

**Tree work (two drains, both measured):**

4. **Green the six money rigs** in `plane-rigs` — class-price, card-epoch, unpriced-refuses on
   both mcp and a2a. `ci umbrella` is required on `qa` **and** `main` and is red by design until
   they pass. These are money defects (a class pricing at a silent zero; closed windows repriced
   off the current card), so this is binary. (N-6)
5. **Green `ship-ready` under a `qa` posture** — three rows, three different drains: five standing
   construction rows (`gates/mod.rs:227`), one ceiling with 474 lines of slack
   (`cargo xtask gate construction --write` re-pins downward), and the ten `kind-isolation-ship`
   rows. `ceiling-rose` already passes. (R-11, K-2)

**Mechanics of the first push to `qa` (get this wrong and the push is refused):**

6. The candidate SHA must carry **all four** of `qa`'s required contexts **green, on that exact
   SHA, before `qa` moves** — `enforce_admins: true` and there is no bypass. Since `ship-ready`
   and `construction gate (…)` exist as job names only on `predev` / `integration/**` / the trunk
   (K-2), the candidate must sit on an `integration/**` ref, which is what `ci.yml:66` triggers
   on.
7. That run must be **FULL tier**, not the fast tier a plain `integration/**` push buys. Use the
   escape hatch `ci.yml` already documents —
   `gh workflow run ci.yml -R GetBusbar/busbar --ref <integration-branch>` — or a pull request.
   A fast-tier `ci umbrella` green is a green about thirteen jobs that did not run, and it will
   satisfy branch protection anyway. (S-1)
8. Promote `qa` → `main` by **fast-forward only** (`git push origin qa:main`). `release.yml:49-51`
   and `:530` resolve the staged record by `main`'s own head SHA; a merge commit, a direct commit
   or a cherry-pick mints a SHA `qa` never saw and is refused with no fallback build.

**Correctness repairs that block nothing and are cheap (do not lose them):**

9. **Commit and push the `oracle-rerecord.yml` fix.** `origin/main` of `busbar-release` still
   carries `default: e2194e4224c3…`; the corrected, defaultless version is uncommitted in a dirty
   worktree on `reconcile/main-final`. It guards a **money** golden. (K-7)
10. **Provision `TURNSTILE_DISPATCH_TOKEN`** on `GetBusbar/busbar` and **`BUSBAR_TRAIN_TOKEN`** on
    `GetBusbar/busbar-release` (it currently exists on the wrong repo). Without the first, no
    candidate reaches the turnstile; without the second, the turnstile can admit and cannot
    promote. (K-6, N-10)
11. **Do not run `scripts/ci-branch-protection.sh` as it stands.** Its PUT body sends only the
    legacy `contexts` array, which discards `app_id` — one run unpins all five pinned contexts to
    "any app". Pin `ship-ready` (currently `app_id: null`) and teach `build_body` to carry
    `checks` before it is run again. (N-5)
12. **Strike the dangling citations**, none of which changes behaviour and all of which cost a
    reader time: `INTEGRATION_REF` in `gate-mutants.sh:49,53,55` and `gate-mutants.yml:81` (the
    constant is gone, and the two bases it warned about have already diverged — N-4);
    `scripts/release-order-lint.py` in seven workflow comments (the gate is
    `cargo xtask gate release-order` — N-7); `ci.yml:23`'s `branches: ['**']`, which is why nobody
    noticed the trunk is dark (N-9); `ci.yml:3607`'s note that ship-ready reads a mutation verdict
    from the checks API (`ship_ready.rs:18-22` removed that row); and
    `docs/design/1.6.0-LEDGER.md:136` (RB3), which records K-4 as live after it was fixed.

**Not in the set, and named so nobody re-derives them:**

* `gate-mutants` — correctly retired from protection, correctly disabled, correctly
  `workflow_dispatch`-only. Its 24-shard default is a money row (Part E), not a promotion row.
* The 17 red conformance suites (K-5) — they block `predev` → `dev` admission through the
  turnstile, which is a different gate from the qa/main promotion this file is about.
* `secret-hygiene` (S-2) — advisory by an owner-visible flag with a written flip condition, and
  its debt count is hard-gated independently.

---

## HOW TO RE-RUN EVERY CLAIM IN THIS FILE

```sh
R=/Users/matthew/Developer/GetBusbar/busbar
gh api repos/GetBusbar/busbar/branches/main/protection --jq .required_status_checks.checks
gh api repos/GetBusbar/busbar/branches/qa/protection   --jq .required_status_checks.checks
gh api repos/GetBusbar/busbar/actions/workflows --jq '.workflows[]|"\(.state)\t\(.path)"'
gh api repos/GetBusbar/busbar/actions/secrets  --jq '.secrets[].name'
gh api repos/GetBusbar/busbar-release/actions/secrets --jq '.secrets[].name'
gh api "repos/GetBusbar/busbar/actions/runs?branch=consolidated%2F1.6.0" --jq .total_count

for r in origin/dev origin/qa origin/main origin/predev HEAD; do
  echo -n "$r  "; git -C $R show $r:.github/workflows/ci.yml \
    | grep -c 'name: ship-ready$'
done

git -C $R/../busbar-release show origin/main:.github/workflows/oracle-rerecord.yml | sed -n '30,33p'
grep -rn INTEGRATION_REF $R --exclude-dir=target --exclude-dir=.git
grep -n "default: '24'" $R/.github/workflows/gate-mutants.yml

$R/target/debug/xtask conformance check --musts --format=tsv ; echo "exit $?"
XTASK_SHIP_TARGET=qa $R/target/debug/xtask gate ship-ready
```

---

*Scope note: this file is a survey of the release engine and CI only. `qa/*`,
`xtask/src/gates/` and `docs/design/1.6.0-map-proof.md` were READ and not written;
`.github/workflows/ci.yml` was READ and not written. No workflow was enabled, dispatched or
edited; no branch protection was modified; nothing was pushed.*
