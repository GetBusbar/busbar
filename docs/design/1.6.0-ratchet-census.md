# 1.6.0 RATCHET CENSUS — every owed item recorded in `qa/`, counted

**Date:** 2026-09-22 · **Branch:** `consolidated/1.6.0` · **Method:** every gate run, every
subject checked against the filesystem, every row tested for whether it suppresses a finding TODAY.

## What this document is, and what it is not

This is a **census, not a repair**. Nothing here was silenced. No ceiling was raised, no row was
struck, no exception was added. Where a number is red, it is recorded red. Where a row is dead
weight, it is named and left in place for its owner.

`qa/*.toml` is the largest reservoir of recorded-but-uncounted work in the tree. Every ratchet row,
every `known_missing_*`, every grandfather entry and every ceiling held red on purpose is a piece of
debt somebody wrote down and nobody has since totalled. This totals them.

### The four questions asked of every entry

1. **What debt does it hold** — the rule it excuses and the count it pins.
2. **Is the subject ALIVE?** A row naming a deleted crate or a moved path excuses nothing and hides
   nothing.
3. **Does it suppress anything TODAY?** The strong test. A row can pass a path-existence check and
   still be vacuous — the file exists but no longer holds the thing the row excuses.
4. **What would closing it take**, and is it blocked on a ruling.

### The dead-subject class, found NINE times now

The brief recorded this class found five times today. This sweep found it in **nine more tables**:

| # | where | what died |
|---|---|---|
| 1 | `construction.toml` `[rules.loc-ceilings].caps_crate` | `busbar-caps` — crate folded away |
| 2 | `construction.toml` `[rules.loc-ceilings].verbs_crate` | `busbar-unit-verbs` — crate folded away |
| 3 | `construction.toml` `[rules.lean-core].known_sites` | 8 of 43 rows drifted off their pinned line |
| 4 | `construction.toml` `[rules.ports-only{,-tests}]` | 8 rows keyed on `busbar_core::`, a namespace that no longer resolves |
| 5 | `construction.toml` `[rules.legacy-reach.prefixes.*]` | 2 of 3 prefixes name deleted crates |
| 6 | `plane-purity-strict.toml` `[test-reach]` | all 4 rows keyed on the same dead `busbar_core::` |
| 7 | `full-gate.toml` skip register | 6 of 16 rows name scripts not in the tree |
| 8 | `audit-ledger.json` | 24 scopes for deleted crates + 10 dead excludes + 1 dead glob |
| 9 | `no-deferral.waivers` | 38 of 52 rows drifted off their line numbers |

**The decisive fact behind #4/#5/#6:** `crates/busbar-core` is deleted, `crates/busbar-substrate` is
deleted, and `crates/busbar-kernel/src/lib.rs:29` now reads `extern crate self as busbar_kernel` —
the old `extern crate self as busbar_core` alias is gone. `busbar_core::` has **exactly one
occurrence in the whole tree**, inside a comment explaining the rewrite. Twelve gate rows are keyed
on a needle that can never match again, and every one of them prints a green `0`.

---

# PART 1 — `qa/construction.toml` (1,656 lines, 110 gate rows)

Gate: `cargo xtask gate construction`. **Verdict today: RED, 14 of 110 rows failing.**

## 1.1 Ceilings held RED — breaches, not waivers

| row | holds | measured | ceiling | over by | subject alive? | suppresses? | cost to close | blocked-on |
|---|---|---|---|---|---|---|---|---|
| `surface-ceiling:contract` | plugin-visible surface of `busbar-contract` | **6,837** | 5,652 | **+1,185** | yes | n/a — breach | move `records.rs` (631) out; #83 still owes `api` ~727, `substrate-values/ir` ~508, `secret-ref` ~274, `media`/`billing`/`wire`/`lossless` ~198 **in** | **owner: is 5,652 still the ruling?** |
| `loc-ceilings:caps-contract` | same measurement, kernel-budget arithmetic | **6,837** | 5,652 | **+1,185** | `caps_crate` **DEAD** | n/a — breach | same subject as above | same ruling |
| `loc-ceilings:union` | kernel + caps/contract + unit-* | **60,189** | 58,870 | **+1,319** | yes | n/a | set to the SUM OF MEMBER CEILINGS on purpose; closes when caps-contract does | derivative |
| `loc-ceilings:kernel` | `busbar-kernel` own + reachable unit files | **53,084** | 52,950 | **+134** | yes | n/a | 134 lines out of the kernel | no |
| `loc-ceilings:kernel:teller` | the Teller loop file | **685** | 661 | **+24** | yes | n/a | 24 lines out of `teller.rs`; pinned at the drained reading on purpose | no |
| `hold-escapes` | deliberate forget/leak/unwind-smuggle of a hold | **65** | 0 | **+65** | yes | n/a | 3 `Box::leak` + 62 `AssertUnwindSafe`, concentrated in `busbar-kernel/src/plane_host/**` | no — engineering |
| `lean-core` | dialect/open-vocabulary literals in kernel + unit crates | **11** | 0 | **+11** | yes | n/a | 8 are line-drift off `known_sites` (§1.4); 3 are deliberate reds (`plane_host` chat/sampling bridge ×2, `cost.rs` `"Prompt"`) | **owner: operator-visible billing term + bridge relocation** |
| `teller-step-order` | nine steps once each, canonical order | **6** | 0 | **+6** | yes | n/a | `run_unit` runs `approve` and `admit` twice and puts them before `verify` | no |
| `one-pricing-site:fee-fields` | per-request fee read only where the card lives | **4** | 0 | **+4** | yes | n/a | 4 D38 sites in `busbar/src/root/units_admin/mod.rs:1118,1149,1155,1247` — belong to the money-function consolidation to DELETE | no — deliberately not waived |
| `no-test-doubles-in-production:doubles` | reviewed stand-ins that are doubles | **7** | 5 | **+2** | yes | n/a | `RecordingRows` in `main.rs` grew 1 → 4 sites under a ratchet of 5 | no |
| `one-pick-site` | lane-pick callers | **4** | 2 | **+2** | yes | n/a | `busbar-kernel-egress/src/{exhaustion,walk}.rs` + 2 in `busbar-llm` | no |
| `plane-no-money` | a plane names a price | **1** | 0 | **+1** | yes | n/a | `priced_from_ms` at `busbar-llm/src/unit/meter.rs:490` | no |
| `ceiling-census` | a rule table or kind glob went away | **3** | 0 | **+3** | yes | n/a | 3 census floors went DOWN vs base `05ea02ef` (`plane` 6→5, `pure_auth` 2→0, `unit` 14→1); **self-expiring** the moment this lands | no |
| `hold-discipline:cancellation-before-await` | cancel check before every route-step `.await` | **vacuous** | 0 | — | **NO** | **NO** | gate says verbatim: *"vacuous: no `.await` found inside a route-step function in scope"* — a RED that measures the empty set | **owner: repoint `route_step_functions` or strike the sub-rule** |

> **CORRECTION to the brief.** The `busbar-contract` breach is quoted as 1,007 lines. That was the
> figure when the comment was written (6,659 vs 5,652). **It measures 6,837 today — the breach is
> 1,185 and growing.** It is held red across three rows on purpose, and that posture is correct;
> the number is not.

> **CORRECTION to the brief.** `one-pricing-site:fee-fields` is quoted at 9 findings. That was the
> count *before* the `busbar-admin` → `busbar-core-admin` repoint landed. The repoint restored 5
> reviewed sites; **4 stand red today**, and the file's own note is explicit that they must stay red
> because they belong to the money-function consolidation.

## 1.2 Live ratchets holding real, owed work (currently PASSing)

| entry | holds | measured / ceiling | alive? | suppresses? | cost to close | blocked-on |
|---|---|---|---|---|---|---|
| `[rules.sealed-unit-traits].max_unsealed` | one unit trait still unsealed | 1 / 1 | yes | yes | seal `busbar-kernel-budget::Admission` on a private supertrait | no |
| `[rules.one-teller-loop].max_legacy_sites` | legacy `run_gauntlet` adapter call sites | 4 / 4 | yes | yes | 4 sites; goes to 0 when the planes move off the shim | no |
| `[rules.legacy-reach].ceiling` | root's distinct symbols into retiring crates | 17 / 17 | yes | yes | 17 distinct `busbar_llm::` symbols in `crates/busbar/src/root/*.rs` + `main.rs` | no |
| `[rules.forbid-unsafe].known_missing_forbid` ×6 | crates lacking `#![forbid(unsafe_code)]` | 6 | **all 6 alive, all 6 genuinely missing the attribute** | yes | one attribute line per crate: `hook-test-plugin`, `hooks-ranking`, `auth-static-plugin`, `auth-admin-tokens`, `secret-example-plugin`, `secret-ref` | no |
| `[rules.forbid-unsafe].known_missing_deny` ×4 | crates lacking `#![deny(unsafe_code)]` | 4 | **all 4 alive, all 4 genuinely missing** | yes | one line per crate: `plugin-loader`, `store-memory`, `store-example-plugin`, `export-example-plugin` | no |
| `[rules.lean-core].known_sites` × **35 live** | reviewed frozen-wire/schema literals | gate prints "3 of 35" (35 tracked, display capped at 3) | yes | **yes — 35 of 43 match a finding** | each is a frozen config key, migration table entry or metrics label; most are correctly permanent | no |
| `[rules.seal-sites].known_sites` ×4 | reviewed capability-minting homes | 4 | all 4 paths alive | yes | `busbar-contract/src/caps/`, `busbar/src/root/`, `busbar-llm/src/unit/`, `busbar-kernel-breaker/src/port.rs` | no |
| `[rules.no-test-doubles…].known_sites` `verdict="double"` ×4 live | fixture values in the shipped binary | 4 of 5 rows live | yes | `NullShipper`, `RecordingRows`, `Pricer::flat(`, `without_directory(` — all in `busbar/src/main.rs` | no |
| `[rules.plane-no-money].allowlist` ×1 | `busbar-llm/src/unit/verify.rs` may say `CostHandle`/`priced` | 1 | alive (`CostHandle`=2, `priced`=10) | yes | opaque-handle yes/no predicates; closes with the plane-side Verify rebuild | no |
| `[rules.manifest-allowlist].reviewed_extra` ×1 | `store-example-plugin` → `libc` | 1 | alive, dep still declared | yes | unix `flock` in the FileStore fixture | no |
| `[rules.manifest-allowlist].known_red_deps` ×2 | a plane names another plane | 2 | both alive and declared | yes | `busbar-plane-llm`→`busbar-llm`, `busbar-plane-streaming`→`busbar-voice` — dies with the codec split | no |
| `[rules.source-denylist].edge_exemptions` ×1 | `(dep=libc, via=cpufeatures)` tree-wide | 1 | alive | **yes — `denylist:stale-waivers` PASSES** | permanent by owner ruling 2026-09-22 | ruled |

**Subtotal: 61 live ratchet entries holding owed work.**

## 1.3 DEAD / VACUOUS entries in `construction.toml`

*(The four classes another agent owns — `no-uninstalled-seam` `seam_root`, `hold-escapes`
`known_sites`, `assert-unwind-safe` `confined_to_paths`, `kernel-seal-impls` `known_sites` — are
excluded here and counted by that agent. `qa-names` flags 9 further paths across those four.)*

| # | entry | what it should excuse | why it is dead weight | cost | blocked-on |
|---|---|---|---|---|---|
| 1 | `[rules.loc-ceilings].caps_crate = "busbar-caps"` | half the caps/contract sum | **crate does not exist**; contributes a constant 0. Flagged by `qa-names:crate-names-a-live-package` | 1 line | ships with the caps/contract ruling |
| 2 | `[rules.loc-ceilings].verbs_crate` / `verbs_ceiling = 0` | `busbar-unit-verbs` LOC | **crate does not exist**; gate prints "vacuous 0". Deliberately left as a floor-against-return | 2 lines | no — documented intent |
| 3 | `[rules.loc-ceilings.kernel_files.slice]` ceiling 0 | a named slice of the kernel's 8k | `slice.rs` is not under `busbar-kernel/src`; gate prints "vacuous 0". **The file itself says an owner decision is owed** | 6 lines | **OWNER: move the budget to `busbar-contract` or strike the row** |
| 4–11 | `[rules.lean-core].known_sites` × **8 line-drifted** | reviewed dialect literals | file exists, but the pinned line no longer holds a banned literal. **Three drifted by 1–4 lines and their finding is now RED elsewhere**: `config/mod.rs:782`→783, `config/mod.rs:2369`→2370, `config/providers.rs:75`→**79**. Five hold nothing at all: `ingress/mod.rs:417,:422,:768`, `ingress/mod.rs:915` (**past EOF**), `metrics.rs:1246` | re-derive 8 line numbers from gate output | no — mechanical |
| 12–14 | `[rules.plane-no-money].scope_globs` × **3 vacuous** | plane-side unit modules | `crates/busbar-{mcp,a2a,voice}/src/unit/*` match **zero files**. Flagged by `qa-names:glob-matches-something`. A ban over zero passes | declare the zero or strike | no |
| 15 | `[rules.no-test-doubles…].known_sites.voice-calls-chain` | `AuthChain::new(Vec::new()` in `main.rs` | **file exists, symbol has ZERO occurrences.** Textbook vacuous row: passes path-existence, excuses nothing | 5 lines | no |
| 16–19 | `[rules.ports-only].max_per_crate` ×4 | `busbar_core::` in plane production code | **needle cannot match** — `busbar-core` deleted, self-alias removed. 4 rows print green `0` forever | repoint to `busbar_kernel::`/`busbar_substrate_values::` or strike | **OWNER: what is the retiring namespace now?** |
| 20–23 | `[rules.ports-only-tests].max_per_crate` ×4 | the same reach from test code | same — 4 more permanently-green rows | same | same ruling |
| 24 | `[rules.legacy-reach.prefixes.busbar_core]` `figure = 0` | root's reach into `busbar_core::` | **crate deleted**; can only ever measure 0 | 6 lines — **but `[gate.census].legacy_reach_prefixes = 3` reds if you strike it** | **OWNER: strike the prefix AND lower the census floor, in one reviewed edit** |
| 25 | `[rules.legacy-reach.prefixes.busbar_substrate]` `figure = 0` | root's reach into `busbar_substrate::` | **crate deleted**; its 30-line comment still argues for a calibration to 47 that no longer exists | same | same ruling |

**Subtotal: 25 dead/vacuous entries.**

### Prose rot worth recording (not counted as entries)

- `[rules.neutral-no-dialect].why` names `busbar-core` and `busbar-substrate` as "the neutral
  crates". Both are deleted. The rule itself delegates to `plane-purity` and still measures.
- `[rules.manifest-allowlist].reviewed_extra` carries a "STALE — verified 2026-09-07" paragraph
  about four plane→engine edges, saying in terms "Delete it … noted here, not done here."
- `[gate.ceiling_raises]` is **empty and correct** — all twelve entries were struck on 2026-09-22
  after `ceiling-rose` reported seven as expired and two duplicate sections were found silently
  overwriting each other. 0 debt items. This is the one register in the tree that is clean.

**`qa/construction.toml` total: 14 breaches + 61 live ratchets + 25 dead = 100 items.**

---

# PART 2 — `qa/plane-purity-strict.toml` (41 lines, 10 gate rows)

Gate: `cargo xtask gate plane-purity-strict`. **Verdict: green — and that green is the finding.**

Every row passes. Five pass with slack nobody voted for, and four pass because their needle is dead.
There is **no `ceiling-slack` row over this file** — that check covers `construction.toml` and
`kind-isolation.toml` only — so the drift is unguarded.

| row | measured | ceiling | slack | alive? | suppresses? | cost | blocked-on |
|---|---|---|---|---|---|---|---|
| `category:PATH-INCLUDE` | 0 | 0 | 0 | yes | tight floor | — | — |
| `category:SYMBOL` | 9 | 61 | **+52** | yes | yes (9 real) | 9 test-scope symbol reaches | no |
| `category:TYPE` | 0 | 13 | **+13** | yes | **no — 0 measured** | re-pin to 0 | no |
| `category:KEY` | 139 | 377 | **+238** | yes | yes (139 real) | 139 test-scope key reaches | no |
| `category:DIALECT` | 312 | 456 | **+144** | yes | yes (312 real) | 312 test-scope dialect reaches | no |
| `category:BACKWARDS` | 0 | 33 | **+33** | yes | **no — 0 measured** | re-pin to 0 | no |
| `test-reach:llm` | 0 | 24 | **+24** | **NO** | **NO** | needle `busbar_core::` is dead | **OWNER (see below)** |
| `test-reach:mcp` | 0 | 1 | +1 | **NO** | **NO** | same | same |
| `test-reach:a2a` | 0 | 3 | +3 | **NO** | **NO** | same | same |
| `test-reach:voice` | 0 | 0 | 0 | **NO** | **NO** | same | same |

**Confirmed from the brief, and worse than stated.** The per-plane `test-reach` ceilings iterate
`xtask::planes::PLANE_KEYS = ["llm","mcp","a2a","voice"]` — the **legacy four engine crates**.
`busbar-plane-streaming` and `busbar-plane-decision` are measured by no row at all. And all four
rows are additionally **vacuous**, because the needle is `busbar_core::`.

The file's own header says `construction.toml` "carries llm=565, mcp=1, a2a=3, voice=0" for
`ports-only-tests`. Those four rows now read 0/0/0/0. The comment describes a tree from before the
`busbar-core` deletion.

**Total: 10 entries — 4 dead, 5 carrying 480 lines of stale slack, 1 tight.**

---

# PART 3 — `qa/reachability.toml` (106 lines, 19 gate rows)

Gate: `cargo xtask gate reachability`. **Verdict: RED on 4 rows.**

| entry | holds | alive? | suppresses? | cost to close | blocked-on |
|---|---|---|---|---|---|
| `[[dormant]] decision / registered` | `busbar-plane-decision` deliberately unwired | yes — `reachability:stale-declaration` PASSES | **YES** — converts a would-be RED into a printed, expiring PASS | add the dep edge to `crates/busbar/Cargo.toml` + `PLANE_DECL` in `register_planes()`, strike the row in the same commit | no |
| `unit-path:mcp` (undeclared) | `root/units_mcp.rs` is 1,884 lines declaring **no** `impl … Units for` | yes | — it is the RED | give it a unit path, fold it, or declare it | no |
| `unit-path:a2a` (undeclared) | `A2aUnits` built only by its own ctor + one test | yes | — RED | switch onto the serving path or declare | no |
| `unit-path:streaming` (undeclared) | `VoiceUnit` has 49 sites: 1 ctor + 48 tests. The NODE half is live; the ten-step UNIT is not | yes | — RED | switch on or declare | no |
| `root-module: money_book` | declared by `root/mod.rs`, named by nothing else | yes | — RED | wire, delete, or declare | no |
| `root-module: vocabulary` | same — **and its doc reads as live infrastructure** | yes | — RED | wire, delete, or declare | no |
| `root-module: cli` | **NEW since the file was written** — the file's own census names only `money_book` and `vocabulary` | yes | — RED | wire, delete, or declare | no |

The file ships with **one** declaration on purpose (red-before-green proof). Its embedded census of
"what it finds on this tree, 2026-09-22" is already one module out of date: `cli` has since joined
the unreached set.

**Total: 7 items — 1 live declaration, 6 undeclared reds.**

---

# PART 4 — `qa/plane-pricing-blindness.toml` (45 lines, 11 gate rows)

Gate: `cargo xtask gate plane-pricing-blindness`. **Verdict: RED on 5 rows. The file is EMPTY by
design** — it ships with zero `[[debt]]` rows so the gate's first run names its own findings rather
than arriving pre-suppressed.

| category | files | detail | needs a human? |
|---|---|---|---|
| `rate-card` | 2 | `busbar-llm/src/engine/usage.rs:113`×1, `busbar-llm/src/unit/verify.rs:164`×4 | **yes** |
| `billing-switch` | 2 | `usage.rs:113`×1, `verify.rs:202`×1 | **yes** |
| `price` | 8 | `busbar-llm/src/{engine/response_body.rs:22, unit/verify.rs:46}`; `busbar-voice/src/{bin/voice-conform.rs:18×40, mount.rs:722×6, runtime/metering.rs:27×58, runtime/mod.rs:33×5, runtime/session.rs:184×3, topology/mod.rs}` | **yes** |
| `hold` | 3 | `busbar-llm/src/unit/{admit.rs:66×2, meter.rs:102×9, route.rs:303×3}` | **yes** |
| `unit-key` | 0 | PASS — no plane mints a kernel-owned identity | — |

**All 15 are `reach = "unproven"`.** The emitter cannot derive `reach`/`why`; by the file's own
design a text gate must not do call-graph analysis and call it honest. Every one requires a human to
read the site, trace the call, and write the sentence.

> **CORRECTION to the brief.** Quoted as 16 verdicts. **15 today** (2 + 2 + 8 + 3), and
> `plane-pricing-blindness:unit-key` passes clean.

**Total: 15 items — 15 blocked on a human, 0 register rows.**

---

# PART 5 — `qa/denylist-allow.toml` (122 lines, 2 rows)

Gate: `cargo xtask gate denylist`. **Verdict: GREEN, all 3 rows** — including
`denylist:stale-waivers`, which means **both rows are live and each covers a real hit**.

| entry | holds | alive? | suppresses? | cost | blocked-on |
|---|---|---|---|---|---|
| `busbar-plane-llm` → `libc` via `getrandom` | one edge: `getrandom → libc` | yes | **yes** | feature-gate the entropy draw in `busbar-llm-codec/src/{lib,synth_rng,anthropic/writer}.rs` — but `cargo metadata` unifies features workspace-wide, so this **cannot clear the report by itself** | **OWNER: rule on `getrandom → libc`, or accept the tool cannot model a plane-only attenuation** |
| `busbar-plane-streaming` → `libc` via `getrandom` | same edge, via `sonic_rs → ahash` seeding | yes | **yes** | same | same ruling |

The six near-identical `cpufeatures` rows that used to live here are correctly gone: four collapsed
into the single `edge_exemptions` entry in `construction.toml`, two struck as stale (neither crate
reaches `libc` at all any more).

**Total: 2 items, both blocked on one ruling.**

---

# PART 6 — `qa/full-gate.toml` (99 lines, 16 skips + 6 discovery pins)

Gate: `cargo xtask full-gate --selftest` — **GREEN**. And the green is the problem: the selftest
checks only that **every skip carries a non-empty reason**. It never checks that the script exists.
`qa-names:path-names-a-live-path` catches what full-gate's own selftest cannot.

## 6 DEAD skip entries — every one names a script not in the tree

| line | script | status |
|---|---|---|
| 60 | `scripts/release-order-lint.py` | **GONE** (only a stray `.pyc` in a worktree) |
| 64 | `testing/shadow-oracle/fetch-golden.sh` | **GONE** |
| 68 | `testing/shadow-oracle/fetch-plugin.sh` | **GONE** |
| 72 | `testing/shadow-oracle/selftest.sh` | **GONE** |
| 76 | `testing/shadow-oracle/record.sh` | **GONE** |
| 80 | `testing/shadow-oracle/replay.sh` | **GONE** |

`testing/shadow-oracle/scripts/` holds 26 scripts and none of these five. A skip for a script that
does not exist excuses nothing — and if any of them returns under its old name, it is pre-excused
with no review.

The other 10 skips are alive and defended (`release-check.sh`, `verify-artifact.py`,
`qa-gate-run.sh`, `qa-segments.sh`, `plugin-registry-check.sh`, `loom.sh`, `txn-fence.sh`,
`build-provenance-gate.sh`, `proof-manifest.py`, `testing/llm-conformance/run.sh`). All 6
`[discovery].must_find` pins resolve.

**Total: 6 items (all dead), 10 live-and-reasoned skips not counted as debt.**

---

# PART 7 — `qa/loc.toml` (47 lines)

`cargo xtask loc --selftest` — **GREEN**. No ratchets live here; the file declares where to look and
one named crate set. `[groups.new-planes]` lists 8 crates and all 8 resolve (no `missing` reported).

**One owner decision owed, stated in the file itself:** two crate directories disagree with their
`package.name` — `crates/secret-ref` is `busbar-secret-ref` and `crates/hooks-ranking` is
`busbar-hooks-ranking` (**both confirmed**). Every ceiling in `construction.toml` is keyed on the
DIRECTORY name. Re-keying onto package names is a separate decision.

**Total: 1 item, blocked on an owner ruling.**

---

# PART 8 — `structure-lint` grandfathered list (9 entries, in code)

Not a `qa/*.toml` file — it lives at `xtask/src/gates/structure_lint/oversized.rs::grandfathered()` —
but it is a grandfather register the brief names, and the rule's own doc says the list "only
shrinks". Cap is `MAX_LINES_IMPL = 2500`.

| entry | lines | over cap? | suppresses? |
|---|---|---|---|
| `busbar-core-admin/src/v1/json/handlers.rs` | 5,195 | +2,695 | **yes** |
| `busbar-substrate-values/src/diagnostics/mod.rs` | 3,982 | +1,482 | **yes** |
| `busbar-a2a/src/a2a/receive.rs` | 3,663 | +1,163 | **yes** |
| `busbar-kernel/src/plane_host/mod.rs` | 3,470 | +970 | **yes** |
| `busbar/src/root/units_admin/mod.rs` | 3,107 | +607 | **yes** |
| `busbar-mcp/src/mcp/method.rs` | 3,029 | +529 | **yes** |
| `busbar-kernel/src/config/mod.rs` | 2,796 | +296 | **yes** |
| `busbar-kernel/src/config/migrate.rs` | 2,569 | +69 | **yes** |
| **`busbar-llm/src/engine/pipeline.rs`** | **2,108** | **NO — under cap** | **NO — VACUOUS** |

`pipeline.rs` has drained below the cap. Its grandfather row now excuses nothing; the list can shrink
by one today at zero risk. All 9 paths are alive.

**Total: 9 items — 8 live (7,811 lines over cap in aggregate), 1 vacuous.**

---

# PART 9 — `qa/kind-isolation.toml` (4,163 lines, 544 rows)

Gate: `cargo xtask gate kind-isolation`. **RED** — `:deps`, `:test-deps`, `:matrix`, `:registry`.

| table | rows | DEAD | mis-pinned | live+exact | blocked on a human |
|---|---|---|---|---|---|
| `[[transitional]]` | 5 | **2** | 0 | 3 | 0 |
| `[[dep]]` | 194 | **31** | 0 | 163 | 6 |
| `[[edge]]` | 118 | **19** | — | 99 | 5 |
| `[[cell]]` | 205 | **21** | **93** | 91 | 2 |
| `[[disagreement]]` | 13 | 0 | — | 13 | 0 |
| `[[question]]` | 6 | 0 | — | 6 | **6** |
| `[[face]]` | 3 | 0 | 0 | 3 | 1 |
| **total** | **544** | **73** | **93** | **378** | **~14** |

### The three findings that matter

1. **31 dead `[[dep]]` rows** — *not ~29.* Every one names a **dead crate**, not merely a dropped
   edge. Seven crates died and took 31 rows with them: `busbar-admin` (→`busbar-core-admin`),
   `busbar-plugin-sign`, `busbar-plugin-pack` (now a `[[bin]]` in `crates/plugin-sdk/Cargo.toml:26`,
   not a package), `busbar-grammar`, `busbar-plane-admin`, `busbar-plane-a2a-host`,
   `busbar-plane-mcp-host`. ≈310 lines to strike. Not blocked.

2. **`[[transitional]]` has no staleness test at all.** Rows at L3928 and L3933 name `busbar-admin`,
   dead since `92e823cd5`. `rule_drain`'s `transitional-live` checks only that the **`from`** crate
   exists — the inverse test. **Nothing ever checks that `to` resolves.** These two rows are
   invisible to every rule in the gate and are a standing pre-authorisation for any future
   `busbar-admin`. This is a gate-code hole, not merely file rot.

3. **17 stale-slack cells**, led by `busbar × unit` (238 pinned vs **18** measured — 220 counts of
   free slack) and `busbar × cleanliness` (57 vs 7). With 4 dead-crate cells, the matrix currently
   licenses roughly **850 vocabulary hits that do not exist**.

### Debt the file does not yet carry (gate red, no row exists)

20 `unlisted-dep-edge`, 81 `unlisted-cell`, 82 `unlisted-edge`, 2 uncovered
`measurement-disagreement`, and 1 `new-forbidden-edge`
(`busbar-plugin-loader → busbar-export-example-plugin` /t) that **no row is allowed to record** —
it must be deleted.

**Outstanding debt items: 351** (73 dead rows ≈560 lines, 93 numbers to re-pin, 185 missing rows
≈1,190 lines, 1 unrecordable breach).

---

# PART 10 — `qa/instance-noun-neutrality.toml` (5,797 lines, 724 rows)

Gate: `cargo xtask gate instance-noun-neutrality`. Row key is **`(noun, file)` only** — `count` is
written by the emitter and **read by nothing**. Not run per-push (release-time only, via
`scripts/verify-1.6.0-done.sh`), so rows can rot for weeks.

| | count |
|---|---|
| `[[leak]]` rows | **724** |
| **DEAD** (path does not exist) | **2** |
| **VACUOUS** | **0** |
| Alive AND suppressing | **722** |
| …whose pinned `count` is already wrong | **15** |
| Live leaks the register **fails to cover** | **14** |

### The 2 dead rows

| entry | why dead | cost |
|---|---|---|
| `bearer @ crates/busbar-plane-voice/src/claims.rs` | crate deleted at `fbead1a31` | 8 lines |
| `valkey @ crates/plugin-sign/src/tests/lib_tests.rs` | `plugin-sign` folded into `plugin-loader` at `d81a76f7f` | repoint to `crates/plugin-loader/src/tests/sign_tests.rs` — live count **36, exact match** |

### ⚠ A DEAD ROW IS BEING ADDED IN THE UNCOMMITTED WORKING TREE

`git diff qa/instance-noun-neutrality.toml` is **+8 lines, one hunk, zero deletions**, and the row it
adds at line 1411 is **born dead**:

```toml
[[leak]]
noun = "bearer"
file = "crates/busbar-plane-voice/src/claims.rs"
count = 2
```

The correct, repointed row (`crates/busbar-plane-streaming/src/claims.rs`, alive, 2 lines) is
already present two blocks above. This edit **re-adds the pre-fold path alongside it** — a
regression, not a repoint. Recorded here, **not fixed here**.

### The 15 drifted counts are diagnostic

The `voice` counts fall by exactly as much as `streaming` rises across the same four files
(−21/−14/−9/−3 against +22/+14/+9/+3). A `voice → streaming` rename is in flight and the baseline was
never regenerated. Largest: `voice @ root/tests/units_voice.rs` pinned 136, live **115**.

`docs/design/1.6.0-owner-decisions.md:1023` claims **three** stale rows; its third was already fixed
by `8c0d64e3a`. That doc line is itself stale.

**Outstanding debt items: 738** (722 live leaks + 2 dead rows + 14 uncovered). 234 of the 722 have
100% of their hits inside string literals — those close by making test data parametric, not by
decoupling.

---

# PART 11 — `qa/segments.toml` (559 lines, 28 entries)

Gate: `scripts/qa-segments.sh --selftest` — **GREEN** (32 active segments fail-injected red-then-green).
Staleness detection is **partial**: only `active` segments' `cargo test --test <target>` is validated.
Nothing validates an xtask gate name, a script path, or a reserved segment's target.

| entry | holds | alive? | suppresses? | cost | blocked-on |
|---|---|---|---|---|---|
| `plugins` (fallback) | whole-fleet serial plugin soak | script alive, **row never emitted** | **NO — the live fan-out expands 10 `plugin-*` legs and drops it** | 9 lines + ~10 in the selftest's `else` arm | **owner: keep the unreachable safety net?** |
| `mcp-integrity` (reserved) | live-mock MCP integrity leg, mapped to **1.6.0** | **NO** — `crates/busbar/tests/qa_mcp_integrity.rs` absent | NO | write the target, flip status, edit the hard-coded reserved list | **owner: does 1.6.0 ship without it?** |
| `a2a` (reserved) | live-mock A2A leg, mapped to **1.6.0** | **NO** — `qa_a2a.rs` absent | NO | same | same |
| `smart-router` (reserved) | a leg mapped to **1.5.6** | **NO on both counts** — target absent, **and 1.5.6 was never cut** (tags stop at `v1.5.5`) | NO | delete row + assertion (~7 lines), or re-map | **owner: is smart-router still a capability?** |
| `plane-config-noun` (reserved) | the 4-noun config-parse debt meter (17 parse targets) | script alive | **NO — and the reservation is VACUOUS.** Its prose states the blocker as *"`section_nouns` finds only THREE"*. **Ran it: `--selftest` exits 0, prints `resolved 4 section nouns: pools tools agents streams`, ALL GREEN.** The documented arming precondition is already met | **one line** (`reserved`→`active`) | **owner: may a report-only meter count as a qa-gate green?** |

The other 23 entries are alive and executing — all 10 xtask gate names resolve, both test targets,
all 5 scripts, both features, both packages, and the template expands to exactly the `plugins.yaml`
repo set.

**Outstanding debt items: 5** (3 dead, 2 vacuous). 23 live-and-executing entries not counted as debt.

---

# PART 12 — the non-TOML registers (14 files)

Included because they are the same instrument in a different file format, and because two of them
hold the largest single blocks of owed work in the tree.

| register | entries | debt items | DEAD | VACUOUS | gate detects staleness? | verdict |
|---|---|---|---|---|---|---|
| `qa/inventory-gaps.json` | 266 | **266** | 0 | 0 (266/266 anchors verified) | yes — regenerated artifact | GREEN |
| `qa/audit-ledger.json` | 200 | **189** | **24 scopes + 10 excludes + 1 glob** | 1 duplicate id | **NO — one-directional only** | GREEN (6/6) |
| `qa/capability-equality.json` | 182 verdicts | **56** | 0 | 0 (1 stale citation) | partial | GREEN |
| `scripts/no-deferral.waivers` | 52 | **52** | **38 rows** | — | **yes — and firing** | **RED** |
| `testing/shadow-oracle/accepted-differences.json` | 41 | **41** | 0 | **3** | counts yes, existence no | GREEN |
| `qa/plane-hook-isomorphism.allow` | 33 pairs | **33** | 0 | 0 | **yes — both directions** | GREEN |
| `qa/documented-claims.json` | 56 | **25** | 0 | **56 anchors off by one** | **NO — never opens the doc** | GREEN |
| `qa/WAIVERS.md` | 23 | **23** | 0 | 0 | yes | GREEN |
| `testing/llm-conformance/named-gaps.json` | 8 | **8** | 0 | 0 | yes | green |
| `testing/a2a-tck/subject-waivers.json` | 3 | **3** | 0 | 0 | yes | green |
| `testing/shadow-oracle/accepted-gaps.json` | 3 | **3** | 0 | 0 | yes | green |
| `qa/design-bindings-gaps.json` | 0 | **0** | 0 | 0 | yes | GREEN |
| `qa/field-coverage.missing` | 0 | **0** | 0 | 0 | yes | GREEN |
| `qa/method-coverage.missing` | 0 | **0** | 0 | 0 | yes | GREEN |

### The three that matter

1. **`qa/audit-ledger.json` carries 24 scopes for deleted crates** — `busbar-core`,
   `busbar-substrate`, `busbar-grammar`, three codec crates, two plane-host crates,
   `busbar-unit-verbs`, `busbar-plane-voice` — and the gate is **structurally blind**:
   `missing-scopes` walks tree→register only. They inflate the audit's own "43 unaudited / 97 open
   findings" denominators with rows that describe nothing. Only 3 scopes use the `moved_from`
   repointing mechanism that exists for exactly this. Plus **a duplicate scope id**
   (`crates/busbar-kernel-egress/tests`, twice, with two different `report` strings) that nothing
   checks for. **Blocked on a ruling:** is a deleted crate's audit history deleted or repointed?

2. **`scripts/no-deferral.waivers` is RED with 38 of 52 rows stale** — pure line-number drift in
   `crates/busbar-plugin/src/hot/host.rs`, with 38 *unwaived* markers now sitting at the current
   lines. Mechanically fixable from the gate's own output, and it is the **only** thing keeping
   `no-deferral-strict-done` from green (its `strict-done` row already passes).

3. **`qa/documented-claims.json`'s 56 ids are line addresses that are all off by one**, and
   `scripts/documented-claims-check.py:55` validates them against a **hardcoded copy of the same
   stale range** (`RUNS = (("README", 1047, 1073), ("CHANGELOG", 1087, 1115))`). A register checking
   its own drift against itself. ~15 minutes to fix; not blocked.

### Also recorded

- **2 stale `#[ignore]` attributes** on tests that now PASS under `--include-ignored`:
  `field_coverage.rs:302` ("red today by design: the field work queue … is long" — the queue is
  empty) and `method_coverage.rs:551` ("until 1.6.0: … `crates/busbar-core/src/{mcp,a2a}/` are being
  deleted" — that crate no longer exists). 9 lines total.
- **`qa/plane-hook-isomorphism.allow` is the best-built register in the tree** — exactness enforced
  in both directions against live `&'static PlaneDecl` reflection, reason-length floors, capability
  cross-checks. Zero dead, zero vacuous, structurally impossible. Its `_doc` prose is nonetheless
  wrong: it claims voice "is NOT installed in the binary", while `crates/busbar/Cargo.toml:177` has
  `plane-voice` in `default`.
- **`cargo xtask gate design-bindings` is RED** on `design-bindings:regen-clean` —
  `qa/design-bindings.json` is not what Appendix B derives, so 105 binding rows are being judged
  against a stale ledger. Needs `--write` + commit.

**Non-TOML total: 699 items, 77 hard dead/vacuous entries + 56 drifted anchors.**

---

# THE COUNT

## Tier A — `qa/*.toml`, the brief's subject

| file | entries | live debt | dead / vacuous | breaches | **items** |
|---|---|---|---|---|---|
| `qa/construction.toml` | ~230 | 61 | **25** | 14 | **100** |
| `qa/kind-isolation.toml` | 544 | 185 missing + 93 mis-pinned | **73** | 1 | **351** |
| `qa/instance-noun-neutrality.toml` | 724 | 722 + 14 uncovered | **2** | — | **738** |
| `qa/plane-purity-strict.toml` | 10 | 6 | **4** | 0 | **10** |
| `qa/reachability.toml` | 1 row + 6 reds | 1 | 0 | 6 | **7** |
| `qa/plane-pricing-blindness.toml` | 0 rows | 0 | 0 | 15 | **15** |
| `qa/full-gate.toml` | 22 | 10 (not debt) | **6** | 0 | **6** |
| `qa/segments.toml` | 28 | 23 (not debt) | **5** | 0 | **5** |
| `qa/denylist-allow.toml` | 2 | 2 | 0 | 0 | **2** |
| `qa/loc.toml` | — | 1 ruling | 0 | 0 | **1** |
| **TIER A** | **~1,560** | | **115** | **36** | **1,235** |

*Excluded by assignment: the four dead-row classes another agent owns —
`no-uninstalled-seam` `seam_root`, `hold-escapes` `known_sites` (6 paths), `assert-unwind-safe`
`confined_to_paths`, `kernel-seal-impls` `known_sites`. `qa-names` counts 9 dead paths across them.*

## Tier B — adjacent registers

| | items |
|---|---|
| `structure-lint` grandfathered list (code) | **9** |
| 14 non-TOML registers (`qa/*.json`, `*.allow`, `WAIVERS.md`, `scripts/`, `testing/`) | **699** |
| **TIER B** | **708** |

## ► TOTAL OUTSTANDING DEBT ITEMS: **1,943**

> **1,235** of them are recorded in `qa/*.toml`. **708** are in adjacent registers of the same kind.

### Of that total

- **116 entries are DEAD WEIGHT** — they excuse nothing today and should be struck or repointed
  (Tier A 115 + the vacuous grandfather row). Add the other agent's 9 and the tree carries
  **125 dead rows**. Add the 93 mis-pinned `kind-isolation` numbers, the 15 drifted
  `instance-noun-neutrality` counts and the 56 drifted `documented-claims` anchors, and
  **289 register entries do not describe this tree.**
- **36 rows are held RED on purpose** — a breach somebody decided to report rather than
  rubber-stamp. Every one is correct to be red.
- **≈48 items are blocked on a human ruling** and cannot be closed by engineering alone. The 15
  `plane-pricing-blindness` verdicts are the largest block: a text gate cannot do call-graph
  analysis honestly, so each `reach`/`why` is a person's claim or it is nothing.

### The rulings owed, gathered

| # | ruling | items unblocked |
|---|---|---|
| 1 | Is `contract_caps = 5,652` still the ruling, now that the breach is **1,185** and #83 owes ~1,700 more lines **in**? | 3 |
| 2 | What is the retiring namespace now that `busbar_core::` cannot resolve? (repoints 8 `ports-only*` rows + 4 `test-reach` rows + 2 `legacy-reach` prefixes) | 14 |
| 3 | Strike `legacy-reach.prefixes.busbar_core`/`busbar_substrate` **and** lower `[gate.census].legacy_reach_prefixes` in one reviewed edit | 2 |
| 4 | `loc-ceilings.kernel_files.slice` — move the budget to `busbar-contract` or strike the row | 1 |
| 5 | `hold-discipline.route_step_functions` — repoint or strike the vacuous sub-rule | 1 |
| 6 | `lean-core` — the `plane_host` chat/sampling bridge and `cost.rs`'s operator-facing `"Prompt"` | 3 |
| 7 | Rule on `getrandom → libc` for the two pure planes | 2 |
| 8 | Re-key ceilings onto `package.name` (`secret-ref`, `hooks-ranking`) | 1 |
| 9 | 15 `plane-pricing-blindness` `reach`/`why` sentences | 15 |
| 10 | `segments.toml` — keep the unreachable `plugins` fallback? arm `plane-config-noun`? does 1.6.0 ship without `mcp-integrity`/`a2a`? is `smart-router` still a capability? | 5 |
| 11 | `audit-ledger.json` — delete or `moved_from`-repoint a deleted crate's audit history? | 24 |
| 12 | `kind-isolation` — the `grammar` dead-kind ruling, the TCB's own kind, 6 `[[question]]` edges | ~14 |

---

## Corrections to the figures the brief carried

| claim | measured today |
|---|---|
| `busbar-contract` ceiling over by **1,007** | **1,185** — the 1,007 was the figure at 6,659; it measures **6,837** |
| `one-pricing-site:fee-fields` has **9** findings | **4** — the `busbar-admin`→`busbar-core-admin` repoint restored 5; 4 correctly stand red |
| `hold-escapes` has **65** | **65** — confirmed |
| `kind-isolation:deps` has **~29** dead dep edges | **31**, and every one names a dead *crate*, not merely a dropped edge |
| `plane-purity-strict` `test-reach` iterates the legacy four | confirmed — **and all four rows are additionally vacuous**; `streaming` and `decision` are measured by no row at all |
| **16** `plane-pricing-blindness` verdicts need a human | **15** — `unit-key` passes clean |

## Method

Ground truth was taken from the gates themselves, not from the registers' prose:
`cargo xtask gate {construction,qa-names,kind-isolation,instance-noun-neutrality,reachability,plane-purity-strict,plane-pricing-blindness,denylist,audit-ledger,design-bindings}`,
`cargo xtask {denylist,loc,full-gate} --selftest`, `scripts/qa-segments.sh --selftest`,
`scripts/plane-config-noun-gate.sh --selftest`, plus per-row filesystem and occurrence checks.

The `qa-names` gate (`xtask/src/gates/qa_names.rs`, untracked on this branch) answers question 2
mechanically for every `qa/*.toml`: it is **RED on 3 of its 9 rows** — 5 dead crate names, 18 dead
paths and 3 empty globs — and it is the instrument that should have caught most of the dead-subject
class before this census had to.

**Nothing in this document was silenced. Every number above is the tree as it measures today.**
