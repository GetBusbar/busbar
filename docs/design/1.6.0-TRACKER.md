# busbar 1.6.0 — task tracker

One line per task. `[x]` done and committed, `[~]` in progress (an agent or a run owns it), `[ ]` not started.
Regenerate the count with: `grep -c "^- \[x\]" docs/design/1.6.0-TRACKER.md` over `grep -c "^- \[" …`.
Done means: landed on the oracle branch with its gate green, not "an agent said so".

## A. Instruments (the five gates + their coverage)

- [x] A1 shadow-oracle recorder, differ, ledger inversion, self-test
- [x] A2 published 1.5.5 golden recorded and committed (780 owed cells)
- [x] A3 accepted-differences register with line-precise transforms
- [x] A4 llm spec-conformance rig (five specs pinned by digest)
- [x] A5 construction gate (structural invariants, thresholds, self-test)
- [x] A6 design-bindings ledger (Appendix B → checks; DESIGN group in the done script)
- [x] A7 CI: llm-conformance, construction, design-bindings jobs wired into ci.yml and the umbrella
- [~] A8 (step S1 of docs/design/store-qa-cycle.md landed: testing/fleet-fixtures/service-images.tsv is the ONE pinned image list — six images, every one by digest, closing the hole where four workflows agreed on a digest while scripts/release-check.sh, which the qa gate runs, used floating tags; testing/fleet-fixtures/store-services.sh is the missing one-command local path, up/down/url/ns/ns-drop/ns-sweep/status on the offset ports 15432/13306/16379 with per-run namespaces, proven-not-slept-on readiness at the measured caps 60/120/60, and a docker-free --selftest of 12 owed rows through testing/fleet-fixtures/verdict.sh; scripts/service-images-check.sh is the workflow-image lint, 16 owed rows, its own --selftest proving every rule RED on a planted fixture — a bumped digest, a floating tag, an image with no row, a pin nobody uses, an empty scan, a malformed table digest, and zero-rows-is-red; ci.yml job `service-image-pins`, fast tier, in ci-umbrella needs and RESULTS; STORE-QA group in scripts/verify-1.6.0-done.sh. STILL OPEN, and this row is NOT done until it is: the three cells are still `needs_fixture` SKIPs on the golden. What remains is S2 (drop needs_fixture, settings from env as a SecretRef, DSN redaction before `step`, named-gap path when the env is unset), S3 (the three services on the shadow-oracle job, mysql on check), and S4 (the full-golden re-record on the new harness revision against the pinned 1.5.5 binary and the pinned published store tarballs, then `replay.sh --rebaseline` so owed-baseline.txt names all four store-persist ids). S1's own measurement — do the three published server-backed stores pass store-persist.sh as-is — needs a host with docker and has not been taken.) oracle coverage: postgres / mysql / valkey store-persist cells (services)
- [x] A9 (b08272f4) oracle coverage: PostHooks / PostPluginsRollback admin fixtures recorded PASS on golden (0 divergence vs HEAD)
- [x] A10 (f65bfb09 + 86294966/2a7ac320: durable-governance and fetch-reload script cells put 8 more on the golden; BOOT-173 is a named gap on both binaries (the earlier load in the same constructor fails first); 3 rows turned out to be single-boot mutations; golden 885 recorded; 9 of the open 27 boot rows have fixtures, golden 9/9 PASS; 18 named unreachable by a single-exec mutation: 3 feature-gated in both binaries, 2 shadowed by the legacy-config detector, 4 architecturally unreachable, 7 need a durable-store precondition, 2 need reload/rollback — the last two groups are a follow-up script, the first three are inventory re-scopes) oracle coverage: the 34 boot mutations without fixtures
- [x] A11 (b08272f4) oracle coverage: migration corpus gets providers files so validate-migrated cells prove acceptance (12 configs gained a providers.yaml; all 138 config.migrate cells re-recorded, 0 divergence vs HEAD)
- [x] A12 oracle coverage: concurrency family (inbound shed 503 + Retry-After, queue mode)
- [x] A13 (testing/shadow-oracle/rigs-ledger.sh + rigs-baseline.json: mcp 190 checks, a2a battery 42 PASS, a2a TCK 114 MUST with 3 pinned waivers, voice 7 legs; 183 owed rows) oracle coverage: mcp / a2a cells through the conformance rigs, ledgered with the rest
- [x] A14 harness review by a second pair of eyes before it is the sign-off
- [x] A18 review R2: the egress request observed (mock records what busbar sent; effects.egress compared)
- [x] A19 review R3: admin write cells read back their side effect (request.post)
- [x] A20 review R4: audit chain content compared (actor/action/resource/link integrity), not a count
- [x] A21 review R5: golden meta carries binary sha256 + harness rev + host triple; differ refuses a rev mismatch
- [x] A22 review R6: exec normalizer rules flow through norm.rules; DURATION rule tightened
- [x] A23 review R7: concurrency / queue / breaker-cooldown runtime cells; threaded mock; cooldown metrics kept
- [x] A24 review R8 (scripts + auth.lifecycle cells landed 37915474): revoke and rotate followed by data-plane requests (no grace)
- [x] A25 review R9: owed-baseline; a cell moving from owed to gap is red without an accepted-gaps entry
- [x] A26 review R10 (keep rules + crosscut.traps cells landed 37915474; x-request-id and plugin-digests are forward traps, absent on 1.5.5): parity traps the normalizer erases get dedicated raw cells (Retry-After floor, no x-request-id, info.version, quantile set, plugin digests)
- [x] A27 review minor: boot alive≠booted status; four boot.refusal cells that exit 0 relabelled; bedrock egress ok_stream cells serve a real stream; primary-slow is a timeout cell; False==0 drop; md5 on Linux
- [x] A29 oracle cell: a key row expired out of band (answer: 200 on both; PB-92 stands) (sqlite store edit + restart) then spent — does 1.5.5 answer 200 or 401? (HEAD: 401; PB-92 is amended to the answer)
- [x] A28 (ab19d92f; scripts/inventory-coverage.py; 256 named gaps, six inventory files carry no id column) 1.5.5-BEHAVIOUR.md coverage matrix (40 rows) maintained as the scoreboard
- [x] A15 (restored as accepted spellings; 3 cells) oracle coverage: hook `plugin:` alias, hook-view `at` field, settings PUT `persist:` (1.5.5 spellings HEAD dropped; owner: restore or register as breaking)
- [x] A17 mock upstream bodies made spec-shaped (openai logprobs/refusal etc.) and the affected golden cells re-recorded from 1.5.5
- [x] A16 (message_count counts folded system turns; cell embedded-system-turn) oracle coverage: hook payload shape on the normalized IR (message_count) vs 1.5.5
- [x] A30 the done-oracle is a STANDING part of every qa push, and the golden rotates at every release. Evidence: `.github/workflows/qa-gate.yml` job `done-oracle` (sibling of `slow`, `needs: [build]`, `timeout-minutes: 240`, ci.yml's golden cache key character for character, report + parity diff uploaded as `qa-gate-done-oracle-report`) and its membership in `umbrella`'s `needs` and scoring; `scripts/qa-gate-run.sh` verb `done-oracle` (fetches the pinned 1.5.5 golden and the pinned plugin set, then `scripts/verify-1.6.0-done.sh` in FULL — `--fast` is refused by construction, so the PROVISIONAL exit 3 the judges' audit named cannot be reached from CI); `testing/shadow-oracle/rotate-golden.sh` (+`--selftest` GREEN over fake release JSON: missing archive, digest-less asset, empty release, a complete set on a DRAFT, an already-pinned version, a malformed version, and `--dry-run` proven byte-identical; `--dry-run v1.6.0` REFUSES today because v1.6.0 is still a draft with 3 of 6 assets); `.github/workflows/release.yml` `promote-release` post-release checklist step (a run summary, never a self-filed issue — `scripts/no-self-filed-issues-lint.sh`); `scripts/ci-umbrella-lint.py --workflow qa-gate` (named REQUIRED member `done-oracle`, three real-file mutations proven red: dropped from `needs`, waited-for-but-unscored, renamed away); `scripts/full-gate.sh` (qa-gate.yml discovered PER TIER into `QA_ACCOUNTED`, fails closed on an unclassified verb, `--selftest` plants the needs-list mutation and requires ci-umbrella-lint red); `docs/design/qa-promote-release.md` §"What every push is judged by" and §"Rotating the oracle golden"

## B. Parity restoration on HEAD (Phase 1.0)

- [x] B1 store plugin ABI window v2..=v4 (published stores load)
- [x] B2 deprecated env vars honoured and warned (BUSBAR_PROVIDERS et al.)
- [x] B3 --migrate-config leaves 1.5.5 pool members untouched
- [x] B4 streams metered, framed in the caller's dialect, no leaked usage chunk
- [x] B5 cross-protocol model field as 1.5.5
- [x] B6 /stats lane limit
- [x] B7 unknown-path 404 generic and uncounted
- [x] B8 reserved name admin refused; --validate does not resolve unreferenced idp secrets
- [x] B9 TLS material validated once before the data workers spawn
- [x] B10 full re-measure: 780/780, zero unaccepted divergences

## C. Decoupling the planes (Phase 1.1–1.5)

- [x] C1 busbar-llm production: zero core references, no core dependency
- [x] C2 busbar-mcp production: zero core references
- [x] C3 busbar-a2a production: zero core references
- [x] C4 busbar-voice tests: zero core references, core out of the crate closure
- [x] C5 busbar-mcp tests: 364 → 1 (engine test kit)
- [x] C6 busbar-a2a tests: 142 → 3 (engine test kit plus)
- [~] C7 busbar-llm tests: 877 → 565; remainder is core-owned vocabulary
- [x] C8 config shapes (groups, limits, failover, auth, on_exhausted …) live in substrate / busbar-api, core re-exports
- [ ] C9 governance / cost / metrics-recorder fixtures behind a neutral seam so busbar-llm drops its core dev-dependency
- [x] C10 state / store port: clock and breaker types only through ClockHost / BreakerHost / LanePoolHost
- [x] C11 governance port: GovCtx → AdmissionHost only (the one Phase-1 change on the charge path; money families required)
- [x] C12 purity lint strict mode (tests included) exists and is green
- [x] C13 (fa55a8bf; no coupling to list; the llm leg boots with the crate deleted and serves mcp/a2a) plane-delete test covers busbar-llm (binary boots serving mcp / a2a / voice with the llm crate deleted)

## D. The kernel and the units — the §3.1 crate graph, built new (ARCHITECTURE.md §1.1, §3.1, §9.1)

- [x] D0 PR-0 instruments: missing tests, four new golden cells, translator installer, new gate rules
- [x] D1 Teller kernel seed (steps, tokens, run_unit, gauntlet adapter) — becomes busbar-caps + the loop of busbar-kernel
- [x] D2 the single `attempt()` seam; walk.rs deleted; attempt-identity test — becomes the body of busbar-unit-egress
- [x] D3 busbar-contract: traits, facts, locators, Frame, Ingress/Progress, UnitDraft, Ctx, bounded types (plugin-visible; the only crate a plugin may name)
- [x] D4 busbar-caps: capability types and tokens (Decision, Hold, HoldAccrual, Posted, VerifiedDestination, Usage, the *Token family), moved out of the substrate
- [x] D5 (ad49838b; 4,493 lines, 97 tests; integration flags: loop synchronous, self-accrual via kernel AccrualMeter at exit, AccruedIntoParent until caps gains the constructor, admit_token() named seam) busbar-kernel: Teller loop, pump/scheduler, in-flight table + HoldCell, sessions, recovery, slices/leases, registry + generations, grammars (JSON span scanner), Ticks/drain — ≤ 8k lines by call-graph with §1.1's per-file table
- [x] D6 (657b9398; 59 + 58 tests; flags: challenge arm outside Decision<Authenticate>, guard refusals mapped to ScopeDenied/Unpriced, SWRR single-stripe, KeyVerifier trait for governance) busbar-unit-auth (the 1.5.5 auth chain as a unit), busbar-unit-trust (destination verification, pick order)
- [x] D7 busbar-unit-scope [done], busbar-unit-admission [0dd45267; 36 tests; bucket/metric/window ride on AdmissionUnit::blocked(), grant via take_grant()] (1.5.5's check-then-charge decision, verbatim)
- [x] D8 (5f805e43; 42 + 44 tests; flags: seal in unit tests needs a test-path allow-list, UsageLine lacks per-line source/estimated in caps, Usage::meter is a trait) busbar-unit-cost, busbar-unit-usage (the five pricing clauses; the variance rule)
- [x] D9 (classify port landed on unit-breaker with a test adapter) busbar-unit-egress (pool, failover loop, `attempt()`) [42aa03f0; 65 tests; flags: Breaker::classify must land in unit-breaker, envelope byte layout marked contract, restrict/invalid_body sheds unreached until the plane decode arrives], busbar-unit-breaker [done]
- [x] D10 busbar-unit-egress-auth, busbar-unit-transport-key
- [x] D11 (b493cd84; ledger 41 tests, audit 52 tests incl. the legacy chain frozen-bytes golden; flags: unreconciled treated as a move, cross-window transfer sign out-minus-in, classes keyed by name in sealed bodies) busbar-unit-ledger, busbar-unit-audit (settle, the audit record)
- [x] D12 busbar-unit-wal [b493cd84; 27 tests; forbid(unsafe) held, positional write + sync_data, poison on error], busbar-unit-verbs [593c645a; 66 legacy verbs proven against openapi-1.5.5; 49 tests; verb bodies behind contract seams until the store unit lands]
- [x] D13 (36b1e800; 1,831 lines over busbar-llm; 64/64 request and 54/54 answer goldens reproduce; 13 codec/design findings listed in the crate docs, incl. pub(crate) cross-protocol pass, responses writer reads the clock, anthropic error writer mints a random id, no streaming goldens, 16 of 36 pairs have fixtures) busbar-plane-llm from today's busbar-llm: codecs as parts, the nine step facts; the twins deleted
- [x] D14 (7297ca21 mcp+a2a 190 tests; admin+voice 42 tests; findings: MCP mount path configured where a claim is a constant, correlation id is u64, no argument on plane_facts, refusal message text placeholder, 17 new admin routes unbound) busbar-plane-mcp, busbar-plane-a2a, busbar-plane-voice, busbar-plane-admin onto the loop
- [x] D15 (ws/grpc/stdio landed, 25 tests; 7297ca21 tcp/tls/http/sse 23 tests; not ported: h2 inbound, chunked TE, keep-alive pipelining, multi-cert SNI) busbar-transport-*: http, sse, ws, grpc, stdio, tcp, tls (in-tree; peer later with fleet)
- [x] D20 (fixed: 503 + Retry-After until the recorder is installed; binary race test) finding: a /metrics scrape right after boot can land on a cold worker and answer 200 with an empty body (thread-per-core SO_REUSEPORT); the kernel's registry generation must make a scrape either full or refused — gate cell + fix
- [ ] D16 busbar-core retired: the binary composes kernel + units + planes + transports; the host furniture lives in units and planes
- [x] D21 owner review resolved by the orchestrator 2026-09-05: ten decisions in ARCHITECTURE.md Appendix A (KernelSeal one meaning, store trait typed after ABI-2, MCP mount fixed, surface ceilings, unreconciled is a move, 17 verbs kebab-case, anthropic id entropy from Ctx, leak-once registration, network guard in trust). Register: docs/design/1.6.0-contract-gaps.md (60 gaps, 20 blocking)
- [x] D23 (15 commits cherry-picked, ca8dd355..faec8394: one TransportKeyHandle, DestinationId, UsageLine source/estimated, stand-ins deleted in caps and kernel, challenge arm on the auth facts, Posted::into_parent + parent-exit conversion, arrival hold through the admission door, PoolNotPermitted/NoRate, meter signature, DOWNGRADED wired, ClaimKey, hard_closes framing; open: contract's public KernelSeal trait stays until constructors move into caps) contract gaps wave 1 + 2 (one vocabulary; the loop shapes): CG-28, 19, 35, 25, 31, 44, 45, 23, 26/32, 33, 24, 34, 27, 46, 30 
- [x] D24 (10 of 11 landed, fde57064..15ae7ad5: draft facts on Unit, CorrelationValue, plane_facts subject, anonymous claim form, PathSegment, two response ceilings, leak-once Registration, reserved transport fact keys, ADMIN_PREFIX, INTROSPECTION_VERBS, claims_from_ladder!; CG-05/08 moved to D29)
- [x] D29 (3 commits e81632b8..0a56ae47: busbar-grammar at 328 surface lines with the kernel's scanner and its per-KiB budget test, one Span; busbar-contract-transport at 390 with the connection/listener/detached stream, the closed transport codes, the arrival record, UpstreamAddress, the reserved fact keys, TRANSPORT_ABI and the composition check, named directly by all seven transports and by egress/trust/transport-key; Arena::alloc_spans + busbar_contract::spans::resolve, the four plane copies deleted and every Ir::new span table real. Pair 3,306 ≤ 3,500. DEVIATION: Transport/TransportMeta stayed in busbar-contract — the traits name VerifiedDestination, TransportKeyHandle, ConfigView, Refusal and the arena, so moving them under a crate the contract depends on would have dragged bounded/ids/plugin/unit down with them) the closed JSON span grammar as its own crate re-exported by the contract and named by the kernel; the transport-facing contract as busbar-contract-transport with its own ceiling; CG-05 + CG-08
- [x] D25 (9 commits 4c2d1b9d..e55f43de: UpstreamAddress per family, adopt/detach with HandoffMismatch produced, ws/grpc/stdio compose over http/tcp with no socket of their own, transport-key unit provisions tls, chunked bodies and multi-call writes, per-stream unit0 refusal, network guard in the trust unit with 90 tests, TRANSPORT_ABI, Listener id, composition check, transport-owned envelope bytes; CG-49 deferred) contract gaps wave 4 (transport surface): CG-09, 11, 47, 48, 50, 13, 51, 12, 14, 21, 36 (49 deferred)
- [x] D26 (8 commits 58e553bb..242e05c3: busbar-llm-codec 97k pure / busbar-llm 47k engine, 2,119 tests preserved, plane answers exact, no clock or entropy read in the codecs) busbar-llm split into a pure codec crate and an I/O crate; answer-normalization surface published; responses writer takes the clock; anthropic id entropy from Ctx (CG-60, 53, 54, 55)
- [x] D26c (c6c3b33f..533f1f73: busbar-substrate-values 12k pure / busbar-substrate 36k engine, 324 tests exact, llm cells identical to a control build; plane-llm's purity rows are three libc leaves: getrandom for synthesized ids, sonic_rs via rkyv/uuid, busbar_api via sha2/cpufeatures)
- [x] D26b (01495b51..787d214e: busbar-mcp-codec 2.2k, busbar-a2a-codec 1.4k, busbar-voice-codec 3.7k; 135/104/79 tests preserved; a2a plane clean on the denylist, mcp/voice libc leaves closed d99a074c/d59c2ceb (phantom optional-dep edge fixed in xtask, cpufeatures+getrandom waived as syscall-only); source-denylist green on all planes; rigs 201/201 green; rmcp stays on the I/O side)
- [x] D27 (cec88e0c/707e698f: cargo xtask denylist over the pure kinds, empty allow-list, folded into the source-denylist gate row; today RED on 8 of 9 pure crates, plane-llm via hyper/reqwest and the rest via sha2 -> cpufeatures -> libc) xtask transitive source denylist with an empty allow-list, wired into the construction gate (CG-59) [in flight, isolated worktree]
- [x] D28 (12cf5e9b/7fb7f206: CG-43 bound — the breaker's sink is the node's logging, BUSBAR-3022 once per value, boot lines unchanged; CG-45 bound — CredentialCache over sha256, GovernanceDirectory as one KeyVerifier+RevocationView at the voice/a2a/mcp sites; admin site bound and CG-39/40 named at Route (ff71b94c); the llm step reads the middleware's resolved outcome by design. Audited: CG-38 and CG-29 closed at the root with tests; open: CG-39/40 the nonce and replay encoder are constructed but PostKeys/PostKeysIdRotate bypass Verbs::execute for the legacy dispatch so neither is live; CG-43 root/kernel.rs builds BreakerUnit::new() with NoopDiagnostics; CG-45 every auth.resolve site passes None for cache/key verifier/revocations) composition-root bindings the crates left open: CONFIG_CLASS_RULES table to the verbs unit, nonce and replay encoder seams, breaker diagnostics sink and token, auth unit seams (CG-38, 39, 40, 43, 29, 45)
- [ ] D30 composition root wave A: root scaffolding (A1 kernel + units shape, A2 registry sealed from claims, A3 leak-once vocabulary, A4 durability branch, A5 transport-key provisioning at listen, A6 tls accept reads the handle's slot [done 5ca63fb1]) [A1-A5 + B10/B11 done 3c6a19c9..f2213181 + adapter alignment 3c76bebb, 62 root tests; findings: the 49 claims do not seal (209 conservative overlaps, CG-62), voice claims twilio-media which has no crate (CG-63), data_dir is not a config key yet]
- [ ] D31 composition root wave B: unit seams bound (B1 CONFIG_CLASS_RULES, B2 nonce, B3 replay encoder, B4 breaker diagnostics reach classify, B5 breaker token, B6 digest, B7 key verifier order, B9 ABI-2 store adapter behind SliceStore/verbs::Store/wal::Shipper, B10 adapters, B11 MeterPolicy from config, B12 ledger marker) [B1-B3 done ebb81591 (also fixed PostPluginsInspect falling into Forbidden); B6/B7/B12 done 829ef007; B4/B5 done 832d25f9; B9 done 9fa7f23e; B10/B11 done b1e2a9f8/f2213181]
- [~] D32 composition root wave C (C0 done; C5-C9, C11-C12 dark steps landed with 52 identity tests; C10 admit/meter landed (59 identity tests); C12b rehearsal 67426877: six fixtures byte-identical through the chain, eight seam gaps named and closed (386fdd21..fc0e718a verify/pool-view/lane interning/trust token lent to Verify; 9d93e5a4 route feeds meter, one posting, admission_check so Audit is the one terminal, pool label, Route and Audit on the token seam, RoutePlan legs filled), ten gap tests green, six fixtures byte-identical, purity lint green; C-llm landed 7c672c58..db2a43b5 behind root-llm: seven arms call the step files directly, route and meter through unit/walk.rs (the engine's carry types are pub(crate)), 259 request-path cells 0 unaccepted and byte-identical on/off; async step seam landed 4b7a892b (run_unit_async over a RouteAwait leg, one terminal, an abandoned unit settles its hold and returns its lease; the llm walk awaits inline, no blocking worker or channel; the exit arm settles the llm posting onto the journal; 176 owed 0 diverging; the URL facts ride the carry, CARRIED_BY_NOBODY deleted); open before default-on: billing_failed/serving lane/stream finish read at the tap (engine report-back), PATH_INGRESS landed cdc53db7..b7e06618 (URL parse is a value; the root's path table drives arrival/decode; 140 gemini/bedrock cells byte-identical on/off; one carry residual pinned CARRIED_BY_NOBODY until walk.rs carries the dialect's miss copy); all five legs together: 172 root tests; C4 voice landed 718b8110/ee843f24 behind root-voice: nine units bound, four seams to busbar-voice, 13/13 legs, rigs 201/201, neutrality 18/18 both legs; C3 a2a landed 0786f50e behind root-a2a: rigs 201/201 identical on/off, neutrality 18/18; C2 mcp landed 15b1e446/84bd6d68 behind root-mcp: rigs 201/201 identical on/off, 28 tests; C1 admin landed 5bb1241b..b3863aa5 behind root-admin: 66 verbs through one seam, admin.ops 234 owed, 0 diverging after 9c5e2750 (the restart drain is released by the exit path, legs byte-identical); admin authenticate takes the node's auth bindings and the mint verbs' choice is named at Route (ff71b94c, closes D28's admin half), notes in docs/design/1.6.0-root-admin-switch-notes.md; all four legs together: 151 root tests. Open before the default flips: the PostRestart header, the full golden on each leg, units_llm over the nine step files): the switch-over, one plane at a time gated by its family (admin, mcp, a2a, voice, then llm behind teller-waist until the full golden is 0 unaccepted on both legs)
- [ ] D33 composition root wave D: busbar-core emptied module by module (74,816 lines mapped in the plan section 4) and the crate deleted; the four legacy gate rows flip green
- [x] D34 (7a8bb674..0bc7fe62: overlap resolves by precedence, equal precedence refuses, disjoint schemes never overlap; an anchored fragment outranks a floating one of the same length; the 49 claims seal with a pinned order, 209 resolved, 0 refused)
- [x] D35 (225b273a: claim dropped, 48 claims seal, the refusal check kept against a purpose-built claim; root::registry::seal answers for the first time) CG-63 the voice plane's twilio-media claim is dropped until the transport crate exists (Phase 0.5); the root's claim-transport check stays as the boot refusal
- [x] D19 (68664d24: proofs out of src, pair measures 3,281 surface lines; architecture amended to 3.5k with the reason; gate row surface-ceiling:contract+caps) (67ebfbb8; 6,815 -> 6,640; pure code is 3,817 so the 3k pair ceiling needs a structural decision, not prose trimming) contract + caps at the 3k ceiling (today ~6.8k: trim prose and fixtures' duplication; the honesty tables stay)
- [x] D17 (db67382f; loc-ceilings, manifest-allowlist, source-denylist, lean-core ratchet 21, no-default-bodies, sealed-unit-traits ratchet 1, hold-discipline, forbid-unsafe; self-test green) construction gate: §1.1 LOC ceilings (union, per file, by call-graph) and the §8.3 scans as rules
- [x] D18 (KERNEL group in scripts/verify-1.6.0-done.sh: kernel battery, caps fixtures, attempt identity) attempt-identity and the kernel battery (§8.3) registered in the done script

## E. Spec compliance fixes (owner rule: maximum spec compliance)

- [x] E0 decision: Anthropic ping stays (named gap of the published spec)
- [x] E1 anthropic: required Message / usage / content / stream-event fields emitted
- [x] E2 openai chat: logprobs and refusal members emitted
- [x] E3 responses: required response-object members emitted (created_at, error, incomplete_details, …)
- [x] E4 responses streams: logprobs on output_text.delta / content parts; usage object not null
- [x] E5 every stream request on the Responses door is streamed (today answered as plain JSON by both binaries)
- [x] E6 bedrock: metrics member on same-dialect responses
- [x] E8 (9fb21948; responses 21/21 response rows; rig 244 PASS, 4 named ping rows) responses request-echo members (temperature, top_p, instructions, metadata) carry the client's values on cross-dialect paths (writer needs request context)
- [x] E7 (rig: 1 distinct violation, the named ping gap; every dialect's response rows green otherwise) conformance rig green with zero unaccepted violations; accepted ones registered

## F. Design bindings (Appendix B → proven)

- [x] F1 PR-0 maps PB-6, 8, 13, 15, 17, 25, 29, 41, 46, 84 (25 → 15 unmapped)
- [x] F2 (ab19d92f; INVENTORY-COVERAGE group in the done script) PB-0 master rule: every inventory row has an oracle cell (a generator check)
- [x] F3 (PB-7 mapped: binary shed test, layer test, oracle cell) PB-7 inbound shed proven (needs A12)
- [x] F4 PB-37 call_with_legacy_default and PB-93 1.6.0 store verbs on an ABI-2 store tested
- [~] F5 PB-70 scrape shape on a 1.5.5 config, PB-92 expires_at unenforced, PB-102 alarms silent
- [x] F6 remaining unmapped bindings mapped or given a check (101/103 mapped; PB-7 stays unmapped, CONTRADICTED by a green test — owner decision needed; PB-71's 56-claim README/CHANGELOG audit is real future work, named not papered over)
- [x] F7 ARCHITECTURE.md amended where the owner decided against the text (PB-11 store window, PB-66 beta headers, PB-75 version, PB-84 taps)
- [x] F8 DESIGN group green: 103 / 103 mapped, --check --strict GREEN (a15d64da; PB-71 by the documented family, PB-7 by the restored shed)

## G. The ledger (Phase 3)

- [x] G1 (caa2e2a1/22fc03f3/215a0b30; `busbar-unit-wal::journal` is the one chain — a 160-byte fixed header (magic `BJRN`, version, one of 14 pinned classes, node, node_seq, lease_epoch, policy_epoch, wall, mono, body_len, body_hash, prev_hash, hash) over an opaque body, `hash = SHA-256(header before the hash field)` so a re-classed or re-clocked record breaks too, one numbering across every unit; the root binds the audit unit's sealed records, the ledger's postings and its sealed checkpoints, and the migration marker — G7's node-local records shim retired, so on a `data_dir` node the marker now survives a restart; without `data_dir` the journal is memory-buffered and shipped synchronously through the store adapter's `append_batch` (all four added verbs answered by the node-local shim, PB-93), the buffer of unacknowledged records is bounded at 8192 = 4 MiB, and reaching the bound drops the OLDEST and seals a `ChainBreak` naming the count and the identity range — never a silent drop, never a refusal (PB-15); 19 tests incl. mutated body / mutated header / removed record, a restart with and without a data dir, and the legacy `AuditEntry` chain byte-identical on the wire (PB-20); oracle 283 of 283 owed cells PASS, 0 unaccepted, `hazard|no-data-dir|files`+`logs`, `plugins.store-persist|store-sqlite` and `admin.ops|GetAudit|ok` all identical) one journal (fixed records, hash chain, checkpoints); memory-buffered without data_dir
- [x] G2 (cf3dcdf0..b8acd8a3: the door opens the reservation after Ok sized by the estimate; Meter accrues; Exit tops up out of the leg's offered headroom and carries the rest as an Overdraft posting (reserved 0, settled 0, overdraft n) beside the settlement; Durability::settle_posted moves the books and journals; voice/a2a/mcp exit arms bound; 181 root tests; billing/route.failover/hooks/teller/hazard 46/46; llm arm's settle binding goes with the async-seam owner) holds as accounting on the Hold token; overdraft never a refusal
- [ ] G3 (root Ledger dual-writing since A4; the plane's Meter reaches it only after the flip — the readout that replaces the identity test's reconstructed snapshot lands with C13) dual-write: Meter posts to the ledger and the untouched legacy path
- [x] G4 (6cb8e57c/2f4f6338: root/ledger_identity.rs over identity::residual per (bucket, day, lane, provider), union of both key sets, nano-units projected once per row; binary test pins 4 admitted at 2,530,000 micro-units each = 10,120,000 on /usage, fee 4; /usage key set, wire order and vocabulary deny-list pin PB-16; red proofs on tier, fee and a one-micro-unit drift; billing family identical) reconciliation identity test (ledger sums == legacy spend, fee_count == billable_requests)
- [x] G5 ledger admin endpoints additive; legacy openapi byte-identical — five read-only verbs under `/api/v1/admin/ledger/*` (totals, checkpoints, reconciliation, migration, openapi.json), `admin:read`, root leg only; the 1.5.5 document unchanged and the 1.6.0 operations described at `docs/openapi-1.6.0-additive.json`
- [x] G6 (6a324da0/e57dda83; the six 1.6.0-additive keys — `mcp`/`oauth_as`/`tools`/`agents`/`streams` and `auth.policy` — are lifted off the document by `config::prepass` before the frozen structs parse; the top-level and `auth:` `expected one of` lists are byte-identical to the 1.5.5 reference binary including the trailing position; `config-stability-gate.sh --check` green + `--selftest` 57 cases; F-013's acceptance narrowed to drop BOOT-P01/NEUT-U-top/NEUT-U-auth) fleet: config block split off before the 1.5.5-shaped parse
- [x] G7 (083a164f/1497dd25/ccefb196: read-only through the ABI-2 adapter, opening checkpoint seq 0 with identical body_hash on re-run, marker in the node-local records shim, root step between durability and the first listener; 23 tests) Migration unit: legacy head and balances read through the store adapter, opening balances sealed
- [x] G8 (cd498c40; new `hazard` family, 2 script cells: boot on a config with no data dir and no peers, one chat request, clean shutdown — the working directory and the config's own directory hold exactly what 1.5.5 leaves, and no log line names a data dir/WAL/peers/fleet; both PASS identical on 1.5.5 and HEAD, both red-proofed; the differ gained an `effects.files` class because it silently ignored the listings otherwise) hazard cells (no data_dir → no probe, files, WAL, warnings)

## H. The planes on the seam (Phase 4)

- [ ] H1 busbar-llm passes the purity lint exactly as the other planes
- [x] H2 (llm 7 cells; mcp 6 + a2a 6 gating scenarios, each red-proofed, ledgered as mcp.rig/a2a.battery; voice 4 steps remain named in qa/teller-steps.json) (llm: 7 teller cells, 3019be6b; mcp/a2a 12 scenarios in flight; voice 4 pending the Gemini Live agent; gate scripts/teller-steps-check.py) conformance rigs carry one cell per Teller step per plane
- [x] H3 (011368a2; 18 cells all PASS on 1.5.5; on HEAD the 404s, no-plane-series and idle stats are identical; 5 diverge on already-accepted additive keys/series/paths + the version literal) neutrality cells on a pure 1.5.5 config with all planes compiled in (boot lines, series, key lists, routes)
- [ ] H4 capability-equality and plane-isomorphism suites green over the Teller path

## K. Voice plane: from scaffold to shipped (found while writing its operator doc)

- [x] K1 (voice commit; conformance leg added) provider credential threaded from the composition root (mint / SDP routes answer 501 today)
- [x] K2 (voice commit; conformance leg added) real per-key metering lease wired (build_runtime_hosted is defined but never called; today an uncapped in-process placeholder)
- [x] K3 (voice commit; conformance leg added) session-scope authorization gate (scope_kinds declares "session"; nothing enforces it)
- [x] K4 (voice commit; `gemini-live-route` conformance leg added) Gemini Live route mounted: a second claim (`/v1/realtime/gemini`), a second composed provider endpoint keyed by dialect (`x-goog-api-key`), a thin-duplex WS accept reusing `TelephonyProxy` under `GeminiLiveCodec`, per-route `ingress_protocol` label
- [x] K5 (voice commit; `provider-dial` conformance leg added) `topology::dial_provider` wired into the telephony + Gemini WS accepts (`open_admitted_telephony` + `TelephonyProxy::run`, gauntlet-free so the WS-accept seam's gate is not re-run); the harness gained a loopback WS provider (`tokio-tungstenite` accept side, `runtime`-gated); voice conformance battery: 9 legs, all ready and passing, ledgered

## J. Code audit of the finished crates (loop: audit, fix, audit, until every lens retires dry)

- [x] J1 round 1 over abi (contract, caps, grammar, contract-transport), the 14 units, the 7 transports, the mcp/a2a/voice codecs + xtask + plugin-loader, llm-codec, substrate-values: 60 finders (10 lenses x 6 groups), 6 judges; 151 raw, 97 confirmed (4 HIGH, 36 MEDIUM, 29 LOW), 20 rejected, 23 nit; every confirmed item fixed red-before-green and landed with its crate's tests, the gate rows it names and the oracle families it can move (abi 12 + design 4, units 14, transports 13 + 14, codecs 10, llm-codec 3, substrate-values 2); the two money-adjacent ones registered as improvements for owner veto (refund on a window straddle, streamed usage attribution); abi-020 (sealing Registration::new) left open by design
- [x] J2 round 2 on a fresh pin (9515cd3d): Opus over the whole scope for the 12 lenses that found nothing raw, Sonnet over exactly the unread files for the rest; 59 raw, 39 confirmed (2 HIGH: link-local IMDS admitted under allow_private; Gemini $ref inliner without a budget), 17 MEDIUM, 20 LOW, 12 rejected, 10 nit, 2 quote mismatches; fixes in flight per group
- [ ] J3 round 3, a fresh full pass on pin 323c6220 (Opus on every lens, whole scope, no memory of rounds 1–2): 60 finders, quotes all verified against the pin; HIGH leads fixed as they land without waiting for the round (http egress whole-body buffering and quadratic chunked re-decode; tcp/tls refusal leaving the socket open; wss dialled in cleartext over tcp; grpc empty message wedging the call; egress stream ceiling re-armed per frame; ledger recompute watermark skipping lower nodes; wal ship failure dropped on disk and double-appended in memory; closed-window movement check; plugin-loader shim lock inversion; openai stream error chunk swallowed; mcp sanitizer reconstituting a tag); judges per group, then fixes, then a fresh round until a round comes back at zero confirmed; then the WIP crates once the defaults flip: busbar-kernel, the planes, the root, busbar-llm, busbar-substrate, busbar-core remnants
- [x] J4 the gate itself: land.sh's test check was masked by pipefail (never able to fail) and now reads cargo's exit; the voice conformance helper reused a stale harness binary and now rebuilds; the oracle's changelog validate cell ran a relative binary after a cd, and the usage window end is date-relative (harness fixes in flight); full replay on 9515cd3d: 890 owed, 0 unaccepted beyond those two harness rows, rigs 201/201, bindings 103/103, clippy clean, fmt clean
- [x] J5 the audit ledger (`qa/audit-ledger.json` + `scripts/audit-ledger.py`, AUDIT-LEDGER group in the done script): one scope per production path, generated from the tree so a new crate cannot be silently uncovered; each scope carries the tree hash of its files at the audited commit, so a result expires the moment the code moves and reads `stale` instead of green; `--check` is red on incomplete coverage or a scope with findings and no fix stamped; `status` writes docs/design/AUDIT-STATUS.md and `next` prints the worklist an orchestrator launches auditors from

## I. Release

- [x] I1 CHANGELOG 1.6.0: every `breaking` and notable `improvement` register entry named with its migration note
- [x] I2 docs: pools grammar presented 1.5.5-first; deprecated env vars; plugin ABI note; spec-fidelity notes
- [x] B12 (fixed by 0ba01252: the ABI-2 adapter speaks 1.5.5's four-tier usage shape; store_errors is a money class) HEAD money regression found by the expiry cell: the usage/budget flush sends a row shape the published ABI-2 sqlite store rejects (missing field 'tokens'), so usage is not persisted on a 1.5.5 store; the ABI-2 adapter must send the 1.5.5 row shape; the store-persist cell must compare chat_after_restart and the post-restart /usage
- [x] B14 HEAD neutrality regression found by the boot-lines cell: a voice line, a thread-per-core line and eighteen listening lines on a 1.5.5 config, the three pool exhaustion-policy lines dropped (fixed d82ace56; crates/busbar/tests/boot_lines_neutrality.rs pins the INFO set)
- [x] B13 (fixed: the fixture's frozen clock, not the chain; test un-ignored, asserts 200) test fixture gap: the in-process test app's keys chain does not read a row edited in the governance store the way the binary does (key_expires_at data-plane test ignored)
- [x] B11 (fixed; shed restored byte for byte; crates/busbar/tests/inbound_concurrency_shed.rs; cell passes) HEAD regression found by the concurrency family: under limits.max_inbound_concurrent the admitted requests hang instead of being served (1.5.5: 2 served, 6 shed with 503) — fix on the request path, oracle cell concurrency|inbound-shed|n8 is the judge
- [x] I3 required status checks on dev; qa and main protections per the CI health doc (scripts/promote.sh enforces the fast-forward + green-check gate; settings recorded in CI-HEALTH-2026-09-04.md's "Applied 2026-09-05" section)
- [ ] I4 verify-1.6.0-done.sh fully green (PARITY, DESIGN, construction, conformance)
- [ ] I5 release-stage on qa, release on main (nothing rebuilt) — 1.6.0 ships as v1.6.0-rc.1 then v1.6.0 from ONE staged record (staged.json names both: `tag` publishes + moves latest + fans out, `rc_tag` mints the git tag and the immutable image pin and nothing else; `docker.yml` gained `promote_latest`); the marketing site deploy is off the release path (dropped from release-notify-targets.txt, observed post-publication by verify-deploy, never a dependency); signing key BUSBAR_SIGN_KEY and RELEASE_DISPATCH_TOKEN both confirmed present as org secrets (visibility all); OWNER-BY-HAND remaining: promote qa-gate.yml's toolchain pin to main (workflow_run loads the dispatcher from the default branch, so qa-gate-dispatch-lint is red until it lands there), and set the five MCP/A2A conformance `vars.*` that no repo or org variable currently defines
- [x] I8 (scripts/service-images-check.sh + testing/fleet-fixtures/service-images.tsv; ci.yml job `service-image-pins`, fast tier, in the umbrella) the backend service containers are the same bytes everywhere they are stood up — one pinned list, and a lint that goes red the moment a workflow's `image:` and the list disagree. Actions cannot read a file into a `services:` block, so the pin cannot be enforced by construction; this is what enforces it instead. RECURRING by nature: the list is re-pinned when an image moves, and the lint is what proves the fan-out was complete rather than three-quarters complete.
- [ ] I9 store QA cycle, the standing loop (docs/design/store-qa-cycle.md steps S2–S9): the three store-persist cells recordable and re-recorded (S2–S4, tracked on A8), the `store-functional` matrix in busbar's own CI against published artifacts (S5), store_conformance's nine checks in-tree and then through a dlopened published plugin over a real DSN (S6), the nightly store matrix (S7), release-check.sh reading the pinned list with all four backends (S8), and the store-upgrade cells that prove a plugin's schema migration does not drop the money (S9)
- [x] I6 docs/voice.md (operator doc for the voice plane; today only a grammar comment in code)
- [x] I7 stale "removed in 1.6.0" wording in operations.md, admin-api.md, migration-1.5.md, observability.md
