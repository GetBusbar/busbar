<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- Copyright (C) 2026 Busbar Inc and contributors -->

# THE LIST — 1.6.0

**The single certified TODO list.** Every other `1.6.0-*.md` sweep is INPUT to this file. One row
per defect, deduplicated across sweeps, ranked by consequence.

**This file has ONE writer.** No other agent appends to it. Its companion is
[`1.6.0-DELETE-LIST.md`](1.6.0-DELETE-LIST.md) — what was swept, proven clean, and can go away.
The loop is **fix → delete → repeat** until only `dev` remains and it is green.

---

## HOW TO READ A ROW

| column | meaning |
|---|---|
| **ID** | `X-nnn`. Allocated here and nowhere else. **Never reused, never renumbered.** |
| **TITLE** | the defect, in one line. Not the file. |
| **CLASS** | money · auth/capability · missing-code · instrument-blind · config · abi · customer-surface · doc-drift |
| **CERTAINTY** | VERIFIED / ADJUDICATE / PARK — below |
| **EVIDENCE** | file:line + the command that reproduces it. `[proven here]` = I ran it. `[carried]` = the source ran it and I did not re-run. |
| **ACTION** | what to do, or the one sentence question that unblocks it |
| **SOURCE** | which sweep found it |

### CERTAINTY — the column that makes this list certifiable

- **VERIFIED** — proven with a runnable command or a red-before-green demonstration. An agent can
  pick this up and act on it today.
- **ADJUDICATE** — a symbol or behaviour is absent **by name**, the concept plainly exists on trunk,
  and nobody has read the behaviour. **This is a question, not work.** A sweep proved why this
  matters: `SessionDriver`/`DuplexWire` read as absent while `SessionPump`/`SessionLease`/
  `SessionView`/`DuplexReader` carried the same seam, and a test absent by name turned out proven on
  trunk **under a different name with a stronger guard**. Lumping renames in with real gaps poisons
  the list.
- **PARK** — needs an owner ruling. **Every money row is PARK, always.** Money is binary,
  bank-auditor standard, and a billed byte is never self-approved (#10/#59). Because PARK is
  mandatory on money it carries no evidential information, so every money row states its evidential
  grade inside EVIDENCE instead: `[proven here]`, `[carried]`, or `[asserted]`.

**CERTAINTY IS NEVER PROMOTED.** If a source says ADJUDICATE, the row says ADJUDICATE. It may be
DEMOTED with a reason. Promotion to VERIFIED requires a proof run here and printable.

### ID ALLOCATION

`X-1` … `X-180` were allocated before this file existed and live in
[`1.6.0-MAP.md`](1.6.0-MAP.md) §3.1. **They are carried here under their original numbers and are
never renumbered.** New rows are allocated from **`X-201`**. `X-181`…`X-200` is a deliberate
unused buffer: concurrent agents were still drawing in the 180s when this file opened, and three
X-numbers were already issued twice by agents sharing a number space. Numbering is a single-writer
job; the buffer is what makes that recoverable rather than an argument.

Rows that arrived carrying a LEDGER id (`M`/`R`/`G`/`S`/`T`/`ENG`/`DOC`/`DR`/`IA`/`GS`/`BR`) keep
that id in SOURCE and are cross-referenced, not renumbered.

### MEASUREMENT DISCIPLINE

The tree is a **shared checkout with ~27 concurrent agents** and it moves under every measurement.
`HEAD = 72c9c14d6` at the open of pass 1, working tree dirty (46 modified paths, several untracked).
Where a number could be changed by another agent mid-read it was taken from the object store
(`git show HEAD:path`) rather than the working tree, and the row says so.

**A prebuilt `xtask` is only valid against the tree it was built from.** `target/debug/xtask` is
from `09:57`; `HEAD` landed `09:59`. Any gate output below taken from that binary says so.

---

# 1. MONEY — every row PARK (#10/#59)

Ranked with anything that changes a **billed byte** at the top.

| ID | TITLE | CLASS | CERTAINTY | EVIDENCE | ACTION | SOURCE |
|---|---|---|---|---|---|---|
| **X-201** | **Two money-path regression guards are deleted in the working tree, uncommitted — one of them is the ONLY guard against the rate-conversion duplicate coming back.** | money | **PARK** | `[proven here]` `git status --porcelain crates/busbar-kernel-ledger/tests/` → ` D hot_path_allocation.rs`, ` D rate_conversion_agreement.rs`; `git diff --stat` → **326 deletions**; both present at HEAD (`git cat-file -e HEAD:…` succeeds); the directory itself is gone. The deleted file's own header: *"What is left is a guard against the second copy coming back… the moment somebody re-forks those three lines to 'avoid a dependency', these are the assertions that stop being trivially true."* | Restore both, or get the owner's word that the guard is retired. An uncommitted deletion of a money regression guard on a shared checkout is indistinguishable from an accident. | this pass |
| **R78** | **A second-currency per-request fee is served FREE and silently, and reported as PRICED.** | money | **PARK** | `[proven here]` `cost/rate.rs:374-376` `per_request_fee` = `self.fees.get(&currency).copied().unwrap_or(0)`; `:383-387` `fee_unit_price_nanos` multiplies that 0; `cost/posting.rs:330-336` pushes the fee line with **`unpriced: false` hard-coded**. A card that names a currency for its RATES and no FLAT FEE in it bills the fee at nothing and reports the line priced. #77(5): free is an EXPLICIT zero row, never silent. #42: a silent 0 is only correct when the card is ABSENT — this card is PRESENT. Trunk already fails closed on an unpriced CLASS and an unpriced LANE; the fee is the one member still reading zero as a price. | Owner: port `fee_for -> Option<i64>` + `Unpriceable::FeeUnpriced` + `Divergence::FeeUnpriced` (exists on `origin/verify/keep-fee-unpriced-currency`), or declare the silent zero intended and say so on the line. | LEDGER R78 · branch-sweep-ledger `verify/keep-fee-unpriced-currency` |
| **X-202** | **The billing-reachability gate is satisfied by a free `fn meter(...)` — it never requires the leg to carry an `impl … Units for …` block, so a leg can price nothing while the gate reads green.** | money | **PARK** | `[proven here]` `crates/busbar/tests/plane_meter_seam_reachability.rs:220` — `common::item_body(&lines, "fn meter(")`. The needle is the free-function spelling; nothing in the loop asserts the `fn meter` it found is the trait method on an `impl … Units for …`. The two conditions it does check (`:217` step present, `:230` seam reached) both read the body of whatever `fn meter(` it found first. | Owner: narrow the needle to the trait-method form, or state that a free `fn meter` is a legitimate Meter step. Also add a red-before-green plant. | seed · branch sweep · gate-blindspot census |
| **M21** | **Every configured price in every deployment is quantized through `f64`, and it UNDERBILLS — reproduced against the real function.** `nano_rate(0.5005) = 500` where #44's half-away-from-zero requires `501`. A silent ~0.2% underbill, forever, on a rate the operator typed exactly. | money | **PARK** | `[carried]` `serde_yaml` → `RateEntryCfg.input_utok: f64` → `RawTierRates` → `TierRates` → `nano_rate`. The tree's own `D7` test already proves it. Live consequence of **M2**. | Owner: this is #81's exact-decimal ruling meeting the config boundary. Rule the boundary, and the fix follows. | LEDGER M21 · M2 |
| **M34** | **The 1.4.x migration reads an operator's price as a double and silently synthesizes a card in which EVERY TIER OF EVERY MODEL IS FREE.** | money | **PARK** | `[carried]` `let n = p1k.as_f64().unwrap_or(0.0); let per_tier = n * 10.0;` — a value `serde_json` will not hand back as `f64` (a string-spelled number, an out-of-range value) becomes `0.0`, and `0.0 * 10.0` is free. #42 is explicit: *"A silent 0 is ONLY ever returned when rate_card is absent."* Here the card is PRESENT and every cell is zero. | Owner: refuse, or declare the all-free card lawful. | LEDGER M34 |
| **M40** | **`nano_rate` answers "FREE" for two different kinds of bad rate, with a card PRESENT.** `nano_rate(1e17) = 0` and `nano_rate(0.0000005) = 0` — any configured rate below `0.0005` micro-units per unit quantizes to zero. | money | **PARK** | `[proven here]` `crates/busbar-kernel-ledger/src/cost/rate.rs:40-46`: `if v.is_finite() && v > 0.0 && v <= u64::MAX as f64 { v as u64 } else { 0 }`. The `else` arm is the whole finding — too big and too small share one answer, and that answer is free. The doc argues the clamp defends against *"an astronomical overcharge"* — it does, by making the class FREE. | Owner: split the arms. Too-large is a refusal; too-small is a refusal or a declared minimum. #42 says money must never silently be free. | LEDGER M40 |
| **M32** | **`/admin/usage` resolves at the BUCKET INSTANT, not at the posting's `arrived_ms` — a tenfold miss, proven in-tree with figures.** | money | **PARK** | `[carried]` #79: *"The resolution key is the posting's own arrival instant (`arrived_ms`)."* `row_priced_at_ms` does `bucket_start_secs.saturating_mul(1_000).max(priced_from_ms)`, so a back-dated correction whose window opens strictly INSIDE an era is clipped to the bucket edge and never reaches the rows it was written for. D9 at `money_one_function_equivalence.rs:718`. | Owner: key on `arrived_ms` per #79, or amend #79. | LEDGER M32 |
| **M33** | **Three money reads are not dated at all, and one of them is THE ENFORCEMENT GATE.** `derived_bucket_usage`, `budget_state`, and **`try_admit`** all price flat off the live config table. Measured gap: 12,330 micro-units. | money | **PARK** | `[carried]` `governance/state.rs:1613,1625,1691,1970`. The doc says it outright at `state.rs:1571`: *"recomputed at read time from the bucket's token ledger × the **CURRENT** rate card."* | Owner: date all three, or rule that enforcement prices at current-card by design and say so on the line. | LEDGER M33 · X-62 |
| **M35** | **FALSE PRICING PROVENANCE — `rate_card_version: 0` is a real entry number, not a null.** `0` is `HistorySeq::OPENING`, so every voice posting and every voice audit record claims the OPENING card priced it. | money | **PARK** | `[carried]` `units_voice.rs:1846,1950`. The other three units each FIXED and DOCUMENTED this — `units_llm.rs:1611`, `units_mcp.rs:1411`, `units_a2a.rs:912`, all calling it *"a confident wrong answer"*. Voice was never converted. Not an active mischarge while voice's root path is boot-sealed — but `root-voice` is default-on, so the PR that flips the serving switch inherits it with zero lines touched. | Convert voice to `card_at(…)` like its three siblings. A landmine, not a fire. | LEDGER M35 · X-126 |
| **X-203** | **`rate_card_version` travels unvalidated from operator configuration into opening-balance SEALING.** | money | **PARK** | `[proven here]` `crates/busbar/src/root/migration.rs:203` `migrate(rows, records, cfg.node, wall, cfg.rate_card_version, secret)` → `crates/busbar-kernel-ledger/src/migration.rs:428` `rate_card_version: u64` → `:440` `opening_balances(&head, rate_card_version)` → `:462` stamped on the marker. `git grep -n rate_card_version -- crates/busbar-kernel-ledger/src/migration.rs` → 4 hits, **not one of them a validation**. The value's origin (`root/migration.rs:118`) resolves `ROOT_CARD.pin()` with a `map_or` fallback to `HistorySeq::OPENING`, so an unpinned card and a genuine opening are indistinguishable downstream. | Owner: does the ledger validate the card version it seals an opening under, or does it trust the root? One sentence settles it. | seed · branch sweep |
| **M37** | **The admin rate-card CORRECTION parses a price through `Value::as_f64` — so an operator correcting a wrong rate can be given a second wrong rate by the parser. And the gate excludes this file BY NAME.** | money | **PARK** | `[carried]` Same defect as **M21**, on the repair path. `no_float_money.rs:21-23`: *"the admin correction parser … lives in files this gate does not scan."* | Owner: same ruling as M21/M2 covers it. Then widen the gate scope — see **X-60**. | LEDGER M37 |
| **M38** | **A fractional billed count is refused and then DEFAULTED TO ZERO — and the seam's own doc states the consequence.** | money | **PARK** | `[carried]` `ir/usage.rs:108-110`: *"every caller in this crate currently ends in `unwrap_or_default()`, so a refused value still lands as ZERO."* #81: a value that will not fit the declared scale exactly is a REFUSAL, never a rounded guess. #42: never a silent 0. The LLM codec's eight billed reads WERE fixed to refuse (`usage_count.rs:123`); the second copy was not. | Fix the second copy the way the first was fixed. Classic half-here-half-there. | LEDGER M38 |
| **M39** | **Every non-LLM plane's declared billing class is DROPPED when persisted to an ABI<4 store** — mcp `calls`, a2a `hops`, streaming `audio-seconds`. | money | **PARK** | `[carried]` `split_units` drops any `usage_units` key outside `{input, output, cache_read, cache_write}`. #77(4) makes those classes the whole mechanism by which a plane declares what it bills. Fairly stated: it is WARNED, not silent (`dropped` returns to `lib.rs:989`). | Owner: a warned drop of a billed class — is that a refusal or an accepted downgrade? | LEDGER M39 |
| **M41** | **Served money bytes are cast through `as f64` and silently lose cents above 2^53.** Six sites. | money | **PARK** | `[carried]` `metrics.rs:401,402,425,462,466,482`. `spend_cents` is `i64`; above 2^53 the gauge stops representing consecutive integers. Already red-proven in-tree at `money_one_function_equivalence.rs:682-708` (D8). The Prometheus exposition format is DECIMAL TEXT — the float is an artefact of the `metrics` crate, not of the wire. | Emit decimal text. #77(8) forbids f32/f64 on any money path and a gauge is a served money byte. | LEDGER M41 |
| **M20** | **A COMPUTED rate-card price is sealed into the WAL** — the read-time conversion #77(3) says is never stored, in a record no subsequent rate row can move. | money | **PARK** | `[carried]` Traced hop by hop: `units_a2a.rs:1107` → `estimate.rs:60` (`fee + Σ(quantity × max_unit_price_nanos)`) → `:72` tier+ceil → `durability.rs:300` seal → `durability.rs:515` writes `reserved` into a hash-chained, fsync'd frame. The tree condemns this exact shape elsewhere. | Owner: #77(3) says price is NEVER stored. Either the WAL stops sealing it or #77(3) gains an exception. | LEDGER M20 |
| **M22** | **`settled` is declared in NANO-UNITS and carries BYTES — so the `OVERDRAFT` flag compares bytes against nano-units.** | money | **PARK** | `[carried]` `hold.rs:645` documents why a usage quantity must never be passed as `priced_nanos`; `teller.rs:1341` does it anyway **and says so in its own comment**. MCP and A2A post bytes; voice posts a cross-class sum; the one live billing plane posts zero. | Owner ruling, then one type. A unit mismatch on the overdraft flag is a bank-auditor finding. | LEDGER M22 |
| **M44** | **Mid-log WAL corruption is truncated exactly like a clean crash tail — on the WAL that seals money holds.** `stopped_because` is written 4×, read **0×**. | money | **PARK** | `[carried]` The distinguishing evidence is recorded and never consulted. Silent data loss on the money path. Same WAL as **M20**/**M22**. | Read `stopped_because`. Refuse on corruption; truncate only on a clean tail. | LEDGER M44 |
| **M45** | **Crash recovery of open holds is an UNREACHABLE module — holds do not recover.** | money | **PARK** | `[carried]` Zero construction sites. Read as a pair with **M44**: the log cannot tell corruption from a clean tail, AND the recovery that would read it is unreachable. Under #77(6) the hold is the enforcement instrument, so an unrecovered hold is an enforcement gap that survives a restart. | Construct it, or declare holds non-recoverable and state the enforcement consequence. | LEDGER M45 |
| **M24** | **A THIRD fee spelling the gate cannot see.** `price_per_request_cents` has four production readers and is not in `one-pricing-site:fee-fields`'s symbol list. | money | **PARK** | `[carried]` The symbol list is `per_request_fee`, `per_request_fee_cents`, `fee_cents`. **M5 closed on a scope fix that is still blind to this spelling**, and `price_per_request_cents` is one of the two inputs to **M20**'s sealed multiply. | Add the spelling. Then ask why the list is hand-maintained at all. | LEDGER M24 |
| **M29** | **A ZERO RATE CARD is in the shipped binary.** `Pricer::flat(` constructed in `main.rs`, where the deployment's own card is what a priced node needs. | money | **PARK** | `[carried]` `no-test-doubles-in-production:doubles` measures **7 against a ratchet of 5, and it went UP** — a rule whose whole contract is *may only go down*. See **X-86**: the register holding these is a TOML table-of-tables and every prior extraction skipped all 14 entries, five of them `verdict = "double"`. | Owner: a flat/zero pricer in `main.rs` is either the split's unfinished half or a live mispricing. Say which. | LEDGER M29 · X-86 |
| **M19** | **An amendment that supplies `rates` but no `per_request_fee` produces a window card with fee `0`.** | money | **PARK** | `[carried]` `amend` replaces the card wholesale with no carry-forward; the missing fee falls through `unwrap_or(0)`. It is AUTHORED, not silent — the signed canonical payload renders `per_request_fee=null` — but #77(5) says *"free is an EXPLICIT zero row, never silent"*, and a wholesale replace that defaults a fee to zero is the shape that rule exists to refuse. Same silent-zero family as **R78**, different site. | Owner: carry forward, or refuse an amendment that omits a fee. | LEDGER M19 |
| **M23** | **A signed back-dated correction MOVES THE INVOICE AND NEVER THE BOOK.** `adjusting_entries` and `recompute` have zero production callers. | money | **PARK** | `[carried]` Built, tested, never constructed. `POST /admin/ledger/amend-rate-history` appends a card entry and books nothing. Same class as **M18** and the unsigned audit chain, but on the money path. | Construct them, or remove the endpoint's implied promise. | LEDGER M23 · M52(b) |
| **M27** | **A registered MCP server can induce an arbitrarily large chat body BILLED TO THE INBOUND CALLER'S BUDGET.** `sampling/createMessage` has no size bound. | money | **PARK** | `[carried]` No bound on message count (iterates `params["messages"]` unbounded). *"the caller's budget bounds what the caller asked for, and it cannot bound what somebody else appended to it."* | Bound it. Owner decides the bound. | LEDGER M27 |
| **M28** | **A signed `amend_rate_history` can be REPLAYED, and can be dated into the FUTURE.** | money | **PARK** | `[carried]` The ed25519 hardening fully landed (`operator_fingerprint` = `sha256(sealed key)`, strict verification over a canonical payload, admitted only under `Set`). What did not: no replay guard, and no upper bound on `effective_from`. | Add a nonce/monotonic guard and an upper date bound. | LEDGER M28 |
| **M18** | **#79's dated-history derivation has no production feed — unfinished wiring, not a mispricing.** `booked_lines()` returns `Vec::new()` in both production impls. | money | **PARK** | `[carried]` `derived_totals_rows` returns `None` on empty, so the read always falls back to the book's balance and the per-row dated derivation never runs in production. Deliberately NOT inflated: the empty answer is argued in the doc comments and is correct. | Wire the feed. Not a mispricing today. | LEDGER M18 |
| **M2** | **Binary floating point on the rate-card path at all. #81 FORBIDS IT BY NAME and the violation is live.** | money | **PARK** | `[carried]` #77(8)/#81 require exact fixed-point decimal (i128 mantissa, scale 6). Rate config is ingested as `f64` micro-units and converted once. #81: *"read the JSON number's DECIMAL TEXT, never its `f64` value."* | **Owner: is a float at the config boundary lawful, given #81 says no binary float on any money path?** This one ruling closes M21, M34, M37 and part of M40. **Highest-leverage money question on the list.** | LEDGER M2 |
| **M36** | **#81's representation ruling is declared and not adopted on the money path — `27.5` is not representable anywhere a count is actually carried.** | money | **PARK** | `[carried]` `busbar_contract::count::Count` is built, correct, float-free and tested; every carrier that actually gets priced is still whole-unit `u64` — `caps::UsageLine.quantity`, `cost::Quantity.amount`, `billing::Usage.usage_units`, `MeteringDelta`/`MeteringRow.tokens_*`, `read_count_u64`. | Adopt `Count` on the carriers, or narrow #81. | LEDGER M36 |
| **M12** | **`BODY_IS_CONTRACT` omits `billing` and `ledger` — so any `improvement` entry may forgive a wrong `spend_micros` in a money response body.** | money | **PARK** | `[carried]` `differ.rs:91-98` lists `admin.ops, boot.refusal, boot.warning, config.migrate, cli, ops.scrape`. The named audit calls this the single highest-leverage upstream change it found. | Add `billing` and `ledger`. One line, owner's signature. | LEDGER M12 |
| **M6–M17** | **Twelve `accepted-differences.json` money waivers, each independently falsified or over-broad** — including one where a money class was added to the forgiven set BY AN AGENT under the owner's signature (**M7**). | money | **PARK** | `[carried]` M6 `PR2-D4` (premise contradicted by its own golden: 250¢ recorded where the entry says zero) · M7 `G-1` (`48ea84225` widened `classes` to include `effects.usage` while `by:` still reads the owner's 2026-09-07 ruling) · M8 `F-011r` · M9 `PR2-D2` · M10 `F-011c` · M11 `D-3` · M13 `F-003` · M14 `F-016` · M15 `D-4` · M16 `PR2-D1/D3/D9` (two mutually exclusive reasons forgiving the same cell) · M17 (transform path skips the money narrowing). | **NOTHING HERE MAY BE BLESSED, RE-RECORDED OR EDITED BY AN AGENT.** Owner reads each. M7 first — widening a forgiven money class is reserved to the owner by #10/#59 and it was done under a stale signature. | LEDGER M6–M17 |
| **M46–M49** | **The $100 hard cap of decision #78 binds nothing.** Implemented correctly in the WRONG REPOSITORY as a weekly report-only script that has never once run; the repository that actually spends contains ZERO dollar ceilings; money/oracle jobs are routed to a backend that does not exist. | money | **PARK** | `[carried]` `scripts/cost-watch.py` implements the bands exactly (`SOFT_ALARM_USD=50`, `REVIEW_USD=80`, `HARD_CAP_USD=100`) with a strict exit-code contract including `exit 4 = "unknown spend, NOT a clean bill"`. But `cost_watch` has **0 occurrences** in the spending repository; `spend` **0** in the autoscaler. And `ec2:false` + `allow_money_on_latchkey:false` means `route_overflow` sends every money/oracle job to a bucket nothing drains, silently — while **#56 says the opposite in verbatim text**. | Owner: this is an infrastructure spend control, not a product defect, and it is one flag from unbounded. | LEDGER M46–M49 |
| **M50** | **A live path from "nothing was compared" to BOARD on the money gate.** | money | **PARK** | `[carried]` The replay driver bails on `rc == 2` (harness skew) but **not on `rc == 1`**, which `run_impl` sets for *"strict: no owed cells matched the filter — nothing was compared"*. `HarnessOracle` hardcodes `refuse_extra_candidate: false`, so a golden with a valid `meta.json` but a no-`PASS` ledger turns every candidate cell into `extra.candidate` → `NoGolden` → not `Red` → green → the turnstile BOARDs on a proof that compared nothing. | Bail on `rc == 1`. A gap and a success must never share an output. | LEDGER M50 |
| **M51** | **A hardcoded SHA in the money-golden RE-RECORD workflow resolves while being wrong.** | money | **PARK** | `[carried]` `oracle-rerecord.yml` pre-fills `default: e2194e42…`. It RESOLVES, it is *"xtask config-schema: repoint PLANE_VERB_SOURCES voice->streaming"*, it is **NOT on the 1.6.0 line**, it lives on `origin/predev`, and it is 380 commits from trunk HEAD. `required: true` does not protect a pre-filled default. | Remove the default. Re-recording a money golden against the wrong tree is the worst available blast radius. | LEDGER M51 |
| **M52** | **Six more money capabilities built and NEVER CONSTRUCTED.** Breaker per-destination budget never installed (`admissible()` always true) · `adjusting_entries` no caller · #79's exact pricing view has no production producer · **no node ever seals a ledger checkpoint**, so `GET /admin/ledger/checkpoints` returns empty on every node and #79's premise is VACUOUSLY TRUE · budget `Pricer` is `Pricer::flat(0)` · multi-currency rate card has no production builder. | money | **PARK** | `[carried]` All machine-checked by **G37**, listed in `qa/unconstructed.toml`. Plus: late-accrual child-hold conversion is back to an assertion, and the ABI minor was bumped 19→20 for a keyed-units capability with no producer and no host consumer. | Owner: six rulings, or one covering ruling about built-and-unconstructed money capability. | LEDGER M52 · X-138 |
| **M53 · M54 · M55** | **Three of the four groups that carry the money path were NEVER MEASURED on the only full readout this release has** — EQUALITY, PARITY, KERNEL. | money | **PARK** | `[carried]` PARITY's root cause is harness, not product: the group compares `target/release/busbar` through **two hard-coded literals that ignore `CARGO_TARGET_DIR`**, so on a shared checkout its "candidate" is whatever binary another agent last built. KERNEL ran 1 leg of 3; the two that did not run are the whole-crate ones (`-p busbar-kernel`, `-p busbar-contract`). | **Get one quiesced checkout, one SHA, no other writers, and run it once.** This is cheaper than any row above it and it unblocks three. | LEDGER M53–M55 · done-readout |
| **X-204** | **The whole priced-`tariff` grammar is absent from trunk code — `Rounding`/`PerUnitTerm`/`FlatTerm`/`TariffTerms`/`tier_price`/`metered_dimensions` exist on a branch and in 7 documents, in ZERO `.rs` files.** | money | **PARK** | `[proven here]` `git grep -l -F tariff -- '*.rs'` → **0**. `git grep -l -F tariff -- 'docs/*'` → **7**. The branch (`origin/verify/keep-tariff-7`, 332 lines in `busbar-contract/src/tariff.rs`) adds declared banker's rounding (half-to-even, the only unbiased rule) for *amount per N units*, per-plane declared metered dimensions with a **boot refusal** for an amount no unit would pay, and the tier price resolved once and threaded to every leg's journal row. Trunk HAS the pool/scope half (`pool_scope`, 12 files); the amounts half did not land. | **Owner: does 1.6.0 ship priced tariffs at all?** This is a feature, not a repair, and it is the single largest absent money surface. If yes, note the branch-side defect to fix on port: `Rounding::Up => whole + 1` is "away from zero" only for non-negative numerators. | branch-sweep-ledger · seed |
| **X-205** | **Ledger reconciliation and checkpoint symbols are absent from every `.rs` file on trunk** — `ReconciliationEntry`, `CheckpointRefusal`, `statement_residual`, `sealed_totals`. | money | **ADJUDICATE** | `[proven here]` each of the four: `git grep -l -F <sym> -- '*.rs'` → **0**; `git grep -l -F <sym>` → **1** (the branch-sweep document itself). **DEMOTED from the seed's implied gap to ADJUDICATE**: absent-by-name with the behaviour unread. `sealed_totals` and `CheckpointRefusal` may be carried on trunk under the checkpoint vocabulary that **M52(d)** says exists-but-is-never-constructed — that is a different defect from absent code, and conflating them poisons the row. | **Read the behaviour before opening work.** One sentence settles it: does trunk's checkpoint/reconciliation seam carry these four concepts under other names, or is the concept genuinely absent? If absent it joins **M52(d)**; if renamed it is a DELETE-LIST row. | branch-sweep-ledger · seed |
| **X-206** | **`lend_posting` / `PostingLent` / `post_lent` are absent from every `.rs` file on trunk, and a branch cell names a double-settlement refusal.** | money | **ADJUDICATE** | `[proven here]` all three: `-- '*.rs'` → **0**; repo-wide → **1** (the branch-sweep document). **ADJUDICATE, not a gap**: a lending/settlement concept can exist under the `hold`/`settle`/`Posted` vocabulary trunk does carry, and the double-settlement refusal the branch cell names may already be proven on trunk under a different name with a stronger guard — that has happened once already in this sweep corpus. | Read the branch cell's assertion against trunk's `Posted::settle` guard. Then either raise a money gap or move it to the delete list. | branch-sweep-ledger · seed |
| **X-6 · X-7** | **Two money reads carrying `resolves: false`, and fourteen named money behaviours with NO ORACLE CELL AT ALL.** | money | **PARK** | `[carried]` oracle-coverage §3.1, §3.4. Two of §3.4's sub-findings coincide with **M1** and **M3** but were reached independently and cite nothing from that source — correctly not credited as a fold. | Owner: fourteen money behaviours with no cell means the oracle is green over them by construction. | oracle-coverage · MAP X-6/X-7 |
| **X-55** | **Oracle-green is structurally SILENT on plane money.** mcp 912 cells / **0** touch money; a2a 468 / **0**; llm 149/14. | money | **PARK** | `[carried]` No 1.5.5 golden can exist for a post-1.5.5 plane — this is not a corpus gap that can be filled by recording more cells. | Owner: the release's central safety claim does not cover plane money. State what does. | MAP X-55 |
| **X-58** | **Plane crates carry 9–41 live money references each where #43/#87 require none; `UnitKey::new(0)` was fixed at `units_a2a.rs:1019` and is STILL LIVE at `units_mcp.rs:1481`.** | money | **PARK** | `[carried]` The textbook half-here-half-there: a fix applied to one copy and not its twin. | Fix the twin. Then ask what gate would have caught it. | MAP X-58 |
| **X-60** | **The no-float-money gate CANNOT SCAN the file with the missing clamp.** `ROW_NO_FLOAT` is hardcoded to `LEDGER_SRC` + 4 named files; `busbar-kernel/src/cost.rs` and `busbar-kernel-budget/src/price.rs` are in neither scan set. | money | **PARK** | `[carried]` `no_float_money.rs:93,117,130`. The gate cannot produce a RED for that file no matter what float code is added to it. Compounded by **M37** (the admin correction parser is excluded BY NAME) and **X-144** (the selftest's 17 red-expecting cases pass trivially because the gate is already red). | Widen the scan set to a walk with a denominator, not a hardcoded file list. | MAP X-60 |
| **X-62** | **#79's dated card history is honoured on `/usage` and on NEITHER of the other two usage surfaces.** The surfaces are non-reconcilable even after unit conversion. | money | **PARK** | `[carried]` `derived_bucket_usage` (`governance/state.rs:1599-1633`) prices against the CURRENT card — zero `card_at`/`arrived_ms` references. Overlaps **M33**; kept as its own row because M33's subject is the enforcement gate and X-62's is the customer-visible reporting surface. | Same fix as M33 for the two remaining surfaces. | MAP X-62 |
| **X-96** | **An advisory claims a LANDED money fix that is not on this trunk.** ADV-1.6.0-2, a billing/rate-cap integrity defect. | money | **PARK** | `[carried]` `docs/advisories/1.6.0/mysql-store-split-flush-request-count-loss.md` carries a present-tense *"Fix (1.6.0)"* citing `crates/plugin-testkit/` and `assert_usage_survives_reopen_atomically`. **`crates/plugin-testkit` does not exist**; the function's only occurrence in the repo is inside the advisory's own text; its cited commit `5e3c13044` is not an ancestor of HEAD. A release-signoff reader believes a money defect is fixed and gated. | Correct the advisory or land the fix. A false "fixed" on a money advisory is worse than an open one. | MAP X-96 |
| **X-48** | **CHANGELOG advertises rate-card-history as shipped; the endpoint is not wired to the dated history.** *"It cannot ship as it stands."* | money · customer-surface | **PARK** | `[carried]` commit `ea94ce933`. | Unship the claim or wire the endpoint. | MAP X-48 |
| **X-50** | **`UpstreamIdx(0)` hardcoded — a gemini-live session declared after an openai one BILLS EVERY TURN ON THE WRONG PROVIDER'S LANE.** | money | **PARK** | `[carried]` commit `7ca14571b`. Notable as the sentence that defeated the S3 residue keyword classifier: unambiguous owed money work that trips no regex. | Resolve the index. | MAP X-50 |
| **X-149** | **A money overflow that escapes every budget cap.** | money | **PARK** | `[carried]` MAP §3.18, verified by that auditor. | Owner. | MAP X-149 |
| **M3** | **A money test cited as proof DOES NOT EXIST.** | money | **VERIFIED-ABSENT (PARK)** | `[carried]` `money_never_ends_an_admitted_unit.rs:30` cites `a_spend_past_the_reservation_is_carried_out_as_an_overdraft` as the overdraft proof. `grep -rn` → 1 hit, the citation itself. | Write the test or strike the citation. A citation is not a proof. | LEDGER M3 |
| **M4 · M5** | **`priced_from_ms` has landed on the PLANE side of the #43 line** — the first symbol of the parked two-money-reads recommendation, placed where Law #43/#71 forbids it. | money | **PARK** | `[carried]` `crates/busbar-llm/src/unit/meter.rs:490`, sole remaining `plane-no-money` finding, at exactly 1. **DO NOT MERGE M4 WITH M5** — M5 is the card-AUTHORING side, M4 is the read/pricing side. | **Two live rulings collide: the money-reads fix is right and its placement is wrong.** Only the owner can resolve it (#10/#59). | LEDGER M4/M5 · gate-sweep §1.2 |
| **M30** | **Fifteen live money references in crates the #83 roster calls planes, and EVERY ONE READS `[UNPROVEN]`.** | money | **PARK** | `[carried]` `plane-pricing-blindness` RED on 5 rows: `:undocumented` (15), `:price` (8 files), `:hold` (3), `:rate-card` (2), `:billing-switch` (2). `qa/plane-pricing-blindness.toml` is deliberately empty — *"a gate that arrives with its census already excused teaches nobody anything."* The emitter **cannot derive** `reach` or `why`; it writes `reach = "unproven"`, which is itself red. | **A human must read each of the 15 and rule debt or bug.** This is a ruling, not a task. The `REPORT_ONLY` posture excusing *"tracked money debt"* does not apply because nothing is tracked yet. | LEDGER M30 · gate-sweep §1.1 |

---

# 2. AUTH / CAPABILITY

| ID | TITLE | CLASS | CERTAINTY | EVIDENCE | ACTION | SOURCE |
|---|---|---|---|---|---|---|
| **X-207** | **The credential-precedence de-dup landed the unit and NEVER SWITCHED THE CALLER — and the caller's crate cannot even reach the unit, because the dependency edge does not exist.** | auth/capability | **VERIFIED** | `[proven here]` The unit: `crates/busbar-kernel-identity/src/carrier.rs:71` `pub fn extract_client_token(headers: &dyn HeaderView)`, with 10 assertions in `tests/carrier_tests.rs`. The caller: `crates/busbar-kernel/src/auth/mod.rs:669` `AuthMiddleware::extract_client_token(req: &Request<Body>)` — a SECOND implementation, and it is the one both production call sites use (`auth/exchange.rs:55`, `auth/mod.rs:1474`). **`git grep -ln busbar-kernel-identity -- '*/Cargo.toml'` → 2 files, and `crates/busbar-kernel/Cargo.toml` is NOT one of them.** The kernel literally cannot call the unit. `auth/mod.rs:950` admits it in a comment: *"ported here because both loops write the SAME…"*. Stronger than the seed stated. | Add the edge and switch the caller, or delete the unit. Two live copies of credential precedence is a security-boundary duplicate. | seed · branch sweep |
| **X-208** | **The only `impl HeaderView` in the tree is in a TEST MODULE.** The de-dup unit has zero production implementors. | auth/capability | **VERIFIED** | `[proven here]` `git grep -n 'impl.*HeaderView\|trait HeaderView' -- '*.rs'` → exactly 2 hits: the trait at `crates/busbar-kernel-identity/src/carrier.rs:32`, and `impl crate::carrier::HeaderView for Headers` at `crates/busbar-kernel-identity/src/tests/mod.rs:120`. Nothing in any binary implements it. | Same fix as **X-207**; recorded separately because the *zero production impls* fact is what makes the unit provably inert, independent of the missing edge. | seed |
| **X-209** | **The declared credential-signature grammar exists on trunk ONLY IN A DOCUMENT — zero code files.** `CredentialSignature`, `CredentialArrival`, `CredentialPresentation`, `SigningParams`. | auth/capability | **ADJUDICATE** | `[proven here]` each of the four: `git grep -l -F <sym> -- '*.rs'` → **0**; repo-wide → exactly 2 files, both docs (`1.6.0-credential-signature-declared.md`, `1.6.0-branch-sweep-ledger.md`). **This is the docs trap in its purest form: a symbol that exists only in `docs/` is a symbol that is NOT in the product.** Held at ADJUDICATE rather than raised as a gap because the *concept* — signing an outbound credential presentation — plainly exists on trunk in `egress_auth/`, and nobody has read whether that seam already carries these four under other names. | **One sentence unblocks it: is the egress-auth signing seam the same seam this grammar declares, or a different one?** Declared-only for the whole release either way; #34's declaration is not code. | seed · branch sweep · MAP |
| **X-210** | **Six kernel-sealed constructors are scanned by NO GATE AT ALL — including `IdempotencyKey::mint(` on the money path.** | auth/capability | **ADJUDICATE** | `[proven here]` `git grep -n 'IdempotencyKey::mint(' -- '*.rs'` → **3 hits and not one is production**: `busbar-contract/src/caps/tests/mod.rs:633` (a test) and `xtask/src/gates/construction/selftest.rs:302,339` (the gate's own FIXTURE — the selftest plants it at `crates/busbar-llm/src/zz_planted_sealed.rs:5`). So the constructor is named by a gate fixture and by nothing the gate actually scans. Directly compounds **X-154** (`token-sealed`'s fixture vocabulary `UnitToken::mint(` has 0 occurrences while the real shapes are `Pass::mint(`/`Grant::<…>::mint(`) and **X-177**. | Read with X-154/X-177 as one subject: **the sealed-constructor gates are scanning a vocabulary the tree does not use.** One ruling on the real mint vocabulary fixes all three. | seed · MAP |
| **X-177** | **`token-sealed` and `token-sealed:admit-token-mint` do not see a FORGED MINT OUTSIDE THE KERNEL.** Each states *"minted only inside"* as its claim while passing with `UnitToken::mint(` and `AdmitToken::mint(` planted in a plane crate. | auth/capability | **VERIFIED** | `[carried]` Red-before-green plant performed by the source. **This is the #65/#73 capability-minting boundary, and two of its five green rows are green about nothing.** Their siblings `kernel-seal` and `seal-sites` DO fire — so the rule is right and the scan set is wrong. | Repoint the scan set to the real mint vocabulary (see **X-210**). | MAP X-177 |
| **X-87 · X-88** | **The AUTH KIND IS NEVER SCANNED, and the selftest built to catch exactly that cannot red it either.** | auth/capability | **VERIFIED** | `[carried]` `rules2.rs:307` still spells `pure_auth`/`egress_auth` after #3 collapsed them into `auth`. `kind_globs()` returns an **empty Vec** for an absent key rather than erroring, so both resolve to zero crates and `auth` is skipped. A `busbar-kernel` dependency creeping into `auth-admin-tokens` or `auth-static-plugin` would never be caught. `construction.rs:242-252` builds the **completeness oracle** from the same wrong vocabulary — checker and checked share the bug. `selftest.rs:882-889` plants one bad dependency per kind and plants nothing for those two. Independently corroborated by the ratchet census: `ceiling-census` lowered `plugin_kinds.pure_auth` from **2 → 0**, i.e. no crate matches the glob at all. | Make `kind_globs()` error on an absent key. Then fix the vocabulary. **A gate that returns empty for an unknown key is the release's dominant defect class landing in the instrument.** | MAP X-87/X-88 · ratchet-census · gate-sweep §1.4 |
| **X-95** | **The ABI ships an EIGHTH plugin kind.** `Kind { Plane, Transport, Auth, EgressAuth, Store, Secret, Hook, Export }` — #3 locks seven. The gate's own `FORBIDDEN_KINDS` bans the string `"egress-auth"` while the type system mints it. | auth/capability · abi | **VERIFIED** | `[carried]` `crates/busbar-contract/src/plugin.rs`, read directly. **This is the root cause of X-87** — `rules2.rs` spells `egress_auth` because the enum still offers it. | Collapse the variant per #3 (auth is ONE kind; inbound-verify and outbound-sign are two OPERATIONS). Fixes X-87 at the root. | MAP X-95 |
| **X-89 · X-143** | **`Secret<T>` (#54) was never constructed, and the one gate wired to catch it is wired never to fail.** | auth/capability | **VERIFIED** | `[proven here]` `.github/workflows/ci.yml:1466` → `run: SECRET_GATE_REPORT_ONLY=1 scripts/secret-hygiene-gate.sh --report`. And the trap: `scripts/secret-hygiene-gate.sh:1440` `report_only="${SECRET_GATE_REPORT_ONLY:-1}"` — **the DEFAULT is report-only, so deleting the env var would not arm it.** Arming requires `=0` explicitly (`:1479`). `[carried]` Armed, it is exit 1 with **17 violations**, stable over two runs: `private_key` in `transport/trust.rs:33` and `jwt_bearer.rs:313`, `access_token` ×2, `secret`, `credentials`, `api_key` in `busbar-voice` ×2, `caller_token` ×7. | **Owner: arm it, or state the date it arms.** A locked decision's capability absent and its gate defaulted off is two halves of one omission. | MAP X-89/X-143 · secret-hygiene |
| **X-172** | **Two real secret leaks in committed product code, now proven by test.** | auth/capability | **VERIFIED** | `[carried]` `egress/engine/mod.rs:750` interpolates the proxy env value **including userinfo** — the test catches `"https://svcacct:hunter2-Pr0xyP@ss@proxy.corp:3128"` in a refusal. `egress_auth/jwt_bearer.rs:303` formats a base64 error that **names a byte offset inside the private key**. The defect is on HEAD; only the detection is new. | Fix both. **Note the path correction from the kernel file-verdict sweep: LEDGER S21/S4 cite `crates/busbar-kernel-egress/src/egress_auth/jwt_bearer.rs` — THAT DIRECTORY DOES NOT EXIST.** The live file is `crates/busbar-kernel/src/egress_auth/jwt_bearer.rs` and the defect is at `:303`. Working S21 from the ledger's path finds nothing and closes a live disclosure as fixed. | MAP X-172 · file-verdicts-kernel correction 1 |
| **X-211** | **The secret-hygiene gate is blind to `format!`-into-`Err`.** | auth/capability · instrument-blind | **ADJUDICATE** | `[carried]` Raised by the secret-hygiene sweep; the shape is exactly **X-172**'s second leak (`jwt_bearer.rs:303` formats a base64 error naming a byte offset inside the private key) reaching a caller through an `Err` rather than a log sink. **ADJUDICATE because I did not run the matched pair**: the gate's CHECK 3 (`secret-hygiene.md` Part 5) is titled *"a secret must not be INTERPOLATED INTO A MESSAGE A CALLER RECEIVES"*, so the rule may already exist and be unarmed rather than absent. | Settle it with one plant: put a `format!`-into-`Err` leak in reach and one out of reach, and print both verdicts. If CHECK 3 fires, this merges into **X-89** (unarmed, not blind). | secret-hygiene · seed |
| **X-51** | **Every TLS private-key read in production is UNAUDITED.** `AccessJournal`/`AccessPurpose` exist and are unit-tested; `transport_key_token` has zero production callers. | auth/capability | **VERIFIED** | `[carried]` commit `6d3bda8a3`. **Contradicts the owner ruling that plugins may not touch secrets *because the kernel audits them*** — the stated justification for the whole boundary does not happen. | Wire it, or withdraw the justification. | MAP X-51 |
| **X-123a** | **`Denylist` has ZERO production constructions — the cloud-metadata SSRF deny/allow set for MCP and A2A outbound dials never exists at runtime, and the guard evaluates against an empty set.** | auth/capability | **VERIFIED** | `[carried]` `busbar-kernel-egress/src/trust/net.rs:1498`. The structs that would carry it — `NetSeam`, `A2aBindings` — are themselves constructed only in their own test files. | Construct it. An SSRF guard over an empty deny set is not a guard. | MAP X-123a |
| **X-124** | **`hook_veto::Veto` is a complete security seat with ZERO CALLERS — and both veto mechanisms in the tree are dead on the Approve step.** | auth/capability | **VERIFIED** | `[carried]` `HookGate` has zero production implementors; `hook_veto::gate()` — the only consumer of `Veto` — has zero callers anywhere outside its own tests. `busbar-llm`'s separate `VetoSeat` is also uninstalled and self-documents as such. The "first veto wins" kill-switch is inert on every deployment. | Install one, or delete both and remove the claim. | MAP X-124 · X-135 |
| **X-125** | **`KeyMaterial` and `SecretValue` have zero construction AND zero read-side calls.** No production code wraps a resolved secret in the one type designed to gate its exposure behind `KernelSeal`. | auth/capability | **VERIFIED** | `[carried]` `busbar-contract/src/kinds.rs`. | Adopt or delete. Read with **X-89**. | MAP X-125 |
| **X-127** | **`float_to_secs()` guards `is_finite() && >= 0.0` then casts to `u64` with NO UPPER BOUND — a token endpoint returning `expires_in: 1e20` yields an effectively ETERNAL egress-auth token.** | auth/capability | **VERIFIED** | `[carried]` `egress_auth/mod.rs:99-107`. Same saturating-cast class as the rate clamp, single site rather than a diverged twin. | Bound it. Cheap, isolated, no ruling needed. | MAP X-127 |
| **X-132** | **The audit chain's READ side is already wired — only the key is missing.** `NodeAudit::keys()` returns an EMPTY SET on every node because `record.public_key_hex()` is `None`. | auth/capability | **ADJUDICATE** | `[carried]` `units_admin/mod.rs:756-767`. **`auth.signing_key` is not a substitute, on three grounds from the tree's own docs:** it is fleet-shared (*"every node resolves the SAME secret"*) so any node could forge any other's chain; it is rotated as a revocation mechanism, and `AuditKeySet` requires keys be only ever ADDED because *"a key removed makes every record it signed unverifiable, which is indistinguishable from forgery"*; and it is optional. A per-node `audit.signing_key` moves the frozen config schema snapshot, gated byte-for-byte. | **Owner decision: add a per-node `audit.signing_key`?** It is the one change that closes S27/S30/R71 together. | MAP X-132 |
| **S27 · S30 · R71** | **The audit chain is UNSIGNED, UNJOINABLE to the ledger, unbounded, and its independent verifier does not ship.** | auth/capability | **VERIFIED** | `[carried]` The 19 audit-chain functions form an island: `hold_ref`/`settle_ref`/… are never `Some`; nothing signs, verifies, anchors or prunes it; it is never replayed at boot and `retain_from` has zero non-test callers. `scripts/verify-audit-chain.py` — the out-of-process verifier that checks the published recipe *"by something that cannot see the code"* — **is absent from HEAD**; it exists on two branches (`2bdd3c743`, `834f62f49`). The signing half IS in HEAD; only the verifier did not make it. | **1.6.0 would ship an unsigned chain, with no independent verifier, not joined to the money it describes.** Read as one subject with **X-132**. | LEDGER S27/S30/R71 |
| **X-165 · X-166** | **`kind-isolation:name` has a live hole, and three of its selftest proof cases plant into a path that no longer exists.** | auth/capability · instrument-blind | **VERIFIED** | `[carried]` `admin` fell out of the live-derived ban list when `busbar-plane-admin` became `busbar-core-admin`, and nothing flagged the removal. A/B proven on disk: `busbar-auth-mcp` → FAIL, **`busbar-auth-admin` → PASS**, identical shape. The three selftest cases plant into `crates/busbar-plane-admin/src/` — **zero files scanned, vacuous by construction**, proven with the fixture's own bytes (same content in a live plane crate → FAIL; at the dead path → PASS). `[proven here]` `crates/busbar-plane-admin` does not exist. | Repoint the ban list derivation and the three fixtures. | MAP X-165/X-166 |
| **X-173** | **`kind-isolation` cannot produce a NO for three real violation shapes** — a waived name shortened to the fusion form (`busbar-auth-admin`); a control surface naming a money symbol (`fee_cents`, `MONEY`); a plane source naming a transport library and socket module (`axum`, `tokio::net`). 19 of 119 selftest cases fail. | auth/capability · instrument-blind | **VERIFIED** | `[carried]` All reproduced at `--jobs 1`, so it is a defect and not load. | Fix the three shapes. | MAP X-173 |
| **X-159** | **Enabling key auth granted EVERY CALLER FULL ADMIN.** | auth/capability | **CLOSED** | `[carried]` Fixed at `9e8dea58b`. Row kept so the fix is traceable to the finding and nobody re-opens it. | None. Verify it stays fixed. | MAP X-159 |

---

# 3. MISSING-CODE

| ID | TITLE | CLASS | CERTAINTY | EVIDENCE | ACTION | SOURCE |
|---|---|---|---|---|---|---|
| **X-64 · X-136** | **Four of five plane crates are not on a serving path, and `busbar-plane-decision` (2,644 lines) is not a dependency of the `busbar` binary at all.** "Protocols as Plugins" is five complete plugins that nothing loads. | missing-code | **VERIFIED** | `[carried]` Zero references in `crates/busbar/src/`. One (decision) was registered at `7265a533b`; the other four remain declared-only. The 5th plane (#48) does not exist for any customer. | The cutover. This is the release's headline sentence and it is 1-of-5 true. | MAP X-64/X-136 |
| **X-167** | **An operator still CANNOT WRITE a `decisions:` block.** Measured, not inferred. | missing-code · config | **VERIFIED** | `[carried]` `busbar --validate` → `BUSBAR-3015: unknown field 'decisions'`. `DeployCfg` is `deny_unknown_fields`, and plane sections reach it only through `LIFTED_TOP_LEVEL_KEYS`, **frozen at `["mcp","oauth_as","tools","agents","streams"]`** and annotated `// plane-purity: frozen-wire`. #48's gate column — *"the `decisions:` config section exists"* — is half true: in `config_sections()` by registration, unreachable from a config file. | A kernel-side config-seam landing. Supersedes **X-65**, which is the same defect at lower resolution. | MAP X-167/X-65 |
| **X-133 · X-134** | **`open_export` has no reachable argument, and `open_plane` is blocked by BOOT ORDER — structurally.** | missing-code | **VERIFIED** | `[carried]` `export.<n>.module` fails closed against a **const list** (`config/sections.rs:434-440`); `preflight.rs:110-112` says it outright. No config can name an export plugin, so the loader can never be called. For planes: `register_planes()` runs at `main.rs:601` inside `fn main()`; the plugin registry built from `plugins.dir` loads at `main.rs:996`, **after** — and `install_planes` panics if called after the plane list was first read, which `--validate` does at `:598-600`. **There is no point in the process where the registry exists and the plane list is still writable.** | Not a missing call — currently impossible. Tracker H6 owns the C-ABI→native `PlaneDecl` adapter; the spec sizes it at 2,513–4,148 LOC. | MAP X-133/X-134 |
| **X-68** | **#39's export kind is compiled into the kernel, not plugin-routed — and `otlp`, named in #39, has ZERO implementation anywhere.** | missing-code | **VERIFIED** | `[carried]` `busbar-kernel/src/export/{prometheus,webhook,file}.rs`; grep clean for otlp. | Implement or strike from #39. | MAP X-68 |
| **X-69** | **The transport kind has NO LOADER ABI AT ALL.** Third-party transports are categorically impossible. | missing-code · abi | **VERIFIED** | `[carried]` `busbar-contract/src/transport/registry.rs` says so in its own doc: *"Transports are in-tree and never dynamically loaded, so there is no loader window to police."* | Owner: is transport a plugin kind or not? #3 says seven kinds; one of them cannot be a plugin. | MAP X-69 |
| **X-70 · X-71** | **The spec-mandated feature `plane-streaming` DOES NOT EXIST, the new plane is gated behind the OLD plane's feature flag, and a file asserts its own completion falsely.** | missing-code | **VERIFIED** | `[carried]` Grepping every `Cargo.toml`: no `plane-streaming` feature anywhere. The only one that exists is `plane-voice` (`crates/busbar/Cargo.toml:203`, `busbar-kernel/Cargo.toml:207`). `root/registry.rs:462-463` gates the NEW `StreamingPlane` push behind `#[cfg(feature = "plane-voice")]`. And `busbar-plane-streaming/src/register.rs`'s header says *"S1 IS LIVE… `busbar-plane-voice` is DELETED"* — `busbar-voice` is alive at 359 LOC and `busbar-plane-streaming`'s own `Cargo.toml` depends on `busbar-voice-codec`. | Add the feature, repoint the cfg, correct the header. | MAP X-70/X-71 |
| **X-72** | **#34/#35 promised an ENFORCING GATE for `busbar-api`'s retirement and it does not exist.** 18 live dependents; `full_gate.rs:203-204` builds it as a permanent package. **Nothing will ever go red over this crate shipping.** | missing-code · instrument-blind | **VERIFIED** | `[carried]` `crates/api/Cargo.toml`. | Write the gate or withdraw the promise. | MAP X-72 |
| **BR-G1** | **The `raise-ledger` gate-integrity tool is entirely absent from trunk** — arithmetic that answers, for one branch against its own `base_ref`, whether its gate findings actually ROSE. | missing-code | **VERIFIED** | `[proven here]` `git grep -rn 'raise-ledger\|raise_ledger'` → hits in **`docs/` only** (LEDGER, branch-sweep-ledger, local-only-audit), **zero code files**. The branches carry it: `origin/delete/keep-gates-4`/`4b` add `xtask/src/gates/construction/raise_ledger.rs` (764 lines) and `scripts/raise-ledger-sweep.sh`. Trunk's construction rows are only `ceiling-rose`/`ceiling-slack`/`ceiling-census`. Three independent sweeps agree. | **Given 816 branches went unread because no tool scored a branch against its base, this is worth a look.** Port from the branch or decide it is not wanted. | LEDGER BR-G1 · branch-sweep-ledger ×3 |
| **X-212** | **The CI pre-proof box-pool engine is absent from trunk code AND from trunk docs.** | missing-code | **VERIFIED** | `[proven here]` `grep -rn 'box.pool\|pre-proof\|preproof'` over the repo excluding `target/` and `.git/` returns **only** `black_box(pool)` in a bench and two rows of `1.6.0-branch-sweep-ledger.md` describing the absence. `[carried]` The branch sweep: `preproof`/`qstamp`/`qlock`/`power_*` → **0 trunk files anywhere (code OR docs)**. Trunk keeps `scripts/land.sh`, `ci-remote-lib.sh` and `testing/fleet-fixtures`, but only `fleet_hosts`/`fleet_pick_host`/`write_fleet_file` — the pooled pre-proof runner engine is absent. Source branches: `origin/delete/keep-fleet-3` (247/271 added symbols absent), `origin/delete/keep-land-engine-11` (144/166). | Owner: port the pre-proof pool or declare the CI model final without it. **Note it interacts with M46–M49** — this is the machinery that would bound spend. | branch-sweep-ledger · seed |
| **X-213** | **Three literal `"http"` strings remain where a branch uses `Transport::Http.name()` — but two of the three are URL-SCHEME comparisons, not transport labels.** | missing-code · doc-drift | **ADJUDICATE** | `[proven here]` All three lines confirmed at the exact cited positions: `plane_host/guard.rs:135` `if !scheme_is(url, "http") && !scheme_is(url, "https")`; `preflight.rs:963` `if !(config_validate::scheme_is(url, "http") && host_local)`; `tls.rs:711` `backoff.absorb("http", &e)`. `Transport::Http` and `pub fn name(self)` both exist (`busbar-contract/src/transport/transport.rs:248-250`). **DEMOTED to ADJUDICATE with a reason:** the first two compare a **URL scheme**, which is a different concept from a transport label, and `Transport::Http.name()` returning `"http"` does not make them the same concept. Only `tls.rs:711` is unambiguously a transport label. | **One sentence settles it: is a URL scheme the same vocabulary as a transport label?** If yes, all three repoint. If no, only `tls.rs:711` does and the other two are correct as written. | seed · branch sweep |

---

# 4. INSTRUMENT-BLIND — checks that cannot say NO

| ID | TITLE | CLASS | CERTAINTY | EVIDENCE | ACTION | SOURCE |
|---|---|---|---|---|---|---|
| **X-214** | **`ports-only` and `ports-only-tests` are EIGHT ceilings of zero over a needle that matches nothing in the whole tree — and one of them is listed as a STANDING RED, which the gate's own rule says must then score.** | instrument-blind | **VERIFIED** | `[proven here]` `qa/construction.toml:340-359`: `[rules.ports-only].needle = "busbar_core::"`, ceilings `busbar-llm/mcp/a2a/voice = 0`, duplicated for `ports-only-tests`. Then: `grep -rn 'busbar_core::' --include='*.rs' crates/ \| wc -l` → **1**, and that one is a COMMENT at `crates/busbar-kernel/src/lib.rs:26`. Zero hits in any of the four plane crates. **Zero is the passing answer to every ban.** And `xtask/src/gates/mod.rs:227-236` lists `ports-only-tests:busbar-llm` among the **5** `CONSTRUCTION_STANDING_REDS` — while `mod.rs:217` states *"a name on this list that is no longer red is STALE and also scores."* A rule that cannot fail is on the list of rules held red on purpose. | Repoint the needle to the retiring namespace, or strike all eight rows AND the standing-red entry in one commit. **Owner: what IS the retiring namespace now?** | ratchet-census §1.3 · MAP X-140 · this pass |
| **X-129 · X-130** | **`plane-purity:backwards` is VACUOUS and so are five siblings** — they grep for the crate name `busbar_core`, which ceased to exist. **Proven by plant:** a real backwards reach spelled `busbar_kernel::profile::Stage` left both `plane-purity` and `plane-purity-strict` fully green, while the dead-name equivalent correctly went red. | instrument-blind | **VERIFIED** | `[carried]` Also covers `plane-purity-strict`'s `category:BACKWARDS` and all four `test-reach:*` rows. `[proven here]` the needle's emptiness is the same measurement as **X-214**: 1 hit tree-wide, a comment. Separately `plane-purity --selftest` **exits 1 on HEAD**, deterministic at `--jobs 1`: the `scan-denominator` message was reworded to *"…yielded ZERO files"* while the fixture still asserts on `"neutral_files=0"`. plane-purity has **no `REPORT_ONLY` posture** to excuse it. | Same repoint as X-214. **The loud failure (X-130) is the tree telling you about the quiet one (X-129)** — fix in that order. | MAP X-129/X-130 |
| **X-150 · X-151** | **`hot-path-perf` and `hot-path-alloc` are pure theatre, proven three ways each.** | instrument-blind | **VERIFIED** | `[carried]` (1) Comment out BOTH real assertions → still 5/5 PASS. (2) Raise the budget **1,000,000×** (1µs → 1s) → still PASS, **row label still reads "under 1µs"**. (3) **Nothing in CI or any script ever runs the bench at all** — its doc names the deleted crate `busbar-core`. It greps source text for literal substrings, comments included. For `hot-path-alloc`: replace the entire **170-line allocator instrument with a 4-line comment stub** → still 3/3 PASS, including the row asserting *"the instrument installs a counting `#[global_allocator]`."* **Cross-reference X-201**: `crates/busbar-kernel-ledger/tests/hot_path_allocation.rs` is being deleted from the working tree right now. | Owner policy call covers this whole class — see the ACTION on **X-215**. | MAP X-150/X-151 |
| **X-138** | **The gate built to catch capabilities that are never constructed is itself never constructed.** | instrument-blind | **VERIFIED** | `[carried]` `qa/unconstructed.toml` is committed, its gate is registered, and the gate never runs. The repo's own self-test says it in terms: `full-gate --selftest` → `[FAILED] registered gate(s) absent from ci.yml: ["unconstructed"] — a gate nothing runs is a gate that cannot fail`. Absent from `ci.yml`, `qa/segments.toml`, `qa/full-gate.toml`, **and** from the 10-entry `REGISTRY_NOT_IN_CI` excuse list. | Add it to `ci.yml`. It is the gate that owns **M52**'s six money rows. | MAP X-138 |
| **X-139** | **`qa/design-bindings-gaps.json` CLAIMS to be enforced and is not** — a landmine for the first real `[[gap]]` entry, which would be silently unchecked. | instrument-blind | **VERIFIED** | `[carried]` Its own text: *"Checked by the design-bindings gate… each have a case in `cargo xtask gate design-bindings --selftest`."* Repo-wide grep for the filename returns **exactly 2 hits, neither a parser**. Vacuously green only because nothing produces a `gap` verdict yet. **`1.6.0-ratchet-census.md:449` compounds it** by marking the file *"gate detects staleness? yes / GREEN."* — a source disagreement, carried in §8. | Write the parser or delete the file. | MAP X-139 |
| **X-152** | **`construction --posture` exits 1 on HEAD right now, and `ci.yml:3481` runs exactly that with `PIPESTATUS[0]`. This is gating CI today.** 3 of 5 `CONSTRUCTION_STANDING_REDS` are stale. | instrument-blind | **VERIFIED** | `[carried]` `[proven here]` the list is exactly 5 entries (`xtask/src/gates/mod.rs:227-236`): `hold-discipline:cancellation-before-await`, `one-pick-site`, `ports-only-tests:busbar-llm`, `request-path-fn-size`, `terminal-doors-in-audit-step`. **`ports-only-tests:busbar-llm` cannot be red at all — see X-214.** | Strike the stale names. The gate's own doc says draining a row means striking its name **in the same commit**. | MAP X-152 · this pass |
| **X-153** | **`ship-ready`'s excuse can never fire.** No entry named `"ship-ready"` exists in `REGISTRY_NOT_IN_CI`; the `?` on the missing lookup means `--all` **unconditionally counts it as a dev-line regression**, contradicting its own posture. Its four siblings all have entries. | instrument-blind | **VERIFIED** | `[carried]` | Add the entry. | MAP X-153 |
| **X-154** | **Three `construction` selftests are unfalsifiable — plant, and they stay GREEN.** `token-sealed` (fixture vocabulary `UnitToken::mint(` has **0 occurrences**; the real shapes are `Pass::mint(`/`Grant::<…>::mint(` at 99/39/40/20/48 hits), `forbid-unsafe-deny` (12 rows), `legacy-reach` (2 of 3 prefixes name deleted crates). | instrument-blind · auth/capability | **VERIFIED** | `[carried]` `[proven here]` corroborated from the other end by **X-210**: `IdempotencyKey::mint(` appears 3× and every one is a test or the selftest's own fixture. | Read X-154, X-177 and X-210 as ONE subject: the sealed-constructor gates scan a vocabulary the tree does not use. | MAP X-154 |
| **X-155** | **`crates/busbar-kernel-scope` is covered by NO GATE SCOPE AT ALL** — checked against every `qa/*.toml`, every `qa/*.json`, every hardcoded Rust scope. Zero per-push architectural rule watches its content, **and it is under active development this session.** | instrument-blind | **VERIFIED** | `[carried]` It appears only in an auto-derived dependency census and the human-review register. `[proven here]` the git status at this pass's open shows `crates/busbar-kernel-scope/src/tests/mod.rs` modified and `crates/busbar-kernel-scope/src/tests/bitset.rs` untracked — the crate is being written to while nothing watches it. | Enrol it. | MAP X-155 |
| **X-156** | **`teller-steps` floors cells but never checks the plane roster.** Deleting an entire plane's cells still scores 5/5 green — and **`busbar-plane-streaming`, a real installed plane, has zero Teller cells today** with nothing flagging it. | instrument-blind | **VERIFIED** | `[carried]` | Add a roster check. | MAP X-156 |
| **X-157** | **`plane-transport-neutrality` has a live exploitable hole, A/B proven:** `rtp_port: u16` → RED, **`output_audio_rtp_port: u16` → GREEN**. Same banned noun, one spelling caught. | instrument-blind | **VERIFIED** | `[carried]` | Match the noun, not the identifier. | MAP X-157 |
| **X-158** | **`config-schema`: 1 of 57 selftest cases is RED on HEAD, its fixture naming the deleted crate `busbar-core-config` — and `ci.yml:1294` gates on that selftest.** Its `additive-only` rule also diffs HEAD-vs-HEAD on `push`, **and this repo promotes by push.** | instrument-blind · config | **VERIFIED** | `[carried]` Compounded by done-readout Finding 5: the selftest is RED while the gate it guards is GREEN, on the same tree in the same second. | **Do this before shipping** — the readout calls it the one open question that could be a config break. | MAP X-158 · done-readout F5 |
| **X-180** | **`config-schema` has one genuine broken case**: *"a grammar type that MOVED between two core-kind roots is NOT a break"* scores as a break, naming `OverlayDoc` and `RootSettings` as removed from a grammar frozen at 1.5.3. 55 of 57 cases behave. | config · instrument-blind | **ADJUDICATE** | `[carried]` Not vacuous — one real relocation bug. **Whether the removal is a plant artifact or live in-flight config work is uncertain and wants the owner's eyes** (the source says so itself). | Owner reads it. | MAP X-180 |
| **X-169** | **`oracle replay` exits 0 while reporting 8 UNACCEPTED DIVERGENCES, and prints no summary line. PARITY scores GREEN on a criterion it fails.** | instrument-blind | **VERIFIED** | `[carried]` | Make the exit code carry the verdict. Read with **M50**. | MAP X-169 |
| **X-171** | **The oracle green rests on a waiver and a hole.** `diverging.txt` carries **297 rows — 289 forgiven** by `accepted-differences.json`. And **1,405 of 2,318 cells (61%) have no golden recording at all.** | instrument-blind | **VERIFIED** | `[carried]` Correctly reported `UNBASELINED` and never counted as PASS — the report says so itself: *"a green run says nothing about them."* **Nothing was blessed; the 289 and the 1,405 ARE the finding.** | Owner: 61% unbaselined is the coverage number the release should quote, not the pass rate. | MAP X-171 · oracle-coverage |
| **X-174** | **One dedup defect, two copies.** `capability-equality-summary.py:264` dedupes `wanted` but counts `per_leg` raw — header says 29, breakdown sums to 35; **6 ledger cells cite a test another cell already cites**, and the floor compares against the deduped number so it never notices. **`xtask/src/gates/teller_steps.rs::run_root_legs` carries the identical bug** — header 47, breakdown 50. **Also: `root-admin` names zero cells and prints no line at all** — a leg that proves nothing is invisible rather than zero. | instrument-blind | **VERIFIED** | `[carried]` The script's own docstring warns *"a change to the refusals here owes the same change there."* | Fix both copies. Make a zero-cell leg print zero. | MAP X-174 |
| **X-175** | **`plane-purity-strict` is green against a budget of 940, not against zero.** The group is titled *"neutral crates carry no side channel (0/0)"* and *"0 backwards"* — **BACKWARDS tolerates 33**; SYMBOL 61, TYPE 13, KEY 377, DIALECT 456. | instrument-blind | **VERIFIED** | `[carried]` Corroborated by the ratchet census: **508 of `plane-purity-strict.toml`'s 968 lines are headroom**, and the census's own verdict on the gate is *"green — and that green is the finding."* | Retitle the rows to their real budgets, or drive the budgets to zero. A label that says 0/0 over a budget of 940 is a false statement in a gate's output. | MAP X-175 · ratchet-census Part 2 |
| **X-179** | **`plane-abi-neutrality` is GREEN on the tree and RED in its own harness, deterministically.** Two `Expect::Green` controls fail with *"a plane key this tree declares is not in the ban list: **busbar**"* — the discovery step derives a degenerate key, consistent with stripping a `busbar-` prefix and picking up the root crate. | instrument-blind | **VERIFIED** | `[carried]` | Fix the key derivation. | MAP X-179 |
| **X-144** | **`no-float-money`'s selftest is degraded, and was before this work.** Every case uses whole-verdict `prove_red`/`prove_green`, and the gate is RED on HEAD — so its **17 red-expecting cases now pass trivially.** *A `prove_red` against an already-red gate proves nothing.* | instrument-blind · money | **VERIFIED** | `[carried]` The agent who found it declined to narrow the 23 cases rather than make it LOOK green — recorded because that was the right call. | Narrow all 23 cases to their covered rows. Read with **X-60**. | MAP X-144 |
| **X-145** | **13 gates read by named path rather than by walk, so they have NO WALK DENOMINATOR at all** — outside the empty-scope rule by construction. | instrument-blind | **VERIFIED** | `[carried]` Sibling of **X-141** (55 of 88 walks could not tell an empty scope from a clean tree — fixed globally in `Ctx::list`) and **X-142** (`release-order` R0–R15 were sixteen false passes; a scope spelled `yaml` matched nothing and read as clean). | Give the 13 a denominator. | MAP X-145 |
| **X-215** | **The instrument audit's top-16 ranking is a single policy question, not sixteen tasks** — and four of its rows are already FIXED, which is what makes the rest legible. | instrument-blind | **ADJUDICATE** | `[carried]` The four repaired mid-audit (`§9.1` PARITY `replay-selftest` reading a missing golden as an empty PASS set via `unwrap_or_default()`; `§9.2` `full_gate::Excuse::holds` doing a raw `text.contains(needle)` over UN-STRIPPED source while its own sibling strips comments; `§5` `no-default-bodies` finding 0 of 10 configured kind traits for `Transport`; `§9.3` `ship_ready::posture()` taking the PASS arm from a run that could not read its own branch). The twelve that remain are all the same shape: **a missing artifact, an empty scope or a dead name reads as success.** | **ONE OWNER RULING COVERS THE CLASS:** *does an instrument that cannot be shown failing count as evidence?* If no, every row here gets a matched-pair plant as an acceptance criterion and the twelve become mechanical. The instrument audit already wrote the rule (§8). | instrument-audit §1, §7, §9 |
| **X-216** | **`audit-ledger`: 146 of 170 scopes live; 24 name paths that do not exist — and 6 HIGH + 23 MEDIUM open findings sit on DELETED crates, red over nothing.** The register is one-directional: `missing-scopes` catches a scope the tree implies, nothing catches a scope the tree no longer has. | instrument-blind | **ADJUDICATE** | `[carried]` instrument-audit row 6. Also done-readout Finding 10: three scopes stale with HIGH findings still open; 17 HIGH + 17 MEDIUM to close; one scope missing for `crates/busbar-llm-codec/tests`. **ADJUDICATE because the two sources disagree on the counts (24 vs 3 stale; 6+23 vs 17+17) and neither was re-derived here.** | Re-derive the numbers once, then act. Some of the 24 are DELETE-LIST rows — but see the warning in the delete list: **one struck scope held three source-scanning confinement guards absent from the tree.** A delete can uncover a finding. | instrument-audit · done-readout F10 |
| **X-217** | **`qa/full-gate.toml`'s skip register checks that each skip carries a REASON, never that its SUBJECT exists** — and one surviving reason cites a proof file that was deleted. | instrument-blind | **VERIFIED** | `[carried]` instrument-audit row 7. **`[proven here]` the six dead skips the ratchet census names are ALREADY GONE from trunk** — see §8 D-4. `qa/full-gate.toml` now carries all 10 live skips and a comment block at `:17-49` recording the repair. **The RULE-SHAPE defect survives the repair**: the selftest still only checks for a reason. | Make the selftest resolve each skip's subject. The data was cleaned; the instrument that let it rot was not. | instrument-audit · ratchet-census Part 6 · this pass |
| **X-218** | **`plane-delete-test.sh` leg 1b covers 3 of 4 planes because `if [ -d crates/busbar-plane-$p ]` has NO `else`** — `crates/busbar-plane-voice` does not exist, so voice is silently skipped. No selftest fixture, unlike every other leg in the file. | instrument-blind | **VERIFIED** | `[carried]` instrument-audit row 11. Compounded by done-readout Finding 8: PLANE-DELETE cannot reach the fifth locked plane; `scripts/plane-keys.sh` `PLANE_KEYS` vs `PLANE_KEYS_LOCKED` disagree and the `decisions` plane has no on-disk crate. | Add the `else`. A missing directory must be a refusal, not a skip. | instrument-audit · done-readout F8 |
| **X-219** | **`structure_lint/oversized.rs:93-99` treats a walk failure as a PASS while its sibling `hybrid.rs:31-39` treats the identical failure as a hard finding. One of the two is wrong.** | instrument-blind | **VERIFIED** | `[carried]` `let Ok(files) = cx.walk(..) else { return; };` — a walk failure pushes no finding and the row reports PASS. | Make them agree. `hybrid.rs` is the correct one. | instrument-audit row 14 |
| **X-220** | **`money-invariants` prints the ROSTER length, not the found count — so a renamed money record silently drops the population to 0 and the row still says "8 … scanned".** | instrument-blind · money | **ADJUDICATE** | `[carried]` instrument-audit row 5: PROVEN YES in-reach / PROVEN NO out-of-reach, 8 of 8 record types found today. No census row. **ADJUDICATE not PARK**: the defect is in the instrument's reporting, not in a billed byte — but it guards the money invariants, so a wrong ruling here is a money-path blind spot. | Print the found count. Add a census row. One line, high leverage. | instrument-audit row 5 |
| **X-221** | **DoD clause 2 — *"ship-ready green, not merely `--posture`-tolerated"* — is enforced by NOTHING on the DONE path, triple-disarmed.** | instrument-blind | **VERIFIED** | `[carried]` Not invoked by name by the DONE oracle; present in `full-gate` but `Excused::ReleaseTime`; and its `standing-reds` row auto-passes off `qa`/`main`. Three independent disarms of one clause. | Owner: the definition of done contains a clause nothing checks. | instrument-audit §4 |
| **X-222** | **BUILD silently downgrades to 3 `cargo build`s when `qa/full-gate.toml` is missing — and still prints the unqualified DONE banner.** This is the exact downgrade `--fast` was demoted to `PROVISIONAL`/exit 3 for. | instrument-blind | **VERIFIED** | `[carried]` `scripts/verify-1.6.0-done.sh:435-439`. | Make the missing file a refusal. | instrument-audit row 8 |
| **X-223** | **`assert_bless_env_empty` covers 11 vars and misses two repoints of the JUDGE ITSELF** — `BUSBAR_ORACLE_RUST_BIN` and `BUSBAR_RELEASE_CHECKOUT`. Strictly stronger than `BUSBAR_ORACLE_TOOL_DIR`, which IS asserted. | instrument-blind | **VERIFIED** | `[carried]` `bin/oracle:50,57`. The selftest plants all 11 it knows about, so the mechanism works — the roster is short. | Add the two. | instrument-audit row 10 |
| **X-224** | **`release-order` R3–R12 report *"no violation"* over a scan that never happened** — `release_order.rs:829` returns before they run, and the file documents this at `:1554-1568`. Covered today only because R0 fails independently. | instrument-blind | **VERIFIED** | `[carried]` Latent. The moment R0 goes green, ten rows start lying. | Fix the early return. | instrument-audit row 13 |

---

# 5. ABI · CONFIG · CUSTOMER-SURFACE

| ID | TITLE | CLASS | CERTAINTY | EVIDENCE | ACTION | SOURCE |
|---|---|---|---|---|---|---|
| **M25** | **The plugin ABI ships `unit_cost_micros`, documented *"core prices NOTHING"* — which inverts Law 6 and #43.** A plugin-supplied cost field on the ABI is the opposite of the rule that pricing lives in one place under core. | abi · money | **PARK** | `[carried]` The field and its own doc comment. Read with **M52**: the ABI minor was bumped 19→20 for a keyed-units capability with **no producer and no host consumer**, so every dlopen plugin still bills via the frozen `amount × unit_cost_micros` path. | Owner: remove the field, or amend Law 6. | LEDGER M25 |
| **X-117** | **The gates DISAGREE about how many planes exist, and `reachability.rs` carries BOTH ROSTERS** — #48's five at `:43`, the retired four-plus-voice at `:173`. | config · instrument-blind | **VERIFIED** | `[carried]` `plane_purity/scanner.rs:29`, `plane_pricing_blindness.rs:94` and `kind_isolation.rs:373,425` all carry the four. `qa/teller-steps.json` adds `admin`, which #5 says is not a plane. | Derive the roster once, from one place. **This single fix touches X-148, X-156, X-166 and X-218.** | MAP X-117/X-148 |
| **X-148** | **`PLANE_ALTERNATION` is a stale roster in live gate code:** `["llm","mcp","a2a","voice"]`. No `busbar-plane-voice` crate exists, and it omits `busbar-plane-streaming` and `busbar-plane-decision` — both real. **Plane-purity is not looking at two of the five planes.** | instrument-blind | **VERIFIED** | `[carried]` `xtask/src/gates/plane_purity/scanner.rs:29`. Same class as X-87. | Subsumed by **X-117**'s single-roster fix. | MAP X-148 |
| **X-63** | **The reachability gate REWARDS THE LEAST-WIRED PLANE.** When a roster plane's `root/units_*.rs` is simply ABSENT, both `unit-path` and `root-reach` score `Ok(...)` — a clean PASS. `units_decision.rs` does not exist, so **decision passes 2 of 3 axes for free**, scoring strictly better than mcp, a2a and streaming whose modules exist but are unreached. | instrument-blind | **VERIFIED** | `[carried]` `reachability.rs:1109,1148`. **None of the gate's 25 selftest cases exercises it** — the auditor did not need to plant anything, the tree already contained a live instance. | A missing file must be a refusal. Same shape as **X-218**. | MAP X-63 |
| **DR7** | **Three unreached unit paths and one unreached root module — and `money_book` is on the money path and is reached by nothing.** | config · money | **PARK** | `[carried]` `reachability:unit-path:{mcp,a2a,streaming}` and `reachability:root-module`. `units_mcp.rs` declares no `impl … Units for`; `A2aUnits` and `VoiceUnit` are constructed only by their own constructors and tests. **`qa/reachability.toml` records that three money bugs — wrong-denomination accrual, hardcoded `rate_card_version: 0`, colliding `UnitKey::new(0)` — shipped INTO the unreached a2a module precisely because nothing runs it.** | Switch each onto the serving path or write a `[[dormant]]` row. The money-book half is why this is not tidiness. | LEDGER DR7 · MAP |
| **X-66** | **#18 violated in the SERVING path**: `register_planes()` installs `&busbar_voice::PLANE_DECL` — **the banned name**. Two crates back two facets of one plane in one composition root, **and customers are served by the legacy one.** | customer-surface | **VERIFIED** | `[carried]` `main.rs` `register_planes` vs `root/registry.rs`. `busbar_plane_streaming` appears only in `plane_claims()` (the OpenAPI/claims surface) and in the never-bound `root-voice` scaffold. | Cut over. Read with **X-70/X-71**. | MAP X-66 |
| **X-170** | **`ledger\|amend\|*` changed 404 → 403 from published 1.5.5 — a user-visible change on a LEDGER path**, which #12 forbids absent an owner signature. Two cells. | customer-surface · money | **PARK** | `[carried]` | Owner signs it or it reverts. | MAP X-170 |
| **X-122** | **`CHANGELOG.md:23` — customer-facing — says "the live-voice plane."** It correctly calls OpenAI Realtime and Gemini Live *dialects*, then hangs them off a plane #18 says must not be named that. `README.md:365` says the model *"spans three planes"*; there are five. | customer-surface · doc-drift | **VERIFIED** | `[carried]` | Edit both. Customer-facing text is the one place the rename must be complete. | MAP X-122 |
| **X-52** | **Compiled-in and dropped-in reach DIFFERENT metrics recorders** — breaks #11's compiled-in == dropped-in equivalence for export counters. | abi | **VERIFIED** | `[carried]` commit `1059d3c36`. | One recorder. | MAP X-52 |
| **X-56** | **No export plugin has ever been published; the oracle corpus holds no load cell, so #85's widened-window claim would ship ON ASSERTION.** | abi | **VERIFIED** | `[carried]` commit `03791e495`. | Publish one and record a cell, or withdraw the claim. | MAP X-56 |
| **X-102** | **`ARCHITECTURE.md:1175` lists `admin` as a plane — and it is NOT only a doc error:** `busbar-core-admin/src/admin_codec/meta.rs:68` really does `impl PlaneMeta for AdminPlane`, against #5. | config · doc-drift | **VERIFIED** | `[carried]` And it omits `decisions` entirely. Read with **R74**: pulling the registration quietly retires the second-plane BOOT REFUSAL guard, which is a behaviour change and a separate decision. | **Owner (R74): may `busbar-core-admin` keep its `PlaneMeta`/`Plugin` declarations?** | MAP X-102 · LEDGER R74 |

---

# 6. DOC-DRIFT — documents that instruct against the tree

These are ranked below code defects but **two of them actively tell an agent to revert shipped money
behaviour**, which is a higher consequence than their class suggests.

| ID | TITLE | CLASS | CERTAINTY | EVIDENCE | ACTION | SOURCE |
|---|---|---|---|---|---|---|
| **X-100 · X-147** | **TWO documents instruct an agent to revert LANDED money behaviour.** | doc-drift · money | **PARK** | `[carried]` `rate-card-epochs.md:264-292` recommends the exact behaviour #79 names as the defect — repricing history flat off the current card — presented as parity-required and final; superseded the same day and never deleted. `SEAMS.md` §3.4.4: *"Do not touch `derive_spend_micros_row` … switching `/usage` to the ledger is G3+C13 work, not a cleanup"* — while `ARCHITECTURE.md` PB-103 records that fix as **landed**. | **Delete both passages.** #58 says delete-don't-supersede, and this is the damage that rule exists to prevent, proven twice on the money path. | MAP X-100/X-147 |
| **X-99** | **`billing-unified.md:172-225` puts money math ON THE HOT REQUEST PATH and persists computed prices** — contradicting #71 (*"no rate lookup/multiply/currency math runs on the request path"*) and #77(3) (*"price is NEVER stored"*). | doc-drift · money | **PARK** | `[carried]` Note it describes **M20**'s defect as the design. | Delete or mark superseded with the decision that supersedes it. | MAP X-99 |
| **X-120** | **Six deleted scripts are cited as LIVE INSTRUCTIONS — including one named inside a live DATA FILE.** `testing/shadow-oracle/accepted-differences.json:12`: *"scripts/changelog-register-check.sh is the gate."* It does not exist. | doc-drift | **VERIFIED** | `[carried]` Also `plane-purity-lint.sh`, `plane-abi-neutrality.sh`, `release-order-lint.py` (one doc says it *now fails CI*, another says it *passes*), `full-gate.sh`, `enumerate-cells.py`. Each has a real xtask replacement. `[proven here]` `scripts/release-order-lint.py` is absent. | Repoint the six. **The `accepted-differences.json` one is the urgent one** — it is a live data file, and nothing in this list may bless or edit that file, so it needs the owner. | MAP X-120 |
| **X-103 · X-94** | **~12 documents cite crates deleted by #19/#36/#37 with no staleness marker.** | doc-drift | **VERIFIED** | `[carried]` `D33-legacy-retirement`, `contract-notes`, `foundation-abi-config`, `feature-sets`, `llm-test-fixture-seam`, `rate-card-history`, `plane-decl-registry`, `CODE-ANALYSIS`, `1.6.0-contract-gaps` (~60 gap rows), `composition-root-plan`, `plane4-seam-audit-B-session.md`, `1.6.0-waist-rebuild-plan.md`, `1.5.3-config-consolidation.md:44`. `[proven here]` `crates/busbar-core`, `busbar-substrate`, `busbar-caps`, `busbar-admin`, `busbar-grammar`, `busbar-plane-admin` all absent. | **#58 says delete, don't supersede.** Most are DELETE-LIST rows — but each needs its blast radius checked first, because `no_deferral.rs:60` reads `1.6.0-TRACKER.md` and fails the gate if it is absent. | MAP X-103/X-94 |
| **X-97** | **A live cost hazard the spec says is gone.** `docs/ci/self-hosted-runners.md` describes an EC2 fleet at **~$9.36/hr**, and all 8 `scripts/ci-runners-*.sh` are present and executable, including `ci-runners-up.sh`. #56 and Part 6 say the fleet is retired — *"zero instances, zero volumes, zero NAT gateways, zero elastic IPs."* **One command stands up infrastructure the owner tore down, and nothing warns a reader.** | doc-drift | **VERIFIED** | `[carried]` Read with **M46–M49** and **X-57** (the CI-runner reaper was never scheduled — it ran only when invoked by hand, which is how a 52-minute / ~63-core-hour runaway survived a 20-minute kill rule). | Delete the scripts or warn in the doc. This is a spend control, not documentation. | MAP X-97/X-57 |
| **X-98** | **Four more instances of the #67 branch-model seam — including `SECURITY.md:69`, which is PUBLIC and linked from GitHub's Security tab.** | doc-drift | **VERIFIED** | `[carried]` Also `RELEASE.md` (the runbook a human executes), `release-path-i5.md`, and `CI-HEALTH-2026-09-04.md:88` which TRACKER row I3 still marks `[x]` current. All say `dev`→`qa`→`main` and hand-push. | Read with **X-91/X-92** below — the decision and the tooling disagree, and the docs side with the tooling. | MAP X-98 |
| **X-91 · X-92** | **The release engine CONTRADICTS the decision that governs it.** #67 (OWNER-LOCKED 2026-09-20): *"`predev` is the permanent WIP branch; `dev` is release-train-write-only… the release train is the ONLY writer of `dev`."* | doc-drift · config | **VERIFIED** | `[carried]` `scripts/pr-land.sh:52` hardcodes `base="dev"` and arms auto-merge — every landing puts WIP directly on `dev`. `scripts/promote.sh:109`'s allowed pairs are exactly `"dev qa"` and `"qa main"`; there is **no `predev dev` case** and anything else exits 1. The roster the spec LOCKS is unreachable by the tool that moves branches. And #67's own enforcement column asserts a branch protection that would have to reject the tooling's own default. **Three halves, no two agreeing.** | **Owner: does #67 stand?** One answer closes X-91, X-92, X-98 and four docs. | MAP X-91/X-92 |
| **X-93** | **`voice-cross-dialect-mapping.md:3` calls it "the busbar voice plane"** — #18's load-bearing distinction is that voice is a DIALECT inside streaming, never a plane. | doc-drift | **VERIFIED** | `[carried]` No `busbar-plane-voice` crate exists either. | Edit. | MAP X-93 |
| **X-101 · X-115** | **A live money gate enforces DECISION #87, which does not exist.** `plane_pricing_blindness.rs:3` — *"THE RULING THIS ENFORCES. DECISION #87 (owner-locked 2026-09-22)"* — plus `gates/mod.rs:146,2254`, `full_gate.rs:472,479`, its test, and four docs. **The table ends at 85.** | doc-drift · money | **PARK** | `[carried]` The ruling is REAL but **unnumbered** — it lives in #43 and Part 7 §13(3). Cited in **7 source files, 5 docs, 2 `qa/*.toml`** (X-101 merged into X-115 at lower resolution and with a wrong count). **An agent told to cite a row and never re-litigate looks up #87, finds nothing, and either deletes a live RED-provable money gate as unfounded or reopens a settled money ruling.** | **Owner: number the ruling.** One line in the spec closes 14 citations. | MAP X-115/X-101 |
| **X-104** | **ADR-0005 documents a defect that is FIXED.** It says unmodelled IR keys drop silently; `busbar-llm-codec/src/chat_handle.rs:284-298` logs every dropped key with a diagnostic banner and the comment *"the DEFECT was the silence, not the drop."* | doc-drift | **VERIFIED** | `[carried]` An engineer could cite the ADR to excuse a gap that no longer exists. | Mark the ADR superseded. DELETE-LIST candidate. | MAP X-104 |
| **X-119** | **The fold arithmetic contradicts itself and its base is stale — and the wrong number is quoted BY INSTRUCTION.** `:1188` says 57 → 34, *"twenty-two still fold"*; `:3551` says **23**. 57−34 = 23 — and **57 is stale, the tree has 49**, so 15 remain. `:1885` instructs *"do not re-derive these; cite the ledger."* | doc-drift | **VERIFIED** | `[proven here]` 49 live workspace packages confirmed by parsing every `crates/*/Cargo.toml` `name =` field. | Re-derive and strike the instruction not to re-derive. | MAP X-119 |
| **X-8 · X-9 · X-10** | **Three items marked DONE `[x]` in `1.6.0-TRACKER.md` that name real, open, un-owned work.** | doc-drift | **VERIFIED** | `[carried]` **A10** ticked `[x]` — 9 named boot-mutation oracle rows still need "a follow-up script". **F6** ticked `[x]` — PB-71's 56-claim README/CHANGELOG audit, self-described as *"real future work, named not papered over."* **`## P. Post-1.6.0` → P1**, plane/transport plugin LOADING, a 2026-09-06 decision predating the ban and never revisited against #39. No ledger row for any of the three. **Marked done in one half, undone in the other — the same broken pair as everything else.** | **`1.6.0-TRACKER.md` CANNOT BE DELETED** — `no_deferral.rs:60,310,346` reads it and fails the gate if it is absent, and every waiver's `expiry` must resolve to a row in it. Carry X-8/X-9/X-10 out of it; leave the file. **Owner ruling owed on P1: does it survive #39?** | MAP X-8/X-9/X-10 |
| **X-53** | **`qa/denylist-allow.toml` concedes one sentence SIX TIMES, four of them load-bearing** — the only reason `denylist` reports 2 hits and not the real, larger count. | doc-drift · instrument-blind | **VERIFIED** | `[carried]` commit `c6f69b6d4`. | Collapse to one row. Note the ruling that landed during the gate sweep did exactly this for the `sha2 → cpufeatures → libc` edge — six per-crate copies collapsed into one `edge_exemptions` row — so the pattern is proven. **Nothing here edits `qa/*.toml`; this is the owner's edit.** | MAP X-53 · gate-sweep §1.5 |

---

# 7. MAP INTEGRITY — is the list of lists trustworthy?

| ID | TITLE | CLASS | CERTAINTY | EVIDENCE | ACTION | SOURCE |
|---|---|---|---|---|---|---|
| **X-225** | **S2a's COUNT OUT reconciles 21/21/Δ=0 against a file that DOES NOT EXIST AT HEAD.** | doc-drift | **VERIFIED** | `[proven here]` `git cat-file -e HEAD:docs/design/1.6.0-owner-decisions.md` → *fatal: path does not exist in 'HEAD'*; `ls docs/design/1.6.0-owner-decisions.md` → absent from the working tree too. The map-proof's S2a row counts `grep -cE '^## [0-9]+\. '` over that file at the PIN and calls it RECONCILED. **The arithmetic is correct and the subject is gone**, which is precisely the independent auditor's "hollow" note, now confirmed from a second direction. | The 21 owner decisions need a home that exists. Until then S2a's ✅ means nothing about the tree. | map-audit App. A · this pass |
| **X-226** | **Three of the map-proof's nine ✅ sources reconcile by PARTITIONING A SET AGAINST ITSELF** — S1, S2a, S2b — so the ✅ is arithmetically earned far more often than it is evidentially earned. | doc-drift | **ADJUDICATE** | `[carried]` map-audit's own verdict: *"Qualified yes on arithmetic, no as a statement about the tree."* 7 CONFIRMED · 2 DISAGREE · 0 UNVERIFIABLE. **ADJUDICATE because "partitions against itself" is a judgement about method, not a measurement** — the independent auditor made it, and it wants the owner's agreement before the ✅ column is restated. | **Owner: is a source that reconciles against itself allowed to carry a ✅?** If no, three ✅s become ⚠ and the map's headline completeness number moves. | map-audit |
| **X-227** | **S4's COUNT IN is 2,442, not 2,444 — the ✅ is carried by two rows that do not exist.** | doc-drift | **VERIFIED** | `[carried]` Two independent re-derivations agree and each shows its arithmetic: map-audit D1 (`tomllib` over `git show $PIN:qa/construction.toml`; part **B = 129 (26 arrays)**, not 131 (27)) and ratchet-census §R.1 (the 2 are the map-proof's own double-count of a dict-held 2-entry list). The map-proof does not rebut either. | Correct the figure. **Then note X-86**: a fourth extraction bug on the same shelf — `[rules.no-test-doubles-in-production.known_sites]` is a TOML **table of named sub-tables**, `isinstance(v, list)` is False, and **every prior extraction silently skipped all 14 entries**, five of them `verdict = "double"` including **`Pricer::flat(`**. | map-audit D1 · ratchet-census R.1 · MAP X-86 |
| **X-228** | **S8's Δ = 0 is true only in an UNCOMMITTED WORKING TREE. At the PIN and at HEAD, Δ = 4.** | doc-drift | **VERIFIED** | `[carried]` map-audit D2: `git show HEAD:scripts/no-deferral.waivers \| grep -vE '^\s*#' \| grep -cvE '^\s*$'` → **52**, against a claimed 56. A reconciliation that only holds against un-landed work is not a reconciliation. | Land the waiver file or restate Δ. **See §8 D-1: the gate this measures was materially changed at HEAD, so re-derive before acting.** | map-audit D2 |
| **X-229** | **The map-proof's own verdict is ⛔ INCOMPLETE, and the release has been quoting its ✅ column.** | doc-drift | **VERIFIED** | `[carried]` map-proof §12. Compounded by the S2d/S2e/S2f finding: **landing a document did not turn its findings into master-list rows.** S2e (instrument-audit) is the worst — *"a 937-line, internally self-reconciled instrument audit exists and the master worklist does not know it exists"*, 8 of 10 items absent, 80%. | **THIS FILE IS THE REMEDY.** Every S2d/S2e/S2f item is now a row above. | map-proof §12 · MAP sweep 6 |
| **X-230** | **`1.6.0-MAP.md` was itself carrying two dead rows within an hour of being written**, and the sweep that caught it refuted its own positive control. | doc-drift | **VERIFIED** | `[carried]` `VERB_COUNT` is fixed at HEAD (the assert derives from live `.len()`s, all 92 verbs real, ledger/audit verbs route to real implementations at `units_admin/mod.rs:985,1000`) and the spec says **85** decisions at lines 9, 13, 20 and 305 with zero hits for "78". Both were listed as live defects. **The map whose job is to be the complete accurate list carried two dead rows.** | Recorded as the standing hazard this file inherits. **Every row above has a re-derivation command precisely because of this.** | MAP sweep 13 |
| **X-231** | **S3 commit residue is UNRECONCILABLE BY ANY COMMAND, and that is now proven rather than asserted.** | doc-drift | **VERIFIED** | `[carried]` All five class labels return **zero literal hits** across all 307 bodies — the per-class tally was a human read, not a command's output. A keyword classifier catches about a third, and **two falsification plants both slipped through**, one of them a real corpus sentence (*"voice still hardcodes `UpstreamIdx(0)`… bills every turn on the wrong provider's lane"* — which is **X-50**, unambiguous owed money work that trips no regex). **Residue is a semantic category, not a lexical one. No regex will ever enumerate it.** Also: `--since` with no upper bound returns 415 today, not 307 — an open-ended range is not a pin. | **Stop trying to enumerate it.** Sample and read. The checkable sample was 59 → 40 OUT, 1 partial, 1 leans-out, 5 mistagged (an OVER-count), 12 new findings. | MAP sweep 3 |

---

# 8. DISAGREEMENTS PRESERVED

Where two sweeps disagree about one subject, that is a row of its own with both verdicts. **Both
directions are findings** — one sweep found 145 of 169 verdicts agreeing with a prior audit and 24
differing; four of those were the agent wrong and one was the prior audit wrong.

| # | SUBJECT | VERDICT A | VERDICT B | ADJUDICATION |
|---|---|---|---|---|
| **D-1** | **The `no-deferral` gate's four named tags** | Seeded to this list as live: *"the no-deferral gate matching none of its four named tags."* LEDGER **G55** called it the headline blind spot and left it **OPEN**. | **FIXED AT HEAD.** `[proven here]` `git log -1 72c9c14d6` = *"no-deferral: the four tags the rule is named after were matched by nothing"*, and `xtask/src/gates/no_deferral.rs:206` now reads `for tag in ["TODO", "FIXME", "XXX", "HACK"]`. | **B. The seed is stale by one commit** — it was true when written and was repaired at the HEAD this pass opened on. **LEDGER G55 is now a stale OPEN row → DELETE-LIST candidate.** The gate's own doc (`:180-201`) records the cost honestly: `// TODO fix this` **tagless** is still NOT matched, and `format!("# TODO(migrate): {t}")` at `config/migrate.rs:428` is correctly excluded as the migrator writing data. |
| **D-2** | **The rate-clamp drift — one conversion or two?** | **M1 / M31 / X-61 / X-131** all carry it OPEN: *"`cost/rate.rs:40` clamps, `busbar-kernel/src/cost.rs:138` does not; at `1e17` the book answers 0 and the door answers `u64::MAX`."* X-131 adds that the fix is *"built, proven, and blocked on a row only the owner can write."* | **THE FIX LANDED.** `[proven here]` `crates/busbar-kernel/src/cost.rs:149` is `let nanos = busbar_kernel_ledger::cost::nano_rate;` and `:134-143` documents it: *"THE CONVERSION IS THE LEDGER'S, NOT A COPY OF IT… the duplicate is gone rather than patched."* The blocking dependency edge also landed: `crates/busbar-kernel/Cargo.toml:66` `busbar-kernel-ledger = { path = ... }` **and** the owner-authored `[[dep]]` row at `qa/kind-isolation.toml:3348-3352` (`from = "busbar-kernel"`, `to = "busbar-kernel-ledger"`, `half = "shipped"`, `count = "1"`). | **B, and it changes four rows.** M1, M31, X-61 and X-131 are **CLOSED** and are DELETE-LIST candidates as stale rows. **⚠ BUT SEE X-201:** the guard that keeps the duplicate from returning — `crates/busbar-kernel-ledger/tests/rate_conversion_agreement.rs`, whose entire purpose is this — **is deleted in the working tree right now, uncommitted.** Closing this row and losing its guard in the same session is exactly the failure the row existed to prevent. |
| **D-3** | **Does `cargo xtask gate config-schema --write` exist?** | Seeded as absent: *"`cargo xtask gate config-schema --write` does not exist though the gate names it as its only remedy."* | **The flag is named AND implemented in the gate.** `[proven here]` `xtask/src/gates/config_schema/mod.rs:65` `const REGEN: &str = "cargo xtask gate config-schema --write";` and `:118` prints *"the committed snapshot IS the fresh render (rewritten by --write)"* — i.e. there is a live `--write` ARM, not just a string. `conformance_sync/mod.rs:62` and `construction/ceilings.rs:263` carry the identical pattern. | **UNRESOLVED — and deliberately not resolved here.** The gate has a `--write` arm; what I could **not** confirm read-only is that the CLI's argument plumbing routes `--write` to it. **The settling command writes a snapshot, and this file is read-only on every artifact**, so it is not run here. **ADJUDICATE.** Settle with `xtask gate config-schema --help`, or by reading the arg-parse site — **never** by running `--write`. |
| **D-4** | **The six dead `full-gate` skip entries** | `1.6.0-ratchet-census.md` Part 6: *"6 DEAD skip entries — every one names a script not in the tree"* (`release-order-lint.py`, `fetch-golden.sh`, `fetch-plugin.sh`, `selftest.sh`, `record.sh`, `replay.sh`), counted as 6 debt items. | **ALREADY REPAIRED ON TRUNK.** `[proven here]` `qa/full-gate.toml` carries exactly **10** `[[skip]]` entries and every one resolves; the six are gone; and `:17-49` is a comment block recording the removal and why. `git status --porcelain qa/` does **not** list the file, so the repair is COMMITTED, not another agent's working-tree edit. | **B. The census is stale on this row.** The six scripts are still absent (`[proven here]`) — the census was right about the tree and wrong about the register. **The RULE-SHAPE defect survives and is X-217**: the selftest still checks only that a skip carries a reason, never that its subject exists. Data cleaned; instrument not. |
| **D-5** | **Do dead-subject rows still exist in `qa/*.toml`?** | `1.6.0-ratchet-census.md` §R.11.3: **62 ratchets that cannot go red**, nine deleted crates accounting for 53. Its sharpest example: the `instance-noun` `valkey` leak pinned to `crates/plugin-sign/src/tests/lib_tests.rs`, a deleted crate, while the same 36 occurrences live at `crates/plugin-loader/src/tests/sign_tests.rs` with no row at all. | **PARTLY REPAIRED.** `[proven here]` A path-resolution scan over every `file=`/`path=`/`script=`/`target=` value in all `qa/*.toml` finds **ZERO dead paths** (411 distinct paths in `instance-noun-neutrality.toml`, all live; 19 in `construction.toml`, all live; 10 in `full-gate.toml`, all live). The only non-resolving value is the literal `tests/…` ellipsis in `qa/segments.toml`, which is prose. **`grep -n 'plugin.sign' qa/instance-noun-neutrality.toml` → no match.** But **the crate-NAME rows survive**: 8 names in `qa/kind-isolation.toml` are not live workspace packages — `busbar-admin`, `busbar-grammar`, `busbar-plane-admin`, `busbar-plugin-pack`, `busbar-plugin-sign`, plus `plugin-abi`, `plugin-tooling`, `store`. | **BOTH, split by row shape.** PATH-keyed dead rows are drained. **NAME-keyed dead rows are not**, and they are the majority of the census's 53. The census's own count is therefore stale in the numerator and **correct in the finding**. `[proven here]` the moved-leak half still holds: `grep -o valkey crates/plugin-loader/src/tests/sign_tests.rs \| wc -l` → **36** (use `-o`, not `-c`: `-c` counts LINES and gives 35). Live rows for the name-keyed class go to the DELETE LIST; the moved-leak row is a TODO. |
| **D-6** | **Is `1.6.0-done-readout.md` a completed run or a draft?** | `1.6.0-LEDGER.md` `## THE FOLD` records it as *"PARTIALLY FOLDED … the document self-declares 'Run in progress', 21 of its 23 group rows read PENDING"*, and row **G9** carries **79 hits**. | The committed file has **ZERO** PENDING rows; it is a completed run, and it says **81**. | **B. The ledger is citing a superseded draft**, and G9's number came along with it. Same shape as D-1 and D-4: **a worklist carrying a stale reading of its own source.** Correct G9 to 81. |
| **D-7** | **Seven gate-sweep verdicts the ledger asserts OPEN** | LEDGER asserts `OPEN` on `GS5` (hold-escapes), `GS15` (no-deferral ×3), `GS22` (`ship-ready:ceiling-slack`). | S2f's own re-run at `a7cceac3f` found all seven **GREEN**. | **B.** *A worklist that carries fixed work as open is as wrong as one that carries open work as fixed; it just wastes effort instead of shipping a defect.* All seven → DELETE-LIST candidates. **Note GS15 is the no-deferral family, i.e. D-1 confirming from a second direction.** |
| **D-8** | **Is `qa/design-bindings-gaps.json` enforced?** | `1.6.0-ratchet-census.md:449` marks it *"gate detects staleness? yes / GREEN."* | **X-139**: repo-wide grep for the filename returns **exactly 2 hits, neither a parser**. The file's own text claims a selftest case that does not exist. | **B.** The census read the file's self-description rather than the gate. **This is the docs trap inside a `qa/` register** — a file that documents its own enforcement is not enforced by documenting it. |
| **D-9** | **Is the `busbar-contract` LOC figure 5,652 / 6,659 / 6,815 / 6,837 / 7,012 / 7,426?** | map-proof §6.5(c) tabulates **four** authorities for one quantity: 5,652 / 6,659 / 7,012 / 7,426. | ratchet-census §R.11.4 measures a **fifth**: `construction` reports **6,815** twice (`loc-ceilings:caps-contract` and `surface-ceiling:contract`). gate-sweep §1.3 reports a **sixth**: **6,837**, also twice. | **SIX AUTHORITIES, ONE QUANTITY, and the counter that produced 7,426 (`scripts/loc-surface.py`) no longer exists.** `caps-contract` and `surface-ceiling:contract` are **the same overage counted twice** — `busbar-caps` measures 0 because it is folded away. **PARK — owner ratifies ONE measurement of record from the surviving Rust counter, retires the dead figures by name, and re-pins both halves of the pair in one edit.** Raising a ratcheted ceiling is an owner act by construction. |
| **D-10** | **`1.6.0-oracle-coverage.md` §6.6 vs `1.6.0-map-proof.md` §4.2 — a section count** | map-proof: **33**. | oracle-coverage: **25**, with its arithmetic shown. | **B — and map-proof does not rebut it.** The same pattern holds in S2c (44 → 42): **in both documents the table-row extraction was sound and the heading regex was wrong.** Found independently by a second agent from a different direction, which is why it is recorded rather than filed. |
| **D-11** | **Six corrections the kernel file-verdict sweep says the collator MUST carry** | `1.6.0-LEDGER.md` rows **S21**/**S4**, **ENG3**, **ENG5**, **ENG22**, and `1.6.0-surface-drift-audit.md:579`. | `1.6.0-file-verdicts-kernel.md` §⚠ contradicts five of the six with tree measurements. | **file-verdicts-kernel, on all five.** (1) S21/S4 cite `crates/busbar-kernel-egress/src/egress_auth/jwt_bearer.rs` — **that directory does not exist**; the live file is `crates/busbar-kernel/src/egress_auth/jwt_bearer.rs` and **the defect is live at `:303`** (see **X-172**). (2) `tests/plane_abi_rider.rs` is **not** an untracked probe — `git ls-files` tracks it; it was committed as `4e39e1a11` as the §11a "no 0-caller ABI" proof, and deleting it as debris would remove a load-bearing ABI-crossing test. (3) ENG3's `mask.rs`/`tick.rs`/`recovery.rs` are **two disjoint sets of three files** under `busbar/src/root/` and `busbar-kernel/src/` with different line counts — fixing one closes neither. (4) ENG5's *"`plane_host/` is unwired scaffold"* names `busbar-plugin`'s module; **`busbar-kernel/src/plane_host/` is a different module and IS wired** — 44/44 vtable slots, driven by a dlopened plane. (5) ENG22's 32 is **31**, confirmed independently. **Every one of these would close a live row or open a dead one.** |
| **D-12** | **`X-67` — do `plugins.yaml` + `plugin-registry-check.sh:192` admitting 4 of 7 kinds block the architecture?** | Raised as an architecture finding. | **DOWNGRADED BY THE OWNER, 2026-09-22: *"really old code."*** Created 2026-08-01, `kind:` line untouched since 2026-08-05 — it predates the 7-kind model and registers out-of-tree plugin REPOS for release coverage, not the in-binary kind taxonomy. | **Owner's ruling stands. LOW-MED, not an architecture blocker.** Recorded because an agent re-deriving it from the tree would raise it again. Update the enum or retire the file. |
| **D-13** | **Were the "15 broken documentation references" real?** | Carried as 15 live broken references. | Independently re-walked: **0 silent ones remain** — every one is either fixed or explicitly flagged in-line as absent/owed. | **B — closed.** Recorded so it is not re-raised. And the pattern that fixed it is named: three advisories (admin-unit-scope, plugin-ABI-discriminant, rate-limiter-clock) each carry a *"where this code lives now"* addendum and **every path in them still resolves.** That is the pattern the other twelve stale docs in **X-103** should copy. |
| **D-14** | **`X-49` — did the float-usage fix land for Cohere only?** | Raised as a partial money fix. | **STRUCK — DISPROVED BY EXPERIMENT.** All six readers go through one seam (`read_count_u64`/`billed_count`, `busbar-llm-codec/src/usage_count.rs`); no live `.as_u64().unwrap_or(0)` remains on any usage field. Proven **by mutation**: neutering the seam fails **26 tests across all six dialects** with the exact under-billing symptom — anthropic `0` vs `27`, gemini `0` vs `832`, openai_chat `0` vs `800`. | **B, and this is the evidentiary standard the rest of the list should be held to.** A red-before-green mutation is worth more than any grep. Landed by `834dc1836`, `812370e09`, `3c321a31e`, `3c533603f`. **X-49 stays struck and never returns.** |
| **D-15** | **`PlaneHostVtable::EMPTY` in `plane-example`** | Carried as a defect (an empty vtable shipping). | **REFUTED.** It appears only in a doc comment describing a **negative test** — hand the fixture a null vtable, it must refuse. `plane-example` is fully wired and is one of only two fixtures proven to link both `rlib` and `cdylib` from one source. | **B.** Removed from the worklist. Recorded so it is not re-raised from the grep that originally found it. |

---

# 9. CLAIMS AWAITING EVIDENCE

**Rule 3: a finding without evidence does not get a row.** These were seeded or asserted and could
not be substantiated in this pass. They are not findings yet.

| claim | what was tried | what would settle it |
|---|---|---|
| **"183 red-ability proofs are vacuous while passing."** | `grep -rn 'prove_red\|prove_green' --include='*.rs' xtask/` → **314** occurrences. No source in the tree carries the figure 183 — it is not in `1.6.0-gate-sweep.md`, `1.6.0-instrument-audit.md`, or any file that exists. The named source `1.6.0-test-vacuity.md` **does not exist yet**. | The vacuity sweep landing its file. The shape is certainly real — **X-144** proves 17 `prove_red` cases pass trivially against an already-red gate — but 183 is an unsourced number and this list does not carry unsourced numbers. |
| **"25 gate rows stand red and are therefore unable to prove themselves."** | `CONSTRUCTION_STANDING_REDS` is **5** entries (`xtask/src/gates/mod.rs:227-236`), not 25. `1.6.0-gate-sweep.md` §0 reports **70 distinct red rows** across **16 RED gates**, and classifies them 11 BLOCKED / 15 DEFECT / 22 DEBT / 19 DRIFT / 2 IN-FLIGHT / 1 DERIVATIVE. No 25 anywhere. | Name the register the 25 comes from. The underlying point is live and is **X-144**'s: a `prove_red` against an already-red gate proves nothing. |
| **"Nine money disagreements are asserted-not-resolved in LEDGER R7, including a 38.5% difference on a rate-card edit."** | `1.6.0-LEDGER.md:272` **R7** reads: *"ONE `price(ledger_slice, card_history) -> Money` for admin reads and the budget gate — today `f` is implemented ~20× across 5 crates. Plan landed; HELD behind the streaming fold. IN-FLIGHT."* It cites `1.6.0-money-one-function.md`. **No enumeration of nine disagreements and no 38.5% figure appears in the R7 row.** | Read `docs/design/1.6.0-money-one-function.md` (10,150 bytes) for the nine and the 38.5%. If they are there, they are nine **money** rows and a PARK each. **Flagged as the highest-value item for pass 2** — a 38.5% difference on a rate-card edit is a billed byte. |
| **"The secret-hygiene gate is blind to `format!`-into-`Err`."** | Raised as **X-211** at ADJUDICATE rather than dropped, because the shape is proven live by **X-172** (`jwt_bearer.rs:303`). But `secret-hygiene.md` Part 5 is titled *"CHECK 3: a secret must not be INTERPOLATED INTO A MESSAGE A CALLER RECEIVES"*, so the rule may exist and be unarmed rather than absent. | One matched-pair plant, in reach and out of reach, with both verdicts printed. |

---

# 10. SOURCES

## Merged in pass 1

| source | present | what was taken |
|---|---|---|
| `1.6.0-MAP.md` | ✅ 140 KB | **§3.1 in full — the prior X register, 137 ids.** Carried under original numbers. |
| `1.6.0-LEDGER.md` | ✅ 595 KB | **390 distinct row ids** (R 78 · G 64 · M 55 · GS 36 · S 35 · T 32 · ENG 32 · DOC 28 · DR 17 · IA 13); states: 267 OPEN · 79 PARKED-OWNER · 16 DONE · 10 STANDING PRACTICE · 6 IN-FLIGHT. **All 55 M-rows read; money folded in full.** |
| `1.6.0-map-audit.md` | ✅ 18 KB | The nine verdicts, both disagreements, the ANSWER. |
| `1.6.0-map-proof.md` | ✅ 258 KB | §2 table, §12 verdict, S2d/S2e/S2f, Appendix A. |
| `1.6.0-ratchet-census.md` | ✅ 91 KB | §1.3 (25 dead/vacuous), Part 2, Part 6, §R.1, §R.11.3 (the 62), §R.11.4. |
| `1.6.0-gate-sweep.md` | ✅ 69 KB | §0 headline, §1 (11 owner-ruling reds), §1.5 (the landed edge ruling). |
| `1.6.0-instrument-audit.md` | ✅ 65 KB | §1 ranking (16 rows), §4, §7, §9 (three repairs). |
| `1.6.0-done-readout.md` | ✅ 38 KB | Findings 0–12, the 13-item work queue. |
| `1.6.0-file-verdicts-kernel.md` | ✅ 123 KB | The six collator corrections, TRASH ×1, RECODE ×2. |
| `1.6.0-branch-sweep-ledger.md` | ✅ 232 KB | **579 branch rows** — 44 EMPTY · 108 HARVESTED · 595 SURVIVOR; verdicts 97 SUPERSEDED · 59 PARK · 45 GAP · 22 TEST-ONLY. Money PARKs folded. |
| `1.6.0-oracle-coverage.md` | ✅ 56 KB | §0 mechanics, §3 money, §4 blind spots. *(Named in the brief as `-oracle-coverage-sweep.md`; the real filename is `-oracle-coverage.md`.)* |
| `1.6.0-secret-hygiene.md` | ✅ 51 KB | Vocabulary, Part 3 gate, Part 5 CHECK 3. |

## Named in the brief and NOT PRESENT at this pass

Twelve of the twenty-four named sources do not exist. **Their absence is itself reportable** — the
brief was written against a plan, and these agents have not landed:

`1.6.0-branch-sweep.md` · `1.6.0-money-sweep.md` · `1.6.0-file-verdicts.md` ·
`1.6.0-config-surface.md` · `1.6.0-plugin-abi-matrix.md` · `1.6.0-teller-matrix.md` ·
`1.6.0-release-engine.md` · `1.6.0-decision-audit.md` · `1.6.0-customer-surface.md` ·
`1.6.0-test-vacuity.md` · `1.6.0-dep-walls.md` · `1.6.0-gate-blindspots.md` ·
`1.6.0-unconstructed-sweep.md`

*(`1.6.0-oracle-coverage-sweep.md` is absent under that name but present as
`1.6.0-oracle-coverage.md` and is merged.)*

Three of the missing four money/vacuity sources are exactly the ones §9's unsubstantiated claims
needed. **Pass 2 re-reads this directory first.**

## Adjacent sources found on disk and NOT yet merged

Present, clearly in scope, not named in the brief. Queued for pass 2:

`1.6.0-money-one-function.md` (**read first — §9 names it**) · `1.6.0-local-only-audit.md` (246 KB) ·
`1.6.0-TRACKER.md` (gate-read, cannot be deleted) · `1.6.0-contract-gaps.md` (~60 gap rows) ·
`1.6.0-shadowed-guard-audit.md` · `1.6.0-security-posture.md` · `1.6.0-engine-split-plan.md` ·
`1.6.0-duplex-plane-and-realtime.md` · `1.6.0-exact-count-migration.md` · `1.6.0-one-net-judge.md` ·
`1.6.0-per-unit-arena.md` · `1.6.0-credential-signature-declared.md` (the **X-209** subject) ·
`1.6.0-codeaudit-findings-ledger.md` · `AUDIT-STATUS.md` (generated — never hand-edit).

---

# PASS LOG

| pass | date | HEAD | rows | what changed |
|---|---|---|---|---|
| **1** | 2026-09-23 | `72c9c14d6` | **140** | File created. **160 distinct prior ids folded into 140 rows.** Money (49) and auth/capability (20) folded in full. 12 of 24 named sources absent. **15 disagreements preserved, 2 of which CLOSE 5 rows** (D-1 closes G55; D-2 closes M1/M31/X-61/X-131). 4 seeded claims demoted to §9 for lack of evidence. **31 new ids allocated, X-201…X-231, contiguous, no gaps, no reuse.** |

### Pass 1 census

| by section | rows | | by CLASS | rows | | by CERTAINTY | rows |
|---|--:|---|---|--:|---|---|--:|
| §1 MONEY | 49 | | money | 57 | | **VERIFIED** | 74 |
| §2 AUTH/CAPABILITY | 20 | | instrument-blind | 38 | | **PARK** | 53 |
| §3 MISSING-CODE | 10 | | doc-drift | 23 | | **ADJUDICATE** | 12 |
| §4 INSTRUMENT-BLIND | 30 | | auth/capability | 21 | | CLOSED | 1 |
| §5 ABI·CONFIG·CUSTOMER | 11 | | missing-code | 10 | | | |
| §6 DOC-DRIFT | 13 | | config | 7 | | | |
| §7 MAP INTEGRITY | 7 | | abi | 5 | | | |
| **total** | **140** | | customer-surface | 4 | | **total** | **140** |

*(CLASS sums above 140 because a row may carry two classes; every money row is PARK by rule.)*

**Duplicates collapsed: 81.** 34 at the id level — 17 rows carry two or three prior ids (20 folded),
`M6`–`M17` collapse twelve waiver findings into one row (11 folded), `M46`–`M49` collapse four into
one (3 folded). 47 more at the source level: **41 rows arrived from two or more sweeps
independently** and are recorded once with every evidence line kept. *A list that counts one defect
three times is not a census.*

**Standing rule for every pass: re-read the sources immediately before merging.** The tree moved
under this one — `qa/full-gate.toml` was repaired between the ratchet census and this read (D-4),
the rate-clamp fix landed between the ledger and this read (D-2), and the `no-deferral` tag fix
landed **one commit** before it (D-1).
