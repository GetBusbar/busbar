# Busbar 1.6.0 — EXECUTION PLAN (minimal, permanent, re-read every session)

Read this + DECISIONS.md before any status/architecture claim. Real 1.6.0 tip = `integration/oracle-phase0`.

## Owner-need target: **Sat 2026-09-20** (throw everything, no quality drop, no defer)
## Honest worst-case floor: **Wed 2026-10-08 17:00 PDT** (never slips silently)
Target is the push goal; worst-case is the commitment. If anything threatens 9/20, surface it the hour
it is known. Worst-case moves ONLY on a named MAJOR EVENT (DECISIONS #16). Recompute status against the
FULL DONE-oracle (21 groups in `scripts/verify-1.6.0-done.sh`), never a slice.

## DONE = `verify-1.6.0-done.sh` exit 0 (21 groups) + LOCKED §11 (two arch audits ≥9/10) + audit-ledger clean.

## Critical path (seam-decoupled — NO blocking poles; everything runs in parallel behind stable seams, DECISIONS #25)
1. **Money-path Commit C** — atomic ~26k-LOC engine relocation onto the plane. DECOMPOSE into N
   individually-oracle-green sub-moves (test oracles first, then engine slices behind the neutral
   `LlmBuildInput` DTO). Each sub-move proven byte-green before the next. cost_per_mtok + pool-hook
   resolution are the byte-movers — keep in core behind a down-facade.
2. **Composition-root switch-over (D30→D31→D32)** — generic loop gets a production caller (D30) + one
   root data-plane chain builder (D31) + default flip to zero-unaccepted on all planes (D32). Unblocks
   the 4 RED oracle groups: PARITY, EQUALITY-root, ISOMORPHISM, TELLER-STEPS. Build D30/D31 in parallel
   with C so the flip is ready when C lands.
3. **Frozen-SHA rolling audit** — freeze tip early; run audit-ledger + §11 audits + J3-to-dry
   continuously against it (per-scope swarm) so it finishes as code lands, not after.

## Parallel bulk (~2-3 days, local, max agents — one per item)
- 5 RED gates: kind-isolation (reconcile taxonomy per DECISIONS #3/4/5), plane-purity (`prepass.rs` mcp
  key), design-bindings regen, construction ceilings + one-pick-site + one-pricing-site.
- H1 + C7 + C9 (busbar-llm ABI purity + neutral fixture seam — C9 is the cluster's long pole).
- 14 open HIGH findings (AUDIT-STATUS.md) — one agent each.

## Needs one+ fleet box (spot, zero-idle, terminate on close) — after a green candidate binary
- ~379 unverified golden cells (admin.ops/billing/route.failover/ledger/etc.) re-record + diff.
- mcp + a2a conformance rigs; 3 store-persist cells (live pg/mysql/valkey).
- teller-steps shipped-path proof; J3 WIP-crate pass.

## Hard cut items (DONE gates, not follow-through)
- busbar-core + busbar-voice deletion IS in 1.6.0 — it is the release (#1 core names zero planes, #12 the re-arch IS 1.6.0). Hard DONE gate `kind-isolation-ship:legacy-drain` = 0. Executed as a parallel wave fan-out (DECISIONS #19).
- Both-ways (compiled-in OR dropped-in) is required for every plugin kind incl. planes — the existing 1.5.5 contract (#2), no extra effort. The folder-drop loader for every kind is a hard cut item; the build gate proves both distributions + a per-kind drop-in conformance rig (DECISIONS #11).

## Marketing delta to fix (design side): audit-record emission must be the single owned step, not
plane-side pass-throughs (folds into switch-over #2). Do NOT edit the marketing site.

## Anti-recurrence (why we never come back here)
- Every settled decision is a row in DECISIONS.md with an enforcing gate; drift → RED, not re-litigation.
- VISION + LOCKED + DECISIONS authoritative; all other docs reconciled or deleted (DECISIONS #14).
- Status recomputed vs the full 21-group oracle each cycle; worst-case date pinned at top.
- READ-FIRST hook + this file re-read every turn; cite-or-ask, never guess.

## RATIFIED sub-plans (verified against the oracle-phase0 tip, not stale branches)
- **Commit C (money-path relocation): LARGELY ALREADY LANDED on the tip** — engine in `busbar-llm/src/engine/` (~47k LOC), `PlaneBuildInput` DTO in `busbar-substrate/.../plane_host/build_input.rs`, `resolve_pool_*` facade in `state.rs`; only ~750 LOC neutral proxy vocab remain in core. The "engine still in core / ~1wk atomic" reading was the STALE `plane-extraction` branch. Remaining: verify oracle-green (in flight), D-cleanup, S5/S6 cold-reader repoint, one-book collapse. Billed-value fidelity carried by `PlaneBuildInput` (cost_per_mtok per member, upstream_model, context_max, default_max_tokens, upstream-cred mode, reasoning_budgets) — any drift = deny-and-fix.
- **Switch-over: `root-llm` is already DEFAULT-ON (`cb2ba5df3`); D32 flag is flipped.** It's a shell-over-engine, not a cutover. Remaining, mostly parallelizable with C: (1) D31 one root chain-builder producing the plane `Units` nodes (dormant boot assembly); (2) mount mcp/a2a/voice on the kernel loop (ingress swap — SHIPPED-PATH change, oracle-gated); (3) audit-record emission → the single owned Audit step on the neutral journal (`busbar-unit-audit::journal`); (4) R4-0 additive pre-cuts (relocate 14 engine symbols, activate `busbar-unit-egress`, deletes nothing); then (5) money one-book: built BEHIND the money-book seam in parallel and activated by a one-line composition flip (shadow-oracle byte-proven) — NOT a serialization point (DECISIONS #25); the mid-stream upstream-error golden is recorded up front in parallel, not a prerequisite that stalls other work; then (6) delete the orphaned `busbar-substrate` `TellerPlane` loop + `GauntletAdapter`. Authoritative measurement: `docs/design/D33-legacy-retirement.md` §7.8/7.10/7.11/7.12/7.4.
- **Audit-single-step law = DECISIONS #1 + `1.6.0-plane-extraction-LOCKED.md` §5** (not DECISIONS #13, which is the marketing row).

## UNATTENDED SAFETY RULE (while owner away)
Blind-swarm ONLY additive/dormant/purely-local work the oracle can prove byte-identical. Any step that
SWAPS the shipped request path or touches the money book waits for a green candidate binary + careful
serial execution + is surfaced to the owner. Money is sacred; deny-and-fix on any user-visible byte;
never self-approve a billed-byte change. Quality + byte-identity outrank raw agent count.

## Status cadence: hourly, format = {worst-case date · how much left · ETA & why-changed · gotchas ·
blocked? · queued questions}.

Commit authorship comes from git config (GitHub noreply). No AI attribution, ever.
