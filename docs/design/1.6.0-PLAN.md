# Busbar 1.6.0 — EXECUTION PLAN (minimal, permanent, re-read every session)

Read this + DECISIONS.md before any status/architecture claim. Real 1.6.0 tip = `integration/oracle-phase0`.

## Owner-need target: **Sat 2026-09-20** (throw everything, no quality drop, no defer)
## Honest worst-case floor: **Wed 2026-10-08 17:00 PDT** (never slips silently)
Target is the push goal; worst-case is the commitment. If anything threatens 9/20, surface it the hour
it is known. Worst-case moves ONLY on a named MAJOR EVENT (DECISIONS #16). Recompute status against the
FULL DONE-oracle (21 groups in `scripts/verify-1.6.0-done.sh`), never a slice.

## DONE = `verify-1.6.0-done.sh` exit 0 (21 groups) + LOCKED §11 (two arch audits ≥9/10) + audit-ledger clean.

## Critical path (serial poles — agents parallelize AROUND these, not through them)
1. **Money-path Commit C** — atomic ~26k-LOC engine relocation onto the plane. DECOMPOSE into N
   individually-oracle-green sub-moves (test oracles first, then engine slices behind the neutral
   `LlmBuildInput` DTO). Each sub-move proven byte-green before the next. cost_per_mtok + pool-hook
   resolution are the byte-movers — keep in core behind a down-facade.
2. **Composition-root switch-over (D30→D31→D32)** — generic loop gets a production caller (D30) + one
   root data-plane chain builder (D31) + default flip to zero-unaccepted on all planes (D32). Unblocks
   the 4 RED oracle groups: PARITY, EQUALITY-root, ISOMORPHISM, TELLER-STEPS. Build D30/D31 in parallel
   with C so the flip is ready when C lands.
3. **Frozen-SHA rolling audit** — freeze tip early; run audit-ledger + §11 audits + J3-to-dry
   continuously against it (per-scope swarm) so it finishes as code lands, not after.

## Parallel bulk (~2-3 days, local, max agents — one per item)
- 5 RED gates: kind-isolation (reconcile taxonomy per DECISIONS #3/4/5), plane-purity (`prepass.rs` mcp
  key), design-bindings regen, construction ceilings + one-pick-site + one-pricing-site.
- H1 + C7 + C9 (busbar-llm ABI purity + neutral fixture seam — C9 is the cluster's long pole).
- 14 open HIGH findings (AUDIT-STATUS.md) — one agent each.

## Needs one+ fleet box (spot, zero-idle, terminate on close) — after a green candidate binary
- ~379 unverified golden cells (admin.ops/billing/route.failover/ledger/etc.) re-record + diff.
- mcp + a2a conformance rigs; 3 store-persist cells (live pg/mysql/valkey).
- teller-steps shipped-path proof; J3 WIP-crate pass.

## Explicitly NOT a cut blocker
- busbar-core deletion (D16/D33, ~155 agent-days) — not a DONE gate; separate arc.
- Plane folder-drop loader wiring — conformance follow-through (DECISIONS #11).

## Marketing delta to fix (design side): audit-record emission must be the single owned step, not
plane-side pass-throughs (folds into switch-over #2). Do NOT edit the marketing site.

## Anti-recurrence (why we never come back here)
- Every settled decision is a row in DECISIONS.md with an enforcing gate; drift → RED, not re-litigation.
- VISION + LOCKED + DECISIONS authoritative; all other docs reconciled or deleted (DECISIONS #14).
- Status recomputed vs the full 21-group oracle each cycle; worst-case date pinned at top.
- READ-FIRST hook + this file re-read every turn; cite-or-ask, never guess.

## Status cadence: hourly, format = {worst-case date · how much left · ETA & why-changed · gotchas ·
blocked? · queued questions}.

Author: Matthew <dev3@getbusbar.com>. No AI attribution.
