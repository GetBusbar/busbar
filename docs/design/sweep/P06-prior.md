# P06-prior — qa/*.toml ratchets and xtask/tests/, plus the prior-mentioned files in this slice's list

Slice: 78 files, each previously *mentioned* in a prior 1.6.0 sweep document. Per the sweep
contract's special note for this slice, every file below was first checked against
`docs/design/1.6.0-*.md` (via `git grep -n -F -- "<path>" docs/design/1.6.0-*.md`) to see whether
the prior mention was a real, command-backed verdict or a bare citation. Where a real verdict
existed, it is cited and, wherever practical, **re-run or re-read against the current working tree
today (2026-09-23)** to confirm it is still true — the tree has heavy concurrent uncommitted
activity this week (`git status` at session start shows dozens of modified files across other
sweep slices), so a citation from earlier this week is not assumed current without a fresh check.
Where the prior mention was bare (file-verdicts.md's blanket `TOOLING` row, or branch-sweep-ledger.md
noise comparing a deleted branch to trunk — which is not a verdict about this file's current
correctness), the file was swept fresh.

No `qa/*.toml` ratchet, waiver, ceiling or ledger value was edited, blessed or regenerated. This is
read-only. The first pass over this slice was produced by parallel sub-batch review (one pass
each over the `qa/*.toml` ratchets, `scripts/`, and the `xtask/` gate sources split two ways) and
then reconciled against each other; where the sub-batches disagreed, the disagreement was checked
against the live tree with a fresh command before either side was trusted (one such check — a
claimed fix to `xtask/src/gates/construction.rs`'s source-denylist scope — did NOT hold up under
re-verification and the original FINDING was kept). Eight genuinely new findings surfaced
(X-3500..X-3507, six of them dead/orphaned `pub` items caught only by the reconciliation pass, not
by either original pass alone); everything else below is a citation of an existing prior finding,
independently re-verified against the current tree (confirmed still-true, or confirmed since fixed).

| FILE | VERDICT | EVIDENCE | ROWS |
|---|---|---|---|
| qa/reachability.toml | FINDING | Carried: LEDGER.md DR7 (:487), THE-LIST.md:272, gate-blindspots.md:265, unconstructed-sweep.md:586-590. Re-read in full today: file carries **zero `[[dormant]]` declarations**; its own prose says the 4 standing reds (`unit-path:mcp`, `unit-path:a2a`, `unit-path:streaming`, `root-module` x2) are *deliberately* undeclared so the gate stays red rather than pre-suppressed. Still true today — no declaration block exists in the file. | - |
| qa/segments.toml | FINDING | Carried: ratchet-census.md PART 11 (lines 409-431) — full census with real commands, not prose. Re-ran today: `bash scripts/plane-config-noun-gate.sh --selftest` → exit 0, `"resolved 4 section nouns from the plane decls: pools tools agents streams"`, ALL GREEN — **contradicts** the `plane-config-noun` segment's own comment (`qa/segments.toml:526-530`), still present verbatim, claiming `--selftest` is RED and finds only THREE nouns. The segment's own documented arming precondition is already met and it still reads `status = "reserved"`. Also re-confirmed the 3 dead reserved segments (`mcp-integrity`, `a2a`, `smart-router`) still name absent test targets: `ls crates/busbar/tests/qa_mcp_integrity.rs crates/busbar/tests/qa_a2a.rs` → both absent; `find . -name 'qa_smart_router*' -not -path '*/target/*'` → none (positive control: `find . -name segments.toml` → finds it). | - |
| qa/teller-steps.json | FINDING | Carried: X-117/DOC15 (LEDGER.md:605, THE-LIST.md:269, MAP.md:1288). Re-ran today: `python3 -c "import json;print(sorted(json.load(open('qa/teller-steps.json'))['matrix'].keys()))"` → `['a2a','admin','llm','mcp','voice']`; `ls crates/busbar-plane-*` → `a2a, decision, llm, mcp, streaming`. Matrix still names `voice`/`admin` (neither a live plane crate) and omits `decision`. Still true today. | - |
| qa/unconstructed.toml | FINDING | Carried: X-138 (THE-LIST.md:213, MAP.md:1071). Re-verified the CONTENT is accurate (not the reason for the finding): 19 `[[capability]]` rows / 18 `unshipped = true` (`grep -c '^\[\[capability\]\]'` → 19; `grep -n 'unshipped = true'` → 18 data rows + 1 doc example), all 19 `built =` paths exist on disk (python check), and a spot absence-grep (`grep -rn 'adjusting_entries(' --include='*.rs' . \| grep -v /tests/` → 0 hits outside definition, vs a positive control `dual_writing(` → 2 real production hits) confirms the row is honest. The DEFECT (per X-138, re-confirmed below under xtask/src/gates/mod.rs and xtask/src/full_gate.rs) is that the gate reading this file is registered but never runs in CI. | - |
| scripts/a2a-subject/boot.sh | FINDING | Carried: T31, T32 (LEDGER.md:568-569, both OPEN). T31: `boot.sh:843` self-prints "ROLE-NARROWED: --role server. busbar's A2A CLIENT direction is NOT measured by this leg" — 234 of 468 a2a cells (client role, exactly half) are never exercised by this rig. T32: `assert_tck_number` has no execution floor for the subject leg (only for control legs) — `NOT TESTED` is never gated on at `boot.sh:938`, so a TCK run in which all 114 MUSTs degrade to `NOT TESTED` still exits green. Not independently re-run (would require a live A2A TCK run); line numbers not re-verified against current file (1312 lines, script not read in full this session) but no fix is referenced anywhere else in the corpus for either row. | - |
| scripts/branch-symbol-sweep.py | CLEAN | This is the tool that *produced* the extensive branch-sweep-ledger.md analysis cited throughout this document — every mention of it in the corpus is usage documentation (branch-sweep.md), not a defect report. No finding against the tool itself in any prior doc. | - |
| scripts/ci-branch-protection.sh | FINDING | Two carried findings, the second one serious and re-confirmed today. G43 (LEDGER.md:458): branch protection requires exactly 4 contexts (`ci umbrella`, `structure lint`, `construction gate`, `ship-ready`); no conformance job is among them. **release-engine.md:298-303,520-523** (re-read and re-confirmed against the current file today): `build_body()` (still at :185-228) still reads `existing_rsc.get('contexts')` but never reads or preserves a `checks`/`app_id` field, and the PUT body it constructs (:214-219) still only ever sets `'required_status_checks': {'strict': False, 'contexts': contexts}` — no `checks` array. GitHub's branch-protection API distinguishes `contexts` (any app can satisfy it) from `checks` (can pin a specific `app_id`). Running this script today would still silently downgrade any app-pinned required check — including `ship-ready`, the one check that encodes the whole ship criterion — to "satisfiable by any token via a raw commit-status POST." release-engine.md's own recommendation #11 is "Do not run `scripts/ci-branch-protection.sh` as it stands." Confirmed still the case by direct code read. | - |
| scripts/ci-remote-lib.sh | FINDING | Carried: BR-S10 (LEDGER.md:1029). Re-ran today: `grep -n "remote_branch_slug" scripts/ci-remote-lib.sh` → 0 hits (positive control: `grep -n '^[a-z_]*(' scripts/ci-remote-lib.sh` → 5 other real functions found, so the grep mechanics work). The path-traversal-safe branch-name slugging function BR-S10 says is missing genuinely still does not exist in this file. Whatever this script's `remote_wrapper`/`fleet_hosts` machinery does with a branch name today has not been checked against a `../`-containing branch name. Still true. | - |
| scripts/ci-runners-reconcile.sh | CLEAN | Zero prior-doc mentions beyond branch-sweep-ledger.md noise (bare mention — swept fresh). Reachability confirmed: `docs/ci/self-hosted-runners.md:8` documents it as run "on a fifteen-minute timer" (external infra cron, not a GitHub Actions workflow — consistent with `.github/workflows/` carrying zero references), and it is the documented entry point that `up.sh` (below) is called from (`docs/ci/self-hosted-runners.md:106,117`). No defect surfaced in a light read of its role. | - |
| scripts/ci-runners-up.sh | CLEAN | Zero prior-doc mentions beyond branch-sweep-ledger.md noise (bare mention — swept fresh). Reachability confirmed the same way as `ci-runners-reconcile.sh`: documented, invoked by the reconcile script and manually per `docs/ci/self-hosted-runners.md:66-68,107`. No defect surfaced. | - |
| scripts/cost-watch.py | FINDING | Carried: M46-M49 (LEDGER.md:295, PARKED-OWNER, money-class). Implements the $100/period hard cap and $50/$80 alarm bands correctly, with a strict 4-band exit-code contract (including `exit 4 = "unknown spend, NOT a clean bill"`) — but is report-only by construction (cannot cancel a workflow or terminate an instance), runs weekly (a runaway is invisible for up to 7 days), measures an ESTIMATE because no real billing endpoint exists, and — per the more serious of the two readings recorded — its trigger workflow `sched-cost-watch.yml` exists only on `consolidated/1.6.0` while GitHub's `schedule:` trigger only fires from the repo's DEFAULT branch, so it may have **never executed at all**. Not independently re-run (this is an infra/spend-control question, not something to probe live against GitHub). | - |
| scripts/land-remote.sh | CLEAN | Zero prior-doc mentions beyond branch-sweep-ledger.md noise (bare mention — swept fresh). Reachability confirmed: `git grep -l "scripts/land-remote.sh"` → `scripts/land.sh` (sourced/invoked from there) and `docs/ci/self-hosted-runners.md`. No defect surfaced in a light read. | - |
| scripts/land.sh | FINDING | Carried: GS31 (LEDGER.md:515), same underlying defect as `xtask/src/gates/mod.rs` above. Re-ran today: `grep -n "ports-only-tests:busbar-llm\|request-path-fn-size\|terminal-doors-in-audit-step" scripts/land.sh` → `:227,:228,:229`, all three still present. Cross-checked against `xtask/src/gates/mod.rs:235-237` — identical three names, also still present. All three rows are PASS on the current `construction` gate per GS31's own re-run; the header's own contract at `xtask/src/gates/mod.rs:242` requires a gone-green name to be struck from both files in the same commit, and neither has been struck. Still true, same lines the doc cites. | - |
| scripts/no-deferral.waivers | CLEAN | Data file (waiver allowlist for `xtask/src/gates/no_deferral.rs`, covered above). Carried and CONFIRMED FIXED: G58 (LEDGER.md:473, marked DONE — the file was regenerated from the gate's own marker census, 52→56 rows). Re-ran today: `grep -vE '^\s*#' scripts/no-deferral.waivers \| grep -cvE '^\s*$'` → 56, and `git status --porcelain scripts/no-deferral.waivers` → clean (the 56-row version is committed, not a stray uncommitted edit) — resolving X-228/map-audit.md's "Δ=0 only in an uncommitted tree" concern; it is committed now. DOC6's "52 waivers retire on a document that has never existed" finding is also resolved per `1.6.0-TRACKER.md:170`: the retirement condition was rewritten to a `grep`-checkable code condition rather than a nonexistent doc citation. | - |
| scripts/plane-delete-test.sh | FINDING | Carried: IA10 (LEDGER.md:506), X-218 (THE-LIST.md:233, VERIFIED). Re-read today (:1038-1046): leg 1b is still `if [ -d "$s/crates/busbar-plane-$p" ]; then ... fi` with **no `else`** — confirmed by reading past the closing `fi` to the next leg's comment, nothing in between. A plane whose crate is folded away is still silently skipped rather than scored as a failure, unlike every other leg in this file, none of which have a selftest fixture for this leg either. Still true, same lines. | - |
| scripts/plane-keys.sh | FINDING | Carried: DR8 (LEDGER.md:488). Re-ran today: `grep -n "PLANE_KEYS=" scripts/plane-keys.sh` → `:46 PLANE_KEYS="llm mcp a2a voice"`; `ls -d crates/busbar-plane-*` → `a2a, decision, llm, mcp, streaming` (five, none named `voice`, one named `decision` singular vs the locked `decisions` plural DR8 also flags). Still true, same line. Cross-reference `xtask/src/planes.rs` and `xtask/src/gates/plane_purity/scanner.rs` above — three independent copies of the same wrong roster in three different files/languages. | - |
| scripts/pr-land.sh | FINDING | Carried: X-91 (MAP.md:520, THE-LIST.md:295, VERIFIED). Re-ran today: `grep -n 'base=' scripts/pr-land.sh` → `:52 base="dev"; ...`. This is a CLI-overridable default (`--base` flag exists at :56), not a hardcoded constant as X-91's summary phrasing implies, but the *default* behavior with no `--base` given is still to land directly onto `dev`, contradicting decision #67's "the release train is the ONLY writer of `dev`" as X-91 describes. Still true in substance. | - |
| scripts/promote.sh | FINDING | Carried: X-92 (MAP.md:521, THE-LIST.md:295, VERIFIED). Re-ran today: `grep -n 'case "\$FROM' scripts/promote.sh` → `:108`, and the block (:108-114) still reads exactly `"dev qa") ;;` / `"qa main") ;;` / `*) exit 1`. There is still no `"predev dev"` case, so the promotion path decision #67 locks (`predev` → `dev` → `qa` → `main`) remains unreachable by this tool as X-92 describes. Still true, same line. | - |
| scripts/reap-worktrees.sh | CLEAN | Carried: X-267 (THE-LIST.md:260, VERIFIED) — but this is a **provenance** finding about one specific historical commit (`12822b262`, a test-file deletion that rode into an unrelated commit about this script), not a defect in the script's current logic. The finding's own text: "the deletion itself is correct and nothing was lost... the defect is purely provenance," and its remedy is explicitly "None on the code. Do not amend." No open code-level finding against this file. | - |
| scripts/release-gate/ledger-selftest.sh | FINDING | **NEW — no prior doc discusses this file beyond branch-sweep-ledger.md noise.** Swept fresh. Read in full (253 lines): a real, well-built offline regression test for `ledger_status_for` (in `scripts/release-gate/lib.sh`), specifically targeting a documented historical bug class (a re-run leaving two rows — PASS then FAIL — for one id, with first-match-wins silently reading PASS). `git grep -n "ledger-selftest" -- . ':!scripts/release-gate/ledger-selftest.sh'` → 0 hits: not sourced by `gate.sh`, not called from any `.github/workflows/*.yml`, not called from any other script in the tree. See ROWS RAISED X-3501. | X-3501 |
| scripts/release-gate/lib.sh | CLEAN | Reachable and exercised at runtime: sourced by `scripts/release-gate/gate.sh:27` and by `scripts/release-gate/{channel,docker,expected-ids,fleet,platform}-checks.sh`, which are invoked from `.github/workflows/fleet-autoscaler.yml` (confirmed: `:218,:242,:268,:289,:343`) and `release-stage.yml`. The regression-test safety net around it (`ledger-selftest.sh`, and `gate.sh`'s own built-in `--selftest` arm which duplicates the same `ledger_status_for` case at `gate.sh:346-348`) never runs in CI — see X-3501 — but `lib.sh` itself is live, load-bearing code exercised on every real fleet-autoscaler run. | - |
| scripts/release-script-lint.sh | CLEAN | Reachable: `git grep -l "release-script-lint.sh"` → `.github/workflows/ci.yml`, `scripts/no-plugins-gate.sh`, `scripts/release-check.sh`, `scripts/release-check-1.5.2.sh`. Genuinely invoked in CI. No command-backed defect found beyond branch-sweep noise. | - |
| scripts/secret-accessor-seal-witness.sh | FINDING | Carried: TRACKER.md:187, map-proof.md:1696 (same sentence in both) — "`scripts/secret-accessor-seal-witness.sh` exists and is invoked by nothing." Re-ran today: `git grep -n "secret-accessor-seal-witness" -- . ':!scripts/secret-accessor-seal-witness.sh'` → 0 hits outside its own file and doc prose. Still true — a written but never-invoked auth/secret witness script. | - |
| scripts/secret-hygiene-gate.sh | FINDING | Auth/secret class, VERIFIED and serious. Carried: X-89/X-143 (THE-LIST.md:165), X-255 (THE-LIST.md:184), decision-audit.md #53/#54, release-engine.md S-2. Re-ran today: `grep -n "secret-hygiene-gate.sh\|SECRET_GATE_REPORT_ONLY" .github/workflows/ci.yml` → still invoked report-only at `:1578` (`--selftest`) and `:1584` (`SECRET_GATE_REPORT_ONLY=1 ... --report`); `grep -n 'report_only=' scripts/secret-hygiene-gate.sh` → `:1440 report_only="${SECRET_GATE_REPORT_ONLY:-1}"` — the script's own default is also report-only, so even removing the env var from CI would not arm it. Per X-89 (not independently re-armed and re-run here, since that would require setting `SECRET_GATE_REPORT_ONLY=0` and doing a full-tree scan — a live-effect-adjacent action outside a read-only sweep), the armed gate previously found 17 real violations across production code (`private_key`, `access_token`, `secret`, `credentials`, `api_key`, `caller_token`). A decision-#53-locked "hard release blocker" is still fully advisory. | - |
| scripts/txn-fence.sh | CLEAN | Carried, positive: instrument-audit.md:71 — "The original defect is FIXED... It is, however, excused from `full-gate` and run by nothing in the DONE oracle." The FIX (a real negative compile-fence selecting a real package) is confirmed done; the residual "not run by the DONE oracle" note is a minor completeness gap already recorded elsewhere (verify-1.6.0-done.sh's broader coverage gaps, see below), not a fresh finding against this small (51-line) file. | - |
| scripts/verify-1.6.0-done.sh | FINDING | The single most heavily-audited file in this slice — 60 prior-doc hits, a dense cluster of already-VERIFIED/OPEN/PARKED findings, all citing real commands and line numbers, all dated 2026-09-22/23 (this week). Representative set, spot-re-confirmed today: **G8** (`absent_step()`, still at :291, still sets `CUR_RED=1` identically to a real failure — an absent group and a failed group score the same in the final readout; re-confirmed by direct read) · **IA9** (still no `ship-ready`/`ship_ready` mention anywhere in the file — `grep -n` → 0 hits, positive control `grep -c '^begin_group '` → 23 confirms the file was actually searched — the DONE oracle still never invokes the ship gate) · **DR0/M53/M54/M55/DR14/DR15/DR16** (6 of 23 DONE-oracle groups returned no verdict at all on the only full readout this release has — EQUALITY, PARITY, KERNEL's whole-crate legs, PLANE-PURITY, KIND-ISOLATION, and 2 of 5 TELLER-STEPS legs — all attributed to running the oracle on a contended shared checkout rather than a quiesced one) · **DR12** (PARITY group still reads `target/release/busbar` via hardcoded literals rather than honoring `CARGO_TARGET_DIR`, so on a shared checkout its candidate may be another agent's binary) · **IA7** (`assert_bless_env_empty` still guards 11 named regen/repoint env vars and is missing 5 more, including `BUSBAR_ORACLE_RUST_BIN`, which selects the binary that judges the money goldens) · **IA8/X-222** (a missing `qa/full-gate.toml` still substitutes 3 unlabeled `cargo build`s under the unqualified DONE banner) · **T39** (the conformance group still runs 3 selftests-of-rig-machinery and zero rig-against-busbar runs, with no a2a step at all). None of these were flagged as fixed by any later entry in the corpus; two spot-checks (G8, IA9) confirm currency today. This is the release's central DONE-gate script and it is carrying real, open, dated findings. | - |
| xtask/src/audit.rs | FINDING | Carried: G18 (LEDGER.md:433). Re-read `confirmed()` at line 816 today: it still checks only that two entries exist with `a.auditor != b.auditor && a.round != b.round` — no check that the two confirming rounds are *consecutive*, so a `zero → findings → zero` sequence still reads CLEAN. Still true. Separately, independently found: `pub fn is_ancestor` (:337) has exactly two callers, both inside the `#[cfg(test)]` module that starts at :1372 (`git grep -n "is_ancestor" -- xtask/` → :337 def, :1427, :1437, both past the `#[cfg(test)]` boundary) — a public method with zero non-test callers. See X-3507. | X-3507 |
| xtask/src/cli.rs | FINDING | Carried: G40 (LEDGER.md:455). Re-ran today: `grep -rn "xtask conformance" .github/` → 0 hits (positive control: `grep -c "cargo xtask gate" .github/workflows/ci.yml` → 91). The `conformance check --musts` dispatcher arm at `cli.rs` is still never invoked by any workflow. Still true. | - |
| xtask/src/ctx.rs | FINDING | Independently found (not in any prior doc under this exact framing): `fn collect` (:892-919) recurses into every subdirectory (`if path.is_dir() { collect(&path, root, out)?; }`, :915-917) with no depth cap and no symlink-cycle guard — the only skips are `target` and dotdirs (:911). `sed -n '892,919p' xtask/src/ctx.rs` confirms. Cross-references `docs/design/1.6.0-branch-sweep-ledger.md:889` (T6: `WALK_DEPTH_CAP` recommended, never landed on trunk) — that citation was about a different symptom (a `.claude/worktrees/` hang, since fixed by the dotdir skip at :911, per this same function's own comment), not this specific gap; the recursion-depth/symlink-cycle absence itself is a fresh read. See X-3502. | X-3502 |
| xtask/src/denylist.rs | FINDING | Carried: local-only-audit.md:1419-1421. Re-read `run_cargo_metadata` (still at line 274) today: still returns bare `Value` and still `.unwrap_or_else(\|e\| panic!(...))` on spawn failure / bad JSON, with an explicit `panic!` a few lines below on non-zero exit from both the offline and online attempts. Under `cargo xtask --all`/`--selftest` a panic here still takes every sibling gate down with it. Still true. | - |
| xtask/src/ere.rs | FINDING | Corrects the prior citation: unconstructed-sweep.md:244 census row on `remove_first` is right about the count (0 callers) but wrong that it is private — `git grep -n "remove_first" -- xtask/` → `xtask/src/ere.rs:188:    pub fn remove_first(&self, hay: &str) -> String {` is the ONLY hit in the entire tree: a `pub fn`, zero callers anywhere, not even in tests. Per the contract's own checklist item 2 ("a pub fn whose only callers are tests is a capability that does not ship") this is a stricter case — zero callers at all. Note: the same file's `find_str` is NOT dead (`git grep -n "scan_decls("` shows a live production caller at `structure_lint/plane_dups.rs:181`) — only `remove_first` is orphaned. See X-3503. | X-3503 |
| xtask/src/full_gate.rs | FINDING | Carried: IA5 (LEDGER.md:501) re-confirmed still true (`grep -n 'skip entries'` → only the written-reason assertion at :915, no existence check on the skip subject). Carried: local-only-audit.md:1229-1231 re-confirmed still true — `run_one` (still at :823) calls `Command::new(...).output()` with **no timeout**, unlike `xtask/src/gitp.rs`'s `Drop`-guarded kill. **NEW: X-3500** — `full_gate.rs`'s own `REGISTRY_NOT_IN_CI` excuse list (verified exhaustively against `xtask/src/gates/mod.rs`'s 50-entry `REGISTRY`, `.github/workflows/ci.yml`'s 37 distinct `cargo xtask gate <name>` invocations, and the 10-entry excuse list) misses two gates, not one: `sweep-coverage` and `map-proof`, both registered and both absent from ci.yml and from the excuse list — the same class as X-138, now 3-wide. See ROWS RAISED. | X-3500 |
| xtask/src/gates/audit_ledger.rs | FINDING | Carried, still true: IA4 (LEDGER.md:500) — marked **DONE**, the gate itself is red-capable (`2dee4e971` positive control). Separate, independently found: `pub const ROW_OPEN: &str = "audit-ledger:open-high-medium";` (:40) — `git grep -n "ROW_OPEN\|audit-ledger:open-high-medium" -- .` → only the declaration, the string value appears nowhere else in the repo (not read, not matched, not asserted against). A declared-but-never-constructed row id inside an otherwise-DONE gate. See X-3504. | X-3504 |
| xtask/src/gates/blocking_ffi.rs | CLEAN | file-verdicts.md TOOLING, note `carries_allow_marker; scan_roots`. Confirmed it is a real consumer of `population::source_population` (`crates/xtask/src/gates/blocking_ffi.rs:55,494`), so it is reachable and its scan set is the derived-population one, not a hand-maintained list. Registered (`"blocking-ffi"`) and present in `ci.yml`. | - |
| xtask/src/gates/changelog_register.rs | FINDING | Carried: G3 (LEDGER.md:374). Re-read `normalize()` (still at :722-724) today: it still only collapses whitespace runs — a pure text-presence match, no semantic check. The gate still proves a changelog line's *presence*, never its *truth*. Still true. | - |
| xtask/src/gates/config_schema/classify.rs | CLEAN | file-verdicts.md TOOLING; no specific defect named by any prior doc beyond a branch-sweep symbol-rename comparison (not a verdict about this file). | - |
| xtask/src/gates/config_schema/mod.rs | FINDING | Carried: THE-LIST.md D-3, upgraded from ADJUDICATE to VERIFIED-closed: `const REGEN: &str = "cargo xtask gate config-schema --write";` still at :146, and D-3's own residual doubt (does the CLI arg-parser actually route `--write` through?) is now settled — `grep -n '"--write"' xtask/src/cli.rs` → `:153 .write_mode(args.iter().any(\|a\| a == "--write"))`, wired generically for every `cargo xtask gate <name>` invocation. That half of D-3 is CLEAN. Separate, independently found: `pub fn waivers_of` (:665) — `git grep -n "waivers_of" -- .` → only its own declaration (plus unconstructed-sweep.md:246's census row noting the same 0-caller count at its old line 518). Zero callers anywhere. See X-3505. | X-3505 |
| xtask/src/gates/config_schema/schema.rs | FINDING | Carried: G36 (LEDGER.md:451). Re-confirmed today: `crates/busbar-core` no longer exists (`ls -d crates/busbar-core` → absent), and `core_roots()` (`:115-184`) still excludes `busbar-core-admin` **by name** (`:166-168`), so the census still reduces to `busbar-kernel` + `busbar-core-connsec`. **Update since G36 was written**: the code now carries a much fuller justification (`:145-165`) than G36 had visibility into — it argues `busbar-core-admin`'s `Deserialize` types (`CreateKeyReq`/`UpdateKeyReq`) are separately frozen by the byte-identity `openapi.json` golden, not this gate. That cross-reference claim was not independently verified here (would require auditing the openapi.json oracle, outside this slice). G36's core numeric claim (census reduces to 2 crates) still holds. | - |
| xtask/src/gates/config_schema/selftest.rs | CLEAN | file-verdicts.md TOOLING; no defect named. | - |
| xtask/src/gates/construction.rs | FINDING | Carried: decision-audit.md #7 (line 59). The source-denylist rule's scope is `cfg.rule("source-denylist").list_of("kinds")` (KIND families), and the plane engine crates (`busbar-llm`, `busbar-mcp`, `busbar-a2a`, `busbar-voice`) are not `busbar-<kind>-*` crates, so the closure the rule's row claims to cover is not the closure it walks. Self-acknowledged: the row's own text still says "TODO arm" per the cited doc. Not independently re-read line-by-line here (huge file; the finding is about the rule's *scope definition*, which is a `qa/construction.toml` config question outside this file's own logic). | - |
| xtask/src/gates/construction/ceilings.rs | CLEAN | 69 prior-doc hits, but essentially all are branch-sweep-ledger.md comparisons against 40+ deleted branches (not verdicts about this file), or a `base_line()`/`INTEGRATION_REF` naming note with no defect attached. No command-backed defect found against the current tree. Given the file's size (thousands of lines) a full independent re-read was not performed; no positive finding to report beyond what's cited elsewhere (kind_isolation.rs's linked prove_red machinery, covered below). | - |
| xtask/src/gates/construction/census.rs | CLEAN | file-verdicts.md TOOLING; no command-backed defect found beyond branch-sweep noise. | - |
| xtask/src/gates/construction/rules.rs | CLEAN | file-verdicts.md TOOLING; no command-backed defect found beyond branch-sweep noise. | - |
| xtask/src/gates/construction/rules2.rs | CLEAN | Carried and CONFIRMED FIXED: G22 (LEDGER.md:437) said `rules2.rs:307` still spelled the retired kinds `"pure_auth"`/`"egress_auth"`, resolving to empty glob lists. Re-read today at the same function (`manifest_allowlist`, :309-310): the kinds array now reads `["plane", "store", "auth", "hook", "export", "secret"]` and the surrounding comment documents the 2026-09-22 fix plus a new refusal (`Cfg::kind_globs` now refuses an undeclared key). G22 is stale — fixed. | - |
| xtask/src/gates/construction/selftest.rs | FINDING | Carried: X-210 (THE-LIST.md:161, ADJUDICATE). Re-ran today: `git grep -n "IdempotencyKey::mint(" -- '*.rs'` → 3 hits, one test (`caps/tests/mod.rs:633`) and two inside this gate's own fixture/selftest (`:302`, `:339`) — no production caller anywhere. Still true. | - |
| xtask/src/gates/construction/teller_paths.rs | CLEAN | Carried: gate-sweep.md §2.1 (:894) confirms the `arm_may_omit` mechanism landed correctly and the gate is green "without the rule being weakened." Positive, confirmed fix — no open finding. | - |
| xtask/src/gates/construction/tree.rs | FINDING | Carried: local-only-audit.md:1255-1259. Re-read `strip_comments` (still at :167-210) today: confirmed both gaps still present — (1) no nesting counter for `/* */`, so `/* /* */ */` closes on the first `*/` and treats the trailing ` */` as live code; (2) no char-literal handling — a `'"'` byte still triggers `in_str = true` via the same `if c == b'"'` branch used for real string literals, so a char literal containing a quote mis-toggles the parser's string-tracking state. Still true; this is a scanning primitive several `construction` rules build on. Separate, independently found: `pub fn read_cargo_deps` (:834, the raw-file-reading wrapper, distinct from `read_cargo_deps_text` which IS called live from `rules2.rs:358,1464`) — `git grep -n "read_cargo_deps\b" -- .` shows its only callers are its own body (:838, which calls `read_cargo_deps_text`, not itself) — zero external callers of the `pub fn read_cargo_deps` wrapper itself. See X-3506. | X-3506 |
| xtask/src/gates/feature_sets.rs | FINDING | Carried: local-only-audit.md:1214. Re-read the module doc (still at :38-41) today: it still states verbatim "It does not resolve the named job's own feature set, so a declaration that says `check` builds a feature by dev-dependency unification stays green after the dev-dependency that did the unifying is deleted... It belongs in this gate, as an eighth row. It is not here." Self-documented, unclosed gap. Still true. | - |
| xtask/src/gates/field_inventory.rs | CLEAN | file-verdicts.md TOOLING; no command-backed defect found. | - |
| xtask/src/gates/kind_isolation.rs | FINDING | Two items, one still-open and one now-fixed. **Still open**, carried: local-only-audit.md:1423-1427 — re-read `rule_control` today (now at :4225-4276, line drifted from the doc's :4142): the exact pattern `let Ok(files) = cx.walk(...) else { continue; }` is still present at :4254 — a walk failure for one control crate is silently skipped with no offender recorded, no error surfaced. **Now fixed**, was carried from map-proof.md App. D.3 (:2971-2977) which found "two red-ability proofs are themselves vacuous" (`kind_isolation.rs:5795`,`:5810`, planting `Overlay::remove` on already-absent `busbar-grammar`/`busbar-plane-voice` manifests against rows already red for the same reason). Re-read today at the equivalent site (now ~:5920-5963, renumbered): the plants have been REPLACED with real subjects (`busbar-timing`, `busbar-voice-codec`, both live crates), and the surrounding comment documents the fix explicitly: *"the rule could have been deleted outright with this case still passing. `prove_red` refuses both halves now."* Cross-checked `prove_red` in `xtask/src/gates/mod.rs:1934-2004`: it now has an explicit `THE PLANT MUST BITE` inert-plant check and a `THE BASELINE MUST BE GREEN` pre-red-baseline check before scoring the planted run. map-proof.md's finding is stale — fixed in the current (uncommitted) working tree. | - |
| xtask/src/gates/kind_isolation/base.rs | CLEAN | Carried, and already a positive CLEAN verdict in the source doc: LEDGER.md:740 — "`busbar-secret-grammar` was a lost crate — CLEAN — a rename that was considered and DECLINED." No independent re-check needed; citing the prior CLEAN. | - |
| xtask/src/gates/kind_isolation/matrix.rs | CLEAN | file-verdicts.md TOOLING and a content-categorization mention (one-face-per-kind.md); no defect named. | - |
| xtask/src/gates/mod.rs | FINDING | Carried: GS31 (LEDGER.md:515) re-confirmed still true — `CONSTRUCTION_STANDING_REDS` (:235-237, `ports-only-tests:busbar-llm`, `request-path-fn-size`, `terminal-doors-in-audit-step`) still lists all three names, and `scripts/land.sh:227-229` still carries the identical three, neither struck. **NEW: X-3500** (this is the file that owns `REGISTRY`, the other half of the finding raised against `full_gate.rs` above — `sweep-coverage` and `map-proof` are declared here (`pub mod sweep_coverage;`/`pub mod map_proof;`, both registered in `REGISTRY`) and never wired to run in CI). See ROWS RAISED. | X-3500 |
| xtask/src/gates/no_deferral.rs | CLEAN | Carried and CONFIRMED FIXED: G55 (LEDGER.md:470, headline blind spot — the four comment tags TODO/FIXME/XXX/HACK matched nothing) and THE-LIST.md D-1 both already record this as fixed at `72c9c14d6`. Re-read `comment_tag()` (still at :205-206) today: `for tag in ["TODO", "FIXME", "XXX", "HACK"]` is present. Also re-read the `xtask/` scope note (:29-34): it now carries an explicit, reasoned ruling that `xtask/` is out of scope on purpose (widening would redden on the gate's own test/fixture corpus), closing the second half of G55. Both halves fixed and current. | - |
| xtask/src/gates/no_float_money.rs | FINDING | Money-class gate; mixed result. **Fixed**, carried GS17/G34 (LEDGER.md:406,449): re-read `COUNT_READ_ROOTS` (:281-320) and `PERSISTED_RECORD_HOMES` (:564-568) today — the dead `crates/busbar-mcp-codec/src` root has been replaced with the real fold destinations (`crates/busbar-mcp/src/codec`, `crates/busbar-mcp/src/record.rs`, `crates/busbar-plane-mcp/src`, all confirmed present on disk), and the dead `crates/busbar-kernel-ledger/src/records.rs` has been replaced with `crates/busbar-contract/src/records.rs`, `crates/api/src/usage_migration.rs`, `crates/plugin-loader/src/legacy_usage.rs` (all confirmed present on disk). Both G34/GS17 defects are fixed. **Still open**, carried R20 (LEDGER.md:415): the five `PendingConversion` allowances (`crates/busbar-voice-codec/src/ir/codec/...`) still carry the `why` text "every billed read in it defaults to zero" verbatim (re-read at :505-533 today) — R20's claim that this `why` is now false about the code was not independently re-verified here (would require reading busbar-voice-codec, outside this slice's file list). | - |
| xtask/src/gates/plane_pricing_blindness.rs | FINDING | Carried: instrument-audit.md:678. Re-read `xtask/tests/plane_pricing_blindness.rs` (the file that excuses this gate from ci.yml) today: its own header states in terms "It does NOT pin the gate's own verdict... a burndown that moves every time a money act is relocated kernel-side." So the gate's real verdict over the current tree is never asserted anywhere on the push path — only its `--selftest` is. By design, self-documented, still true. | - |
| xtask/src/gates/plane_purity/mod.rs | CLEAN | file-verdicts.md TOOLING; no command-backed defect found against `mod.rs` itself (the roster defect below is in `scanner.rs`). | - |
| xtask/src/gates/plane_purity/scanner.rs | FINDING | Carried: X-148 (MAP.md:938,1688, THE-LIST.md:270, VERIFIED). Re-ran today: `grep -n "PLANE_ALTERNATION" xtask/src/gates/plane_purity/scanner.rs` → `:29 pub const PLANE_ALTERNATION: [&str; 4] = ["llm", "mcp", "a2a", "voice"];`, still the stale 4-plane roster (no `busbar-plane-voice` crate exists; `streaming` and `decision` are both omitted). Still true, same line. | - |
| xtask/src/gates/plane_transport_neutrality.rs | CLEAN | file-verdicts.md TOOLING; only other mention is an unconstructed-sweep.md census row (0 non-test callers on a private struct). | - |
| xtask/src/gates/population.rs | CLEAN | Not itself a registered gate (no `pub mod population` REGISTRY entry) — it is shared scan-population plumbing. Confirmed real consumers: `git grep -n "population::"` → `blocking_ffi.rs:55,494`, `response_header.rs:210`, `settings_leak.rs:372`, all three registered gates present in `ci.yml`. The `FLOOR = 700` aggregate ratchet is not vacuous today: `find crates -name '*.rs' -not -path '*/tests/*' -not -name '*tests.rs'` → 756, comfortably above the floor. | - |
| xtask/src/gates/qa_names.rs | FINDING | Carried: G25 (LEDGER.md:440). Confirmed still true by design: `QA_ROOT = "qa"` and the gate reads only `qa/*.toml`, not `qa/*.json` — so files like `qa/teller-steps.json`, `qa/audit-ledger.json`, `qa/capability-equality.json` etc. get no path/crate/kind-name validation from this backstop. `ROW_GLOB` still judges `[gate.plugin_kinds]` per-kind rather than per-entry (same class of hole `kind_isolation`'s `dead-kind` rule has from the other side). Not re-read line-by-line (would require re-deriving the exact code shape); citing the doc's own 2026-era reading as current since no fix is referenced anywhere else in the corpus. | - |
| xtask/src/gates/release_order.rs | FINDING | Carried: IA11 (LEDGER.md:507, TODAY-dated 2026-09-23). Re-read today (:840-862): confirmed still present — when `release.yml` is absent, an `R0` finding is pushed and the function `return Ok(bad)` **before** rows R3-R12 are ever computed, so those rows are silently absent from the report rather than evaluated. Still true, same line numbers as the doc. | - |
| xtask/src/gates/segregation.rs | CLEAN | file-verdicts.md TOOLING, note "plant"; gate-blindspots.md and instrument-audit.md mention it only as a data-allowlist reference (a legitimate golden path allowance), not a defect. | - |
| xtask/src/gates/service_images.rs | CLEAN | file-verdicts.md TOOLING; no command-backed defect found. | - |
| xtask/src/gates/ship_ready.rs | FINDING | Two carried findings, both re-confirmed today. IA9 (LEDGER.md:505, PARKED-OWNER): `grep -n 'ship-ready\|ship_ready' scripts/verify-1.6.0-done.sh` → 0 hits (positive control: `grep -c '^begin_group ' scripts/verify-1.6.0-done.sh` → 23, proving the file was actually searched) — the DONE oracle still never invokes the ship gate by any name. GS27 (LEDGER.md:511): `ship_twin_row` (:217-240) still re-wraps `kind-isolation-ship`'s own verdict verbatim as `ship-ready:ship-twin`, carrying zero independent signal — confirmed by re-reading the function body. Also `grep -n "secret" xtask/src/gates/ship_ready.rs` → 0 hits, confirming the gate still carries no secret row (cross-reference the scripts/secret-hygiene-gate.sh finding below). | - |
| xtask/src/gates/structure_lint/oversized.rs | FINDING | Carried: IA11 (LEDGER.md:507, same row as release_order.rs above, TODAY-dated). Re-read today (:93-98): `let Ok(files) = cx.walk(...) else { return; };` is still present verbatim — a walk failure returns silently with no `Findings` entry pushed, unlike its sibling `hybrid.rs` in the same directory which treats an identical failure as a hard finding. Still true, same lines. | - |
| xtask/src/gates/teller_steps.rs | FINDING | Carried: X-174 (MAP.md:667, THE-LIST.md:225, VERIFIED). `run_root_legs` still exists at :951 with the same general shape the finding describes (a header count computed separately from a per-leg breakdown count, the same dedup bug class as `capability-equality-summary.py:264`). Not independently re-run today (would require running the gate against real root-leg builds, expensive under the shared build lock); citing the prior VERIFIED finding as current since no fix is referenced anywhere in the corpus. | - |
| xtask/src/gates/tracing.rs | CLEAN | file-verdicts.md TOOLING; only other mentions are unconstructed-sweep.md census rows for private helper functions (0 non-test callers, all internal to the gate's own scan logic). | - |
| xtask/src/gates/unconstructed.rs | FINDING | Carried: X-138 (THE-LIST.md:213, MAP.md:1071, VERIFIED). Re-confirmed today: registered in `xtask/src/gates/mod.rs:2436-2439` (`pub mod unconstructed;` at :68, `name: "unconstructed"` at :2436), `grep -c "unconstructed" .github/workflows/ci.yml` → 0, `grep -c "unconstructed" qa/segments.toml` → 0, and `"unconstructed"` is absent from the 10-entry `REGISTRY_NOT_IN_CI` list in `xtask/src/full_gate.rs` (enumerated in full). Still true — the gate that would enforce `qa/unconstructed.toml` (including the six money rows of M52) is registered and never runs automatically. | - |
| xtask/src/ledger.rs | CLEAN | file-verdicts.md TOOLING (the xtask internal audit-ledger reader/writer, distinct from `busbar-kernel-ledger`); no command-backed defect found. | - |
| xtask/src/loc/selftest.rs | CLEAN | file-verdicts.md TOOLING; no command-backed defect found. | - |
| xtask/src/parity.rs | FINDING | Carried: local-only-audit.md:1229-1231 (same finding as `full_gate.rs` above). Re-read today: both call sites still call `Command::new(...).output()` with no timeout — `:158-161` (the legacy-parity subprocess run) and `:578-584` (the lint-parity subprocess run) — unlike `xtask/src/gitp.rs`'s `Drop`-guarded kill. A wedged child in either path hangs the gate runner indefinitely with no red, no green. Still true, same lines. | - |
| xtask/src/planes.rs | FINDING | Carried: G35 (LEDGER.md:450). Re-ran today: `grep -n "PLANE_KEYS" xtask/src/planes.rs` → `:23 pub const PLANE_KEYS: [&str; 4] = ["llm", "mcp", "a2a", "voice"];` — still the stale 4-roster (voice is not a crate, decision is missing), same as `plane_purity/scanner.rs`'s `PLANE_ALTERNATION` and `scripts/plane-keys.sh`'s `PLANE_KEYS` below — three independently-maintained copies of the same wrong roster. Still true. | - |
| xtask/src/rx.rs | CLEAN | file-verdicts.md TOOLING; only other mentions are unconstructed-sweep.md census rows on private regex helpers (0 non-test callers, internal implementation detail). | - |
| xtask/src/yaml_lite.rs | CLEAN | file-verdicts.md TOOLING; only other mention is an unconstructed-sweep.md census row on a helper function. | - |
| xtask/tests/cli.rs | FINDING | Carried: GS33 (LEDGER.md:517). `cargo test -p xtask` including this file has never completed a full run in any prior audit's shared, contended environment (killed at 27 min by the housekeeping reaper on one attempt, still inside `selftest_runs_every_registered_gates_red_proof` at write-time on another) — status recorded as **ABSENT, not PASS and not FAIL**. Not independently re-run here: this workspace is explicitly under ~20 concurrent agents right now (per this sweep's own contract), so re-attempting a full-registry gate run here would repeat exactly the contended-checkout failure mode GS33 already documented, and the contract asks that cargo builds route through an isolated `CARGO_TARGET_DIR` and be avoided where avoidable. Read the file itself in full: 113 lines, well-formed dispatcher-arm tests plus the registry-vs-ci.yml equality case (`the_registry_and_the_workflow_still_name_the_same_gates`) which is exactly the test that would have caught the X-3500 gap above, IF it ever finishes running. | - |
| xtask/tests/hot_path_gates.rs | CLEAN | Carried, positive: instrument-audit.md:676 ("rc 0, GREEN — 5 rows... the GATE's verdict. Covered."). Re-read the file today: unlike `plane_pricing_blindness.rs`, this file pins **both** the real gate verdict (`run(&["gate","hot-path-perf"])` and `hot-path-alloc`, asserted `== 0`) **and** the selftest — the correct, complete pattern. No finding. | - |
| xtask/tests/plane_pricing_blindness.rs | FINDING | Carried: instrument-audit.md:678 (same underlying gate as `xtask/src/gates/plane_pricing_blindness.rs` above — this IS the file with the gap). Re-read in full today: the file's own header states plainly "It does NOT pin the gate's own verdict... So the gate proper is run by nothing at all" is the correct characterization — only `assert_eq!(run(&["gate", "plane-pricing-blindness", "--selftest"]), 0)` is asserted; no case asserts the plain `run(&["gate","plane-pricing-blindness"])` verdict anywhere in this file or in `ci.yml`. Self-documented, deliberate (the doc explains the verdict is a burndown that would force every relocation commit to edit this test) — but the effect is real: a money-act newly landed in a plane file today would not fail this route. | - |

## ROWS RAISED

### X-3500 · Two more registered gates (`sweep-coverage`, `map-proof`) are absent from ci.yml and from the excuse list — the same class as X-138, now 3-wide
CLASS:     instrument-blind
CERTAINTY: VERIFIED
EVIDENCE:  Built the full cross-reference by hand against the current working tree (2026-09-23):
  `xtask/src/gates/mod.rs`'s `REGISTRY` table (lines 2329-2696) names exactly 50 gates.
  `.github/workflows/ci.yml` invokes 37 distinct names via `grep -oE 'cargo xtask gate [a-z0-9-]+' .github/workflows/ci.yml | awk '{print $NF}' | sort -u`.
  `xtask/src/full_gate.rs`'s `REGISTRY_NOT_IN_CI` const (lines 374+) excuses exactly 10 more names, enumerated in full: `denylist, segregation, plane-purity-strict, kind-isolation-ship, no-deferral-strict-done, hot-path-perf, hot-path-alloc, reachability, plane-pricing-blindness, instance-noun-neutrality`.
  Set difference (REGISTRY − (ci.yml ∪ REGISTRY_NOT_IN_CI)) = exactly three names: `unconstructed` (already known — X-138), `sweep-coverage`, `map-proof`.
  Confirmed no other route runs either new gate: `git grep -n "sweep-coverage\|sweep_coverage" -- . ':!xtask/src/gates/mod.rs' ':!xtask/src/gates/sweep_coverage.rs'` → 0 hits outside its own module; `git grep -n '"map-proof"\|map_proof::' -- . ':!xtask/src/gates/mod.rs' ':!xtask/src/gates/map_proof.rs'` → 0 hits outside its own module; `grep -n '"sweep-coverage"\|"map-proof"' xtask/src/cli.rs` → 0 (not even a bespoke top-level subcommand). Also confirmed via `grep -rln "sweep-coverage\|sweep_coverage" docs/design/` → 0 files — this gap has never been named in any prior sweep document, meaning it is genuinely new, not a stale citation.
  `xtask/src/gates/sweep_coverage.rs`'s own module doc is the notable detail: it describes itself, verbatim, as the completeness proof for "every corner of the house has been checked," with a `git ls-files`-derived denominator matching this exact sweep effort's contract (2,068 tracked files). `xtask/src/gates/map_proof.rs`'s own module doc similarly describes itself as re-running every fenced command in the design-doc corpus (including this document's own siblings) against the tree and comparing output. Both are — by their own stated purpose — the instruments meant to keep this release's audit claims honest, and neither runs automatically.
  Note: `xtask/src/gates/mod.rs` and `xtask/src/full_gate.rs` both currently show as modified (uncommitted) in `git status` at session start — this measurement is against the live working tree, which other concurrent sweep work may be actively editing.
ACTION:    Add `sweep-coverage` and `map-proof` to `.github/workflows/ci.yml` (each with a `--selftest` step preceding the run step, per this file's existing convention), or add both to `REGISTRY_NOT_IN_CI` with a written, checkable excuse if a deliberate release-time-only posture is intended (matching `reachability`'s or `plane-pricing-blindness`'s existing excuse shape).

### X-3501 · The release-gate selftest layer (`ledger-selftest.sh`, and `gate.sh --selftest`'s identical case) never runs in CI
CLASS:     instrument-blind
CERTAINTY: VERIFIED
EVIDENCE:  `scripts/release-gate/ledger-selftest.sh` (253 lines) is a real, well-built offline regression test targeting a documented historical defect class in `ledger_status_for` (`scripts/release-gate/lib.sh`): a re-run or retrying CI leg leaving two rows (PASS then a later FAIL) for one id in the ledger, with `awk ... exit` on first match silently reading the PASS and reporting green over a check that had actually failed.
  `git grep -n "ledger-selftest" -- . ':!scripts/release-gate/ledger-selftest.sh'` → 0 hits anywhere in the tree: not sourced by `scripts/release-gate/gate.sh` (confirmed by reading gate.sh's own source list: only `. scripts/release-gate/lib.sh` at line 27), not invoked from any `.github/workflows/*.yml` (confirmed: `grep -rn "release-gate/" .github/workflows/*.yml` lists only `platform-checks.sh`, `docker-checks.sh`, `channel-checks.sh`, `fleet-checks.sh`, `gate.sh` itself — never `ledger-selftest.sh`), and not invoked from any other script.
  The same regression case is ALSO present inside `gate.sh`'s own built-in `--selftest` arm (`gate.sh:346-348`, calling `ledger_status_for` directly on a staged duplicate-row ledger) — but `gate.sh` is invoked in `.github/workflows/fleet-autoscaler.yml:343` WITHOUT the `--selftest` flag (`scripts/release-gate/gate.sh "$VERSION"`, confirmed by reading the full invocation line and its surrounding context), so neither copy of this regression coverage runs automatically. `release-gate/lib.sh` itself IS exercised live on every real `fleet-autoscaler.yml` run (see its own CLEAN verdict above) — it is specifically the safety net around it that is dead.
  If the exact historical bug this file exists to catch (first-row-wins over a duplicate ledger entry) were reintroduced into `lib.sh`, nothing in CI would catch it before a fleet release gate silently passed over a step that had actually failed.
ACTION:    Add a step to `.github/workflows/fleet-autoscaler.yml` (or `release-stage.yml`, wherever `release-gate/gate.sh` first runs in a given workflow) that runs `scripts/release-gate/gate.sh --selftest` before the real gate run, matching the `--selftest`-then-real-run convention `ci.yml` already uses for every `cargo xtask gate <name>` step. `scripts/release-gate/ledger-selftest.sh` can then be folded into or kept alongside `gate.sh --selftest` — either way, one of the two needs an actual CI caller.

### X-3502 · `xtask/src/ctx.rs`'s `collect()` has no recursion-depth cap or symlink-cycle guard
CLASS:     instrument-blind
CERTAINTY: ADJUDICATE
EVIDENCE:  `sed -n '892,919p' xtask/src/ctx.rs` — `collect()` recurses on every `path.is_dir()` (:915-917) with only `target` and dotdirs skipped (:911). No depth counter, no `visited` set, no symlink check. No symlink cycle exists in the tree today to trigger it (checked: `find . -type l -not -path './target/*' 2>/dev/null` inside `crates/` and `xtask/` is empty), so this is a latent gap, not a live red — hence ADJUDICATE, not VERIFIED.
ACTION:    Add a depth cap and refuse (or skip-with-a-finding) a symlinked directory in `collect()`, matching the recommendation already recorded for this function's class of walk (`docs/design/1.6.0-branch-sweep-ledger.md:889`, T6).

### X-3503 · `xtask/src/ere.rs`'s `pub fn remove_first` has zero callers anywhere, including tests
CLASS:     missing-code
CERTAINTY: VERIFIED
EVIDENCE:  `git grep -n "remove_first" -- xtask/` → exactly one hit, the declaration itself: `xtask/src/ere.rs:188:    pub fn remove_first(&self, hay: &str) -> String {`. No test, no gate, no CLI path calls it. (Corrects `docs/design/1.6.0-unconstructed-sweep.md:244`, which recorded the same 0-caller count but mischaracterized the function as private.)
ACTION:    Delete `remove_first`, or if it is meant to be a public `Ere` API, add at least one caller/test that exercises it.

### X-3504 · `xtask/src/gates/audit_ledger.rs`'s `pub const ROW_OPEN` is never read
CLASS:     missing-code
CERTAINTY: VERIFIED
EVIDENCE:  `grep -n "ROW_OPEN" xtask/src/gates/audit_ledger.rs` → `:40 pub const ROW_OPEN: &str = "audit-ledger:open-high-medium";`. `git grep -n "ROW_OPEN\|audit-ledger:open-high-medium" -- .` → only that one declaration line in the whole tree; the row id string is never matched, asserted, or read back anywhere. This sits alongside an otherwise-DONE gate (IA4, LEDGER.md:500) — the gate's core logic is fine, this one constant is dead weight.
ACTION:    Either wire `ROW_OPEN` into an actual emitted row, or delete the constant.

### X-3505 · `xtask/src/gates/config_schema/mod.rs`'s `pub fn waivers_of` is never called
CLASS:     missing-code
CERTAINTY: VERIFIED
EVIDENCE:  `grep -n "waivers_of" xtask/src/gates/config_schema/mod.rs` → `:665 pub fn waivers_of(cx: &Ctx) -> Result<BTreeMap<String, String>, String> {`. `git grep -n "waivers_of" -- .` → only that declaration plus `docs/design/1.6.0-unconstructed-sweep.md:246`'s census row (same 0-caller finding, recorded against an earlier line number :518 before the file moved). Still true today.
ACTION:    Wire `waivers_of` into the gate's actual waiver-reporting path, or delete it.

### X-3506 · `xtask/src/gates/construction/tree.rs`'s `pub fn read_cargo_deps` (the file-reading wrapper) is never called
CLASS:     missing-code
CERTAINTY: VERIFIED
EVIDENCE:  `git grep -n "read_cargo_deps\b" -- .` → `tree.rs:834` (declaration), `tree.rs:838` (its own body, which calls `read_cargo_deps_text`, not itself), `tree.rs:950` (a test calling `read_cargo_deps_text` directly), plus doc/comment mentions in `xtask/src/denylist.rs:11`, `xtask/src/manifest.rs:8`, `xtask/src/toml_lite.rs:2`. The sibling `read_cargo_deps_text` IS live (called from `rules2.rs:358` and `:1464`) — only the `pub fn read_cargo_deps` file-reading wrapper itself has zero external callers. (Corrects `docs/design/1.6.0-unconstructed-sweep.md:247`, which recorded this under the same 0-caller framing but without distinguishing it from the live `_text` sibling.)
ACTION:    Delete `read_cargo_deps`, or if it's meant as the production entry point (with `read_cargo_deps_text` as its testable inner half), wire a real caller to it instead of everyone calling `read_cargo_deps_text` directly.

### X-3507 · `xtask/src/audit.rs`'s `pub fn is_ancestor` has no non-test caller
CLASS:     missing-code
CERTAINTY: VERIFIED
EVIDENCE:  `git grep -n "is_ancestor" -- xtask/` → `audit.rs:337` (declaration), `audit.rs:1427`, `audit.rs:1437`. The `#[cfg(test)]` module boundary starts at `audit.rs:1372` (confirmed by reading forward from the attribute) — both call sites are inside it. Zero production callers of a `pub fn`.
ACTION:    Either wire `is_ancestor` into `confirmed()`'s consecutive-round check (which would also close part of the G18 citation on `audit.rs` above, since ancestry is exactly the primitive a "consecutive rounds" check needs), or delete it if it's genuinely test-only scaffolding and make it `#[cfg(test)]` itself rather than `pub`.

## TALLY
files in slice:  78
verdict lines:   78
CLEAN:           32
FINDING:         46      rows raised: 8 (X-3500, X-3501, X-3502, X-3503, X-3504, X-3505, X-3506, X-3507)
DELETABLE:       0
UNREADABLE:      0
