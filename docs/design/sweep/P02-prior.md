# P02-prior — sweep of 83 files previously *mentioned* (not verdicted) by a prior 1.6.0 sweep document

Per CONTRACT.md: every file below was named somewhere in `docs/design/1.6.0-*.md` prose, which is why
no fresh slice claimed it. Being named is not a verdict. For each file this report either (a) cites a
prior document that already reached a real, command-backed verdict and re-confirms it is still true at
HEAD with a fresh command, or (b) does the sweep fresh where the prior mention was bare. Money findings
are PARK regardless of how well-evidenced, per the task's standing instruction.

X-id block: X-3100–X-3199.

| FILE | VERDICT | EVIDENCE | ROWS |
|---|---|---|---|
| crates/busbar-kernel-budget/src/price.rs | FINDING | Prior real verdicts (LEDGER M1/M2/M21/M40, THE-LIST X-237, decision-audit #42/#77(8), money-sweep A1/A4/B8/D-7) predate today's fix commits 8f083cc38/fdb6c648d. Re-verified fresh at HEAD: `./target/debug/xtask gate no-float-money` → still RED at price.rs:76-79 (f64 params); `git grep -n "with_card(" -- crates/` → 0 production callers; X-237's literal claim ("derive paths price it at 0") is now stale — `derive_spend_cents` returns `i64::MAX` (blocks) for an unpriced model, not 0. | X-3100, X-3101, X-3102 |
| crates/busbar-kernel-budget/src/window.rs | FINDING | THE-LIST X-253 / file-verdicts P3 (PARK, money): `window_end`/`civil_from_days`/`days_from_civil` byte-identical to `busbar-kernel/src/governance/mod.rs`. Re-verified: `diff <(sed -n '946,957p' governance/mod.rs) <(sed -n '84,95p' window.rs)` → no output (still byte-identical). Fresh: `git grep -n "is_known_window" -- '*.rs'` → 0 non-test/non-doc callers anywhere; not re-exported from `lib.rs`. | X-3103, X-3104 |
| crates/busbar-kernel-egress/src/arity.rs | FINDING | Sub-agent (chunk B) re-ran `git grep -n "on_primary_unavailable(" -- crates/` → only `tests/arity_tests.rs`; re-exported at `lib.rs:71` but zero external callers. | X-3105 |
| crates/busbar-kernel-egress/src/exhaustion.rs | FINDING | `git grep -n "exhaustion::" -- crates/ \| grep -v tests` → only `walk.rs:30`, itself only reachable through the dead `Egress::route` (see X-3106). | X-3106 |
| crates/busbar-kernel-egress/src/lib.rs | FINDING | unconstructed-sweep.md:656 (shape 4.2) reused + reconfirmed fresh: `git grep -n "\.egress\b" -- crates/busbar/src/root/kernel.rs` → 0 (field written once, never read); `git grep -n "\.route(" -- crates/ \| grep -v axum` → no `Egress::route` call; `git grep -n "\.floor()" -- crates/` → 0 hits anywhere. | X-3106 |
| crates/busbar-kernel-egress/src/pool.rs | FINDING | unconstructed-sweep.md:204 reused + reconfirmed: `git grep -n "position_of(" -- crates/` → only the declaration (`pool.rs:156`), no caller including tests. | X-3107 |
| crates/busbar-kernel-egress/src/ports.rs | FINDING | unconstructed-sweep.md:670-693 (shape 4C) reused + reconfirmed: `git grep -n "impl.*PermitHandle for\|impl.*Capacity for\|impl.*EgressAuth for\|impl.*Journal for\|impl.*Telemetry for" -- crates/` → all confined to `tests/harness.rs`. `Breaker`/`Admit` (a different trait in the same file) IS wired via `crates/busbar/src/root/adapters.rs:258`. | X-3106 |
| crates/busbar-kernel-egress/src/select.rs | FINDING | local-only-audit.md §E2 (real verdict, re-verified a 3rd time in that doc) reused + reconfirmed: `sed -n '409,427p' select.rs` — `Order::new`'s sticky branch never calls `lane_admissible`; `git grep -n "lane_admissible" -- select.rs` → 0 (positive control: `lane.rs:101` has the predicate, so this is a real, controlled zero). | X-3108 |
| crates/busbar-kernel-egress/src/tests/harness.rs | CLEAN | `grep -n "mod harness" tests/mod.rs` → `mod.rs:11`; consumed by `allocation_tests.rs`/`deadline_tests.rs`/`exhaustion_tests.rs`/`probe_tests.rs`/`walk_tests.rs`, all with real, falsifiable `assert!`/`assert_eq!`. | - |
| crates/busbar-kernel-egress/src/trust/lane.rs | FINDING | unconstructed-sweep.md:675-676,688-693 reused + reconfirmed: `git grep -n "impl.*BreakerView for\|impl.*LaneTable for" -- crates/` → all 5 hits are test doubles (own tests + downstream busbar test doubles). Positive control (`impl Breaker for BreakerAdapter`) confirms the grep methodology finds real impls when they exist. | X-3106 |
| crates/busbar-kernel-egress/src/trust/net.rs | FINDING | THE-LIST X-123a (VERIFIED) reused + reconfirmed fresh: `git grep -n "Denylist::new(" -- crates/` → every hit in `trust/tests/net_tests.rs`, none in production; `allows_all()` (unconstructed-sweep.md:205) also 0 callers. The static `METADATA_HOSTS` baseline still functions independently (not a total SSRF bypass), but the operator-configurable overlay is dead. | X-3109 |
| crates/busbar-kernel-egress/src/trust/order.rs | FINDING | unconstructed-sweep.md:677 (shape 4C) plus a new correction: `git grep -n "OrderingHook" -- crates/` → only decl/re-export/tests, zero production implementors or callers of `pick`/`reconcile_order`. `docs/design/1.6.0-config-surface.md:601,613` cites `order.rs:72` as a WIRED `hooks.<h>.priority` read site — traced and confirmed a name collision (`OrderingHook::priority()`, unimplemented in production) with the real, unrelated consumer at `crates/busbar-kernel/src/hooks/mod.rs`. | X-3110 |
| crates/busbar-kernel-egress/src/trust/tests/destination_tests.rs | CLEAN | 509 lines, 13 `#[test]` fns, 35 `assert*`; reached via `trust/mod.rs:97-99` `#[cfg(test)] mod tests`. | - |
| crates/busbar-kernel-egress/src/trust/tests/mod.rs | CLEAN | Registers 8 trust test submodules + shared fixtures; reached via `trust/mod.rs:97-99`. | - |
| crates/busbar-kernel-egress/src/trust/tests/order_tests.rs | CLEAN | 534 lines, 20 `#[test]` fns, 42 `assert*` — genuinely exercises the otherwise-dead `trust/order.rs` (the defect is production never reaching it, not that these tests are vacuous). | - |
| crates/busbar-kernel-egress/src/walk.rs | FINDING | `git grep -n "\bwalk(" -- crates/busbar-kernel-egress crates/busbar crates/busbar-llm \| grep -v tests` → only caller is `lib.rs:152` inside the already-dead `Egress::route`. | X-3106 |
| crates/busbar-kernel-egress/src/wire.rs | FINDING | unconstructed-sweep.md:141,206-207 reused + reconfirmed: `git grep -n "restrict_no_lane" -- crates/` and `invalid_body` → declaration only in both cases; two named `Shed` refusal reasons can never actually be produced by any code path. | X-3111 |
| crates/busbar-kernel-identity/src/admin.rs | FINDING | unconstructed-sweep.md:332-333 (census fact) promoted to a real finding: `git grep -n "admin_grants(\|kernel_verb_scope_satisfied(" -- '*.rs'` → only `tests/unit_tests.rs`; the live open-admin-posture decision is a separate, independent implementation at `crates/busbar-kernel/src/auth/mod.rs:1240 admin_scope_for` using a different `Grants`/`Scope` type from `busbar-contract::authz`. | X-3112 |
| crates/busbar-kernel-identity/src/cache.rs | FINDING | unconstructed-sweep.md:334 reused + extended: `Sha256Digest` (`cache.rs:84`) compiled in specifically for the `busbar` binary (`Cargo.toml:102` `features=["sha256"]`) but `git grep -n "Sha256Digest" -- '*.rs'` shows zero production constructions — `auth_bindings.rs:197` constructs `CredentialCache::new(busbar_api::sha256_hex)` instead, a separate function a test pins byte-equal to `Sha256Digest`. | X-3113 |
| crates/busbar-kernel-identity/src/carrier.rs | FINDING | THE-LIST X-207/X-208 (VERIFIED) + LEDGER S11 reused and reconfirmed fresh: `git grep -n "extract_client_token(" -- '*.rs'` → decl + own tests only; production uses `busbar-kernel/src/auth/mod.rs:669`'s independent copy; `busbar-kernel/Cargo.toml` still has no dependency edge on `busbar-kernel-identity`; `CallerToken(` (this crate's copy) constructed only in `tests/carrier_tests.rs`. | X-3114 |
| crates/busbar-kernel-identity/src/chain.rs | FINDING | LEDGER BR-S9 (OPEN) reused + reconfirmed: `git grep -lF keys_arm_view_tests HEAD` → 0 hits today, so the `is_open()` regression-test net named in BR-S9 is still absent from trunk even though `AuthChain` itself (`new`/`run_chain`/`is_open`) is genuinely live and wired (`busbar/src/main.rs:513`, `root/kernel.rs:833`). | X-3115 |
| crates/busbar-kernel-identity/src/egress_auth/mod.rs | FINDING | No prior real verdict — unconstructed-sweep.md only flagged one of six unconstructed public entry points, missing the rest because every symbol name (`decorate`, `substitute`, `lane_cross_check`, `EgressBody`) collides with an unrelated, live, same-named symbol elsewhere in the tree. Fresh sweep: `git grep -n "kernel_identity::egress_auth\|egress_auth::sigv4\|egress_auth::Scheme\|egress_auth::decorate\|egress_auth::substitute\|egress_auth::lane_cross_check\|egress_auth::continue_handshake" -- '*.rs'` (excluding this crate) → 0 hits; not re-exported from `lib.rs`; no egress crate depends on `busbar-kernel-identity`. A full second AWS SigV4 signer ships here and is never reached. | X-3116 |
| crates/busbar-kernel-identity/src/exchange.rs | FINDING | unconstructed-sweep.md:339 reused + extended: `is_bypassed`/`dispatch`/`BrowserAction`/`AUTH_TOKEN_PATH` all zero production references; the live `/auth/token` exact-match bypass and browser dispatch are independently reimplemented in `busbar-kernel/src/auth/{exchange.rs,token.rs}` and `router.rs`. | X-3117 |
| crates/busbar-kernel-identity/src/tests/mod.rs | CLEAN | `lib.rs:65-66` `#[cfg(test)] #[path="tests/mod.rs"] mod tests;` — reachable in test builds only, pure fixture file (also the sole `HeaderView` impl site counted under X-3114/carrier.rs). | - |
| crates/busbar-kernel-ledger/src/checkpoint.rs | FINDING | unconstructed-sweep.md rows 64/65 reused + reconfirmed: `git grep -n "\bshould_alarm(\|\bseal_as_of(" -- '*.rs'` (`\b` verified working via positive control) → only `tests/checkpoint_tests.rs`. Core API (`Checkpoint::seal`, `totals_for`, `body_hash_verifies`) IS wired: `seal` called at `migration.rs:442`, `totals_for`/`body_hash_verifies` called at `verify.rs:119,135` and `busbar/src/root/units_admin/mod.rs:1541`. | X-3118 |
| crates/busbar-kernel-ledger/src/cost/currency.rs | FINDING | THE-LIST X-242 / decision-audit #66 (PARK, money) reused: a full ISO-4217 `CurrencyCode` system ships while decision #66 (dated 2026-09-20, predates the module) says money should be unitless. Re-confirmed current: `CurrencyCode::USD` (`:30`), per-currency minor-unit exponents (`:60-62`) still required by `price`/`price_in_view`/`price_exact` (`cost/view.rs:340,353,369`). | X-3119 |
| crates/busbar-kernel-ledger/src/cost/mod.rs | CLEAN | Pure re-export aggregator (`mod currency; mod history; mod posting; mod project; mod rate; mod view;` + `pub use` block); `NANOS_PER_CENT`/`NANOS_PER_MICRO`/`MICROS_PER_CENT` constants match their cited call sites elsewhere in this report. | - |
| crates/busbar-kernel-ledger/src/cost/posting.rs | FINDING | LEDGER R78 (PARK, money) is now STALE/FIXED: `grep -n unpriced crates/busbar-kernel-ledger/src/cost/posting.rs` shows the hard-coded `unpriced: false` R78 cited is gone — current code computes `unpriced: !priced` (`:441`) and `unpriced: priced_fee.is_none()` (`:459`). Fresh finding: `cache_diverges`/`unpriced_classes` (posting.rs:145,221 — recompute.rs's own doc calls `cache_diverges`-equivalent detection "the whole of Verdict", the tamper-detection mechanism) have `git grep -n "\bcache_diverges(\|\bunpriced_classes("` → 0 non-test callers anywhere, including same-crate. | X-3120 |
| crates/busbar-kernel-ledger/src/cost/project.rs | FINDING | branch-sweep-ledger.md (PARK, money, "HALF LANDED") reused: `price.rs:212`'s hand-rolled `nanos / NANOS_PER_CENT` divide still has not been switched to call this file's currency-aware `minor_of` (`:29-32`); re-confirmed both sites unchanged at HEAD. No live divergence for USD today (both saturate/floor identically), but the structural duplicate remains. | X-3121 |
| crates/busbar-kernel-ledger/src/cost/rate.rs | FINDING | LEDGER M1's ceiling-off-by-one is FIXED: `nano_rate` now guards `v < 2.0_f64.powi(64)` (was `<= u64::MAX as f64`, which admitted `v==2^64` and saturated to `u64::MAX`) — re-confirmed at `rate.rs:49`. Two OPEN money items reused and reconfirmed still true: **M40** (`rate.rs:40-46`, `else { 0 }` — both too-large AND too-small configured rates price at free) and **M21** (`nano_rate(0.5005)` still rounds to 500 not 501 via f64 imprecision — the multiply-then-round logic at `:39` is unchanged). | X-3122, X-3123 |
| crates/busbar-kernel-ledger/src/cost/tests/rate_tests.rs | CLEAN | file-verdicts.md TEST verdict reused; reached via `#[cfg(test)] #[path="tests/mod.rs"] mod tests;` → `cost/tests/mod.rs` → `mod rate_tests;`. | - |
| crates/busbar-kernel-ledger/src/cost/tests/view_tests.rs | CLEAN | file-verdicts.md TEST verdict reused; same module chain; exercises `price_ledger`/`price_exact`/`price_in_view`/`with_whole`/`with_fee_count` with real falsifiable assertions. | - |
| crates/busbar-kernel-ledger/src/cost/view.rs | CLEAN | money-sweep.md M-10 (PARK, "THE ONE FUNCTION IS UNREACHABLE") is STALE: re-ran its own cited command fresh — `git grep -n "price_exact\|price_in_view\|price_ledger" -- crates/` now ALSO shows `crates/busbar-core-admin/src/v1/service.rs:274` (production, committed 2026-09-23 11:14:47) calling `price_in_view`, and `price()` (the un-suffixed alias, which M-10's grep pattern didn't cover) has called `crates/busbar/src/root/units_llm.rs:761` since 2026-09-20 (`git blame`) — `price()` internally calls `price_in_view→price_exact`, so the "unreachable" chain is in fact the live LLM-plane settlement figure. Also `with_whole`/`with_fee_count` (view.rs:209,218), flagged test-only by unconstructed-sweep.md rows 72-73, are now called in production at `service.rs:271,273`. `with_count`/`with_tier` remain genuinely test-only (low materiality — no plane currently varies tier or needs the raw-`Count` constructor). | - |
| crates/busbar-kernel-ledger/src/identity.rs | CLEAN | `residual`/`closed_window_is_settled` (the books-balance identity) are called from `crates/busbar-kernel-ledger/src/verify.rs:119,135` (production, same crate) — matches unconstructed-sweep.md:822's "READ in production" note. | - |
| crates/busbar-kernel-ledger/src/migration.rs | FINDING | THE-LIST X-203 (PARK, money) reused + reconfirmed: `git grep -n rate_card_version crates/busbar-kernel-ledger/src/migration.rs` → 4 hits, none a validation; still travels from `crates/busbar/src/root/migration.rs:118` (`ROOT_CARD.pin()` with a silent `map_or` fallback to `HistorySeq::OPENING`) into the sealed opening-balance marker unvalidated. Fresh: `is_sealed` (migration.rs:502) has `git grep -n "\bis_sealed("` → 0 non-test callers. | X-3124, X-3125 |
| crates/busbar-kernel-ledger/src/recompute.rs | FINDING | decision-audit #77(2)/(3) (CONTRADICTED, MONEY PARK) reused: the decision doc's "no field is ever edited, price is never stored" rule is contradicted by `DerivedPrice` (a stored, corrected-in-place cache) — but the code's own module doc argues this is a deliberate, reasoned design (a cache the lookup always overrides on disagreement), not an accident; citing so the owner can rule on the decision doc vs. the shipped design. Fresh: `is_pre_history`/`from_pairs`/`mark_for` (recompute.rs:246,431,440) confirmed 0 non-test callers via `git grep`. | X-3126, X-3127 |
| crates/busbar-kernel-ledger/src/settle.rs | FINDING | **Most serious finding in this file cluster.** Core booking path IS wired: `Ledger::post`/`book`/`settle` called from `crates/busbar/src/root/durability.rs:288`, `harness.rs:141`, `units_admin/mod.rs:478`. But `record_hold_opened` — the ONLY writer that INCREMENTS `figures.open_holds`, documented "Called when the door says yes" — has `git grep -n "\brecord_hold_opened("` → 0 callers anywhere outside `tests/`, while `post()` (wired, production) DECREMENTS `open_holds` on every settlement (`settle.rs:220`, `saturating_sub(reserved)`). So in a production ledger `open_holds` can only ever go down, never up. Also test-only: `is_dual_writing`, `book_mut`, `record_draw`, `record_cross_window_transfer`, `record_unreconciled`, `record_repricing`, `adjusting_entries`, `record_adjustment_releasing` (unconstructed-sweep.md rows 79-87, all reconfirmed 0 non-test callers fresh). | X-3128 |
| crates/busbar-kernel-ledger/src/tests/checkpoint_tests.rs | CLEAN | file-verdicts.md TEST verdict reused; exercises `Checkpoint::seal_as_of`/`should_alarm`/`Book::retain_from`/`CheckpointAnchor::anchor` (the functions found test-only elsewhere in this cluster) with real assertions — not vacuous, just uncalled from production. | - |
| crates/busbar-kernel-ledger/src/totals.rs | FINDING | local-only-audit.md:434 (VERIFIED, by call-site enumeration) reused + reconfirmed: `git grep -n "retain_from(" -- '*.rs'` → callers are `tests/checkpoint_tests.rs:258` and `tests/totals_tests.rs:{27,51,69,73}` only — zero production callers, so the book's window-retention/eviction mechanism never runs in a live node. `total_nanos` (totals.rs:328) also 0 non-test callers. | X-3129 |
| crates/busbar-kernel-ledger/src/usage/evidence.rs | FINDING | unconstructed-sweep.md rows 18/90 reused + reconfirmed: `with_lane_legs`/`with_proxies` (the evidence constructors that carry cross-check data — lane names, kernel-side proxy counts) have 0 non-test callers; production only ever builds evidence via the plain `new()`, so the richer cross-check fields are never populated outside tests. | X-3130 |
| crates/busbar-kernel-ledger/src/usage/meter.rs | CLEAN | branch-sweep.md's "a removed clamp on a money path yields EMPTY" row is a mutation-testing methodology note (what happens if you delete the clamp), not a claim the clamp is missing — reconfirmed present: `grep -n "reported.min(k)" usage/meter.rs` → `:144`, live in the beyond-tolerance dispute arm. | - |
| crates/busbar-kernel-ledger/src/usage/series.rs | FINDING | unconstructed-sweep.md row 91 reused + reconfirmed: `git grep -n "\baccrue_response("` → 0 non-test callers; `merge`/`is_empty`/`quantity` (the rest of `MeterCounts`) are not flagged and were not independently re-verified here. | X-3131 |
| crates/busbar-kernel-ledger/src/usage/source.rs | FINDING | money-sweep.md M-21 (PARK, money) reused + reconfirmed: `quantity_from_raw` (source.rs:29-47) "truncates a billable quantity and invents a zero, and has no caller" — `git grep -n "\bquantity_from_raw("` → 0 non-test callers anywhere; a `KernelBytes{divisor:0}` source silently converts to quantity 0 rather than refusing. | X-3132 |
| crates/busbar-kernel-ledger/tests/hot_path_allocation.rs | FINDING | THE-LIST X-150/X-151 (VERIFIED, instrument-blind) reused: proven three ways in the prior doc that `hot-path-alloc`'s 170-line counting-allocator instrument can be replaced with a 4-line stub and still pass 3/3, and nothing in CI runs the bench either way. Cross-referenced against X-201 (file was briefly deleted, restored byte-identical) — file currently present and tracked (`git status --porcelain` empty). | X-3133 |
| crates/busbar-kernel-ledger/tests/rate_conversion_agreement.rs | CLEAN | THE-LIST D-2 (money) reused + reconfirmed: this is the regression guard DELETE-LIST.md/X-201 warned was "deleted in the working tree, uncommitted" — `git status --porcelain -- crates/busbar-kernel-ledger/tests/rate_conversion_agreement.rs` → empty (present, committed, 248 lines); `git grep -n nano_rate` confirms it now asserts against derived expected integers rather than only agreement-by-construction, per its own header's stated fix for the tautology M1 originally found. | - |
| crates/busbar-kernel-scope/src/hook_veto.rs | FINDING | THE-LIST X-124 (VERIFIED, auth) reused + reconfirmed: `HookGate`/`hook_veto::gate()` — `git grep -n "HookGate" -- '*.rs'` → only decl + `tests/hook_veto_tests.rs`; `git grep -n "hook_veto::gate\|scope::gate\b" -- '*.rs' \| grep -v tests` → empty. Positive control (`kernel_scope::` cross-crate hits for `admin_required_scope`/`PolicyView`/etc. in `units_admin/*.rs`) confirms the grep mechanism works — this is a real, controlled zero. | X-3151 |
| crates/busbar-kernel-scope/src/lib.rs | FINDING | unconstructed-sweep.md rows 369-370 (real, evidenced, not yet promoted) extended: `is_kernel_granted`/`ADMIN_SCOPE_TABLE` — `git grep -n "is_kernel_granted\|ADMIN_SCOPE_TABLE" -- '*.rs' \| grep -v busbar-kernel-scope` → 0 hits. The module's own doc says a transport must check `is_kernel_granted` before calling `approve()`; the real, shipped `/healthz` bypass is a separate, independent mechanism in `busbar-kernel/src/auth/mod.rs` and `plugin_routes.rs` that never consults this crate. | X-3152 |
| crates/busbar-kernel-scope/src/tests/hook_veto_tests.rs | CLEAN | 5 `#[test]` fns, real falsifiable assertions (veto-wins-over-approval, first-veto-in-order-wins) — this is the sole consumer that makes X-3151 true, but the tests themselves are not vacuous. | - |
| crates/busbar-kernel-scope/src/tests/mod.rs | CLEAN | 302 lines, 8 `#[test]` fns, real assertions incl. row-for-row parity check of `admin_required_scope` against 66 pinned admin ops. Note: working tree adds `mod bitset; mod narrowing;` wiring two **untracked** files outside the `git ls-files` denominator — flagged, not acted on. | - |
| crates/busbar-kernel-wal/src/backend.rs | FINDING | unconstructed-sweep.md row 371 (`segment_count`, test-only) reused. New, VERIFIED: `SegmentFactory::is_durable` (backend.rs:71, impl'd by both factories) — `git grep -n "is_durable" -- '*.rs'` → 4 hits, all decl/impl, zero callers anywhere including tests; positive control (`is_poisoned` → real call site at `wal.rs:384`) confirms the grep works. The doc-promised operator visibility ("the log reports it so an operator can see which mode a node is in") is unmet; a separate `Mode` enum on `Wal`/`Journal` carries the actual signal instead. | X-3153 |
| crates/busbar-kernel-wal/src/journal.rs | FINDING | **Most serious finding in the D/E/scope+wal portion of this slice.** `Journal::resuming` (journal.rs:806) — the documented restart-safety constructor for a store-backed, no-data-dir node — has `git grep -n "resuming(" -- '*.rs'` → 2 test-only call sites, 0 production; `crates/busbar/src/root/durability.rs::build_for_node` only ever calls `Journal::in_directory` or `Journal::memory_buffered_to`, never `resuming`, so that deployment shape resets `head=0,next_seq=1` on every restart — exactly what the module's own doc calls "a settlement lost behind an `Ok`." Not confirmed as an actual data-loss repro (a separate legacy-rows dual-write path may cover it) — PARK. Secondary, lower severity: `hash_hex` (unconstructed-sweep.md row, SHAPE 1a) zero references anywhere, reused. | X-3154 |
| crates/busbar-kernel-wal/src/recover.rs | FINDING | New, ADJUDICATE: `Recovered::discarded_bytes`/`stopped_because` (the crash-tear signal) is computed here but `wal.rs:237` (`wal.recovered = recovered.records;`) drops both fields on construction — `Wal::recovered()` only exposes `&[Record]`. `git grep -n "tracing::\|log::\|warn!\|error!\|info!" -- 'crates/busbar-kernel-wal/src/*.rs'` → 0 hits, controlled against 91 files elsewhere in the repo that do use `tracing::`. An operator cannot learn a crash silently truncated a torn tail. | X-3155 |
| crates/busbar-kernel-wal/src/segment.rs | FINDING | unconstructed-sweep.md row 211 (SHAPE 1a) reused + reconfirmed: `frames_remaining` (segment.rs:206) — `git grep -n "frames_remaining" -- '*.rs'` → 1 hit (declaration only). Core append/poison/roll logic is well-wired via `wal.rs`; the actual roll decision is reactive (`SegmentError::Full`), not via this pre-check. | X-3156 |
| crates/busbar-kernel/benches/plane_host_vtable_perf.rs | CLEAN | file-verdicts.md TEST verdict reused; gate-sweep.md:697-703 explicitly adjudicates the "0.00s bench" shape as correct (deliberate source-contract gate, real measurement lives in the criterion instrument run in the perf lane). `grep -n plane_host_vtable_perf crates/busbar-kernel/Cargo.toml` → registered `[[bench]]` target. | - |
| crates/busbar-kernel/Cargo.toml | FINDING | THE-LIST X-207 (VERIFIED) manifest half, reused + reconfirmed: `grep -n busbar-kernel-identity crates/busbar-kernel/Cargo.toml` → no match (positive control: `git grep -l busbar-kernel-identity -- '*/Cargo.toml'` finds 2 other files, so the grep mechanism works) — the dependency edge X-207 requires is still absent. D-2's edge (`busbar-kernel-ledger`, line 66) and ENG2/ENG26's `busbar-kernel-egress` edge (line 55) are both present and correct. DELETE-LIST.md's warning that the D-2 regression guard was "deleted, uncommitted" is stale (file present and committed — see rate_conversion_agreement.rs above). | X-3134 |
| crates/busbar-kernel/src/admin/v1/contract/schema.rs | FINDING | unconstructed-sweep.md row 21 (SHAPE 1a) reused + extended: `ErrorBody`/`ErrorDetail` (schema.rs:459-467) claim via doc comment to keep the generated schema in sync with the hand-written `Error` component, but `busbar-core-admin/src/v1/json/handlers.rs:4774` states the derived schema is explicitly NOT registered, and no code anywhere compares the two — the cross-check the comment promises does not exist. | X-3135 |
| crates/busbar-kernel/src/appbuild.rs | CLEAN | LEDGER BR-S1 (OPEN) is now fixed in the **uncommitted working tree**: `git diff -- appbuild.rs` shows a new `#[must_use] InstalledLimits` guard with `.keep()` deferred past persist+swap; traced all 9 `.keep()` call sites (4 admin-apply handlers, named_map.rs, boot in main.rs, test fixture) and confirmed each defers correctly. Flagging so this diff is not lost and BR-S1 is not re-opened as still-broken — it is fixed, just not committed. | - |
| crates/busbar-kernel/src/audit/journal.rs | FINDING | unconstructed-sweep.md row 98 (SHAPE 1b) reused + extended: `take_resume_breaks` (journal.rs:179) — `git grep -n take_resume_breaks -- '*.rs'` → only declaration + `audit/tests/journal_tests.rs`. Module's own doc says a wrapper drains and reports runtime-resume tamper evidence; no such wrapper exists anywhere. Sibling boot-time path (`Restored::chain_breaks`) IS drained/reported in production (`plane/auditlog.rs:875`) — positive control confirming the grep methodology. | X-3136 |
| crates/busbar-kernel/src/audit/tests/journal_tests.rs | CLEAN | file-verdicts.md TEST verdict reused; this file is the sole caller of `take_resume_breaks` (the gap recorded against journal.rs above), consistent with that finding. | - |
| crates/busbar-kernel/src/auth/challenge.rs | CLEAN | file-verdicts.md LIVE verdict reused; `git grep -n "challenge::refuse"` → 4 production call sites in `auth/mod.rs`. | - |
| crates/busbar-kernel/src/auth/mod.rs | FINDING | (1) THE-LIST X-207 (VERIFIED) reused — `AuthMiddleware::extract_client_token` (`:669`) is a live, independent duplicate of `busbar-kernel-identity::carrier::extract_client_token`, unreachable via the crate's own missing dep edge (see Cargo.toml, same X-id). (2) New, VERIFIED by direct trace: the keys-arm identification return (`:504-505`, reached when `self.keys_in_chain` and no plugin module identified) drops buffered `pending_pass` credential-cache entries without flushing them, unlike the sibling plugin-Identify arm a few lines above (`:456-465`) — so a `[<cacheable module>, keys]` chain re-runs every module ahead of `keys` on every request instead of caching them. | X-3134, X-3137 |
| crates/busbar-kernel/src/auth/tests/plugin_chain_tests.rs | CLEAN | file-verdicts.md TEST verdict reused; branch-sweep-ledger.md's money-shaped `KeyBudgetView` note is about a deleted branch's copy — `grep -n KeyBudgetView` on trunk's file → 0 hits, nothing to harvest. | - |
| crates/busbar-kernel/src/auth/token.rs | CLEAN | file-verdicts.md LIVE + config-surface.md WIRED (8 production read sites) + branch-sweep-ledger.md (real security fix at `:419` confirmed on trunk) all cross-checked and internally consistent. | - |
| crates/busbar-kernel/src/calllog.rs | FINDING | security-posture.md (adjudicated, "STRONG mechanism, PARTIAL on two honest caveats") reused: `verify_principal_chain` (calllog.rs:806) has no production caller — the module's own header explicitly names this a real, accepted gap pending an admin "verify now" verb (roadmap §6), and asks not to be described as continuous verification. Independent sibling in `proxy/reqlog.rs:407` self-documents the identical gap. | X-3138 |
| crates/busbar-kernel/src/config_validate/mod.rs | FINDING | THE-LIST X-236 (PARK, money) reused + reconfirmed: completeness walk at `:1433-1460` still iterates only `cfg.models` (LLM plane); mcp/a2a/streams/decisions classes still have no card-completeness boot refusal. Lines re-confirmed current. | X-3139 |
| crates/busbar-kernel/src/config/named_map.rs | FINDING | unconstructed-sweep.md row 100 (SHAPE 1b) reused + extended: `parse_rel`'s only non-test caller (`admin/v1/contract/taxonomy.rs::declared_errors`) is itself only called from `busbar-core-admin/src/v1/json/mod.rs::record_declared_error`, gated `#[cfg(any(test, feature="test-support"))]` — and `test-support` is absent from that crate's `default` feature list, so the "production" call chain compiles out of a normal release build. | X-3140 |
| crates/busbar-kernel/src/config/prepass.rs | FINDING | unconstructed-sweep.md row 22 (SHAPE 1a) reused + reconfirmed: `deploy_from_yaml_value` (prepass.rs:486, re-exported at `config/mod.rs:41`) — `git grep -n deploy_from_yaml_value` → 2 hits total (decl + re-export), zero callers anywhere, including the "management-surface overlay merge" path its own doc comment names as the intended caller. | X-3141 |
| crates/busbar-kernel/src/config/providers.rs | CLEAN | config-surface.md exhaustive field-by-field table (14+ fields, e.g. `health.interval_secs`, `api_key`, `token_url`) — every field marked WIRED with a named production read site and 9-162 read sites each. file-verdicts.md LIVE. No contradiction found. | - |
| crates/busbar-kernel/src/config/sections.rs | FINDING | LEDGER M21 / money-sweep M-07 (OPEN/PARK, money) reused: `RateEntryCfg`'s four `_utok: f64` fields (sections.rs:324-333) feed `nano_rate`, and `nano_rate(0.5005)` rounds to 500 not 501 (half-away-from-zero required by #44) due to f64 imprecision — a ~0.2% silent underbill. `config/mod.rs:2783` confirms module reachable. | X-3142 |
| crates/busbar-kernel/src/cost.rs | FINDING | THE-LIST D-2 (money) — the rate-clamp duplicate itself is CLOSED (`cost.rs:149` now delegates to `busbar_kernel_ledger::cost::nano_rate`, confirmed live). Fresh, still-open: LEDGER R3 — `cost.rs:58`'s own `NANOS_PER_CENT: u128 = 10_000_000` has no static-assert pin against the ledger's canonical constant, unlike the two other independent copies at `units_a2a.rs:1216`/`units_voice.rs:164` which each carry `const _: () = assert!(...)`. | X-3143 |
| crates/busbar-kernel/src/egress_auth/jwt_bearer.rs | FINDING | THE-LIST X-172 (VERIFIED) is now STALE/FIXED: the base64-decode private-key Display leak at `:303` is gone — current code (`:320-339`) classifies `base64::DecodeError` by hand with no Display read, doc comment confirms the fix. Two lower-severity items from secret-hygiene.md remain live: `serde_json::from_str(...).map_err(\|e\| format!("...{e}"))` at `:88` and `:223` still format `serde_json::Error`'s Display (SA-JSON / token-response bodies) into a returned error string. | X-3144 |
| crates/busbar-kernel/src/egress/engine/resolve.rs | CLEAN | `ResolveNames` trait is a self-documented test seam ("this trait exists so a test can observe") — production posture arms use `EgressResolver::System`/`Pinned`, not `Custom(dyn ResolveNames)`. Module reachable (`egress/engine/mod.rs:68`). | - |
| crates/busbar-kernel/src/egress/engine/tests/resolver_tests.rs | CLEAN | Reached via `#[cfg(test)] mod resolver_tests;` (`egress/engine/mod.rs:567`); real, falsifiable assertions (`assert_eq!(engine_err, reqwest_err, ...)`). | - |
| crates/busbar-kernel/src/egress/engine/tls.rs | CLEAN | branch-sweep-ledger.md + local-only-audit.md (real verdict): private-key `wipe()`/`Drop` zeroize fix confirmed present at `:116-133`. Note: a related but distinct GAP in `plane_host/identity.rs` (count-based not generation-based mTLS identity eviction) is OUTSIDE this slice — flagged for whoever owns that file. | - |
| crates/busbar-kernel/src/egress/interface.rs | FINDING | unconstructed-sweep.md section 4B (real verdict) reused + reconfirmed fresh: `git grep -n "egress::interface\|interface::Engine\|interface::EgressEngine" -- crates/` → rc=1, zero hits — the `EgressEngine` trait + `Engine` struct (157 lines) has zero consumers anywhere; module's own doc names an intended migration (`busbar-mcp`/`busbar-voice`/`busbar-llm` onto `EgressEngine`) that never happened. | X-3145 |
| crates/busbar-kernel/src/export/projection.rs | CLEAN | `granted_fields` (unconstructed-sweep.md row 377, low materiality) is `pub(crate)`, `#[cfg_attr(not(test), allow(dead_code))]`, and self-documented as diagnostics/test-only — not a silently abandoned public capability. | - |
| crates/busbar-kernel/src/failover/mod.rs | FINDING | file-verdicts.md PARK (half-migrated per Decision #36) + unconstructed-sweep.md rows 378-379 reused: `repeatability`/`record_outcome`/`walk` are `pub` with zero production callers, but each carries a dated in-code comment explaining this is a deliberate "Phase-0 visibility lift" ahead of an unfinished "Phase-6 tighten-back" migration — tracked, not silent. | X-3146 |
| crates/busbar-kernel/src/governance/mod.rs | FINDING | THE-LIST X-253 (PARK, money) reused + reconfirmed — same underlying defect as window.rs's X-3104: `civil_from_days`/`days_from_civil`/`window_end` (governance/mod.rs:930,946,959) still byte-identical to `busbar-kernel-budget/src/window.rs`. | X-3147 |
| crates/busbar-kernel/src/governance/state.rs | FINDING | LEDGER M33 (PARKED-OWNER, money) + money-sweep D-10 reused + reconfirmed: `try_admit` (state.rs:1782, the enforcement gate) and two usage reads (`derived_bucket_usage`:1599, `budget_state`:1674) still price flat off the CURRENT rate card rather than the card in force at accrual time; `cap.saturating_mul(10_000)` (`:1697`, D-10's USD-hardcoded literal) confirmed present. | X-3148 |
| crates/busbar-kernel/src/handlers/tests/chat_tests.rs | DELETABLE | DELETE-LIST.md:193 (already YES) + file-verdicts.md ORPHAN reused + reconfirmed: `grep -rn "chat_tests" --include='*.rs' crates/` → zero hits anywhere, not even a `mod chat_tests` attempt; `handlers/tests/` has no `mod.rs`. Unreachable, already tracked for deletion. | - |
| crates/busbar-kernel/src/hooks/wire.rs | FINDING | money-sweep M-23 (PARK, money) reused + reconfirmed: `cost_per_mtok: Option<f64>` (wire.rs:577, used at :725) carries a rate-card-derived price in raw `f64`, serialized to third-party hook endpoints as a JSON number and also used for `cheapest`-routing float comparisons. | X-3149 |
| crates/busbar-kernel/src/inflight.rs | FINDING | No prior verdict (unconstructed-sweep.md table listed it as a neutral fact, not raised). New, VERIFIED: `reserve_for` (the documented "hold back capacity for already-open paying sessions under load" mechanism, inflight.rs:57) has zero production callers; all 4 production `InFlight::new(...)` sites (`gauntlet_kernel.rs:296,397`, `units_admin/admin_mount.rs:44`, `units_llm.rs:335`) hardcode the reserve argument to literal `0`, and no setter exists to apply it post-construction. | X-3150 |
| crates/busbar-kernel/src/ingress/arrival.rs | CLEAN | unconstructed-sweep.md's near-zero reference count for `PathIngressEntry`/`BodyIngressEntry` is a measurement artifact (production code spells out the structural tuple type instead of the alias name); `install_path_ingress`/`path_ingress_for` are real, live, `OnceLock`-guarded install-once functions. | - |

## ROWS RAISED

### X-3100 · no-float-money gate is RED on price.rs's own signature, right now
CLASS:     money
CERTAINTY: PARK
EVIDENCE:  `./target/debug/xtask gate no-float-money` (run directly against the built binary, current HEAD) → `FAIL no-float-money:no-float ... crates/busbar-kernel-budget/src/price.rs:76: \`f64\` on the money path ... outside the #44 card-build boundary` (and :77,:78,:79). The #44 exemption (`CARD_BUILD_BOUNDARY` in `xtask/src/gates/no_float_money.rs`) names exactly one file, `busbar-kernel-ledger/src/cost/rate.rs` — price.rs is not it, even though it only forwards its f64 params one line later into `nano_rate`. (The same gate run also showed 4 unrelated hits in untracked `zzz_f*.rs` debris files left by another agent's self-test in `busbar-kernel-audit`, `busbar-kernel-budget`, `busbar-kernel-ledger`, `busbar-kernel-wal` — noted separately below, not part of this row.)
ACTION:    Either extend the #44 exemption to cover a parameter that is provably forwarded unchanged into `nano_rate` within the same statement, or change `from_micros_per_token`'s signature to accept the already-converted integer rate and push the f64→nano_rate call to price.rs's own caller (inside the exempt boundary).

### X-3101 · Pricer::with_card (the rate-card constructor) and RateNanos::from_micros_per_token are never called in production
CLASS:     money
CERTAINTY: PARK
EVIDENCE:  `git grep -n "\.with_card(\|Pricer::with_card" -- '*.rs'` → 12 hits, every one in a `tests/` file or `crates/busbar/tests/money_one_function_equivalence.rs` (an integration test). The only production `Pricer` constructor called anywhere is `Pricer::flat(0)`, once, in `crates/busbar/src/main.rs:512`, building a voice-node stub whose own surrounding comment reads "No group reaches this node's door." `units_llm.rs` (the LLM plane) never imports `Pricer` at all — its pricing goes through `busbar-kernel/src/cost.rs`'s independent implementation instead (ENG7's documented duplication). `A2aBindings`/`McpBindings`-shaped structs that hold a `pricer: &Pricer` field are themselves only constructed in `crates/busbar/src/root/tests/units_a2a.rs` (confirmed by `git grep -n "A2aBindings" -- '*.rs'`), suggesting the gap may be wider than this one file. unconstructed-sweep.md rows 50-51 already flagged `with_card`/`from_micros_per_token` as SHAPE 1b (test-only) with matching test-call-site counts (5, 8) — this reconfirms and sharpens that census entry with the actual production-caller trace.
ACTION:    Owner decision: either wire a real caller (a2a/mcp/voice admission binding assembly at the composition root) to build a `Pricer::with_card` from the configured rate table, or confirm those planes intentionally do not use this crate's Pricer for real traffic and downgrade the extensive money analysis already spent on this constructor's internals accordingly.

### X-3102 · price.rs's cents projection is a second hand-rolled currency-blind divide, still not unified with the ledger's currency-aware minor_of
CLASS:     money
CERTAINTY: PARK
EVIDENCE:  `crates/busbar-kernel-budget/src/price.rs:240` `let mut cents = i64::try_from(nanos / NANOS_PER_CENT).unwrap_or(i64::MAX);` vs. `crates/busbar-kernel-ledger/src/cost/project.rs:29-32` `minor_of`: `i64::try_from(nanos / currency.nanos_per_minor()).unwrap_or(i64::MAX).max(0)`. Reused from `docs/design/1.6.0-branch-sweep-ledger.md`'s D-7-shaped row (PARK, "HALF LANDED"): half (i) landed (`price.rs:24` now `pub use busbar_kernel_ledger::cost::NANOS_PER_CENT;`), half (ii) — switching the divide itself to call `minor_of` — did not. No live divergence for USD today (both saturate/floor identically; `NANOS_PER_CENT == CurrencyCode::USD.nanos_per_minor()`), but two independently-written truncating divides is the identical shape that already drifted once on the rate-clamp (M1).
ACTION:    Owner decision — finish half (ii): route price.rs's cents projection through `busbar_kernel_ledger::cost::minor_of`, or record on the line why the admission door must keep its own copy.

### X-3103 · window.rs's is_known_window is never called — the corrupt-window-word detector it exists for has no caller
CLASS:     missing-code
CERTAINTY: VERIFIED
EVIDENCE:  `git grep -n "is_known_window" -- '*.rs'` → declaration (`window.rs:61`), one doc-comment self-reference, and one doc-comment mention in `lib.rs:232` ("the crate has no logger, so the condition is exposed for the caller to detect with `is_known_window`") — zero actual calls anywhere, and it is not re-exported from `lib.rs`'s `pub use window::{budget_window, window_end};` list.
ACTION:    Wire `is_known_window` into whichever caller reads a stored window word off a bucket key (so a corrupt/foreign row can be distinguished from an intentionally-unrecognized word, both of which currently fall silently to the all-time window), or delete it and the doc-comment promise pointing at it.

### X-3104 · budget_window/window_end/civil_from_days/days_from_civil are duplicated byte-for-byte between window.rs and governance/mod.rs
CLASS:     money
CERTAINTY: PARK
EVIDENCE:  THE-LIST X-253 / file-verdicts.md P3 (PARK, money) reused: `diff <(sed -n '946,957p' crates/busbar-kernel/src/governance/mod.rs) <(sed -n '84,95p' crates/busbar-kernel-budget/src/window.rs)` → no output (byte-identical `civil_from_days`/`days_from_civil`); `grep -n "fn civil_from_days\|fn days_from_civil\|fn window_end" crates/busbar-kernel/src/governance/mod.rs` → `:930,946,959`, all three present, confirmed still current. Two independent copies of the boundary that decides which window a charge lands in.
ACTION:    Fold to one home (spec names `busbar-kernel-budget` per Decision #36) and make `governance/mod.rs`'s copy a re-export, so a future window-boundary fix cannot land on only one side.

### X-3105 · Arity's failover posture (on_primary_unavailable) never reaches the walk
CLASS:     missing-code
CERTAINTY: ADJUDICATE
EVIDENCE:  `git grep -n "on_primary_unavailable(" -- crates/` → only `tests/arity_tests.rs:21,32`; re-exported at `lib.rs:71` (part of the public API) but zero external callers. `may_failover` similarly only called from `arity.rs` itself and its own tests.
ACTION:    Wire `on_primary_unavailable` into `walk.rs`'s hop loop so a pinned (`ExactlyOne`) member's failure refuses instead of failing over, or delete the module if the posture distinction is superseded by `busbar-llm`'s own implementation (see X-3106).

### X-3106 · busbar-kernel-egress's entire routing pipeline is constructed at boot and never called — the real router is a parallel implementation in busbar-llm
CLASS:     missing-code
CERTAINTY: PARK
EVIDENCE:  `git grep -n "EgressUnit::new\|EgressUnit::default" -- crates/` → only `crates/busbar/src/root/kernel.rs:732` (constructed). `git grep -n "\.egress\b" -- crates/busbar/src/root/kernel.rs crates/busbar/src/root/units_a2a.rs` → 0 hits (field written once, never read). `git grep -n "\.route(" -- crates/ \| grep -v axum` → no call of `Egress::route`. `git grep -n "\.floor()" -- crates/` → 0 hits anywhere. `git grep -n "\bwalk(" -- crates/busbar-kernel-egress crates/busbar crates/busbar-llm \| grep -v tests` → the only caller of `walk()` is `Egress::route` itself (dead). `ports.rs`'s 5 of 9 port traits (`PermitHandle`/`Capacity`/`EgressAuth`/`Journal`/`Telemetry`) and `trust/lane.rs`'s `LaneTable`/`BreakerView` traits have zero production implementors — only `tests/harness.rs` and downstream test doubles implement them. This matches and independently reconfirms `docs/design/1.6.0-unconstructed-sweep.md:656` (shape 4.2) and `docs/design/1.6.0-gate-sweep.md:231-242` ("the kernel-egress pair and the busbar-llm pair are the same routing decision written twice — the third egress/breaker/identity architecture that is not on any serving path"). Files carrying this finding: lib.rs, exhaustion.rs, ports.rs, trust/lane.rs, walk.rs.
ACTION:    Owner decision: (a) wire the real dispatch path (`busbar/src/root/units_llm.rs`'s routing, or wherever LLM egress actually happens) through the sealed `EgressUnit::route` seam and retire `busbar-llm`'s parallel `Walk` implementation, or (b) if `busbar-llm`'s implementation is the one being kept, delete `EgressUnit`/`Egress` and stop constructing the field in `kernel.rs`.

### X-3107 · Pool::position_of is dead code with zero references anywhere
CLASS:     missing-code
CERTAINTY: ADJUDICATE
EVIDENCE:  `git grep -n "position_of(" -- crates/` → single hit, the declaration itself (`pool.rs:156`). No caller anywhere, including tests. Matches unconstructed-sweep.md row 13/204 (a genuine zero-doc-mention, zero-reference entry).
ACTION:    Delete `Pool::position_of`, or wire it where a caller needs a member's index by `DestinationId` (e.g. the wait/spill accounting `exhaustion.rs` currently performs by hand).

### X-3108 · Sticky-affinity pick bypasses lane admissibility — a drained/killed member can still be dispatched to
CLASS:     auth
CERTAINTY: VERIFIED
EVIDENCE:  `docs/design/1.6.0-local-only-audit.md` §E2 (real, independently re-verified a 3rd time in that doc) reused + reconfirmed: `sed -n '409,427p' crates/busbar-kernel-egress/src/select.rs` — `Order::new`'s sticky branch computes stickiness from `member.weight != 0 && !ctx.is_excluded(...)` and never calls `lane_admissible`/`survives_prewalk_filter`. `git grep -n "lane_admissible" -- crates/busbar-kernel-egress/src/select.rs crates/busbar-kernel-egress/src/trust/order.rs` → 0 hits in both files (positive control: `lane.rs:101` has the predicate and is called elsewhere, so this is a real, controlled zero). Duplicated in both `select.rs::pick_among` and `trust/order.rs::pick` — the same bug written twice.
ACTION:    Make sticky-affinity offers pass through `lane_admissible`/`survives_prewalk_filter` before being returned, in both `select.rs` and `trust/order.rs`. Currently latent in production because the pipeline itself is unreached (X-3106); becomes live the moment that pipeline is wired.

### X-3109 · Denylist (the operator-configured SSRF allow/deny overlay) is never constructed in production
CLASS:     auth
CERTAINTY: VERIFIED
EVIDENCE:  THE-LIST X-123a (VERIFIED) reused + reconfirmed fresh: `git grep -n "Denylist::new(" -- crates/` → 10 hits, all in `trust/tests/net_tests.rs`, none in production; `git grep -n "Denylist {" -- crates/` → only the struct's own constructor body, no production struct-literal either. The static `METADATA_HOSTS` baseline (net.rs:75) still functions independently, so this is not a total SSRF bypass — but any operator who configures `security.blocked_metadata_hosts`/`allow_metadata_hosts`/`allow_all_metadata` has that configuration silently ignored, since nothing in production ever builds a `Denylist` from parsed config.
ACTION:    Already tracked as X-123a — construct `Denylist` from the three named config keys at boot and thread it into the live `check_destination`/`check_destination_facts` call sites in `units_a2a.rs`/`units_mcp.rs`.

### X-3110 · trust::order is an entire second, fully test-only duplicate selection engine; config-surface.md's WIRED claim for hooks.<h>.priority cites it by name collision
CLASS:     missing-code
CERTAINTY: VERIFIED
EVIDENCE:  `git grep -n "OrderingHook" -- crates/` → every mention is the declaration, a re-export/doc line, or `order_tests.rs` — zero production implementors, zero production callers of `pick`/`reconcile_order`/`sticky_position`. `docs/design/1.6.0-config-surface.md:601,613` cites `trust/order.rs:72` (`chain.sort_by_key(|h| h.priority())`) as a "WIRED" production read site for `hooks.<h>.priority` — traced and confirmed this is `OrderingHook::priority()`, a trait method nothing in production implements; the real, unrelated consumer of `hooks.<h>.priority` is `crates/busbar-kernel/src/hooks/mod.rs` (lines 390,434,1505,1559,1596), outside this crate entirely.
ACTION:    (a) Delete `trust::order`'s dead-parallel selection engine or wire it as the crate's actual replacement for `select.rs::pick_among`; (b) separately, correct `docs/design/1.6.0-config-surface.md`'s WIRED claim for `hooks.<h>.priority` — the real production read sites are in `busbar-kernel/src/hooks/mod.rs`, not `trust/order.rs:72`.

### X-3111 · Two Shed refusal constructors (restrict_no_lane, invalid_body) can never actually be produced by any code path
CLASS:     missing-code
CERTAINTY: ADJUDICATE
EVIDENCE:  unconstructed-sweep.md rows 141/206-207 reused + reconfirmed: `git grep -n "restrict_no_lane" -- crates/` → declaration only (`wire.rs:115`); `git grep -n "invalid_body" -- crates/` → declaration only (`wire.rs:127`). `empty_pool()`/`request_timeout()`/`internal()` (sibling `Shed` constructors) ARE used by `walk.rs`; these two named customer-facing refusal reasons never fire.
ACTION:    Wire `restrict_no_lane()` where a gate-restricted spill empties the candidate set, and `invalid_body()` where request-body-shape validation happens — or delete both constructors and their `DETAIL_*` constants if the refusal vocabulary they describe no longer applies.

### X-3112 · busbar-kernel-identity::admin (the open-admin-posture unit) has zero production callers — a second, independent implementation in busbar-kernel is what actually runs
CLASS:     auth
CERTAINTY: VERIFIED
EVIDENCE:  `git grep -n "admin_grants(\|kernel_verb_scope_satisfied(" -- '*.rs'` → only `admin.rs`'s own declarations and `tests/unit_tests.rs`. The live open-admin-posture decision ("no principal ⇒ Full, dev-only") is made independently at `crates/busbar-kernel/src/auth/mod.rs:1240 admin_scope_for`, using `busbar_contract::authz::{Grants, Scope}` — a completely different type from this crate's `admin::Grants`/`admin::Scope`. `git grep -n "busbar_kernel_identity::admin\|identity::admin\b" -- '*.rs'` → 0 hits in `busbar`'s own source.
ACTION:    Wire `busbar-kernel/src/auth/mod.rs::admin_scope_for`'s open-posture arm to call through `busbar_kernel_identity::admin::{admin_grants, kernel_verb_scope_satisfied}`, or delete `admin.rs` — two independently-maintained implementations of the same open-admin-full-scope rule is the same security-boundary-duplicate shape as X-3114 (carrier.rs).

### X-3113 · Sha256Digest is compiled in specifically for busbar but never constructed; production uses a separate, test-pinned duplicate
CLASS:     missing-code
CERTAINTY: ADJUDICATE
EVIDENCE:  `busbar/Cargo.toml:102` turns the `sha256` feature ON specifically to compile `cache::Sha256Digest` in, yet `git grep -n "Sha256Digest" -- '*.rs'` shows zero production constructions — `crates/busbar/src/root/auth_bindings.rs:197` constructs `CredentialCache::new(busbar_api::sha256_hex as fn(&[u8]) -> String)` instead, a separate function a test (`cache_tests.rs:214-232`) pins byte-for-byte equal to `Sha256Digest`.
ACTION:    Either have `auth_bindings.rs:197` construct `Sha256Digest` directly (dropping the separate `busbar_api::sha256_hex` closure and the parity test that exists only to keep them in sync), or drop the `sha256` feature/`Sha256Digest` struct if `busbar_api::sha256_hex` is to remain canonical.

### X-3114 · carrier.rs's credential-precedence extraction and CallerToken are landed but never switched to as the caller (X-207/X-208/S11)
CLASS:     auth
CERTAINTY: VERIFIED
EVIDENCE:  THE-LIST X-207/X-208 (VERIFIED) + LEDGER S11 reused and reconfirmed fresh: `git grep -n "extract_client_token(" -- '*.rs'` → decl + own tests only; production exclusively uses `crates/busbar-kernel/src/auth/mod.rs:669`'s independent copy, itself unreachable-from because `busbar-kernel/Cargo.toml` still has no `busbar-kernel-identity` dependency edge (see X-3134). `CallerToken(` (this crate's copy) constructed only in `tests/carrier_tests.rs`; production uses `busbar_api::CallerToken` (re-exported at `busbar-kernel/src/auth/mod.rs:65`) instead — the S11 duplicate.
ACTION:    Add the `busbar-kernel-identity` dependency edge to `busbar-kernel/Cargo.toml` and switch `AuthMiddleware::extract_client_token` to call the shared unit, or formally retire it and fold `CallerToken` per S11's own recommendation.

### X-3115 · chain.rs's is_open() fix has no regression-test net in trunk (BR-S9, still open)
CLASS:     instrument-blind
CERTAINTY: PARK
EVIDENCE:  LEDGER BR-S9 (OPEN) reused + reconfirmed: `git grep -lF keys_arm_view_tests HEAD` → 0 hits today — the 4-test regression net that would catch a regression of `AuthChain::is_open()`'s two-flag correctness (`self.chain.is_empty() && !self.keys_in_chain`) is still absent from trunk, even though `AuthChain` itself is genuinely live and wired (`busbar/src/main.rs:513`, `root/kernel.rs:833`, consumed by `busbar-core-admin/src/v1/service_operations.rs:420,1564,1569`).
ACTION:    Re-land the 4-test `keys_arm_view_tests.rs` regression net pinning `is_open()`'s correctness, per BR-S9's own recommendation.

### X-3116 · busbar-kernel-identity::egress_auth (including a full AWS SigV4 signer) is unreachable from any production path
CLASS:     auth
CERTAINTY: VERIFIED
EVIDENCE:  No prior real verdict — unconstructed-sweep.md flagged only 1 of 6 unconstructed entry points because every symbol name here (`decorate`, `substitute`, `lane_cross_check`, `EgressBody`, `Scheme`) collides with an unrelated, live, same-named symbol elsewhere in the tree (`AuthDecoration::decorate` in busbar-contract, `DecoratePort::decorate` in busbar-kernel-egress, `busbar-contract::dest::EgressBody`, an independent `lane_cross_check` in `busbar-kernel-egress/attempt.rs`). Fresh: `git grep -n "kernel_identity::egress_auth\|egress_auth::sigv4\|egress_auth::Scheme\|egress_auth::decorate\|egress_auth::substitute\|egress_auth::lane_cross_check\|egress_auth::continue_handshake" -- '*.rs'` (excluding this crate) → 0 hits; not re-exported from `lib.rs`; no crate that performs real egress depends on `busbar-kernel-identity` at all. The module's own header says it was "Folded from the former busbar-unit-egress-auth crate (#36)" — code landed, caller never switched. The live AWS SigV4 signer for Bedrock is a third, wholly independent implementation at `busbar_substrate_values::sigv4::sign_v4`.
ACTION:    Either wire the real egress send path onto this unit and delete the parallel implementations it currently duplicates, or delete the entire module (293 lines + sigv4 submodule) as an abandoned #36 fold. At minimum correct the module doc's "the egress-auth schemes actually shipped" claim.

### X-3117 · exchange.rs's /auth/token bypass-path decision and browser dispatch are dead code; production reimplements them independently
CLASS:     auth
CERTAINTY: VERIFIED
EVIDENCE:  unconstructed-sweep.md row 339 (partial) reused + extended: `is_bypassed`/`dispatch`/`BrowserAction`/`AUTH_TOKEN_PATH` all zero production references — `AUTH_TOKEN_PATH` here is a separate constant from the live `pub(crate) const AUTH_TOKEN_PATH` at `busbar-kernel/src/auth/exchange.rs:33`, wired into `router.rs:389,395` via `RouteAuth::None`. Browser dispatch (`?logout=1`/`?code=`/`?method=&refresh=1`) is independently reimplemented at `busbar-kernel/src/auth/token.rs::browser()` (lines 324-344), same four-branch precedence, no shared code.
ACTION:    Delete this module or wire `router.rs`/`token.rs::browser()` onto it — same "landed the unit, never switched the caller" pattern as X-3114. Lower immediate risk since the security-relevant property (exact-match, not prefix) is independently re-derived correctly in `router.rs`.

### X-3118 · checkpoint.rs's should_alarm and seal_as_of are never called in production
CLASS:     instrument-blind
CERTAINTY: VERIFIED
EVIDENCE:  unconstructed-sweep.md rows 64-65 reused + reconfirmed: `git grep -n "\bshould_alarm(\|\bseal_as_of("` → only `tests/checkpoint_tests.rs`. `should_alarm` is the imbalance-threshold check on a sealed checkpoint; nothing in production ever asks it whether the books are far enough out to page someone. The core checkpoint API (`seal`, `totals_for`, `body_hash_verifies`) IS wired (`migration.rs:442`, `verify.rs:119,135`, `units_admin/mod.rs:1541`).
ACTION:    Wire `should_alarm` into whatever periodic reconciliation/checkpoint-seal path runs in production so an imbalance past threshold actually pages an operator, or `seal_as_of` into a caller that needs a point-in-time (rather than latest) seal — or delete both if superseded.

### X-3119 · cost/currency.rs — a full ISO-4217 currency system ships while decision #66 says money should be unitless
CLASS:     money
CERTAINTY: PARK
EVIDENCE:  THE-LIST X-242 / decision-audit #66 (PARK, money) reused + reconfirmed current: `CurrencyCode::USD` (`:30`) and per-currency minor-unit exponents (`:60-62`) are still a required parameter of `price`/`price_in_view`/`price_exact` (`cost/view.rs:340,353,369`). Decision #66 is dated 2026-09-20 and predates the module; nothing gates the ruling either way.
ACTION:    Owner decision (already stated in X-242): amend #66 to acknowledge the shipped currency design, or specify a non-currency rounding-scale source first. Do not touch on an agent's judgement — `CurrencyCode::USD`'s exponent-2 path is what keeps 1.5.5 figures bit-identical.

### X-3120 · cost/posting.rs's tamper-detection (cache_diverges) and unpriced-visibility (unpriced_classes) helpers are never called in production
CLASS:     money
CERTAINTY: PARK
EVIDENCE:  LEDGER R78's literal claim ("posting.rs pushes the fee line with unpriced: false hard-coded") is now STALE/FIXED — current code computes `unpriced: !priced` (`:441`) and `unpriced: priced_fee.is_none()` (`:459`), confirmed by `grep -n unpriced crates/busbar-kernel-ledger/src/cost/posting.rs`. New: `cache_diverges` (posting.rs:145 — the exact tamper-vs-legitimate-amendment detector `recompute.rs`'s own module doc calls "the whole of Verdict") and `unpriced_classes` (posting.rs:221 — the operator-facing "which classes did a present card stay silent about" reporter) both show `git grep -n "\bcache_diverges(\|\bunpriced_classes("` → 0 non-test callers anywhere, including same-crate.
ACTION:    Confirm recompute.rs's actual reconciliation pass performs the equivalent comparison inline (if so, note it and consider this file CLEAN on that point) — or wire `cache_diverges` into the real reconciliation loop and `unpriced_classes` into the usage-read surface so a present-but-silent card is visible to an operator, not just structurally detectable.

### X-3121 · cost/project.rs's minor_of is still not the one the budget door's cents projection calls
CLASS:     money
CERTAINTY: PARK
EVIDENCE:  Same underlying defect as X-3102 (price.rs), cited there from `busbar-kernel-budget/src/price.rs:240` vs. this file's `minor_of` (`:29-32`). branch-sweep-ledger.md's "HALF LANDED" row reused: half (i), the shared `NANOS_PER_CENT` constant, landed; half (ii), routing the door's divide through this file's currency-aware function, did not.
ACTION:    See X-3102 — same fix, same owner decision.

### X-3122 · cost/rate.rs's nano_rate answers "free" for both too-large and too-small configured rates (M40, still open)
CLASS:     money
CERTAINTY: PARK
EVIDENCE:  LEDGER M40 (PARK) reused + reconfirmed: `sed -n '39-52p' crates/busbar-kernel-ledger/src/cost/rate.rs` → `if v.is_finite() && v > 0.0 && v < 2.0_f64.powi(64) { v as u64 } else { 0 }` — the `else` arm is the whole finding: a rate too large to represent and a rate quantizing to zero (anything below 0.0005 micro-units/unit) share one answer, and that answer is free. Note: the CEILING half of M1 (the off-by-one at `2^64`) IS now fixed — the guard reads `v < 2.0_f64.powi(64)` rather than the old `<= u64::MAX as f64`, which used to admit `v==2^64` and saturate to `u64::MAX` on cast.
ACTION:    Owner: split the arms per M40's own recommendation — too-large is a refusal, too-small is a refusal or a declared minimum. #42 says money must never silently be free.

### X-3123 · cost/rate.rs's nano_rate under-bills by one nano-unit per billion tokens at exact half-cent rate boundaries (M21, still open)
CLASS:     money
CERTAINTY: PARK
EVIDENCE:  LEDGER M21 (OPEN) reused: `nano_rate(0.5005)` computes `(0.5005 * 1000.0).round()`; `f64` stores `0.5005` as `0.50049999999999994493...`, so the product is `500.49999999999994`, which rounds to `500` where #44's half-away-from-zero rule requires `501`. The multiply-then-round logic at `rate.rs:39` is unchanged by today's clamp fix, so this remains live. money-sweep.md's D7 table (reused) measures the same pattern at five boundary values (`0.5005→501 vs 500`, `0.5015→502 vs 501`, ...).
ACTION:    Owner: read the configured rate's decimal TEXT and convert exactly (per #81's own prescription for counts), rather than through an intermediate `f64` multiply — the card test corpus already has a candidate exact-conversion helper (`money_one_function_equivalence.rs:618 nano_rate_exact_from_text`).

### X-3124 · migration.rs — rate_card_version travels from operator config into the sealed opening balance unvalidated (X-203, still open)
CLASS:     money
CERTAINTY: PARK
EVIDENCE:  THE-LIST X-203 (PARK) reused + reconfirmed: `git grep -n rate_card_version crates/busbar-kernel-ledger/src/migration.rs` → 4 hits, none a validation. `crates/busbar/src/root/migration.rs:118` resolves `ROOT_CARD.pin()` with a silent `map_or` fallback to `HistorySeq::OPENING` — an unpinned card and a genuine opening are indistinguishable downstream — and this value is stamped straight onto the sealed opening-balance marker at `migration.rs:462`.
ACTION:    Owner: decide whether the ledger should validate the card version it seals an opening under, rather than trusting whatever the root hands it.

### X-3125 · migration.rs's is_sealed is never called in production
CLASS:     missing-code
CERTAINTY: ADJUDICATE
EVIDENCE:  `git grep -n "\bis_sealed("` → 0 non-test callers (declaration at `migration.rs:502` plus tests only).
ACTION:    Low severity — confirm whether the migration-sealed check is meant to gate a boot-time or admin-read decision and wire it there, or delete if superseded by a different sealed-state check elsewhere in the crate.

### X-3126 · recompute.rs's DerivedPrice cache-and-correct design contradicts decision #77(2)/(3), which the code itself argues is intentional
CLASS:     money
CERTAINTY: PARK
EVIDENCE:  decision-audit.md #77(2)/(3) (CONTRADICTED, MONEY PARK) reused: decision #77 says "no field is ever edited, price is never stored"; `DerivedPrice` (recompute.rs:156-166) is a stored, in-place-corrected cache on `Posting.cached`, and the module's own extensive doc comments (`:1-45`) argue this is a deliberate, reasoned upgrade from an old design (an auditor that alarmed on every disagreement) to an arbiter that corrects legitimate amendment-staleness silently and alarms only on true tamper. This is a decision-doc-vs-shipped-design mismatch, not obviously a code defect — citing per the money-PARK rule so an owner rules on which one is wrong.
ACTION:    Owner: either amend decision #77(2)/(3) to describe the cache-and-correct design actually shipped, or rule that the design must change to match the original no-storage decision.

### X-3127 · recompute.rs's is_pre_history, from_pairs, and mark_for are never called in production
CLASS:     missing-code
CERTAINTY: VERIFIED
EVIDENCE:  `git grep -n "\bis_pre_history(\|\bfrom_pairs(\|\bmark_for("` → 0 non-test callers for all three (declared at recompute.rs:246,431,440).
ACTION:    Confirm these are genuinely superseded/unused parts of the recompute machinery and delete, or wire them into the reconciliation pass that should be using them.

### X-3128 · settle.rs's open_holds ledger column can only ever decrease in production — record_hold_opened, its only incrementer, is never called
CLASS:     money
CERTAINTY: PARK
EVIDENCE:  **Most serious finding in this file cluster.** `git grep -n "\.open_holds\b" -- '*.rs' | grep -v _tests.rs | grep -v /tests/` → exactly two write sites: `settle.rs:220` (`figures.open_holds = figures.open_holds.saturating_sub(reserved);`, inside `post()`, which IS wired to production via `crates/busbar/src/root/durability.rs:288`) and `settle.rs:253` (`figures.open_holds = figures.open_holds.saturating_add(...)`, inside `record_hold_opened`, documented "Called when the door says yes"). `git grep -n "\brecord_hold_opened("` → 0 callers anywhere outside `tests/`. So in a production ledger, `open_holds` is decremented on every settlement but never incremented on the matching hold-open event — the column can only trend down. Also test-only in this file: `is_dual_writing`, `book_mut`, `record_draw`, `record_cross_window_transfer`, `record_unreconciled`, `record_repricing`, `adjusting_entries`, `record_adjustment_releasing` (unconstructed-sweep.md rows 79-87, all reconfirmed 0 non-test callers fresh) — meaning several other Totals columns (draws, cross-window transfers, unreconciled, repricing, adjustments) may have the same "written on one side of the identity only, or not at all in production" shape, though I did not trace each one as deeply as open_holds.
ACTION:    Owner: trace where "the door says yes" actually happens for the production hold-open event and confirm whether it should be calling `record_hold_opened` (in which case this is a real, live money-integrity gap and needs an urgent fix) or whether holds are tracked by a different mechanism entirely (busbar-kernel-budget's own bucket accounting) and this ledger column is vestigial/for a not-yet-wired feature, in which case the column and its half of the books-balance identity should be labeled as such rather than left silently one-sided.

### X-3129 · totals.rs's Book::retain_from (the window-retention/eviction mechanism) is never called in production
CLASS:     missing-code
CERTAINTY: VERIFIED
EVIDENCE:  local-only-audit.md (VERIFIED, by call-site enumeration) reused + reconfirmed: `git grep -n "retain_from(" -- '*.rs'` → callers are `tests/checkpoint_tests.rs:258` and `tests/totals_tests.rs:{27,51,69,73}` only. A production ledger's `Book` never evicts/retains-from-a-window-forward, meaning old per-key totals rows accumulate without bound over the life of a node. `total_nanos` (totals.rs:328) also 0 non-test callers.
ACTION:    Wire `retain_from` into whatever periodic maintenance pass (checkpoint-seal, most likely) should be bounding the book's memory footprint, or confirm a different eviction mechanism exists and document why this one is unused.

### X-3130 · usage/evidence.rs's richer evidence constructors (with_lane_legs, with_proxies) are never used — production evidence always lacks cross-check data
CLASS:     money
CERTAINTY: PARK
EVIDENCE:  unconstructed-sweep.md rows 18/90 reused + reconfirmed: `git grep -n "\bwith_lane_legs(\|\bwith_proxies("` → 0 non-test callers; production only ever builds `RetainedLocatorValues`/`KernelSideLines` via the plain `new()`, which carries no lane-leg names or kernel-side proxy counts. Since `usage/meter.rs`'s dispute logic (`beyond_tolerance`, `reported.min(k)`) depends on a companion figure being present to catch a mismatched locator, evidence built without proxies may silently skip a real cross-check class.
ACTION:    Confirm whether any production evidence-assembly path should be populating lane legs / proxies and isn't, or whether these constructors are for a feature not yet turned on.

### X-3131 · usage/series.rs's accrue_response is never called in production
CLASS:     missing-code
CERTAINTY: VERIFIED
EVIDENCE:  unconstructed-sweep.md row 91 reused + reconfirmed: `git grep -n "\baccrue_response("` → 0 non-test callers (declared series.rs:31). `merge`/`is_empty`/`quantity` (the rest of `MeterCounts`) were not independently re-verified here.
ACTION:    Confirm whether `MeterCounts` is accrued through `accrue_response` in intent but through a different call in practice, and wire or delete accordingly.

### X-3132 · usage/source.rs's quantity_from_raw truncates a billable quantity and invents a zero on divisor=0, and has no caller (M-21, still open)
CLASS:     money
CERTAINTY: PARK
EVIDENCE:  money-sweep.md M-21 (PARK) reused + reconfirmed: `quantity_from_raw` (source.rs:29-47) — `git grep -n "\bquantity_from_raw("` → 0 non-test callers anywhere; a `QuantitySource::KernelBytes{divisor:0}` converts to quantity `0` rather than refusing, on the one conversion function this unit adds to a closed, seven-arm quantity-source set.
ACTION:    Confirm this metering-policy conversion is genuinely dead (in which case the money risk is dormant, not live) or wire it to the real per-class conversion call site and decide whether a zero divisor should refuse instead of silently reading zero.

### X-3133 · hot_path_allocation.rs and its sibling hot-path-perf gate are proven theatre (X-150/X-151, still true)
CLASS:     instrument-blind
CERTAINTY: VERIFIED
EVIDENCE:  THE-LIST X-150/X-151 (VERIFIED) reused: proven three ways in the prior doc — commenting out both real assertions still passes 5/5; raising the perf budget 1,000,000× still passes with the row label unchanged; nothing in CI or any script ever runs the bench at all. For hot-path-alloc specifically: the 170-line counting-allocator instrument can be replaced with a 4-line stub and still pass 3/3. Cross-referenced against X-201 (file briefly deleted during a prior session, restored byte-identical) — file currently present and tracked (`git status --porcelain` empty).
ACTION:    Owner policy call (per X-150/X-151's own ACTION, covers this whole instrument-blind class) — either wire these gates into CI for real, or relabel them as documentation-only and stop reporting a passing row number as if it were a live measurement.

### X-3134 · busbar-kernel still lacks the busbar-kernel-identity dependency edge X-207 requires (Cargo.toml + auth/mod.rs)
CLASS:     auth
CERTAINTY: VERIFIED
EVIDENCE:  Same finding as X-3114, from the other side: `grep -n busbar-kernel-identity crates/busbar-kernel/Cargo.toml` → no match; positive control `git grep -l busbar-kernel-identity -- '*/Cargo.toml'` → 2 other files, confirming the grep mechanism works and the edge is genuinely absent. `busbar-kernel/src/auth/mod.rs:669`'s `AuthMiddleware::extract_client_token` is therefore structurally unable to call the shared unit even if someone wanted it to.
ACTION:    Add the `busbar-kernel-identity` path dependency to `busbar-kernel/Cargo.toml` and switch the caller — see X-3114.

### X-3135 · admin/v1/contract/schema.rs's ErrorBody claims a consistency cross-check with the hand-written Error schema that does not exist
CLASS:     missing-code
CERTAINTY: ADJUDICATE
EVIDENCE:  unconstructed-sweep.md row 21 (SHAPE 1a) reused: `ErrorBody`/`ErrorDetail` (schema.rs:459-467) doc comment claims "Kept as a schema-only type so the generated Error component matches the hand-written one exactly." `busbar-core-admin/src/v1/json/handlers.rs:4774` states the derived schema is explicitly NOT registered, and `git grep -n "ErrorBody"` finds no comparison anywhere between the two schemas. `ErrorBody`/`ErrorDetail` are never constructed as values.
ACTION:    Either add a real cross-check (a test asserting `schemars::schema_for!(ErrorBody)` matches the hand-written `Error` component so drift fails the build) or delete `ErrorBody`/`ErrorDetail` and the false claim.

### X-3136 · audit/journal.rs's runtime chain-tamper evidence is recorded but never drained or reported
CLASS:     instrument-blind
CERTAINTY: VERIFIED
EVIDENCE:  `take_resume_breaks` (journal.rs:179) — `git grep -n take_resume_breaks -- '*.rs'` → only declaration + `audit/tests/journal_tests.rs`. Module's own doc (`:154-160`) says a wrapper drains and reports a runtime-resume tamper-evidence break; no such wrapper exists anywhere. Positive control: the boot-time sibling `Restored::chain_breaks` IS drained/reported in production (`plane/auditlog.rs:875`, `plane_host/journal.rs:776`), proving the grep methodology finds real callers when they exist — so only boot-time tamper is currently visible to an operator, not tamper detected between boots.
ACTION:    Add the missing wrapper — have the plane-facing journal caller that currently ignores runtime resume tamper evidence call `take_resume_breaks()` after each resume and report it the same way boot's `chain_breaks` is reported, or delete the dead accumulation code and its misleading doc comment.

### X-3137 · auth/mod.rs's keys-arm identification path silently discards buffered credential-cache entries
CLASS:     auth
CERTAINTY: VERIFIED
EVIDENCE:  New finding, direct code trace: `pending_pass` (declared `auth/mod.rs:430`, buffered at `:454`) is flushed to the credential cache only inside the `AuthOutcome::Identify` arm (`:456-465`, reached when a plugin/boxed module identifies). At `:504-505`, when `self.keys_in_chain` and no plugin module identified, the function returns `keys_arm_verdict(...)` directly with no flush — so a chain shaped `[<cacheable module>, keys]` re-runs every module ahead of `keys` on every single request instead of caching them. This exact gap was described (VERIFIED, as a GAP) against a *deleted branch* comparison in `docs/design/1.6.0-branch-sweep-ledger.md:1056,1113`, but has not been promoted into THE-LIST.md or LEDGER.md against trunk (`git grep -n pending_pass` against both → 0 hits) — this is the first time it's recorded as a live trunk finding.
ACTION:    Flush `pending_pass` into the credential cache before/alongside the `keys_in_chain` return at `auth/mod.rs:504-505`, mirroring the flush already done in the `Identify` arm above it.

### X-3138 · calllog.rs's on-demand principal-chain verification has no production caller (already roadmapped, not new)
CLASS:     instrument-blind
CERTAINTY: PARK
EVIDENCE:  security-posture.md (adjudicated: "STRONG mechanism, PARTIAL on two honest caveats") reused: `verify_principal_chain` (calllog.rs:806) — `git grep -n verify_principal_chain -- '*.rs'` → only test callers, plus an independent sibling implementation in `proxy/reqlog.rs:407` with the identical, self-documented gap. The module's own header (`calllog.rs:96-107`) names this a real, accepted gap pending an admin "verify now" verb (security-posture.md §6) and explicitly asks not to be described as continuous verification — a tamper is currently detected only at the next restart.
ACTION:    Mount the "verify now" admin verb the existing roadmap already specifies, giving `verify_principal_chain` a production caller so tamper is detectable between boots.

### X-3139 · config_validate/mod.rs — only the LLM plane can boot-refuse an unpriced billing class (X-236, still open)
CLASS:     money
CERTAINTY: PARK
EVIDENCE:  THE-LIST X-236 (PARK) reused + reconfirmed: `sed -n '1425-1465p' crates/busbar-kernel/src/config_validate/mod.rs` — the completeness walk still iterates only `cfg.models.keys()` (the LLM plane's lane table); mcp `tools`, a2a `agents`, `streams`, `decisions` classes have no equivalent card-completeness boot refusal, even though `rate_card` presence is one global config key that bills the whole node.
ACTION:    Land a per-plane `rate_card`/`fees` reserved sub-key (X-236's own recommendation) and extend the completeness walk to every plane's declared `MeterClassDecl` set.

### X-3140 · config/named_map.rs's parse_rel's only "production" caller is itself gated behind a non-default test-support feature
CLASS:     instrument-blind
CERTAINTY: VERIFIED
EVIDENCE:  unconstructed-sweep.md row 100 (SHAPE 1b) extended: `parse_rel`'s one non-test call site (`admin/v1/contract/taxonomy.rs::declared_errors`) is itself only called from `busbar-core-admin/src/v1/json/mod.rs::record_declared_error`, gated `#[cfg(any(test, feature = "test-support"))]`; `test-support` is absent from that crate's `default` feature list (`grep -n 'test-support\|^default' Cargo.toml`), so this call chain compiles out of a normal release build.
ACTION:    Low severity (backs an internal contract self-check, not a customer-facing capability) — confirm the `test-support` feature is actually enabled wherever this openapi under-claim guard is expected to run in CI.

### X-3141 · config/prepass.rs's deploy_from_yaml_value has zero callers, including the one its own doc comment names
CLASS:     missing-code
CERTAINTY: VERIFIED
EVIDENCE:  unconstructed-sweep.md row 22 (SHAPE 1a) reused + reconfirmed: `deploy_from_yaml_value` (prepass.rs:486, re-exported at `config/mod.rs:41`) — `git grep -n deploy_from_yaml_value -- '*.rs'` → 2 hits total (declaration + re-export), zero callers anywhere, including the "management-surface overlay merge" path its own doc comment names as the intended caller.
ACTION:    Wire the management-surface overlay-merge path to actually call `deploy_from_yaml_value`, or delete the function and its re-export.

### X-3142 · config/sections.rs's RateEntryCfg f64 tier rates round down instead of half-away-from-zero, underbilling ~0.2% (M21/M-07, still open)
CLASS:     money
CERTAINTY: PARK
EVIDENCE:  Same root cause as X-3123, from the config side: LEDGER M21 / money-sweep M-07 reused — `RateEntryCfg`'s four `_utok: f64` fields (sections.rs:324-333) feed `nano_rate`, and `nano_rate(0.5005)` rounds to 500 not 501. `config/mod.rs:2783` confirms the module is reachable and these fields are the actual config-parse origin of the floats that hit rate.rs.
ACTION:    See X-3123 — same fix (read decimal text, not f64), applies at the source of the float.

### X-3143 · cost.rs's NANOS_PER_CENT is the one unpinned copy of a four-site money constant (R3, still open)
CLASS:     money
CERTAINTY: PARK
EVIDENCE:  LEDGER R3 reused + reconfirmed: `cost.rs:58`'s own `const NANOS_PER_CENT: u128 = 10_000_000;` has no static-assert pin against the ledger's canonical constant, unlike two other independent copies at `crates/busbar/src/root/units_a2a.rs:1216` and `units_voice.rs:164`, which each carry `const _: () = assert!(NANOS_PER_CENT as u128 == busbar_kernel_ledger::cost::NANOS_PER_CENT);`. The rate-clamp duplicate itself (D-2, the bigger prior finding at this file) is CLOSED — `cost.rs:149` now delegates to `busbar_kernel_ledger::cost::nano_rate`.
ACTION:    Add the same `const _: () = assert!(...)` pin at `cost.rs:58`, or delete the local copy and re-export the ledger's constant directly.

### X-3144 · egress_auth/jwt_bearer.rs's private-key Display leak (X-172) is fixed; two lower-severity serde_json Display leaks remain
CLASS:     auth
CERTAINTY: PARK
EVIDENCE:  THE-LIST X-172 (VERIFIED) is now STALE/FIXED: `sed -n '308,340p' jwt_bearer.rs` shows the base64-decode error is now classified by hand with no Display read of `base64::DecodeError` — the 6-bit private-key leak X-172 found is closed. Residual, reused from secret-hygiene.md: `serde_json::from_str(&sa_json).map_err(|e| format!("service-account JSON is invalid: {e}"))` at `:88` and a token-response equivalent at `:223` still format `serde_json::Error`'s Display (which typically reports type/position, not values, but is exactly the class of Display-string risk X-172 proved could be wrong) into a returned error string reaching a read-scope admin surface.
ACTION:    Update THE-LIST/LEDGER to mark X-172 CLOSED (cite jwt_bearer.rs:320-339 as the fix). Separately, owner ruling on whether the two residual `serde_json::Error` Display sites can ever echo secret-adjacent content.

### X-3145 · egress/interface.rs's EgressEngine trait and Engine struct have zero consumers, ever
CLASS:     missing-code
CERTAINTY: ADJUDICATE
EVIDENCE:  unconstructed-sweep.md section 4B reused + reconfirmed fresh: `git grep -n "egress::interface\|interface::Engine\|interface::EgressEngine" -- crates/` → rc=1, zero hits. The 157-line module's own doc names an intended migration ("port busbar-mcp/busbar-voice/busbar-llm onto EgressEngine instead of egress::engine::*") that never happened; module is reachable/compiled (`egress/mod.rs:36 pub mod interface;`) but is compiled scaffolding nobody calls.
ACTION:    Either port the three named consumers onto `EgressEngine` per the module's own stated plan, or delete the 157-line module as unrealized migration scaffolding.

### X-3146 · failover/mod.rs's repeatability/record_outcome/walk are pub with zero production callers, for a migration that has not reached its next phase
CLASS:     missing-code
CERTAINTY: ADJUDICATE
EVIDENCE:  unconstructed-sweep.md rows 378-379 reused: `repeatability`/`record_outcome`/`walk` are `pub` (visibility raised from `pub(crate)`) with zero production callers, but each carries a dated in-code comment explaining this is a deliberate "Phase-0 visibility lift" ahead of an unfinished "Phase-6 tighten-back" — tracked, in-progress scaffolding, not a silent drift. `repeatability`'s own comment states "No production caller now: both planes drive walk_with with the host breaker_admit seam directly (CLUSTER-1)."
ACTION:    Track whether Phase-6 lands and consumes these; if the migration stalls, revert the visibility lift and drop the `dead_code` allow per the comment's own stated plan.

### X-3147 · governance/mod.rs — same byte-identical window-boundary duplicate as window.rs (X-253, still open)
CLASS:     money
CERTAINTY: PARK
EVIDENCE:  Same underlying defect as X-3104: THE-LIST X-253 reused + reconfirmed — `grep -n "fn civil_from_days\|fn days_from_civil\|fn window_end" crates/busbar-kernel/src/governance/mod.rs` → `:930,946,959`, all three present and byte-identical to `busbar-kernel-budget/src/window.rs`.
ACTION:    See X-3104 — fold to one home (busbar-kernel-budget per Decision #36), make this copy a re-export.

### X-3148 · governance/state.rs's try_admit and two usage reads price money at the current rate card, not the rate in effect when spend occurred (M33/D-10, still open)
CLASS:     money
CERTAINTY: PARK
EVIDENCE:  LEDGER M33 (PARKED-OWNER) + money-sweep D-10 reused + reconfirmed: `try_admit` (state.rs:1782, the enforcement admission gate), `derived_bucket_usage` (`:1599`), and `budget_state` (`:1674`) all recompute a bucket's spend at read time against the CURRENT rate card rather than the card in force when the tokens were accrued — measured in the prior doc as a 12,330 micro-unit gap on a fixture. `cap.saturating_mul(10_000)` (`:1697`, D-10's separate USD-hardcoded-literal finding) also confirmed present.
ACTION:    Owner ruling per M33: whether `try_admit` (and the two usage reads) should price at accrual-time rate rather than current rate — a rate-card change currently changes who gets admitted retroactively for the same historical spend.

### X-3149 · hooks/wire.rs serializes a rate-card price to third-party webhooks as a raw f64, also used for routing decisions (M-23, still open)
CLASS:     money
CERTAINTY: PARK
EVIDENCE:  money-sweep M-23 (PARK) reused + reconfirmed: `cost_per_mtok: Option<f64>` (wire.rs:577, used at `:725`) carries a rate-card-derived price in raw `f64`; it is both serialized to a third-party hook endpoint as a JSON number AND read by `cheapest`-routing logic, so a float comparison decides which provider serves a request.
ACTION:    Carry `cost_per_mtok` as the fixed-point nano-rate type (or an explicit decimal string) across the hook wire instead of `f64`, matching #77(8)/#81's no-binary-float-on-money-path rule.

### X-3150 · inflight.rs's documented session-reservation admission control is never applied — every production InFlight table hardcodes reserve=0
CLASS:     money
CERTAINTY: PARK
EVIDENCE:  New finding, not previously raised in any 1.6.0-*.md. VERIFIED by direct grep with a positive control: `reserve_for(cap, any_session_transport)` (inflight.rs:57) — the documented "10% of the table held back for provider frames of already-open sessions" mechanism, module doc: "A node under load should shed new arrivals, not the paying conversation it is already having" — has zero production callers. All 4 production `InFlight::new(...)` sites hardcode the reserve argument to literal `0`: `crates/busbar/src/root/gauntlet_kernel.rs:296,397`, `units_admin/admin_mount.rs:44`, `units_llm.rs:335`. No setter exists to apply a reserve post-construction (`reserve: usize` is a private field set only in `new`).
ACTION:    Wire the production `InFlight::new` call sites to compute and pass a real `reserve` value via `reserve_for(cap, any_session_transport)`, or if the reservation was deliberately disabled, delete `reserve_for` and update the module doc to stop claiming the protection exists.

### X-3151 · hook_veto.rs's HookGate/gate() is a complete security seat with zero callers — both veto mechanisms in the tree are dead on the Approve step (X-124, still open)
CLASS:     auth
CERTAINTY: VERIFIED
EVIDENCE:  THE-LIST X-124 (VERIFIED) reused + reconfirmed: `git grep -n "HookGate" -- '*.rs'` → only decl + `tests/hook_veto_tests.rs`; `git grep -n "hook_veto::gate\|scope::gate\b" -- '*.rs' | grep -v /tests/ | grep -v _tests.rs` → empty. Positive control (`kernel_scope::` cross-crate hits for `admin_required_scope`/`PolicyView` in `units_admin/*.rs`) confirms the grep mechanism works.
ACTION:    Already stated in X-124 — install a caller for `hook_veto::gate()`/`HookGate` at the composition root's APPROVE step, or delete the seat and its "first veto wins" claim from the module doc.

### X-3152 · scope/lib.rs's is_kernel_granted bypass is never wired to any transport — the real /healthz bypass is a separate, older mechanism
CLASS:     auth
CERTAINTY: ADJUDICATE
EVIDENCE:  unconstructed-sweep.md rows 369-370 extended: `is_kernel_granted`/`ADMIN_SCOPE_TABLE` (lib.rs:194,270) — `git grep -n "is_kernel_granted\|ADMIN_SCOPE_TABLE" -- '*.rs' | grep -v busbar-kernel-scope` → 0 hits. The module's own doc (`:403`) says a transport must check `is_kernel_granted` before calling `approve()`; the real, shipped `/healthz`/`/stats`/`/metrics` bypass is a separate, independent mechanism (`HEALTHZ_PATH` path-equality checks) in `busbar-kernel/src/auth/mod.rs` and `plugin_routes.rs` that never consults this crate. `ADMIN_SCOPE_TABLE` by contrast looks deliberately test-only — a golden-reference table the live `admin_required_scope` fn is checked against row-for-row.
ACTION:    Either wire `is_kernel_granted`/`TRANSPORT_HANDSHAKE` into the transport(s) this crate's doc claims to serve, or state plainly in the module doc that this is unbuilt scaffolding and the real healthz bypass lives in `busbar-kernel/src/auth/mod.rs`.

### X-3153 · wal/backend.rs's SegmentFactory::is_durable has zero callers anywhere — a promised operator signal that doesn't fire
CLASS:     missing-code
CERTAINTY: VERIFIED
EVIDENCE:  `git grep -n "is_durable" -- '*.rs'` → 4 hits, all declaration/impl sites (both `MemoryFactory`→false and `DirectoryFactory`→true), zero call sites anywhere including tests. Positive control (`is_poisoned` → real production call site at `wal.rs:384`) confirms the grep mechanics. The doc comment explicitly promises "the log reports it so an operator can see which mode a node is in without inspecting a directory" — unmet; the actual operator-visible signal is a separate `Mode` enum on `Wal`/`Journal` set explicitly at construction. Secondary, prior-established: `segment_count` (unconstructed-sweep.md row 371) also test-only.
ACTION:    Call `SegmentFactory::is_durable()` from wherever the operator-facing durability/mode signal is actually surfaced, or delete it and its doc-comment promise since `Wal::mode()`/`Journal::mode()` already carries the information a different way.

### X-3154 · wal/journal.rs's Journal::resuming (the restart-safety constructor for no-data-dir+store nodes) has zero production callers
CLASS:     missing-code
CERTAINTY: PARK
EVIDENCE:  New finding, VERIFIED as dead code: `git grep -n "resuming(" -- '*.rs'` → 2 test-only call sites (`tests/journal_chain.rs:227,355`), 0 production. `crates/busbar/src/root/durability.rs::build_for_node` only ever calls `Journal::in_directory` or `Journal::memory_buffered_to`, never `resuming` — so on a store-backed node with no local data directory, every restart resets the chain to `head=0, next_seq=1` rather than resuming from what the store already acknowledged, exactly the scenario the module's own doc comments (`:758-765,812-819`) warn produces "a settlement lost behind an Ok." Not confirmed as an actual data-loss repro end-to-end (a separate "legacy rows" dual-write path in the production `Shipper` may cover this deployment shape) — marked PARK rather than asserting the loss is real. Secondary, prior-established: `hash_hex` (unconstructed-sweep.md row, SHAPE 1a) zero references anywhere.
ACTION:    Either wire `Journal::resuming` into the no-data-dir restart path in `durability.rs::build_for_node` (sourcing head/next_seq from the store), or get an owner ruling that the legacy-rows dual-write path already covers this deployment shape and relabel/delete `resuming` accordingly.

### X-3155 · wal/recover.rs's crash-tear signal (discarded_bytes, stopped_because) is computed but has zero consumers — an operator cannot see a crash-truncated segment
CLASS:     instrument-blind
CERTAINTY: ADJUDICATE
EVIDENCE:  New finding: `Recovered::discarded_bytes`/`stopped_because` (the fields that say "a crash tore a record and bytes were thrown away") are computed in `recover.rs`, but `wal.rs:237` (`wal.recovered = recovered.records;`) drops both on construction — `Wal::recovered()` only ever exposes `&[Record]` (`wal.rs:252-253`). `git grep -n "tracing::|log::|warn!|error!|info!" -- 'crates/busbar-kernel-wal/src/*.rs'` → 0 hits, controlled against 91 other files in the repo that do use `tracing::` macros. `was_torn` (recover.rs:46, unconstructed-sweep.md row) is also test-only.
ACTION:    Surface `Recovered::was_torn()`/`discarded_bytes`/`stopped_because` from `Wal::with_parts`/`Wal::roll` up through `Journal`/`Durability` to a log line or metric, instead of silently truncating and moving on. (Fix point is in `wal.rs`, outside this slice — flagged for whoever owns it; `recover.rs` is where the signal originates and dies.)

### X-3156 · wal/segment.rs's frames_remaining has zero callers anywhere
CLASS:     missing-code
CERTAINTY: VERIFIED
EVIDENCE:  unconstructed-sweep.md row 211 (SHAPE 1a) reused + reconfirmed: `git grep -n "frames_remaining" -- '*.rs'` → 1 hit total (the declaration, `segment.rs:206`). Core append/poison/roll logic is well-wired via `wal.rs`; the actual roll decision is reactive (`SegmentError::Full` returned from `append_batch`), not via this pre-check — looks like superseded/never-adopted API rather than a gap with consequence.
ACTION:    Either call `frames_remaining()` from wherever a caller would want a pre-emptive roll decision, or delete it.

## TALLY
files in slice:  83     (must equal slice file count)
verdict lines:   83
CLEAN:           27
FINDING:         55     rows raised: 57 (X-3100–X-3156, no gaps, no ids outside range; two ids each shared by 2 files — X-3106 by 5 files — reflecting one underlying defect with multiple file-level manifestations rather than duplicate rows)
DELETABLE:       1
UNREADABLE:      0

