<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- Copyright (C) 2026 Busbar Inc and contributors -->

# QUESTIONS — the queue, read by the owner at the end

**This session runs unattended. Nothing here was asked; everything here was queued.**

Rule 1.8 of `docs/design/1.6.0-KICKOFF.md`: when a slot hits something only the owner can decide,
it appends one row here, marks the item BLOCKED, and **moves on**. The run does not stop.

## What belongs here, and what does not

**Queue it** only when it genuinely needs the owner's authority:

- a **money figure changed** — the PARK file is the evidence, this is the flag (§8.7)
- a **customer-visible behaviour change** that no ruling covers (§9.5)
- a **cost** you were not authorised to incur
- a **phase cannot exit** and you have drained everything else (§1.12)

**Do not queue** a choice you can make yourself. If the code, the spec or an existing ruling
answers it, that is not a question — decide, record the assumption in your slot-log row, and keep
going (§1.10). A queue full of things the reader would answer with "you could have looked" is a
queue nobody finishes reading, and the real question gets lost in it.

## The one rule about writing a row

**State the cost of being wrong.** A question without it cannot be prioritised, and the owner is
reading this cold, after the fact, with no memory of your slot.

| # | slot | item | what I could not decide | what I did instead | cost if my choice is wrong |
|---|---|---|---|---|---|
| Q1 | P0A-116 | 116 (llm-conformance) | `testing/llm-conformance/fixtures/gemini-vertex-recording` goes RED through `run.sh` on the named-gaps gate ("a gap NO entry names") for the `ok`/`ok_stream` #response rows: three vendor fields `usageMetadata.trafficType`, `createTime`, `groundingChunks[].web.domain` are undeclared in the discovery doc (rev 20260207 and 20260923 alike — pre-existing, not caused by the repin). Declaring a gap is owner-locked (#59: an agent-added gap is RED) | left it RED; repinned gemini after a diff review (all changes additive, nothing busbar sends/reads moved); `selftest.sh` 20/20 green | if the gaps are real vendor fields and stay undeclared, that fixture's run.sh leg stays RED; nothing references the fixture today, so no CI job is currently red on it |
| Q2 | P0A-112 | 112 (`busbar-contract` `test-seal` feature) | whether the contract may keep ONE dev-only feature. The alternative, the kernel minting the test seal, is refused by kind-isolation (`new-forbidden-edge` ×11: plane/transport → kernel) and by 7 plane/transport purity tests; removing the feature entirely leaves 12 plane/transport harnesses with no legal seal (#65 sealed trait, planes may not name `caps`) | architect ruled: `test-seal` stays as a named, empty, third exemption in `feature_invariance.rs` (beside dispatch/runtime), guarded by `test_seal_is_dev_only.rs` (no non-dev edge may enable it); feature-sets satisfied by building it in CI, no gate exemption. The one-surface purpose (plugin and kernel disagreeing at the call) cannot arise from a type absent from every release build | if the owner wants the contract strictly feature-free: the 12 harnesses must be rewritten to receive kernel-built values over the ABI from a host (≈12 test harnesses, ~1 agent-day); no production or customer effect either way |
| Q3 | W0.6 | 536 (public-hygiene-lint long lines) | the lint now scans lines >4000 chars and finds 2 hits in `testing/shadow-oracle/accepted-differences.json:292` ([commit-hash-citation], [internal-issue-id]). That file is an oracle artifact (§9.2) — editing an entry's text is the owner's | left the file untouched; full-tree lint was already exit 1 before this change, now 2 hits more | none to customers or money; the public-hygiene full-tree run stays red on those 2 hits until the entry is re-signed (Phase 3 re-signs every entry anyway, BUSBAR-1.6.0.md Part 0 instrument 3) |
| Q4 | W0.15 | 318 (money — FIXED, owner told per §15.4) | nothing to decide; told because it reaches served figures. Named fault: broken derivation — `Posted::settle` took the overdraft FIGURE from the hold's spend counter and the settled figure from the priced total, so settled > reserved with counter 0 posted the OVERDRAFT flag with figure 0 and the book closed unbalanced | fixed (0046867e7 + test 8th follow-up): overdraft figure = settled − reserved when S > R. Before→after (R=100, S=250): posted.overdraft 0→150; Settlement.overdraft None→Some(150); ledger residual +150→0 | reaches the journaled Posting/overdraft record, the `overdraft` column served by units_admin postings, and the next-window overdraft carry at admission. If the owner wanted the 1.5.5 figure kept, revert 0046867e7 — but that leaves a book the identity check cannot balance |
| Q5 | W0.12 | 333 (verify-deploy checks now red when they could not run) | an owner-side action: getbusbar.com's edge (Cloudflare bot rules) answers GitHub-hosted runners 403/429. Item 333 made "a check that did not run" RED instead of a green no-op, so verify-deploy daily, its 3-hourly pointers job and release.yml's post-publish verify-deploy call will be red while the block lasts | fixed the instrument honestly; did not re-green it by skipping | none to customers; the red is true (the checks cannot see the site). Allow GitHub Actions through the bot rules, or run those checks from an allow-listed runner |
| Q6 | architect (phase gate) | Phase 0 exit test | nothing to decide; told because it changes the book's phase order. Phase 0's exit test (`cargo test --workspace --locked`) contains `xtask/tests/cli.rs::selftest_runs_every_registered_gates_red_proof`, which asserts `xtask selftest` over every gate exits 0 — that IS Phase 1's exit test (202 PROOF-IMPOSSIBLE cases across 12 gates, item 89). The two phases cannot exit in the book's order | per §4.5 ("the phase is its exit test"), Phase 1 stream A (items 9, 89, 90, 1a, 118/C1-KIND, 10, 2, 3, 87d/97, 119, 120, 121) is pulled forward; Phase 0 is NOT printed COMPLETE; both exit tests are judged together. Measured at 2093c7dc1+: full-gate --selftest 0; cargo test --workspace 8 failed (1 fixed: stale assertion; 4 need the store-example cdylib CI pre-builds; 2 qa-names + 1 cli = Phase 1). gate --all red 17→12, and now runs on every push | if the owner wanted Phase 0 closed first, the only other route was halting (§1.12) — nothing Phase 0 could do alone closes the cycle |
| Q7 | P1A-cs | 89/37b (config-schema) | config-schema's real-tree control is RED on 7 PoolMember field "removals" vs v1.5.3. The renderer cannot see through PoolMember's hand-written `Deserialize`, so fields that still parse look removed. Clearing it = fix the renderer AND regenerate `config-schema.snapshot.json` — a §9.2 artifact regeneration only the owner may approve | left RED; cured all 22 PROOF-IMPOSSIBLE cases with a green fixture base (98345d473), no snapshot/rule/case touched | none to customers (config parsing unchanged); cost is a standing red in config-schema until approved. Recommendation: approve renderer fix + regen, on condition the snapshot diff only re-adds those 7 PoolMember fields |
| Q8 | P1A-119 | 119 (Law 10 premise evaluation) | (a) PERMISSION: the fix is committed in the sibling repo busbar-release (`57da1eb` on branch `fix/oracle-recorder-residual-gaps`, the branch holding busbar's pinned ref ab88903) but NOT pushed — the run is only authorised to push busbar `predev`. Then `testing/shadow-oracle/oracle-rust.pin` release_ref must move ab88903→57da1eb. (b) RULING: Law 10 says an unevaluable premise "is refused"; all 41 existing entries have NO premise and are, for now, honoured + reported UNCHECKED-PREMISE (exit unchanged). Should UNCHECKED-PREMISE be RED? (c) Writing premises re-signs each entry (§9.2) — owner's, Phase 3; F-011b first (`status_not: 404` on an MCP/A2A cell) | built and tested the evaluator (6 tests: false/unevaluable → DIVERGING, true honoured, missing reported, malformed refused); left pin and branch untouched | until (a): CI keeps the blind oracle and F-011b keeps forgiving endpoints an LLM-only build lacks. Until (b)/(c): C3 cannot be judged honestly in Phase 3 |
| Q9 | P1A-9 | 9 (money-invariants now scans journalled records) | whether a settlement POSTING (`busbar-kernel ledger durability.rs:489`, journalled RecordClass::Transaction, storing `reserved/settled/overdraft` as i128 money figures) is a "stored price" under #77(3). The hold/settle protocol (items 99/100/127–129) is built on those figures; if they are prices, the money book itself violates "nothing stores a dollar" | did not widen the gate's tokens to them; gate stays RED only on AuditRecord.amount / Amount.priced / HookApplied.priced_delta (Phase 2 drain) | if they ARE prices and are left: C4 ("nothing stores a dollar") is false for the money book. If they are NOT (settled figures of a book, not a price list): no change. Recommendation: a hold/settle figure is a ledger fact in nano-units derived at settle time — the owner should rule whether #77(3) permits it or the book must store counts + card id only |
| Q10 | W1.5 | Q7 update + 164 + SecretRef | (Q7) RESOLVED without a regen: the classifier now sees through PoolMember's forwarding hand-written Deserialize — snapshot diff empty. (a) The real-render control is now RED on ONE TRUE finding: `manual-de SecretRef::visit_str` — v1.5.3 refused every bare string; since 33adc9906 the bare string `none` is accepted (explicit keyless secret ref). Customer-visible WIDENING vs 1.5.5 (§9.5): waive (one line in config-schema.waivers) or revert? (b) item 164: the config-schema snapshot omits the fifth plane's section; the fix (add busbar-plane-decision/src/config.rs to sources()) needs a snapshot regen that ADDS exactly one type, `DecisionsSection` {hooks, models, upstream_credentials; deny_unknown_fields} — additive vs v1.5.3 | (a) left RED, no waiver written; (b) measured the regen in a worktree, did not commit the one-line code change (it would red snapshot-drift until the regen is approved) | (a) none if waived (a config 1.5.5 rejected now loads); (b) until approved the schema instrument cannot see the decisions plane's config at all |
| Q11 | W1.1 | 376, 381 (construction ceilings) | both fixes are NEEDLE repairs whose true measurement exceeds the existing ceilings, i.e. formally a RAISE (§9.4 forbids an agent raising). 376: needle `busbar_core::` names a crate that no longer exists (a false zero) → `busbar_kernel::`; ports-only ceilings would go 0 → llm 266 / mcp 286 / a2a 249 / voice 89 (tests: 1000/179/99/43). 381: legacy-reach adds busbar_mcp 5, busbar_a2a 3, busbar_voice 12, strikes busbar_core/busbar_substrate (0 hits) → total 17→37, census 3→4 | BLOCKED both; ready patch with declared `[gate.ceiling_raises]` at /Users/matthew/Developer/tmp/busbar-run/W1.1-376-381-qa.patch (gate-checked at the pin: all PASS) | until approved these two rows keep measuring a crate that does not exist — a false green over real debt (Law 8). Recommendation: approve — a needle repair re-arming at the true count is not a relaxation of what the instrument ever saw |
| Q12 | W1.1 | 421 (money, PARKED) | see docs/design/1.6.0-PARKED/421.md: GET /admin/usage prices through `price_in_view` since 0ff99df5e; the construction/unconstructed register still records it as unshipped. Needs the owner's sign-off that this read-time pricing path is the intended one (#79: dated history by arrived_ms) before the toml row and the KNOWN_UNSHIPPED id are struck together | PARKED, no figure moved | the register keeps calling a live money path unshipped (a false statement in the money instrument) until signed |
| Q13 | architect (phase gate) | Phase 0 + Phase 1 joint exit | nothing new to decide — told because it is now the ONLY thing between the tree and a green Phase 0/1 exit. Measured on the committed tree (fc190fe1a): full-gate --selftest 0; clippy -D warnings 0; `xtask selftest` over every gate: 0 PROOF-IMPOSSIBLE, exit 1 on ONE case — config-schema's real-tree control, red on the SecretRef bare `none` widening (Q10a); `cargo test --workspace` 101 only through that same case (xtask/tests/cli.rs) | per §1.12 I am draining every phase-independent slot (Phases 2–4 do not depend on the SecretRef ruling) rather than halting; Phase 0/1 are NOT printed COMPLETE | if Q10a is answered "waive": one line in config-schema.waivers and both phases exit. If "revert": revert 33adc9906 (keyless local upstreams lose their explicit spelling) and both phases exit |
| Q14 | P2A-hist | 23, 32 (money reads reprice history) | already PARKED in BUSBAR-1.6.0.md ~2727-2748 ("TWO MONEY READS OF THE SAME CONSUMPTION DISAGREE") and LEDGER.md Q3 option A — re-confirmed and sharpened: `/keys/{id}/usage` + `/groups/{name}/usage` price at the CURRENT card because the budget ledger (`ModelTokens`/`UsageLedger`) stores no price instant (worked: 1,500¢ dated vs 1,200¢ served). The same derivation feeds enforcement (try_admit) and /metrics, so dating only the read would split enforcement from the read. 32: metering rows aggregate per UTC day × era, so a sub-day back-dated correction cannot reach its rows | BLOCKED both, no code; window stays all-time (v1.5.5 served WINDOW_TOTAL) | a customer whose card changed sees a /usage figure priced at the new card for old usage (ratecard fault). The fix needs the owner to rule on: `priced_from_ms` in the accrual key (hot path + every store's durable wire type), and how fine amendment boundaries may be |
| Q15 | P2A-a2a | 135 (money — FIXED, owner told) | nothing to decide on 135; told because a served figure moved. LEDGER fault: a2a metered the hop and stamped APPLIED before five gates could still refuse it. Now a hop refused before the socket (breaker open, SSRF guard, demotion, unleasable credential, in-handler gates) writes no metering row and is audited `rejected`; a hop that left gets `applied` after the relay (end of stream for streaming). Before→after: 2nd submission to a tripped agent: a2a metering requests 2→1; admin /usage requests + spend_micros for that row drop accordingly. REMAINDER still wrong: admission still charges +1 billable request (per_request_fee) for the refused hop — a2a has no refund door (only the kernel's llm ingress can refund) | FIXED metering (144a63201); fee refund needs a kernel BudgetHost refund seam (Phase 2 follow-up) | until the refund seam lands a refused a2a hop still costs one per_request_fee |
| Q16 | P2A-pool | 147 (security — FIXED, owner told) | nothing to decide; told because it is customer-visible on the admin API and config load. `PUT /config/settings` (and boot / `--validate`) now REFUSE (400 invalid_request, named error) any duration past the runtime's own 30-year horizon: eight `limits.*`/`health.*` seconds fields > 946,080,000 s, `advanced.usage_flush_interval_ms` + `routing.default_policy_timeout_ms` > 946,080,000,000 ms, the request-log-webhook `delivery_timeout_secs` (per-lane health/attempt timeouts being added). 1.5.5 accepted every u64 — and a value ≥2⁶³ poisoned the egress pool and killed the shard until restart. Also: a poisoned pool lock now recovers (pool cleared, logged) instead of panicking every request | FIXED (c168ec811, 1a1442afa). Did not touch openapi.json (§9.6) — it may need to state the new maxima and the 400 | if the owner wanted 1.5.5's accept-anything: revert 1a1442afa; the pool no longer dies either way (c168ec811 uses checked arithmetic) |
| Q17 | P2A-tests | 37 (money — FIXED, owner told) | nothing to decide — the change is ruled by OWNER-LOCKED #62; told because it is a 1.5.5 divergence customers see. LEDGER fault: a streamed LLM request cut mid-answer (transport cut, stream-ceiling timeout, in-band terminal error, translate abort, drop after any of these) recorded 0 tokens for tokens actually streamed. Now bills what streamed (tests: 0→18, 0→150, 0→1000, 0→1000 tokens); flat fee unchanged; a failure before the first byte still bills 0. Reaches token ledger, /usage and the metering series. ARCHITECTURE.md:411 records 1.5.5 billed zero here | FIXED (d034c93dc). Oracle cell route.failover|fo|primary-cut-stream unaffected (cut before its usage frame) | none if #62 stands; if not, revert d034c93dc |
| Q18 | architect (§9.6) | uncommitted core-admin change | KICKOFF §9.6: the working tree carries a pre-session, UNCOMMITTED change across `crates/busbar-core-admin/src/v1/json/{openapi.json, openapi.json.gz, handlers.rs, named_map.rs}` + `src/tests/tests.rs`: it adds `scheme` to AuditEntry's `required` array (TIGHTENS the contract) and documents a NEW `400` on a key-rotation path (a new customer-facing refusal). openapi.json.gz is include_bytes!'d into every binary | nobody commits it (§9.6); the files are left exactly as found | while it sits uncommitted, every slot touching core-admin must work around it, and the committed binary serves the old document. Decide: bless (commit as a signed §9.5 change) or discard |
| Q19 | P2A-flip | 125/126/36 + C3 money cells | (a) MEASURED (item 125): the money-authority flip moves NEITHER C3 money cell — flip ON and OFF recordings are byte-identical. (b) `billing|rate-card|history-mid-window` (1.5.5: 25,000,000 / 500,060,000 / 750,090,000; 1.6.0: 2,500,000 / 27,500,000 / 277,530,000) is moved by the `root-llm` dated-card repricer, i.e. OWNER-LOCKED #79 (price in force at arrival). With root-llm off it is byte-identical to 1.5.5. The cell's own note ("only the final read moves") is wrong arithmetic under #79. This needs an owner-SIGNED accepted-differences exception citing #79 (with a Law-10 premise the runner can evaluate) — an agent may not write it (#59). (c) `billing|key-usage|after-upstream-down` (250¢/18 tok → 0/0) is independent of both switches — cause untraced, carried to Phase 3. (d) item 36 FIXED: the plane ledger write is unconditional (#43); a billing-off MCP/A2A node's GET /api/v1/admin/usage now shows one row with requests 1 instead of an empty list (spend = flat fee × count, 0 at default) — touches the open "does an uncarded node charge the flat fee" question (Part 7 §13) | 36 + 126 FIXED (0ee95aafd, 21f725601); 125 docs corrected (826d485fe); nothing reverted, nothing parked | (b) until signed, C3 cannot pass on that cell; (d) if the owner wants billing-off nodes to show no row, revert 0ee95aafd — but #43 says the ledger always records |
| Q20 | P2A-auth | 144 (security + money — FIXED, owner told) | nothing to decide unless you disagree; told because it is CUSTOMER-VISIBLE and 1.5.5 had the same fail-open (v1.5.5 auth/mod.rs:1543, same three-part guard). BEFORE: a data-plane caller identified by a plugin/IdP module that earns no governance key (no roles, or the module has no role_bindings) was admitted 200 with key:None — no pool ACL, no budget, spend booked to `anonymous` (a LEDGER fault: spend attributed to nobody). AFTER: IdentityRefusal::NoGrant → 401 native envelope (403 insufficient_scope when OAuth resource metadata is configured); MCP stdio session bind fails. HIT: any deployment with an IdP/plugin auth chain and no role_bindings for that module, or an IdP asserting no roles. Fix for operators: bind roles under `auth.role_bindings.<module>`. Unchanged: keys/SigV4 virtual keys, `chain: []` open door, bound roles. Also 149: `verify` is GET (new 1.6.0 surface, no 1.5.5 parity impact) | FIXED (164bde9ac alone, c44bb5bbe); 4 busbar-llm tests asserting the fail-open being corrected | if the owner wants 1.5.5 parity: revert 164bde9ac — and the spend-to-anonymous hole returns |
| Q21 | P2A-stream | 137, 138, session ceiling (streaming plane; voice dialect) | (a) CUSTOMER-VISIBLE, decide keep/revert: a served voice session longer than `streams.session_max_secs` (DEFAULT 3600 s) is now HARD-CLOSED at the ceiling (27d447e02). The ceiling was documented and plumbed but never compared, so in 1.5.5 an over-long session ran on. A default of 3600 means every call longer than an hour is now cut. (b) 138 BLOCKED — the owner's: two voice pipelines, only ONE meters in production — busbar-voice's D2 lease, which prices token usage to NANODOLLARS at settle time (a stored price: violates #43/#71). The count-based pipeline in the streaming plane is registered but never driven. Fixing it = switch serving onto the plane's units (root units_voice.rs + main.rs compose_voice_governed_calls, which today passes Pricer::flat(0) and an unbound auth) and delete the lease pipeline. That CHANGES INVOICES: audio_seconds_in and tool_calls classes start billing; the lease's hard-close on budget exhaustion goes (this is the known "w5-voice deletion BLOCKED — money-byte" issue). (c) 137 FIXED — ledger fault (a Twilio stop / barge-in dropped the open turn's counters: audio_seconds_in 1→2, tool_calls none→1; barge-in 0→1/0→1) — no served figure moves today because the plane path has no production driver | (a) FIXED as ruled by the TODO item, reverting is one commit; (b) left as is; (c) FIXED (b6b883725, 4b28e3d64) | (a) calls >1h cut unless operators raise session_max_secs; (b) until switched, voice money is priced at write time by the lease and audio seconds/tool calls are never billed |
| Q22 | P2-foreign | pre-session edits touching customer-visible behaviour | (a) G-kernel `InstalledLimits` guard (appbuild.rs, lib.rs, test_support/fixtures.rs, main.rs hunk) is LEFT UNCOMMITTED in the tree: it rolls back process-wide limits when an admin config persist fails (1.5.5 said "nothing was changed" while body caps HAD changed — a bug fix). It cannot land without the uncommitted core-admin half (handlers.rs, named_map.rs, tests.rs), which shares files with the §9.6 openapi/scheme change (Q18). Whoever owns core-admin lands both halves in ONE commit. (b) REVERTED: mcp `tools.upstream_credentials` section default (X-3305 / LEDGER S23) — would start forwarding caller credentials upstream; no ruling, no exit test; snapshot `~/Downloads/busbar-1.6.0-snapshots/pre-session/crates_busbar-mcp_src_mcp_{catalogue,config}.rs.diff`. (c) REVERTED: mcp sampling input bounds (message count, prompt bytes, stop sequences, temperature range) — new customer-facing refusals, no ruling; snapshot `crates_busbar-mcp_src_mcp_sampling.rs.diff` | (a) left as found; (b)(c) reverted, re-landable from snapshots | (a) the limits leak on a failed persist stays until landed; (b)(c) behaviour = 1.5.5 |
| Q23 | P2A-panic | 148 (failure path, customer-visible — FIXED, owner told) | nothing to decide unless you disagree. BEFORE (1.5.5): a panic inside a request handler tore down the connection; on HTTP/2 every request multiplexed on it died. AFTER: only that request fails, with a 500 in the kernel's standard error body; /api/* gets the frozen {error:{code:"internal"}}, a mounted plane gets its JSON-RPC refusal, other paths get their dialect envelope. Only a request that panics is affected | FIXED (e60f8d95b) | revert e60f8d95b for 1.5.5 parity: a panicking request drops the connection again |
| Q24 | P2A-codec | 133 (money + customer-visible — FIXED, owner told) | nothing to decide unless you disagree. LEDGER fault: a usage count that is PRESENT but unreadable (e.g. {input_tokens:"1500",output_tokens:9}) was recorded as 0, so 1.5.5 billed input 0 / output 9. NOW: buffered cross-protocol → the request gets an error (CodecError::Malformed); streaming → the stream ends in an error event; voice duplex → an Error{code:"usage_unreadable"} turn. Where the refusal can only arrive AFTER the body was delivered (same-protocol relay, streaming), the request is billed like any request whose usage could not be recovered: the existing floor estimate, not 0 (being fixed now in P2A-ppb2). Cache-count fields follow the same rule (P2A-codec2). Also found and being fixed: a LIVE PANIC — a multi-byte unreadable count spelling crashes the reader (usage_count.rs truncate at byte 64) | FIXED (92227db57) + follow-ups in flight | revert 92227db57 for 1.5.5 parity: unreadable counts bill 0 again |
| Q25 | P2A-spend | 124, 25, 31, 28, 22, 27 (money + customer-visible — FIXED per #42, owner told) + one strike | (a) told: with a rate_card PRESENT, an UNPRICED model/lane/class no longer derives a fee-only figure. BEFORE (card gpt-4o 3/16, fee 2, 1M in + 1M out): the LLM door admitted on the fee (2 ¢, 19,020,000 µ dropped); /keys/{id}/usage, /groups/{g}/usage and /admin/usage showed 20,000 µ. AFTER: the request is blocked on `budget`; those usage reads fail with HTTP 500 (refusal logged); the /metrics gauge is skipped; the hook budget_state reports remaining 0. An overflowing figure is refused instead of shown pinned at i64::MAX. A rate below 0.0005 µ/unit (e.g. $0.10/GB per byte) was silently 0 and now refuses — such a config still boots (a boot-time refusal is being added). Tier rounding now has one rule: no production figure moves. (b) decide: the usage-read refusal is a bare 500 (the existing StoreError channel); the architect recommends it stays until you want a named code, because a new code is itself a customer-visible surface. (c) decide: strike qa/unconstructed.toml row `money-one-function-view` — its switch says "owner signs"; the one function now exists (789d55a78) | (a) FIXED (f81ec89fe, 029577a2d, 789d55a78); (b)(c) waiting | (a) revert 789d55a78 for 1.5.5 parity: fee-only figures for unpriced classes return; (b) 500 on a misconfigured card; (c) the row stays red |
| Q26 | P2A-hold | 99, 128, 26, 127 (operator-visible — FIXED, owner told) | nothing to decide unless you disagree. (a) 128: on a node with a data directory the admin ledger + reconciliation views now show the history REBUILT from the journal after a restart (1.5.5: they reset to empty and the reconciliation passed at 0 == 0). (b) 26: during a store outage a settlement whose journal append failed shows `unreconciled` rather than `settled` until a later append confirms it, so a row can briefly read short (ARCHITECTURE §4.2). (c) 99: a client that disconnects mid-request now leaves one durable posting row (settled 0, ESTIMATED) instead of none; no invoice or /usage figure moves. (d) PERF: each LLM request now journals its hold at entry, one more append per request at the durability class the operator configured (with an fsync class, one more fsync per request). Needed for a hold to survive kill -9 | FIXED (0fff8e2df, c05a45207) | revert c05a45207: restarts forget the book again and a crash loses open holds |
| Q27 | P2A-spend2 | 22 (config, customer-visible — FIXED per #77(5), owner told) | nothing to decide unless you disagree. A rate_card rate between 0 and 0.0005 micro-units per token (e.g. `input_utok: 0.0004`) booted in 1.5.5 and billed 0. It now FAILS VALIDATION at boot and on `--validate` with: `rate_card['<model>'].<tier> = <rate> micro-units per token is a rate the card cannot hold (non-zero rates run from 0.0005 to about 1.8e16), so the class would bill as unpriced; configure 0 to make it free, or a rate in range`. 0.0005 and an explicit 0 still pass; no config in the repo is affected. (28: the new 1.6.0 admin ledger totals/reconciliation reads now refuse an out-of-range figure instead of serving it pinned at i64::MAX; not a 1.5.5 surface) | FIXED (a9f72ef62, 69c58179a) | revert a9f72ef62: such configs boot again and every hit on that class refuses at runtime |
| Q28 | P2A-ppb2 | 133 addendum (money, INVOICE + served /usage — FIXED, owner told) | nothing to decide unless you disagree. When the usage of a response that was ALREADY DELIVERED cannot be read (a same-protocol relay or a stream), the request is now billed like any request whose usage could not be recovered: the existing floor estimate (the truncated-tail rule 1.5.5 already has). BEFORE: billed 0 tokens. AFTER, e.g. {input_tokens:"1500",output_tokens:9}: buffered Anthropic relay 0 → 51 tokens; OpenAI same-protocol stream 0 → 126 tokens; readable usage unchanged (1509 → 1509). It reaches wider than bad counts: ANY reader failure on a delivered same-protocol body (bad JSON included) now bills the floor instead of 0 | FIXED (86734752f) | revert 86734752f: those requests bill 0 again |
| Q29 | P2A-123 | 134, 123 (money + config, customer-visible — FIXED per #42, owner told) + one blessing | (a) told: Cohere rerank search units now reach the price. Card `rerank-v3.5 { units: { search_units: 2000 } }` (0.2 ¢/unit): 50 units 0 → 10 ¢; 1 unit 0 → 0 (truncated). Card PRESENT but silent about search_units: 0 → REFUSAL — the usage read fails and a group `budget:` bucket blocks until the operator prices search_units (a `total` window never rolls). No rate_card: 0 → 0; the shipped config.yaml has none. EVERY 1.5.5 deployment with a rate_card AND rerank traffic hits this refusal on upgrade. (b) told: new optional config `rate_card.<model>.units: {class: rate}` (micro-units per unit, integer or decimal string; a bare float and a reserved class under `units:` are boot refusals). (c) DECIDE: `config-schema:snapshot-drift` is RED on the additive `units` field — only you may bless it (`cargo xtask gate config-schema --write`, §9.2); it rides with Q10 | (a)(b) FIXED (d10cf4147, 84f2861bd); (c) waiting | (a) revert 84f2861bd: rerank bills 0 again; (c) config-schema stays red, the P0/P1 exit with it |
| Q30 | P2A-136 | 136 (served /usage shape — FIXED, owner told) + A2A blocked on §13 | (a) told: MCP tool calls now reach the budget ledger as a `tool_calls` count (5 calls → `tool_calls: 5` on the caller's group bucket). NO spend figure moves: a deployment's rate_card is the llm plane's card only (#42 per plane, #47), MCP has none, so the counts read 0; nothing refuses; `requests:` caps trip on MCP exactly as in 1.5.5. The new thing a customer can see is the tool_calls count in usage reads. (b) the per-plane `tools.rate_card` (#47) is being built so MCP can be priced and capped by money. (c) A2A stays unmetered: its only class is `bytes`, parked for you in BUSBAR-1.6.0.md §13 (billed-byte sign-off, #10/#59) | (a) FIXED (6a32b3a67 + 1f93dbd71); (c) waiting on §13 | (a) revert both: MCP calls leave no usage count again |
| Q31 | W2.24, W2.25 | 302, 303 (customer-visible failure signalling — DECIDE) | A generation the upstream reports as FAILED is relayed as a SUCCESS. (a) 302: a buffered Cohere body with finish_reason ERROR (tokens 10 in / 5 out) reaches an OpenAI client as HTTP 200 finish_reason "stop" (anthropic "end_turn", gemini "OTHER", bedrock "end_turn"), is billed 10/5, recorded Complete, and the lane breaker counts a success; root: busbar-llm buffered deliver_json never reads the stop reason. (b) 303: a streamed Gemini MALFORMED_FUNCTION_CALL ends as a normal stop (no error event): breaker unaffected, cross-protocol clients see a success terminator; its billing (tokens charged) is correct per #62. DECIDE: (1) keep 1.5.5 behaviour, or (2) surface these as errors (client gets an error; breaker records a fault) — and for the buffered case whether the upstream-reported 10/5 is still charged (#62 bills what a failed stream streamed; a failed buffered transfer bills 0 today). Architect recommendation: (2), charging what the upstream reports it used (the #62 rule applied to buffered), because a success for a failed generation misleads the client and hides lane faults | nothing changed | 1.5.5 behaviour stands |
| Q32 | owner (answered in session, 2026-09-24) | RULINGS on Q8, Q10a, Q11, Q12, Q14, Q18, Q19, Q21b, Q22, Q25b, Q25c, Q29, Q31, #47 fees | ANSWERED: Q10a approve all schema drift · Q18/Q22 land the limits guard, snapshot+revert the openapi scheme/400 change · Q31 failures surface as errors + charge upstream-reported usage · Q19 accepted-difference for billing|rate-card|history-mid-window (#79) · Q21b switch voice to plane units, delete the lease · #47 implement per-plane fees · Q29 keep the rerank refusal + refuse at boot · Q25b named 409 unpriced_class · Q14 date everything (#79) · Q11 re-pin 376/381 at true counts · Q12+Q25c strike both unconstructed rows · Q8 push the busbar-release branch + move the oracle pin | being executed | — |
| Q33 | W2.16, W2.28, unconstructed | 367, 362, #43 row, keyed-units strike | (a) told: 367 — a same-protocol non-stream answer the client drops mid-relay now bills what was relayed (176-byte prefix: 0 → 44 output tokens) instead of 0 (#62 applied to buffered relays). (b) told: billing-off LLM nodes now write a metering row per request (0 → 1 row; figures read 0; #43). (c) DECIDE: 362 — a streamed OpenAI-egress request with a wrong-typed `stream_options` (string/number/bool/array) is now refused 400 "Invalid type for 'stream_options': expected an object." before any send (1.5.5 forwarded it: strict upstreams 400, lenient ones served 200 with 0 tokens billed); `stream_options: null` is silently upgraded so usage is reported (client sees no change). (d) DECIDE: strike unconstructed row `plugin-abi-keyed-units` — item 123 wired the host decode of keyed units (govern.rs:259); the row says an owner signs | (a)(b) FIXED; (c) FIXED pending sign-off (revert 822d3f2b0 part); (d) waiting — unconstructed gate RED on it | — |
| Q34 | W2.5 | 382, 393, 383 (told) | (a) 382 money, LEDGER: a published store plugin at abi 2/3 merged a day's metering across an in-day rate-card edit into one row priced at the midnight card (100×A + 100×B read 200×A; A=3,B=5: 600 → 800). Now split. Rows written after a card edit carry `<provider>|priced_from_ms=<ms>` in the provider column — direct SQL readers and a 1.5.5 rollback see that string; no card edit = byte-identical rows. The external store-sqlite at abi 4 has the same merge (its own repo). (b) 393: a dropped-in `kind: transport` tarball's refusal text gains "transports are in-tree only". (c) 383: a non-SDK Rust plugin that lets a panic escape aborts the gateway (measured; nothing host-side can catch a foreign panic) — documented | FIXED/documented | — |
| Q35 | owner (answered in session) | §13/Q30c, Q33c, Q33d, Q22b/c, Q29-boot | ANSWERED: A2A billed byte = bytes relayed both ways per hop (class `bytes`, agents.rate_card) · keep the stream_options 400 · strike plugin-abi-keyed-units · re-land MCP sampling bounds (configurable, named, tested); upstream_credentials stays per-server opt-in (MCP is new in 1.6.0, no parity constraint) · Q29 boot rule is GENERIC: a plane with a rate card must configure every billable class the plane declares, the boot error names the missing ones; same for every plane | being executed | — |
| Q36 | owner (answered in session) | Q1, Q3, Q5, Q9 | ANSWERED: Q1 carry the 3 Vertex fields in the IR · Q3 reword the oracle rationales (no internal refs, meaning unchanged) · Q5 verify-deploy site checks move to a self-hosted runner (this repo pushes nothing to getbusbar.com; verify-deploy only reads it) · Q9 money is a view on ledger × rate card: durable postings store counts + card epoch, money derived at read | being executed | — |
| Q37 | owner (answered in session) | Q21a, Q31 follow-up, Q10b | ANSWERED: Q21a the voice session ceiling becomes an OPTIONAL setting of the streaming plane's own config (no default, no code constant; absent = no limit) · Q31 a same-protocol non-stream failed generation records the breaker fault (body unchanged, usage charged) · Q10b the decisions section joins the config-schema sources at the Phase 2 close bless (under Q10a) | being executed | — |
| Q38 | W2.3 (unconstructed row wal-corruption-verdict) | durability — DECIDE | On boot, the journal recovery truncates whenever it discards bytes, so a checksum failure MID-log (real data loss: postings, holds) is treated exactly like a clean torn tail from a crash and silently cut. The row asks: on a Corrupt verdict, REFUSE or ALARM? (a) refuse to boot, naming the file and byte offset, until the operator runs an explicit repair command that truncates (fail closed; no silent money loss) — ARCHITECT RECOMMENDATION; (b) boot, truncate, and raise a loud alarm/metric + audit record (availability first; loss is recorded, not prevented). A torn tail (crash mid-append) keeps truncating silently either way | not changed; unconstructed row stays declared | until ruled: mid-log corruption is silently truncated like a torn tail |
| Q39 | P2-verifyrunner | Q5 follow-up — OWNER ACTION (infra) | The getbusbar.com checks now run in their own `verify-site` job on the self-hosted `latchkey-small` runners (d33648d43). But the old combined job was ALREADY on `latchkey-small` when Cloudflare answered 403/429 — so Cloudflare is blocking those runners' egress IPs too. ACTION for you in Cloudflare (getbusbar.com zone): add a WAF custom rule / IP Access rule that SKIPS bot fight mode + rate limiting for the latchkey runners' egress IP(s) (or for a secret header — tell me the header name and I wire it from a repo secret). Nothing in this repo can clear it | workflow side done | verify-deploy site checks stay red until the edge lets the runner through |
| Q40 | W2.2 | 281 (operator /metrics — DECIDE) + 283/556/570 (told) | (a) DECIDE — `busbar_lane_state{pool}` today reads a lane's state across ALL pools: a pool whose own breaker cell is Open while a sibling pool's cell for the same lane is healthy reads 1 (half-open). The fix makes the gauge read that pool's OWN cell: it then reads 2 (tripped) — correct per pool — but state 1 (half-open) becomes unreachable for pool lanes unless the per-pool half-open probe state is exported too. Options: (1) per-pool own cell AND export the per-pool half-open state so 0/1/2 all stay meaningful — ARCHITECT RECOMMENDATION; (2) per-pool own cell, 1 unreachable, document it; (3) keep the cross-pool reading. (b) told: two plane claims differing only in ALPN case no longer refuse boot (contract byte-exact rule; no shipped plane declares such a pair); a hook plugin's diagnostics now reach the log; "OTLP tracing enabled" is no longer printed when the exporter failed | (a) patch ready, not landed; (b) FIXED | (a) the gauge keeps the cross-pool reading |
| Q41 | P2-coreadmin | openapi.json drift (DECIDE — §9.6 artifact) | Your Q18/Q22 ruling reverted the pre-session openapi edit, but the openapi drift test is still RED at HEAD: COMMITTED code already generates exactly those hunks — (1) `scheme` is required on AuditEntry (the audit record carries its scheme byte, pinned by W2.9/7c3306990), (2) POST /keys/{id}/rotate documents the 400 invalid_request Overlong response the handler really emits, (3) a restart_required_default doc-path rename. Choices: (a) REGENERATE openapi.json(.gz) so the published spec describes what the code does — ARCHITECT RECOMMENDATION (the spec must not lie; each behaviour is already in the tree); (b) revert the code producing those three hunks so the document stays byte-identical to the pre-session HEAD | openapi.json = HEAD + the Q25b 409 only | drift test stays red |
| Q42 | P2-billunits | generic billable-class boot rule (Q29/Q35 — FIXED, told) | Landed exactly as ruled. Upgrade impact to publish: EVERY 1.5.5 config with a `rate_card` now FAILS BOOT with `pools.rate_card does not configure billable unit(s) search_units declared by this plane; add them (0 to make them free)` — fix: add `units: { search_units: 0 }` to any card entry. Declared classes per plane: pools = input, output, cache_read, cache_write, search_units · tools = tool_calls, bytes · agents = bytes · decisions = decision · streams = audio_tokens_in, audio_tokens_out, text_tokens_in, text_tokens_out, cached_tokens, audio_seconds_in, tool_calls. For the 1.5.5 oracle (1.5.5 rejects `units:`), the Phase 3 harness adds these classes at 0 to the 1.6.0 run's config only — the operator upgrade step — golden untouched | FIXED (dbc02c8b9) | — |
| Q43 | P2-voice | Q21b voice invoices (FIXED as ruled — told) + Q21a | Voice now bills on the streaming plane's units, priced by `streams.rate_card` at read: audio seconds and tool calls start billing; with NO streams card voice reads 0 even if an llm card exists (it used to be priced by the llm card, and a present llm card missing the model closed the session on its first turn). Fixed script (card 1/token, streams card 1/count): before 18 (lease stored 180,000,000 nano), after 20 (audio_tokens_out 11, text_tokens_out 4, text_tokens_in 3, audio_seconds_in 1, tool_calls 1), nothing stored. A key whose requests/tokens cap is exhausted is refused a voice session (402); a live session closes when its budget runs dry from any traffic. Q21a: no session is cut unless `streams.session_max_secs` is set (then a `session_expired` error frame + settle). Being fixed: `cached_tokens` is a subset of input tokens so pricing it would double-bill — dropped from the billable classes (disjoint-classes rule) | FIXED (f87fd3800, 2beb4fb96) | — |
| Q44 | P2-fees | Q32 per-plane fees (FIXED as ruled — told) | Customer-visible money moves (24bb507d0): (1) MCP and A2A calls no longer carry the pools `per_request_fee` — budget gate, /keys and /groups usage, /metrics spend gauges all move: flat fee 5, 4 MCP calls on a plane with no `fees:` 20 → 0; a group at 3/day that used to 429 on MCP call 1 is now served and reads 0; with tools.fees.per_request 3, 2 LLM + 3 MCP calls 25 → 19. (2) New optional config `<section>.fees: {per_request, per_session}` (additive): 2 voice sessions at per_session 40 → 80. (3) Durable budget rows gain a `<plane>␟` model entry for fee counts; durable billable_requests counts pools-plane requests only. (4) /metrics gains a zero-valued `<plane>␟` model series. (5) A session fee is counted after the open-time dry check, so one session can pass a cap by at most one session fee. Being fixed by the architect: GET /admin/usage still prices MCP metering rows at the flat fee — made to agree with the budget book. |
| Q45 | P2-landpatches | rerank search units (FIXED — told) | Customer-visible money move (d7bcbaf2b): a same-protocol Cohere→Cohere or Bedrock→Bedrock rerank now reports its search units to both books. With card `rerank-v3.5: { units: { search_units: 2000 } }`, a rerank billing 50 search units goes from 0 to 10 cents on the invoice and /usage. A present card that does not price search_units now refuses it (#42); no card still reads 0. A cross-protocol rerank with a Bedrock egress now bills its search units, where before it billed flat. A Bedrock rerank body whose search_units is present but unreadable is now refused (item 133 rule). |
| Q46 | P2-tokcaps | token-family caps + undeclared units: key (FIXED as ruled — told) | (A) A `tokens:` cap now counts every class a plane declares in the token family, not only input/output/cache: the streaming plane's audio_tokens_in/out and text_tokens_in/out count. Example: group limit `{metric: tokens, amount: 100, per: minute}` with 60 audio_tokens_in + 40 audio_tokens_out: before read 0 and admitted, after read 100 and the next admission is refused. No LLM figure moves. (B) A rate card `units:` key naming a class the plane does not declare now refuses boot and --validate: `pools.rate_card configures unit(s) typo_class not declared by this plane (declared: …); remove them or fix the name`. The 1.5.5 upgrade card `units: {search_units: 0}` still passes. |
| Q47 | owner (answered in session, 2026-09-24) | Q38, Q40, Q41, role-bound subscriptions | ANSWERED: Q38 boot never blocked; mid-log journal corruption is quarantined byte-for-byte beside the segment plus a loud error log, a metric and an audit record, a torn tail is still cut silently, with a remediation page and no repair command · Q40 busbar_lane_state reads the pool's own cell (2 tripped, 1 only on a real half-open probe) AND the breaker is plane-agnostic (item 142 fold) · Q41 regenerate openapi.json at the Phase 2 close, upgrade notes listing the three changes · role-bound subscriptions: re-check through the live bindings, a removed binding ends the session | being executed (P2-walq done, P2-281, P2-rolebindfix running; openapi at the close) | — |
| Q48 | P2-cardhistory | rate-card history survives restart (FIXED per #79 / Q14 — told) | Customer-visible money correction (38ed759f2, 2c3bdcf61). Before, a restart rebuilt the dated rate-card history as the boot card from instant zero, so units that arrived under an older price repriced at the new one on /admin/ledger/totals and /admin/usage. Example: card A 3 then card B 5 µ per input token, 1M tokens each, restart at B: the book read 10,000,000,000 nano after the restart and now reads 8,000,000,000 (the figure served before the restart); the /admin/usage A-era row read 5,000,000 µ and now reads 3,000,000. A restart with no price change moves nothing. Signed back-dated corrections also survive a restart. A journal written before this change starts its history at the boot card. |
| Q49 | P2-usagegaps | /admin/usage prices every class the budget book holds (FIXED — told) | Customer-visible money move on GET /admin/usage (bda0fe068): figures now equal the budget book. With flat fee 5: 2 voice sessions at per_session 40 → 0 before, 80 now; 3 tool calls with tool_calls priced 7 + fee 3 → 9 before, 30 now; 2 reranks of 1M search units at 3 µ → 10 before, 610 now; mixed traffic 216 → 570; pools-only token traffic unchanged. by_model can show new rows (a plane's fee row, a plane's class rows). A present plane card silent on a class it now sees refuses 409 unpriced_class, as the budget book already did. A 1.5.x store plugin (abi < 4) has no column for the new counts and drops them with a one-time warning. |
| Q50 | P2-construct | kernel LOC ceiling (DECIDE — qa ceiling) | loc-ceilings:kernel is red: busbar-kernel measures 51,673 lines against a ceiling of 51,439 (+234). The growth came from Phase 2 money and security fixes you ruled: role-bound re-check, decision config checks, the amendment journal, the per-pool lane gauge, the named 409, per-plane fees, token-family caps. Every behaviour-neutral drain available now moves a kernel module into a busbar-kernel-* crate (e.g. plane_host/spki.rs, about 100 lines), which only raises THAT crate's kind-isolation cells and adds dependencies to it — a raise either way. Choices: (a) approve the kernel ceiling at today's measure (51,673) in its own commit, to be ratcheted back down by the Phase 4 fold: item 142 deletes the kernel's second breaker (store/in_memory/breaker.rs, ~580 code lines), which alone takes the kernel well under 51,439 — ARCHITECT RECOMMENDATION; (b) move modules into kernel-* crates now and approve their kind-isolation raises instead. Until ruled, the Phase 2 close CI run shows this one construction row red. | construction posture: loc-ceilings:kernel the only NEW red | the row stays red |
| Q51 | P2-landwire | adjust corrections on the budget-book reads (tied to Q50) | Since 4b6a113a9, a sealed `adjust` (e.g. 1,000 → 800 input units) moves GET /admin/usage (2,500 → 2,000 µ), but /keys and /groups usage and the /metrics spend gauges still read the uncorrected kernel budget cells, so the two books disagree after an adjust. The fix is a small read-path change inside busbar-kernel, which is already over its LOC ceiling (Q50). If Q50 is approved as recommended, the architect lands this fix in the same Phase 2 close and the books agree; nothing else is needed from you. |
| Q52 | P2-feeunits | fee keys a plane does not count now refuse boot (FIXED — told) | Customer-visible boot refusal (42d542400): a config that sets a NONZERO fee on a unit the plane does not count now fails boot and --validate. Every such fee charged 0 before, so no invoice or /usage figure moves. Texts: `streams.fees.per_request is not counted by this plane (counted: per_session); remove it` · `tools.fees.per_session … (counted: per_request)` · `agents.fees.per_session … (counted: per_request)` · `decisions.fees.per_request … (counted: none)` (and per_session). The decision plane counts no fee unit today because nothing serves it yet; when its serving path lands in Phase 4 it declares per_request. |
| Q53 | P3A-record | item 48 — record 1.5.5 on 18 new migration cells (DECIDE — golden addition, §9.2) | The migration corpus has never held the configs v1.5.3, v1.5.4 and v1.5.5 actually shipped. Adding them is ready (the migrator upgrades all 78 corpus configs cleanly). To compare boot warnings against 1.5.5 for them, the reference recording needs 18 NEW `config.migrate` cells recorded by running the PUBLISHED v1.5.5 binary on those configs. No existing golden cell changes; this only ADDS reference recordings of 1.5.5's own behaviour. Choices: (a) approve recording the published v1.5.5 binary on these 18 new cells (in CI, latchkey, the same way the golden was taken) — ARCHITECT RECOMMENDATION; (b) leave the three releases' configs out of the migration corpus. | patch ready | the three shipped configs stay untested |
| Q54 | P3A-gating | overlay reset sections (DECIDE — customer-visible, no ruling) | 1.6.0 (ee4486486) made two more overlay sections resettable — `identity-providers` and `export` — and reworded the refusal from 1.5.5's "expected `groups`, `hooks`, `root`, or `plugin_versions`" to "expected one of …". So DELETE /api/v1/admin/overlay/<unknown> now answers with 6 sections in different words where 1.5.5 listed 4. Choices: (a) keep the two new resettable sections (additive capability) and restore 1.5.5's exact sentence shape with the longer list, recorded as an owner-signed additive difference — ARCHITECT RECOMMENDATION; (b) remove the two new reset targets so the answer is byte-identical to 1.5.5. | architect restores the wording shape either way | the oracle cell diverges |
| Q55 | P3A-diverge2 / P3A-register | 1.5.5 differences recorded under owner-locked spec rows (told) | (1) spec #79 (arrival pricing, owner-locked): `ledger|currency|minor-unit-rounding` reads 2,500,000 / 2,500,000 / 5,000,018 where 1.5.5 read 18 / 18 / 54 — requests that arrived before a new card are priced at the card in force when they arrived (11×100,000 + 7×200,000 = 2,500,000 each), where 1.5.5 repriced everything at the latest card; `PostConfigApply` no longer reprices the window (1.5.5's budget_remaining +1 / spend −250 deltas are gone). (2) A non-stream body cut by the upstream mid-body bills only what the upstream reported, as 1.5.5 did (0) — an interim 1.6.0 change that billed a byte floor (720¢ / 36 tokens) is reverted. (3) #64: `operator_pub` appears among `auth:`'s expected fields. Each is recorded as a signed, premise-checked register entry. |
| Q56 | P3A-admin | new core admin verbs in the served spec for a 1.5.5 config (DECIDE — customer-visible, additive) | The served admin document (GET /api/v1/admin/openapi.json) is now filtered to configured planes (MCP/A2A paths vanish for a 1.5.5 config). But 1.6.0 also adds core admin verbs that are served on EVERY node regardless of config — the ledger, audit and money-governance verbs (adjust, ledger reads, audit reads, …), each marked x-busbar-since: 1.6.0. So a 1.5.5 config's served document = 1.5.5's paths + those verbs (every 1.5.5 operation entry byte-unchanged). Choices: (a) accept as an additive difference — new admin capability every 1.6.0 node has; register an owner-signed entry for admin.ops|GetOpenapiJson|ok — ARCHITECT RECOMMENDATION; (b) serve those verbs only when a config opts in (a new config switch), so a 1.5.5 config serves exactly 1.5.5's document. Verbs whose effect is not yet bound are removed from service and from the document either way (architect ruling). | recorded | the cell diverges |
| Q57 | W3.4 / W3.5 | upstream citations now reach clients (DECIDE — customer-visible, a data-loss fix) | 1.5.5 silently DROPPED citations an upstream sent: Cohere citations in both streamed and buffered answers (d87fa7e74 now carries streamed ones), and the Responses dialect's streamed annotations (W3.5, in flight). From 1.6.0 those citations reach clients of every protocol that can carry them (Anthropic, Bedrock, Gemini, Responses, Cohere). No oracle cell covers it (the corpus has no citation-bearing streamed response). Choices: (a) keep — the upstream sent them and dropping them lost content; add a CHANGELOG line and a fixture cell pinning the new behaviour — ARCHITECT RECOMMENDATION; (b) revert to 1.5.5's drop. | shipped on predev behind this row | clients start receiving citation events |
| Q58 | P3A-register2 | a 1.5.5-shaped rate card written through the admin API is refused (told — follows #42 + Q42) | At HEAD, PUT /api/v1/admin/config/settings with a rate_card that does not configure every billable class the build declares (e.g. no `search_units`) answers 400 "pools.rate_card does not configure billable unit(s) search_units…", where 1.5.5 answered 200. This is #42 (an unpriced class refuses, never a silent 0) applied at the admin write path, the same refusal Q42 already covers at boot. The operator upgrade step (add each class the build names, at 0) is the remedy for both paths, and the oracle harness now applies it to rate cards cells PUT to the candidate, exactly as Q42 ruled for boot configs. Release notes must say: an operator script that PUTs a 1.5.5 rate card must add the new classes. |
| Q59 | W3.1 | the three store plugin repos still depend on the deleted testkit (DECIDE — authorise pushes outside this repo) | Your ruling of 2026-09-22 ("delete it") removed busbar-plugin-testkit from this repo (2846dcff5). But GetBusbar/store-sqlite, store-mysql and store-postgres origin/dev still path-depend on it (`busbar-plugin-testkit = { path = "../../busbarAI/crates/plugin-testkit" }`), so qa-gate.yml's `loader` leg (and each store repo's own build against 1.6.0) dies at the missing sibling. The fix is in those repos: drop the testkit dev-dependency and carry each repo's Store conformance tests on the plugin ABI directly. This session is authorised to push only busbar predev, so patches for all three are being prepared under /Users/matthew/Developer/tmp/busbar-run/Q59-*.patch. Recommend: authorise pushing them to each repo's dev. Until then the qa loader leg stays red; busbar's own CI is unaffected (the kind:store fixture falls back to the in-tree example plugin, a8f3c93aa). |
