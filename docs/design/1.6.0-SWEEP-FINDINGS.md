# SWEEP FINDINGS — raw, as slices land

Merged into `1.6.0-TODO.md` once all ten report. Every row below was verified by the architect
independently of the agent that found it, or is marked UNVERIFIED.

## Slice 4 — the release engine. **This is the most consequential slice.**

**22 of 26 registered workflows are `disabled_manually` at GitHub.** Only four are active: CI,
qa-gate, Dependabot, oracle-rebless. A disabled workflow fires on NO trigger, and the disable state
is keyed by PATH — so promoting a branch does not clear it.

Disabled: Release · Release stage · Prepare release · tag-on-main · Verify deploy · Docker ·
plugin-ci · **CodeQL** · **MCP conformance** · **A2A conformance** · **Voice conformance** ·
**OAuth Conformance** · gate-mutants · keep-proof · fleet-control · CI images mirror · monthly
refresh · oracle record store cells · a2a-hang-hunt · runner probe · Security.

Three consequences, none of which any todo item named:
1. **The whole release engine is off.** A push to `main` cuts no tag; a push to `qa` produces no
   staged record — so `main`'s required context `record the staged digest` can never report.
2. **Every plane conformance workflow is off** — mcp, a2a, voice, oauth. The four planes whose
   conformance is a C2 acceptance criterion have no CI leg running.
3. **Security scanning is off.** CodeQL and Security both disabled.

**`qa → main` cannot fast-forward and cannot legally be made to.** `origin/main` holds **7 commits
reachable from no other branch** (README badges, a Codecov job, a 1.5.5 openapi regen).
`merge-base --is-ancestor origin/main origin/predev` → NO. `promote.sh` refuses non-ancestors and
never forces; `release.yml:530` independently refuses any sha qa never saw, which rules out the
merge-commit workaround by design; live protection has `allow_force_pushes: false`,
`enforce_admins: true`. The content is already re-landed on predev, so this is ancestry-only — but
the remedy is an owner protection change, not a code change.

**`predev → dev` has no mover.** `TURNSTILE_DISPATCH_TOKEN` is absent from the repo's 3 secrets.

**`ship-ready` is RED on all four rows** under a shipping posture, not just `standing-reds` — the
existing items understated it. `ship-twin` alone is 10 red rows.

**`cargo xtask full-gate --selftest` exits 1** inside a required check, on two rows: three
registered-but-unrun gates, AND two unclassified cargo invocations that no item names.

> **The verdict that reorders the whole list: if all 51 items went green tomorrow, the train still
> would not move.** Three stops — a missing PAT, a divergent `main`, and 22 switched-off workflows —
> and all three are in the plumbing, not the tree. The list measured the code and never asked
> whether the machine that ships it was switched on.

Two corrections the slice made to its own brief, both in the repo's favour: `verify-1.6.0-done.sh`
has **no** drift (23 declared, 23 defined, guard is two-sided) and **no** group calls a retired
script — all 23 resolve. Its real defect is that nothing in CI runs it (0 `run:` sites against
three positive controls). And `scripts/cost-watch.py` is **healthy** — selftest green, all four
bands proven on fixtures ($50/$80/$100/tool-error). It simply never fires, because its cron only
runs from the default branch and the file is not there.

## Slice 2 — crates a..busbar-k

**`crates/busbar/src/root/mod.rs:64` is a blanket `#![allow(dead_code)]` over 44 files and 38,356
lines, and its own stated removal condition is already met** — the comment says it comes off "when
`main()` calls into this module", and `main.rs` references `root::` **61 times**. This is a
measurement-suppression defect: one line disables the compiler's dead-code judgement across the
entire composition root, which is why everything below it was invisible.

**Repo-wide dead-code census, measured with `RUSTFLAGS="--force-warn dead_code"` per crate** (a
multi-`-p` run masks it — it reports 0 for the kernel family when the true figure is 19):

| crate | dead items |
|---|---:|
| busbar | 282 |
| busbar-mcp | 71 |
| busbar-a2a | 48 |
| busbar-transport-stdio | 41 |
| busbar-transport-grpc | 39 |
| busbar-transport-http | 37 |
| busbar-llm | 23 |
| busbar-core-admin | 22 |
| **TOTAL, 15 crates** | **643** |

Six blanket `#![allow(dead_code)]` exist in production code. Item 40's "~5,100 lines" was an
undercount of a different thing; the honest figure is 643 compiler-named items.

Money corrections to the existing list: there are **THREE** copies of the spend derivation, not two,
and they disagree on the unpriced rule (ledger → 0, kernel → 0, budget → `i64::MAX` and blocks). One
hardcodes `10_000` where another uses the named `MICROS_PER_CENT`, so the divisors are only
coincidentally equal. And this slice alone holds **22** saturating money sites against the 17 the
list claimed tree-wide.

`units_llm.rs:1048` — `history` and `arrived` are **never read**, while the doc beside them promises
the pinned snapshot exists "so a live apply mid-flight cannot price one unit two ways." That is the
root cause of item 53, made concrete.

## Slice 7 — config and served surface

**The config contract HOLDS.** 26 top-level keys at v1.5.5 → 32 at HEAD; six added, **none removed,
none retyped**. An unmodified 1.5.5 `config.yaml` parses and boots. v1.5.3 carries the same 26, so
the surface has been stable across two releases.

**The SERVED contract does not hold.** Law 7 says every plugin is off until its config verb is used.
Measured per plane:

| plane | verdict | gating expression |
|---|---|---|
| llm | INERT | `build: \|_\| None` — it is the 1.5.5 surface itself |
| mcp | **LIVE at boot** | `mcp_hydrate`: `if !ctx.has_store()` — gated on `store:`, not `tools:` |
| a2a | **LIVE at boot** | `a2a_hydrate`: `ctx.plane_store()` — gated on `store:`, not `agents:` |
| streaming | **LIVE at mount AND boot** | `mount.rs:489`: `ctx.public_url?` — a 1.5.5 key |
| decisions | INERT | everything returns empty |

**Only 2 of 5 planes are genuinely inert, and the root cause is in CORE, not in any plane.**
`crates/busbar-kernel/src/boot.rs:70-77` folds `plane_decls()` and calls every `hydrate` hook with
**no slot and no config check**. Item 25 named the voice symptom; this is the disease.

**The dup-claim guard watches the wrong field.** The ratified contract's verb is `config_section`,
and resolution is `.find(...)` — **first match silently wins**, which is exactly the "ambiguity
resolved by picking a winner" the contract forbids. The guard that exists reads
`owned_config_sections`, which 3 of 5 planes declare as `&[]`, so it cannot fire on any shipped
build. `pools` is claimed twice — by the llm plane as its verb and by core as concretely owned.

**No transport dup-guard exists at all**: `TRANSPORT_DECL`, `load_transport`, `scheme_claim`,
`claims_scheme`, `owned_schemes`, `check_owned_scheme` → 0 hits (positive control: 532 files contain
"transport").

**The pragmas' back-compat justification is false.** Three suppressions exempt `mcp` / `tools` /
`agents` as "frozen since 1.5.3". **v1.5.3 and v1.5.5 both carry 26 DeployCfg fields and none of the
three exists at either tag.** They are 1.6.0-additive keys wearing a back-compat exemption.

**26 admin verbs are served and appear in NO OpenAPI document** (18 of them money-governance), and
11 new admin paths mount unconditionally because both admin mount loops iterate `plane_decls()`
(linked) rather than `plane_slots` (configured). A side-car OpenAPI document exists explicitly so
the pinned bytes need not move, and says so in its own source.

## Slice 5 — the oracle. **C3 has never been tested.**

`crates/busbar/Cargo.toml:192`:
```
default = ["auth-admin-tokens","hooks-ranking","proto-llm","plane-mcp","plane-a2a",
           "plane-voice","plane-decision","root-admin","root-mcp","root-a2a","root-voice","root-llm"]
```
`scripts/verify-1.6.0-done.sh:555` — `parity_build_and_resolve()` — runs
`cargo build -p busbar --release --locked`. No `--no-default-features`. **Every plane is compiled
into the binary the oracle records.** The only `--no-default-features` in the script is at `:670`,
a build-only smoke step that never reaches the oracle. Positive control: five other scripts use the
flag, so its absence here is a choice, not an oversight.

C3 says *"LLM-only 1.6.0 ≡ published 1.5.5."* The instrument has recorded an all-planes build for
the entire release. **C3 is not weakly evidenced. It is unevidenced.** Every other number below is
downstream of that.

### The denominator, layer by layer
| stated | measured | honest |
|---|---|---|
| 2,318 cells | **916 owed** | **≤371 fully exposed**, on the wrong binary, against a reference of unverified size |

- 1,402 not owed: 1,380 are a2a+mcp (planes absent from 1.5.5 — correct), **22 are in-scope cells
  with no golden**, 9 of them LLM, including all six `stream_upstream_error` cells and
  `ok_tool_use_tokens` — the exact cell exercising the `G-1` money entry. 14 of the 22 are named
  nowhere.
- 545 of 916 carry a class-wise waiver; **371 are fully exposed**. 163 have `body` forgiven.
- **The LLM plane's entire oracle surface is ONE verb.** The only `op` value across all 2,318 cells
  is `chat` (142 cells, 133 owed). No embeddings, rerank, speech or images.

### `--strict` has no floor — the floor is ONE cell
Proven empirically: the agent built the pinned judge and ran it with a golden carrying one `PASS`
row → `owed: 1`, `unbaselined: 2317`, **exit 0**. `assert_parity_verdict` passes the same report.
`run.rs:2511` sets rc=1 only when `owed.is_empty()`. Repo precedent: `DENOMINATOR_FLOOR = 1_700`,
`SCAN_FLOOR = 200`, `BINDING_FLOOR = 40`.

### The DONE script re-records its own reference
`ci.yml:3316` and `oracle-proof.yml:212` copy the committed golden in and assert
`pass == meta.recorded > 0`, with the comment *"A reference that is re-derived on the machine under
test is not a reference."* `verify-1.6.0-done.sh:957` re-records into `target/`, reuses a stale one
if present, and checks only the BINARY's digest — never the recording's completeness. The DONE claim
runs the exact practice both CI jobs name as the thing that went wrong.

### Other C3 findings
- `BODY_IS_CONTRACT`'s ONLY effect is `body` (status/missing.* are already weight 10 everywhere).
  **`llm.wire` is off the list**, so `body` rates 3 there and a plain `improvement` can forgive it —
  62 of 133 LLM cells have exactly that. **All 5 `ledger` cells have `body` waived, 4 by an
  improvement.** `billing` is off the list too.
- **Under-matching a register entry is a `note`, not a red** (`run.rs:1360`). Coverage shrink is
  silent by construction — and has already happened, which is what item 12's 4 dead entries are.
- **Two `breaking` entries carry in-file AUDIT NOTES saying their own premise is false today** —
  `F-011r` (the change was reverted by `3c118f729`, an ancestor of HEAD) and `D-3` (`as_of` is
  parsed nowhere in the admin layer). Both still grant their classes.
- **The admin corpus is a hand-maintained fixture frozen at the 1.5.5 op set** (66 ops,
  `fixtures/admin-bodies.json`). Nothing reconciles it against the served router. The 11 new admin
  paths have zero cells AND no mechanism that could ever give them one. `cells --check` is green
  because it compares the corpus to what the generator derives from that same frozen fixture.
- `BUSBAR_ORACLE_RUST_BIN` and `BUSBAR_RELEASE_CHECKOUT` **repoint the judge itself** and are the
  two vars NOT in `assert_bless_env_empty`'s list of 11.
- The 1.5.5 golden's provenance is **impeccable** — verified end to end against the live GitHub
  release, binary sha `84bde0a0…` matches. But its `harness_rev` is the retired Python harness,
  which is why `--allow-harness-skew` must be passed to use it at all.

## Slice 3 — crates busbar-l..z

- **All four non-LLM planes ship DEFAULT-ON** (above). Part 0 says they are "purely additive and off
  by default" in four separate places. Item 25 caught only streaming, and only via `voice_build()`'s
  `public_url` gate — missing the Cargo-feature mechanism entirely and the other three planes
  completely.
- **Two streaming plane crates both ship and both register**: `busbar-voice` (13,353 loc, old arch,
  `main.rs:300`) and `busbar-plane-streaming` (5,135 loc, new arch, `registry.rs:94`).
- **`busbar-plane-llm` has ZERO `PLANE_DECL`** and one production reference repo-wide. The plane C1's
  sharp test and C3's byte-identity both rest on is the least-wired of the five.
- **For all 7 kinds the only `cdylib` artifact is an unpublished test fixture.** 6 such crates, all
  `publish = false`. Every shipped implementation is compiled-in-only. Transport is worse than item 6
  states: absent from `KNOWN_KINDS` (`sign.rs:70`), absent from `supported_abi`, and no
  `TRANSPORT_DECL` — four independent absences.
- **3,653 lines of "BUILT DARK" unit scaffolding in `busbar-llm/src/unit/`** with no production
  caller, inside the plane C3 rests on. Item 40 measures a disjoint ~5,100 lines under `root/`, so it
  covers 58% of the real dark surface; the missing 42% is the LLM path.
- **99 `pub fn` with zero production callers** across 23 crates.
- `busbar-plane-a2a/src/a2a/anomaly.rs:105` — `pub egress_spend_ratio: f64`, a float on a
  spend-named field, inside the gate's count-read roots but outside its no-float walk.
- Zero plane→plane dependency edges (positive control: the scanner found all 7 transport edges).
  Of those 7, only 3 are real `[dependencies]`; 4 are dev-deps. Item 7's flat "7" overstates.

## Slice 6 — money. **An OVERCHARGE, and it inverts the basis of owner ruling 8.**

### The flat fee is billed on failed requests

`busbar-contract/src/records.rs` documents both fields itself:
- `:679` `requests` — *"ADMISSION count: every admitted request, NEVER refunded... A failed request
  still consumed a request slot"*
- `:683` `billable_requests` — *"BILLABLE request count: admitted requests MINUS the non-2xx
  refunds — **the flat-fee base (the fee bills 2xx only)**, so budget SPEND derives from this, not
  from `requests`."*

The read path bills on `requests`: `service.rs:128` `derive_spend_micros(..., b.requests, true)` and
`service.rs:273` `.with_fee_count(b.requests)`.

**The root cause is at the WRITE, not the read** — this corrects the finding as first reported. The
only non-test `MeteringDelta` construction in the tree is `governance/state.rs:954`, and at `:963`
it reads:
```rust
requests: counts.requests,
billable_requests: counts.requests,
```
The live writer copies the admission count into the billable field. The correct number is not
"sitting unused on the record" — **it is never computed on the live path at all.** The only writer
that honours the distinction is `busbar-llm/src/unit/meter.rs:482` (`billable_requests: 1`, set only
on a billable outcome), which lives in the 3,653 lines of dark unit scaffolding with no production
caller. Reader and writer therefore agree, on the wrong number, and repointing the reader at
`billable_requests` would change nothing.

**Every failed request incurs the flat fee. The overcharge is `fee × failures` per key per window.**

> **This inverts the factual basis of owner ruling 8.** That ruling — correct a 1.5.5 billing error
> with no customer restatement — rested on the error being an UNDER-charge: customers paid less than
> the rate card specified, busbar absorbed the loss, nobody was owed money. This defect has the
> opposite sign. Customers are charged for requests that failed. The reasoning that made "no
> restatement" right for G-1 makes it wrong here, and the owner has not been asked about this one.

### Four live endpoints answer four different figures for the same window
After a rate-card change, for 1,000,000 output tokens settled at 15 micro/token:

| endpoint | figure | card used |
|---|---|---|
| `/admin/usage` | 15,000,000 micro | **dated** — correct per #79 |
| `/admin/keys/{id}/usage` | 1,200 cents | current — reprices history |
| `/admin/groups/{name}/usage` | 1,200 cents | current — reprices history |
| `/admin/ledger/totals` | 15,000,000 micro | stored settlement |

Two of the four silently reprice history that #79 says must never be repriced. The Prometheus
gauges make a fifth answer: `metrics.rs` contains zero occurrences of `busbar_kernel_ledger`,
`History`, `price_in_view` or `rate_history` across all 1,359 lines, and its own HELP string ships
the claim *"reprice-on-read … CURRENT rate card"* (`:1276`). An operator's budget alert and the
customer's invoice disagree by 20% with no error raised anywhere.

### Nine of ten ledger mutators are dead
`Ledger::record_hold_opened`, `record_draw`, `record_slice_spent`, `record_release`,
`record_adjustment`, `record_adjustment_releasing`, `record_cross_window_transfer`,
`record_unreconciled`, `record_repricing` — all zero production callers (`settle.rs:251-356`). The
only live book-moving call is `Durability::settle_posted → ledger.post`. So `drawn`, `adjustments`
and `unreconciled` are **structurally always zero**, and the residual the reconciliation endpoint
reports is arithmetically incapable of being non-zero.

**Bank-auditor verdict: NO.** Every function that refuses an unpriced line or repairs a wrong rate
card is dead code. The books can move in exactly one direction, by exactly one function.

## Slice 10 — root and infra (207 files, the coverage hole)

**Corrected on verification.** The agent reported `.keep-proof.toml` as cutting the shadow oracle
from 2,318 cells to 161. The filter is real — `^(llm|billing|ledger)` = 161 cells, 6.9% — but the
blast radius was overstated. `ci.yml` (`:52,:55`), `verify-1.6.0-done.sh` (`:1004`) and `bin/oracle`
(`:164`) mention keep-proof **only in comments**; none reads the file. `manual-keep-proof.yml` reads
it and is `disabled_manually`. The one live consumer is `scripts/prove-remote.sh:64`. So it narrows
that script, not the oracle generally. Still owed: it is a keep-branch artefact whose own header
says *"The INTEGRATION tree carries no such file"*, sitting on `predev`, and it names 3 packages
that do not exist.

Other findings: **the migration corpus has no entry for v1.5.3, v1.5.4 or v1.5.5** — the corpus
proving "every shipped config migrates" has never seen the shipped release. Its gate computes a
completeness denominator and discards it, asserting only `n >= 20` against an actual 69. 140 corpus
files are only 38 distinct by md5.

The `Dockerfile` claims *"Ships with ZERO plugins pre-installed"*; `auth-admin-tokens` and
`hooks-ranking` are both in the default feature set. `plugins.yaml`, "the single source of truth for
what plugins exist", expresses 4 of 7 kinds — export, plane and transport cannot be registered.
`.githooks/pre-push` is not a gate: no-op unless an env var is set, nothing installs it, and
`core.hooksPath` points at a directory that does not exist.

`assets/readme/generate.py:118` holds a hand-maintained 6-dialect roster — the file the coverage
census was created to catch. Nothing runs the generator; the 8 SVGs it produces are pinned at
1.5.1 while the repo builds 1.6.0.

## Slice 9 — the spec against the code. **118 claims checked, 61 wrong.**

44 stale (the code moved ahead), 17 false (never true, or never built).

### The GOLD statement: 7 clauses false, 4 true, 2 partly
FALSE: core knows nothing about a protocol (kernel hardcodes all five config verbs at
`prepass.rs:131`, types a struct field per plane, names `anthropic` as the protocol default) ·
everything protocol-aware is a plugin (the four crates that actually serve traffic are not plugins
and carry `reqwest`/`axum`/`hyper`) · core holds no list of instances (a fixed five-element list
plus 7,766 lines of per-instance unit logic) · compiled-in or dropped-in over the same contract
(true of zero kinds) · core asks transports who handles a scheme (`TransportMeta` has no scheme
member — #50 is entirely unbuilt) · only the thing itself may name itself (3,047 plane-vocabulary
hits in `busbar-kernel`) · adding a dialect changes nothing in core (untestable as stated: the six
llm dialects live in a separate 105,589-line crate, not in the plane, so Law 5 is unmet and the
sharp test has no defined boundary).
TRUE: 7 kinds · 5 planes · two lanes, one per kind (`kind-abi-lane` green) · the 7 transport edges.

### Part 3 outranks Part 2, and Part 3 is the section that is wrong
Part 3 §3 asserts ONE seam — *"Registered built-in or dynamic — SAME decl."* The tree has **two
unrelated `PlaneDecl` types**: `#[repr(C)]` at `busbar-plugin/src/hot/decl.rs:132`, constructed by
exactly one crate (the fixture), and native Rust at `plane/registry.rs:811`, used by all five real
planes. Only Part 5's ruling 9 — the LOWEST authority Part — records the truth.

### Instrument #2's hard clause is unenforced by both instruments that claim to enforce it
`LAW0_ENFORCED_NEUTRAL_CRATES = &[]` (`kind_isolation/matrix.rs:129`), so the ARMED class blocks
nothing on the everyday gate. The `[[cell]]` ceiling for `busbar-kernel × plane` was raised
**1 → 3047**. And `plane-purity:key` is GREEN only because 11 in-source
`// plane-purity: frozen-wire` pragmas delete the hits before judgement
(`plane_purity/scanner.rs:256-258`). The doc's clause bars ratchet rows and says nothing about the
pragma, which is the live escape.

### Already DONE — the doc lists these as owed
testkit deleted · Pass/Grant rename landed (#73) · `UnitKey::new(0)` fixed (0 hits) ·
`busbar-caps` folded · `busbar-core` deleted · the branch collapse landed (origin has 4 branches,
not 817) · **two of the three #40 edges closed** — `api→kernel-ledger` (records moved to
`busbar-contract`) and `plane-decision→busbar-kernel`; only `kernel→substrate-values` is live ·
the whole L3828-3841 section on the money book being in every plugin's closure is superseded.

### Numbers in the doc that are now wrong
22 groups → **23** · 12 construction FAIL rows → **16** · 57 crates → **49** · closure of 6 → **5** ·
`ceiling-rose` stale base → resolves fine, 10 raises not 123 · three mutually inconsistent hard
dates survive in one file (2026-10-08 twice, 2026-09-25 once) against ruling 10's "forget a date".

### Refuted a live todo item
Part 7 item 13 says `METER_CLASSES` is read in production by nothing. **False** — read on three
production paths (`units_voice.rs:937,994`, `units_mcp.rs:1803`). Law 6 holds.

Eleven citations in the spec name a path or symbol that does not exist, including
`PlaneMeta::CONFIG_VERB` — the symbol #49 is written around, which has never existed.
