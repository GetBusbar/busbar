# 1.6.0 — the LLM plane's switch-over: what is served, what is proved, and what is left to delete

Measured on `integration/oracle-phase0` at `ad8887f2b`, with `--features root-llm`.

## 0. The premise this document corrects

The switch-over was planned against a tip where `root-llm` was **default off** and the two legs ran
side by side, so that the root leg could be flipped on one class at a time and compared against a
live legacy twin. That tip is gone. `root-llm` joined the default feature set at `39e8dbf0e` ("the
llm plane ships through the composition root by default"), and three facts follow from it that the
plan has to be re-cut around:

1. **The root leg already serves every class.** There is no class waiting to be switched on. The
   shipped binary answers all six dialects through the kernel's loop over the plane's nine step
   files. The remaining work is not flipping, it is **deleting** — the legacy leg is still compiled,
   still reachable with the feature off, and still the path the plane's own test binary drives.

2. **Both legs call the same engine.** `unit/route.rs` reached
   `engine::pipeline::forward_with_pool_parsed_inner`; `native_ingress.rs:479` reaches
   `engine::forward_with_pool_parsed`, which is a shell around that same core. The engine
   (`engine/{select,pipeline,exhaustion}`) is not duplicated between the legs. It is duplicated
   between `busbar-llm` and `busbar-unit-egress`, which is a different problem with a different
   owner (MOVE 7).

   The consequence for measurement is sharp: **no outcome class can diverge between the legs inside
   the engine**, because there is only one engine. Divergence is only possible in the shell and in
   the arrival. That is where this document looks, and that is where it found the one real
   divergence.

3. **"Byte-identical, zero diverging" is a claim about the wire only.** The golden judges what a
   client and an operator can see. It cannot judge what neither can. Section 2 is the list of things
   it cannot judge, and section 1.3 is the defect that lived in exactly that gap.

## 1. The measurement

### 1.1 What the oracle actually covers

`testing/shadow-oracle/cells.json` — 2,318 cells, of which the LLM money path is 178:

| family | cells | `op` axis | `transport` axis |
|---|---|---|---|
| `llm` (`family: llm.wire`) | 142 | `chat` — **all 142** | `http` — all 142 |
| `billing` | 14 | n/a (administrative views) | — |
| `route.failover` | 16 | n/a | — |
| `ledger` | 5 | n/a | — |
| `cooldown` | 1 | n/a | — |

The 142 LLM cells are a 6×6 ingress-dialect × egress-dialect matrix (anthropic, openai, gemini,
bedrock, responses, cohere) crossed with an outcome class:

| outcome class | cells | root leg vs legacy |
|---|---|---|
| `ok` (buffered chat) | 36 | **PASS** |
| `upstream_down` | 36 | **PASS** |
| `ok_stream` | 30 | **PASS** |
| `malformed` | 6 | **PASS** |
| `out_of_scope` | 6 | **PASS** |
| `over_budget` | 6 | **PASS** |
| `over_budget_total` | 6 | **PASS** |
| `stream_upstream_error` | 6 | **PASS** |
| `unauthenticated` | 6 | **PASS** |
| `ok_cachepoint_document` (bedrock) | 1 | **PASS** |
| `ok_stream_array` (gemini) | 1 | **PASS** |
| `ok_tool_use_tokens` (gemini) | 1 | **PASS** |
| `ok_citation` (responses) | 1 | **PASS** |
| `billing|*` (key/group/admin usage, rate-card epoch + history) | 14 | **PASS** |
| `route.failover|{fo,fb,lb}|*` | 16 | **PASS** |
| `ledger|{amend,currency,rate-history}|*` | 5 | **PASS** |
| `cooldown|trip-then-serve` | 1 | **PASS** |

PASS here means: replayed against the published 1.5.5 recording with the root leg serving, at zero
diverging. That measurement was made at the default flip and is re-made by this branch's proof leg.
It is a strong result and it is **not** the interesting part of this document.

### 1.2 What the oracle does not cover — the UNSERVED list

Every one of the 142 LLM cells carries `"op": "chat"` and `"transport": "http"`. The golden
therefore says **nothing at all** about:

| class | status | why it is unserved |
|---|---|---|
| embeddings | **UNSERVED** | no cell has `op: embeddings`. The engine forwards it (`native_ingress.rs`: "every operation — chat included — forwards through the same failover/breaker/policy pipeline"), so it is *served by the code* and *unjudged by the oracle*. |
| images, audio (transcribe/speech), moderations, rerank | **UNSERVED** | same — no cell on the operation axis at all. |
| tool calls as a round trip | **UNSERVED** | `gemini|ok_tool_use_tokens` is a *token-accounting* cell, not a tool-call exchange. Nothing records a tool-call request and its result. |
| rate limit (429) as a client-visible class | **UNSERVED** | 429 appears only as a failover *trigger* (`route.failover|fo|primary-429`), never as an outcome a caller sees. |
| non-HTTP transports (stdio, gRPC, WS) on the LLM plane | **UNSERVED** | all 142 cells are `http`. |
| the stage profiler | **UNSERVED — and this is where the defect was** | see 1.3. |
| mid-stream upstream error, per dialect × fault | partially served | 6 `stream_upstream_error` cells here; the real 12-cell matrix is the separately-recorded `llm.stream|<dialect>|<fault>` golden from the **EG-1** line, which is not in this `cells.json`. EG-1/EG-2 carry three findings against the tip; EG-2 and EG-5 are landed, the golden lands with them. |

The honest summary of part 1: **for every class the golden can judge, the root leg is byte-identical.
For the classes it cannot judge, "byte-identical" has never been measured, and one of them was
already wrong.**

### 1.3 The one DIVERGE — `profile::Stage::WrapSetup`

`engine::forward_with_pool_parsed` (`engine/pipeline.rs:163`) is the shell around the dispatch core.
It does five things before and after the core runs:

- stamps the per-request correlation id (`host.next_request_id()`), once, at the earliest point;
- opens the `forward` tracing span and records the id onto it;
- times `profile::Stage::WrapSetup` over the setup;
- captures the completion shape *before* the parsed body moves into the core;
- fires the response-stage taps once the head is known.

`unit/route.rs` — the Route step of the root leg — **re-implemented all of that beside the original**
and then called the core directly. Same span, same id stamp, same completion-shape capture, same
`fire_stage_taps` projection. One step, written twice, once per leg.

The copy omitted the third item. `busbar_substrate::profile::Stage::WrapSetup` had **exactly one call
site in the workspace**, and it was the leg that is no longer the default. So since the default flip
the shipped binary has reported zero samples for a stage the plane's own shell times, and the
`BUSBAR_PROFILE` report has carried a permanently-empty `wrap_setup` row.

No oracle cell could have caught it. The profiler is not on the wire; a client sees nothing, an
operator's metrics see nothing, the ledger sees nothing. The golden came back at zero diverging and
was **right** to — every byte either party can see was identical. The thing that was missing is a
thing neither party can see, which is exactly the blind spot a wire-only proof has by construction.

Two neighbouring stages are in the same state and are named here so they are not re-discovered:
`Stage::InboundParse` and `Stage::PostSend` have **zero** call sites anywhere in the workspace, while
`dump()` still enumerates them. They are reported as stages that never fire. That is a smaller defect
of the same kind (the report promises an attribution the code never makes) and it is **not** fixed
here — see section 4.

## 2. Per-gap: the line that supplies it

| gap | what is missing | supplied by |
|---|---|---|
| `WrapSetup` not timed on the served leg | one shell, not two | **this branch** (`the_dispatch_core_has_exactly_one_caller`, red→green). Landed. |
| `InboundParse` / `PostSend` never fire | either the call sites, or the stages come out of `dump()` | **none.** New line **PROF-1**, specified in section 4. |
| embeddings / images / audio / moderations / rerank byte-identity | oracle cells on the operation axis | **none.** The recorder's `op` axis is chat-only. New line **OR-OP-1**, specified in section 4. |
| tool-call round trip | an oracle cell that records a tool-call request and its result | **none.** Folded into **OR-OP-1**. |
| 429 as a client-visible class | an oracle cell where the caller sees the 429 | **none.** Folded into **OR-OP-1**. |
| mid-stream error × dialect × fault | the 12-cell `llm.stream` golden | **EG-1** (recorded, provenance merged), gated on **EG-2** (anthropic\|cut fixed; openai\|stream-error ruled a register entry, not a defect) and **EG-5** (responses terminal clock; landed). |
| the legacy arrival leg still compiled and still driven | the plane's own tests reach the served path instead of the plane's own table | **none as a standalone line.** It is the test-side half of the **P-series** re-point that **P1** already named ("the 15 remaining doubles are test-only and go as each plane's tests re-point to the served path through the root, P2–P10"). Section 3 is the blocker in detail. |
| the engine's second copy (`busbar-unit-egress`) | delete the original, keep the move | **MOVE 7** (blocked on the mid-stream golden = EG-1, and on unit-total headroom). Until it lands, `one-pick-site` reads **4** and stays there. |
| `arrival.rs`'s parse half homed in the plane | the parse moves to `busbar-plane-llm::claims` | **MOVE 1** (~919 prod lines, no ceiling raise — planes carry no line ceiling). |
| `engine/hooks.rs` | home | **MOVE 3** (busbar-core-policy, declare §1.1 row at 2,750). |
| `engine/usage.rs` — the dual-book collapse | the 13 billing cells still read the legacy book | **MOVE 2** (needs a unit-total delta; after Rx3's drain). |
| `engine/health.rs` | no home under any kind | **MOVE 4** — owner ruling outstanding. **PROBE-1**'s unit half is HELD for the same reason: `BreakerUnit` is only composed on the admin surface, so deleting `health.rs` now switches probing OFF for served traffic. |

## 3. Why the legacy leg cannot yet be deleted per class — the real blocker

The obvious next deletion is the **path-model (URL-model) arrival class**: gemini and bedrock, the
two dialects that carry the model in the URL. It is the smallest separable surface — its own feature
arm (`main.rs:621`), its own table (`busbar_llm::PATH_INGRESS`), its own entry points
(`arrival::{gemini_arrival, bedrock_arrival}`), and 23 oracle cells that prove its bytes. Its root
twin is live and default, and it re-uses the dialect's own parse (`gemini_path_parse`,
`bedrock_path_parse`, `gemini_rest`), so no parse would be moved or copied by deleting it. The legacy
entry points are thin: `gemini_arrival` → `gemini_ingress` → `native_ingress::ingress_path_model`.

It is nevertheless **not** landable today, for a reason that is structural rather than incidental:

- `busbar_llm::PATH_INGRESS` is what **`busbar-llm`'s own test binary** drives. `testkit.rs` seeds it
  through `set_test_path_ingress`, and the plane's own tests reach the URL-model surface through it
  from three files — 34 drives in `engine/tests/ingress_integration_tests.rs`, 4 in
  `forward_pool_integration_tests.rs`, 1 in `hook_seam_tests.rs`. `busbar-llm` **cannot name the root**
  — the root is the binary crate that depends on it — so those tests have no served path to re-point
  to inside their own crate.
- `busbar-core`'s test binary does the same through
  `ingress/tests/path_ingress_builtins.rs`, for the same reason and with the same constraint.
- `crates/busbar/src/root/tests/units_llm.rs` — **the switch-over's own identity proof** — drives the
  legacy leg through `native_ingress::ingress_path_model` at two sites specifically in order to
  compare it against the root leg. Deleting the legacy leg deletes the proof that the two legs agree.

That last point is the one worth stating plainly, because it is the hinge of the whole remaining
plan: **the identity proof is a live twin, and the deletion is what removes the twin.** What replaces
it is the recorded golden. A live twin proves "these two code paths agree today"; the golden proves
"this code path emits the bytes 1.5.5 emitted". The second is the stronger claim and the one that
survives the deletion — but it only covers what the golden covers, which is section 1.2's problem,
which is why **OR-OP-1 is a prerequisite for deleting the legacy leg, not an improvement on it.**

## 4. New lines this measurement specifies

**PROF-1 — the stage report promises no attribution the code does not make.**
`Stage::InboundParse` and `Stage::PostSend` are enumerated by `profile::dump()` and have zero call
sites. Either restore the two call sites at the points their doc comments name, or remove the two
variants and their `dump()` rows. Red-first: a source witness asserting every `Stage` variant
enumerated in `dump()` has at least one call site outside `profile.rs` — red at 2, green at 0. Small,
self-contained, no ceiling movement, no wire bytes. Should land before the tag: it is the same class
of defect this branch fixed and the fix is cheaper than the discovery was.

**OR-OP-1 — the oracle's operation axis.**
Add cells for the operations the engine forwards and the golden does not record: embeddings, images,
audio, moderations, rerank, one tool-call round trip, and one client-visible 429. This is recorder
work in the pinned tool (the same place EG-1's recorder gaps were named in `accepted-gaps.json`), not
busbar's to edit unilaterally — so it is an owner/tool-release line, cut against the same tool version
EG-2's T6-4 items go into. **It gates the legacy deletion** (section 3): deleting a live twin in
favour of a recording is only sound where the recording covers the class, and today it covers one
operation out of seven.

## 5. The ordered plan

Each row is one landing. "Flips" means the legacy twin for that class is **deleted**, not that the
root leg starts serving it — the root leg already serves all of it.

| # | class | line | net | blocked on |
|---|---|---|---|---|
| 1 | the forward shell (all classes) | **this branch** | −70 surface | — **LANDED** |
| 2 | the stage report | **PROF-1** | ~0 | — |
| 3 | the operation axis in the recording | **OR-OP-1** | +cells | owner / tool release |
| 4 | mid-stream error × dialect × fault | **EG-1** (+EG-2, EG-5) | +12 cells | tool release for `responses` streaming |
| 5 | the parse half → `busbar-plane-llm::claims` | **MOVE 1** | ~−919 from `busbar-llm` | — (no ceiling raise; planes carry none) |
| 6 | path-model arrival class (gemini, bedrock) | **P-series test re-point** (P1's named remainder) + OR-OP-1 | −`PATH_INGRESS`, −`{gemini,bedrock}_arrival`, −`{gemini,bedrock}_ingress`, −`ingress_path_model(_inner)` | §3 |
| 7 | body-model arrival class (all six dialects) | same line, second half | −`BODY_INGRESS`, −`native_ingress.rs`'s body core, −`finish_admitted`/`finish_rejected` legacy sites → `terminal-doors-in-audit-step` goes green | 6 |
| 8 | hooks | **MOVE 3** | −1,074 | — |
| 9 | usage / the dual-book collapse | **MOVE 2** | −209 + unit delta | Rx3's drain |
| 10 | health | **MOVE 4** | −535 | owner ruling; also releases PROBE-1's held unit half |
| 11 | `unit/` → kernel steps + units + the root leg | **MOVE 6** | −3,623 | — |
| 12 | **the engine itself** | **MOVE 7** | −7,917; `busbar-unit-egress` stops being a copy and becomes the one engine | EG-1 golden + unit-total headroom |

**The last commit** — the one the whole sequence exists to make — is row 12. It deletes
`crates/busbar-llm/src/engine/{select,pipeline,exhaustion}` and, with row 7 already gone, the
forwarder in `native_ingress.rs` that is their only remaining production entry. At that commit:

- `one-pick-site` falls from **4 to 2** (the two `busbar-unit-egress` sites become the only sites,
  which is the rule's stated two legitimate callers: the Route loop and the fallback-pool re-entry);
- `request-path-fn-size` loses eleven of its fifteen named files;
- `plane-no-money` and `one-pricing-site:fee-fields` lose their `busbar-llm` offenders;
- `terminal-doors-in-audit-step` is already green from row 7;
- `busbar-llm` is left as composition and declarations (MOVE 8) and is deleted.

Until then `one-pick-site` reads **4** and every branch that touches this area should say so rather
than appear to have raised it: the two extra sites are the MOVED-not-rewritten engine's copy in
`busbar-unit-egress` (`walk.rs:132`, `exhaustion.rs:240`), whose legacy original is still the served
one. The row goes green when the original is deleted, not before.

## 6. Ceilings

This branch raises none. Every figure it touches moves down: `unit/route.rs` 496 → 425 raw and
272 → 202 surface; `busbar-llm` production surface 9,308 → 9,238. `one-pick-site` is unchanged at 4
and is not this branch's to move. `substrate` is untouched, so the freeze holds; legacy-reach is
unchanged, so it has not risen.
