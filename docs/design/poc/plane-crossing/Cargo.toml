[package]
name = "zcpoc"
version = "0.0.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
flatbuffers = "25"
rkyv = "0.8"
libc = "0.2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1

[workspace]
