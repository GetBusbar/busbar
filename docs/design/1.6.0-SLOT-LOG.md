<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- Copyright (C) 2026 Busbar Inc and contributors -->

# SLOT LOG

One row per slot, appended when the slot finishes. This is the only status channel
(rule 15.8 of `docs/design/1.6.0-KICKOFF.md`) — there is no other place a slot reports.

`verdict` is per item: **FIXED** · **STRUCK** (with the measurement that refuted it) ·
**PARKED** (money changed, see `1.6.0-PARKED/`) · **BLOCKED** (names the slot it waits on).

`RED proof` is the command and its non-zero exit, captured in the slot's own pinned worktree
per rule 16.2 — not in the shared checkout, where another slot's write invalidates it.

| slot | model | items | verdict | exit test | RED proof |
|---|---|---|---|---|---|
| A572 (stream A, pre-wave) | opus | 572 | 572=FIXED (fd3f2bb5b; oauth-as 1.0.0; no client-visible change — every 0.9.4–1.0.0 change is behind an un-enabled feature or a default-preserving ServerConfig field; changelog enumerated in commit) | `crates/busbar-oauth2/src/tests/signer_tests.rs::the_jwks_and_metadata_documents_are_byte_identical_to_1_5_5` | observable did not move: test exit 0 at 3abc4cd20 on 0.9.3 (bytes unchanged = the proof); RED capability: `kid: None` planted on 1.0.0 → `cargo test -p busbar-oauth2 --locked the_jwks_and_metadata` exit 101 |
| P0A-13 (stream A) | opus | 13 | 13=FIXED (f107c224f deletes .keep-proof.toml — snapshot ~/Downloads/busbar-1.6.0-item13-keep-proof.toml.snapshot; 042ac84e5 prove-remote reads scope from proven tip, logs every-family fallback, refuses a scope file off keep-*) | `scripts/prove-remote.sh --selftest` (7 arms) | at 43b61bff2 extracted resolver yields families=^(llm\|billing\|ledger) (narrowed); new resolve_scope on 43b61bff2/predev → exit 3 |
| P0A-106 (stream A, money, alone) | opus | 106 | 106=FIXED (b58d05f71; test-only, no figure moved; kernel reserved_nanos == budget copy == u128::MAX at top, 3_516_250 in range) | `crates/busbar/tests/money_one_function_equivalence.rs::d3_…` | at 43b61bff2 `cargo test -p busbar --test money_one_function_equivalence` → 101; planted plain-`+` fold → 101 |
| P0A-114 (stream A) | opus | 114 | 114=FIXED (39e25d208; test restated to reach the floor: empty+allow_empty → BelowFloor{0,1}, have-1 → BelowFloor, at-floor passes, empty w/o allow_empty → Empty; impl unchanged, Empty-before-floor is list()'s documented rule) | `xtask/tests/infra.rs::a_walk_below_its_floor_is_an_error_not_a_clean_tree` | at 43b61bff2 → 101; floor check disabled (`if false &&`) → 101 at infra.rs:198 |
| P0A-49 (stream A) | opus | 49 | 49=FIXED (f89fc5644; +11 vars refused: BUSBAR_ORACLE_RUST_BIN, BUSBAR_RELEASE_CHECKOUT, BUSBAR_EMIT_OPENAPI, BUSBAR_UPDATE_GOLDEN, UPDATE_KEYS_ERROR_GOLDEN, UPDATE_DIAGNOSTICS, XTASK_INSN_EMIT_BASELINE, XTASK_PPB_EMIT_BASELINE, GREP_GATE_REPORT_ONLY, SECRET_GATE_REPORT_ONLY (+1 per commit); BUSBAR_ARM64_BASELINE not added — release target-cpu selector, not a judge) | `scripts/verify-1.6.0-done.sh --selftest` | at 43b61bff2 `GREP_GATE_REPORT_ONLY=1 … --selftest` → 0 (accepted); new selftest vs old guard → 1 |
| P0A-F0 (stream A) | opus | F0 | F0=FIXED (98434a220; MIN_CRATE_MANIFESTS 40→30: 35/34/33 end-states pass, 0/5/20 fail) | `xtask/src/gates/workspace_deps.rs::tests::the_crates_walk_floor_admits_the_fold_end_state_and_rejects_a_collapse` | at 43b61bff2 `cargo test -p xtask the_crates_walk_floor_admits` → 101 (35 under floor 40) |
| P0A-115 (stream A) | opus | 115 | 115=FIXED (62a6d7683; 12 voice testkit impls gated `#[cfg(feature = "test-support")]` to match the kernel trait gates; no test deleted) | workflow command `cargo test -p busbar-voice --features runtime config::` (qa-conformance-voice.yml:402) → 0 | at 43b61bff2 same command → 101 (12×E0407) |
| P0A-117 (stream A) | opus | 117 | 117=FIXED (dced64d9b; TEST_MEMO keyed on decl-address identity of the set, test-only) | `crates/busbar-kernel/src/plane/registry.rs::registry_memo_tests::two_distinct_registration_sets_of_equal_size_do_not_alias`; ordered run `cargo test -p busbar --bin busbar -- --test-threads=1 root::plane_decision root::units_llm` → 41/41 | at 43b61bff2 ordered run → 101 (8 units_llm fail); memo test → 101 |
| P0A-116 (stream A) | opus | 116 | 116=FIXED (b95bade9c repin after diff review — all changes additive, busbar gemini wire unaffected; 627aa9b31 judge reads committed reviewed copy `specs/gemini-v1beta-20260923.json`, live drift split to `vendor.sh --drift` exit 5). Queued Q1 (gemini-vertex fixture gaps, #59) | `testing/llm-conformance/selftest.sh` 24/24 (cases n,r,s,t,u) | at 43b61bff2 selftest → 1 (19 failed); URL-pinned tree with simulated revision bump → 1 |
| P0A-113 (stream A) | opus | 113 | 113=FIXED (2b8aa99ef; row repointed to `engine_kit/core_impl.rs` count 1 kept (measured 1); new owed row `instance-noun-neutrality:dead-path` refuses any [[leak]] row whose file is not a scanned .rs, or missing noun/file) | `cargo xtask gate instance-noun-neutrality --selftest` (33 cases, 3 new) | at 43b61bff2 with dead-path check removed → 1 (3 cases expected Red got Green) |
| P0A-24 (stream A, money) | opus | 24 | 24=FIXED (482801cec; architect ruling: metrics.rs → metrics/{mod,money}.rs, money gauges integer up to one named MONEY_EGRESS boundary fn (the `metrics` crate forces f64 there, same path as 1.5.5), money.rs in KERNEL_MONEY_FILES; follow-up repoint by architect in the next commit). No figure moved, no served byte changed | `crates/busbar-kernel/src/tests/metrics_tests.rs::money_gauge_bytes_are_the_1_5_5_bytes`; `no_float_money.rs::tests::{every_declared_egress_boundary_…, a_float_outside_the_egress_boundary_…}`; `cargo xtask gate no-float-money` + `--selftest` (33) | at 43b61bff2 arming → 1 (14 findings); planted f64 fn in money.rs → 1; byte-pin test with `(exact/2*2) as f64` → FAILED |
| P0A-6a (stream A) | opus | 6a | 6a=FIXED (0db853a54; ci.yml gains matrix job `content-debt-gates` running unconstructed/sweep-coverage/map-proof `--selftest` then gate, unsoftened, report-only in umbrella `needs` until 6b/89 drain them; full_gate.rs classifies the two cargo lines) | `xtask/tests/cli.rs::the_registry_and_the_workflow_still_name_the_same_gates`; `cargo xtask full-gate --selftest` → 0 | at 43b61bff2 `cargo test -p xtask --locked --test cli the_registry_and_the_workflow` → 101 |
| P0A-88 (stream A) | opus | 88 | 88=FIXED (4ccc6591d; qa-names `line_of` lands on the value not a quoted comment; design-bindings selftest covers ledger-rewrite plants via `ledger_owed()`; reachability overlay plants its evidence doc + 3 new RED arms for `reachability:evidence`). qa-names/design-bindings selftests still exit 1 on standing reds owned by item 89 | `qa_names.rs::tests::a_citation_skips_a_quoted_spelling_in_a_comment`; `design_bindings/selftest.rs::tests::the_ledgers_own_note_plant_scores_red`; `reachability.rs::tests::the_selftest_proves_every_owed_row` | at 43b61bff2 `cargo xtask selftest qa-names` → 1; `… design-bindings` → 1; `… reachability` → 1 |
| P0A-112 (stream A) | opus | 112 | 112=FIXED (b26f97c27; architect re-ruling after kernel-mints-the-seal was refused by kind-isolation `new-forbidden-edge` ×11 + 7 purity tests: `test-seal` stays as the contract's one named, empty, dev-only exemption in feature_invariance.rs, guarded by test_seal_is_dev_only.rs; CI feature-sets matrix builds it (8 legs). Queued Q2) | `crates/busbar-contract/tests/feature_invariance.rs::{the_crate_declares_no_features, no_item_is_conditionally_compiled, every_test_seal_item_is_present_behind_its_attribute, an_extra_feature_is_red, a_non_empty_test_seal_is_red, test_seal_over_another_item_is_red}`; `cargo xtask gate feature-sets` → 0 | shared tree pre-fix (inputs == 43b61bff2) `cargo xtask gate feature-sets` → 1; `cargo test -p busbar-contract` → 101 (2 failures) |
| W0.14 | opus | 331, 354, 360 | 331=BLOCKED (workflows only on predev resolve schedule/dispatch against default branch main; KICKOFF §13.3 rules it non-blocking and carried by the train; OWNER STILL OWES #1 — no predev change closes it) · 354=FIXED (782a631d0) · 360=FIXED (b530f6112) | 354: `actionlint .github/workflows/release.yml` + errexit harness; 360: turnstile-dispatch.yml step asserting GITHUB_WORKFLOW == turnstile-dispatch | 354 @99b61b3d7 actionlint → 1 (SC2181×2), harness → 1; 360 @99b61b3d7 name check → 1 |
| W0.19 | opus | 482, 483, 542 | 482=FIXED · 483=FIXED · 542=FIXED (c6f4cd351; owed-list runtime floor 50 kept (real contract 66); WRONG RELEASE refused on the unfiltered ledger first; checks read STAGED_RECORD only through lib.sh lookups). Release gate stricter, no customer/money effect | `scripts/release-gate/gate.sh --selftest` (runs ledger-selftest.sh; CASE 5d) | @99b61b3d7 `bash scripts/release-gate/ledger-selftest.sh` → 1 (482, 483); old gate.sh with new selftest → 1 (542, 17 FAILED) |
| W0.17 | sonnet | 454, 455, 458 | 454=FIXED · 455=FIXED (1a85b8dbf: required-status-checks.md names Voice conformance + cargo-fuzz, flags cargo-deny promotion gap) · 458=FIXED (1a3a0a0bf: actionlint.yaml stops claiming a CI guarantee). No branch-protection change (measured read-only) | `.github/scripts/required-status-checks-selftest.sh`, `.github/scripts/actionlint-claim-selftest.sh` (new, W0.17-owned) | @99b61b3d7 each selftest → 1 (5 and 4 RED assertions) |
| W0.16 (money, each item alone) | opus | 498, 499, 500, 546, 547 | all FIXED (4ef6a59d8 498 card-epoch fails closed on non-integer; e2e594268 499 class-price likewise; 92038c35e 500 ARM 2 accepts only a 4xx refusal; 3e1f96866 546 boot.sh declares its shell options; 92c26490f 547 h2_verdict exits as documented; 05d003a73 shellcheck follow-up). Rig assertions only — no ledger/ratecard/invoice path touched | `scripts/mcp-subject/h2-lib.sh --selftest {card_epoch,class_price,unpriced_refuses,verdict_exits}`; `scripts/mcp-subject/boot.sh --selftest` | each @99b61b3d7 → 1 (per-item worktrees) |
| W0.8 | opus | 169, 170, 176, 177, 195, 196, 205–209, 235 | 169, 170, 195, 196=FIXED (f61b3c850; hot-path gates match each row's own comparison/budget/delta clauses in parsed bench code and read `harness = false` from the bench's own block) · 176, 177, 205, 206, 207, 208, 209, 235=BLOCKED — all verified real, all live in `xtask/src/gates/mod.rs` owned by W0.7: partition defect (§15.7), sequenced to run after W0.7 | `xtask/src/gates/hot_path_{perf,alloc}.rs::tests::*` (8) + `xtask/tests/hot_path_gates.rs::both_hot_path_gates_prove_red_in_their_selftests` | @99b61b3d7 `cargo test -p xtask --lib hot_path` with the 8 tests → FAILED 0/8 |
| W0.13 | opus | 334–337, 344–351, 355, 359 | 334, 335, 336, 344, 345, 346, 347, 348, 349, 350, 351, 355, 359=FIXED (59bbac556, 9ac091f38, 8debbb3ce, a47bfffe2, 06ea94ff3, 2c4708227, 2907a3697, 307accb6c, 6fc0332de, 9e10c3972) · 337=STRUCK (slow matrix derives from qa/segments.toml in-run: 12 legs == 12 active rows; fail-fast false makes needs.slow.result all-legs) | fail-closed checks inside each workflow (334 docker promote assert; 335 armed(); 336 fuzz list floor; 346 codecov token; 347 targets floor; 350 rc; 355 band; 359 yaml name); 344/345/348/349/351 comment fixes with grep falsifiers | extracted step bodies @99b61b3d7 under `bash -e` with mocks: 334 MOCK_V8=503 → 0 (new 1); 336 empty list → 0 (new 1); 347 empty → 0 (new 1); 350 rc=3 → 0 (new 1); 355 rc=2 → 2 (new 0); 359 name truncated |
| W0.18 | opus | 469, 486, 544 (+ a2a twins of 498, 499, 500, 547) | 469=FIXED · 544=FIXED (b7e6e335a: TCK NOT TESTED MUST excused only if a pinned control also could not test it; zero-MUST run refused; reaper list word-split) · 486=FIXED (a2441c7fa, c2d7c1988: per-attempt 502s + exact Retry-After judged) · twins 547/498/499/500 FIXED alone (c69617a8f, 04942e015, d2e68ab7d, 56e08b2fa). Flag: debug binary refuses a2a h2 rig config with BUSBAR-9007 (agents: + rate_card:) — carried to Phase 2 (#47) | `scripts/a2a-subject/boot.sh --selftest` (RED 4b, 9–12); `h2-route-failover.sh --selftest` (9 cases); `h2-lib.sh --selftest {verdict_exits,card_epoch,class_price,unpriced_refuses}` | each @99b61b3d7 → 1 (469 fixture accepted; 544 pid fused; 486 6/9 MISS; twins as W0.16) |
| W0.6 | opus | 484, 521, 525, 536, 543 | all FIXED (1ccb45d9a secret-hygiene refuses zero-file scan; be43fb14f qa-segments refuses duplicate ids; f8a6b1530 rooms.sh measures its own checkout; d2e723ecb public-hygiene-lint scans every line; 87de7f547 verify-artifact --selftest runs the coverage gate). Queued Q3 (2 new lint hits in accepted-differences.json) | `scripts/secret-hygiene-gate.sh --selftest`; `scripts/qa-segments.sh --selftest` (k); `scripts/rooms.sh --selftest`; `scripts/public-hygiene-lint.py --selftest` (6); `scripts/verify-artifact.py --selftest` | each @99b61b3d7 with the fix reverted → 1 |
| W0.1 | opus | 468, 476–478, 481, 485, 514–515, 529–530, 539–541, 551 | 468, 476, 477, 478, 481, 514, 515, 529, 530, 539, 540, 541, 551=FIXED (78f18ed57 proof-manifest: every verdict stands on existing evidence; 6c3683844 release-check: a run that executed nothing is RED; 191e33727 verify-1.6.0-done: no silent BUILD downgrade, voice legs =ready, no dead gate name, full --help) · 485=STRUCK (already fixed by item 49, f89fc5644). Public proof dashboard evidence paths corrected (instrument honesty, no product change) | `scripts/proof-manifest.py --selftest`; `scripts/release-check.sh --selftest` (cases 6–10); `scripts/verify-1.6.0-done.sh --selftest` | worktree @99b61b3d7: proof-manifest --selftest → 2 then 1; release-check --selftest → 1 (10 cases); verify-1.6.0-done --selftest → 1 |
| W0.15 | opus | 315–320, 326–327, 329 | 318=FIXED money, alone (0046867e7 + architect test follow-up; figure moved, fault named: broken derivation; queued Q4) · 315, 319, 320, 326, 327=FIXED (adac7384f: take-site count and honesty-table names match the tree) · 329=FIXED (9b97fd36e) · 316, 317=BLOCKED on item 181 (seal-witness, W0.9/W0.11 xtask) — the doc states #65's intent correctly; the gate is RED on 135 test-only sites | `caps/hold.rs::settle_carries_the_excess::*`, `::take_site_census::*`; `caps/mod.rs::honesty_table::*`; `caps/token.rs::seal_implementors::*` | worktree @99b61b3d7 `cargo test -p busbar-contract --lib <each>` → 101 |
| W0.5 | opus | 474–475, 501–502, 506–508, 511–513, 519, 535, 548–549 | all 14 FIXED (ac695a75f 474, 4487fb446 475, 371b99df6 507, 657238911 506, 949086c9a 548, e3ec4c752 501, 1077c0568 502, 5dc45d663 508, c85be961c 511, 0f3aafcc1 512, 925fe1fcd 513, bc7059594 519, 41762fe29 535, eec209ae4 549 deletes scripts/plane-roots.sh — snapshot ~/Downloads/busbar-1.6.0-snapshots/plane-roots.sh.55999865e; replacement asserted by xtask infra test). Report-only meters moved: plane-grep 271→230, plane-noun debt 158→245 (no ceilings; they were under-counting) | per-script --selftest cases (474 RED 7; 501 X1/X2; 502; 506 TEST FILES ×3; 507 ABI SURFACE; 508 case 5; 511 F,G; 512 H; 513 new; 519 oracle_leg; 535 case 6; 548 REJECTED NAMES); 475/549 `xtask/tests/infra.rs` | worktree @99b61b3d7, each selftest with the pin code → 1 (475 → 101; 513, 549 by reading) |
| W0.6 (follow-up) | opus | 506 twin | FIXED (37765e920: secret-hygiene gate treats module-style tests.rs as test code; live report unchanged 17/0/7) | `scripts/secret-hygiene-gate.sh --selftest` TEST-FILE SHAPE | @99b61b3d7 old TEST_FILE_RE → 1 |
| W0.4 | opus | 467, 473, 487–489, 492–493, 495–496, 526–528, 532–533 | all 14 FIXED (ecbfaadd3 467/526 seal witness header + zero/partial scan RED; 462bf7495 473; eee2ebeb1 487; f8510b739 488; d514dd940 489; cf5c9e133 492; 32c24e144 493 (turns documented-claims RED on a real +1 id drift — register repoint dispatched); a594b0089 495; a123417d9 496; cb4a03a23 527/528; 8b2f9e0a8 532; c8e607f8a 533) | per-script --selftest (467/526 seal-witness CLAIMS + scan legs; 473; 487; 488 (i)–(k); 489 new; 492 new; 493 arm 6; 495; 496 new; 527/528 new; 532; 533 new) | worktree @99b61b3d7, each new selftest over pinned behaviour → 1 (487/495 pinned selftest with mutant → 0, new → 1) |
| W0.3 (money items alone) | opus | 471–472, 480, 494, 497, 509–510, 520, 524, 534, 537–538, 545, 550 | all 14 FIXED (c1d2122b7 471 unpriced runner label refuses exit 4, alone; 9c0dc5ee8 472 failed fetch exits 4, alone — CI infra spend only, totals unchanged, no invoice/usage; b7210cb6b 480; 91d963faa 538; 01e779ae4 494; 438989834 545; 65967cd2d 497; d845074d4 534; 1e3f48c6f 509; 9ef61d2d3 510; 55999865e 520; a05e39e1f 537; c9296bbfc 524; 5c77974a4 550; b5a97062f plane-roots comment) | per-script --selftest (cost-watch 5/7f/7g; release-check-1.5.2 1–6; gate-mutants; mcp-conformance RED 10–16; plugin-registry-check CASE 5; qa-gate-run selftest; release-script-lint VACUOUS/TALLY) | worktree @99b61b3d7 (+test) each → 1; 538 by predicate measurement rc 0 → 1 |
| W0.4 (follow-up) | opus | 493 register | FIXED (ee9ae100a; qa/documented-claims.json 56 ids +1 under one-time architect authorisation — each id addresses the byte-identical quote it did before ac4a25a8d, 56/56; no verdict changed; selftest case (f) re-pointed to a genuinely never-recorded cell since 225cf0e32 recorded the old one) | `scripts/documented-claims-check.sh --selftest && --check` → 0 | pre-repoint `python3 scripts/documented-claims-check.py` → 1 (56 problems) |
| W0.1 (follow-up) | opus | 480 dependency | FIXED (c3174b88f; release-check.sh imports release-check-1.5.2.sh gap rows via $BUSBAR_RELEASE_GAP_FILE into record_phase_skip; child run with REQUIRE_SIBLINGS=0, parent decides; unknown gap status → HARNESS exit 78) | `scripts/release-check.sh --selftest` case 7b | @6c3683844 with import no-op → 1 (3 checks) |
| W0.2 | opus | 470, 479, 490–491, 503–505, 516–518, 522–523, 531 | all 13 FIXED (854030890 470/531 branch-symbol-sweep; 08ad1effd 479/522/523 reap-worktrees; dbf3a79ee 490/491 ci-runners-selftest runs reconcile + live path; bfe6644a3 516/517/518 proto-deletion-gate derived needles 11→18, strict HTTP; ab45ec894 503/504/505 plane-delete-test teardown, codec half, leg 1b). Blocking CI gates widened, green on the current tree | branch-symbol-sweep selftest T0/T7; reap-worktrees --selftest S1–S3; ci-runners-selftest B/D; plane-delete-test --selftest 1c/1d/6b; proto-deletion-gate --selftest NEEDLE/DIALECTS/HTTP | worktree @99b61b3d7 with pin shapes → 1 each (reap-worktrees pin has no --selftest → 2) |
| W0.12 | opus | 332–333, 338–343, 352–353, 356–358, 361 (+ owed a,b,c,d) | all 14 FIXED (d686e226c; plus removed ci.yml duplicate `coverage` job that GitHub would refuse; owed (a) prove-remote --selftest, (b) keep-proof guard, (c) sched-llm-spec-drift.yml non-required; script-selftests job wiring every stream-A/B selftest; W0.4(b) consumer-state exit). Owed (d) `gate --all` on push: in progress (needs full_gate.rs CARGO_CI_ONLY, authorised). Queued Q5 (edge 403) | `.github/workflows/lint/workflow-invariants.py --only <id>` + `--selftest` (new, run by ci.yml script-selftests) | worktree @99b61b3d7 every id → 1 |
| W0.11 | opus | 180, 181, 219, 221, 230 | 180=FIXED (3dbdf4004; decision plane with no unit module now RED on both rows — true red, carried to qa/ W1.1) · 181=FIXED re-ruled (6d6444c02; single-minter judges shipped code; test mints a printed census row; gate green at pin; HEAD red only on zoo row from adac7384f literals — follow-up dispatched to W0.15) · 219=FIXED (d0f17b541) · 221=FIXED (2cd6d70ce) · 230=FIXED (535e13208) | `xtask/src/gates/{reachability,seal_witness,segregation,ship_ready,unconstructed}.rs::tests::*` | worktrees @99b61b3d7 `cargo test -p xtask --lib -- --exact <each>` → 101 |
| W0.4 (follow-up 2) | opus | 532, 488 | FIXED (b66201d9d agreement scoped to ci.yml `check` vs release-stage.yml `gate` via explicit AGREEMENT_JOBS; 36950d747 + 93eae518d column-0 comment no longer ends a job list — ci-images and ci-branch-protection). Mirror list now derives 3 images (adds mysql:8) | `scripts/ci-images.py --selftest` (4 new arms) → 0 on real tree; `scripts/ci-branch-protection.sh --selftest` (j) | @99b61b3d7 pinned ci-images --selftest → 1 (mysql:8); old `^\S` regex mutant → (j) FAIL |
| W0.15 (follow-up) | opus | 319/327 follow-up; 316, 317 re-marked | 62042c33e honesty-table zoo check reads seal-witness `ZOO` via include_str! (no literals). `cargo xtask gate seal-witness` → 0 and selftest → 0, no IMPOSSIBLE — **KICKOFF §13.1 breakage FIXED**. 316, 317: BLOCKED → FIXED (single-minter green after 181 re-rule; token.rs/caps/mod.rs text true as written) | `caps/mod.rs::honesty_table::no_row_names_a_deleted_proof_type`; `cargo xtask gate seal-witness` | planted deleted name in caps/mod.rs → 101 |
| W0.17 (follow-up) | sonnet | 458 | FIXED (253aa840e; claim selftest checks an exact WIRED/NOT WIRED marker against a real `run: actionlint` invocation, not any mention) | `.github/scripts/actionlint-claim-selftest.sh` | @99b61b3d7 → 1; scratch: mention-only → 0, real invocation + NOT WIRED → 1, WIRED → 0 |
| W0.10 (money items alone) | opus | 182, 186–187, 193, 197–199, 204, 210–212, 220, 228–229 | 182, 186, 187, 193, 198, 199, 204, 210, 211, 212, 220, 228, 229=FIXED (b606c4907 210/211 money alone; f6d6d4980 228/229; 3a3ef66b5 212; bc0b04a0d 199 money alone (kernel = 9 crates, floor 8→200 over 292 measured); d39f490ac 204; 55542c177 193; 0597faee8 187; 90482e0fb 186; 24fe823c1 182/220; c8044d319 198) · 197=BLOCKED (confirmed live; needs qa/ rows + fixture — W1.1). New true reds surfaced: service-images names 6 floating images (pin follow-up dispatched), 10 baselined noun files grew (Phase 4), map-proof 2 vacuous docs | per-gate selftest cases + `service_images.rs`, `map_proof.rs`, `instance_noun_neutrality.rs` unit tests | worktree @99b61b3d7 each new case → 1 / 101 |
| W0.10 (follow-up) | opus | 182, 220 | FIXED (c71303ca5; 6 floating script images pinned by real registry digest lookup — mysql:8.0 + autobahn gain table rows; postgres/valkey pinned to the table's existing digests (still resolve), not upgraded; every-pin-used counts a script run at the pinned digest) | `cargo xtask gate service-images` → 0; `--selftest` 14 cases → 0; `cargo test -p xtask --lib service_images` → 0 | as first report |
| W0.9 (money/oracle instruments) | opus | 166, 168, 178, 189, 194, 213–217, 225–227, 234 | 166, 168, 178, 189, 194, 213, 214, 215, 216, 217=FIXED (143d34bb3, 7b9679532, 04669b118, 18e9ea384, fe0084c92, 63f24b624, 6ddf6de2e, 2b0659924, d796dacd0, 2c0c5c81a, 75b0dc95b). 178 armed count-read debt at today's 37 (32 live llm-codec unwrap_or(0) billed-count sites → Phase 2 item 133). 225, 226, 227, 234=BLOCKED on pre-session foreign edit in sweep_coverage.rs — adopt-or-revert ruling dispatched | `no_float_money.rs`, `field_inventory.rs`, `construction.rs` unit tests; plane-pricing-blindness selftest cases via `xtask/tests/plane_pricing_blindness.rs` | worktree @99b61b3d7 each → 101 / 1 |
| W0.9 (remainder) | opus | 225, 226, 227, 234 | all FIXED after architect ruling: 415d89fdb adopts the PRE-SESSION uncommitted sweep_coverage.rs edit (16 lines, overlay plants reach claims(); reviewed; snapshot ~/Downloads/busbar-1.6.0-snapshots/sweep_coverage.pre-session.diff); a8997ef5c 225 git error no longer swallowed; af516a3e1 226/227 every declaration spelling + #[path]; e73fd1eac 234 header names all 7 rows | `sweep_coverage.rs::tests::{the_reachability_population_is_the_denominators_rs_half, the_derived_population_equals_git_on_the_tree, a_module_declared_with_any_visibility_or_attribute_is_declared, an_untracked_file_declared_in_any_spelling_is_a_ghost, the_header_table_names_every_owed_row}` | @99b61b3d7 planted `pub(crate) mod ghost;` → row PASS (grep FAIL exit 1); 234 → 101; 225 by reading (`Err(_) => BTreeSet::new()`) |
| W0.12 (owed d + wiring) | opus | owed (a)–(d), dependency wiring | 7852d07ba: `cargo xtask gate --all` runs on every push, report-only in the umbrella (full_gate.rs CARGO_CI_ONLY entry, authorised) — **KICKOFF §13.2 "runs in no automatic workflow" FIXED**; actionlint is a real CI step (marker WIRED); every stream-A/B selftest wired (script-selftests job / structure-lint); plugin-ci.yml SC2155 fixed; release-stage.yml:361 comment corrected; sweep verdict row for sched-llm-spec-drift.yml. W0.4(d) already fixed by b66201d9d | `workflow-invariants.py --only owed-d` + `--selftest`; ci-umbrella; `full-gate --selftest` | @99b61b3d7 `--only owed-d` → 1 |
| W0.7 | opus | 171–174, 200–202, 233 | all 8 FIXED (7d988cae7 kind-isolation: every selftest plant bites, every needle has an emitter; 6b3dcff46 construction posture names the 9 true reds W0.9 surfaced, each with its drain). Selftest cases for 171/172/202 are well-formed but still IMPOSSIBLE until :deps/:registry standing reds drain (Phase 4) | `xtask/src/gates/kind_isolation.rs::plant_tests` (11) | worktree @99b61b3d7 `cargo test -p xtask --lib pin_red::item<N>` → 101 each |
| P0A-115 (regression fix) | opus | 115 | FIXED again (76dbe81b4 reverts 62a6d7683 which broke `cargo test --workspace` with E0046 under feature unification; 1012d0d33 kernel `verify_token_test` defaults to None + voice fixture host hands out the kernel's own HealthState — no test-support-dependent impls left; the voice→kernel dev edge was refused by kind-isolation and NOT added) | workflow cmd `cargo test -p busbar-voice --features runtime config::` → 0; `cargo test --workspace --locked --no-run` → 0 | as before (@43b61bff2 → 101); regression caught by architect's pinned `cargo test --workspace` → 101 |
| W0.8 (sequenced remainder) | opus | 176, 177, 205–209, 235 | all 8 FIXED (7f75396c9; gate postures hold only what they check; land.sh subtracts exactly CONSTRUCTION_STANDING_REDS, test holds them equal; ship-ready excuse `RequiredAtPromotion` checks its own facts; falsification refuses a green case over a no-op plant) | `xtask/src/gates/mod.rs::posture_tests::*`, `::falsification_tests::a_green_case_over_a_no_op_plant_is_refused_too`; `scripts/land.sh --selftest` | worktree @99b61b3d7 grafted tests → 101 (6 FAILED); 235 by reading |
| P1A-cs (stream A) | opus | 89 (config-schema / 37b) | 89=FIXED for PROOF (98345d473: 22 IMPOSS → 0, cases measured from a green fixture base) · selftest still exit 1 on 2 GREEN controls: (a) real-tree control RED on 7 PoolMember fields — queued Q7 (§9.2 snapshot); (b) moved-grammar-type case needs crates/busbar-core-config — item 231 (W1.5) | `cargo xtask selftest config-schema` (58 cases, 0 IMPOSS) | @fd3455e53 → 1 (22 IMPOSS) |
| P1A-121 (stream A) | opus | 121 | FIXED (9beaba082; verdict-covers-every-leg.py derives LEGS from leg files on disk; voice tool-reply leg gets its job; CI runs the lint selftest) | `testing/verdict-covers-every-leg.py --selftest` (tool-reply shape case) | @fd3455e53 old lint → 0 on a leg with no job; new lint same tree → 1 |
| P1A-seg (stream A) | sonnet | 87d, 97 | 87d=FIXED, 97 plane-config-noun half FIXED (03a128c1f: plane-config-noun segment active, report-only posture like plane-grep, today's 17-line/8-file baseline recorded); 97 smart-router half: paired strike dispatched (qa-segments.sh roster) | `bash scripts/qa-segments.sh --selftest` → 0; `--list` shows plane-config-noun active | by running @fd3455e53: plane-config-noun --selftest already green (reservation reason stale); smart-router target absent at both points |
| W0.17 (follow-up 2) | sonnet | 454 | FIXED (f44db80b4; required-status-checks.md names the voice leg set incl. tool-reply and points at `verdict` needs: as the source; selftest derives the list at run time) | `.github/scripts/required-status-checks-selftest.sh` | scratch: current workflow vs old "8 legs" text → 1 |
| P1A-119 (stream A, oracle) | opus | 119 | FIXED in sibling repo (busbar-release 57da1eb, unpushed — push + pin move queued Q8; premise field evaluated against the recording: false/unevaluable → DIVERGING; missing → UNCHECKED-PREMISE report) | busbar-release `crates/busbar-release-oracle/tests/premise_law10.rs` (6) | busbar-release worktree @ab88903 (the pinned ref) → 101, 6/6 (F-011b row PASS ACCEPTED while /mcp probe 404) |
| P1A-seg (follow-up) | sonnet | 97 smart-router | FIXED (57adc9d56; dead reservation struck with its selftest roster entry in one commit; no qa_smart_router target in all history; reserved/active-absent invariants still proven by synthetic fixtures) | `bash scripts/qa-segments.sh --selftest` → 0 | by reading: `git log --all --diff-filter=A -- "*qa_smart_router*"` empty |
| P1A-9 (stream A, money, alone) | opus | 9 | FIXED (fe45f1751; money-invariants scans the journalled audit record and DERIVES its record population (38 types / 7 files, floors 18 + 4). Now RED on the TRUE finding: AuditRecord.amount, Amount.priced, HookApplied.priced_delta — Phase 2 drain; posture wiring pending mod.rs release; queued Q9 on posting figures) | `cargo xtask selftest money-invariants` (9 cases, 0 IMPOSS) | @fd3455e53 new case → 1; old gate → 0 with `pub amount: Amount` present |
| P1A-10 (stream A) | opus | 10 | FIXED (9bb473f04; executing mode runs plane_host_vtable_{perf,alloc} and judges real criterion output: p50/p99 < 1µs, per-token zero, POD batch zero alloc; did-not-run = RED on every executed row; segment wiring dispatched) | `xtask/tests/hot_path_gates.rs::the_hot_path_{perf,alloc}_gate_executes_its_bench_and_judges_the_real_output` (--ignored) + 6 unit tests | @fd3455e53 planted BUSBAR_PERF_STREAM_CROSS / BUSBAR_ALLOC_INJECT → gates stayed GREEN (test 101) |
| P1A-seg (follow-up 2) | sonnet | 169/10 bench execution | FIXED (002ccd69f; benches segment runs the executing hot-path gates + the busbar-plugin vtable bench test; qa-segments.sh missing_test_target() made crate-aware (was hardcoded to crates/busbar/tests)) | `bash scripts/qa-segments.sh --selftest` → 0 (2 new cases) | new line under old checker → RED false positive "NON-EXISTENT test target hot_path_gates" |
| P1A-map (stream A) | opus | 89 (map-proof, unconstructed, sweep-coverage), 90, 209-case | all FIXED (3480ca692 map-proof red arms plant into a fixture corpus, floored has its RED case, bound names orphans; 8952482f9 unconstructed controls independent of the parked money row, restore-site is a real plant; f779932c5 sweep-coverage git answers overlay-plantable, every row RED over a green fixture). "7 corpus docs missing" STRUCK: docs exist, extraction PASS. Real gates stay RED on content debt (6b) | `cargo xtask selftest {map-proof (14), unconstructed (12), sweep-coverage (7)}` → 0, 0 IMPOSS | @fd3455e53 → 1 each (9 / 2+INERT / 1 IMPOSS + 4 uncovered rows) |
| P1A-lint (stream A) | opus | 89 (structure-lint) | FIXED (8c5b4a554; 4 IMPOSS cured via a DebtFree wrapper that hides a row's current offenders in both baseline and plant; stale allow-path repointed engine_kit.rs→engine_kit/core_impl.rs (clears 2 real reds); roots.rs comment → xtask/src/planes.rs). Remaining real reds: inline-tests ×5 (session-introduced; move dispatched), plane-dup tap_join_verdict (item 223, W1.2) | `cargo xtask selftest structure-lint --jobs 1` → 0 (39 cases, 0 IMPOSS) | @fd3455e53 → 1 (4 IMPOSS + green control Red) |
| P0A-117 (follow-up) | opus | 117 | 6812ea726: memo test moved to `crates/busbar-kernel/src/plane/tests/registry_memo_tests.rs` (new file, W0-117-owned), wired by #[path]; structure-lint inline-tests clear for registry.rs | same test, passes | as before |
| P1A-names (stream A) | opus | Phase-0 exit (qa-names), 89 (design-bindings, no-deferral-strict-done), cli/ship-ready text | all FIXED (892c97b9b REGISTER_REL kept (ledger default home, still used) with a `// qa-names:` declaration; standing red struck; 2 broken plants fixed; e0d1c461c note-witness + strict-done proven over green baselines; e02a745ad --posture refusal names the gate's own list). Real reds remain: note-witness (PB-58 witness never existed — Phase 2), strict-done 4 waivers | `qa_names.rs::tests::{each_plant_reddens_…, the_selftest_proves_every_owed_row}`; `cargo xtask gate qa-names` → 0; selftests design-bindings / no-deferral-strict-done → 0; `cli.rs::tests::the_posture_refusal_names_the_gates_own_standing_list` | @fd3455e53 gate qa-names → 1; lib tests → 101; selftests → 1 (IMPOSS) |
| W0.15 (follow-up 3) | opus | 315–329 tests relocated | 3f2b70c6a: four inline test mods moved to `crates/busbar-contract/src/caps/tests/{settle_carries_the_excess,take_site_census,honesty_table,seal_implementors}.rs` (new, W0.15-owned); structure-lint inline-tests PASS | same tests, pass | unchanged |
| P1A-2 (stream A) | opus | 2 | FIXED in part (984a9530e: 10 false frozen-wire pragmas removed — mcp:/McpEndpointSection/tools:/agents: absent at v1.5.3 AND v1.5.5 (DeployCfg 26 fields at both); 1 true kept + made verifiable (providers.rs:78 protocol:); new plane-purity:frozen-wire-claim row reads the snapshot at the claimed tag from git). mod.rs wiring handed to P1A-pur (same dir; partition error mine); plane-grep allowlist follow-up dispatched. Surfaced true debt: plane-purity key 5 / type 6, construction neutral-no-dialect 17→22 | `cargo xtask selftest plane-purity` (3 new RED cases) + `plane_purity/claims.rs::tests` | @fd3455e53 with the check wired → `frozen-wire-claim` 11 of 11 refused (exit 1) |
| P1A-2 (follow-up) | opus | 2 | 1be06f4ea: plane-grep-gate.sh drops the two false frozen-wire mcp allowlist rows (anthropic row kept); report 230→232 (true debt, report-only) | `bash scripts/plane-grep-gate.sh --selftest` → 0 | by reading (script's own dead-row check) |
| P1A-ki (stream A) | opus | 89 (kind-isolation, -ship), 118 (C1-KIND) | both FIXED (8375de737). 89: debt-free gate copy hides the findings the unplanted tree already reports (measured at run time) — 88+64 IMPOSS → 0; 12 stale plants retargeted. 118: C1 now measured over all 7 kinds — store/secret/auth/hook/export axes derived from the tree (whole string literals), 24 new [[instance]] rows armed at today's number, no ceiling moved | `cargo xtask selftest kind-isolation` (163) / `-ship` (124) → 0, 0 IMPOSS; `matrix/instances.rs` selftest + tests (new files) | @fd3455e53 selftests → FAILED (88 / 64 IMPOSS); ki-118-red.sh: 5 planted instance literals in core add 0 findings → 1 |
| P1A-pur (stream A) | opus | 3, 89 (plane-purity, plane-transport-neutrality, plane-purity-strict), 1a | 3=FIXED (3ad3aab43: instance vocabulary DERIVED from the tree — a new plane/transport/dialect crate moves its row RED; scanner byte-wise, 28s→5s); 89=FIXED (3ad3aab43 incl. P1A-2 frozen-wire wiring, 6814b9ca1; all four selftests 0 IMPOSS); 1a=BLOCKED→Phase 4 (architect: kernel×plane [[cell]] ceiling 3047 is the armed Law-0 mechanism; measured 3087 = +40, already RED; the 3084 figure is not reproducible). Derivation widened plane-purity:key 5→13 (the fifth plane "decisions" was invisible) | `plane_purity/vocab.rs::new_instance_tests::a_new_plane_transport_and_dialect_each_move_their_row_red` + selftests | @fd3455e53 test → 101 (0 hits for a new instance); selftests → 1 |
| P1A-con (stream A) | opus | 120, 89 (construction), pre-session rules.rs, qa loc rows | 120=FIXED (31270af83: manifest-allowlist reads transport; 7 transport rows now RUN and FAIL for real — unreviewed third-party deps + tls→unit-transport-key, grpc/sse→http — stay standing reds, drained by #40 work); 89=FIXED (31270af83: every case plants into a green fixture; exposed + fixed forbid-unsafe counting a string literal; 55960→15234 work units); rules.rs pre-session edit ADOPTED (6c18beeff, pure rename; snapshot in ~/Downloads); loc rows STRUCK with evidence (architect-applied patch, this log's next commit) | `construction.rs::tests::the_manifest_allowlist_rule_emits_a_row_for_every_transport_crate`; `cargo xtask selftest construction` → 0, 0 IMPOSS | @fd3455e53 → 101; selftest → 1 (5 IMPOSS) |
| P1A-9 (posture) | opus | 9 | c704d555e: MONEY_INVARIANTS_STANDING_REDS = [money-invariants:no-stored-price] (Phase 2 drain named); ci.yml + full_gate.rs run `--posture`; plain gate stays RED (1), posture 0 | `mod.rs::posture_tests::the_money_invariants_posture_holds_on_the_tree` | as before |
| W1.6 | opus | 165, 188 | 165=FIXED (80ae8c857: freshness judged against the checkout's HEAD, not a commit elected by the verdicts — 10 suites now correctly STALE (67ee79103, 953 behind); posture per §7.3 dispatched); 188=FIXED (80ae8c857: no-orphan-claim scans a claim vocabulary). Selftest 18 cases, 0 IMPOSS. README/manifest byte-identical | `conformance_sync/selftest.rs` cases (freshness ×2, orphan-claim, prose guard) | worktree @e970ed0ae new selftest → 1 (majority-stale + unresolvable → Green; SOC 2 certified → Green) |
| W1.8 | opus | 179, 218 | 179=STRUCK (already fixed by stream-A 3ad3aab43: at e970ed0ae the plant is seen; at 3ad3aab43^ → 101; regression pin added) · 218=FIXED (170291503: LLM-family freeze walks every neutral root, not the kernel alone; real tree 0 hits over 21 roots). Note: agent used `git stash` in its OWN worktree and dropped it — stash list empty, shared tree untouched | `xtask/src/gates/plane_purity/w18_tests.rs` (new, W1.8-owned) + selftest case | @e970ed0ae `cargo test -p xtask w18_tests` → 101 (freeze Pass over planted IrStreamEvent) |
| W1.6 (posture) | opus | 165 | 4de8b99eb: CONFORMANCE_SYNC_STANDING_REDS = [conformance:freshness] (KICKOFF §7.3/§7.4; drain Phase 5 release sha); ci.yml step → --posture (full_gate.rs line with W1.4) | `mod.rs::posture_tests::the_conformance_sync_posture_holds_on_the_tree` | gate RED on 10 STALE; --posture → 0 excused on exactly 1 row |
| W1.5 | opus | 163, 164, 231 | 163=FIXED (3f1763046: refusal arm reads a predating baseline's own source — exposed a TRUE widening, SecretRef accepts "none", queued Q10); 231=FIXED (MOVED control re-rooted on today's census member); Q7 resolved by instrument fix (classifier sees through forwarding Deserialize — no regen needed); 164=BLOCKED (needs owner-approved snapshot regen adding DecisionsSection, measured additive — Q10) | `config_schema/selftest.rs::run` (163 ×3, 231, see-through ×2); 63 cases, 0 IMPOSS | worktree @e970ed0ae → 1 (163 cases Green; 231 MOVED Red on dissolved crate) |
| W1.3 (money instruments, each alone) | opus | 167, 190, 191, 192, 232 | all FIXED (196324934 167 source-denylist UNPROVEN reachable; a39fa2ff3 190 single-terminal refuses a vanished terminal / silent door; 9da27e058 191+192 sealed-unit-traits refuses a vanished trait (one defect filed twice); 98bd18850 232 ceiling-slack reports an orphaned pin). Real-tree rows unchanged (PASS); gates only refuse more | `construction/external.rs::tests::a_denylist_scan_that_reports_defects_is_not_an_answer`; `construction.rs::tests::{single_terminal_refuses_…, sealed_unit_traits_refuses_…}`; `construction/ceilings.rs::tests::ceiling_slack_reports_a_pin_whose_row_never_appeared` | worktrees @e970ed0ae → 101 each |
| W1.4 (oracle instruments) | opus | 160, 161, 162, 185 (+a,b,c) | all FIXED (1626a084b 160 + (a): cli refuses unknown flags/stray words (exit 2), changelog-register --require-version, hot-path --execute; f100c192e 161 release-script excuse needs a workflow that RUNS the script, 162 dead gate names found outside ci.yml, 185 Tier::Fast acted on (+ conformance-sync --posture line); e518fcc7d (b)(c) clippy clean in loc/, planes.rs comment). Surfaced TRUE reds: no workflow runs verify-1.6.0-done.sh in full; instance-noun-neutrality + plane-pricing-blindness are Tier::Fast but run on no push — integration agent dispatched | `cli.rs::tests::*` (4), `full_gate.rs::tests::*` (3) | worktree @e970ed0ae `cargo test -p xtask --lib -- red_` → 101 (5 failing); 185 by reading |
| W1.1 (money items alone) | opus | 376–381, 421, 465–466 (+ a, b, c) | 377, 378, 379, 380, 465, 466=FIXED (c47a6a8a1, 27aa53c23, a1e21cba6, d378e9a3d, 1ab165a78, 129ba93c9); 376, 381=BLOCKED (needle repairs = formal raises → Q11, patch ready); 421=PARKED (ddac8fc6b, docs/design/1.6.0-PARKED/421.md → Q12); (a) decision plane rows correctly RED — no unit path (4362cd894; Phase 2 W2.4 adds units_decision.rs); (b) STRUCK — only stale KNOWN_UNSHIPPED id is the parked money one; (c) 197 FIXED (cff287a46, 3 nouns armed at today: memory 43, store, ranking) | construction rows plane-no-money / ceiling-slack / lean-core / hold-discipline; `cargo xtask selftest instance-noun-neutrality` (36, 0 IMPOSS) | worktree @e970ed0ae planted defects: 377 1→2 hits, 379 14→15, 380 PASS→FAIL, 378 FAIL→PASS, (c) new cases read GREEN on old fixture → 1 |
| W1.7 | opus | 175, 203 | both FIXED (1995cb3df: a vendor name in any neutral crate is Law 1 RED via new matrix/vendors.rs (reuses plane_purity::vocab::derive) — surfaced 62 true findings, Phase 4 drain; plant() refuses a plant under a directory no manifest governs) | `kind_isolation/matrix.rs::tests::{a_plant_under_a_directory_no_manifest_governs_is_refused, the_red_team_vendor_plant_in_a_neutral_plugin_crate_moves_the_row}` + 5 selftest cases | worktree @e970ed0ae red_proof_175/203 → 101 |
| W1.2 | opus | 183, 184, 222, 223, 224, 236 | all FIXED (be9c473f5: plane set, protocol crates and tree-wide scope DERIVED from PLANE_DECL + PLANE_KEYS (5 planes incl. decisions); grandfathered list a measured shrink-only ratchet (8); DEBT half of the plane ledger can fail). Surfaced true reds: plane-dup 24 + stale-ledger 3, axis:purity 4, census:count 3 — Phase 4; posture dispatched to P1-int | `xtask/src/gates/structure_lint/exit_tests.rs` (6, new); `cargo xtask selftest structure-lint --jobs 1` (42, 0 IMPOSS) | worktree @e970ed0ae → 101 (6/6) |
| P1-int (integration) | opus | 161/185/160 wiring, posture baselines, construction + structure-lint postures, clippy | all FIXED (b49db00d3 release-stage runs verify-1.6.0-done.sh in FULL on every qa staging and gates the promotion; changelog-register wired; instance-noun-neutrality + plane-pricing-blindness re-tiered Full; d4eaf02a6 clippy clean + cli.rs doc; 4be7fd3b5 CONSTRUCTION_STANDING_REDS re-derived from a real run, 10 expired kind-isolation raises struck; structure-lint posture (4 true reds, Phase 4)) | `full_gate.rs::tests::every_registered_excuse_holds_on_this_tree`; posture_tests (release-time, land.sh, structure-lint) | full-gate --selftest → 1 before (5 unproven excuses + 2 tier contradictions); construction --posture → 1 (13 NEW, 3 STALE) |
| P2A-hist (stream A, money) | opus | 23, 32 | both BLOCKED on the existing owner park (BUSBAR-1.6.0.md ~2727, LEDGER Q3) — root is the ledger storing no price instant; read-only fix would split enforcement from read; queued Q14 with worked figures | none | 23/32 by reading (keys.rs:1791 → derived_bucket_usage current card; D9 test asserts 7,000 vs 70,000) |
| P2A-a2a (stream A, money) | opus | 135, 151, 136(a2a) | 135=FIXED money alone (144a63201: hop metered/applied only after the relay says it left; queued Q15); 151=FIXED (6332f09ba: refused cursor write surfaced under error-once latch); 136=BLOCKED (a2a declares only `bytes`, whose billed meaning is owner-parked (§13); relay exposes no raw byte count; open classes unpriced until 123) | `busbar-a2a/src/a2a/tests/metered_verbs_tests.rs::a_hop_the_breaker_refused_bills_nothing`; `relay_stream_tests.rs::a_resume_cursor_the_store_refuses_is_surfaced` | worktree @079a16efc → 101 each (2 vs 1; not logged) |
| P2A-pool (stream A, security) | opus | 147 | FIXED (c168ec811 poisoned pool lock recovered, checked idle-expiry arithmetic; 1a1442afa every settings-surface duration bounded at the 30-year runtime horizon — `validate_limit_ceilings` forces a bound or stated reason for every new numeric field; queued Q16). Per-lane health/attempt timeouts dispatched as sweep 2 | `egress/engine/tests/pool_tests.rs::{an_unrepresentable_idle_timeout_does_not_kill_the_pool, a_poisoned_pool_lock_is_recovered_and_later_requests_are_served}`; `config_validate/tests/tests.rs::test_validate_refuses_every_duration_past_the_runtime_horizon` | worktree @079a16efc → 101 (overflow panic pool.rs:793, PoisonError :317; validate Ok for 2^63) |
| P2A-tests (stream A, money alone) | opus | 37 | llm half FIXED (d034c93dc: 4 tests inverted to #62, ledger fault — mid-stream cut bills streamed tokens; `billing_failed` removed end to end; queued Q17); D5/D8/D9 half BLOCKED on P2A-spend's file (dispatched to it; D9 = M32 owner park) | `busbar-llm/src/unit/tests/meter.rs::a_stream_that_died_bills_the_tokens_it_streamed_…` + 3 `ingress_indistinguishability_tests.rs` tests | worktree @079a16efc → 101 (0 vs 18/150/1000/1000) |
| P2A-dur (stream A) | sonnet | 150 | BLOCKED partial: operator-clearance site FIXED (3a1461f8b: checked settle → 5xx instead of silent 200 on a failed write); automatic verify-on-call site in mcp/method.rs is foreign-dirty; and the kernel quarantine primitive returns Ok unconditionally (record/clear swallow the store Result) — both carried | `busbar-mcp/src/mcp/admin_view.rs::settle_checked_tests::*` | worktree @079a16efc → compile error (no settle_checked) |
| P2A-amend (stream A, money alone) | opus | 29, 30 | both FIXED (d154cd25d 29: a negative fee is refused at the signing door (400), never sealed then clamped — RATECARD fault; verb is new in 1.6.0, no 1.5.5 client affected; 06c76cbe1 30: every sealed amendment journalled durably (RecordClass::Policy) with its figures BEFORE the append; survives restart and 1000 later calls). Separate money defect carried: RootHistory rebuilt from config at boot, so back-dated corrections stop repricing after restart — needs boot replay in main.rs (foreign-dirty) | `busbar/src/root/units_admin/tests/units_admin.rs::{amend_rate_history_refuses_a_negative_fee_…, a_sealed_amendment_survives_a_restart_…, a_thousand_later_amendments_do_not_erase_the_first, …}` | worktree @079a16efc → 101 (fee -1 applied; ring erased first record) |
| P2A-http (stream A, security) | sonnet | 145, 153 | 153=FIXED (34692b3c5, bound on the egress request wait — fix-up dispatched: the bound must be the operator-configured upstream_request_timeout_secs, not a hard 300s); 145=BLOCKED (dial cannot refuse a hostname until the kernel rewrites the authority to the pinned literal upstream — route.rs:198 passes operator hostnames; carried to Phase 4 egress) | `busbar-transport-http/src/tests/mod.rs::tests::a_stalled_upstream_response_is_cut_by_the_request_timeout` | @079a16efc `timeout 20 cargo test …` → 124 (hang) |
| P2A-grpc (stream A, security) | sonnet | 146 | FIXED (97b0bf8cc: HTTP/2 preface bounded on dial and accept (10s, own constant, PrefaceGuard bounds only time-to-first-read so idle-after-preface is not punished)) | `busbar-transport-grpc/src/tests/battery.rs::battery::{dial_side_stalled_preface_is_dropped, accept_side_stalled_preface_is_dropped}` | worktree @079a16efc red_proof probes → 101 (both directions unbounded) |
| P2A-pool (sweep 2) | opus | 147 | b0ff0527d: per-lane health interval/timeout + attempt_timeout_ms bounded at the same horizon; probe deadline checked_add (Q16 covers the visible part) | `config_validate/tests/tests.rs::test_validate_refuses_per_lane_durations_past_the_runtime_horizon`; `busbar-llm/src/engine/tests/health_tests.rs::test_probe_with_an_unrepresentable_timeout_still_classifies` | HEAD before b0ff0527d → 101 (validation Ok; tokio overflow panic) |
| P2A-flip (stream A, money + oracle) | opus | 125, 126, 36 | 125=FIXED as a measurement (flip ON ≡ OFF byte-identical on both C3 money cells; history-mid-window moved by root-llm #79 dated card; upstream-down cause independent — Q19; docs 826d485fe); 126=FIXED (21f725601: the invoice named; kernel-loop book must agree cell by cell, planted divergence RED); 36=FIXED (0ee95aafd: plane ledger write unconditional, billing-off is the view) | `plane_host/tests/govern_tests.rs::billing_off_charge_still_appends_the_counts_to_the_ledger`; `root/tests/units_llm.rs::the_second_book_agrees_with_the_invoice_cell_by_cell_and_a_divergence_is_red` | worktree @079a16efc 36 → 101 (0 vs 1); 126 plants → 101 (4 / 2 cells red) |
| P2A-auth (stream A, security + money) | opus | 144, 149 | both FIXED (164bde9ac alone: an identified principal with no governance key is refused, never admitted key:None — queued Q20 (1.5.5 had the same hole); c44bb5bbe: verify binds GET so the live admin gate and the verb table agree on all 92 routes). 4 busbar-llm tests that asserted the fail-open: correction dispatched | `busbar-kernel/src/auth/tests/tests.rs::a_principal_without_a_governance_key_is_refused`; `busbar-core-admin/src/admin_codec/tests/verbs.rs::the_live_gate_and_the_verb_table_agree_on_every_admin_route` | worktree @079a16efc → 101 each (admitted key None; 1 disagreement) |
| P2A-http (153 fix-up) | sonnet | 153 | 5e3518f5e: request bound threaded from `limits.upstream_request_timeout_secs` via ClientSettings (root/policy.rs:189) — the operator's configured value governs, default 300 unchanged | `busbar-transport-http/src/tests/mod.rs::tests::{a_stalled_upstream_response_is_cut_at_the_configured_request_timeout, a_response_within_the_configured_request_timeout_is_not_cut}` | configured bounds 1s/2s far from default — only pass if configuration drives the cut |
| P2A-auth (144 tests) | opus | 144 | d29cabcd7: the 4 busbar-llm tests bind the role their subject needs (none became a refusal test; no assertion loosened) + carriers test now also asserts an unbound role is refused 401 end to end | busbar-llm lib 585/585 | follow-up (tests were RED on HEAD after 164bde9ac) |
| P2A-stream (stream A, money) | opus | 137, 138, session ceiling | 137=FIXED (b6b883725 stop/barge-in settle the open turn — ledger fault; 4b28e3d64 every served session settles its durable row, idle sweep abandons it); session ceiling FIXED (27d447e02, compared and hard-closed — customer-visible, Q21a); 138=BLOCKED (switchover to the plane's units + lease deletion changes invoices — owner, Q21b) | `busbar-plane-streaming/src/tests/codec.rs::{a_twilio_stop_settles_the_open_turn, a_barge_in_bills_what_the_interrupted_turn_served}`; `busbar-voice/src/runtime/tests.rs::{an_idle_voice_session_is_abandoned_terminal_then_evicted, serving_a_session_to_its_teardown_settles_and_evicts_its_row, a_session_past_its_wall_clock_ceiling_is_hard_closed}`; `topology/tests/mod.rs::a_finished_telephony_call_settles_and_evicts_its_durable_row` | worktree @079a16efc → 101 (Some(1) vs Some(2); Some(0) vs Some(1); voice tests FAILED; ceiling timed out) |
| P2-foreign (reviewer) | opus | pre-session foreign edits | ADOPTED: G-docs sweep (cc264fb53), plugin-loader fmt (0fc810c6b), MCP handshake id S22 (5660e047c), root transports tests (b45d6bc20), settle_tests fmt (8d50605ed). REVERTED (snapshots kept): G-scripts (repointed at folded crate), LIST/DELETE-LIST stamps, busbar Cargo.toml comment (wrong line count), mcp upstream_credentials (Q22), mcp sampling bounds (Q23). LEFT AS FOUND: G-kernel InstalledLimits guard (inseparable from core-admin §9.6 files — Q18/Q22), posting_tests.rs (now owned by P2A-spend) | `busbar-mcp/src/mcp/client/jsonrpc.rs` const assert; `busbar/src/root/tests/transports.rs::{a_real_listener_bring_up_journals_the_access_the_boot_path_makes, a_plain_boot_with_no_tls_configured_journals_nothing}` | S22 by reading (0 > u32::MAX fails the build); each group checked in a worktree at 20d207322 |
| P2A-ppb (stream A, money) | opus | 35 | 35=BLOCKED (part-fixed): 56225cbbd the #42 billing switch moves kernel-side (plane_host::meter_series_billed); the LLM plane no longer branches on billing state — no figure moved; gate findings 17→12. Remaining 12 are the architect's, re-dispatched: (a) hold ×3 in busbar-llm/src/unit → kernel per #83, after P2A-hold; (b) voice D2 lease prices in the plane ×8 → kernel-owned session metering, figure-neutral; (c) response_body.rs CostHandle → opaque kernel pin | `busbar-kernel/src/plane_host/tests/mod_tests.rs::the_kernel_not_the_plane_decides_whether_a_metering_row_is_written`; gate row plane-pricing-blindness:billing-switch PASS | worktree @079a16efc gate → 1 (billing-switch FAIL); test → 101 (E0425) |
| P2A-panic (stream A) | opus | 148, 152 | 148=FIXED (e60f8d95b: a request panic → 500 in the path's dialect envelope, connection survives; /api/* frozen internal envelope), 152=FIXED (0ab9e8f25: byte_duplex releases the in-flight slot on unwind) | `busbar-kernel/src/ingress/tests/byte_duplex_tests.rs::a_panicking_handler_releases_its_inflight_slot`; `busbar-kernel/src/tests/tests.rs::{a_panicking_handler_fails_only_its_own_request, a_500_on_the_native_api_root_is_the_frozen_internal_envelope, the_request_panic_boundary_wraps_every_served_router}` | worktree @079a16efc → 101 (4 failed: gate, IncompleteMessage, 404, slot leak) |
| P2A-codec (stream A, money) | opus | 133, 134 | 133=FIXED (92227db57: a present-but-unreadable billed count REFUSES, never ledgers 0 — LEDGER fault; e.g. {input_tokens:"1500",output_tokens:9} was input 0/output 9, now CodecError::Malformed / stream error event / duplex Error usage_unreadable); no-float-money PENDING_CEILING 37→0. 134=BLOCKED (rerank search_units needs a counted-unit Billing variant + open-class pricing — carried into item 123). Follow-ups dispatched (P2A-codec2): usage_count::billed_count truncate(64) panics on multi-byte spellings (live DoS); cache-count helpers still map unreadable→None; the relay/streaming post-delivery refusal now ledgers 0 for the whole request | `busbar-llm-codec/src/{anthropic,bedrock,cohere,gemini,openai_chat,openai_responses}/tests/unreadable_count_refusal_tests.rs`; `busbar-voice-codec/src/ir/codec/usage_refusal_tests.rs` | worktree @079a16efc → 101 (18 + 2 refusal tests failed; controls passed) |
| P2A-codec2 (stream A, money) | opus | 133 remainder | FIXED: b6de4cb69 billed_count cuts on a char boundary (live panic on multi-byte spellings gone; per-dialect helpers now call billed_count, outcomes unchanged); 3efd38dd4 a present-but-unreadable CACHE count refuses (e.g. Responses cached_tokens:"40" was ledgered cache_read 0 → refused; Bedrock cacheReadInputTokens, Chat cache_write_tokens, Anthropic 5m/1h tiers + web_search_requests likewise) — covered by Q24 | `busbar-llm-codec/src/tests/usage_count_tests.rs::{a_multi_byte_unreadable_spelling_refuses_rather_than_panicking, billed_count_opt_is_none_when_absent_and_refuses_when_unreadable}`; `busbar-llm-codec/src/{openai_chat,openai_responses,bedrock,anthropic}/tests/unreadable_count_refusal_tests.rs` cache cases | worktree @079a16efc → 101 (panic at usage_count.rs:128; 2/3/3/3 refusal tests failed; controls green) |
| P2A-spend (stream A, money) | opus | 139, 22, 104, 25, 124, 28, 27, 31 | all FIXED. f81ec89fe CostBreakdown checked sum, overflow refused (139); 029577a2d a sub-quantum rate is an UNPRICED cell, never priced at 0 — RATECARD fault (22); 789d55a78 ONE function: every spend derivation routes through busbar_kernel_ledger::cost::Tally — six copies collapsed (104/25), unpriced lane refuses at both doors and every read (124/31 — BROKEN DERIVATION: was fee-only 20,000 µ, dropping 19,020,000 µ), overflow refused not pinned at i64::MAX (28), one tier rule (27: non-standard tier 3 nano @5,000bp 2→1; production all standard, no move). d5 inverted to #42, d8 exact to MONEY_EGRESS | `busbar/tests/money_one_function_equivalence.rs` 14/14 (census + d4/d5/d6/e/i22/i28); `busbar-kernel/src/plane/tests/cost_tests.rs` overflow ×2; `busbar-kernel-ledger/src/cost/tests/rate_tests.rs::a_sub_quantum_rate_is_unpriced_on_the_card_and_refuses_never_priced_at_zero` | worktree @079a16efc → 101 (red_139, red_22, red_p2a_spend 5/5, census named all six copies) |
| P2A-hold (stream A, money) | opus | 99, 100, 129, 127, 128, 26, 105/131, 130, 132 | all FIXED (26 partly: record_release/cross_window_transfer/repricing/adjustment kept — owned by amendment/window-roll paths). 0fff8e2df an abandoned unit's end is POSTED — LEDGER fault (1000 hold, 900 served: (rows,settled,open,released) (0,0,1000,0)→(1,900,0,100)); drop guard marks, the sweep is wired (129/100). c05a45207 holds journalled (Transaction "hold.open"), recovered at boot, restart reconciles the REAL book (127/128: restarted book settled 0→4200; lost row → RED), drawn/unreconciled written (26). 9a83a8399 residual() checked (131). 941d24005 verify() takes the anchored head (132). 7dad9d91e HoldCell take counted (130). Kernel code −5 vs pin; qa ticks ceiling re-pinned DOWN 193→192. unconstructed row crash-recovery-open-holds struck per its own switch | `busbar-kernel/tests/abandoned_settlement.rs`; `busbar/src/root/tests/units_llm.rs::a_unit_whose_task_went_away_is_marked_and_the_sweep_posts_its_hold`; `busbar/src/root/tests/durability.rs::{a_hold_survives_a_kill_9, the_restart_reconciliation_goes_red_on_a_book_that_lost_a_row, the_reconciliation_names_a_figure_the_book_moved_without_a_record, a_posting_the_journal_lost_is_unreconciled_until_the_log_confirms_it}`; ledger identity/checkpoint tests; construction selftest +2 cases | worktree @079a16efc → 101 (99, 128, 131 debug+release, 132 E0061); 127/129/130/26 by reading |
| P2A-spend2 (stream A, money) | opus | 22 residue, 28 residue | 22=FIXED (a9f72ef62: a rate the card cannot hold is a BOOT refusal naming model, class, rate — #77(5)); 28=FIXED (69c58179a: ledger-identity rows + legacy rows project CHECKED via Money::of_nanos; i64::MAX×1000+1000 nano was served "9223372036854775807" → the read refuses; in-range byte-identical). Pinning-helper deletion + units_llm.rs settle (priced_posting .ok(), priced_amount 0/u64::MAX, LateAccrual early return) handed to P2A-unit. Kernel code 51967→51967 | `busbar-kernel/src/config_validate/tests/tests.rs::test_validate_rate_card_refuses_a_rate_the_card_cannot_hold`; `busbar/src/root/tests/ledger_identity.rs::a_row_past_the_served_range_refuses_and_is_never_pinned_at_the_ceiling`; `busbar/src/root/units_admin/tests/units_admin.rs::a_ledger_figure_past_the_served_range_fails_the_read_and_is_never_served_pinned` | worktree @079a16efc → 101 (validation accepted; figure pinned at i64::MAX) |
| P2A-ppb2 (stream A, money) | opus | 133 addendum, 35 (voice + response_body) | 133-add=FIXED (86734752f: a usage the reader refuses after delivery bills the floor, never 0 — buffered anthropic relay 0→51 tokens; OpenAI same-proto stream 0→126; readable control 1509→1509); 35b=FIXED except FixtureHost (1ee8dac1d: the D2 lease pricing moves behind kernel plane_host::session_meter — figure-neutral, voice-conform 15/15 legs PASS; kernel files touched 4331→4330, paid by deleting uncalled ExportDispatch/resolve_pool_*/test_support helpers); 35c=BLOCKED (UsageSink literals live in P2A-unit files — carried to P2A-ppb4). Gate findings 12→6 | `busbar-llm/src/engine/tests/unreadable_usage_floor_tests.rs::{buffered_relay_refused_usage_bills_the_floor_not_zero, stream_refused_usage_bills_the_floor_not_zero}`; `busbar-kernel/src/plane_host/tests/session_meter_tests.rs::fixed_script_stores_the_same_figure_and_closes_on_the_same_turn` (+4); `busbar-voice/.../voice_d2_billing_oracle.rs::voice_d2_lease_billing_oracle` | worktree @789d55a78 → 101 (both billed 0); 35b by reading (gate at pin: voice price ×7 + rate-card ×1; session_meter absent) |
| P2A-123 (stream A, money) | opus | 123, 134, 140 | 140=FIXED (4e1da4cb6 thread::spawn CAS race test). 123=FIXED (d10cf4147, landed by the architect from the agent's local branch — branch deleted, bundle at ~/Downloads/busbar-1.6.0-snapshots/P2A-123/: open meter classes priced in config are charged AND capped across all four layers (POD decode, host meter, budget gate, RateEntryCfg `units:`); dead kernel cost::price + reserved_rate `_ => 0` deleted; kernel code −114). 134=FIXED (84f2861bd rerank search units reach the price: 50 units @2000 µ 0→10 ¢). Architect follow-up ba1f560e6: census exemptions 1ee8dac1d fixed struck → money_one_function_equivalence 14/14 | `busbar-kernel-budget/src/tests/leases.rs::racing_admissions_never_jointly_overshoot_the_concurrent_cap`; `busbar-kernel/src/governance/tests/limits_tests.rs::an_open_class_priced_in_config_is_charged_and_capped_end_to_end` (+ refusal twin, config grammar, govern decode); `busbar-llm/src/engine/tests/rerank_search_units_tests.rs::a_reranks_search_units_reach_the_price` | 140 CAS→load+fetch_add → 101 (admitted 4 of cap 3); 123 @079a16efc → 101 (unknown field `units`); 134 → 101 (Ok(0) vs Ok(10)) |
| P2A-unit (stream A, money) | opus | 35 (plane holds), 35 spend residue, 28 addendum | all FIXED. ba0fdb438 the LLM plane opens no hold and accrues nothing — the kernel admit row opens it (figure-neutral; gate row plane-pricing-blindness:hold 3 files→PASS). 8fbbc9acb LLM late settlement prices FAIL-CLOSED — RATECARD applied through the read posture at settlement (Durability book: silent-card class 7,000,000 nano→refused; overflow u64::MAX→refused; no invoice or /usage figure moves). 681d52c54 pinning projections micros_of/minor_of/cents_of + Priced::micros/minor deleted; every caller reads the checked Money::of_nanos. Durability book holds money only, no counts → carried to P2A-dur (#71) | `busbar/src/root/tests/units_llm.rs::{an_unpriced_class_on_a_present_card_keeps_its_counts_row_and_the_read_refuses, a_settled_figure_past_the_record_refuses_and_is_never_pinned}`; ledger posting/scale/identity projection tests; gate row hold PASS | worktree @5e14d75a9 gate hold FAIL (admit.rs, meter.rs, route.rs); @ba0fdb438 probe → 101 (posts 7000000 nano); 28 by reading |
| P2A-kpay (stream A) | opus | loc-ceilings:kernel, :teller | FIXED: cca32c9e6 teller 681→659 (imports, borrowing closures — no step/arm/mint moved); a1a5b3cbc egress/fixtures.rs carries the file-level test-only cfg its `mod` line already has, so the counter bills its 391 lines as test (kernel 51859→51446). Architect 7a786417a re-pinned DOWN in a clean worktree (kernel 51536→51439, teller 661→659; ceiling-slack green). Counter note: a mod-level cfg is not carried into the file — test_support/** (~2,400 test-only lines) still counts as code | `cargo xtask gate construction` rows loc-ceilings:kernel + :teller PASS | before FAIL 51859/681 (kpay-gate0-full.txt) |
| P2A-books (stream A, money) | opus | #71/126 follow-up | FIXED (15d23bb90): the Durability book carries raw counts per class (tail, older journals replay byte-identically); a refused unit posts its counts row (0→1 rows); the books agree on EVERY class cell (rerank second book 30,000,000→130,000,000 nano when fed search units). No served figure moves. Carried: TapReport.usage drops Billing::Counted (busbar-llm engine — after P2A-ppb4); units_admin NodeLedger::rows_of should read Durability::settled_read so /admin/ledger/totals refuses over a refused row | `busbar/src/root/tests/units_llm.rs::{an_unpriced_class_on_a_present_card_leaves_a_durable_counts_row_and_the_read_refuses, a_reranks_search_units_agree_across_both_books, a_planted_open_class_divergence_goes_red}`; `busbar/src/root/tests/durability.rs::{a_journal_written_before_the_counts_tail_rebuilds_the_same_book, a_refused_counts_row_survives_a_restart_and_the_read_refuses, the_reconciliation_names_a_refused_row_the_node_lost}` | worktree @dd6761259 red_probe → 101 (0 vs 1 rows; 30M vs 130M; divergence unseen) |
| P2A-136 (stream A, money) | opus | 136 | MCP=FIXED, A2A=BLOCKED (§13 parked billed-byte, line 1853). 6a32b3a67 an answered MCP tools/call ledgers one tool_calls on the caller's budget chain — LEDGER fault (was a Queries meter at 0). Architect ruling (per-plane card, #42/#47) → 1f93dbd71: an MCP class is priced by the MCP plane's card (absent today → reads 0), never the llm card: llm card + MCP traffic ALL REFUSED → served, rows ledgered at 0; kernel net −6 (dead derive_spend_minor deleted). Lane tag `mcp\\u{1f}<tool>` via PLANE_LANE_SEP until the #47 per-plane card slot | `busbar-mcp/src/mcp/tests/upstream_join_tests.rs::{an_llm_card_does_not_price_mcp_tool_calls_they_are_ledgered_and_nothing_refuses, a_requests_cap_trips_on_mcp_tool_calls_and_the_llm_budget_does_not}` | worktree @dd6761259 → 101; @6a32b3a67 → 101 (429 budget_exhausted on call 2) |
| P2-clippy (hygiene) | sonnet + architect | pinned-toolchain clippy regressions from Phase 2 | FIXED: architect 3b… store RAM MeteringKey alias + conformance outer doc (#79 key), 58634ff64 redundant must_use, result_large_err per the file's precedent, orphaned test doc, same-type conversions; agent db70f4723 orphaned MOVED-test doc comments (kernel config/plane/tls tests), d5729eb11 constant assert → const _ (same guarantee) | `cargo clippy --workspace --all-targets --locked -- -D warnings` → exit 0 | worktree @b145f2640 clippy → nonzero (all 4 lints reproduced) |
| P2A-adminread (stream A, money) | sonnet | #71/126 follow-up (served read) | FIXED (4a2f03ecf): /admin/ledger/totals + /reconciliation answer 503 (GovernanceError::Store) when the node's book holds a refused counts row, instead of serving a figure missing that row; clean books byte-identical (1.6.0-only endpoints) | `busbar/src/root/units_admin/tests/units_admin.rs::a_refused_counts_row_fails_the_totals_and_reconciliation_reads_a_clean_book_is_untouched` | worktree @d5729eb11 → 101 (served 200, want 503) |
| P2A-ppb4 (stream A, money) | opus + architect | 35 (last findings) | 35=FIXED (036d64472, built by the slot as a patch, landed by the architect because the seam change breaks five call sites outside the slot): UsageSink carries a kernel-owned opaque MeterPin; MeteringHost leaves EngineHost's supertraits; BudgetHost is counts + verdicts only; FixtureHost names no price type. Figure-neutral. plane-pricing-blindness 17 → 12 → 6 → 0 across P2A-ppb/ppb2/unit/ppb4: gate exit 0 (11 rows green). Kernel code 51440→51428 | `busbar-kernel/src/plane_host/tests/mod_tests.rs::the_kernel_not_the_plane_decides_whether_a_metering_row_is_written`; `cargo xtask gate plane-pricing-blindness` → 0 | @6a32b3a67 → 101 (E0046/E0425/E0050); gate before RED (response_body.rs:22, fixture_host.rs) |
| P2A-adminread (follow-up) | sonnet | construction one-pricing-site:fee-fields | FIXED (57e02d813): the 5 extra reads were one commit (06c76cbe1) spelling its journalled AmendmentRecord field `per_request_fee`; renamed `sealed_fee` — architect review: accepted because the field is dated rate-card HISTORY (#79), i.e. data of the card itself, not a pricing read; wire spellings, signed payload and figures unchanged; 9/4 → 4/4 | `cargo xtask gate construction` row one-pricing-site:fee-fields PASS; amend-rate-history tests green | worktree @e700b7488 gate → 1 (9/4) |
| W2.22 | opus | 439 | 439=FIXED (bcdfd42f6 + 82a08d4b6 fmt: meter_tests doc's unterminated opening line described the removed short form as present; doc-only, not money) | `busbar-kernel-ledger/src/usage/tests/meter_tests.rs::the_fold_cell_doc_opens_with_its_claim` | worktree @079a16efc → 101 |
| W2.26 | opus | 304 | 304=FIXED by 84f2861bd (item 134): RerankResp::billing() threw away the search_units it read — BROKEN DERIVATION (search_units=3 billed Flat → Counted{search_units,3}); covered by Q29; no new commit | `busbar-llm-codec/src/ir/tests/rerank_tests.rs::rerank_resp_billing_counts_search_units_as_an_open_class` | by reading: at 079a16efc rerank.rs:100 returns Some(Billing::Flat) |
| W2.21 | opus + architect | 436 | 436=FIXED (e57a52af8, built as a patch spanning W2.22's tests + busbar-kernel/tests, landed by the architect): the dead second settlement table deleted — BROKEN DERIVATION (DurabilityLost erased the end kind: floor 120 billed vs live teller 0); zero production callers, no served figure moves | `busbar-kernel-ledger/src/usage/tests/one_table_tests.rs::the_usage_unit_ships_no_second_settlement_table`; `busbar-kernel/tests/settlement_table.rs::row_a_lost_settle_record_on_a_completed_unlocated_unit_still_bills_nothing` | worktree @079a16efc → 101 |
| W2.24 | opus | 302 | 302=BLOCKED (owner, Q31): a buffered Cohere finish_reason ERROR is relayed to every other protocol as a SUCCESS (openai stop / anthropic end_turn / gemini OTHER), billed 10/5, recorded Complete, breaker success — LEDGER fault rooted in busbar-llm buffered.rs deliver_json ignoring stop_reason; the fix changes what clients see | none (blocked) | measured in a worktree at HEAD (zz_measure_302) |
| W2.25 | opus | 303 | 303=STRUCK: "a failed generation is billed as a completed one" refuted — the stream-end failure flag decides the END and the breaker, never the charge (#62; response_body.rs:716-717, :826-840, :889-899); Gemini MALFORMED_FUNCTION_CALL billing is correct. The non-money half (no Error event, breaker unaffected, success terminator cross-protocol) joins Q31 | none (STRUCK, test unchanged) | refuted by reading response_body.rs |
| W2.8 | opus + architect | 244, 245, 246, 247, 259 + Cargo.toml comment | 244/247/259=FIXED (b7e8d15d7: main.rs doc claims match the code; landed by the architect hunk-only through the index — main.rs's foreign hunk untouched); 245/246=BLOCKED on root/ (W2.4): 246 is MONEY — root/kernel.rs:815 prices the usage fold against an EMPTY policy (no pools members, no lane prices); Cargo.toml comment fixed (ccae7093c) | `busbar/src/tests/tests.rs::main_rs_doc_claims_match_the_code_beside_them` | worktree @bcdfd42f6 → 101 |
| W2.9 | opus | 406, 407, 408, 409 | all FIXED (610ef9295 export vs hook access pinned; 4751e0a52 restore at top of range pins the saturated position; 82150d568 head history past 8,760/yr loses no anchor; 7c3306990 scheme byte admitted in prose + flipped byte a detected tamper) — tests/prose only, not money | `busbar-kernel-audit/src/tests/{amend_tests,legacy_ring_tests,sign_tests,digest_framing_tests}.rs` (4 named) | worktree @079a16efc with each defect planted → 101 ×4 |
| W2.27 | opus | 306 | 306=FIXED (bff1fc4f9: the silent-zero guard now sees the read-through-the-seam spelling — 40 offenders at 079a16efc, 0 at HEAD); remaining lenient cohere billed reads (search_units, classifications) → P2A-codec3 | `busbar-llm-codec/src/tests/usage_count_tests.rs::no_dialect_defaults_a_lenient_count_read` | worktree @079a16efc → 101 (40 offenders) |
| W2.23 | opus | 274 | 274=FIXED (5dcb03146: Journal::forget latches overflowed — a late write to a forgotten scope resumes from the store tail instead of forking the durable chain at seq 1; kernel code −3) | `busbar-kernel/src/audit/tests/journal_tests.rs::a_forgotten_scope_on_an_uncapped_stream_resumes_from_the_store_not_seq_one` | worktree @079a16efc → 101 (seq 1 vs 3) |
| W2.24 (addendum) | opus | order-dependent rerank test | FIXED (99ad3dcbb: no_rerank_handler_on_the_other_four installs the protocol registry itself; assertions unchanged) | `busbar-llm-codec cohere::handler::rerank_tests::no_rerank_handler_on_the_other_four` alone | worktree @079a16efc run alone → 101 (panic at :360) |
| W2.13 | opus | 435, 438 | 435=FIXED (e10d7701f, money alone: recompute priced a line at an archive per-bucket tier production never filled — BROKEN DERIVATION; 9,000bp line 13,000,000 → 11,700,000 nano, now agrees with the statement; zero production callers, nothing served moves); 438=FIXED (e04c67858: seal and verify share one preimage + one digest call; byte-identical signatures) | `busbar-kernel-ledger/src/tests/recompute_tests.rs::the_recompute_and_the_statement_price_a_discounted_line_alike`; `checkpoint_tests.rs::the_seal_and_the_verify_share_one_preimage_and_one_digest` | 435 worktree @079a16efc → 101; 438 by reading (two encode_body + two sha256 calls at the pin) |
| W2.19 | opus | 418, 419 | both FIXED (201bc6c9d, money alone: the Twilio media base64 treats padding as terminal like the workspace's one decoder — LEDGER fault: "QQ=="×2000 recorded 3000 bytes / 375 ms audio_ms_in → frame refused, 0; the comment claiming an identical algorithm now states the contract). No served invoice moves: voice production billing is the D2 lease behind the kernel session meter (Q21b) | `busbar-plane-streaming/src/tests/twilio.rs::{a_media_payload_resuming_after_padding_is_refused_not_billed_as_a_longer_payload, a_padding_resumed_media_frame_is_refused_at_the_plane_entrypoint_and_opens_no_turn, the_media_payload_decoder_keeps_the_rest_of_the_shared_decode_contract, the_local_base64_copy_names_the_one_decoder_it_must_match_and_claims_no_unchecked_equivalence}` | worktree @079a16efc → 101 (3 failed) |
| W2.11 | opus | 374, 375 | 374=FIXED (e6afd7686, money alone: GET /admin/usage refuses a HOLE in the dated rate-card history — RATECARD fault: a row no entry covers was priced at the current card (fixture 1,000 µ served) → MoneyError::NoCardInForce; production histories open at effective_from 0 so no served figure moves); 375=FIXED (6f67be8ba: v1/ cites the xtask gates that replaced deleted shell lints; a test keeps every cited scripts/ path real). Rule breach noted: the agent ran `git stash` once in its worktree (shared stash list) and dropped it at once — stash list empty, shared tree untouched | `busbar-core-admin/src/v1/tests/service_tests.rs::{dated_rate_card_history::a_hole_in_the_dated_history_refuses_rather_than_pricing_at_the_current_card, every_scripts_path_cited_in_v1_exists}` | worktree @079a16efc → 101 ×2 |
| W2.7 | opus | 395, 397, 398, 399, 400, 403 | all FIXED. 3ee30dbef (money alone): a usage member present but not a whole count is LOCATED, never dropped — LEDGER fault (units 7.5/-3/"7"/null: 0 lines → 1 located line; 2^64-1: Int(-1) fact → location-only); 3396a0ffb one provider resolution + provider session fact (397/399: several providers fail closed like EMPTY); 9d1decf9e crate prose matches the tree (398/403); 18fe7c764 CONFIG_SCHEMA model entry held to ModelCfg (400). Nothing served moves: the decision plane has no unit path yet (W2.4) | `busbar-plane-decision/src/tests/plane.rs` (395 ×3, 397, 399 ×2); `src/tests/lib.rs` (398, 403); `src/tests/meta.rs::config_schema_model_entry_is_model_cfg_field_for_field` | worktree @079a16efc → 101 (5 failed; 400 by plant) |
| W2.18 | opus | 450, 451 | 450=FIXED (a8a1b9da4, money alone: a hop goes to the agent the request NAMED, not agents().first() — LEDGER fault: /a2a/agents/beta was dialled alpha.invalid:443, scope agent:alpha, lane a2a-a → beta / agent:beta / a2a-b; unnamed with several configured → unreachable; production registers only A2aPlane::EMPTY so nothing served moves); 451=BLOCKED → dispatched P2A-451 (busbar-kernel-egress attempt.rs:381/:717 hardcode None so Codec.streaming is never set — every streamed answer ends at its first /result frame, later events never relayed or metered) | `busbar-plane-a2a/src/tests/plane.rs::{a_hop_goes_to_the_agent_the_request_named, a_request_naming_no_carried_agent_reaches_none}` | worktree @079a16efc → 101 |
| P2A-codec3 (stream A, money) | opus | 133 remainder (cohere) | FIXED (940d2b128, money alone): Cohere billed_units search_units/classifications and the rerank/embed billed counts refuse when present-but-unreadable — LEDGER faults: rerank search_units:"3" billed Flat/0 → refused; embed input_tokens:"1500" billed 0 → refused; chat billed units dropped → refused (covered by Q24). Guard tightened (lenient_billed_fields). Carried to P2A-codec4: gemini/bedrock embeddings handlers read billed input leniently | `busbar-llm-codec/src/cohere/tests/unreadable_count_refusal_tests.rs` (5 + control); `src/tests/usage_count_tests.rs::no_dialect_defaults_a_lenient_count_read` | worktree @079a16efc → 101 (5 failed); guard @0b0a9e698 → 101 (7 cohere sites) |
| P2A-tap (stream A, money) | opus | tap report open classes (#71/#43) | FIXED (b96981d2b, money alone): TapReport carries every billed class (open_units via engine::usage::open_units_of — the same map the governance ledger records) — a served cross-protocol rerank's 50 search units reach the durable book: counts {}→{search_units:50}, settled 30,000,000→130,000,000 nano (= the invoice); /admin/ledger/totals +100,000,000 nano per such rerank; silent card → refused counts row → totals/reconciliation refuse (#42). /usage + invoices unchanged. Found: a SAME-protocol rerank ledgers search units on NEITHER book (CohereRerank has no usage tap; Bedrock same_protocol_usage None for results) → P2A-rerank2 after P2A-codec4 | `busbar/src/root/tests/units_llm.rs::a_served_rerank_puts_identical_search_units_on_both_books` | worktree @e700b7488 → 101 (counts {} vs 50) |
| W2.6 | opus | 242, 243, 257, 258, 262, 263, 264, 266, 267 | 257/258/262/263/264/266/267=FIXED (bea770849 refusal scan is a census of every Refusal::new site; 6ff585681 carried claims held to the evidence bar + totality enumerates workspace plane crates; 184b5dff0 per-(leg,column) proof tally; 87d6debb4 names the blocking twin that exists; 9410beded voice_boot imports the kernel const) — tests only; 242=BLOCKED (fee-count half of the reconciliation compares 0 with 0 on every row: neither store keeps a fee count — kernel-ledger legacy.rs + book totals + units_admin; operator-visible fee_count 0→N); 243=BLOCKED (decision plane needs a capability-equality column + root leg + 15 argued allow rows — qa/ owner; patch ready) | 7 tests across `busbar/tests/{capability_equality,money_never_ends_an_admitted_unit,field_coverage,plane_isomorphism,plane_transport_neutrality,voice_boot}.rs` | worktree @079a16efc, one planted defect per item: old → 0, new → 101 ×6 |
| W2.12 | opus + architect | 404, 405, ring addendum, money-invariants:no-stored-price | 405=FIXED (0586fca50 renumbered record reported as RENUMBERED; scope half STRUCK — one chain carries every subject); ring addendum FIXED (0b0a9e698 a position is never reused and never wraps); no-stored-price=FIXED (7b8999163, built as a patch spanning root/ + xtask, landed by the architect: AuditRecord.pre_tier/priced + HookApplied.priced_delta drained, recipe v2 beside v1 — LEDGER fault, always 0 in production; money-invariants 3 rows GREEN = Phase 2 exit test); 404=BLOCKED (needs production callers in the hook runner, export dispatch and a correction verb; Adjust{was,now} must become counts) | `record_tests.rs::break_reporting_tests::a_renumbered_record_run_with_every_link_intact_is_reported_as_renumbered`; `legacy_ring_tests.rs::a_ring_position_is_never_reused_and_never_wraps`; `cargo xtask gate money-invariants` green | 405 @079a16efc → 101; ring @e49ebadc9 → 101 ([MAX,MAX,0]); gate unpatched → 1 |
| P2A-card47 (stream A, money) | opus | #47/#42/#43 per-plane cards (gap, no TODO row) | FIXED (6c618af89, money alone): each non-llm plane section takes the reserved core-owned rate_card (stripped before the plane parses, #43); Tally resolves a row's card from its plane key; tools.rate_card prices MCP tool_calls (3 × 10,000 µ = 3 minor; budget 3 trips on call 4); PLANE_LANE_SEP stopgap gone; kernel −3. A flat card beside a card-less tools/agents section now boots (that plane reads 0). <plane>.fees parsed and refused → owner ruled IMPLEMENT (P2-fees) | `busbar-mcp/src/mcp/tests/upstream_join_tests.rs::{a_tools_card_prices_tool_calls_and_a_budget_cap_trips_on_mcp, a_present_tools_card_silent_about_a_tool_refuses, an_llm_card_entry_named_like_a_tool_never_prices_it}`; ledger view_tests ×4; kernel config + config_validate tests | worktree @3ee30dbef → 101 (rate_card parsed as a server; E0432) |
| P2A-codec4 (stream A, money) | sonnet | 133 remainder (embeddings) | FIXED (84e3f2e6e, money alone): Gemini + Bedrock embeddings billed input refuse when present-but-unreadable (input_tokens:"1500" billed 0 → refused; covered by Q24); guard gains lenient_struct_maps | `busbar-llm-codec/src/{gemini,bedrock}/tests/unreadable_count_refusal_tests.rs::embeddings_response_refuses_an_unreadable_billed_input` | worktree @079a16efc → 2 failed |
| W2.10 | opus | 363, 366, 368 | 366=FIXED (53c71924f, money alone, figure-neutral: the fee-base refund decided in ONE place, the admitted terminal door; the Meter step's unread copy deleted); 368=FIXED (3a09087b7: step files are live — "BUILT DARK" notes and file-wide allow(dead_code) dropped); 363=STRUCK (the item-126 finding again: reported_after_terminal calls no host seam; both books agree — 21f725601/15d23bb90/b96981d2b) | `busbar-llm/src/unit/tests/chain.rs::the_refund_is_the_terminal_doors_and_it_lands_on_the_ledger`; `unit/tests/route.rs::no_step_file_claims_to_be_dark_or_silences_dead_code_file_wide` | 366 planted lost refund @079a16efc → 101; 368 by reading |
| W2.28 | opus | 362 | 362=FIXED (822d3f2b0, money alone): a streamed OpenAI-egress request is never forwarded unable to report usage — LEDGER fault: stream_options:null was forwarded verbatim so the ledger read 0 (fixture 0 → 24 tokens; client sees no difference); a wrong-typed stream_options is refused 400 before any send (customer-visible new refusal → Q33) | `busbar-llm/src/engine/attempt/tests/assemble.rs::{a_null_stream_options_is_upgraded_so_the_upstream_reports_usage, a_wrong_typed_stream_options_is_refused_before_any_send, the_injectors_answer_err_on_a_wrong_typed_stream_options}` | worktree @079a16efc → 101 (no include_usage; lenient mock served 200) |
| W2.17 | opus | 364, 365 | both FIXED (88b505dbb /metrics doc no longer calls /healthz the sole open route; ba3c2313c charged=true refund is the synchronous decrement, not async) — test comments + extended assertions, no figure moves | `busbar-llm/src/engine/tests/forward_pool_integration_tests.rs::test_metrics_requires_auth_in_chain_mode`; `ingress_integration_tests.rs::finish_admitted_does_not_refund_an_uncharged_admit` | mutated assertions → 101 (401 vs 200; 30 vs 0) |
| P2A-451 (stream A, money) | opus | 451 | FIXED (35400e7c6, money alone, latent): the egress attempt opens the plane's upstream half and threads it through encode_egress and every frame's decode_response — an N-event streamed answer is relayed and metered to its terminal (was cut at event 1: 1 vs 4); PlaneRef{Stateless,Session}; id-only envelope half is already bounded (walk budget / stream ceiling, refunded, never Complete) | `busbar-kernel-egress/src/tests/upstream_half_tests.rs::an_incremental_answer_of_n_events_is_relayed_and_metered_to_its_terminal` (+ one-shot control) | worktree @079a16efc → 101 (1 vs 4) |
| W2.16 | opus | 367, 369, #43 carried | 367=FIXED (056aa005c, money alone: a same-protocol non-stream body dropped mid-relay bills what it relayed — LEDGER fault: 176-byte anthropic prefix 0 → 44 output tokens; commit message says 164/41, erratum recorded); #43 carried=FIXED (d09c2e0b0, money alone: the LLM metering row is written unconditionally, billing-off is the view — billing-off /admin/usage metering rows 0 → 1 per request, spend unchanged); 369=FIXED (e8ca5f96e comment). The billing-off test flip in root/tests/units_llm.rs handed to W2.4 (patch); meter_series_billed now has no production caller → delete (kernel LOC) | `busbar-llm/src/engine/engine_tests/nonstream_drop_billing_tests.rs::{a_same_protocol_nonstream_body_dropped_mid_relay_bills_what_it_relayed_not_zero, a_billing_off_delivery_still_writes_its_metering_row_and_the_view_reads_zero}` | worktree @9410beded → 101 ×2 |
| W2.5 | opus | 382–390, 392, 393 | 382/383/384/386/387/388/389/392/393=FIXED (934bafd6c money alone: a published abi<4 store keeps a rate-card-split metering day split — LEDGER fault: 100×A+100×B read back 200×A, 600→800 at A=3/B=5; e9703e82e bounded kind scan + caller deadline on SecretRequest::Resolve + ffi_guard claims as measured; b305c10ee PlaneDecl size over-claim refused / owned PlaneState / admin_routes+openapi readers / confined constructor crossings; c3aab4459 dropped-in kind:transport refused with the reason); 385=BLOCKED (C-ABI→native PlaneDecl adapter + composition-root open_plane — item 63/H6, main.rs + kernel plane registry); 390=BLOCKED (store ABI 5 request variants — busbar-plugin cold, plugin-sdk, store repos). Architect ruling on 388 residue: hydrate/start stay on the caller thread (host-generation rule wins; the TLS-confinement rule covers build/config_validate) | `plugin-loader/src/tests/{legacy_metering_tests,loader_seam_tests,plane_decl_tests,kind_refusal_tests}.rs` (9 named); crate 261 green | worktree @079a16efc → 101 (382, 384, 392, 388, 389); 383 abort measured (exit 134); 386/387/393 by reading |
| W2.20 | opus | 460 | 460=BLOCKED → folded into P2-voice (owner ruling Q21b): the admission door charges each request but never records its tokens — Door::record_usage/record_usage_unchained have no production caller; the production voice node's audio/text/cached tokens never reach the door cells, so token caps read 0 and budget caps see fees only (LEDGER fault on a production path). The budget crate is correct as written; the callers are the fix | none (no edit) | by reading at b091b4e83 (record_usage callers only in tests; LedgerCell::accrue reached only through them) |
| W2.15 | opus | 268, 269 | 269=FIXED by d10cf4147 (item 123: the budget gate prices every counted class through Tally; reserved_rate _=>0 gone from production); 268=BLOCKED (A2A half only — MCP half fixed by 6a32b3a67/1f93dbd71/6c618af89): a2a posts Queries at amount 0 with no keyed bytes unit (busbar-a2a receive.rs:721-739), so a budget: cap reaches A2A only through the flat fee — waits on BUSBAR-1.6.0.md §13 billed-byte sign-off (Q30c). No edits | `busbar-kernel/src/governance/tests/limits_tests.rs::an_open_class_priced_in_config_is_charged_and_capped_end_to_end` (+ refusal twin); `busbar-mcp upstream_join_tests` ×4 | 269 per d10cf4147 (@079a16efc → 101); 268 MCP per 1f93dbd71 (@6a32b3a67 → 101) |
| W2.14 | opus | 434, 437 | both FIXED (6a4306df6 + 222373d20 fmt: the posting-lookup = view certificate asked at every tier incl. the exact half — BROKEN DERIVATION at phase start, unified by 789d55a78; 151c4b685 nanos_sum doc stops claiming to be the only multiply-and-sum — every spend goes through Tally). No figure moves | `busbar-kernel-ledger/src/cost/tests/{rate_tests.rs::the_spend_fold_refuses_an_overflow_and_the_sizing_fold_pins_it, view_tests.rs::it_equals_the_posting_lookup_at_every_tier_including_an_exact_half}` | worktree @079a16efc → 101 ×2 (500 vs 499 nano; lookup billed Ok where the read refused) |
| P2-needles (owner ruling Q11) | sonnet | 376, 381 | both FIXED (6493bed3f): needle repairs re-pinned at their TRUE counts, owner-signed — ports-only busbar_core:: → busbar_kernel:: (llm/mcp/a2a/voice 0→265/288/252/91; tests 0→1045/204/99/45); legacy-reach 17→37 (busbar_core/busbar_substrate 0 struck; mcp 5, a2a 3, voice 12 added), declared under gate.ceiling_raises citing Q11/Q32; selftest plants repointed. Rule note: the agent verified a pre-existing red with `git stash` in its worktree | `cargo xtask selftest construction` exit 0, 0 IMPOSS (36 cases); posture 20/20 | selftest before the plant repoint FAILED (expected Red got Green) ×2 |
| P2-failsig (owner ruling Q31) | opus | 302, 303 | both FIXED (2f8c5d16b money alone: a buffered chat 2xx whose own stop reason is Error → ingress-native 502, breaker fault upstream-generation-failed, end Error, upstream 10/5 still ledgered; 25a133d98 money alone: a streamed Gemini MALFORMED_FUNCTION_CALL pushes IrStreamEvent::Error → error frame, breaker fault, tokens charged). Same-protocol non-stream passthrough left byte-for-byte (no fault) → owner (Q37) | `busbar-llm/src/engine/attempt/tests/failed_generation.rs` ×4 (2 + controls); gemini codec tests | worktree @6a6c22b51 → 101 (200 stop relayed; no error frame) |
| W2.3 | opus | 271, 292, 557, 558, 571 | 271=FIXED by c05a45207 (item 127: recovery wired); 292=FIXED (preflight identity-provider stand-in exemption reads the same gate); 557=FIXED (8b65bce4e: a one-shot's place is a permit that gives itself back on drop); 558=FIXED kernel half (c1a0a3a0e doc names the three deciders; admin second table cross-check → core-admin); 571=FIXED (6b3cfe321 bucket store keyed by stage — no index past the end). 271 residue: recovery posts VOIDED where RECOVERED is due (no dispatch/checkpoint journalled; figure 0→0) → Q9 durability rework; voids_claim has no production impl | `busbar/src/root/tests/durability.rs::a_hold_survives_a_kill_9`; `busbar-kernel/tests/preflight_stand_in_gate.rs` ×2; `tests/table_and_pump.rs::a_one_shot_that_ends_gives_its_place_back_and_one_past_the_concurrency_is_refused`; ratelimit + profile lib tests | worktree @079a16efc → 101 (292, 557, 558, 571 planted stage); 271 by reading |
| architect (count correction) | — | 302, 451, 376, 381 | BLOCKED → FIXED: 302 by 2f8c5d16b (P2-failsig, owner ruling Q31); 451 by 35400e7c6 (P2A-451); 376 + 381 by 6493bed3f (P2-needles, owner ruling Q11). Remaining BLOCKED, each with its path: 164 (config-schema decisions section — at the Phase 2 close bless, Q10a/Q37); 242, 243 (architect, after W2.4); 245/246 (W2.4, running); 268-A2A (P2-a2abytes, running, owner ruling §13); 460 (P2-voice, running, Q21b); 385, 390 (Phase 4 ABI); 404 (needs production callers); 331 (workflows resolve on main — non-blocking per KICKOFF §13.3); 197/234/235 (carried to their owning slots) | — | — |
| P2-mcpbounds (owner ruling Q22c/Q35) | sonnet | MCP sampling input bounds | FIXED (da5d98e94): configurable, named refusals under tools.<server>.sampling — max_messages 64, max_prompt_bytes 65536, max_stop_sequences 8, max_stop_sequence_bytes 64, temperature 0.0–2.0 (integer thousandths); boot refuses 0 / inverted ranges. Rule note: the agent used `git stash` in its worktree for the RED proof (stash list checked empty; BRIEF tightened) | `busbar-mcp/src/mcp/tests/sampling_bounds_tests.rs` (14), `config_tests.rs::the_sampling_block_round_trips_through_yaml_and_the_new_bounds_default_when_omitted` | implementation reverted in worktree → 101 (compile) |
| W2.4 | opus | 237–241, 248–253, 255, 256, 265 + carried (a),(c),(d),(e), 245, 246, billing-off flip | FIXED: 238 (ecf84908b), 241 (by 0fff8e2df), 248 (5153910b0 boot seal runs check_label_banks), 249 (921236ad1 no second unflushable credential cache), 251 (1b2f17c3e decision plane joins the boot seal), 252 (03e40921e), 253+265 (e20dacf70), 255+256 (b091b4e83), (c) 81e7e6aa6, (d) 3dcedca9f, (e) 7cb0470b6, billing-off flip 0d1e46698. BLOCKED: 237 (root RAM-only second admin audit ring), 239/246 (meter_policy has no production reader — not a money fault, measured), 240 (claims overlap only checkable in kernel build_dispatch), 250 (Cargo.toml patch), (a) decision unit path (plane_decision.rs dirty; /v1/models collides with the llm route); 245 → P2-voice | 12 exit tests in `busbar/src/root/tests/{transports,gauntlet_kernel,units_mcp,units_a2a,units_voice,policy,adapters,auth_bindings,registry}.rs` | worktree @079a16efc / HEAD-with-fix-reverted → 101 each; 241 by reading |
| P2-dated (owner ruling Q14) | opus + architect | 23/32 dated budget reads | FIXED (9370ce354 kernel read + gate path; architect landed the root wiring as its own money commit): every /usage read, the /metrics spend gauges and the budget gate price each era at the card in force — worked example (20→10 µ/token, 300k before + 900k after): 1,200¢ served → 1,500¢ = GET /admin/usage; the gate now REFUSES under a 1,300¢ cap. No card edit → no change. BLOCKED: durable era on the store wire (ModelTokens.priced_from_ms / billable_by_era — store ABI step; ~15 literal sites) → Phase 4 ABI; root history cards carry no open-class rates (RawRates lacks units — rate_apply.rs + appbuild.rs) → follow-up | `busbar-kernel/tests/dated_budget.rs::a_card_edit_mid_window_prices_every_budget_read_and_the_gate_at_the_card_in_force`; `busbar-kernel-ledger/src/usage/tests/dated_tests.rs` (7) | worktree @6a6c22b51 → 101 (1200 vs 1500) |
| W2.1 | opus + architect | 273, 284, 285, 293, 300, 552, 554, 555, 560, 561, 562, 564, 565 | FIXED: 552 (1fca27419 kernel seam + ac5fe1ac2 root attach — the first admin mutation stopped active health probing for the process life; restores 1.5.5), 554+555 (7a4f01f69 calllog refuses an append below an undecodable tail; one reporter), 560/561/564 (67e925768 router docs), 565, 284 (d76d3d7e2 money alone: a closing session tick carries owed interval + checkpoint, 0→60,000 ms; latent), 300 (6e218d54f doc), 562 (f6f914ad2 −52 dead lines); STRUCK: 273 (overdraft = settled − reserved since 0046867e7), 285 (usage class never reaches Posted), 293 (clip pinned by ARCHITECTURE.md:470, flagged not dropped). 284/300/552-root/562 built as patches, landed by the architect after a clean-worktree run (kernel lib 1893, recovery_and_ticks 21, busbar tests check) | `busbar-kernel/src/tests/{calllog_tests,router_doc_tests,state_tests,tick_tests}.rs` (9 named) | worktree @079a16efc → 101 (6 tests); 552/555/284 by reading |
| P2-a2abytes (owner ruling §13/Q35) | opus | 136 + 268 (A2A halves) | FIXED (72178d0ed money alone; 029ab8a4d spec §13 records the ruling): an A2A hop ledgers payload bytes relayed BOTH ways under `bytes` (161 + 219 = 380 per message/send), agents.rate_card prices them (card key agent:<id>), no card → 0, a silent card refuses (#42), a streamed relay counts every frame, a hop refused before the socket counts 0; budget caps now trip on A2A. Root units_a2a Meter step still reports response bytes only → P2-rootcleanup | `busbar-a2a/src/a2a/tests/billed_bytes_tests.rs` (5) | worktree @8b65bce4e → 101 (all 5; nothing ledgered) |
| P2-failsig (Q31 follow-up) | opus | same-protocol non-stream failed generation | FIXED (4b2eb118f money alone): a same-protocol non-stream relay whose own stop reason is Error records the breaker fault (upstream-generation-failed) and ends Error; body relayed byte-for-byte, 10/5 charged; bounded forward key scan, ≤64 bytes read, no parse/alloc. Gap: an UNGOVERNED relay keeps no body copy, so no fault is recorded there → architect ruling: scan the chunks as they pass (no copy) | `busbar-llm/src/engine/attempt/tests/failed_generation.rs::same_protocol_cohere_error_relays_verbatim_faults_the_breaker_and_charges_10_5` (+ control); codec raw_stop_reason tests | worktree @c1a0a3a0e → 101 (breaker fault at :204) |
| P2-oraclewording (owner ruling Q3) | sonnet | accepted-differences rationale wording | FIXED (7f4d76436): internal commit hashes, an issue id and a branch name reworded out of entries :61, :292, :415 — meaning, premise, cells unchanged; rationale is not signed (oracle load_register reads it as plain text); public-hygiene lint 0 hits; ./bin/oracle cells --check 0; self-diff 0 | hygiene lint + oracle checks | lint flagged the three lines before |
| P2-verifyrunner (owner ruling Q5) | sonnet | verify-deploy site checks | DONE (d33648d43): the getbusbar.com checks (g)(h)(i)(k)(l.1)(n) move into their own `verify-site` job on self-hosted `latchkey-small` (the repo's live self-hosted label); alert gates on it; workflow-invariants c332 reads both jobs; actionlint clean. NOTE: the mixed `verify` job was ALREADY on latchkey-small when Cloudflare answered 403 — so the label alone does not fix the block; the owner must let that runner's egress through Cloudflare (Q39) | `.github/workflows/lint/workflow-invariants.py --selftest`; `--only 332` → 0 | unpatched c332 on the split file → FAIL (11 < 14 check steps) |
| W2.2 | opus + architect | 272, 280, 281, 283, 289, 291, 299, 556, 559, 563, 566, 567, 569, 570 | FIXED: 291, 563, 569, 289, 299, 559 (docs + tests), 283 (78c55a0fe boot seal uses the contract's byte-exact ALPN rule, −15), 556 (146ff307e hook diagnostics logged), 570 (25383a517 OTLP "enabled" only when installed), 567 (de8732595 exited threads' banks released, counts kept), 272 (6338ab7fd canary counts balance 6/1/8 → 8/8/8 — built as a patch, landed by the architect hunk-only after loop_battery 23/23 in a clean worktree). BLOCKED: 281 (busbar_lane_state{pool} semantics → Q40), 566 (voice front door records no duration → P2-voice), 280 (in-flight reserve dead in production → architect: delete, after P2-rootcleanup). Kernel code −19 | 11 tests under `busbar-kernel/src/tests/` + `tests/loop_battery.rs::the_canary_balances_over_refusals` | worktree @079a16efc → 101 each |
| P2-coreadmin (owner rulings Q18/Q22, Q25b) | opus | limits guard, openapi revert, named 409 | FIXED: 8d7c15091 build_app_from_config returns an InstalledLimits guard — a failed admin persist rolls the process-wide limits back (pre-session edit landed per ruling; snapshots ~/Downloads/busbar-1.6.0-snapshots/P2-coreadmin/); pre-session openapi (b) hunks reverted in the tree; 12750f9d4 an unpriced class under a present card answers NAMED 409 unpriced_class (lane + class in the frozen {code,message}) on /keys/{id}/usage, /groups/{g}/usage, /admin/usage (was 500 internal). The stash@{0} was this agent's (popped; list empty). Found: openapi drift test RED at HEAD because committed code already generates the (b) hunks → Q41 | `busbar-core-admin/src/tests/tests.rs::{test_admin_v1_config_settings_persist_failure_does_not_install_limits, key_usage_/group_usage_/admin_usage_over_an_unpriced_class_answers_named_409}` | guard @079a16efc → 101; 409 @6a6c22b51 → 101 (500 internal ×3) |
| P2-billunits (owner ruling Q29/Q35) | opus | generic billable-class boot rule | FIXED (dbc02c8b9, money alone): PlaneDecl.billable_classes — every plane declares its classes (pools: input, output, cache_read, cache_write, search_units; tools: tool_calls, bytes; agents: bytes; decisions: decision; streams: audio_tokens_in/out, text_tokens_in/out, cached_tokens, audio_seconds_in, tool_calls); boot and --validate refuse a plane card that leaves any unconfigured, naming each: `pools.rate_card does not configure billable unit(s) search_units declared by this plane; add them (0 to make them free)`; identical for every plane; kernel LOC 51408→51408 | `busbar-kernel/src/config_validate/tests/tests.rs::{a_card_missing_a_declared_class_fails_validation_naming_every_missing_class, a_card_configuring_every_declared_class_passes_with_explicit_zero, a_non_fallback_planes_card_is_held_to_the_same_rule, a_dropped_in_planes_declared_classes_are_honoured}` | worktree @1fca27419 → 101 (3 failed) |
| P2-vertexfields (owner ruling Q1) | sonnet | Vertex vendor fields | FIXED (b08fb1698): usageMetadata.trafficType, top-level createTime and groundingChunks[].web.domain round-trip through the IR (via IrUsageDetail + IrCitation.raw, no cross-dialect construction breakage); codec 1735 green. The qa field inventory lists the PUBLISHED Google schema only, so carried vendor extras correctly get no coverage line (no regen). The conformance run stays red on a harness bug: run.sh §3b reconciles gaps-named SKIPs against the `accepted` section → P2-conformfix | `busbar-llm-codec/src/gemini/tests/field_carry_tests.rs::{gemini_vertex_traffic_type_survives, gemini_vertex_create_time_survives, gemini_vertex_grounding_chunk_domain_survives}` | worktree @62b910f9b → 101 (E0609) |
| P2-docs | sonnet | operator docs for the rate-card rulings | DONE (288f9fc07): docs/config-at-a-glance.md, docs/configuration.md, config.yaml (commented) — per-plane rate cards, open `units:` classes, the generic billable-class boot rule with its exact error and the 1.5.5 upgrade step (`units: { search_units: 0 }`), sub-quantum boot refusal, A2A `bytes` (agent:<id>), 409 unpriced_class, MCP sampling bounds (pointers to mcp.md); every sentence cross-checked against source | docs only | by reading |
| P2-failsig (ungoverned relay) | opus | Q31 follow-up | FIXED (d659d15c7): an UNGOVERNED same-protocol non-stream relay also records a failed generation's breaker fault via an incremental stop-key scan across chunks (32-byte carry + 64-byte token inline, no body copy); the governed path shares the scan; codec whole-body vs incremental scan held equal at every split. Architect follow-up 9f…: runtime_carry_tests.rs read App.client_settings, which f6f914ad2 deleted — moved to the plane runtime copy (test build restored) | `failed_generation.rs::ungoverned_same_protocol_cohere_error_split_key_faults_the_breaker` (+ control); gemini `stop_key_scanner_matches_the_whole_body_scan_at_every_split` | worktree @3c1000ff1 → 101 (breaker fault at :366) |
| P2-rootcleanup (architect rulings on W2.4 residue) | opus | 237, 239/246, (a), A2A bytes, qa rows | FIXED: 237 (11fac98ab one admin audit ring — the root RAM ring was write-only; served audit body byte-identical), 239/246 (62b910f9b unread meter_policy + pools_without_expansion deleted), A2A bytes (f6b282eb5 money alone: root A2A Meter counts request+response, 256→384; no production constructor), qa (5af7a24e5 four stale streaming noun rows struck). BLOCKED: (a) decision serving path — nothing builds a DecisionPlane from `decisions:` and no route leg reaches a provider; building it = a new serving path (providers from config, decision-egress credential, meter/settle the `decision` class, mount POST /v1/systemone only when `decisions:` is present) → Phase 4. Found: root-only 1.6.0 admin verbs (operator-key…) now leave NO audit row → P2-rootfollow | `root/tests/durability.rs::journalling_carries_no_admin_row_and_the_root_holds_no_second_admin_ring`; `units_admin/tests/admin_path_without_plane_face.rs::the_served_audit_body_is_byte_identical_with_and_without_the_root`; `root/tests/kernel.rs::the_units_carry_no_metering_policy_and_the_root_names_no_pool_check`; `root/tests/units_a2a.rs::the_metered_bytes_are_the_payload_relayed_both_ways` | worktree → 101 each (256 != 384 for A2A) |
| P2-conformfix | sonnet | llm-conformance run.sh reconciliation (Q1 follow-up) | FIXED (0e1b1e2ab): a SKIP reconciles against the section that produced it — gaps-produced SKIPs (exact ledger id in gaps[].cells) against `gaps`, never-judged rows against `accepted` by regex (unchanged); named-gaps.json untouched; an unnamed SKIP stays RED. gemini-vertex recording GREEN 3/3, selftest recordings 7/7 + 12/12, selftest.sh 26/26 | `testing/llm-conformance/selftest.sh` cases (i2) gaps-named → green, (j2) unnamed → red | pre-fix run.sh on the fixture → exit 1; selftest (i2) FAIL at 7c1bc8c7d |
| P2-rootfollow | opus | root-only admin audit, dated-card units, 280 | FIXED: 6fb12f40d every root-only mutating 1.6.0 admin verb (16) seals one durable row on the kernel audit ring (read verbs none; 1.5.5 verbs byte-identical); 2ae1f59cc money alone: every DATED rate card carries the open-class units rates (search_units 5→3 µ, 1M before + 2M after: refused → 11,000,000 µ); d89ae7c71 dead in-flight provider reserve deleted (kernel −20). cargo check --workspace --tests clean in a clean worktree | `units_admin/tests/admin_path_without_plane_face.rs::every_root_only_mutating_verb_seals_one_durable_row_and_a_read_seals_none`; `root/tests/kernel.rs::a_card_edit_mid_window_prices_an_open_class_in_both_eras_and_refuses_neither`; `busbar-kernel/tests/table_and_pump.rs::a_full_table_refuses_every_counted_unit_at_the_cap_and_holds_no_slot_back` | worktree @ffe89cf4e → 101 ×3 |
| P2-voice (owner rulings Q21b, Q21a; items 138, 460, 566) | opus + architect | voice billing switch | FIXED: f87fd3800 (money alone) voice meters on the streaming plane's units via the kernel session account, priced at read by streams.rate_card, the D2 lease + stored-nano arithmetic deleted — fixed script: lease stored 180,000,000 nano (view 18) → lane voice␟gpt-realtime audio_tokens_out 11, text_tokens_out 4, text_tokens_in 3, audio_seconds_in 1, tool_calls 1 (view 20, nothing stored); 460 budget caps now see voice counts; 2beb4fb96 Q21a ceiling optional plane config (absent = no cut; `session_expired` frame when configured); eb039a319 566 duration histogram. Architect landed 3 follow-up patches after a clean-worktree workspace check: ce27a8105 census strike, 9ddbf2076 SessionMeter shell deleted, 5b75e5b85 main.rs doc. Open: cached_tokens is a SUBSET of the input classes (double-bills if priced) → P2-voicefix; tokens: caps count reserved tiers only → after P2-fees | `root/tests/units_voice.rs::plane_units::*` (4), `busbar-voice/src/runtime/tests.rs` (6), `voice_plane_unit_oracle`, `plane-streaming codec class_counts…`, `units_voice…a_spent_budget_refuses_the_next_frame`, `config_tests…session_ceiling…` | worktree @151c4b685 → 101 (row {} vs Some(3); 3600 cut unconfigured; door cell empty) |
| P2-voicefix (architect ruling: disjoint billable classes) | sonnet | voice cached_tokens | FIXED (be3cbb9d9 money alone: cached_tokens dropped from the streaming plane's billable classes — a subset of input, kept only as unbilled attribution; streams class list 7 → 6; 5bc136695 docs: voice.md + configuration.md updated, stale 3600 session default removed). Found: config_validate does not refuse a `units:` key naming a class no plane declares (a typo prices nothing silently) → architect ruling: refuse it at boot, naming the plane's declared list | `busbar-voice/src/tests/mount_tests.rs::billable_classes_are_pairwise_disjoint_and_exclude_cached_tokens` (+ 4 updated plane/voice tests) | worktree @5b96b2f00 → 101 (6 failed) |
| P2-fees (owner ruling Q32 / #47) | opus | per-plane fees | FIXED: 24bb507d0 (money alone) each plane section's `fees: {per_request, per_session}` prices that plane's own billable units, counted per (bucket, plane, era); the flat `per_request_fee:` is the pools plane's alone. Figures: flat fee 5, 4 MCP calls on a plane with no fees 20 → 0; 2 LLM + 3 MCP at tools.fees.per_request 3: 25 → 19; 2 voice sessions at per_session 40 → 80. Kernel 51311 → 51318 (ceiling 51439). Found: GET /admin/usage still prices MCP metering rows at the flat fee (books disagree) — architect follow-up queued; session fee counted after the open-time dry check (≤1 session-fee overshoot). Queue Q44 (told). |
| P2-secretref (Q10a) | opus + architect | SecretRef bare `none` | The finding is real, not a false positive, and the widening predates this run (33adc9906): HEAD accepts exactly one scalar the v1.5.3 baseline refused — `none`, the explicit keyless reference, which resolves to no credential. Every other scalar is still refused, now without echoing the value, which is stricter than 1.5.5 on non-string scalars. The owner approved it under Q10a, so the architect committed the one exact-path waiver in config-schema.waivers; additive-only and waivers now PASS. Only snapshot-drift remains, which the owner-approved Phase 2 close bless clears. |
| P2-hygiene | sonnet | public-hygiene-lint new hits | FIXED: 5329cbc56 + c35c0e06d — 18 hits in 15 owned files → 0 (ruling ids, item numbers, § citations, commit hashes rewritten as plain invariants; comment/message text only, cargo check green). Hits in xtask/, scripts/, qa/, governance/, config_validate/ and core-admin are with the slots that own those files. Left: testing/llm-conformance/specs/gemini-v1beta-20260923.json:2490 'Raw PCM (L16) audio format.', verbatim vendored Google discovery text that trips audit-finding-id; the architect handles it. |
| P2-citests | opus | CI test reds at 71ecd5bb2 | 2 FIXED: 67f66ec21 the core-admin /config/settings cards configure search_units (went red at dbc02c8b9, the generic billable-class rule) and the settings tests hold the limits lock; 3 FIXED: 7391bf3ed the test-idp stand-in asserts a role idp_chain binds (went red at 164bde9ac, item 144; production auth unchanged). 4 STRUCK: calllog_dispatch ×4 were environmental (store-example cdylib not built), green once it is built. 1 BLOCKED → architect: after item 144 a role-bound principal's subscriptions/listen is refused identity_not_live on its first frame, because Standing::still_permitted re-resolves through GovState::lookup_by_sub, which never finds a key built from role_bindings; patch P2-citests-item1.patch; governance/state.rs is held by P2-tokcaps. 5: the posture test holds its declared standing reds; NEW content red structure-lint:oversized — a2a relay.rs 2518 > 2500 since 72178d0ed (A2A bytes) → architect slot P2-relayshrink. |
| P2-relayshrink | sonnet | structure-lint:oversized | FIXED: 6c9503f96 relay.rs HopBytes billed-bytes accounting moves to sibling relay_billing.rs (declared from relay.rs; mod.rs untouched), 2518 → 2464 lines, oversized PASS; busbar-a2a 617/617, clippy clean. |
| P2-rolebind | opus | role-bound standing re-check | DECIDE (new 1.6.0 surface, not a regression): 1.5.5 had no MCP plane, no subscriptions/listen and no standing re-check, so it never served a role-bound principal's long-lived subscription. At HEAD a role_bindings key is refused identity_not_live on the first frame (Standing re-resolves via lookup_by_sub; synthesize_principal_key keys are never in the registry). The defect predates item 144, which only removed the roleless test hiding it. Recommended fix: Standing records Registry(id) vs Bound{module, principal, roles} and re-runs synthesize_principal_key against the live role_bindings (removed binding ends the session). Queued to the owner. |
| P2-landpatches | opus | rerank part 2, 243 | rerank2 FIXED: d7bcbaf2b (money alone, LEDGER) same-protocol Cohere/Bedrock rerank reports search units — 50 units at 2000 µ¢ 0 → 10¢; RED proven per protocol; cargo check --workspace --tests clean; Q45 told. 243 BLOCKED → architect: the patch needs qa/capability-equality.json decision column + root leg and 15 argued rows in qa/plane-hook-isomorphism.allow (architect-owned); plane_decision.rs currently held by P2-tokcaps. Gaps noted: Bedrock same-protocol unreadable search_units still bills nothing; over-cap Cohere body bills token floor only. |
| P2-558dead | sonnet | 558 admin half, meter_series_billed | FIXED: 5d96db052 cross-check test — core-admin's CONFIG_CLASS_RULES agrees with the kernel path classifier for every legacy mutating verb (RED on a planted divergence; collapsing would move /tools and /agents rate-limit tiers, which is item 372, W4.7); deb6b43af dead plane_host::meter_series_billed + its test deleted (0 production callers; replacing behaviour proven by response_body nonstream_drop_billing_tests). Kernel code 51342 → 51329. |
| P2-tokcaps | opus | token-family caps, undeclared units: key | FIXED: 0fb654405 (money alone) each declared billable class names its unit family (BillableClass{class, family}); a tokens: cap counts every token-family class a plane declares (60 audio_tokens_in + 40 out under a 100/min cap: read 0 admitted → read 100 refused, kernel governance door); 295699e1f a rate card units: key naming an undeclared class refuses boot and --validate naming the key and the declared list (Q42's search_units: 0 still passes); 206bd52e2 governance comment hygiene. money_one_function_equivalence 14/14, money-invariants green, kernel 51344. Carried: the voice door (busbar-kernel-budget cells.rs total_tokens, root units_voice.rs) still sums the reserved four → P2-voicecaps. Q46 told. |
| P2-voicecaps | opus | voice door tokens: cap | FIXED: fec259799 (money alone, BROKEN DERIVATION) the voice door's tokens: cap counts every token-family class an installed plane declares — 60 audio_tokens_in + 40 out under 100/min: next frame admitted → refused (Admit, RateLimited); audio seconds and tool calls never count; Door::new keeps the reserved four for other doors. No invoice or /usage figure moves (node-local cells). Q46 covers it. |
| P2-243 | opus | 243, 257 decision isomorphism column | BLOCKED → Phase 4: the decision plane has no root leg (no units_decision.rs, no impl Units, plane_decision.rs declares identity only; qa/reachability.toml already carries reachability:unit-path:decision as a standing red), so the capability-equality column cannot be answered honestly. Of the 15 asymmetries, 11 are argued (draft P2-243-decision-allow-rows.DRAFT.json) and 4 are defects: routes/openapi/build_runtime = the missing serving path (Phase 4), config_validate = decisions.models.<m>.provider and decisions.hooks cross-refs are never checked and a non-jev dialect still boots (#51) → architect slot P2-decvalidate now. |
| P2-ciselftest | opus | 8 gate self-tests | FIXED: teller-steps bffafb7ca (plants from a green base), response-header d1f137219 + structure-lint cb8f27d1d (lex/scan once — back under their work budgets), money-invariants e5cc36a10 (journal-writer floor 4 → 5, stricter), design-bindings 266b82e9f, config-schema 684cdb9a9 + c0cb6de27 (red proofs from the fresh render; verdict wording), hygiene e5b5cc974. construction STRUCK (a detached HEAD without XTASK_CEILING_BASE; passes on a branch). 0 IMPOSS everywhere in a clean worktree. Left as real tree reds → P2-cifollow: teller-steps matrix.admin.audit.root cites a test 11fac98ab deleted; design-bindings PB-44/PB-87 cite tests d89ae7c71/84f2861bd deleted; kind-isolation build-inputs include_str!(busbar/Cargo.toml) at drain_facade_tests.rs:57 (28aae1252); config-schema's two control cases clear at the snapshot bless. |
| P2-walq (owner ruling Q38) | opus | wal-corruption-verdict | FIXED: 344dad75c the WAL recovery branches on the verdict: a torn tail is cut silently; mid-log corruption boots, the damaged remainder is copied and fsynced to <segment>.wal.quarantine-<unix_ms> BEFORE the cut, the verdict is reported, record numbers in the quarantined bytes are never reissued; if the copy cannot be written the segment is sealed and writes roll to a new one (boot never blocked). docs/operations.md gains a 'Journal corruption at boot' remediation section. 8 exit tests, RED at HEAD. Root wiring (ERROR boot line, busbar_journal_quarantined_total, durable audit record, KNOWN_UNSHIPPED strike) is in P2-walq-root.patch → architect, after P2-q9 and P2-281. |
| P2-281 (owner ruling Q40) | opus | 281 lane-state gauge | FIXED: 0abf8865c busbar_lane_state reads the pool's own breaker cell (2 tripped, 1 only for a half-open probe on that cell, 0 healthy) and is emitted for every plane's destinations through the plane-neutral breaker cells (tool:/agent: keyed series). 4 exit tests on rendered /metrics, RED at HEAD. Architect ratifies the read-only PlaneBreakers::cell_readings accessor in store/planes.rs. Oracle: about 35 1.5.5 golden cells record lane_state=1 (11 route.failover + about 24 llm upstream_down / retry-after-floor) and will read 2. Those are owner-ruled (Q40) accepted differences for Phase 3. |
| P2-rolebindfix (owner ruling 2026-09-24) | opus | role-bound subscriptions | FIXED: ee14fbbae a role-bound subscription is re-checked through the LIVE role_bindings (Standing reads the admission recorded on the synthesized key; principal_standing re-reads handle.load() so a config apply takes effect): served; a changed pool/group applies from the next frame; a removed or emptied binding ends it identity_not_live; registry-key standing unchanged; a forged marker on a vk_ key still fails closed. 5 exit tests, RED at HEAD; busbar-kernel lib 1912 green, mcp_stdio_serve 6/6. docs/mcp.md updated. |
| architect (count correction) | — | citests item 1, rolebind | BLOCKED → FIXED: the role-bound subscription defect (P2-citests item 1 and P2-rolebind's DECIDE) is fixed by ee14fbbae under the owner ruling. Open BLOCKED now, each with its path: 164 (Phase 2 close bless); 242 (P2-q9 running); 243/257 (Phase 4, decision serving path); 385, 390 (Phase 4 ABI); 145 (Phase 4 egress, pinned authority); 404 (P2-404 running); 197/234/235 (P2-xtaskcarry running); 317 (on item 181); 331 (non-blocking, KICKOFF §13.3). |
| P2-xtaskcarry | sonnet | 197, 234, 235 | Already FIXED in earlier phases; the blocked list was stale: 197 cff287a46, 234 e73fd1eac, 235 7f75396c9. Re-verified this session: instance-noun-neutrality selftest 36/36, sweep-coverage 7/7, unit tests green, clippy/fmt clean. BLOCKED → FIXED count correction. |
| P2-q9 (owner ruling Q9) | opus | Q9, 242, 271 residue, LaneRates | FIXED (each money commit alone): 34d049ba8 a durable posting and hold record carry the unit's raw counts and card epoch, never money; the rebuilt book prices them through the one spend function (old money-shaped rows still load; figures unchanged where the card did not change, 4,200→4,200); f7ebaff00 242 the billable-request count settles onto both books, so the node reconciliation compares real fee counts (served /admin/ledger/totals fee_count 0 → one per delivered response); 980932a56 271 dispatch, accrual checkpoints and idempotency claims are journalled, so a unit killed after dispatch recovers RECOVERED at its checkpoint and a claim with no hold is voided; 18edc5f56 LaneRates::nanos deleted (snapshot in ~/Downloads). money_one_function_equivalence 14/14, money-invariants green. FOUND (RATECARD fault) → P2-cardhistory: config-applied card entries are not journalled, so after a restart the dated history starts at the boot card and pre-restart units reprice at it. Carried: the idempotency cache in core-admin must call Durability::journal_claim (after P2-usagebooks). |
| P2-cifollow | opus | stale citations, build-inputs | FIXED: 8bbe9dcc4 teller-steps admin.audit.root cites a new root-leg test (one admin unit seals exactly one entry on the one ring, a read seals none; RED with the seal disabled); 34e1ba956 design-bindings PB-44/PB-87 cite the tests proving what the owner ruled (reserve deleted; rerank bills search_units) with the claim text updated in ARCHITECTURE.md Appendix B; e0ad610a5 kind-isolation build-inputs: the drain-facade prose test moves to the binary that owns the manifest; 4cda5a976 schema.rs:732 wording. selftests teller-steps 15/15, design-bindings 21/21, kind-isolation 167/167, 0 IMPOSS. schema.rs:44 wording is rendered into the frozen config-schema snapshot, so it goes in the owner-approved Phase 2 close bless (Q10a). Still RED: design-bindings:note-witness PB-58 cites a test that never existed (overdraft carried out) → P2-pb58; ARCHITECTURE.md :1098/:1177 still describe the deleted reserve → P2-pb58. |
| architect (count correction) | — | 421 | PARKED → STRUCK: docs/design/1.6.0-PARKED/421.md is resolved (owner ruling Q12/Q25c, row struck). The PARKED directory holds no open item. |
| P2-walqland (owner ruling Q38) | sonnet | WAL root wiring | FIXED: 8284a4c33 a corrupt journal segment quarantines and boots, loud: ERROR boot line, busbar_journal_quarantined_total, a durable ChainBreak record naming file/offset/bytes/quarantine path; KNOWN_UNSHIPPED wal-corruption-verdict struck. unconstructed gate green (17 rows), root durability 26/26, metrics_cross_plane 12/12, cargo check --workspace --tests clean; docs/observability.md metric row added. |
| P2-pb58 | opus | PB-58 witness, reserve prose | FIXED: 55c7f509d + a7e44f7aa PB-58's overdraft witness now exists — a unit reserving 1,000 and spending 1,500 settles and the rebuilt book derives overdraft_carried_out 500 at read (RED with the carry forced to 0); design-bindings gate 106 rows green, selftest 21/21 0 IMPOSS; 2ee2ea89a ARCHITECTURE.md reserve prose (two passages) rewritten to the one-cap behaviour. The architect fixes the third passage (§ ARRIVAL) and the stale selftest comment. |
| P2-decvalidate | sonnet | decisions config cross-refs (#51) | FIXED: e59b30de8 plane-owned validate_cross_refs + 75d992592 wired through the plane-agnostic seam: PlaneCfg gains default-empty model_provider_refs/known_dialects, called from the one config::resolve that boot and --validate share (no main.rs/cli.rs edits; PlaneDecl.config_validate only fires on named-def admin writes, which decisions: does not have). Boot and --validate now refuse a decisions model naming an undefined provider, a hook naming an undefined hook, or a provider on a dialect the plane does not speak; the good config boots. Provider Rule 4's known-protocol set unions every registered plane's known_dialects (it only grows; no existing config changes). 9 exit tests, RED at HEAD. |
| P2-usagebooks (owner ruling Q32 / #47) | opus | /admin/usage plane fees | FIXED: 3799a0621 (money alone, BROKEN DERIVATION) GET /admin/usage prices a plane's metering row at that plane's own fee through the budget book's fee lane and the one function: flat fee 5 + tools.fees.per_request 3, 2 LLM + 3 MCP calls 25 → 19 (= budget book); 4 calls on a plane with no fees 20 → 0; pools-only unchanged. core-admin 410/410. Gaps carried → P2-usagegaps: /admin/usage cannot show per_session fees (metering rows carry no session count) and does not price the ledgered tool_calls class. Q44 covers the customer-visible move. |
| P2-cardhistory (#79, owner ruling Q14) | opus | card history across restart | FIXED (each money commit alone, RATECARD fault): 38ed759f2 every config-applied card (every plane) is journalled with its effective_from and boot rebuilds the dated history from the chain before the book: card A 3 / B 5 µ, 1M tokens per era, restart at B — book 10,000,000,000 → 8,000,000,000 nano (= pre-restart figure), plane lane 20e9 → 16e9, /admin/usage A-era row 5,000,000 → 3,000,000; a restart without a price change moves nothing; 2c3bdcf61 signed back-dated corrections survive a restart (3e9 → 4e9 inside the window). busbar bin 482/482, money gates green. Carried → P2-cardfollow: rates_applied fires before the config commit (a failed apply still enters history); units_llm bind_book does not bind the card journal; amendment records save an unpriced cell as 0; migration-1.6 §7 stale. durability.rs is over the 2500-line cap (2661) → P2-durasplit. Q48 told. |
| P2-404 (item 404, owner ruling Q9) | opus | amendment journal | FIXED: 3bd8e805b (money alone) the amendment journal is constructed: a hook or export handed content leaves exactly one access amendment (shape-only gates leave none); a root correction amends a unit's COUNTS on the same chain (never money), refuses a count below zero, a correction without a reason, and a correction below root scope; the money view follows the corrected count (1,000 input units at 2.5 µ = 2,500,000 nano → 800 = 2,000,000). 10 exit tests, RED at HEAD. Architect rulings: adjust verb takes counts (new 1.6.0 surface, Q9); the test digest constant change is accepted under §9.2. Carried → P2-rootwire: admin adjust effect in units_admin (patch P2-404-admin.patch, needs a book lookup), /usage read applying counts_now, amendments made durable in the journal, the llm plane's hook sites, and the digest script moving into the repo. Kernel LOC 51543 → 51597 (ceiling 51439, already over) → P2-construct. |
| P2-claimwriter (item 271 writer) | sonnet | idempotency claims | FIXED (seam): 93baea437 the core-admin idempotency cache gains a ClaimJournal seam, so a claim it takes is journalled exactly once (in-flight refusal journals nothing; no journal bound = exactly as before); 4 exit tests. Production wiring (Verbs::new threads the journal; RootClaimJournal adapter in units_admin; durability) is in P2-claimwriter.patch → P2-rootwire. An admin claim uses a reserved synthetic hold key, so it is always voided at the next boot — the same as today, where the in-process cache is lost on restart. |
| P2-durasplit | sonnet | structure-lint oversized durability.rs | FIXED: 847c22f98 durability.rs → durability/mod.rs (2311 lines) + durability/replay.rs (376 lines, the book-rebuild replay), public paths re-exported unchanged; oversized PASS for it, root durability tests 27/27 unchanged, cargo check --workspace --tests + clippy clean. Remaining structure-lint reds are the standing four (axis:purity, census:count, plane-dup ×2). |
| P2-cardfollow | opus | card apply follow-ups | FIXED (money commits alone): 7759ea6f7 the rate-apply seam fires at the config commit beside limits.keep, so a failed persist leaves history and journal unchanged (rejected card B=5 after A=3, 1M tokens: /admin/usage 5,000,000 → 3,000,000 µ; book 5e9 → 3e9 nano); f4773e220 a root-llm build without root-admin journals applied cards too (book after restart 10e9 → 8e9; /usage A-era row 5,000,000 → 3,000,000 — the commit message wrongly says /admin/usage is not served there); 1918456fe a sealed amendment's durable record marks refused cells, so a restart refuses them (ClassUnpriced/LaneUnpriced) instead of pricing 0; 067b12eec migration-1.6 §7 rewritten (dated /usage pricing, per-plane cards and fees, restart-safe history). busbar-kernel 1917, busbar bin 484, money gates green. Q48 covers the customer-visible moves. |
| P2-usagegaps | opus | /admin/usage session fees + unlisted classes | FIXED: bda0fe068 (money alone, BROKEN DERIVATION) the same accrual that fills the budget book now writes every class it holds (a plane's session count, a plane's declared classes, a pools open class) onto the metering row, and /admin/usage prices them through the one function: 2 sessions at per_session 40: 0 → 80; 3 tool calls (tool_calls 7 + fee 3): 9 → 30; 2 reranks of 1M search units at 3 µ: 10 → 610; mixed 216 → 570 (each = budget book); pools-only tokens unchanged. A present plane card silent on a class now refuses 409 unpriced_class (#42). Carried → P2-voicerows: voice turn rows are written with the upstream provider as provider, so /admin/usage charges them the flat per_request fee while the budget book charges none. Also found: money-invariants (2 rows) and no-float-money scan-floor are RED at HEAD because the durability split and kernel-audit moves changed paths → P2-moneygates. Q49 told. |
| P2-construct | opus | construction gate ceiling breaches | FIXED: f4a477595 ports-only ×7 rows (the four planes reach the kernel through their existing ports); 1a337640c no-escaped-newline-doc-comment; 4c6f7d803 unit-no-wall-clock (the WAL takes its clock from the composition root); 4a4c1ce52 ceiling-rose (the 10 declared raises came from 6493bed3f in this run, owner-signed 376/381, expired and struck; no number moved), no-test-doubles (the durability split), lean-core 34 → 13; 64ba7eb88 ceiling-slack re-pinned DOWN. Gate posture after: only loc-ceilings:kernel NEW. STANDING and worse: loc-ceilings:union; token-sealed 266 → 278 and kernel-seal 136 → 146 (P2 money exit tests mint tokens in root tests). BLOCKED → Q50: kernel LOC 51673 vs 51439, grown by P2 money/security items (ee14fbbae, 75d992592, 3bd8e805b, 0abf8865c, 12750f9d4, 24bb507d0, 0fb654405); every behaviour-neutral drain is a move into a busbar-kernel-* crate, which raises that crate's cells instead. Table: P2-construct-table.md. |
| P2-moneygates | sonnet | money gates after the durability split | FIXED: 2692a5e4e money-invariants JOURNAL_SEAM → durability/mod.rs (every journal writer lives there; replay.rs holds none); no-float-money BINARY_MONEY_FILES names both mod.rs and replay.rs. The journal-writer floor was armed at 5 while the seam always wrote 7 (UnitMark and ClaimRecord uncounted): re-armed at 7, measured on both sides of the move; stricter, not weaker. Both gates green, both selftests 0 IMPOSS. |
| P2-rootwire-b (item 404, 271; owner ruling Q9) | opus | admin adjust + claim journal | FIXED: eca30dfc6 (money alone) POST /api/v1/admin/adjust (it used to fall through to a 404-less router) seals a COUNT correction {seq, hash, amends, lane, card_epoch_ms, now}, never amount_nanos; negative or blank reason 400, read-only 403, unknown digest 404; the money view over counts_now moves 1,000 → 800 input units at 2.5 µ: 2,500,000 → 2,000,000 nano; a8a85babc RootClaimJournal + AdminBinding.claims journal an Idempotency-Key claim exactly once. Patches → P2-landwire: P2-rootwire-b-v1.patch (the served /admin/usage reads counts_now, money alone) and P2-rootwire-b-kernel.patch (bind the claim journal on a data-dir node). FOUND: no production admin request reaches the idempotency caches (POST /keys and rotate go straight to dispatch; Verbs is built per unit) → P2-landwire checks this against 1.5.5. |
| P2-landwire | opus | land rootwire-b patches; idempotency check | FIXED: 4b6a113a9 (money alone, BROKEN DERIVATION) GET /admin/usage prices counts as corrected by sealed adjusts: 1,000 tokens / 2,500 µ → 800 / 2,000; 406e140c5 the admin listener binds the claim journal on a data-dir node only. STRUCK as regression: POST /keys and rotate with a repeated Idempotency-Key return the first response byte for byte at v1.5.5 AND at HEAD, via the live App-state cache that core-admin keys.rs keeps; the Verbs caches and the claim journal are wired but inert → P2-claimhome moves the journal onto the live cache and removes the inert duplicate. OPEN → Q51: after an adjust, /keys and /groups usage and /metrics (kernel budget-book reads) still price uncorrected counts; the fix is in busbar-kernel, which is over its LOC ceiling (Q50). |
| P2-voicerows (owner ruling Q32 / #47) | opus | voice turn rows | FIXED: 1b42f89bc (money alone, BROKEN DERIVATION) a voice turn writes no series row keyed by the upstream provider; the streams plane's metering row is the plane-keyed one the kernel accrual writes, so /admin/usage stops charging the pools flat fee per turn (flat fee 5, 3 turns: 15 → 0; per_session 40: 55 → 40; with 2 LLM calls: 25 → 10 — each = budget book). by_model loses the (<model>, <upstream dialect>) rows; the counts now show on the (<model>, voice) rows. FOUND → architect ruling: streams.fees.per_request never charges (voice never calls per-request admission; a turn pays no fee by design) — a configured fee that silently does nothing. |
