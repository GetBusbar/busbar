# 1.6.0 — Branch corpus sweep ledger

Half-here-half-there: code that exists on a branch and **not** on trunk.
Trunk pinned at `d2548391bc3a76b4a01c3dfa4c486efebe93a172` (`consolidated/1.6.0`).

Six agents append to this file. **Append only. Never rewrite another agent's rows.**

Method (settled): per branch, versus its merge-base with trunk, extract the symbols the
branch ADDS (identifiers, test names, string literals) and search trunk for each — **with
`docs/` and every `*.md` EXCLUDED**, because an audit document describing a gap is prose
about missing code, not the code. `patch-id` is useless (the rename waves moved every path)
and ancestry is useless (3 of 819 branches are ancestors of HEAD). Symbols survive renames;
paths do not. Dedup by `(path, blob)`.

Buckets: **EMPTY** (no source content trunk lacks) · **HARVESTED** (every unique symbol on
trunk, possibly at a renamed path) · **SURVIVOR** (one or more symbols absent from trunk).

Verdict grades used on SURVIVOR rows:
`GAP` real missing work · `SUPERSEDED` trunk implements the same behaviour under another
design/name · `TEST-ONLY` only test names/assertions absent · `PARK` money path, needs a
named owner before anything moves.

---

## Slice: `origin/verify/*` (172 branches) — agent `vk`

`verify/keep-*` names are a **past assertion, not evidence**; every branch in this slice was
treated as unswept. Trunk-side search excludes `docs/` and `*.md` throughout.

### Money path — read at BEHAVIOUR depth, not symbol depth (PARK)

| branch | bucket | symbols-in | symbols-found | absent symbols | verdict |
|---|---|---|---|---|---|
| `origin/verify/keep-fee-unpriced-currency` | SURVIVOR | 47 | 36 | `RateCard::fee_for` · `RateCard::fee_unpriced` · `Unpriceable::FeeUnpriced` · `Divergence::FeeUnpriced` · `Priced.fee_unpriced` (branch: `crates/busbar-unit-cost/src/{rate,posting,project}.rs`, `crates/busbar-unit-ledger/src/recompute.rs`) | **PARK — money.** Branch turns `RateCard::per_request_fee -> i64` into `fee_for -> Option<i64>` so a card that names a currency for its RATES but no FLAT FEE in it fails closed instead of billing 0. Trunk (`crates/busbar-kernel-ledger/src/cost/rate.rs:374`) still has `self.fees.get(&currency).copied().unwrap_or(0)`, and `fee_unit_price_nanos` (`rate.rs:383`) flattens it to `0u128` — a multi-currency card silently bills every flat fee at nothing and reports nothing. Trunk HAS the lane half (`lane_unpriced`, `Unpriceable::LaneUnpriced`, `price_fail_closed`) and is missing only the fee half. Root cause: `unwrap_or(0)` on an absent map key on a billed line. Recommendation: owner decision — port `fee_for -> Option` + `Unpriceable::FeeUnpriced` + `Divergence::FeeUnpriced`, or declare the silent zero intended and say so on the line. Branch also DELETES `derive_spend_cents`/`derive_spend_micros` (USD-pinned 1.5.5 shims) which trunk still carries (17/15 files). |
| `origin/verify/keep-tariff-7` (tip of `tariff-2 ⊂ 3c ⊂ 4 ⊂ 5 ⊂ 5b ⊂ 7`) | SURVIVOR | — | — | `Rounding` (`Bankers`/`Up`/`Down` + `Rounding::divide`) · `PerUnitTerm` · `FlatTerm` · `TariffTerms` · `tier_price` · `metered_dimensions` (branch: `crates/busbar-contract/src/tariff.rs` 332 ln, `crates/busbar-core/src/config/tariff.rs`, `crates/busbar-unit-cost/src/schedule.rs`) | **PARK — money.** The whole tariff/schedule grammar is absent from trunk: `git grep -F tariff` over trunk code (docs excluded) returns **0 files**; 6 doc files mention it. Branch adds declared banker's-rounding (`half to even`, the only unbiased rule) for `amount per N units`, per-plane declared metered dimensions with a **boot refusal** for an amount no unit would pay, and the tier price resolved at one root site and threaded to every leg's journal row. Trunk has `pool_scope` (12 files) so the pool/scope half LANDED; the amounts half did not. Note a latent branch-side defect if ported: `Rounding::Up => whole + 1` is "away from zero" only for non-negative numerators. Recommendation: owner decision on whether 1.6.0 ships priced tariffs at all — this is a feature, not a repair. |
| `origin/verify/keep-ledger-tier` | SURVIVOR (behaviour HARVESTED) | — | — | test names only: `the_reserved_fold_saturates_rather_than_wrapping_at_the_top_of_the_range` · `one_reserved_key_at_the_maximum_is_exact_and_does_not_saturate` · `ordinary_reserved_counts_still_sum_exactly` · `maximal_rate` | **TEST-ONLY — money behaviour already on trunk.** Branch commit `a90ce856d` replaces `acc + (n as u128) * (rate as u128)` with `saturating_mul`/`saturating_add` in `RateNanos::reserved_nanos`. Trunk HAS this: `crates/busbar-kernel-ledger/src/cost/rate.rs:74-83` `nanos_sum` folds with `acc.saturating_add(u128::from(q).saturating_mul(u128::from(r)))`, and both `busbar-kernel/src/cost.rs:199` and `busbar-kernel-budget/src/price.rs:112` delegate to it. Trunk carries its own cells (`cost_tests.rs:795,836,1032`). Nothing to port on the arithmetic; the branch's three cell NAMES are absent but the property they assert is covered. |


---

## Slice: `origin/delete/*`, rows `NR%3==1` (170 branches) — agent `da`

Derived deterministically:
`for-each-ref refs/remotes/origin | grep '^origin/delete/' | sort | awk 'NR%3==1'` → 170 branches.
Trunk pinned at `d2548391bc3a76b4a01c3dfa4c486efebe93a172`; re-checked at the end of the sweep.

**Instrument.** Trunk was indexed once into three disjoint tiers — `code`, `docs` (`docs/**` +
every `*.md`) and `qa` (`qa/**`, goldens, waivers, shadow-oracle). "Present on trunk" means
**`code ∪ qa`; `docs` is NEVER counted.** Per branch, the diff against its merge-base yields
definition-site symbols and string literals on ADDED lines; a symbol counts as *branch-authored*
only if it is absent from the merge-base tree (45 distinct merge-bases, each indexed) and is not
also on a removed line — without that subtraction, rename-driven file moves read as thousands of
false additions.

**Bucket distribution (170):** SURVIVOR 142 · EMPTY 17 · HARVESTED 11.
Refined verdicts: BULK-SUPERSEDED 94 · REVIEW 39 · TEST-ONLY 7 · AUDIT-BOOKKEEPING 2.

**Docs-exclusion proof — it changed answers in this slice, repeatedly.**
6,503 identifier tokens exist on trunk in `docs/` but in no code file. Across the slice,
**2,145 symbol/string occurrences on 119 of 170 branches** would have read as "present on trunk"
had `docs/` been searched. Named instances, each verified by hand with
`git grep -w <sym> <TRUNK> -- ':(exclude)docs/'` returning 0 files while the `docs/`-only search
returns ≥1: `CredentialSignature`, `CredentialArrival`, `CredentialPresentation`, `SigningParams`
(whole design, `docs/design/1.6.0-credential-signature-declared.md`, **zero code**);
`RequestHeaders`; `WIRE_HTTP`/`WIRE_STDIO`; `refusal_answer`; `credential_refusal`;
`group_bucket_windows`; `EXPECTED_UNWIRED`; `WiringArm`; `lane_in_pool`; `HostShaped`;
`webpki_client_config`; `KeyRowsRefused`; `CatalogueEntry`; `KeyScope`; `BootFold`.

**Zero control.** Every "absent" verdict was controlled against symbols known present on trunk
using the identical query shape. Index controls: 12 positive (`AccrualMeter`, `AdminAuthChain`,
`interpolate_env`, `reject_yaml_unsafe_value`, `AdminMountSeam`, `splice_occurrences`, …) all
True; 3 negative all False. Shell controls: the same `git grep -c -w … | wc -l` loop that
returned 0 for the survivors returned 21 for `AccrualMeter`, 6 for `AdminAuthChain`, 4 for
`OPENING_CHECKPOINT_SEQ`. Two false-zero traps were hit and fixed during the sweep: zsh parsed
`"$T:crates/…"` as a `:c` history modifier (fixed to `${T}:`), and an early run tested
branch-side `qa/` manifest content against a **code-only** trunk index, manufacturing false
survivors (3 of 4 sampled auditor ids were in fact on trunk in `qa/audit-ledger.json`) — fixed by
making presence `code ∪ qa`, then re-running all 170.

### Money path — read at BEHAVIOUR depth (PARK)

| branch | bucket | symbols-in | symbols-found | absent symbols | verdict |
|---|---|---|---|---|---|
| `origin/delete/keep-meter-gap` | SURVIVOR | 78 | 42 | `UNITS_IMPL_SIGNATURE` · `units_impl_meter_step` · `has_units_impl` · `EXPECTED_UNWIRED` · `WiringArm` · `wiring_arm` · `selftest_a_free_fn_meter_is_not_the_loops_meter_step` · `selftest_expected_unwired_names_the_gap_and_only_the_gap` · `lane_in_pool` · `HostShaped` (branch: `crates/busbar/tests/plane_meter_seam_reachability.rs`, `crates/busbar/src/root/units_mcp.rs`) | **PARK — money.** The billing-reachability gate on trunk locates a plane's Teller **Meter step** as `common::item_body(&lines, "fn meter(")` over the WHOLE leg file (`crates/busbar/tests/plane_meter_seam_reachability.rs:220`). A **free** `fn meter(...)` that is on no `impl … Units for …` block — therefore callable by nobody, since the Teller loop holds the leg and calls the method on it — satisfies that gate while the leg contributes **no Meter step at all**, i.e. nothing it serves over the root is ever priced. The branch replaces the file-wide search with `units_impl_meter_step()`: scope to the `impl … Units for …` block, then require a `&self`/`&mut self` receiver; plus `EXPECTED_UNWIRED` to name known-unwired legs and two selftests that prove the gate would go red on a free `fn meter`. Root cause: the gate asks the FILE a question it must ask the TRAIT METHOD. Recommendation: owner decision — forward-port `units_impl_meter_step` + the two selftests (the branch predates the `busbar_unit_usage::meter(` → `busbar_kernel_ledger::usage::meter(` rename, so this is a port, not a cherry-pick). **Same hardening independently present on `origin/delete/keep-root-mcp-fix` (513 ln vs trunk's 387) — two branches carry it, trunk carries neither.** |
| `origin/delete/keep-root-mcp-fix` | SURVIVOR | 63 | 42 | `UNITS_IMPL_SIGNATURE` · `units_impl_meter_step` (branch: `crates/busbar/tests/plane_meter_seam_reachability.rs`) | **PARK — money (duplicate of the row above).** Second, independent copy of the Meter-step-location hardening. Its own prose names the defect: *"THE THIRD FAILURE. The step used to be located by the first `fn meter(` ANYWHERE in the leg file, so a FREE function nothing calls satisfied a gate asking whether the loop's Meter step meters."* Corroborates the finding; port once, from whichever branch is the fuller text (`keep-meter-gap`). |
| `origin/delete/keep-m9-migration` | SURVIVOR | 121 | 119 | `OPENING_HISTORY_SEQ` · `the_opening_entries_carry_the_opening_history_entry` (branch: `crates/busbar-unit-ledger/src/migration.rs`) | **PARK — money.** Trunk's ledger migration still takes the rate card as a **caller-supplied parameter**: `migrate(…, rate_card_version: u64, …)` (`crates/busbar-kernel-ledger/src/migration.rs:428`) → `opening_balances(&head, rate_card_version)` (`legacy.rs:173`), and the root hands it an operator config value verbatim — `migrate(rows, records, cfg.node, wall, cfg.rate_card_version, secret)` (`crates/busbar/src/root/migration.rs:203`). Nothing validates it, so a year of opening balances can be sealed priced under an arbitrary card. The branch replaces the parameter with `pub const OPENING_HISTORY_SEQ: u64 = 0` — *"a parameter is a place a mistake can be made, and there is exactly one entry a migration may open at"* — keeping the field at its offset so the marker's 88 bytes do not move. Trunk already accepts this pattern next door (`OPENING_CHECKPOINT_SEQ`, 4 files), which is corroborating rather than decisive. Root cause: operator-supplied value on a billed line with no validation. Recommendation: owner decision — pin to the sealed history entry, or state on the line why an operator may choose it. |
| `origin/delete/keep-a2a-terminal-seam` | SURVIVOR | 79 | 32 | `UNDER_REPORTED` · `a_provider_usage_shortfall_reaches_the_money_path` · `the_shortfall_is_never_folded_into_a_class_it_was_not_consumed_under` (branch: `crates/busbar-plane-llm/tests/usage_identity.rs`) | **SUPERSEDED — money, resolved differently and better.** Branch carries a deliberately-`#[ignore]`d red-by-design test asserting a Gemini provider under-report (stated `usageMetadata.totalTokenCount` > sum of decoded buckets) must REACH the money path rather than being computed, threaded and dropped. Trunk resolved the underlying defect instead of keeping the red: `crates/busbar-llm-codec/src/gemini/mod.rs:354-369` attributes the shortfall, and `gemini/tests/usage_identity_tests.rs:150,232` assert *"the shortfall IS the tool-use term"* and *"every term Google stated is now billed, so there is no shortfall left to report."* Nothing to port. Recorded because the branch's test NAME is the only in-code record of the original decision. |
| `origin/delete/keep-root-wiring` | SURVIVOR | 176 | 141 | `group_bucket_windows` · `KeyRowsRefused` · `webpki_client_config` · `boot_checks` · `a_store_that_will_not_list_its_key_rows_still_opens_the_configured_buckets` · `the_boot_step_seals_the_opening_on_the_nodes_one_book` (branch: `crates/busbar-core/src/cost.rs`, `crates/busbar/src/root/migration.rs`) | **REVIEW — money-adjacent boot/seal path.** `group_bucket_windows` (cost grouping) and the migration boot-seal cells (`KeyRowsRefused`, *"a store that will not list its key rows still opens the configured buckets"*) are absent from trunk code; `group_bucket_windows` and `KeyRowsRefused` each survive on trunk **only in `docs/`**. Branch is off an old base (`crates/busbar-core/` is split on trunk), so paths moved — but the trunk-side symbol search is path-independent and returns 0. Recommendation: owner read of whether trunk's post-split cost/migration code covers the same two behaviours under other names; not certified either way here. |

### Auth / credential path

| branch | bucket | symbols-in | symbols-found | absent symbols | verdict |
|---|---|---|---|---|---|
| `origin/delete/keep-auth-e` | SURVIVOR | 231 | 136 | `CredentialSignature` · `CredentialArrival` · `CredentialPresentation` · `read_arrival` · `SigningParams` · `AT_AUTHORIZATION_BEARER` · `AS_AUTHORIZATION_BEARER` · `NO_SIGNING` · …(+87) (branch: `crates/busbar-contract/src/kinds.rs`, `crates/busbar-unit-auth/src/carrier.rs`, `crates/busbar-unit-egress-auth/src/lib.rs`) | **GAP — documented-as-designed, code never landed.** The declared-credential-signature grammar (a credential's carriers DECLARED per dialect — worded vs verbatim carriers, scheme-word matched case-insensitively, empty falls through, an unmounted dialect refused by the declaration's absence) exists on trunk **only as `docs/design/1.6.0-credential-signature-declared.md`**. `git grep -w CredentialSignature <TRUNK> -- ':(exclude)docs/'` → **0 files**; same for `CredentialArrival`, `CredentialPresentation`, `SigningParams`. This is the exact trap the sweep was warned about, at full scale: the design doc reads as the feature. Branch carries the implementation plus 11 carrier cells. Recommendation: owner decision — port, or retract the doc so it stops reading as landed. |
| `origin/delete/keep-r5-auth-2` | SURVIVOR | 44 | 43 | `RequestHeaders` (branch: `crates/busbar-core/src/auth/mod.rs`) | **GAP — two live copies of one credential-precedence rule.** Trunk carries the carrier unit at `crates/busbar-kernel-identity/src/carrier.rs` (`extract_client_token`, `HeaderView`, `X_API_KEY`, `X_GOOG_API_KEY`, `AUTH_SCHEME_BEARER`) — but its **only `HeaderView` impl is in `crates/busbar-kernel-identity/src/tests/mod.rs:118`**, so the unit is wired to nothing in production. The live path is a **second, independent copy**: `AuthMiddleware::extract_client_token(&Request<Body>)` at `crates/busbar-kernel/src/auth/mod.rs:669` with its own duplicate constants (lines 27/28/33), called from `auth/exchange.rs:55` and `auth/mod.rs:1474`. The two agree today; nothing keeps them agreeing. The branch's `RequestHeaders` newtype is precisely the missing adapter that collapses them to one (branch commit: *"ONE copy of the credential carriers"*). Root cause: a de-dup that landed the unit but never switched the caller. Recommendation: port the adapter and delete the kernel-local copy, or the precedence rule (Bearer → `x-api-key` → `x-goog-api-key`, empty-is-absent, non-Bearer falls through) must be kept in lockstep in two places by hand. |
| `origin/delete/keep-admin-c1` | SURVIVOR | 46 | 43 | `refusal_answer` · `credential_refusal` · `without_admin_carriers` (branch: `crates/busbar/src/root/units_admin.rs`) | **REVIEW — admin credential refusal.** Three refusal-path symbols absent from trunk code; `refusal_answer` and `credential_refusal` survive on trunk **only in `docs/`**. Branch composes an admin refusal answer for a request presenting no admin carrier. Recommendation: owner read of whether trunk's post-split admin auth expresses the same refusal under another name. |
| `origin/delete/keep-stepown-sos` | SURVIVOR | 96 | 94 | `WIRE_HTTP` · `WIRE_STDIO` (branch: `crates/busbar-substrate-values/src/lib.rs`) | **REVIEW — egress wire-kind constants.** Both absent from trunk code, both present on trunk in `docs/` only. Branch centralises the wire-kind spellings used by `net_guard.rs` and the two `egress_auth` modules so the string is stated once. Low blast radius; likely superseded by a different constant name on trunk, but the two spellings themselves return 0. |

### Remaining 161 branches

Rows below are the mechanical result. `SUPERSEDED (bulk)` marks branches whose absent set is a
large parallel restructure taken off an old merge-base (trunk is 532–1,684 commits past these
bases) and which trunk implements under a different crate/name split — **spot-checked, not
certified per-symbol**. Two spot checks are recorded: `keep-a2a-node-3`'s
`CatalogueEntry`/`PromptTemplate` in `busbar-contract` are trunk's `Catalogue`/`PromptEntry` in
`crates/busbar-mcp/src/mcp/catalogue.rs`; `keep-auth-e` spot-checked the OTHER way and was
promoted to GAP above — so the label must not be read as a clearance.
`REVIEW` marks a small discrete delta genuinely absent from trunk code that warrants an owner read.

| branch | bucket | symbols-in | symbols-found | absent symbols | verdict |
|---|---|---|---|---|---|
| `origin/delete/integration/audit-landed` | SURVIVOR | 767 | 120 | `test_admin_v1_put_auth_accepts_configured_external_module` · `ExternalAdminIdp` · `test_admin_v1_config_settings_request_body_max_bytes_flagged_reload_to_apply` · `ControlSurface` · `_KIND_ALL_IS_COMPLETE` · …(+642 more) (branch: `.github/workflows/ci.yml`, `.github/workflows/streaming-conformance.yml`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-a2a-default-on` | SURVIVOR | 1388 | 419 | `a_posting_is_lent_for_one_settlement_and_the_second_lend_is_refused` · `lend_posting` · `PostingLent` · `RecordSink` · `CursorArena` · …(+964 more) (branch: `.github/workflows/keep-proof.yml`, `.keep-proof.toml`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-a2a-instr-b` | SURVIVOR | 46 | 0 | `_executed` · `_tck_report` · `unrun_deviations` · `expect_code` · `runner_floor_mutations` · …(+41 more) (branch: `scripts/a2a-subject/boot.sh`, `testing/a2a-harness/a2aht/runner.py`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-a2a-node-3` | SURVIVOR | 780 | 3 | `CLASS_LINE` · `sibling_source` · `every_class_on_the_line_still_has_the_arm_that_answers_it` · `every_verb_this_plane_answers_locally_is_a_class_the_line_knows_about` · `the_contract_face_answers_this_planes_scoped_read_and_nothing_more` · …(+772 more) (branch: `.keep-proof.toml`, `crates/busbar-a2a/src/a2a/tests/local_tests.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-actions-sha-pinned` | HARVESTED | 15 | 15 | — | **HARVESTED** — every branch-authored symbol present on trunk at a renamed path. |
| `origin/delete/keep-admin-cut0` | SURVIVOR | 365 | 218 | `RecordSink` · `CursorArena` · `a_cursor_shaped_arena_is_what_the_trait_takes` · `_RECORD_SINK` · `_store_is_a_record_sink` · …(+142 more) (branch: `crates/busbar-contract/src/kinds.rs`, `crates/busbar-contract/tests/object_safety.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-admin-cut2` | SURVIVOR | 527 | 219 | `RecordSink` · `CursorArena` · `a_cursor_shaped_arena_is_what_the_trait_takes` · `_RECORD_SINK` · `_store_is_a_record_sink` · …(+303 more) (branch: `Cargo.toml`, `crates/busbar-contract/src/kinds.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-admin-money-views` | SURVIVOR | 366 | 218 | `RecordSink` · `CursorArena` · `a_cursor_shaped_arena_is_what_the_trait_takes` · `_RECORD_SINK` · `_store_is_a_record_sink` · …(+143 more) (branch: `crates/busbar-contract/src/kinds.rs`, `crates/busbar-contract/tests/object_safety.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-admin-settings-note-restored` | SURVIVOR | 16 | 0 | `RESTART_NOTE_WHY` · `test_admin_v1_config_settings_restart_note_wire_bytes` · `test_admin_v1_config_settings_note_names_every_flagged_field` · …(+11 more) (branch: `crates/busbar-core/src/admin/tests/tests.rs`, `qa/construction.toml`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-announce-rename-tokenmint` | SURVIVOR | 183 | 3 | `RAISE_SHAPE` · `because_short` · `raises_in` · `struck_text` · `identity_of` · …(+175 more) (branch: `qa/construction.toml`, `qa/kind-isolation.toml`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-api-retire-2-secret-grammar-rename` | SURVIVOR | 50 | 0 | `secret_error_keeps_a_denial_apart_from_a_miss_and_from_an_outage` · `secret_error_kind_wire_tokens_are_the_frozen_five` · `secret_error_kind_is_byte_identical_to_the_legacy_enum_it_replaces` · `SECRET_LANE_KEY` · `SECRET_REF_GRAMMAR` · …(+45 more) (branch: `Cargo.toml`, `crates/api/Cargo.toml`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-arch-substrate-row` | EMPTY | 0 | 0 | — | **EMPTY** — branch adds no source content trunk lacks. |
| `origin/delete/keep-audit-planes-rB` | HARVESTED | 1 | 1 | — | **HARVESTED** — every branch-authored symbol present on trunk at a renamed path. |
| `origin/delete/keep-audit-xtask-r3` | HARVESTED | 1 | 1 | — | **HARVESTED** — every branch-authored symbol present on trunk at a renamed path. |
| `origin/delete/keep-batch3b` | SURVIVOR | 112 | 111 | "2026-09-07" (branch: `qa/inventory-coverage.json`) | **EMPTY (bookkeeping)** — absent items are `qa/` audit-ledger / design-binding records, not code. |
| `origin/delete/keep-bindings-three` | SURVIVOR | 318 | 165 | `load_gaps` · `gap_index` · `gap_register_problems` · `broken_refs` · `gapreg` · …(+148 more) (branch: `crates/busbar/tests/money_never_ends_an_admitted_unit.rs`, `qa/design-bindings-gaps.json`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-breaker-peek` | SURVIVOR | 393 | 2 | `KeyScope` · `operator_token_hash` · `ScopeSpec` · `LimitSpec` · `GroupSpec` · …(+386 more) (branch: `crates/busbar-contract/src/kinds.rs`, `crates/busbar-contract/src/limits.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-ci-feature-sets` | SURVIVOR | 108 | 107 | "nobody compiled it for days" (branch: `.github/workflows/ci.yml`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-ci-gate-mutants` | SURVIVOR | 274 | 249 | `ROW_MUTANTS` · `MUTANTS_CHECK` · `repo_of` · `CheckAnswer` · `ask_github` · …(+20 more) (branch: `.github/workflows/gate-mutants.yml`, `scripts/ci-branch-protection.sh`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-ci-gates` | SURVIVOR | 55 | 52 | `claims_any` (branch: `crates/busbar-plane-admin/src/codec.rs`, `crates/busbar-plane-mcp/src/claims.rs`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-ci-latchkey` | EMPTY | 0 | 0 | — | **EMPTY** — branch adds no source content trunk lacks. |
| `origin/delete/keep-ci-reds-2` | HARVESTED | 8 | 8 | — | **HARVESTED** — every branch-authored symbol present on trunk at a renamed path. |
| `origin/delete/keep-ci-triage-rebased` | SURVIVOR | 604 | 490 | `update_behaviour_md` · `write_json` · `cmd_write` · `passing_citers` · `expanded_calls` · …(+109 more) (branch: `.github/workflows/ci.yml`, `qa/design-bindings.json`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-ci-xtask-shard` | SURVIVOR | 1 | 0 | `selftest_dispatches_to_a_named_gate` (branch: `xtask/tests/cli.rs`) | **TEST-ONLY** — only test names/assertions absent; no production symbol missing. |
| `origin/delete/keep-config-gate-2` | EMPTY | 0 | 0 | — | **EMPTY** — branch adds no source content trunk lacks. |
| `origin/delete/keep-contract-transport-open` | SURVIVOR | 23 | 12 | `WireFraming` · `wire_format_name` · `a_dialect_is_a_framing_and_a_channel_is_a_family` · `the_labels_are_unchanged_by_the_split` · `TransportFamily` · …(+6 more) (branch: `crates/busbar-substrate-values/src/lib.rs`, `crates/busbar-substrate-values/src/tests/transport_tests.rs`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-control-tokenmint` | SURVIVOR | 399 | 0 | `MAX_ERROR_PARAMS` · `MAX_CATALOG_CODES` · `MAX_CATALOG_LOCALES` · `ErrorClass` · `ParamValue` · …(+394 more) (branch: `Cargo.toml`, `crates/busbar-contract/src/bounded.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-core-config` | SURVIVOR | 37 | 0 | `engine_trust_policy` · `MAX_ADMIN_SCOPE_READ_ONLY` · `MAX_ADMIN_SCOPE_FULL` · `MAX_ADMIN_SCOPE_TOKENS` · `max_admin_scope_token` · …(+32 more) (branch: `crates/busbar-core/src/config/config-schema.snapshot.json`, `crates/busbar-core/src/preflight.rs`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-core-config-gate` | SURVIVOR | 12 | 9 | "crates/busbar-core/src/config/overlay.rs" · "crates/busbar-core-config/Cargo.toml" · "a grammar type that MOVED from busbar-core t" (branch: `xtask/src/gates/config_schema/selftest.rs`) | **TEST-ONLY** — only test names/assertions absent; no production symbol missing. |
| `origin/delete/keep-core-delete-2` | EMPTY | 0 | 0 | — | **EMPTY** — branch adds no source content trunk lacks. |
| `origin/delete/keep-core-delete-3-stale-30a0519` | EMPTY | 0 | 0 | — | **EMPTY** — branch adds no source content trunk lacks. |
| `origin/delete/keep-core-delete-6` | SURVIVOR | 143 | 0 | `KeyScope` · `operator_token_hash` · `_VIRTUAL_KEY_DIRECTORY` · `ResolvedGroupView` · `ResolvedBucketView` · …(+138 more) (branch: `crates/busbar-contract/src/kinds.rs`, `crates/busbar-contract/tests/object_safety.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-core-delete-9e` | SURVIVOR | 189 | 0 | `KeyScope` · `operator_token_hash` · `ScopeSpec` · `LimitSpec` · `GroupSpec` · …(+184 more) (branch: `crates/busbar-contract/src/kinds.rs`, `crates/busbar-contract/src/limits.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-core-hooks` | SURVIVOR | 33 | 1 | `uninstalled_policy_timeout_is_the_historical_default` · `admin_suffix` · `assert_admin_cleaves` · `pipe_separated_prelude_plus_suffix_equals_the_whole_digest` · `Cleaved` · …(+27 more) (branch: `Cargo.toml`, `crates/busbar-core-hooks/Cargo.toml`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-core-p1` | HARVESTED | 6 | 6 | — | **HARVESTED** — every branch-authored symbol present on trunk at a renamed path. |
| `origin/delete/keep-core-plane-proto-move-rekey` | SURVIVOR | 95 | 0 | `RAISE_SHAPE` · `because_short` · `raises_in` · `struck_text` · `identity_of` · …(+90 more) (branch: `qa/construction.toml`, `xtask/src/gates/construction/ceilings.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-core-rows-12-16` | SURVIVOR | 102 | 0 | `OperatorRoute` · `PlaneDeclaration` · `check_operator_route_claims` · `BootFold` · `BUILTINS` · …(+97 more) (branch: `crates/busbar-contract/src/plane/registry.rs`, `crates/busbar-contract/tests/plane_registry.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-cross-kind-fixes` | SURVIVOR | 11 | 0 | `WireFraming` · `wire_format_name` · `a_dialect_is_a_framing_and_a_channel_is_a_family` · `the_labels_are_unchanged_by_the_split` · `TransportFamily` · …(+6 more) (branch: `crates/busbar-substrate-values/src/lib.rs`, `crates/busbar-substrate-values/src/tests/transport_tests.rs`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-design-bindings-export` | SURVIVOR | 611 | 2 | `KeyScope` · `operator_token_hash` · `RouteBar` · `RouteStatement` · `ServeRequest` · …(+604 more) (branch: `Cargo.toml`, `crates/busbar-contract/src/kinds.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-dialect-announce` | SURVIVOR | 182 | 3 | `RAISE_SHAPE` · `because_short` · `raises_in` · `struck_text` · `identity_of` · …(+174 more) (branch: `qa/construction.toml`, `qa/kind-isolation.toml`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-dialect-track-k11` | SURVIVOR | 435 | 16 | `BLOCK_BYTES` · `DIGEST_BYTES` · `hex_of` · `nist_one_block_message_abc` · `nist_empty_message` · …(+414 more) (branch: `.keep-proof.toml`, `Cargo.toml`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-egress-golden` | SURVIVOR | 5 | 0 | `write_response_event_error_type_carries_the_signal_verbatim` (branch: `crates/busbar-llm-codec/src/anthropic/tests/anthropic_hardening_tests.rs`) | **TEST-ONLY** — only test names/assertions absent; no production symbol missing. |
| `origin/delete/keep-eventstream` | EMPTY | 0 | 0 | — | **EMPTY** — branch adds no source content trunk lacks. |
| `origin/delete/keep-export-5` | SURVIVOR | 346 | 0 | `KeyScope` · `operator_token_hash` · `ScopeSpec` · `LimitSpec` · `GroupSpec` · …(+341 more) (branch: `Cargo.toml`, `crates/busbar-contract/src/kinds.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-export-kind` | SURVIVOR | 199 | 0 | `KeyScope` · `operator_token_hash` · `ScopeSpec` · `LimitSpec` · `GroupSpec` · …(+194 more) (branch: `crates/busbar-contract/src/kinds.rs`, `crates/busbar-contract/src/limits.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-face-governance` | SURVIVOR | 41 | 0 | `KeyScope` · `operator_token_hash` · `_VIRTUAL_KEY_DIRECTORY` · `the_bound_directory_is_the_one_the_unit_is_handed` · `the_denylist_reads_the_same_directory` · …(+36 more) (branch: `crates/busbar-contract/src/kinds.rs`, `crates/busbar-contract/tests/object_safety.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-fleet-fix` | SURVIVOR | 175 | 1 | `_uint` · `why_invalid` · `idp_case` · `check_arm` · `vault_case` · …(+169 more) (branch: `testing/fleet-fixtures/export-sink.py`, `testing/fleet-fixtures/fixture-selftest.sh`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-gate-battery-ignored-homes` | SURVIVOR | 215 | 3 | `RAISE_SHAPE` · `because_short` · `raises_in` · `struck_text` · `identity_of` · …(+207 more) (branch: `qa/construction.toml`, `qa/kind-isolation.toml`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-gate-fee-reads-nonzero` | EMPTY | 0 | 0 | — | **EMPTY** — branch adds no source content trunk lacks. |
| `origin/delete/keep-gate-kind-mint` | SURVIVOR | 78 | 2 | `a_census_retired_row_admits_the_floor_it_names_and_nothing_else` · `a_lowered_floor_with_no_retirement_row_is_still_red` · `a_retirement_row_may_lower_its_floor_by_exactly_one` · `a_retirement_row_missing_a_field_is_refused_not_skipped` · `MintedKind` · …(+71 more) (branch: `qa/kind-isolation.toml`, `xtask/src/gates/construction/census.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-gate-new-and-retired` | SURVIVOR | 54 | 2 | `a_census_retired_row_admits_the_floor_it_names_and_nothing_else` · `a_lowered_floor_with_no_retirement_row_is_still_red` · `a_retirement_row_may_lower_its_floor_by_exactly_one` · `a_retirement_row_missing_a_field_is_refused_not_skipped` · `base_names` · …(+47 more) (branch: `qa/kind-isolation.toml`, `xtask/src/gates/construction/census.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-gate-rebaseline-declared` | SURVIVOR | 278 | 4 | `RAISE_SHAPE` · `because_short` · `raises_in` · `strike_row` · `struck_text` · …(+269 more) (branch: `qa/construction.toml`, `qa/kind-isolation.toml`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-gate-rules` | SURVIVOR | 149 | 1 | `neither_decoder_reads_a_method_the_other_side_is_the_sender_of` · `planted_test_that_cannot_fail` · `planted_evidence` · `PLANTED_CARD` · `PLANTED_UNANSWERED` · …(+143 more) (branch: `crates/busbar-plane-mcp/tests/conformance.rs`, `qa/construction.toml`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-gate-selftest-parallel-tip` | HARVESTED | 60 | 60 | — | **HARVESTED** — every branch-authored symbol present on trunk at a renamed path. |
| `origin/delete/keep-gate-write-and-keys` | SURVIVOR | 36 | 0 | `identity_of` · `ordinal_form` · `striking_a_cell_does_not_re_target_the_cells_below_it` · `every_countable_row_of_the_kind_ledger_is_named_by_its_identity` · `a_row_this_reader_cannot_name_keeps_its_ordinal_rather_than_vanishing` · …(+31 more) (branch: `xtask/src/gates/construction/ceilings.rs`, `xtask/src/gates/construction/selftest.rs`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-gates-4b` | SURVIVOR | 513 | 66 | `RAISE_SHAPE` · `because_short` · `Ordinals` · `raises_in` · `raises_in_at` · …(+442 more) (branch: `qa/construction.toml`, `qa/kind-isolation.toml`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-gates-7` | SURVIVOR | 457 | 66 | `RAISE_SHAPE` · `because_short` · `Ordinals` · `raises_in` · `raises_in_at` · …(+386 more) (branch: `qa/construction.toml`, `qa/kind-isolation.toml`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-gates-close` | SURVIVOR | 25 | 0 | `RAISE_FORM_RETIRED_ON` · `is_ordinal_key` · `raises_in` · `the_retired_from_to_header_is_refused_by_name_with_the_closing_date` · `the_array_form_is_read_as_a_live_declaration` · …(+20 more) (branch: `xtask/src/gates/construction/ceilings.rs`, `xtask/src/gates/construction/selftest.rs`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-gates-rust-ctx` | SURVIVOR | 508 | 489 | `expanded_calls` · `cargo_deps_reads_the_dependencies_table_the_way_the_allow_list_was_calibrated_against` · …(+14 more) (branch: `qa/design-bindings.json`, `xtask/src/gates/construction.rs`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-golden-prep` | SURVIVOR | 113 | 110 | "'/usage' across a rate-card change — **the O" · "governance ('proxy/usage.rs:57-107'), routes" · "llm-conformance selftest: GREEN (13/13)" (branch: `qa/design-bindings.json`, `testing/llm-conformance/selftest.sh`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-health-probe-unit` | SURVIVOR | 1157 | 5 | `a_posting_is_lent_for_one_settlement_and_the_second_lend_is_refused` · `lend_posting` · `PostingLent` · `RecordSink` · `CursorArena` · …(+1147 more) (branch: `.keep-proof.toml`, `crates/busbar-caps/src/tests/mod.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-hygiene-examples-r1-text` | HARVESTED | 64 | 64 | — | **HARVESTED** — every branch-authored symbol present on trunk at a renamed path. |
| `origin/delete/keep-inventory-coverage` | HARVESTED | 149 | 149 | — | **HARVESTED** — every branch-authored symbol present on trunk at a renamed path. |
| `origin/delete/keep-k12-rows-11-16-b` | SURVIVOR | 475 | 155 | `OWNS_ITS_STEP` · `ErrorClass` · `PluginError` · `RecordSink` · `_RECORD_SINK` · …(+315 more) (branch: `crates/busbar-caps/src/unit.rs`, `crates/busbar-contract/src/error.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-k12-rows-8-16-ruled` | SURVIVOR | 456 | 155 | `OWNS_ITS_STEP` · `ErrorClass` · `PluginError` · `RecordSink` · `_RECORD_SINK` · …(+296 more) (branch: `crates/busbar-caps/src/unit.rs`, `crates/busbar-contract/src/error.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-k14-rows-3-7` | SURVIVOR | 294 | 152 | `OWNS_ITS_STEP` · `RecordSink` · `_RECORD_SINK` · `_store_is_a_record_sink` · `a_bare_record_sink_answers_the_three_verbs_behind_a_pointer` · …(+137 more) (branch: `crates/busbar-caps/src/unit.rs`, `crates/busbar-contract/src/kinds.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-k5-l02-funded` | SURVIVOR | 101 | 0 | `reference_ulaw_to_pcm16` · `REFERENCE_BIAS` · `every_ulaw_code_decodes_to_the_standard_s_own_value` · `the_published_reference_values_are_what_both_expressions_produce` · `RAISE_SHAPE` · …(+96 more) (branch: `crates/busbar-plane-voice/src/tests/ulaw.rs`, `qa/construction.toml`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-k5-l06-funded` | SURVIVOR | 187 | 0 | `CursorArena` · `a_cursor_shaped_arena_is_what_the_trait_takes` · `ArenaSpace` · `UnitArena` · `the_unit_arena_copies_what_it_is_given` · …(+182 more) (branch: `crates/busbar-contract/tests/object_safety.rs`, `crates/busbar-kernel/src/arena.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-k5-l11-funded` | SURVIVOR | 114 | 0 | `admin_suffix` · `assert_admin_cleaves` · `pipe_separated_prelude_plus_suffix_equals_the_whole_digest` · `Cleaved` · `CleavedInput` · …(+109 more) (branch: `crates/busbar-unit-audit/src/tests/legacy_cleave_tests.rs`, `qa/construction.toml`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-k8-4471ee569` | SURVIVOR | 306 | 152 | `the_kinds_contract_trait_has_an_implementor` · `OWNS_ITS_STEP` · `SessionFrame` · `SessionReply` · `SessionEnd` · …(+149 more) (branch: `crates/auth-admin-tokens/tests/auth_conformance.rs`, `crates/busbar-caps/src/unit.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-k8-6e06ad69d` | SURVIVOR | 1108 | 161 | `the_kinds_contract_trait_has_an_implementor` · `OWNS_ITS_STEP` · `SessionFrame` · `SessionReply` · `SessionEnd` · …(+942 more) (branch: `Cargo.toml`, `crates/auth-admin-tokens/tests/auth_conformance.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-k8-743c2749c` | SURVIVOR | 596 | 156 | `the_kinds_contract_trait_has_an_implementor` · `OWNS_ITS_STEP` · `SessionFrame` · `SessionReply` · `SessionEnd` · …(+435 more) (branch: `crates/auth-admin-tokens/tests/auth_conformance.rs`, `crates/busbar-caps/src/unit.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-k8-8b14bc87b` | SURVIVOR | 1107 | 161 | `the_kinds_contract_trait_has_an_implementor` · `OWNS_ITS_STEP` · `SessionFrame` · `SessionReply` · `SessionEnd` · …(+941 more) (branch: `Cargo.toml`, `crates/auth-admin-tokens/tests/auth_conformance.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-k8-b6e440fc1` | SURVIVOR | 464 | 155 | `the_kinds_contract_trait_has_an_implementor` · `OWNS_ITS_STEP` · `SessionFrame` · `SessionReply` · `SessionEnd` · …(+304 more) (branch: `crates/auth-admin-tokens/tests/auth_conformance.rs`, `crates/busbar-caps/src/unit.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-k8-cf4353037` | SURVIVOR | 816 | 159 | `the_kinds_contract_trait_has_an_implementor` · `OWNS_ITS_STEP` · `SessionFrame` · `SessionReply` · `SessionEnd` · …(+652 more) (branch: `Cargo.toml`, `crates/auth-admin-tokens/tests/auth_conformance.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-k8-d41991c3e` | SURVIVOR | 504 | 155 | `the_kinds_contract_trait_has_an_implementor` · `OWNS_ITS_STEP` · `SessionFrame` · `SessionReply` · `SessionEnd` · …(+344 more) (branch: `crates/auth-admin-tokens/tests/auth_conformance.rs`, `crates/busbar-caps/src/unit.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-k8-f5d65b8cf` | SURVIVOR | 474 | 155 | `the_kinds_contract_trait_has_an_implementor` · `OWNS_ITS_STEP` · `SessionFrame` · `SessionReply` · `SessionEnd` · …(+314 more) (branch: `crates/auth-admin-tokens/tests/auth_conformance.rs`, `crates/busbar-caps/src/unit.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-k9` | SURVIVOR | 1756 | 172 | `foreign_wire_field_does_not_break_the_key_row` · `malformed_scope_field_fails_the_read_rather_than_widening_to_wildcard` · `redeem_plane_token_default_refuses_rather_than_claiming_a_first_redemption` · `NoLedgerBackend` · `the_kinds_contract_trait_has_an_implementor` · …(+1579 more) (branch: `.github/workflows/ci.yml`, `.github/workflows/verify-deploy.yml`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-kernel-open-unit` | SURVIVOR | 152 | 3 | `SessionPosture` · `POSTURED` · `a_binding_that_declares_nothing_is_released` · `a_duplex_binding_declares_hold_beside_its_transport` · `on_step` · …(+144 more) (branch: `crates/busbar-contract-transport/src/surface.rs`, `crates/busbar-contract-transport/tests/wire_surface.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-kernel-route-outcome` | SURVIVOR | 127 | 0 | `BodyLease` · `CompletedUnits` · `ArenaBuf` · `span_slab` · `UnitArena` · …(+122 more) (branch: `crates/busbar-caps/src/egress.rs`, `crates/busbar-kernel/src/arena.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-kind-isolation-selftest-env` | SURVIVOR | 9 | 1 | `with_ambient` · `PLANTED_BASE_SHA` · `ki_env_1_probe` · …(+3 more) (branch: `xtask/src/ctx.rs`, `xtask/src/gates/kind_isolation.rs`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-kis-transitional` | SURVIVOR | 643 | 595 | `expanded_calls` · `cargo_deps_reads_the_dependencies_table_the_way_the_allow_list_was_calibrated_against` · `CONTROL_SINKS` · `MONEY_LIST_FILE` · `MONEY_LIST_RULE` · …(+43 more) (branch: `qa/design-bindings.json`, `qa/kind-isolation.toml`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-land-batching` | SURVIVOR | 758 | 735 | `expanded_calls` · `cargo_deps_reads_the_dependencies_table_the_way_the_allow_list_was_calibrated_against` · …(+18 more) (branch: `qa/design-bindings.json`, `scripts/land.sh`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-land-engine-12` | SURVIVOR | 2722 | 147 | `fleet_file_has_lifecycle` · `fleet_proof_hosts` · `remote_branch_slug` · `remote_work_dir` · `remote_workdir_script` · …(+2570 more) (branch: `.github/workflows/ci.yml`, `scripts/ci-remote-lib.sh`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-land-engine-15` | SURVIVOR | 4387 | 148 | `pdie` · `pdry` · `power_box_script` · `busy_count` · `_rsh` · …(+4234 more) (branch: `.github/workflows/ci.yml`, `scripts/ci-fleet-power.sh`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-land-engine-5` | SURVIVOR | 1476 | 146 | `fleet_pick_hosts` · `remote_push_sha` · `fanout_parse_request` · `_lib_selftest` · `_age` · …(+1325 more) (branch: `scripts/ci-remote-lib.sh`, `scripts/land-remote.sh`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-land-engine-8` | SURVIVOR | 1919 | 146 | `fleet_pick_hosts` · `remote_push_sha` · `fanout_parse_request` · `_lib_selftest` · `_age` · …(+1768 more) (branch: `scripts/ci-remote-lib.sh`, `scripts/ci-runners-reconcile.sh`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-land-fanout-live` | SURVIVOR | 794 | 175 | `fleet_pick_hosts` · `remote_push_sha` · `fanout_parse_request` · `_lib_selftest` · `shard_launch` · …(+614 more) (branch: `scripts/ci-remote-lib.sh`, `scripts/land-remote.sh`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-land-fullgate-voice-p3` | SURVIVOR | 887 | 213 | `an_open_surface_names_no_credential` · `the_extended_card_still_names_a_credential` · `every_narrowing_is_declared_by_the_claim_it_arrives_on` · `unit_of` · `OP_METADATA` · …(+669 more) (branch: `crates/busbar-plane-a2a/tests/conformance.rs`, `crates/busbar-plane-mcp/src/ops.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-land-throughput` | SURVIVOR | 320 | 36 | `land_shard_count` · `land_shard_union` · `land_shard_remote_one` · `land_shard_remote` · `land_shard_local` · …(+279 more) (branch: `scripts/land-remote.sh`, `scripts/land.sh`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-ledger-plugins-r1` | SURVIVOR | 1 | 0 | "opus-codeaudit-plugins-r1" (branch: `qa/audit-ledger.json`) | **EMPTY (bookkeeping)** — absent items are `qa/` audit-ledger / design-binding records, not code. |
| `origin/delete/keep-lk12-no-context` | SURVIVOR | 6412 | 147 | `pdie` · `pdry` · `power_box_script` · `busy_count` · `_rsh` · …(+6260 more) (branch: `.github/workflows/ci.yml`, `scripts/ci-fleet-power.sh`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-lk5b-engine-knobs` | SURVIVOR | 5066 | 146 | `lllog` · `ll_family_buckets` · `ll_load_families_reader` · `ll_families_branches` · `ll_onbox_script` · …(+4915 more) (branch: `.github/workflows/ci.yml`, `scripts/land-latchkey.sh`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-llm-move-4` | EMPTY | 0 | 0 | — | **EMPTY** — branch adds no source content trunk lacks. |
| `origin/delete/keep-llm-switch-over-2` | EMPTY | 0 | 0 | — | **EMPTY** — branch adds no source content trunk lacks. |
| `origin/delete/keep-m3-ledger` | SURVIVOR | 281 | 216 | `DIGEST_SCHEME_LEGACY_PIPE` · `DIGEST_SCHEME_LEN_PREFIXED` · `default_digest_scheme` · `is_legacy_scheme` · `DIGEST_SCHEME_2_TAG` · …(+60 more) (branch: `crates/busbar-unit-audit/src/legacy/entry.rs`, `crates/busbar-unit-audit/src/record.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-m6-usage` | SURVIVOR | 423 | 268 | `UsageCardHistory` · `USAGE_CARD_HISTORY` · `install_usage_card_history` · `usage_card_history` · `accrued_under` · …(+150 more) (branch: `crates/busbar-core/src/admin/tests/tests.rs`, `crates/busbar-core/src/admin/v1/service.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-mcp-engine-measure` | SURVIVOR | 322 | 2 | `KeyScope` · `operator_token_hash` · `ScopeSpec` · `LimitSpec` · `GroupSpec` · …(+315 more) (branch: `crates/busbar-contract/src/kinds.rs`, `crates/busbar-contract/src/limits.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-mcp-host-faces-2` | SURVIVOR | 487 | 3 | `the_contract_face_answers_this_planes_scoped_read_and_nothing_more` · `CatalogueEntry` · `PromptTemplate` · `PromptContent` · `ResourceBody` · …(+479 more) (branch: `crates/busbar-a2a/src/a2a/tests/taskstore_tests.rs`, `crates/busbar-contract/src/catalogue.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-mcp-m2-on-ph4` | SURVIVOR | 381 | 3 | `RegistrationState` · `KeyScope` · `operator_token_hash` · `ScopeSpec` · `LimitSpec` · …(+373 more) (branch: `crates/busbar-contract/src/counterparty.rs`, `crates/busbar-contract/src/kinds.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-mcp-moves-1` | SURVIVOR | 339 | 2 | `KeyScope` · `operator_token_hash` · `ScopeSpec` · `LimitSpec` · `GroupSpec` · …(+332 more) (branch: `crates/busbar-contract/src/kinds.rs`, `crates/busbar-contract/src/limits.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-mount-chain-funded` | SURVIVOR | 1114 | 5 | `a_posting_is_lent_for_one_settlement_and_the_second_lend_is_refused` · `lend_posting` · `PostingLent` · `RecordSink` · `CursorArena` · …(+1104 more) (branch: `.keep-proof.toml`, `crates/busbar-caps/src/tests/mod.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-mount-chain-funded-7` | SURVIVOR | 1106 | 5 | `a_posting_is_lent_for_one_settlement_and_the_second_lend_is_refused` · `lend_posting` · `PostingLent` · `RecordSink` · `CursorArena` · …(+1096 more) (branch: `.keep-proof.toml`, `crates/busbar-caps/src/tests/mod.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-netguard-p1` | SURVIVOR | 12 | 1 | `nat64_translated_metadata_and_internal_addresses_are_refused` · `ssrf_blocked_host_reads_the_nat64_spelling_of_metadata` · …(+6 more) (branch: `crates/busbar-substrate/src/tests/net_guard_tests.rs`) | **TEST-ONLY** — only test names/assertions absent; no production symbol missing. |
| `origin/delete/keep-one-net-judge` | SURVIVOR | 354 | 1 | `RegistrationState` · `KeyScope` · `operator_token_hash` · `ScopeSpec` · `LimitSpec` · …(+348 more) (branch: `crates/busbar-contract/src/counterparty.rs`, `crates/busbar-contract/src/kinds.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-oracle-base-reds` | EMPTY | 0 | 0 | — | **EMPTY** — branch adds no source content trunk lacks. |
| `origin/delete/keep-oracle-drivers` | EMPTY | 0 | 0 | — | **EMPTY** — branch adds no source content trunk lacks. |
| `origin/delete/keep-oracle-judge-p3` | SURVIVOR | 18 | 0 | `harness_touched_by_picks` · `lsay` · …(+13 more) (branch: `scripts/land.sh`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-oracle-pin-0.3.20` | SURVIVOR | 5 | 0 | `the_dispatch_core_has_exactly_one_caller` (branch: `crates/busbar-llm/src/engine/tests/engine_identity_witness_tests.rs`) | **TEST-ONLY** — only test names/assertions absent; no production symbol missing. |
| `origin/delete/keep-oracle-pin-pointer` | SURVIVOR | 14 | 0 | `named_def_shape` · `PUBLISHED_NAMES` · `valid_names_or` · `AgentDefView` · `NamedDefEntry` · …(+9 more) (branch: `crates/busbar-core/src/admin/v1/contract/taxonomy.rs`, `crates/busbar-core/src/admin/v1/json/handlers.rs`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-oracle-product` | SURVIVOR | 23 | 8 | "$here/testing/shadow-oracle/oracle.pin" · "$(awk -F= '$1==" · "{print $2}' " · …(+12) (branch: `bin/oracle`, `qa/design-bindings.json`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-overlay-notfound-wording` | SURVIVOR | 6 | 0 | `valid_names_oxford` · `valid_names_oxford_restores_1_5_5_template_with_grown_list` · …(+1 more) (branch: `crates/busbar-core/src/admin/v1/contract/taxonomy.rs`, `crates/busbar-core/src/admin/v1/json/handlers.rs`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-pb75-corrections` | SURVIVOR | 65 | 1 | `RECORDED_BODY` · `V155_REQUIRED` · `V160_ADDED` · `served_hook_body_carries_exactly_the_recorded_fields_and_values` · `the_1_5_5_members_are_a_prefix_of_the_served_1_6_0_body` · …(+59 more) (branch: `crates/busbar-core/src/admin/v1/json/openapi.json`, `crates/busbar-core/src/admin/v1/mod.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-plane-decl-registry` | SURVIVOR | 26 | 0 | `ROW_CORE_SPLIT` · `ROW_PLANE_CRATE_CENSUS` · `crate_dirs_with_prefix` · `core_split_row` · `plane_crate_census_row` · …(+21 more) (branch: `xtask/src/gates/plane_purity/mod.rs`, `xtask/src/gates/plane_purity/scanner.rs`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-plane-host-counterparty` | SURVIVOR | 319 | 1 | `RegistrationState` · `KeyScope` · `operator_token_hash` · `ScopeSpec` · `LimitSpec` · …(+313 more) (branch: `crates/busbar-contract/src/counterparty.rs`, `crates/busbar-contract/src/kinds.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-plane-list-substrate` | SURVIVOR | 18 | 9 | `install_builtin_plane_decls` · `BUILTINS` · `set_installed_plane_decls` · `installed_plane_decls` · `plane_list_first_read` · …(+4 more) (branch: `crates/busbar-core/src/plane/registry.rs`, `crates/busbar-substrate/src/plane/registry.rs`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-plane-mcp-a2a-p2` | SURVIVOR | 61 | 40 | `an_open_surface_names_no_credential` · `the_extended_card_still_names_a_credential` · `every_narrowing_is_declared_by_the_claim_it_arrives_on` · `unit_of` · `OP_METADATA` · …(+16 more) (branch: `crates/busbar-plane-a2a/tests/conformance.rs`, `crates/busbar-plane-admin/src/tests.rs`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-planedecl-section-in-buildctx` | SURVIVOR | 762 | 10 | `SessionFrame` · `SessionReply` · `SessionEnd` · `SessionDriver` · `SessionBudgets` · …(+747 more) (branch: `Cargo.toml`, `crates/busbar-contract-transport/src/session.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-prove-latchkey` | SURVIVOR | 4702 | 148 | `pdie` · `pdry` · `power_box_script` · `busy_count` · `_rsh` · …(+4549 more) (branch: `.github/workflows/ci.yml`, `scripts/ci-fleet-power.sh`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-queue-C2-lines3-4` | SURVIVOR | 1703 | 1386 | `boot_pass` · `redeem_plane_token_default_is_true_every_time` · `UsageCardHistory` · `USAGE_CARD_HISTORY` · `install_usage_card_history` · …(+312 more) (branch: `.fix/m8-red.py`, `crates/api/tests/mutation_hardening_store_contract.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-r31g-a2a-mount` | SURVIVOR | 547 | 218 | `RecordSink` · `CursorArena` · `a_cursor_shaped_arena_is_what_the_trait_takes` · `_RECORD_SINK` · `_store_is_a_record_sink` · …(+324 more) (branch: `crates/busbar-contract/src/kinds.rs`, `crates/busbar-contract/tests/object_safety.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-r32b-mcp-leg` | SURVIVOR | 574 | 218 | `RecordSink` · `CursorArena` · `a_cursor_shaped_arena_is_what_the_trait_takes` · `_RECORD_SINK` · `_store_is_a_record_sink` · …(+351 more) (branch: `crates/busbar-contract/src/kinds.rs`, `crates/busbar-contract/tests/object_safety.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-r4-0c-llm-leg-2` | SURVIVOR | 1112 | 415 | `a_posting_is_lent_for_one_settlement_and_the_second_lend_is_refused` · `lend_posting` · `PostingLent` · `RecordSink` · `CursorArena` · …(+692 more) (branch: `.github/workflows/keep-proof.yml`, `crates/busbar-caps/src/tests.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-r5-store` | SURVIVOR | 505 | 484 | `expanded_calls` · `cargo_deps_reads_the_dependencies_table_the_way_the_allow_list_was_calibrated_against` · …(+16 more) (branch: `qa/design-bindings.json`, `xtask/src/gates/construction.rs`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-rate-card-history` | EMPTY | 0 | 0 | — | **EMPTY** — branch adds no source content trunk lacks. |
| `origin/delete/keep-record-sink-contract` | SURVIVOR | 98 | 69 | `RecordSink` · `_RECORD_SINK` · `_store_is_a_record_sink` · `a_bare_record_sink_answers_the_three_verbs_behind_a_pointer` · `a_scan_limit_of_zero_is_nothing_and_not_everything` · …(+24 more) (branch: `crates/busbar-contract/src/kinds.rs`, `crates/busbar-contract/tests/record_sink.rs`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-red-holdescape` | EMPTY | 0 | 0 | — | **EMPTY** — branch adds no source content trunk lacks. |
| `origin/delete/keep-registry-mount-rows` | SURVIVOR | 1133 | 415 | `a_posting_is_lent_for_one_settlement_and_the_second_lend_is_refused` · `lend_posting` · `PostingLent` · `RecordSink` · `CursorArena` · …(+713 more) (branch: `.github/workflows/keep-proof.yml`, `crates/busbar-caps/src/tests.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-rekey-k9-row1` | HARVESTED | 149 | 149 | — | **HARVESTED** — every branch-authored symbol present on trunk at a renamed path. |
| `origin/delete/keep-rekey-keep-strike-path-reds` | SURVIVOR | 20 | 2 | `StreamIntent` · `DispatchPrep` · `WalkInput` · `read_client_stream_intent` · `apply_request_rewrites` · …(+13 more) (branch: `crates/busbar-llm/src/engine/pipeline.rs`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-release-scripts-p3` | SURVIVOR | 370 | 195 | `_uint` · `why_invalid` · `idp_case` · `check_arm` · `vault_case` · …(+170 more) (branch: `scripts/release-gate/ledger-selftest.sh`, `testing/fleet-fixtures/export-sink.py`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-scripts-m-z-p3` | SURVIVOR | 1163 | 522 | `confirmation_gap` · `_unproven` · `_census` · `expected_ids` · `rule_census` · …(+636 more) (branch: `.github/workflows/bolt-pass.yml`, `.github/workflows/ci.yml`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-security-advisory-8` | EMPTY | 0 | 0 | — | **EMPTY** — branch adds no source content trunk lacks. |
| `origin/delete/keep-speech-routes-to-llm` | SURVIVOR | 676 | 10 | `SessionFrame` · `SessionReply` · `SessionEnd` · `SessionDriver` · `SessionBudgets` · …(+661 more) (branch: `Cargo.toml`, `crates/busbar-contract-transport/src/session.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-status-table-recut` | SURVIVOR | 18 | 14 | "gate.surface_ceilings.contract_caps" · "rules.loc-ceilings.caps_contract_ceiling" · "rules.loc-ceilings.union_ceiling" · …(+1) (branch: `qa/construction.toml`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-stdio-p3` | SURVIVOR | 10 | 0 | `a_payload_carrying_the_delimiter_is_refused_and_does_not_fence` · `a_refusal_body_carrying_the_delimiter_is_refused` · `FRAME_DELIMITER` · `carries_the_delimiter` · …(+5 more) (branch: `crates/busbar-transport-stdio/src/tests/battery.rs`, `crates/busbar-transport-stdio/src/transport.rs`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-stepown-soa` | EMPTY | 0 | 0 | — | **EMPTY** — branch adds no source content trunk lacks. |
| `origin/delete/keep-store-hydrate-before-ready` | SURVIVOR | 123 | 0 | `a_flush_that_straddles_the_response_keeps_the_request_count_and_the_tokens` · `sample_ledger` · `sample_metering` · `REOPEN_MODEL` · `REOPEN_PROVIDER` · …(+118 more) (branch: `.github/workflows/ci.yml`, `crates/busbar-core/src/governance/tests/limits_tests.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-store-persistence-proof` | SURVIVOR | 98 | 0 | `sample_ledger` · `sample_metering` · `REOPEN_MODEL` · `REOPEN_PROVIDER` · `REOPEN_WINDOW` · …(+93 more) (branch: `.github/workflows/ci.yml`, `crates/plugin-loader/src/tests/lib_tests.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-streams-boot-composition` | SURVIVOR | 927 | 10 | `SessionFrame` · `SessionReply` · `SessionEnd` · `SessionDriver` · `SessionBudgets` · …(+912 more) (branch: `.keep-proof.toml`, `Cargo.toml`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-streams-funded` | SURVIVOR | 423 | 10 | `SessionFrame` · `SessionReply` · `SessionEnd` · `SessionDriver` · `SessionBudgets` · …(+408 more) (branch: `crates/busbar-contract-transport/src/session.rs`, `crates/busbar-contract-transport/src/surface.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-streams-mount-flip-2` | SURVIVOR | 1091 | 11 | `SessionFrame` · `SessionReply` · `SessionEnd` · `SessionDriver` · `SessionBudgets` · …(+1075 more) (branch: `.keep-proof.toml`, `Cargo.toml`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-strike-carried-raises` | EMPTY | 0 | 0 | — | **EMPTY** — branch adds no source content trunk lacks. |
| `origin/delete/keep-strike-seal-pick-reds` | SURVIVOR | 8 | 2 | `same_module` · `the_two_spellings_of_one_module_are_one_pin` · `a_sibling_of_a_pinned_module_is_not_pinned` · …(+1 more) (branch: `xtask/src/gates/construction/rules2.rs`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-structure-lint-fix-2` | SURVIVOR | 2 | 0 | "../../fixtures/lint_rules.rs" · "../../../../qa/construction.toml" (branch: `crates/busbar-caps/src/tests/mod.rs`) | **TEST-ONLY** — only test names/assertions absent; no production symbol missing. |
| `origin/delete/keep-substrate-fix` | SURVIVOR | 38 | 2 | `a_second_apply_retires_the_first_applys_client_identities` · `zeroize_key` · `demo_abandon_with_event` · `submit_chainless` · `settle_with_event` · …(+31 more) (branch: `crates/busbar-a2a/src/a2a/tests/transport_mtls_tests.rs`, `crates/busbar-substrate/src/egress/engine/tls.rs`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-substrate-ws-p2` | SURVIVOR | 5 | 0 | `the_acceptor_refuses_a_message_larger_than_the_front_door_body_cap` (branch: `crates/busbar-substrate/src/egress/tests/duplex_ws_tests.rs`) | **TEST-ONLY** — only test names/assertions absent; no production symbol missing. |
| `origin/delete/keep-tariff-3` | SURVIVOR | 496 | 0 | `KeyScope` · `operator_token_hash` · `ScopeSpec` · `LimitSpec` · `GroupSpec` · …(+491 more) (branch: `crates/busbar-contract/src/kinds.rs`, `crates/busbar-contract/src/limits.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-teller-recut` | SURVIVOR | 1164 | 3 | `the_escape_boundary_sits_below_the_space_and_not_at_it` · `an_ordinary_sentence_canonicalizes_with_its_spaces_intact` · `the_null_character_is_escaped_rather_than_emitted_raw` · `every_remaining_control_character_takes_the_four_digit_lower_case_escape` · `the_five_short_escapes_are_preferred_over_the_unicode_form` · …(+1156 more) (branch: `crates/busbar-a2a-codec/src/a2a/canonical.rs`, `crates/busbar-a2a-codec/src/a2a/tests/canonical_boundary_tests.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-tests-audit-wal-verbs` | SURVIVOR | 364 | 208 | `default_digest_scheme` · `is_legacy_scheme` · `a_caller_supplied_separator_cannot_move_a_recorded_outcome_or_attribution` · `a_legacy_chain_verifies_and_so_does_one_that_crosses_the_upgrade` · `the_scheme_is_on_the_wire_for_a_new_entry_and_off_it_for_a_legacy_one` · …(+151 more) (branch: `.audit/plant.py`, `crates/busbar-core/src/admin/audit.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-tests-http-sse-ws` | EMPTY | 0 | 0 | — | **EMPTY** — branch adds no source content trunk lacks. |
| `origin/delete/keep-tests-substrate` | SURVIVOR | 73 | 2 | `a_second_apply_retires_the_first_applys_client_identities` · `record_then_snapshot_contains_the_witnessed_tuple_verbatim` · `a_missing_condition_records_and_reads_back_as_none_not_a_sentinel_string` · `recording_the_same_tuple_twice_is_idempotent_in_the_set` · `distinct_fields_are_distinct_witnesses_not_folded_together` · …(+66 more) (branch: `crates/busbar-a2a/src/a2a/tests/transport_mtls_tests.rs`, `crates/busbar-substrate/src/admin_witness.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-tree-reds-on-k1` | HARVESTED | 143 | 143 | — | **HARVESTED** — every branch-authored symbol present on trunk at a renamed path. |
| `origin/delete/keep-unit-trait-testkits` | SURVIVOR | 265 | 175 | `the_kinds_contract_trait_has_an_implementor` · `OWNS_ITS_STEP` · `the_declaration_is_well_formed` · `AdmitInput` · `the_kind_has_one_entry_and_it_names_its_own_step` · …(+85 more) (branch: `crates/auth-admin-tokens/tests/auth_conformance.rs`, `crates/busbar-caps/src/unit.rs`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-vtable-budget-proof` | HARVESTED | 51 | 51 | — | **HARVESTED** — every branch-authored symbol present on trunk at a renamed path. |
| `origin/delete/keep-workflows-fix` | SURVIVOR | 35 | 0 | "chore(deps): headroom-core -> v0.36.5" · "battery subject seam" · "merge and push the tag" · …(+32) (branch: `.github/workflows/docker.yml`, `.github/workflows/mcp-conformance.yml`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-workflows-p3` | SURVIVOR | 383 | 175 | `probe_expectation_absent` · `probe_expectations_absent` · `probe_row_matches` · `start_commented` · `start_fake_redirect` · …(+203 more) (branch: `.github/workflows/bolt-pass.yml`, `.github/workflows/ci.yml`) | **SUPERSEDED (bulk)** — large parallel restructure off an old base; trunk implements the area under a different crate/name split. Spot-checked, not certified per-symbol. |
| `origin/delete/keep-xtask-batch2a-2` | SURVIVOR | 606 | 583 | `expanded_calls` · `cargo_deps_reads_the_dependencies_table_the_way_the_allow_list_was_calibrated_against` · …(+18 more) (branch: `qa/design-bindings.json`, `xtask/src/gates/construction.rs`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-xtask-catfile-fix` | SURVIVOR | 68 | 67 | "$(base64 < " (branch: `scripts/ci-runners-up.sh`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-xtask-leftovers` | SURVIVOR | 298 | 272 | `SPEC_SINK` · `MEASURED_EDGES` · `UNSCORED_SOURCES` · `render_graph` · …(+21 more) (branch: `xtask/src/full_gate.rs`, `xtask/src/gates/kind_isolation.rs`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
| `origin/delete/keep-xtask-tests-fix` | SURVIVOR | 8 | 0 | `ReachCache` · `reach_cache` · `FilesCache` · `files_cache` · `BetweenCache` · …(+3 more) (branch: `xtask/src/audit.rs`) | **REVIEW** — small discrete delta, absent from trunk code; needs an owner read. |
