# 1.6.0 — retiring `busbar-api`

> **The ruling this document executes.** "We are deleting it all, new code only. Compat? The new
> code IS compat." There is no shim, no deprecation window, and no frozen legacy contract crate on
> the ship SHA. `crates/api` (package `busbar-api`, the 1.5.5 plugin-contract crate) is deleted
> kind by kind, and each kind's only face afterwards is the trait `busbar-contract` already names.
>
> **The single most important measured finding, up front.** The 1.5.5 faces and the 1.6.0 faces are
> **not the same face under a new name.** `busbar_api::Store` has 33 methods and
> `busbar_contract::Store` has 22, and they share **none** of them. `busbar_api::AuthModule` is
> `name`/`authenticate`/`cacheable`; `busbar_contract::AuthScheme` is
> `locations`/`does_io`/`verify`/`refresh`. `busbar_api::RoutingPolicy` is seven methods over a
> prompt projection; `busbar_contract::Hook` is eight methods over a seat and a bounded view. So no
> row below is a repoint, and any plan that reads "change the `use` line" is wrong. **Every kind is
> a re-implementation against a face that already exists**, which is why the owner's ruling is the
> cheap one: writing new code against the new face is less work than reconciling two.

This file is the LEDGER for the whole `busbar-api` retirement. It is measured, not estimated —
every count in it came from the commands in the last section, run on this branch's base
(`b0c5b6f1b`, `origin/integration/oracle-phase0`). Later agents work off this file, drive their
count down, and amend the row rather than the rule.

## 1. The edge count, which is the thing that must reach zero

`cargo xtask gate kind-isolation` measures the retirement as **manifest edges**: one row per
`Cargo.toml` declaration, at its exact count, in `qa/kind-isolation.toml`. That is the number the
gate's ratchet moves, and it is the number a landing must lower.

| measure | at `b0c5b6f1b` |
|---|---|
| manifest edges **into** `busbar-api` | **23** (22 shipped, 1 test) |
| manifest edges **out of** `busbar-api` | **1** (`busbar-api -> busbar-secret-ref`, `owner-ruling-pending`) |
| `busbar_api::` path mention lines outside `crates/api` | **1 711** |
| root-level names `busbar-api` exports | **70** `pub use` items + 3 public modules |
| `busbar-api`'s own source | 3 591 lines across 10 files, plus 7 in-crate test files and 3 integration tests |

The 23 inbound edges, with the verdict the ledger already carries for each:

| verdict | n | crates |
|---|---|---|
| `not-allowed` | 16 | `busbar-a2a`, `busbar-a2a-codec`, `busbar-auth-admin-tokens`, `busbar-auth-static-plugin`, `busbar-core`, `busbar-hooks-ranking`, `busbar-llm`, `busbar-llm-codec`, `busbar-mcp`, `busbar-mcp-codec`, `busbar-secret-example-plugin`, `busbar-store-example-plugin`, `busbar-store-memory`, `busbar-voice`, `busbar-voice-codec`, and `busbar-unit-auth` on the test half |
| `tcb` | 3 | `busbar-plugin-loader`, `busbar-plugin-sdk`, `busbar-plugin-testkit` |
| `owner-ruling-pending` | 3 | `busbar-plugin`, `busbar-substrate`, `busbar-substrate-values` |
| `allowed` | 1 | `busbar` (the composition root) |

Four of the retirement's ledger rows are `[[question]]` rows — `busbar-api -> busbar-secret-ref`,
`busbar-plugin -> busbar-api`, `busbar-substrate -> busbar-api`, `busbar-substrate-values ->
busbar-api`. **This retirement answers all four the same way:** the edge is deleted, so no ruling
is owed. A question row whose edge no longer exists is not a question.

Per-crate reach, `busbar_api::` mention lines in `.rs` outside `crates/api`, measured:

| crate | n | crate | n |
|---|---|---|---|
| `busbar-core` | 510 | `busbar-substrate-values` | 26 |
| `busbar-llm` | 294 | `busbar-store-memory` | 13 |
| `busbar-a2a` | 178 | `busbar-voice-codec` | 8 |
| `busbar-mcp` | 131 | `busbar-mcp-codec` | 8 |
| `busbar-substrate` | 122 | `busbar-store-example-plugin` | 5 |
| `busbar` | 102 | `busbar-unit-auth` | 5 |
| `busbar-llm-codec` | 75 | `busbar-auth-static-plugin` | 4 |
| `busbar-plugin-loader` | 63 | `busbar-secret-example-plugin` | 3 |
| `busbar-voice` | 59 | `busbar-plugin-testkit` | 3 |
| `busbar-plugin-sdk` | 57 | `busbar-a2a-codec` | 3 |
| `busbar-plugin` | 46 | `busbar-hooks-ranking` | 2 |
| | | `busbar-auth-admin-tokens` | 2 |
| | | `xtask` | 2 |

## 2. The kinds, smallest first — and the two that are already done

The work order names six plugin kinds loading through `busbar-api`: store, auth, secret, hooks,
export, egress-auth. **Measured, only four of the six touch it at all.**

| kind | api symbols | mentions | crates | face in `busbar-contract` | status |
|---|---|---|---|---|---|
| **export** | **0** | **0** | **0** | `Export`, `ExportItem`, `Ack`, `Anchor` | **already api-free.** `crates/plugin-loader/src/export.rs`, `crates/busbar-plugin/src/cold/export.rs` and `crates/export-example-plugin/**` contain zero occurrences of `busbar_api`, and `busbar-export-example-plugin`'s manifest carries no `busbar-api` edge at all. Its remaining debt is the SDK edge, not the api edge. |
| **egress-auth** | **0** | **0** | **0** | `EgressAuthScheme`, `Signer`, `SignFailed` | **already api-free.** `crates/busbar-unit-egress-auth/**` contains zero occurrences of `busbar_api`. |
| **secret** | 8 | 100 | 10 | `Secret`, `SecretRef`, `SecretValue`, `SecretError` | **the first kind to move** — see below |
| **hooks** | 13 | 129 | 7 | `Hook`, `HookView`, `HookFacts`, `HookKindDecl`, `Seat`, `OnFailure` | second |
| **auth** | 20 | 230 | 16 | `AuthScheme`, `AuthOutcome`, `Credential`, `CredentialFacts`, `CredentialLocator`, `Challenge`, `ChallengeState`, `KeyMaterial` | third |
| **store** | 25 | 1 278 | 17 | `Store`, `StoreError`, `RecordBytes`, `Head`, `SliceGrant` | last, and by an order of magnitude the largest |

The per-kind mention totals sum to more than 1 711 because one `use busbar_api::{Store,
StoreError, StoreResult}` line is counted once per symbol it names. That is deliberate: the
retirement's unit of work is the symbol at the line, because each is a thing an agent must decide
about.

The residue — 4 symbols and 3 modules that belong to no kind, 221 mentions — has its own section
below. It is what is left after all four kinds land, and it is why the crate cannot be deleted by
draining kinds alone.

**Ordering follows the measurement, not the list order in the work order.** Export and egress-auth
are done; secret is the smallest live kind at 100 mentions; store at 1 278 is the last.

## 3. Symbol ledger

`n` is the count of `busbar_api::…<symbol>` mention lines in `.rs` files outside `crates/api`. A
symbol with `n = 0` is exported and never named outside its own crate — a **free delete**, and
there are five of them plus one whole module.

### 3.1 kind `secret` — 8 symbols, 100 mentions, 10 crates

| n | symbol | reaching crates | replacement in `busbar-contract` |
|---|---|---|---|
| 48 | `SecretRef` | `busbar-a2a`, `busbar-core`, `busbar-llm`, `busbar-mcp`, `busbar-substrate`, `busbar-voice` | `busbar_contract::SecretRef` **exists** — but it is `SecretRef(pub String)`, one opaque string in the plugin's own reference grammar, where the 1.5.5 type is a `{ module, settings }` struct re-exported from its own leaf crate `busbar-secret-ref`. Same name, different type. The move is a re-spelling of every configured reference site into the plugin's grammar, not a swap. |
| 19 | `SecretResolve` | `busbar-a2a`, `busbar-core`, `busbar-substrate`, `busbar-voice` | **no face, and it must not get one.** `SecretResolve` is a HOST-side seam a plane names to turn a ref into bytes. `busbar-contract` is plugin-visible-only — "core calls plugin, never the reverse" — so a resolver seam cannot live there. Its home is the caller's own port: the three plane/legacy crates each name their own, or they stop resolving and are handed a `SecretValue`. |
| 15 | `SecretModule` | `busbar-plugin`, `busbar-plugin-loader`, `busbar-plugin-sdk`, `busbar-secret-example-plugin` | `busbar_contract::Secret` **exists**. `SecretModule` is `resolve(&Map) -> Vec<u8>` plus a defaulted `resolve_with_deadline`; `Secret` is `ref_grammar`/`resolve`/`watch`/`sign`/`seal`/`unseal` over `SecretRef`/`SecretValue`. Re-implementation, not repoint. |
| 5 | `SecretError` | `busbar-plugin-loader`, `busbar-plugin-sdk`, `busbar-secret-example-plugin` | `busbar_contract::SecretError` **exists** (`Unknown`/`Unavailable`/`NotAuthentic`/`Malformed`). The 1.5.5 enum is `NotFound`/`Unavailable`/`Denied`/`Invalid`/`Internal` plus a message string. `NotFound`→`Unknown`, `Unavailable`→`Unavailable`, `Invalid`→`Malformed`, `Internal`→`Unknown`; **`Denied` has no variant.** That is the one-line contract addition this kind needs — or the mapping folds `Denied` onto `Unknown` and loses the operator-actionable "config/policy error, not an outage" distinction the 1.5.5 doc comment exists to preserve. **Owner decision: fold or add.** |
| 5 | `SecretResult` | `busbar-plugin-loader`, `busbar-plugin-sdk`, `busbar-secret-example-plugin` | none needed. It is `Result<T, SecretError>`; the contract spells the `Result` out per method. Delete the alias. |
| 4 | `SecretErrorKind` | `busbar-plugin`, `busbar-plugin-sdk`, `busbar-secret-example-plugin` | folds into `busbar_contract::SecretError` — the contract type IS the kind; the 1.5.5 kind-plus-message split is what goes. |
| 3 | `resolve_builtin_string` | `busbar-core`, `busbar-mcp` | none, and none wanted. The `env`/`file` built-ins are an in-tree **secret plugin**, not a contract function. They become a crate that implements `busbar_contract::Secret` and is registered like any other. |
| 1 | `resolve_builtin` | `busbar-core` | ditto. |

### 3.2 kind `hooks` — 13 symbols, 129 mentions, 7 crates

| n | symbol | reaching crates | replacement in `busbar-contract` |
|---|---|---|---|
| 25 | `TransformOutcome` | `busbar-core`, `busbar-llm`, `busbar-plugin-loader` | no direct face. `busbar_contract::Hook::may_rewrite` plus `IrPatch`/`IrEdit` carry the rewrite; the outcome enum's answers become the patch or its absence. |
| 21 | `RoutingPolicy` | `busbar-core`, `busbar-llm`, `busbar-plugin`, `busbar-substrate`, `busbar-plugin-loader` | `busbar_contract::Hook` **exists**. Zero method overlap; re-implementation. |
| 14 | `BudgetBucketState` | `busbar-core`, `busbar-llm`, `busbar-substrate`, `busbar-voice` | `busbar_contract::{BucketRef, BucketChain, BucketScope}`. **Money-adjacent — byte-identity required; this row does not move ahead of the money path.** |
| 12 | `RoutingRequest` | `busbar-core`, `busbar-llm`, `busbar-substrate` | `busbar_contract::HookView` plus `HookFacts` (bounded, arena-backed). |
| 11 | `PromptProjection` | `busbar-core`, `busbar-llm`, `busbar-plugin-loader` | `busbar_contract::HookView` — the projection IS the view under the new face. |
| 10 | `RewriteReply` | `busbar-core`, `busbar-llm`, `busbar-plugin-loader` | `busbar_contract::IrPatch`. |
| 9 | `RoutingDecision` | `busbar-llm`, `busbar-hooks-ranking`, `busbar-plugin-loader` | `busbar_contract::{VetoCode, Permutation}` over a `CandidateSet` — a hook restricts and reorders; it does not return a decision object. |
| 9 | `Candidate` | `busbar-llm`, `busbar-substrate`, `busbar-hooks-ranking` | `busbar_contract::{CandidateSet, CandidateIdx}`. |
| 8 | `RoutingContext` | `busbar-llm`, `busbar-substrate`, `busbar-hooks-ranking` | `busbar_contract::HookFacts`. |
| 6 | `HookStatus` | `busbar-core`, `busbar-plugin-loader` | `busbar_contract::OnFailure` covers the failure axis; the liveness axis is loader-internal and stays in the loader. |
| 3 | `PolicyResult` | `busbar-llm` | delete the alias. |
| 1 | `CallerIdentity` | `busbar-llm` | `busbar_contract::PrincipalId`. |
| 0 | `PolicyError` | — | **free delete.** |

### 3.3 kind `auth` — 20 symbols, 230 mentions, 16 crates

| n | symbol | reaching crates | replacement in `busbar-contract` |
|---|---|---|---|
| 79 | `UpstreamCreds` | `busbar-a2a`, `busbar-core`, `busbar-llm`, `busbar-llm-codec`, `busbar-mcp`, `busbar-substrate`, `busbar-substrate-values`, `busbar-voice` | this is **egress**, not ingress auth: `busbar_contract::{AuthDecoration, SecretSlot, SecretOnce}` on the destination, driven by `EgressAuthScheme`. The largest single symbol in the kind, and it belongs to a different kind than the file it lives in. |
| 29 | `AuthPrincipal` | `busbar`, `busbar-a2a`, `busbar-core`, `busbar-llm`, `busbar-mcp`, `busbar-substrate` | `busbar_contract::{PrincipalId, CredentialFacts}`. |
| 24 | `sha256_hex` | `busbar-auth-admin-tokens`, `busbar-auth-static-plugin`, `busbar`, `busbar-a2a`, `busbar-core`, `busbar-substrate-values`, `busbar-unit-auth` | **not a contract symbol at all** — a hashing helper. It moves to whichever crate needs it, or into the auth unit. A contract does not export a hash function. |
| 19 | `AuthModule` | `busbar-auth-static-plugin`, `busbar-core`, `busbar-plugin`, `busbar-plugin-loader`, `busbar-plugin-sdk`, `xtask` | `busbar_contract::AuthScheme` **exists**. Zero method overlap; re-implementation. |
| 17 | `constant_time_eq` | `busbar-auth-admin-tokens`, `busbar-auth-static-plugin`, `busbar`, `busbar-a2a`, `busbar-core`, `busbar-llm`, `busbar-substrate-values` | as `sha256_hex` — a primitive, not a face. |
| 16 | `AuthOutcome` | `busbar-auth-admin-tokens`, `busbar-auth-static-plugin`, `busbar-core`, `busbar-plugin-sdk` | `busbar_contract::AuthOutcome` **exists** — same name, different shape. Variant reconciliation when the kind lands. |
| 9 | `AuthPlugin` | `busbar-core`, `busbar-plugin-loader`, `busbar-plugin-sdk` | a loader-internal handle. Stays with the loader; not a contract name. |
| 7 | `Principal` | `busbar-auth-admin-tokens`, `busbar-auth-static-plugin`, `busbar-core`, `busbar-plugin`, `busbar-plugin-loader` | `busbar_contract::{PrincipalId, CredentialFacts}`. |
| 7 | `LoginKind` | `busbar-core`, `busbar-plugin` | the login sub-face (six symbols below) has **no** counterpart in `busbar-contract`, by design: interactive login is a control-surface concern, not a plugin kind. Its home is the admin control surface. |
| 6 | `IdentityRefusal` | `busbar-core`, `busbar-mcp`, `busbar-substrate` | `busbar_contract::{Refusal, RefusalReason}`. |
| 6 | `LoginModule` | `busbar-plugin-sdk` | login sub-face. |
| 4 | `CallerToken` | `busbar-core`, `busbar-llm` | `busbar_contract::{Credential, CredentialLocator}`. |
| 3 | `FieldKind` | `busbar-core`, `busbar-plugin` | login sub-face. |
| 2 | `LoginForm` | `busbar-core`, `busbar-plugin` | login sub-face. |
| 1 | `LoginField` | `busbar-plugin` | login sub-face. |
| 1 | `LoginHop` | `busbar-core` | login sub-face. |
| 0 | `BeginLogin` | — | **free delete.** |
| 0 | `CompleteLogin` | — | **free delete.** |
| 0 | `LoginHttpResponse` | — | **free delete.** |
| 0 | `LoginOutcome` | — | **free delete.** |

### 3.4 kind `store` — 25 symbols, 1 278 mentions, 17 crates

The largest kind by a factor of nine, and the one whose types are not really store types at all:
`PlaneRecord`, `PlaneSelector`, `PlaneRequestCtx` and `PlaneDisposition` are **316 of its 1 278
mentions**, and they are the legacy plane journal's row shapes leaking through the store face. They
drain with the plane extraction, not with the store kind.

| n | symbol | reaching crates | replacement in `busbar-contract` |
|---|---|---|---|
| 228 | `VirtualKey` | `busbar`, `busbar-a2a`, `busbar-core`, `busbar-llm`, `busbar-mcp`, `busbar-plugin`, `busbar-substrate`, `busbar-voice`, `busbar-plugin-loader`, `busbar-plugin-sdk`, `busbar-store-memory` | no contract type. Under the new face a key is a `RecordBytes` at a stream, read with `record_get`/`record_put`. The 1.5.5 struct is a governance record, and its home is the governance/teller path, not the plugin contract. |
| 219 | `StoreResult` | `busbar`, `busbar-a2a`, `busbar-a2a-codec`, `busbar-core`, `busbar-mcp-codec`, `busbar-substrate`, `busbar-voice`, `busbar-plugin-loader` | delete the alias. |
| 176 | `PlaneRequestCtx` | `busbar`, `busbar-a2a`, `busbar-core`, `busbar-llm`, `busbar-mcp`, `busbar-substrate`, `busbar-voice` | not a store type — plane request context. Drains with the plane extraction. |
| 132 | `Store` | 13 crates | `busbar_contract::Store` **exists**. 33 methods against 22, **zero shared**. Re-implementation. |
| 71 | `UsageLedger` | `busbar`, `busbar-a2a`, `busbar-core`, `busbar-llm`, `busbar-plugin`, `busbar-plugin-loader`, `busbar-plugin-sdk` | `busbar_contract::{UsageLocator, UsageLocators, MeterClassId}`. **Money path — byte-identity required.** |
| 68 | `ScopeRef` | 9 crates | `busbar_contract::{ScopeFacts, BucketScope}`. |
| 63 | `PlaneRecord` | 10 crates | plane journal row — drains with the plane extraction. |
| 44 | `PlaneSelector` | 10 crates | plane journal — drains with the plane extraction. |
| 43 | `StoreError` | 9 crates | `busbar_contract::StoreError` **exists**; variant reconciliation when the kind lands. |
| 37 | `UNIT_INPUT` | 10 crates | `busbar_contract::MeterClassId` — the four unit constants become declared meter classes. **Money path.** |
| 35 | `MeteringRow` | 7 crates | `busbar_contract::{MeterClassDecl, UsageLocator}`. **Money path.** |
| 33 | `PlaneDisposition` | 10 crates | plane journal — drains with the plane extraction. |
| 25 | `UNIT_OUTPUT` | 9 crates | as `UNIT_INPUT`. **Money path.** |
| 24 | `AuditRecord` | `busbar-core`, `busbar-plugin`, `busbar-substrate`, `busbar-plugin-sdk` | `busbar_contract::AuditFacts`. |
| 24 | `MeteringDelta` | `busbar`, `busbar-a2a`, `busbar-core`, `busbar-plugin-loader`, `busbar-plugin-sdk` | `busbar_contract::UsageLocators`. **Money path.** |
| 20 | `UNIT_CACHE_READ` | 10 crates | as `UNIT_INPUT`. **Money path.** |
| 12 | `UNIT_CACHE_WRITE` | 6 crates | as `UNIT_INPUT`. **Money path.** |
| 6 | `ModelTokens` | `busbar-core`, `busbar-plugin`, `busbar-plugin-loader`, `busbar-store-memory` | `busbar_contract::UsageLocators`. **Money path.** |
| 5 | `ModelTokensDelta` | the same four | `busbar_contract::UsageLocators`. **Money path.** |
| 3 | `CredentialSecret` | `busbar-core` | `busbar_contract::{SecretValue, KeyMaterial}` — and note it is a **secret**-kind type sitting in the store file. |
| 3 | `UsageDelta` | `busbar-core`, `busbar-plugin` | `busbar_contract::UsageLocators`. **Money path.** |
| 2 | `CredentialMeta` | `busbar-core` | `busbar_contract::CredentialFacts`. |
| 2 | `SecretForm` | `busbar-plugin`, `busbar-store-memory` | a **secret**-kind type in the store file; folds into the secret kind, not this one. |
| 2 | `RESERVED_UNITS` | `busbar-substrate-values`, `busbar-plugin-loader` | `busbar_contract::MAX_VOCABULARY` guards the same thing on the new side. |
| 1 | `register_scope_kind` | `busbar-core` | a mutable global registration function. **No contract equivalent, and none wanted** — the contract's vocabulary is closed at `MAX_VOCABULARY`, declared rather than registered at runtime. |

### 3.5 residue — belongs to no kind: 4 symbols and 3 modules, 221 mentions

This is why draining the four kinds does not delete the crate. Each row needs a named home before
`crates/api` can go.

| n | item | reaching crates | home |
|---|---|---|---|
| 141 | module `operation` (`Operation`, `OpShape`) | `busbar`, `busbar-core`, `busbar-llm`, `busbar-llm-codec`, `busbar-mcp-codec`, `busbar-substrate-values` | the operation taxonomy. Closest contract kin is `busbar_contract::{OpClassId, AdminVerbId, Selector}`, but the mapping is not one-to-one. **Needs a ruling before anyone moves it.** |
| 45 | `Redacted` | `busbar-core`, `busbar-llm`, `busbar-mcp`, `busbar-plugin`, `busbar-substrate` | a redaction wrapper. `busbar_contract::SecretValue` has the same debug-prints-nothing discipline and a test that holds it, but `Redacted<T>` is generic and `SecretValue` is bytes. **One-line contract addition, or a leaf crate of its own.** |
| 14 | `Signal` | `busbar-core`, `busbar-llm`, `busbar-plugin`, `busbar-substrate`, `busbar-plugin-sdk` | the decision-observability signal catalog. No contract counterpart. Its home is the audit unit or a leaf of its own. |
| 8 | `SignalBag` | `busbar-core`, `busbar-llm`, `busbar-plugin`, `busbar-substrate` | with `Signal`. |
| 5 | `SignalValue` | `busbar-core`, `busbar-llm`, `busbar-plugin` | with `Signal`. |
| 8 | module `durable` (`write`, `write_with`, `remove`, `create_dir_all`, `DurableOpts`) | `busbar-core`, `busbar-plugin-loader`, `busbar-store-example-plugin` | **a filesystem helper inside a contract crate.** It is not a contract and never was. It moves to a leaf utility crate; nothing about it needs a ruling. |
| 0 | module `usage_migration` (`USAGE_SCHEMA_V2`, `TierTokensV1`, `ModelTokensV1`, `UsageLedgerV1`, `fold_v1_model`, `fold_v1_ledger`) | — | **free delete of a whole module.** Zero mentions outside `crates/api`; exercised only by the crate's own tests. Confirm the on-disk v1 usage corpus is gone before deleting — the fold is money-shaped even though nothing calls it. |
| — | the `busbar_secret_ref::SecretRef` re-export | — | the one **outbound** edge (`busbar-api -> busbar-secret-ref`). It disappears with the secret kind. |

## 4. The first kind moved — `secret`

`secret` is the smallest live kind: 8 symbols, 100 mentions, 10 crates. Its landing is defined here
so the next agent picks it up mid-flight rather than re-deriving it.

**The face.** `busbar_contract::Secret` is the only face. `crates/api/src/secret.rs` is DELETED
whole — not deprecated, not re-exported — along with the `crates/api/src/tests/` rows that cover it
and `crates/api/tests/mutation_hardening_secret_contract.rs`, which move with the kind.

**The three shared seams, each its own `[SEAM]` commit.**

1. `busbar-contract` — the `Denied` question above, and nothing else. If the owner folds `Denied`
   onto `Unknown`, this commit does not exist and the kind lands in two.
2. the loader — `crates/plugin-loader/src/lib.rs`'s `DynSecret` resolves `kind: secret` through
   `busbar_contract::Secret`, and `crates/busbar-plugin/src/cold/mod.rs`'s secret payload stops
   naming `busbar_api::SecretErrorKind`.
3. the workspace and per-crate `Cargo.toml` lines the two above require.

**The example plugin.** `crates/secret-example-plugin` (`busbar-secret-example-plugin`) builds
against `busbar-contract` only; its `busbar-api = { path = "../api" }` line is deleted, which is
one of the 23 edges. It is a `cdylib` and it must be BUILT (`cargo build -p
busbar-secret-example-plugin`) and exercised over the dlopen seam, not merely compiled as an rlib —
its own manifest records why a cdylib-only build silently produces no artifact under `cargo test`.

**What the kind costs beyond the loader,** stated so nobody re-discovers it: the 48 `SecretRef`
mentions in `busbar-a2a`, `busbar-core`, `busbar-llm`, `busbar-mcp`, `busbar-substrate` and
`busbar-voice` are the `{ module, settings }` reference type, and the contract's `SecretRef` is an
opaque string in the plugin's own grammar. Retiring the 1.5.5 type means re-spelling every
configured secret reference in the tree. That is the secret kind's real cost, it is not in the
loader, and it is why "smallest kind" reads 100 mentions rather than the 27 the loader-side symbols
alone would suggest.

### 4.1 The blocker in the middle of the secret kind — a FROZEN SIGNED WIRE names a `busbar-api` type

This was found by reading the seam, it is not visible from the symbol counts, and it will stop the
next agent cold if they do not know it first.

`crates/busbar-plugin/src/cold/mod.rs` line 617 declares the secret kind's success payload:

```rust
pub enum SecretResponse {
    Bytes(Vec<u8>),
    Error {
        kind: busbar_api::SecretErrorKind,   // <- a busbar-api type ON THE WIRE
        message: String,
    },
}
```

`busbar_api::SecretErrorKind` is `#[serde(rename_all = "snake_case")]` over
`NotFound`/`Unavailable`/`Denied`/`Invalid`/`Internal`, so the bytes an installed third-party
`kind: secret` plugin emits today are `{"Error":{"kind":"not_found","message":"…"}}`. The cold lane
is declared frozen in its own module doc — its `extern "C"` symbols and JSON shapes are a SIGNED
wire contract that must not change — and `SECRET_ABI_VERSION` is `1`.

So **deleting `busbar_api::SecretErrorKind` changes a signed wire unless the enum is re-homed
first.** The order is forced, and it is three steps, not one:

1. **`[SEAM]`, `busbar-plugin`** — declare `SecretErrorKind` in `busbar-plugin::cold`, which is the
   crate that owns the wire and should always have owned its own wire enum. Byte-identical serde:
   same five variants, same order, same `rename_all = "snake_case"`. The red-before-green witness
   is a serde round-trip asserting the five wire tokens `not_found`, `unavailable`, `denied`,
   `invalid`, `internal` against literal JSON, so the freeze is proven by a test rather than by a
   comment. `SecretResponse::Error.kind` retypes onto it. `SECRET_ABI_VERSION` does **not** move —
   nothing on the wire changed, which is the whole point.
2. **`[SEAM]`, the loader and the SDK** — `crates/plugin-loader/src/lib.rs` (`DynSecret`, line
   1335, and `load_secret_from_bytes`, line 1373), `crates/plugin-loader/src/registry.rs` line 359,
   `crates/plugin-sdk/src/lib.rs` (`SecretHandle` line 592, `dispatch_secret` line 606,
   `secret_dispatch`) move onto `busbar_contract::Secret`. This is where the `Denied` question is
   answered in code: the loader is the only place that maps a wire token to a face error.
3. **the kind** — `crates/secret-example-plugin` re-written against `busbar_contract::Secret`, its
   `busbar-api` manifest line deleted (**one edge down, 23 → 22**), `crates/api/src/secret.rs`
   deleted whole with `crates/api/src/tests/` rows and
   `crates/api/tests/mutation_hardening_secret_contract.rs`.

Note what step 3 does **not** do: `busbar-plugin`, `busbar-plugin-loader` and `busbar-plugin-sdk`
keep their `busbar-api` edges, because each also names store/auth/hook/signal types. The secret
kind lowers the edge count by exactly **one** — `busbar-secret-example-plugin` — and lowers the
mention count by the 27 loader-side symbols. The other 73 are the `SecretRef` re-spelling above.
An agent who plans on "the secret kind deletes several edges" has mis-scoped it.

The host-side half — `SecretResolve` (19) and `resolve_builtin`/`resolve_builtin_string` (4) —
is deliberately NOT in these three steps. It is not plugin-visible, so it cannot go to
`busbar-contract` at all, and it moves with `busbar-core`'s own resolver, on its own commit.

## 5. Ordering, and the things that must not move first

1. **export** — done; close its row.
2. **egress-auth** — done; close its row.
3. **secret** — the loader/SDK/example-plugin half first, the `SecretRef` re-spelling second.
4. **hooks** — but `BudgetBucketState` is money-adjacent and waits for the money path.
5. **auth** — `UpstreamCreds` (79 mentions) is really egress-auth and should be re-filed onto that
   kind before the auth kind is scoped, or the auth kind carries a third of its weight for nothing.
6. **store** — last. Strip the four plane-journal types (316 mentions) with the plane extraction
   first, then the money-path types under byte-identity, then the face.
7. **residue** — `usage_migration` is a free delete today, `durable` is a mechanical move.
   `operation`, `Redacted` and `Signal` each need a home named before `crates/api` can be deleted.

**Money path.** Twelve rows above are marked *money path*: every `UNIT_*` constant, `UsageLedger`,
`MeteringRow`, `MeteringDelta`, `ModelTokens`, `ModelTokensDelta`, `UsageDelta` and
`BudgetBucketState`. Byte-identity is required on every one of them, and none of them moves on a
commit that also moves a face.

**Kinds never cross.** A store plugin knows the contract's store trait and nothing else. Three rows
above are filed under the wrong kind in the 1.5.5 crate and must be re-filed rather than carried:
`UpstreamCreds` (auth file, egress-auth kind), `CredentialSecret` and `SecretForm` (store file,
secret kind).

## 6. What was measured, and how

Every number above came from these, run at `b0c5b6f1b`:

```
# the edges the gate counts
grep -rn '^busbar-api\s*=' --include=Cargo.toml crates | wc -l          # 23

# total reach
grep -rn 'busbar_api::' --include='*.rs' crates xtask tests \
  | grep -vc '^crates/api/'                                             # 1711

# per crate
grep -rn 'busbar_api' --include='*.rs' crates xtask tests \
  | sed 's|^\(crates/[^/]*\)/.*|\1|' | sort | uniq -c | sort -rn

# per symbol (n, and the crates that reach it)
grep -rn "busbar_api::[A-Za-z_0-9:{}, ]*\b<SYMBOL>\b" --include='*.rs' \
  crates xtask tests | grep -v '^crates/api/'

# the two kinds that are already api-free
grep -c busbar_api crates/plugin-loader/src/export.rs \
  crates/busbar-plugin/src/cold/export.rs \
  crates/export-example-plugin/src/*.rs \
  crates/busbar-unit-egress-auth/src/*.rs                               # all 0

# the faces, side by side
sed -n '/pub trait Store/,/^}/p' crates/api/src/store.rs \
  | grep -o 'fn [a-z_0-9]*'                                             # 33
sed -n '/pub trait Store/,/^}/p' crates/busbar-contract/src/kinds.rs \
  | grep -o 'fn [a-z_0-9]*'                                             # 22, none shared
```

A row's `n` is a mention count, not a call count: a `use` line and a doc-comment reference each
count one. That is deliberate — every one of them is a line an agent has to open and change.
