# BUSBAR 1.6.0 — THE PLAN FOR DONE

**STATUS: 157 items — 109 from the sweep (1-111, two struck), 41 from the code audit (112-156),
7 lettered sub-items. Eleven sweep slices, four adversarial passes, two audit rounds, and five
oracle builds. Awaiting owner sign-off.**

**Two of the four claims are REFUTED by measurement, not by argument.** C3 diverges on 16 cells
where its own criterion is one; C2 fails for the `export` kind, which cannot be dropped in at all.
C1 is unmeasured over five of its seven kinds. C4's instruments cannot currently produce a NO.
The measured status of each is in `BUSBAR-1.6.0.md` Part 0.

When every item here is done, the four claims in `BUSBAR-1.6.0.md` Part 0 return YES and the train
moves. Nothing else counts. Ordered by **dependency**, not by date — owner ruling 10: *"forget a
date, i want a plan for DONE."*

Every figure was measured, not estimated, and re-verified by the architect before landing here.
The sweep's own coverage proof FAILED once and the failure is recorded in `BUSBAR-1.6.0.md` Law 8:
ten slices were assigned by path prefix, `busbar-k` < `busbar-kernel` < `busbar-l`, and the nine
kernel crates — 574 files of CORE — fell in neither range. An eleventh slice took them, and items
99-111 are what it found. Proving every file has an owner is not proving the owners' scopes tile.

> **Why the order is what it is.** Waves 0 and 1 come first because without them every later wave
> is unmeasurable or unshippable. A gate that cannot fail turns a green board into no evidence at
> all, and a switched-off release engine means a 100%-green tree still never leaves the shed. The
> sweep found both conditions live.

---

## HOW TO READ THIS DOCUMENT

**If you are going to WORK an item, you need two sections and nothing else:** THE EXECUTION ORDER
immediately below (what runs when, and the exit test that says a phase is done), and the item
tables (1-111 from the sweep, 112-156 from the audit). Everything after those is EVIDENCE — the
measurements and refutations each item rests on. Read it when an item surprises you, not before.

**Nothing here is estimated.** Every figure was measured and re-verified by the architect. Where a
figure was wrong it was corrected in place and the correction says so, because a plan that quietly
fixes its own numbers cannot be audited either.

---

## THE MEASUREMENT HAZARD IN THIS CHECKOUT — read before running anything

`predev` moved **four times** under a running measurement during this session, and the agents doing
the measuring noticed before I did. One consequence already landed: the audit-ledger deletion went
in 10.5 minutes into a workspace test run, so that run built a binary from before the deletion and
read a `ci.yml` from after it, and left three tests reading a file that no longer existed.

**The rule that follows:** any measurement that takes longer than a commit runs against a **pinned
worktree**, never against `predev`. `git worktree add --detach <pin> HEAD`, and put the pin outside
`/private/tmp` — the hourly reaper sweeps that directory and ate an audit pin mid-run today,
truncating six of ten finders, which could not tell "found nothing" from "tree gone". The reaper now
skips any directory carrying a `.codeaudit-pin` marker, but the placement rule is the real fix.

Also live in this checkout: two `xtask` files sit **staged** in the index that this session did not
stage. A bare `git commit` here sweeps them in. Every commit in this session used
`git commit --only <paths>` for that reason, and so should every commit after it.

---

# THE EXECUTION ORDER — SIX PHASES, EACH WITH AN EXIT TEST

The seven waves below this section are the **inventory** — what is owed, grouped by claim. This
section is the **order**, grouped by what must be TRUE before the next group is judgeable. They are
different questions and the plan previously conflated them, which is where the cycle came from.

**The rule that governs every phase boundary: a phase is done when its EXIT TEST passes, not when
its items are ticked.** An exit test is one command with an exit code. If a phase's items are all
green and its exit test is red, the phase is not done and the items were the wrong items.

---

## PHASE 0 — THE FLOOR: make CI capable of being green
**Why first:** `cargo test --workspace --locked` fails today at `xtask/tests/cli.rs:73`, on every
push. **CI has never produced a green run on this tree.** Until it can, no gate result anywhere is
evidence, and every "green" in this plan is unfalsifiable. This is the smallest phase and it
unblocks the meaning of all the others.

| item | what | files | agents |
|---|---|---|---|
| 106 | invert the stale red-first money assertion | `money_one_function_equivalence.rs` | 1 |
| 6a | wire `unconstructed`/`map-proof`/`sweep-coverage` into `ci.yml`; classify 2 cargo lines | `ci.yml`, `full_gate.rs` | 1 |
| 24 | add `metrics.rs` to `KERNEL_MONEY_FILES` | `no_float_money.rs:331` | 1 |
| 49 | **THREE, not two, and here they are by name:** `GREP_GATE_REPORT_ONLY`, `XTASK_INSN_EMIT_BASELINE`, `XTASK_PPB_EMIT_BASELINE`. The guard checks 11 vars and misses these; each one either regenerates the baseline the judge compares against or switches a gate to report-only. Check `BUSBAR_EMIT_OPENAPI` and `BUSBAR_ARM64_BASELINE` too | `verify-1.6.0-done.sh:363` | 1 |
| 13 | **a committed file at the tree root silently NARROWS the oracle's proof scope.** `.keep-proof.toml` is tracked, and its `families`/`tests` keys scope which families the proof runs. Both readers — `prove-remote.sh:64` and `manual-keep-proof.yml:693` — are `if -f` guards whose fallback is *"the oracle filter is '.' (every family)"*. **So deleting it is safe and WIDENS the proof**; whichever hand-back committed it narrowed what gets proven for everyone after | `git rm`; `prove-remote.sh` logs the every-family fallback | 1 |
| 88 | 3 rule regressions where a planted defect returns GREEN | `qa-names` selftest | 1 |
| F0 | lower `MIN_CRATE_MANIFESTS` from 40 — **it errors at 39 crates, i.e. at fold #10, not at 33** | `workspace_deps.rs:74` | 1 |

**7 agents, fully parallel — no two share a file.**
**EXIT TEST:** `cargo test --workspace --locked` exits 0 **and** `cargo xtask full-gate --selftest`
exits 0.

---

## PHASE 1 — ARM THE INSTRUMENTS (Law 0)
**Why second:** *"An instrument that cannot produce a NO is not a check."* 51 selftest cases across
11 gates currently score PROOF IMPOSSIBLE; `money-invariants` — the gate enforcing C4's first
clause — scans **the wrong file**; two gates claiming to enforce C1 cover **2 of the 7 plugin
kinds**. Draining debt measured by blind instruments produces false green, which is worse than
no green.

Every item here lands under **ARM AT TODAY'S NUMBER**: the ceiling is pinned at the current
measurement, so the gate blocks every RISE immediately, and the drain happens in Phase 4 behind it.

| item | what | agents |
|---|---|---|
| 9 | `money-invariants` scans `busbar-contract/src/records.rs`; production uses `busbar-kernel-audit/src/record.rs`, which carries `pub amount: Amount` — **a stored price the gate has never looked at** | 1 |
| 89 | 51 PROOF-IMPOSSIBLE cases across 11 gates (`config-schema` 22, `construction` 10, `map-proof` 7, …) | 6 |
| 90 | 6 owed rows with no RED case at all — 2 of them need a structural change, not a test | 3 |
| 1a | arm `LAW0_ENFORCED_NEUTRAL_CRATES` at today's 3084 | 1 |
| C1-KIND | `INSTANCE_VOCAB_KINDS = ["plane","transport"]` → all 7 kinds. **This is the biggest instrument hole in the release** — see below | 1 |
| 10 | `hot-path-perf`/`hot-path-alloc` execute zero benches; they grep bench source text | 1 |
| 2 | the 11 `frozen-wire` pragmas whose back-compat justification is false at both tags | 1 |
| 3 | the neutrality witnesses cannot see a NEW instance (plant moved 0 of 238 rows) | 1 |
| 87d/97 | `plane-config-noun` is GREEN today and armable; its recorded blocker is stale | 1 |

**~16 agents.** `89`'s six split one-gate-each; `90`'s three split one-gate-each; the rest are
single-file.
**EXIT TEST:** `for g in $(xtask gate --list); do xtask selftest $g; done` — every gate exits 0,
and no owed row is covered by zero RED cases.

### THE C1 HOLE, stated plainly because it is the release's central claim
C1 says *"core names no instance."* It is asserted over **7 plugin kinds** and measured over **2**.
`kind_isolation/matrix.rs:116` is `INSTANCE_VOCAB_KINDS = &["plane", "transport"]`, and `:1193`
skips every other kind. The counter-example is in core and I verified it by hand:
`busbar-kernel/src/config/sections.rs:435` defines `EXPORT_MODULES` — **a closed list of export
plugin INSTANCE names inside the engine** — and `config/mod.rs:1983` refuses any name outside it.

That is not only a C1 breach. It is a **C2 breach**: a dropped-in export plugin cannot be named in
a config file at all, so for the `export` kind the compiled-in fold and the dropped-in fold are not
the same contract. One hole, two claims, no instrument that can see it.

---

## PHASE 2 — MONEY
**Why third:** money is binary, and Phase 1 is what makes it checkable. Every item here is judged
by instruments Phase 1 repaired.

| item | what |
|---|---|
| 99/100 + | **the abandoned hold**, now confirmed by four independent routes — `Abandoned::drop` discards the settlement return value; `tick::sweep` has no production caller; `Occupied::drop` removes the slot **without** `mark()` so a sweeper could not see it even if wired; `recover_all` is production-compiled with zero callers and the WAL has no `Hold` class |
| BUDGET | **the enforcement path is not the invoice path.** `price_in_view` has exactly one caller and it is an admin read. The budget gate sums only the four reserved unit keys, so any open class (a2a hops, mcp calls, streaming audio-seconds) can bill on the invoice while contributing **zero** to the cap |
| C3-MONEY | the two PARKED oracle cells — rate-card history, and spend 250→0 after upstream down |
| 104 | six spend derivations collapse to one. **NOT PARALLELISABLE** — two agents each collapsing "their half" produce a seventh implementation |
| 22,23,25,26,28,32,105 | the remainder |

**EXIT TEST:** `cargo test -p busbar --test money_one_function_equivalence` exits 0 **and**
`xtask gate money-invariants` green over `busbar-kernel-audit/src/record.rs` **and**
`xtask gate plane-pricing-blindness` exits 0.

---

## PHASE 3 — C3, BYTE-IDENTITY
**Why fourth:** the corpus contains money cells, so it cannot settle before Phase 2.
Fix or owner-park the **12 product divergences**; close the **F-011b premise hole**; land 38/39/40
(config gating, not feature removal — Part 2 #17 is LOCKED and lists the planes in `default`);
41 (stop re-recording the reference on the machine under test); 44–48, 51, 52.
**EXIT TEST:** `./bin/oracle diff-cells --strict` rc=0 against the published 1.5.5 golden, with
every remaining difference carrying an owner-signed register entry **whose premise the runner
evaluates**.

---

## PHASE 4 — C1, C2 AND THE FOLD — the wide parallel drain
**Why fourth-and-a-half:** this is 90% of the raw item count and ~95% of the parallelism. It is
also where the crate fold lands, because the fold renames crates and **every instrument that would
notice a bad fold is keyed on crate names** — so it must run after Phase 1 armed them and after
Phase 3 recorded C3's evidence.

**THE FOLD — the owner's milestone, with the honest number: 49 → 35.**
Not 33. The planner's roster arithmetic: **35 is the executable plan, 34 if `busbar-core-connsec`
folds, and 33 is an all-repos roster total rather than a count of `crates/`.** The gap is
bookkeeping, not work avoided — and the hard blocker is Phase 0's F0, because
`MIN_CRATE_MANIFESTS = 40` returns an ERROR the moment crates drop to 39.

Parallel batches, sized by write-set overlap, not by logical dependency:
- **92/94 ledger drains** — 1,203 ratchet rows across 5 files. **5 agents** (one per file); the
  `kind-isolation.toml` agent runs LAST because `--write` refuses a rising count.
- **61a noun drain** — 744 rows over 418 files across 29 crates. **~29 agents**, crate-partitioned,
  file sets provably disjoint.
- **60 strict-purity drain** — 61 files. **15 agents** after a strict-scope hit dump exists.
- **59 dialect move** — 6 dialect agents in the middle, one serialized prereq (`ir/`), one
  serialized epilogue (manifests, 101 ratchet rows, CI).
- **68/69 dep-wall** — 18 manifests / 16 edges. **6 agents** clean, 13 after a contract-surface prereq.
- **the fold itself** — cheap folds (7 non-shipping crates) first, then one shipping fold per
  commit, each gated on `qa-names && package-selectors`.

**EXIT TEST:** `xtask gate kind-isolation` exits 0 · `plane-purity-strict` all rows `0 <= 0` ·
`instance-noun-neutrality` exits 0 · `ls crates | wc -l` = 35.

---

## PHASE 5 — THE RECORD
**Why last:** these items read the tree, so they can only be true once the tree stops moving. This
is where the cycle was: doing them earlier destroys the corpus the Phase 0/1 instruments read.
6b (the three gates green on content) · 87 (re-point 55 hardcoded doc paths, then delete the
unbound rest) · 83, 85, 86 · 75/76/77/78/107 (dead code, re-derived against the real denominator) ·
82 (backport two High advisories + records + CHANGELOG) · 17 (`ship-ready`) · 14/15/16/19/20
(the release engine, of which 15 and 16 are owner actions, not engineering).
**EXIT TEST:** `scripts/verify-1.6.0-done.sh` FULL, all bless/repoint vars empty, exits 0 across
all 23 declared groups. **That is dev-green.**

---

## WHAT THE OWNER STILL OWES — these block phases, and no agent can do them

| # | ruling | blocks |
|---|---|---|
| 1 | `main` carries 7 divergent commits and `enforce_admins: true`. **No code change exists that closes this** | Phase 5 (14/15/20) |
| 2 | the audit-chain `currency` field sits in a SIGNED published digest (field 29 of 44). Removing it is recipe v2 + per-record tagging + a re-derived golden + a v1 verifier kept alive — not a field deletion | Phase 2 (34) |
| 3 | the two money cells above — approve the new figures, or they are defects to fix | Phase 3 |
| 4 | `TURNSTILE_DISPATCH_TOKEN` exists in neither repo nor org secrets | Phase 5 (16) |

---


---

## THE DENOMINATOR, MEASURED — every LOC figure above this line was fiction

`wc -l` over `crates/` and `xtask/` answers **870,458**. That number is not a measurement of
anything: it counts doc comments (this tree writes a great many), blank lines, and test code — and
test code here is **57% of all Rust in the repo**. `scripts/rust-loc.py` is a real lexer (nested
block comments, strings containing `//`, raw strings), proven both directions before use.

| | lines of CODE |
|---|---|
| the product (`crates/` less xtask) | **189,075** |
| the instruments (`xtask/`) | **69,308** |
| production code, total | **258,383** |
| test code | **337,086** — 1.8× the product |

**The instruments are 27% the size of the product they measure.** Consequences for this plan:

- `busbar-kernel` is **50,694** lines of code, not 186,671.
- `busbar-llm-codec` is **22,063**, not 105,589 — item 59's move is a fifth of its recorded size.
- Every "N thousand lines" estimate in the fold plan and in items 59/74/75/81 must be re-derived.

---

## TWO BREAKERS SHIP, AND THE BUGGY ONE IS THE LIVE ONE

The concurrency lens reported three races in `store/in_memory/breaker.rs` and noted each is
"already fixed in its `busbar-kernel-breaker` twin." I checked which twin is wired, and the
reassurance inverts:

| | code lines | reached from |
|---|---|---|
| `store/in_memory/breaker.rs` — **has the three races** | 578 | the kernel's own admission path (`vocab.rs:493-553`, `availability.rs:6`) |
| `busbar-kernel-breaker` — **has the fixes** | 1,198 | **one** production file, `busbar/src/root/adapters.rs`; everything else is tests |

Positive control: `busbar_kernel_ledger` is referenced from 34 crates under the identical search;
`busbar_kernel_breaker` from five files, four of them tests.

**So the corrected logic is the one that is nearly unreached, and the defective logic is the one
deciding admissions.** The three races are live:

1. `breaker.rs:400` — `breaker_verdict` loads `cooldown_until` **before** `breaker_state`, the same
   order every writer writes them. `cell.rs:246-252` documents that order as *"an obligation, not a
   style choice."* A concurrent hard-down makes a benched lane report `usable: true` on `/stats`
   and `/healthz`; the mirror case drops a just-recovered lane from the healthy set and sheds the
   request.
2. `breaker.rs:795` — the under-lock re-check refuses unless `state == ST_OPEN`, so a cell a peer's
   probe just **closed** — ready to serve — is Denied instead of `ReadyNoProbe`.
3. `availability.rs:355` — `ProbeAdmit::Denied` carries no reason, so a cooling denial is reported
   as `ProbeInFlight`, whose `recovery_hint_ms` is **250 ms** against a real cooldown of up to
   **1,800,000 ms**. The twin splits `DeniedBy::Cooling { until }` from `DeniedBy::ProbeInFlight`
   for exactly this.

**This is the fold argument in miniature, and it belongs to the owner's milestone.** Two
implementations of one concept both ship; the duplication is not stylistic, it is a live
correctness difference, and no instrument compares them. Folding these two is worth more than
folding any two crates chosen to hit a count.

---

## A DOUBLE-BILLING CLAIM, REFUTED BY READING THE CODE — and what is really there

A lens reported that the live LLM path posts the same request's usage to **two independent money
ledgers**: the synchronous `ledger_and_meter` inside `FirstByteBody` (`response_body.rs:869,:984`)
and an asynchronous late accrual via `Walk::reported_after_terminal` (`walk.rs:474`) reading the
identical `TapCell`. It flagged its own uncertainty, correctly, because resolving it needed code
outside its slice.

**Double-charging is the worst defect available here, so I read it rather than escalate it.
It is not happening.** `walk.rs:464` states the design in terms that leave no room:

> *"It ACCRUES NOTHING. The tap already put this response on the governance ledger when it filled
> the cell, which is what the previous release bills and what `/usage` reports; calling the accrual
> seam again here would bill the same tokens twice. What this produces is a reading, for a second
> book to post onto."*

And `teller.rs:1222`'s `settle_late` is not a second write either — it is the `Err` arm of a match
on `Posted::into_parent`, i.e. the fallback for a child whose parent exited first. **Exactly one
branch fires.** The comment beside it: *"one posting, one sealed end… always posted, flagged as
late, against a synchronous draw."*

**What IS there, and it is the C4 question.** The system keeps **two books** over one event: the
governance ledger the tap fills (what 1.5.5 bills, what `/usage` reports) and the new kernel-loop
book the reading posts onto. One charge, two records — which is sound only while the two agree.
The oracle measures that they do not: `billing|rate-card|history-mid-window` reports different
figures per surface, across `by_key`, `by_model` and `total`.

So the item is not "stop double-billing." It is: **name which book is the invoice, prove the other
agrees with it cell by cell, and make a disagreement RED.** That is C4's own text — *"money is a
view on the ledger"* — applied to the case where there are two ledgers. It belongs beside the
money-authority flip, because the second book arrived with it.

*Method note, since it nearly cost a false escalation:* the reporting lens was honest about its
boundary and said so. The doc that settled it was two screens above the line it quoted. **Read the
module, not the call site** — that is the positive-control rule applied to intent rather than to
existence.

---

## THE CODEC: A TOKEN COUNT THAT CANNOT BE READ IS BILLED AS ZERO

Two HIGH findings, both "silently free", both on the revenue plane.

**1. `anthropic/reader.rs:732`, and the identical pattern in ALL SIX dialects, buffered and
streaming.** Primary chat-completion usage extraction is `read_count_u64(..).unwrap_or(0)`. A
present-but-unreadable token count — a stringified `"1500"` from a compatible self-hosted
backend — **bills as $0 instead of refusing.**

The damning part: **the correct function already exists in the same crate.** `usage_count::billed_count`
refuses exactly this input, and it is used for the transcription and image leaf ops — but never for
the flagship chat path. Verified live: `cargo test -p busbar-llm-codec usage_count` proves
`read_count_u64` genuinely rejects such input, so the `unwrap_or(0)` is converting a refusal into a
free request.

This is decision #42's rule — *a hit class not priced ⇒ REFUSE, never a silent 0* — violated at the
very first step, before any gate or ledger sees the number.

**2. `ir/rerank.rs:97`.** `RerankResp.search_units` is correctly read off Cohere's and Bedrock's
wire and then **discarded**: `RerankResp::billing()` hardcodes `Billing::Flat`. A 5,000-document
rerank bills identically to a 1-document one. The comment defers it to *"the 1.3 pricing engine"*;
the tree is at 1.6.0.

**3. MEDIUM — `anthropic/mod.rs:567`.** The streaming block-index DoS clamp
(`MAX_ANTHROPIC_BLOCK_INDEX = 1023`) collides two distinct out-of-range tool-call indices into one;
the Gemini egress writer reads a repeated index as "block re-opening", clears the accumulator, and
so corrupts one tool call's arguments and drops the other entirely. It needs indices ≥1024 from a
non-conforming upstream — the same threat model the clamp itself exists to defend against.

**Refuted by the same lens, worth recording:** `lib.rs:163`'s "the order is load-bearing" claim is
true for the boot-time protocol list, but the telemetry half of the comment overstates it — build
and lookup both re-derive position from one call in one process, and Prometheus series carry the
protocol name as an explicit label, so a reorder cannot desync telemetry.

---

## THE CONVERGENCE: ONLY FOUR TOKEN COUNTERS ARE EVER MONEY, AT EVERY LAYER

Four lenses hit this from four directions without knowing about each other. Stated as one finding,
because it is one.

busbar's billing model says a plane reports a name-keyed `Usage` map and money is a view over it.
In the shipped binary **only the four reserved token keys (`input`, `output`, `cache_read`,
`cache_write`) can ever become money.** Every other declared class — `bytes`, `tool_calls`,
`audio_seconds_in`, `audio_tokens_in/out`, `text_tokens_in/out`, `cached_tokens`, `decision`, a2a
`hops`, mcp `calls` — is dropped, and it is dropped **four separate times**:

| layer | mechanism | evidence |
|---|---|---|
| **the ABI** | the minor-20 keyed-unit tail (`units_ptr`/`units_len`) — **the field ABI_MINOR 19→20 and POD_VERSION 2→3 were bumped to carry** — is decoded by nothing outside its own crate's unit tests. The host never reads it | `busbar-plugin/src/hot/pod.rs:1008` |
| **the host meter** | the hot ABI promises identical metering for all four `UsageComponent`s; the only host implementation records units for **`Tokens` alone**. `Bytes`/`Frames`/`Queries` land in the ledger as a request with every unit counter zero | `pod.rs:272` |
| **the budget gate** | sums only `RESERVED_UNITS`; `RateNanos::reserved_rate` answers `_ => 0` for everything else, while `accrue`, `total_tokens`, `flush_budgets` and `hydrate_budgets` on the same cell are all open-keyed | `records.rs:601`, `cost.rs:175` |
| **the config grammar** | `RateEntryCfg` is `deny_unknown_fields` with exactly four `_utok` floats, so **no open class can be priced from config at all** | — |

**So the two planes that meter in open classes cannot be billed or capped.** MCP and A2A never
accrue anything into the budget ledger — `grep -rn -e meter_ledger -e record_usage` across both
crates returns **nothing**, against 9 files for `busbar-llm`/`busbar-voice`. They meter
`UsageComponent::Queries` at amount **0**, which `token_usage_for` maps to `None`. With
`per_request_fee` defaulting to 0, **a `budget:` cap is unreachable by those planes entirely** —
while A2A's refusal text tells the caller *"this key's budget is spent."*

**Why this is one finding and not four.** Fixing any single layer changes nothing: priced open
classes in config still hit a gate that sums four keys; a widened gate still reads units the host
never recorded; a fixed host still reads an ABI tail nothing decodes. It has to be closed end to
end or not started. That makes it the largest coherent piece of work in Phase 2 and the one most
likely to be mis-scoped as four small items.

**And it explains the shape of the whole money section.** The reserved four are the only units that
work, which is why every defect found lands on planes or paths that use anything else: a2a hops
charged before refusal, mcp queries metered at zero, streaming audio-ms discarded on barge-in,
audio-seconds unpriceable. The design is name-keyed; the implementation is four hardcoded columns.

---

## THE PATTERN WITH A NAME: TWO IMPLEMENTATIONS OF ONE THING, BOTH SHIPPING

Found twice, independently, by lenses that were not looking for it. This is the strongest argument
for the owner's crate fold that the audit produced, and it is stronger than any count.

| concept | the two implementations | which one is live | the consequence |
|---|---|---|---|
| **circuit breaker** | `store/in_memory/breaker.rs` (578 code lines) vs `busbar-kernel-breaker` (1,198) | the **578-line one**, on the kernel's admission path. The 1,198-line one is reached from a single production file | three live races — a benched lane reports `usable: true`, a just-closed cell is Denied, and a cooling denial advertises a 250 ms recovery hint against a real cooldown of up to 1,800,000 ms. **All three are already fixed in the copy nothing calls** |
| **realtime voice** | the old `busbar-voice` plane vs the new `StreamingPlane` (`claims.rs:150`) | **both** — compiled in and registered by default | two independent, non-communicating metering pipelines over one capability: a D2 nanodollar lease and a meter-class rate card. This is the "two rosters disagreeing" consequence of the incomplete voice→streaming rename, in money |

A third instance is the money-authority flip above: the kernel loop and the substrate loop were two
implementations, and the resolution was to delete one while four files still describe it as live.

**The rule this yields.** A fold is not justified by reaching a crate count. It is justified when
two implementations of one concept both ship and **no instrument compares them** — because then the
duplication is not redundancy, it is an unmeasured behavioural difference. Both cases above are
exactly that, and both are money. Fold these two first, ahead of any crate chosen to hit 35.

---

## STREAMING: THREE WAYS AUDIO IS SERVED AND NOT BILLED

The streaming plane bills by elapsed time, which makes the session lifecycle a money path.

- `busbar-plane-streaming/src/plane.rs:1038` — a Twilio call ending mid-turn (`stop`) **discards the
  open turn's accumulated audio-ms and tool-call counters** instead of billing them.
- `plane.rs:1130` — a client-authored **barge-in does the same**, with no compensating `Close`
  event, on every OpenAI-Realtime / Gemini-Live session supporting client-side interrupt.
- `busbar-voice/src/mount.rs:1544` (also `telephony.rs:209`, `mount.rs:1572`) — **every production
  call site discards the durable `SessionHandle` without ever calling `settle_terminal()` or
  `close()`**, and the plane's own idle-abandon callback (`scope.rs:159`) opts OUT of the sweep. So
  every voice session's durable row leaks in memory forever.

That third one is the abandoned-hold defect again, on a different plane, by a different mechanism —
the fourth independent route to the same conclusion: **the settle/sweep half of the money protocol
was designed and never wired.**

Plus `runtime/carrier.rs:87` — unbounded mpsc channels on the downlink/uplink audio relay, so a
stalled session grows memory without bound and nothing detects the stall.

---

## PANICS: THERE IS NO PANIC-CATCHING LAYER, AND ONE ADMIN PATCH CAN KILL A SHARD'S EGRESS

The opus escalation read **39 of 65 files line by line** (16,460 LOC) — the whole non-test request
path — after the cheap model returned zero over a fraction of it. It found the slice genuinely
hardened (50 `unwrap`/`expect` sites, each traced to a caller; poison-recovering locks nearly
everywhere; no runtime division in the slice) and produced one determination that matters more than
its three findings.

**There is no panic-catching layer anywhere in the workspace.** `CatchPanic`/`catch_panic` → **0
matches**; `tower-http` is not a dependency. Positive control: the router carries 10 `.layer(`
calls, so the search path is right. The 138 production `catch_unwind` sites are at the plugin ABI
boundary, not on the request path. With `panic = "unwind"` kept deliberately, **a panic on the
request path does not return 500 — it unwinds the hyper connection task, so the client sees a torn
down TCP connection and every request multiplexed on that h2 connection dies with it.** The process
survives. That is the actual blast radius of every `unwrap` in the tree, and it is not what a
reader would assume.

**Two agents disagreed about `pool_idle_timeout_secs`; I checked, and the alarming reading is
right.** The concurrency lens judged the overflow unreachable because the timeout "is not
operator-settable." It is: `config/limits.rs:244` declares it `pub pool_idle_timeout_secs: u64`
with `#[serde(default)]`, and `busbar-core-admin/.../handlers.rs:2785` exposes
`"limits.pool_idle_timeout_secs"` on the admin PATCH surface.

So: an admin PATCH setting it at or above 2⁶³ makes `egress/engine/pool.rs:793` overflow
`Instant + Duration` **while holding the pool mutex**; std poisons the mutex; and all nine
`.expect("engine pool lock")` sites — including `checkout` on the hot path — panic on every
subsequent request. **That shard's egress is dead until restart.** The egress engine and
`byte_duplex` are the only two subsystems in the tree that cascade a poisoned lock rather than
recovering it.

Two smaller ones worth keeping: `ingress/byte_duplex.rs:467` leaks an `inflight` key forever if a
plane callback panics (its sibling at `plane/handle_engine.rs:858` wraps the equivalent callback in
`catch_unwind` for exactly this reason), and `plane/cost.rs:221` checks its one money invariant with
the **raw non-saturating** `u128` `Sum`, bypassing the saturating `Sum` the same module defines 160
lines earlier explicitly against "a hostile/buggy plugin breakdown" — so release wraps and can
validate an ill-formed breakdown.

---

# THE CONTRACT CRATE: 7 HIGH DOC-LIES — AND ONE I HAD TO CUT DOWN BEFORE PASSING IT ON

The contract lens read **125 of 125 files (38,410 LOC)** — full coverage of the slice round 2 left
unread — and **ran** the gates rather than inferring them. 10 findings, 7 HIGH. This is the crate
named `contract`; every other crate trusts it.

## FIRST, THE CORRECTION, because it was nearly an escalation

The lens's headline was that *"only the kernel mints tokens, CI-enforced"* is false, citing
**"135/264/48/5 real non-kernel call sites"**. I ran the gate. It is genuinely RED:

```
FAIL  seal-witness:single-minter   a non-kernel site obtains the kernel seal
      135 site(s) of `KernelSeal::acquire_for_kernel(` outside crates/busbar-kernel/src/
```

Then I asked the question the word *"real"* was covering. Of **139** sites outside the kernel:

- **135 are test files or `src/tests/` modules**
- **the other 4 are DOC-COMMENT EXAMPLES** — `//! let seal = KernelSeal::acquire_for_kernel();` in
  `caps/{hold,token,decision,unit_end}.rs`
- **PRODUCTION SITES: ZERO**

**The token-minting invariant is intact in production.** Reporting "135 real non-kernel sites can
mint a kernel seal" would have been materially false in the direction that causes panic.

**But the finding survives, transformed, and is arguably worse as an instrument defect:**

1. A **BLOCKING** CI step (`ci.yml:574`) is permanently RED on **test-fixture debt**, none of it on
   the standing-reds allowlist. That is the permanently-red-gate disease this plan already names —
   it trains everyone to read this red as "that one always fails."
2. **The gate counts a NAME, not a THING.** Four of its hits are doc comments. It greps the literal
   `KernelSeal::acquire_for_kernel(` with no idea whether the line is code. That is exactly the
   `design_bindings/tables.rs:602` problem — 409 prose strings poisoning a census — reappearing
   inside a gate whose subject is the release's tightest security invariant.

**The rule this re-proves, now from the other side:** *a name is not a thing* cuts both ways. It has
cost me six false positives by finding a name where the thing was absent; here it nearly cost a
false positive by finding 135 names where the thing was harmless.

## The other six HIGH — all verified by the lens running the code, not reading it

| where | the claim, and what is true |
|---|---|
| `caps/egress.rs:138` | `SecretOnce`'s *"exactly once or the mint is reversed"* has **zero implementing code anywhere**; its "verbs unit only" claim is separately gate-confirmed RED |
| `dest.rs:355` | a **second, dead, differently-shaped `SecretOnce`/`AuthDecoration` pair** exported from the crate root, zero production callers, doc-mismatched to its own constructor. **Fourth sighting of two-implementations-both-shipping** |
| `caps/unit_end.rs:211` | *"only the exit path can build one"* — contradicted by `busbar-kernel/src/tick.rs:322`, an independent second builder. **`tick.rs` is the same file that was the invisible third caller in round 2's `HoldCell::take` finding** |
| `plane.rs:53` | cites an **"interior-mutability scan" gate that does not exist** anywhere in xtask's ~58-entry registry |
| `records.rs:503` | `CredentialMeta::slot`'s documented **SigV4 overlap-window rotation has no implementing code**; the one rotate endpoint explicitly does not touch SigV4 credentials |
| `tests/feature_invariance.rs:17` | *"exactly two feature gates"* — `test-seal` is a third, and **the enforcing test actually fails when run** (`4 passed; 2 failed`). Independent confirmation of item 112 from a second direction |

Plus three MEDIUM: the crate's own canonical *"what Rust actually enforces"* table cites **dead type
names**; *"exactly `IDEMPOTENCY_TTL_SECS`"* is **two independently-defined, coincidentally-equal
constants**; and the *"single JSON seam"* is bypassed by two production `.to_string()` calls
**invisible to its own guarding self-test** (wrong pattern, wrong scope).

**`tick.rs` has now been the hidden second or third writer in three separate findings** — the hold
cell's take, the unit-end builder, and the sweep. It is worth reading in full as its own item
rather than meeting it three more times.

---

# A2A: 21 FINDINGS, 7 HIGH — AND A BREAKER WITH NO IMPLEMENTATION

The a2a lens re-read all 51 non-test source files (25,595 LOC) across six overlapping passes, each
cross-verifying the others. **21 findings, 7 HIGH**, none overlapping round 2's.

**Verified by me:**

- **`registry.rs:135` — the anomaly circuit breaker can never trip.** Its observation window is
  never written in production: **`impl Window` appears 0 times**, against a positive control of 3
  `impl` blocks in the same file. So the breaker — **including its egress-spend signal** — is a
  structure that is read and never fed. There is also no config surface for its thresholds. This is
  the instrument-that-cannot-fire shape, sitting on a money signal.
- **`originate.rs:164` — busbar's OWN outbound hops are unmetered, unaudited, and exempt from the
  breaker.** The line is literally `breakers: None,`. These are list-refresh and push-config
  mirroring hops: they **dial real backends with a leased credential**, spend real money, and are
  recorded nowhere. It is the exact mirror of round 2's `receive.rs:2085` — that one bills a hop
  that never happened; this one performs a hop that is never billed.

**Reported with the lens's evidence, not independently re-verified by me** (my own line probe landed
on unrelated content, so I am not claiming it):

- **`plane.rs:240` — any admin config apply, even one unrelated to A2A, rebuilds the whole agent
  registry from the fail-closed floor**, wiping approval, pin and cached card for **every** agent
  until a human re-approves each one by hand. If that holds, an unrelated config change is an
  outage requiring manual per-agent recovery.
- `receive.rs:607` — the REST `GET /a2a/agents/{id}` handler runs admission and signing but **never
  meters or audits**, unlike the JSON-RPC spelling of the same read. Two spellings of one operation,
  one of them off the books.
- `serve.rs:509` — the backend-leak security sweep is **silently skipped on a malformed-but-prefixed
  backend URL**, serving the vendor's real address to callers.
- `relay.rs:1221` — gRPC Trailers-Only errors are misread as unbreakable, so a permanently broken
  gRPC agent **never trips its breaker**.

Plus 11 MEDIUM including audit-chain integrity on rehydrate, two never-called retention/verification
functions permitting unbounded durable growth, a JWS multi-signature bug, and a swallowed
store-write failure.

**Three of the seven HIGH are breakers that cannot open** (`registry.rs` has no window,
`originate.rs` passes `None`, `relay.rs` misclassifies the error). A circuit breaker that cannot
trip is Law 0 applied to runtime protection rather than to a gate — and all three sit on paths that
spend money.

---

# A FAILED GENERATION IS BILLED AS A COMPLETED ONE

The codec lens closed round 3 with **15 findings, 11 HIGH**. The sharpest is money, and the proof is
the code's own enumeration.

`proto_stream.rs:117-123` defines `terminal_error` as *"the breaker-failure signal the stream-end arm
reads to distinguish a clean close from an aborted one"*, and states exactly what sets it: an
`IrStreamEvent::Error` from **an Anthropic `error` event, an OpenAI in-band `{"error":…}` frame, a
Responses `response.failed` frame, or a Bedrock in-band `*Exception` frame.**

**Four dialects are on that list. Gemini and Cohere are not** — and both have failure outcomes:
`gemini/mod.rs:1241` maps `MALFORMED_FUNCTION_CALL` (*"model produced an unparseable tool call"*)
and Cohere maps its buffered generic `ERROR`, both to `IrStopReason::Error` — **a stop reason, not
a stream Error event.**

So the stream ends, `terminal_error` is never set, the stream-end arm reads a **clean close**, the
production billing-suppression path never fires, and **the customer is charged in full for a
generation that failed.** On two of six dialects.

## The `unwrap_or(0)` class is wider than round 2 found

Round 2 confirmed `read_count_u64(..).unwrap_or(0)` on the primary chat path. Round 3 found the same
lenient read at **two further call sites round 2 did not list**: `recover_truncated_usage` in **all
six dialects**, and `wire_shim.rs`'s `tier_usage()` cache-tier fields.

**And Bedrock's Titan-embeddings read is worse than the others.** It uses the same lenient pattern
under a comment that calls it *"BILLED COUNT"* — and there an unparseable count produces **zero
ledger rows**, not merely zero tokens. Nothing records that the work happened at all.

## Two convergent streaming defects

- **Tool-call accumulators keyed by wire index can be corrupted or silently swallow a second tool
  call** — across **Anthropic, Bedrock, Cohere and OpenAI-Chat**, by three different mechanisms
  (missing open-guards, index clamping, index reuse after close). Four dialects, one shape.
- **Cohere and OpenAI-Responses silently drop citations and annotations on the streaming path
  only** — buffered and writer both support them. **Cohere's own comments record that this exact
  bug class was already found and fixed once — on the writer side only.** The fix existed and was
  applied to one of the two paths.

---

## THE LENS THAT DROPPED ITS OWN FINDINGS

Two of this lens's five sub-agents exceeded scope, each re-ran a full-crate pass, and raced to write
the output file — the fourth instance of my nested-forking defect.

Its response is the standard the other lenses should be held to: it **did not trust either
sub-agent's self-verification or its file write.** It personally re-verified all 15 findings against
the pinned tree — exact `file:line`, exact quote, causal mechanism re-traced — **corrected several
line numbers**, and **dropped two candidate findings it could not verify to that standard** (a
mid-close billing claim and a moderation-flag default) rather than carry them on trust.

**Dropping a plausible finding you cannot prove is harder than reporting it, and worth more.** A
report padded with two unverified claims is not 15 findings plus 2 — it is 17 findings none of which
a reader can now trust without re-checking, which is the whole cost the audit exists to avoid.

---

# THE OAUTH LOGIN FLOW IS UNREACHABLE, AND THE UNBOUNDED-NUMBER SWEEP FOUND FOUR MORE

## OAuth consent is gated by a credential no browser can present

`crates/busbar-oauth2/src/routes.rs:86`, verified:

```rust
.route(id.consent_path(), RouteMethod::Get,  RouteAuth::Admin, consent_screen)
.route(id.consent_path(), RouteMethod::Post, RouteAuth::Admin, consent_submit)
```

The consent screen is **where a browser lands after the `/authorize` redirect** — it is the human
half of the flow. Both its GET and its POST are `RouteAuth::Admin`, the header-only admin
credential. The route immediately above it is `RouteAuth::None`, so this is a deliberate per-route
choice rather than a blanket default.

**A browser following a redirect cannot present an admin header.** In any deployment with a real
admin-auth chain, the OAuth login flow cannot be completed by the person it exists for.

## The unbounded-numeric sweep: one proven defect became five

I asked for every numeric field on the admin PATCH surface to be enumerated and traced, on the
strength of the single `pool_idle_timeout_secs` instance. That is what a systematic sweep is for,
and it found **four more of the same class**:

| field | where it lands | when it bites |
|---|---|---|
| `limits.upstream_request_timeout_secs` | a panicking `Instant + Duration` on the **LLM streaming hot path** (`send.rs:67`) | live |
| `health.default_probe_timeout_secs` | `busbar-llm/src/engine/health.rs:385` | **delayed fuse** — crashes the next restart's freshly-spawned prober, not immediately |
| `advanced.usage_flush_interval_ms` | `busbar-kernel/src/governance/mod.rs:1230` — unvalidated **and falsely classified as live** | **delayed fuse** — widens the governance-spend durability window after a later restart |
| `export/webhook.rs:178` | fourth instance, config-file-reachable rather than PATCH-reachable | boot |

Two of those are **delayed fuses**, which is the more dangerous shape: the operator sets a value,
the API reports success, nothing happens, and the failure arrives at the next restart — by which
time the change and the crash are separated by hours or days and by whoever restarted the process.

**The remedy is one rule, not five fixes:** every numeric on the admin surface declares a bound, and
a value that reaches an arithmetic operation, an allocation or a sleep without one is a gate failure.
That belongs in Phase 1 beside the "a comment asserting a measurement must name its command" rule.

## Two more, and the third sighting of a shipped duplicate

- **`busbar-core-admin/src/verbs.rs:303`** — a complete, shipped-but-dead **duplicate key-mint/rotate
  implementation with its own idempotency cache**. Unreachable today; a latent double-mint the
  moment anything wires a second transport to it. **Third instance of two-implementations-both-shipping**
  (after the two breakers and the two voice planes) — and this one mints credentials.
- **`busbar-core-admin/src/rate.rs:72`** — the admin rate-limit class table never gained the two
  1.6.0 named-map sections (`tools`, `agents`), so full-App-rebuild mutations on those routes get
  **6× the intended rate budget.** A 1.6.0 surface added without its 1.6.0 limit.

---

## AN ORCHESTRATION DEFECT OF MINE, NOW AT THREE INSTANCES

Three lenses have now reported that sub-agents they dispatched **ignored an explicit instruction not
to write files and wrote directly to the shared findings file** — in two cases racing, in one case
the second silently overwriting the first.

All three caught it. The resource lens re-verified twelve quotes and found a directory nothing had
read. The admin lens verified every claim itself and **corrected two factually wrong failure
scenarios** — the health-probe and usage-flush findings had been framed as immediate crashes when
the real mechanism is restart-gated, which is exactly the kind of error that makes a report sound
more alarming than the truth.

**The defect is mine, not theirs:** I told lenses to write one file and left nested forking
available, so a fork inherits "write your findings to X" and races its siblings. The fix for the
next round is structural — **one writer per file, and forks return text to their parent** — and it
goes in the round-4 brief.

The saving grace is that every lens that hit this caught it and said so unprompted. A round where
the sub-agents misbehave and the lens reports it is worth more than a round where they behave
silently, because it tells me which reports were re-verified and which were merely received.

---

# A CLIENT CAN SEND ONE MALFORMED FIELD AND GET FREE INFERENCE

`crates/busbar-llm/src/engine/attempt/assemble.rs:251`, verified by hand. busbar forces
`stream_options.include_usage = true` on streaming OpenAI-family requests so a usage frame always
arrives. The injection has one escape hatch:

```rust
let Some(so_obj) = so.as_object_mut() else {
    // `stream_options` present but not an object: leave the body untouched (the upstream will 400
    // on the malformed field; busbar must not silently reshape a caller's value).
    return payload;
};
```

**The whole defence rests on the comment's parenthesis — an assumption about a third party's
parser.** It holds for OpenAI proper. It does not hold for the OpenAI-*compatible* lanes this
product exists to aggregate (vLLM, Together, Groq, self-hosted), which routinely ignore an unknown
or wrong-typed field and serve the stream normally.

The chain, end to end:
1. client sends `"stream_options": "x"` — a string, not an object
2. busbar returns the payload untouched; **`include_usage` is never injected**
3. a lenient upstream serves the full stream
4. no usage frame arrives
5. busbar's own *"no usage frame ⇒ bill zero"* rule fires
6. **the entire per-token charge for real, delivered output is zero**

There is **no estimation fallback anywhere in the crate** — I checked; the only `fallback` in
`engine/usage.rs` concerns a latency value.

**This is client-triggerable, needs no special credentials, and is one JSON field.** It is the most
directly exploitable money defect in the audit, and the reasoning that permits it is written down as
a safety justification. That makes it the ninth instance of the release's dominant pattern: a
comment asserting a guarantee the code does not hold — here, a guarantee about someone else's server.

## Two more money splits on the LLM plane, both from the two-driver era

- **`unit/admit.rs:173`** — the new step-chain builds a request's `UsageSink` pool key from the
  **raw destination, before Route resolves it**; the legacy driver builds it **after**. So a
  bare-model request's spend lands in a **different accounting bucket depending on which of the two
  live drivers served it.**
- **`native_ingress.rs:602`** — MCP-sampling-triggered completions are hard-wired to the legacy
  `run_gauntlet` path and **can never reach the new durable ledger book** that direct HTTP traffic
  posts to by default. A permanent two-books-disagree gap on an entire traffic class.

**And the oracle that exists to catch exactly this is blind to both.** `unit/tests/chain.rs:458` —
the crate's own legacy-vs-chain money-parity check — tracks **only aggregate totals**, never
pool/bucket attribution, and **never touches the new ledger book at all.** A parity oracle that
compares sums cannot see a misattribution that preserves the sum.

*(Honest resolution of an open question I posed: `Metered.refund`/`Walk::refund()` and the live
`finish_admitted` refund compute the **identical** predicate today — same `charged` flag, same
response status. They are redundant, not conflicting. But they are uncoupled, so future drift would
be caught by neither suite.)*

---

# THE INSTRUMENTS, SECOND OPINION: 51 FINDINGS, AND TWO BLOCKING GATES THAT CANNOT FAIL

A second, independent opus lens over `xtask/src/gates/` — 92 files, 82,309 lines, all opened —
returned **51 findings (21 HIGH)**, converging with the first lens at the same HIGH count on the
same slice. It re-verified all 51 quotes after assembly and corrected three. Two results stand out
because both gates **block CI**.

**`ci_umbrella.rs:632` — the gate that decides whether CI can fail polices the ledger and never the
judge.** All fourteen rows are computed from the umbrella job's `needs:`, its `env.RESULTS` block
and the workflow's job/`if:` map. I checked what it actually reads: `RESULTS`. **Nothing reads the
umbrella's `steps:`** — and the steps are where the verdict lives (`ci.yml:3866-3871`:
`if [ "$fails" -ne 0 ]; then … exit 1`).

Exploit, stated by the finder and trivial: change that `exit 1` to `exit 0`, or replace the loop
with `run: true`. `RESULTS` is untouched — 28 well-shaped rows, every one in `needs`, all floors
clear — **the gate stays GREEN while CI's umbrella stops failing on anything.**

**`teller_steps.rs:913` — a blocking CI step prints its denominator and never checks it.** Verified
verbatim:

```rust
"ROOT-GATING: the shipped legs are {legs}; {considered} gating plane x step cell(s) are theirs to drive."
…
if red.is_empty() { println!("GREEN: every gating step on every shipped root leg…"); return 0; }
```

An empty shipped-leg set produces an empty `red`, so it prints GREEN and exits 0. Remove the five
`root-*` features from `Cargo.toml:192` and a blocking gate passes over nothing. **That is Law 8's
exact shape — a measurement that states its denominator and never proves it is non-empty** — sitting
in a step that gates promotion.

The rest of the 51 run the same way: `shared-route` scans a kind that does not exist (both lenses
found this); `ports-only`'s eight rows search for a **deleted crate's path spelling**;
`Excuse::ReleaseScript` proves a gate is named in a script **nothing runs**;
`field-inventory:both-directions` has a FAIL arm **no input can reach**; `reachability` scores an
**absent** unit module as PASS; `hot-path-*` matches `harness = false` anywhere in a manifest with
three bench blocks; and `ship-ready`'s posture dereferences a table it can never be in, which is why
`gate --all` is **permanently red** — the standing red this plan has been treating as debt is partly
a bug in the gate.

---

# CORRECTION: THE FIVE MCP CLIENT FAILURES ARE ONE DEFECT AND A BLIND HARNESS

I reported five hostile-server scenarios as client security defects — that busbar detects neither a
rug-pull, nor an undeclared capability, nor an outputSchema violation, nor a bad icon scheme, nor an
evil elicitation prompt. **That was the conformance suite's verdict, restated by me without going to
the code.** The round-3 lens went to the code. The picture is different and narrower.

**One is real, and its cause is a path asymmetry — verified by hand:**

`crates/busbar-mcp/src/mcp/tasks.rs` — the async SEP-2663 task-completion path contains **zero
references to `outputschema`**. The synchronous path validates correctly at `method.rs:2049`
(`super::outputschema::check(structured, schema)`). **So a task-capable tool with a published
`output_schema` gets no structured-output validation at all, while the identical synchronous call
does.** Same contract, two paths, one checked.

*(And a narrower one beside it: even the sync check only runs when `structuredContent` is present,
so an upstream that publishes a schema and then omits the field entirely escapes the MUST check.)*

**The other four did not reproduce.** Rug-pull digest-pinning, MRTR deny-by-default gating and the
outputSchema check itself all read as correct, well-engineered code on the HTTP client path. No
defect was found behind the undeclared-capability or evil-elicitation claims.

**The likely explanation is a harness that is wrong about the product it tests.**
`testing/mcp-conformance/` drives everything through an HTTP mirror, with at most one `tools/call`
per scenario, on the premise that busbar's MCP client speaks a single transport and has no child
supervisor. I checked: `crates/busbar-mcp/src/mcp/client/stdio.rs` is **582 lines of code with 31
spawn sites** — a real, production, config-validated subprocess supervisor. **The battery never
exercises it.**

So the honest statement is: **one confirmed product defect, and a conformance suite that cannot see
half the client's transport surface.** Both are items; neither is the five-headed security finding I
reported.

**Why I got it wrong, and the rule that follows.** A failing test is evidence that *something* is
wrong — it is not evidence about *what*. I treated five red rows as five defects because the rows
were named after behaviours. That is the same error as trusting a failure message over the code
(item 106) and trusting a grep over a positive control. **A red test names a symptom; the defect is
wherever the code says it is.** This is now the eighth instance in the release of prose asserting
something the code contradicts — and the first where the prose was a *test harness's* own
description of the system under test, which is the most dangerous place for it.

### The rest of the MCP round

| sev | where | finding |
|---|---|---|
| MED | `client/catalogue.rs:70` | the MCP `icons` field is unmodeled everywhere — client parsing, config grammar, server rendering. Harmless **today** only because busbar drops non-allowlisted upstream fields; no scheme gate exists if that changes |
| MED | `busbar-plane-mcp/src/claims.rs:118` | the plane's compile-time boot-collision `Claim` **hardcodes `/mcp`** while the real mount path is operator-configurable via `canonical_uri` — self-documented in the file's own header as unresolved |
| MED | `client/peer.rs:312` | on the stdio transport **every granted authority ask is unconditionally `Unsatisfiable`**, including `sampling`, which has a working satisfier on HTTP. Config validation never warns that `transport: stdio` + `grants.sampling` + a full sampling policy can never fire |
| LOW | `client/roots.rs:118` | `grants.elicitation: true` passes validation with no satisfier anywhere — an intentional, recorded waiver |

`peer.rs:312` is worth pairing with the harness gap: the stdio transport is both **the one the
conformance battery never drives** and the one where every authority ask silently fails.

---

# C2 IS REFUTED FOR A THIRD KIND: A DROPPED-IN PLANE CAN TAKE MONEY AND CANNOT GIVE IT BACK

Round 2 reported the `LEASES` map as "mintable over FFI with no forced release." Round 3 found the
sharper truth and I verified every part of it:

**The C-ABI `PlaneHostVtable` has no lease-release verb at all.**

- `busbar-plugin/src/hot/host.rs:722,724` declares exactly two slots: `cost_reserve`,
  `cost_settle`. The stub at `:983,984` has the same two. `plane_host/vtable.rs:121,122` wires the
  same two. **There is no `cost_close` slot.**
- The function exists — `cost_host.rs:118` `pub(crate) fn close_lease` — and is `pub(crate)`, so it
  is unreachable from the other side of the ABI.
- **Positive control, and it is the clincher:** `busbar-voice` *does* close its leases, at
  `runtime/metering.rs:218` and `:417`, via the `MeteringHost` **Rust trait**. Its own doc at `:179`
  says `cost_close` *"fires on ANY return path (including a panic unwinding through `run()`)"* and
  is idempotent.

So the compiled-in fold has a release verb, uses it, and is careful enough to make it panic-safe and
idempotent. **The dropped-in fold has no such verb in its vocabulary.** A dlopen'd plane can reserve
money and settle money and can never release a lease it opened.

**That is C2's third disjunct — "any kind whose two folds differ" — and it is money.**

### C2's scoreboard: refuted for three of the seven kinds

| kind | how the two folds differ |
|---|---|
| **export** | a dropped-in export plugin **cannot be named in a config file**; core hard-errors on any module name outside its four built-ins, and `open_export` has zero callers |
| **store** | `priced_from_ms` has no ABI adapter, so a dropped-in v2 store **merges a split day back into one row and prices it all at the midnight card** while the compiled-in store gets it right |
| **plane** | the vtable has **no `cost_close`**, so a dropped-in plane cannot release a money lease while the compiled-in path does so on every return path including a panic |

Three kinds, three different mechanisms, all money or configuration. The claim is not marginally
false; it is false in the ordinary case for nearly half the taxonomy.

---

## ROUND 3 RESOURCE: FIVE MEDIUM, AND AN HONEST NEGATIVE WORTH MORE THAN THEM

The lens read **49 previously-unread production files in full (18,580 LOC)** — exactly the set
round 2's own `unread` key named — split across six readers, with every finding re-verified by
direct grep before acceptance.

It was told to hunt a fifth instance of the confirmed *removed-not-marked* pattern. **It looked
across all six groups and reported it did not find one.** That is the most valuable sentence in the
report: a lens that returns a clean negative on a pattern it was primed to find is a lens whose
positives can be trusted. It found *close cousins* instead — uncapped rather than unmarked — and
said so rather than stretching the label.

| where | finding |
|---|---|
| `plane/handle_engine.rs:141` | **active (non-terminal) handles have no cap at all** — only the terminal population is bounded, so a caller that keeps handles perpetually touched grows the map without limit |
| `plane_host/dispatch.rs:121` | the durable work-handle registry is never swept **and its happy path never removes the entry either** — it leaks on the successful path as well as the abandoned one |
| `plane_host/trust_anchor.rs:37` | no cap or eviction, unlike its declared sibling `identity.rs`, **which was given a FIFO cap to fix this exact accumulation bug** |
| `plane_host/trust.rs:76` | the verify-freshness `fresh` map only grows; its siblings `leading`/`inflight` are cleaned up properly, and the primitive it is modelled on (`trust::verify::VerifyGate`) has a `retain()` this cache lacks |

Two of those — `trust_anchor` and `trust` — are cases where **the correct fix already exists beside
the defect**, applied to a sibling. That is the same shape as the two breakers and the two
`reserved_nanos` copies: the tree knows the answer and did not propagate it.

---

# THE BANK-AUDITOR ANSWER: THE BOOKS SHIP WITH NO WORKING TAMPER DETECTION

The money lens read **52 of 52 files** — every previously-unread test file in the three money crates
(14,689 LOC) plus the unread `usage/` and `cost/` sources. It was sent to find the sibling of
`residual`'s wrapping verifier. **It found that the sibling is not one checker. It is all of them.**

## Every money-side tamper detector has zero production callers

- `verify::verify` — the ledger verifier
- `CheckpointAnchor::anchor` — the checkpoint-vs-anchor rollback detector
- `recompute` / `recheck`
- `adjusting_entries` / `record_repricing`
- all four `AuditChain` verifiers
- **`AmendChain` — the entire amendment journal, which the design names as the ONLY documented way
  a recorded money figure may legitimately change**

I verified the two most load-bearing myself, and the check is subtle enough that a coarse grep
reports the opposite:

- **`AmendChain`: 9 non-test references, every one inside `busbar-kernel-audit` itself** — its own
  `pub struct`, its own `Default`, its own `impl`, two internal `digest_of` uses, and one
  `pub use` in that crate's `lib.rs`. **Nothing outside the defining crate names it.**
- **`verify`: `fn verify(` matches five places**, and four are unrelated trait methods on other
  types (`units_mcp.rs:664`, `units_voice.rs:1462`, `kernel.rs:959`, `:1132`). Only
  `ledger/src/verify.rs:113` is the ledger's, and nothing calls it. **Counting those four as
  callers would have produced the opposite conclusion** — the same "a name is not a thing" trap
  that has now cost this release seven false starts.

## The one live edit-detector is defeated by the person it guards against

`Checkpoint::body_hash_verifies` is wired (5 references). It is a **keyless SHA-256** over the body.
Anyone who can alter the stored figure can recompute the hash beside it. It detects corruption. It
does not detect tampering, which is the property the audit chain exists to provide.

**Stated plainly, because this is the question a bank auditor asks first:** the money books carry a
signed audit chain, an amendment journal, a checkpoint anchor and four verifiers — and **not one of
them runs.** The only live integrity check is one a tamperer updates as part of tampering.

## A 64-case money test that cannot fail

`settle_tests.rs:406` is a 64-case "byte-neutrality" sweep. It asserts the overdraft column against
`posted.overdraft()`, **which is `0` in all 64 cases — including the 28 where `used > reserved`**,
i.e. precisely the input that makes the books not balance. A sweep that asserts a constant over the
one variable that matters.

**And the coupling is real in production, not a fixture artifact.** `teller.rs:1323` spends the
hold by `run.meter.total()`; `teller.rs:1346` settles the settlement table's `amount`. Two different
numbers. So `Posted.overdraft` and `Posted.settled` derive from different figures, and `residual`
reads non-zero on **ordinary** ends — not just adversarial ones.

## The ten previously-documented items: all still true, two worse

Round 2 skipped ten money items because in-tree docs already carried them with citations. **A
citation is not a verification** — several findings this session were documented-and-stale — so they
were re-checked at HEAD. All ten hold. Two have got worse and are now items in their own right:

- **`MeterPolicyConfig` is never populated from configuration.** Both production sites pass
  `::default()`, so pool expansions and lane prices are permanently empty and the *"post the
  cheaper lane"* rule **degenerates to an alphabetical tie-break.** Lane selection is not choosing
  the cheaper lane; it is choosing the earlier letter.
- The **truncating multiply-before-divide tier arithmetic** is still live at
  `busbar-kernel/src/cost.rs:286`, under that file's own `allow(dead_code)`.

---

# STREAMING: A PHONE CALL THAT CANNOT WORK, AND IS BILLED ANYWAY

Three HIGH, two of them money, all verified by hand.

## The caller's audio is silently discarded, and the call still settles usage

`busbar-voice`'s telephony endpoint `/v1/realtime/telephony/{call_id}` is live, mounted, governed
and provider-dialing. It feeds raw client bytes straight into `OpenAiRealtimeCodec::read_up`, which
understands only OpenAI's `type`-tagged JSON. **Real Twilio Media Streams frames are `event`-tagged**,
so every one of them reaches the match arm I read at `ir/codec/mod.rs:667`:

```rust
_ => Vec::new(),
```

An unknown frame shape produces an empty event vector. Silently. **The caller's audio never reaches
the model.**

The purpose-built decoder that belongs in between exists and is tested — and has **zero production
callers**. I checked: `TwilioEnvelope`'s only two non-test references in the whole tree are its own
`pub struct` declaration and its own `impl` block (`topology/twilio.rs:154,156`). Positive control
on the identical search: `OpenAiRealtimeCodec` has **65**.

**The session still opens, still dials the provider, and still bills real settled usage** for
one-way model output on a call that cannot function. A customer is charged for a phone call in
which nobody could hear them.

## The session cost ceiling is documented, plumbed, and never compared to anything

`runtime/mod.rs:67` declares `session_max_secs` as *"the hard session wall-clock ceiling
(`streams.session_max_secs:`) **the pump enforces**"*, default 3600s. I traced every reference: a
doc comment, a field declaration, a default fn, a serde attribute, and four plumbing assignments.
**Not one comparison against elapsed time anywhere in the tree.**

The one thing named as its enforcer — the sweep loop at `session.rs:455-475` — only expires stale
tool-call waits, an unrelated concern.

**An operator who sets this ceiling to bound worst-case session cost gets no enforcement at all.**
On a plane that bills by the second, with the abandoned-session leak already confirmed, that is the
difference between a bounded and an unbounded bill.

## And the safety gate the dual-plane comment leans on cannot see the plane it is cited for

`busbar-plane-streaming/src/register.rs:28`'s own doc says the boot's cross-plane claim-overlap
check (`seal_claims`) is what makes co-registering `busbar-voice` and `StreamingPlane` safe.

`plane_claims()` (`busbar/src/root/registry.rs:241-261`) is built **only** from `PlaneMeta::CLAIMS`
on the five new planes. `busbar_voice::PLANE_DECL` uses an entirely different, older claim
mechanism and **is never imported into `registry.rs` at all.** The check structurally cannot see
busbar-voice's audience claim on `/v1/realtime`, so *"it would have tripped the overlap check"* is
false comfort for precisely the pair it is offered about.

---

## THE SHAPE THAT HAS NOW APPEARED SEVEN TIMES

A comment states a guarantee; the mechanism it names cannot deliver it; nothing re-checks the
claim. This is no longer an observation, it is the release's dominant defect class:

| the claim | the reality |
|---|---|
| `Cargo.toml:263` — `root-llm` "moved no cell, every money class byte-identical" | moves a money cell by 472,560,000 µ |
| `Cargo.toml:309` — `root-admin` "the whole 890-cell owed golden read the same either way" | moves two owed ledger cells |
| `field_inventory.rs:84` — a dialect registered with no schema "is a hard error" | all 7 rows stay PASS |
| `layout_golden.rs:596` — a resize "is a MAJOR airlock break — do not re-seed" | re-seeded on a minor bump |
| `conformance:freshness` — "a pass carried over from an older sha is RED" | passing at 647 commits stale |
| `runtime/mod.rs:67` — the ceiling "the pump enforces" | never compared to anything |
| `register.rs:28` — `seal_claims` makes the pair safe | cannot see one of the pair |

**Phase 1 gets one more item from this, and it is mechanical:** a comment that asserts a
measurement or a guarantee must name the command that reproduces it, and that command joins the
gate set. Seven instances is not carelessness by seven authors — it is a missing rule.

---

# C2 IS NOW REFUTED FOR A SECOND KIND — AND THE AIRLOCK BROKE ITS OWN FROZEN RULE

## The store kind: the two folds price a day differently

`priced_from_ms` is **money**. The engine makes it part of the metering accrual key
(`governance/state.rs:893,899`, verified), so a mid-day rate-card edit splits the day's cell in two
— which is the whole point of the field.

It was added to `MeteringDelta`/`MeteringRow` with `#[serde(default)]` and **no `abi_version`
bump**, while `supported_abi("store")` still admits v2 — *every published first-party store*.

So a dropped-in v2 store has no such column, merges both halves back into one row, and returns
`priced_from_ms: 0`. **The admin read then prices the whole day at the midnight card.** The
compiled-in store (`store-memory`) keys on the field correctly.

**Same plugin kind, two folds, two different invoices.** That is C2's third disjunct — *"any kind
whose two folds differ"* — and it is money.

The positive control is damning, and I checked it: `legacy_usage_wire()` guards **5** call sites
(`put_usage`, `add_usage`, `get_usage`) and is **absent from `add_metering`**. The adapter pattern
exists, is used three times beside this, and was not applied to the one that carries a price.

## The airlock: a MINOR bump resized an interior field, and nothing can refuse it

`ABI_MINOR` 20→21 changed `BuildCtx.host_ctx` from `*mut c_void` (8 bytes) to a `#[repr(C)]
HostCtx` (16), shifting `config_ptr`, `config_len`, `resolved_refs_ptr` and `resolved_refs_len` by
8 bytes. `ABI_MAJOR` stayed at 1.

Verified, both sides:
- **`abi_minor` is compared in ZERO places.** Positive control: `abi_major` is compared in one
  (`lib.rs:141`). `check_preamble` refuses on magic and major only.
- **The repo's own layout golden records the resize** across `ec21d27f0`: `config_ptr 32→40`,
  `__size 64→72` — in the same commit that bumped only the minor.
- **And the golden's own assert message states the rule that commit broke.**
  `crates/busbar-plugin/tests/layout_golden.rs:596`: *"If it is a REORDER/RESIZE/INSERT, that is a
  MAJOR airlock break — do not re-seed…"*. It was re-seeded.

**Consequence:** a minor-20 plane passes every check and then reads its config pointer out of
`HostCtx`'s generation and kind bytes. That is memory corruption across the plugin boundary, on the
seam C2 exists to guarantee.

**Stated honestly, as the finder did:** `open_plane` has no in-tree production caller yet, so this
is latent rather than live. It is still a hard blocker, because the whole of Wave 5 is about giving
it one — landing that caller without fixing this ships the corruption.

**The remedy is structural, not a version number.** `abi_minor` must be compared by something, and
the layout golden must refuse a re-seed whose diff is a reorder, resize or insert rather than
printing a sentence about it.

## Five MEDIUMs, all the same shape the round-2 brief predicted

Every one is "a field was added and the other side never reads it" — the pattern proven twice in
round 2 and now found five more times: the hook half of the endpoint ABI (`HookRequest::Routes`/
`Endpoint` — the SDK answers, `DlopenPolicy` never asks, while `export.rs:137` *does*);
`SecretRequest::deadline_ms` (forwarded by the SDK, hardcoded `None` by the one host producer); the
#85 observability envelope (universal by declaration, `drain_observations` on `ExportHandler`
alone); `PlaneDecl.admin_routes`/`openapi` (no slot reader in `DynPlane`, while the compiled-in
native equivalents *are* called); and the log bridge, which `ffi_thread.rs:58` claims runs "per
load" and is emitted by neither `export_plane!` nor `wire_up_plane`.

---

## KERNEL: AN OPERATOR CAN CLEAR A QUARANTINE AND BE TOLD IT WORKED WHEN IT DID NOT

Two more from the kernel subsystems no round-2 lens was pointed at (confirmed unread first, by
parsing all 19 round-2 files for path matches).

**`trust/mod.rs:376` — `Approval::approve` walks only the OBSERVED capability set, so the `removed`
drift axis is never settled.** An A2A agent whose upstream drops a skill goes `Quarantined`; the
operator calls `POST /agents/{id}/approve`, gets **200 `OUTCOME_APPLIED`**, and the agent is **still
undelegable, permanently.** The only API that clears that axis — `reject_capability` — has zero
production callers.

**`export/webhook.rs:87` — tightening a disclosure bound silently does not apply.** Request-log
sinks (URL, auth header, and the resolved `Projection`) are `OnceLock`-frozen by the single boot
call to `export::configure`. The live `PUT /admin/export/{name}` route reports a restart requirement
only for new plugin *route paths*. So adding, re-pointing, or **narrowing** a sink answers "applied
live" and takes effect at the next restart — and `projection.rs` defines a projection as a
disclosure bound. An operator narrowing what leaves the building is told it worked.

---

# THE INSTRUMENTS AUDIT: 21 HIGH, 100% COVERAGE — AND THE MONEY GATE CANNOT SEE THE MONEY DEFECT

The instruments lens read **90 of 90 files, 78,680 of 80,042 lines** — the only lens in the audit at
full coverage — and it **built and ran** the gates rather than reading them, which turned four
findings from argument into measurement. **43 findings: 21 HIGH, 22 MEDIUM.** Every quote re-checked
against `file:line`, zero mismatches.

This is the most consequential slice in the release, because every other green on record is reported
by this code.

## THE ONE TO READ FIRST: the money gate is blind to the money defect, three ways over

`no-float-money` carries a row whose title is *"a count is read with a silent zero default instead
of a refusal (#81)"*. The codec audit independently found exactly that defect —
`read_count_u64(..).unwrap_or(0)` billing an unreadable token count as $0, in all six dialects. The
row cannot see it. I verified each link:

1. **Wrong vocabulary.** `NUMBER_ACCESSORS` is `["as_u64","as_i64","as_f64","as_u128","as_i128"]` —
   raw `serde_json` accessors. The codec reads through a **named helper**,
   `crate::usage_count::read_count_u64`, so no accessor appears at the call site.
2. **The helper's own file is excluded.** `SUPERSEDED_SEAM` is literally
   `crates/busbar-llm-codec/src/usage_count.rs` — the gate skips the one file where the accessor
   does appear.
3. **The helper is the correct one.** `read_count_u64` *refuses* an unreadable count by returning
   `None`; it is the `.unwrap_or(0)` at the call site that converts that refusal into free money.
   The gate is looking for a bad read, and the read is good — the discard is the defect.

**So the single row that exists to catch "#81: never a silent zero" is structurally incapable of
seeing the release's clearest instance of it.** That is Law 0 landing on the money path, and it is
the strongest argument in the plan for why Phase 1 precedes Phase 2.

## THE FOUR THAT WERE MEASURED, NOT ARGUED

| | |
|---|---|
| `conformance_sync/render.rs:310` | `conformance:freshness` claims *"a pass carried over from an older sha is RED"* — and **derives the release commit from the thing it is judging**. Measured live: a row passing over ten verdicts **647 commits stale** |
| `construction/selftest.rs:1052` | **both `forbid-unsafe` RED proofs come back GREEN when run.** The plant strips `#![forbid(unsafe_code)]` from **every held crate of the kind**, and every covered row stays green. The gate cannot detect its own total defeat |
| `construction/selftest.rs:539` | the RED proof covering the **entire fourteen-row LOC-ceiling battery** demands a `naming` string that appears nowhere in the gate's output |
| `construction` loc-ceilings | two rows pass over **subjects that do not exist** |

## THE REST OF THE 21, BY SHAPE

**Denominator is the subject** — `teller_steps.rs:151`: *every* population (plane roster, step
roster, cell denominator, root-leg set) is read out of `qa/teller-steps.json`, **the very file under
judgement**. Law 8 in its purest form.

**Empty denominators** — `inventory_coverage.rs:88`: **31 of the 40 rows** of the "40-row coverage
matrix" are bound to an empty family list and return `UNMAPPED` on every possible input.
`kind_isolation.rs:2734`: the banned-needle list is populated for **3 of 21 kinds**.
`kind_isolation.rs:3236`: a refusal iterates crates of kind `"control"`, which is not one of the 21
kinds, so the population is always empty. `design_bindings/tables.rs:620`: the only path by which a
binding's NOTE can refuse it reads an **empty static**.

**Rules that `continue` past their own subject** — `config_schema/classify.rs:313`: the arm catching
`deny_unknown_fields` or `transparent` being flipped — *which operator documents parse* — `continue`s
on **every node, every flag, every run**. `:275`: the arm catching a hand-written `Deserialize`
quietly accepting input it used to reject returns early whenever the baseline node has no refusal.

**Scan roots narrower than the invariant** — `structure_lint/plane_store.rs:88`: *"a plane never
holds the power to forge the audit chain"* judges only `busbar-kernel/src/plane`, and a production
plane trust-state sink lives outside it. `structure_lint/census.rs:109`: a row existing to catch *"a
SECOND spelling of a wire word"* is scoped to a tree that excludes the directory where the second
spelling sits.

**Selftests that cannot bite** — five cases demand offender strings **no rule in the gate can
emit**; four plant into `crates/busbar-plane-admin/`, a directory **with no `Cargo.toml`**, which
every relevant rule keys on; two plant dependency edges onto **packages that do not exist**; one
plants an empty overlay that `inert_plant` refuses before the gate ever runs.

**And `plane_abi_neutrality.rs:233`** derives the tree's plane keys by grepping `pub const
PLANE_DECL` — **a grammar zero `busbar-plane-*` crates use.**

## WHAT THIS DOES TO THE PLAN

Phase 1 was sized at ~16 agents against items 88/89/90 and the C1 kind hole. It is now **21 HIGH +
22 MEDIUM on top of that**, and several are structural (a trait signature, a scan root, a
denominator source) rather than a missing test case.

**It also settles the ordering argument beyond debate.** Wave 0's preamble said *"nothing below this
line can be trusted until this is done."* That was a judgement; it is now a measurement. The money
gate cannot see a confirmed money defect, the unsafe gate cannot see its own removal, and a
freshness gate is 647 commits stale while reporting PASS.

---

# THE C1 SHARP TEST HAS BEEN RUN. C1 IS FALSE — AND THE FAILURE IS CHEAP.

Part 0 calls add-a-dialect-then-diff *"the command that confirms or falsifies the entire premise."*
It has now been run. A seventh dialect (`mistral`) was built end to end, and **the shipped binary
reports it**: `must be one of: anthropic, openai, gemini, bedrock, responses, cohere, mistral`.
That string comes from `known_protocols()` after the composition root folds `DECLS` — the same
registry the six live in. `cargo build --workspace` green. The dialect is real, not a no-op.

**Verdict: 17 files — 10 plugin, 7 CORE. C1 is false.**

### But read the shape before reading the number

- **Phase A** — the codec, reader, writer, handler, registration and arrival wiring: 11 files, only
  1 of them core. Across **596 gate rows, exactly 2 changed**, and both are the same rule
  (`legacy-reach`, a *symbol-count ratchet on the composition root*).
- **Phase B** — all 17 files: **0 rows changed.** Row-for-row identical to baseline.

**So `gate --all` notices a seventh dialect through exactly one rule out of 596.**

**The code side of C1 is nearly true.** The codec, reader/writer/handler, arrivals and metering
table all live in the plane's own crates. What breaks C1 is not architecture — it is **pinned
census**: seven hand-maintained rosters of "the six dialects" and five hand-typed *counts* that one
new claim rung renumbers. Every one of them is a number somebody typed. **That is a data-modelling
problem, not an architecture one, and it is far cheaper than the verdict sounds.**

### The 7 core files a dialect forces

| file | what forces it |
|---|---|
| `busbar/src/root/units_llm.rs` | the root's `BODY_INGRESS`/arrival twin must spell every dialect |
| `busbar/src/root/tests/registry.rs` | **6 pinned counts + a pinned 50-entry sealed-claim order**, and two test functions whose NAMES encode counts had to be renamed (`…forty_eight_claims` → `…forty_nine…`) |
| `qa/construction.toml` | `legacy-reach` ratchet 17→18 — **a dialect addition is structurally a ratchet VIOLATION**, on a rule whose own doc says the total "may go DOWN and never up" |
| `xtask/src/gates/field_inventory.rs` | `const DIALECTS: [&str; 6]` |
| `qa/field-schemas/<d>.json` · `qa/field-inventory.json` · `qa/field-coverage.missing` | a new schema, a regenerated artifact (+285 lines), 40 new pinned field ids |

Plus, forced by other CI jobs and not yet landed: the shadow-oracle golden cell
`boot.refusal__BOOT-020__validate.json` (which pins that stderr byte-for-byte and now diverges,
needing an `accepted-differences.json` entry), `conformance/{registry.toml,manifest.json}`,
`proof-manifest.py` (**four** rosters), `proto-deletion-gate.sh`, `plane-grep-gate.sh`,
`assets/readme/data.json` and the README's 6×6 matrix, and the kernel's own
`proto/tests/registry_builtins.rs`.

### FOUR INSTRUMENT DEFECTS THE TEST EXPOSED, each with a positive control

1. **`field-inventory` advertises the guarantee it does not provide.** Its doc at
   `field_inventory.rs:84` says a dialect *"registered with no schema file is a hard error rather
   than a silently smaller inventory."* In Phase A the dialect was fully registered and printed by
   the shipped binary **with no schema file — and all 7 rows stayed PASS.** The row claims the two
   sets are "EQUAL, both ways"; **both are hardcoded consts and neither reads the registry**, so it
   cannot see the half it calls load-bearing. (The inverse works: schema-without-registration reds
   all 7.)
2. **`plane-purity:dialect` cannot see a new dialect.** Planting a literal in
   `busbar-kernel/src/lib.rs`: clean **17 hits**, `"cohere"` **18** (caught), `"mistral"` **17**
   (invisible). `lean-core` behaves identically: 14 / 15 / 14. Both carry their own `DIALECTS[6]`.
3. **The test written to pin the operator-visible order is a PREFIX match.**
   `cli_validate.rs:1035` is `all.contains("must be one of: anthropic, …, cohere")`, so appending
   `, mistral` **passes**. The one instrument that does catch it is a shadow-oracle golden cell in
   a different CI job.
4. **`cargo xtask gate field-inventory --write` does not exist** — and it is the command printed in
   the gate's own FAIL message AND in the generated artifact's header. `cli.rs:290` has `--write`
   arms for `design-bindings` and `construction` only; everything else exits 2. The artifact had to
   be regenerated by re-implementing `render()` by hand.

**Item 5 is answered.** It estimated "10 dialect rosters". Measured: **7 rosters + 5 hand-typed
counts**, enumerated above. That list IS the C1 worklist.

---

## ROUND 3 — first return: a task waiting on a human is killed at 24 hours

`crates/busbar-a2a/src/taskstore.rs:308` — the retention sweep's abandonment ceiling **durably and
irreversibly cancels any non-terminal task idle for more than exactly 24 hours**, including a task
PAUSED in `input-required` or `auth-required` — i.e. one that is idle precisely because it is
waiting for a person to answer.

The failure is ordinary rather than exotic: a request for human input raised on a Friday afternoon
is cancelled before Monday, and the cancellation is durable, so the answer arrives to a task that
no longer exists. A2A's whole point is long-running delegated work with humans in the loop; a
24-hour hard ceiling on "idle" treats waiting-for-a-human as abandonment.

---

## RESOURCE: SIX MORE HIGH, INCLUDING A LEASE MINTED OVER FFI WITH NO RELEASE

The resource lens re-ran with real coverage (66 of 122 production files, 49,852 LOC) and returned
**14 findings, 6 HIGH** — up from 2 on the thin first pass. Every quote was verified against the
pinned tree by the reporting agent before it reported.

| sev | where | finding |
|---|---|---|
| HIGH | `busbar-kernel-egress/src/attempt.rs:354` | **`Transport::close()` is called only on the success path.** Every non-success exit leaks the dialled connection — an OS child process on stdio, a live task and socket on gRPC/WS. A flapping upstream leaks one process per failed attempt |
| HIGH | `busbar-kernel/src/plane_host/egress.rs:727` | the documented "bounded timeout" backstop ends in an **unbounded `join.join()`** on a thread that can wedge in DNS resolution. The bound is on the wrong side of the join |
| HIGH | `busbar-kernel-egress/src/trust/net.rs:633` | the `Resolver` trait **has no way to carry a deadline at all** — so the timeout above cannot be fixed at the call site; it needs a trait change |
| HIGH | `busbar-kernel/src/plane/handle_engine.rs:842` | a per-handle mutex is **held across an untimed store round-trip**, amplified by an inline sweep under the same guard |
| HIGH | `busbar-kernel/src/plane_host/trust.rs:76` | `VerifyCache.fresh` is a **process-global, insert-only cache with no eviction** |
| **HIGH** | `busbar-kernel/src/plane_host/cost_host.rs:41` | **the `LEASES` map is mintable over the FFI boundary with no forced release.** A plugin can take a money lease and never give it back, and nothing reclaims it. This is the hold protocol's defect at the ABI — the fourth distinct place the same shape appears |
| MED | `plane/handle_engine.rs:143` | `max_retained` bounds terminal handles only, never active ones |
| MED | `plane_host/journal.rs:1015`, `:463` | unbounded per-scope journal accumulation, plus an ABI shape that can disable the cap |
| MED | `plane_host/dispatch.rs:211` | the durable work-handle registry has exactly **one** removal site, unreachable for a zero-TTL or never-resumed handle |
| MED | `plane_host/identity_admit.rs:39` | same mint-over-ABI / no-release-verb shape as `cost_host`: `take()` is `pub(crate)` and not exposed over the ABI |
| MED | `ingress/duplex_ws.rs:79` | the WS reader's only await has **no timeout and no idle or ping liveness check anywhere in the codebase**, so a silent-but-open peer pins the task and its state indefinitely |
| LOW | `plane_host/trust_anchor.rs:37` | the unbounded-registry bug its sibling `identity.rs` already needed a fix for — latent today |

**The pattern, now at four instances:** a resource is minted and the release path is either absent,
`pub(crate)`, or on the success branch only. `cost_host`'s `LEASES` is the money one.

### A process note worth keeping, because it is how a wrong answer was avoided

Two of that lens's forks misbehaved — nested forking failed inside them, so instead of reporting
their sub-slice each re-ran large parts of the whole task and wrote **directly to the shared output
file**, racing each other. The lens did not take the surviving file at face value: it re-verified
all 12 quotes against the pinned source (all matched), re-checked the severity downgrades against
real reachability, found that `busbar-kernel-egress/src/trust/` had never actually been read
(one fork admitted covering 330 of 1,701 lines), and independently added two findings itself.

That is the correct response to a subagent, and it is the same rule the house method states for
greps: **an agent's report is a lead, not a fact.** Had it accepted the file, the round would have
shipped a confident report over a slice nothing read.

---

## SECURITY: THE DATA PLANE FAILS OPEN, AND THE SPEND LANDS ON `anonymous`

`crates/busbar-kernel/src/auth/mod.rs:1868`, verified by hand:

```rust
let bindings = app.role_bindings.get(&module);
let gov_key = resolved.or_else(|| synthesize_principal_key(&principal, bindings));
if gov_key.is_none() && !principal.roles.is_empty() && bindings.is_some() {
    return Err(IdentityRefusal::NoGrant);       // <- three conditions, not one
}
Ok((AuthPrincipal(Some(principal)), GovCtx { key: gov_key }))
```

The refusal requires **all three** of: no governance key resolved, the principal carries roles, and
the module has a `role_bindings` table. So an IdP-authenticated caller who carries **no roles**, or
who arrives under a module with **no bindings table**, satisfies the first condition and fails the
other two — and is admitted with `GovCtx { key: None }`.

`key: None` downstream means **no pool ACL, no budget, and spend booked to the `anonymous` actor.**

The correct guard is the first condition alone: if no governance key resolved, refuse. The two
extra conjuncts only weaken it. This is simultaneously an authorization fail-open and a money
defect — unbudgeted spend, attributed to a principal that is not the one who spent it, which is
exactly the attribution a bank auditor checks first.

The finder read all four out-of-slice call sites to establish the consequence rather than assume
it, over a denominator of 99 files / 34,430 LOC with 49 read line-by-line.

---

## ROUND 2 — the remaining confirmed findings

Recorded compactly; each carries `file:line` and a positive control in
`~/.cache/busbar-codeaudit/findings/`.

| sev | where | finding |
|---|---|---|
| HIGH | `busbar-transport-http/src/transport.rs:128` | `HttpTransport::dial()` accepts any URI authority — **no IP-literal enforcement**, unlike sibling `busbar-transport-tls` which requires `authority.parse::<SocketAddr>()`. hyper's `HttpConnector` re-resolves a non-literal host at connect time **and on every pooled reconnect**, so this is a DNS-rebinding / TOCTOU SSRF backstop gap. A real construction site populates it from configured lane data (`busbar-llm/src/unit/route.rs:198`) |
| HIGH | `busbar-transport-grpc/src/client.rs:76`, `server.rs:88` | the HTTP/2 connection preface has **zero timeout** on both dial and accept, where `tls` has `HANDSHAKE_TIMEOUT` and `ws` has `HANDSHAKE_BUDGET` — an unbounded slow-loris vector |
| HIGH | `busbar-kernel-ledger/src/identity.rs:83` | `residual`, the books-balance verifier, was left out of the saturating-guard conversion that `settle.rs` and `totals.rs` announce in their own headers. No `overflow-checks` in release ⇒ it **wraps** on exactly the pinned columns those files produce, and a maximally broken book verifies clean |
| MED | `busbar-transport-http/src/transport.rs:275` | no timeout bounds `client.request(req).await`; only TCP connect and HTTP/2 keepalive are bounded, so an HTTP/1.1 upstream that hangs after connecting is unbounded at this layer |
| MED | `busbar-kernel-scope/src/lib.rs:207` | the live admin authorization gate is a verbatim port of the 1.5.5 66-operation matrix, never extended for the 1.6.0 verb table. It disagrees with `busbar-core-admin`'s own `required_scope()` on exactly one row — `POST /admin/verify`, deliberately read-only at `verb.rs:719`, computed as `Full` by the gate. **Fails closed** (denies rather than over-grants), so it is a correctness defect, not a hole |
| MED | `busbar-mcp` `method.rs:1174`, `admin_view.rs:369` | both `quarantine_settle` call sites discard the durable write with `let _ =`. If that write fails as a rug-pull quarantine (or an operator's clearance) is decided, and the process restarts before the next successful settle, **the demotion silently reverts** with no error surfaced |
| MED | `busbar-a2a/src/a2a/receive.rs:2804` | the streaming relay swallows a failed durable write of the artifact resume cursor with a bare `let _ =` and no logging, while the structurally identical `transition` failure six lines above is logged with an error-once latch. `tasks/resubscribe` reads that cursor, so a store hiccup causes duplicate artifact redelivery with no trail |
| MED | `busbar-kernel-budget/src/decide.rs:283` | the concurrent-admission CAS loop, documented as guaranteeing racing admissions cannot jointly overshoot a cap, has **zero multi-threaded test**. Positive control: `HoldCell::take` and the breaker's single-flight probe both have real `thread::spawn` race tests |

### What the round REFUTED, which matters as much

- The egress pool's 8 `.expect()` mutex sites are a real idiom outlier (the house pattern
  `lock_recover` has 45 call sites) but **no panic is reachable** in any of the five critical
  sections: the one inner `.expect` is guarded two lines up, both `unreachable!()` are provably
  dead, the `Instant + Duration` overflow needs an operator-settable timeout that does not exist,
  and every `tokio::spawn` is inside a runtime. Downgraded to LOW. *Worth keeping because the blast
  radius, if one ever became reachable, is asymmetric:* `deliver_dial_outcome` disarms `DialGuard`
  only **after** taking the lock, so a panic at acquire leaves the guard armed and leaks
  `inflight_dials` permanently.
- Redirects are never followed by the HTTP egress client — structurally, not by policy.
- Request-smuggling defences in `raw.rs` are thorough (ambiguous CL/TE, obs-fold, non-final chunked
  all rejected); TLS verification and SPKI pinning have no production bypass; WS and gRPC
  compression are disabled at the dependency level, so there is no decompression-bomb surface.

---

## TWO WRITTEN BYTE-NEUTRALITY CLAIMS ARE FALSE — and each was the justification for a default

A 2×2 factorial over `root-admin` × `root-llm` isolated the C3 divergences perfectly — additive,
no interaction. Both features are default-ON, and the comment beside each says the measurement that
authorised that default. **Both measurements are false at HEAD, and I verified both comments myself.**

| cell | 1.5.5 | neither | `root-admin` | `root-llm` | both |
|---|---|---|---|---|---|
| `billing\|rate-card\|history-mid-window` (`spend_final`) | 750,090,000 | 750,090,000 ✓ | 750,090,000 ✓ | **277,530,000 ✗** | **277,530,000 ✗** |
| `ledger\|amend\|adjusting-entries` | 404 | 404 ✓ | **403 ✗** | 404 ✓ | **403 ✗** |
| `ledger\|amend\|refused-unsigned` | 404 | 404 ✓ | **403 ✗** | 404 ✓ | **403 ✗** |
| `ledger\|rate-history\|as-of` | 200 | **400 ✗** | **400 ✗** | 200 ✓ | 200 ✓ |

**154 — `root-llm` moves money, and its own comment says it does not.** `crates/busbar/Cargo.toml:263`
reads *"the whole owed golden was re-run with it on and moved no cell — every money class
byte-identical, the llm, billing, route-failover and admin families at zero. That last measurement
is the one the default was waiting for."* Measured: it moves `billing` by **472,560,000 micro-units
on a single read.**

**155 — `root-admin` moves two owed ledger cells, and its own comment says it does not.**
`Cargo.toml:309` reads *"the 234 owed `admin.ops` cells and the whole 890-cell owed golden read the
same either way, which is the measurement the default was waiting for."* Measured: both
`ledger|amend` cells move 404→403, `not_found`→`forbidden`.

**156 — `root-llm` is load-bearing for CORRECTNESS, and the repo's own LLM-only gate builds without
it.** Without `root-llm`, `ledger|rate-history|as-of` answers **400 `invalid_request` where 1.5.5
answers 200**. And `scripts/proto-deletion-gate.sh:606` is
`MCP_KEEP="auth-admin-tokens,hooks-ranking,proto-llm"` — **`root-llm` is absent**. So that gate
builds, and passes judgement on, a binary with a broken rate-history endpoint.

### The shape, named — because it is not Law 8 and it is not Law 0

These are **measurements asserted in prose, recorded as settled, and re-run by nothing.** Each
comment states a denominator honestly (890 owed cells, 234 `admin.ops` cells) and says the
measurement was performed. Neither is wrong about what it measured; both are wrong about the tree
as it stands, because **the tree moved and no instrument re-ran them.** A fact written into a
comment is a snapshot with no expiry date on it.

**The remedy is mechanical and belongs in Phase 1:** any comment that authorises a default by
citing a measurement must name the command that reproduces it, and that command joins the gate set.
A default justified by an unreproducible measurement is a default justified by nothing.

### PARK-P1 IS SETTLED, DEFINITIVELY

`1.6.0-parity-unblock.md` PARK-P1 offered two explanations for the rate-card cell and declined to
choose: a real pricing change, or clock skew. **The `root-admin` build reproduced the golden's
three figures exactly, ten minutes after the LLM-only build diverged, on the same host.**

*Clock skew cannot be switched off by a Cargo feature.* It is the code path. Supporting fact
already recorded: no cell in the corpus pins a clock, 0 of 2,318.

---

## C2 IS REFUTED — the export kind cannot be dropped in at all

Confirmed independently by two lenses that did not know about each other, each with positive
controls, and corroborated by a doc already in the tree.

C2 says *"every plugin compiles in OR drops in — same contract, same loading path"* (Part 2 #2,
the one universal rule). For `kind: export` the drop-in fold does not exist:

- `config/mod.rs:1983` — `resolve_export()` matches `def.module.trim()` against the four built-ins
  in `sections.rs:435` and **hard-errors at boot on anything else.** There is no fallback arm
  delegating to the plugin registry, so an `export.<name>.module:` naming a dropped-in plugin
  cannot be written in a config file at all.
- `plugin-loader/src/registry.rs:398` — `open_export` has **zero callers anywhere in the tree.**
  Positive controls under the identical search: `open_auth`, `open_hook`, `open_secret` and
  `open_store` all have real production call sites.
- `migrate_export.rs` carries the same closed vocabulary, so the migration tool is blind the same way.
- `plugin_routes.rs:115` — `ExportDispatch` is `#[allow(dead_code)]` and never constructed.
- `docs/design/1.6.0-config-surface.md:888` **already flags this exact gap**, as
  "DOCUMENTED-NOT-IMPLEMENTED". It was known and never became an item.

So the compiled-in fold serves four modules and the dropped-in fold is unreachable by construction.
**That is not a gap in a ratchet; it is the claim being false for one of the seven kinds** — and
the instrument that should catch it is the same `INSTANCE_VOCAB_KINDS` hole that hides the C1
breach. One root cause, two refuted claims, recorded once in `BUSBAR-1.6.0.md` Part 1.

---

## NOT ON THE LIST — found by running the tests, not by reading them

The full-workspace run produced **27 unique failures: 13 real defects across 6 root causes**,
2 stale tests, 12 harness. **Four of the six root causes were on no item.**

| finding | evidence | why it matters |
|---|---|---|
| **`busbar-contract` declares a `test-seal` feature**, breaking the one-surface invariant (landed in `217b21cc0`) | 5 tests, incl. `the_crate_declares_no_features`; takes `feature-sets` RED on the real tree, which cascades into 3 more | **largest single failing cluster in the release, and `grep -i test-seal` on the todo returned 0 hits** |
| **`qa/instance-noun-neutrality.toml:2753` names a path the tree no longer has** — `engine_kit.rs` is now a directory | the `[[leak]]` row scans **zero files and PASSES** | a textbook false zero, inside the C1 census |
| **the walk floor has lost its own proof** — `walk(min_files=1)` on an empty set returns `WalkError::Empty`, not `BelowFloor` | `infra.rs:192` | the `min_files` floor is the single most-repeated safety device in the gate library, and it is now unproven |
| **the audit-register trio** read a file this session deleted | `cli.rs:90`, `readers.rs:59`, `:106` | **mine — fixed in `2a0a832ff`**, with the writer's byte-for-byte parity proof repointed rather than dropped |
| `cargo test -p busbar-voice --features runtime` **does not compile** — 12 × `E0407` in `testkit/fixture_host.rs` | verified on a clean worktree, so committed | the Voice workflow's `boot-validate` job runs this verbatim; `cargo test --workspace` never sees it because it builds default features only |
| **5 MCP client-role hostile-server findings** | `CLI.HOSTILE.{EVIL-ELICITATION-PASSWORD, MRTR-UNDECLARED-CAPABILITY, RUGPULL-SCHEMA-CHANGE, OUTPUTSCHEMA-VIOLATION-DETECTED, BAD-ICON-SCHEME}` | busbar-as-client does not detect a rug-pull, an undeclared capability, an outputSchema violation, a bad icon scheme, or an evil elicitation prompt. **Security, and 0 hits on the todo** |
| **`llm-conformance` is red on EVERY branch** | pinned Gemini discovery doc drifted upstream (`836bf6ea…` → `fe47cc95…`); `validate.py` then refuses to judge anything | one `vendor.sh --repin gemini` away, after reviewing the upstream diff. Note the drift DETECTOR is the one case that passes — it works; everything downstream of it is starved |
| **the voice `tool-reply` leg is run by no workflow**, and the anti-vacuity lint structurally cannot see it | `verdict-covers-every-leg.py` checks the verdict job depends on every *job*; a leg with no job at all is invisible | Law 0 again: the coverage lint's denominator is jobs, not legs |

### And the best-diagnosed harness bug of the round

**8 × `root::units_llm::tests::*` fail only when `root::plane_decision` runs in the same process** —
including under `--test-threads=1`, so it is ordering, not concurrency. Root cause:
`plane/registry.rs`'s `TEST_MEMO` is keyed on `(installed_len, registered_len)`, so **two different
registration sets of equal size alias.** A decision-only fold cached under `(N,1)` is served to a
later `registered = [llm]` — also length 1 — the LLM plane vanishes from `plane_decls()`, and every
route 404s.

The comment above that memo reasons carefully about why a *sum* would alias and why the *pair*
fixes it. **The pair does not fix the equal-length case, which is the one that bites.** The tests'
own comments say they were written to catch exactly this collapse — *"green on six identical 404s
and prove nothing about the loop at all"* — so the suite anticipated the failure mode and the
cache defeated it.

---

## THE MONEY AUTHORITY WAS FLIPPED WITHOUT THE PROOF THAT GATES IT — and it likely explains the C3 money cells

This is the top finding of the session. Every step verified by hand.

**Decision #29 says the per-plane money-authority flip onto the unified kernel loop lands only when
that plane's money family is fleet-box oracle-green.** Four places in the tree state the flip has
not happened:

- `main.rs:635` — *"DORMANT — it registers ZERO planes, so every gauntlet/session path stays on the
  substrate loop, byte-identical."*
- `gauntlet_install.rs` module doc — *"DORMANT: `install` registers ZERO planes."*
- `a2a_kernel_rider.rs:16` — *"DORMANT — DUAL-PATH, MONEY AUTHORITY NOT FLIPPED."*
- `units_llm.rs:1931` — *"IT IS DORMANT. Nothing mounts it, and it is NOT the shipped authority."*

**All four are false.** `install()` is called unconditionally at `main.rs:638` and its body flips
all four planes:

```rust
pub fn install() {
    #[cfg(feature = "plane-mcp")]  flip_one_shot_to_kernel(busbar_mcp::PLANE_KEY);
    #[cfg(feature = "plane-a2a")]  flip_one_shot_to_kernel(busbar_a2a::PLANE_KEY);
    #[cfg(feature = "proto-llm")]  flip_one_shot_to_kernel(busbar_llm::PLANE_DECL.key);
    #[cfg(feature = "plane-voice")] flip_session_to_kernel(busbar_voice::PLANE_KEY);
}
```

Every one of those `cfg`s is **on by default** (`busbar/Cargo.toml:192`).

**The mechanism, which is what makes the "dormant" claims sound plausible and wrong.** The riders
say *"the shipped authority stays `run_gauntlet`"* — true, and irrelevant, because `run_gauntlet`
**is** the seam. `plane_host/mod.rs:2394`:

```rust
let selected = plane.capability_key().and_then(session_runner);
if let Some(runner) = selected { return runner(req, plane); }
```

`flip_*_to_kernel` populates exactly that registry. So calling `run_gauntlet` now dispatches into
the kernel loop for all four planes. The same file confirms the other half at `:2398`, referring
to *"the (now-deleted) substrate `admit_open`"* — **the substrate loop this plan believes is the
live money path no longer exists.**

### Why this matters more than a stale comment

`gauntlet_install.rs`'s own function doc asserts the flip is *"proven byte-identical (the
shadow-compare rider tests + the fleet-box oracle, #29)."*

**The oracle run in this session measures 16 diverging cells, two of them money** — including
`billing|key-usage|after-upstream-down` reading `spend 250 → 0` and `tokens 18 → 0`. The proof
#29 names as the precondition for this flip does not hold on the binary that has already been
flipped.

**This is the leading explanation for the two money divergences**, and it is testable rather than
speculative: the money-settlement path moved from the substrate loop to the kernel teller loop
under a byte-identity claim, and the oracle now reports money figures that differ. It also fits the
abandoned-hold family — the kernel loop is where `Abandoned::drop`, the unwired sweep and the
never-called `recover_all` live.

**The first action is not a fix, it is a measurement:** re-run the two divergent money cells against
a build with the flip reverted (comment out the four lines in `install()`). If they return to the
1.5.5 figures, the flip is the cause and #29's gate was load-bearing exactly as written. That is
one build and two cells, and it decides how much of Phase 2 and Phase 3 is one root cause.

### Corroborating: the metering that does not happen

`grep -rn -e meter_ledger -e record_usage crates/busbar-mcp/ crates/busbar-a2a/` returns **nothing**
(control: the same grep over `busbar-llm`/`busbar-voice` returns 9 files). MCP and A2A admit through
`GovState::try_admit` and meter `UsageComponent::Queries` at amount **0**, which `token_usage_for`
maps to `None`. With `per_request_fee` defaulting to 0, **a `budget:` cap is unreachable by those
two planes entirely** — while A2A's own refusal text says *"this key's budget is spent."*

And `Door::record_usage` in `decide.rs` has **zero non-test callers**, so the new admission stack
charges requests and ledgers nothing.

---

## THE WORST ONE: TWO ADMISSION DOORS ANSWER THE SAME QUESTION IN OPPOSITE DIRECTIONS

Verified by hand, both sides, at HEAD. Decision #42 says an unpriced model must REFUSE, never
silently price at zero. There are two live doors and they disagree:

```rust
// crates/busbar-kernel/src/cost.rs:755        THE LIVE LLM ADMISSION GATE
for (model, units) in models {
    if let Some(rate) = self.rate_for(model) {          // no rate -> body never runs
        nanos = nanos.saturating_add(rate.reserved_nanos(units));
    }                                                    // ...the model's WHOLE spend is dropped
}

// crates/busbar-kernel-budget/src/price.rs:230          THE BUDGET DOOR
for (model, units) in models {
    if self.model_unpriced(model) { return i64::MAX; }   // blocks, per #42
```

**The kernel door is live on the admission path** — `governance/state.rs:1613`, `:1625`, `:1691`
and `:1970` all call it.

**The attack is configuration, not code.** Send traffic to a model the rate card does not name.
Its consumption derives as **zero**, the budget cap never trips, and spend is unbounded on the
plane that carries essentially all revenue. The same arm at `:783` feeds the operator's
"spend so far" and "budget remaining" figures, so the operator's dashboard agrees that nothing was
spent.

`docs/design/1.6.0-money-sweep.md:752` **named both sites.** One was fixed and one was not, which
is what turns this from a standing PARK into a live regression: the tree now holds two doors
answering one #42 input in opposite directions, and the wrong one is the one on the revenue path.

### And the books-balance verifier wraps

`busbar-kernel-ledger/src/identity.rs:83` — `residual`, the function that checks the books balance —
was left out of the saturating-guard conversion that `settle.rs` and `totals.rs` each announce in
their own file headers. Release builds set no `overflow-checks`, so it **wraps** on exactly the
pinned columns those files deliberately produce. A maximally broken book verifies clean.

That is the third instance in this section of the same shape: **the money is wrong AND the thing
that checks the money cannot say no.** Reconciliation compares 0 to 0 and agrees; the tamper
detector is never constructed; the balance verifier wraps to zero. Under the owner's standing
standard this is the top of Phase 2.

---

## THE MONEY FINDING — the hold/settle/recover protocol is half-built, and the check that would notice passes at 0 == 0

Four independent audit lenses, plus the durability sweep, converged on one picture. None of them
knew what the others were looking at. Every line below carries a `file:line` and was verified.

### 1. A hold is never written down

`recovery.rs:35` states that open holds are journalled and recovered. They are not. **All six
production `Entry::new` sites are at or after SETTLE time**, so no hold is ever journalled. The
`Transaction = 1` record class documented to carry holds is real — the class is not the defect;
nothing writes one.

### 2. Nothing recovers them, and the recovery code is production-compiled

`recover_all` has **zero production callers** — the entire `recovery` module is compiled into the
shipping binary and referenced only from one integration test. Positive control: `open_store` is
production at `appbuild.rs:1160` under the identical search.

### 3. Both replay paths filter out everything except migrations

`durability.rs:232` and `:637` are the only two `replay()` calls in the tree, and both filter
`RecordClass::Migration` alone.

### 4. THE ONE THAT MATTERS: the money view resets to zero, and the reconciliation PASSES

`durability.rs:773` builds `Ledger::dual_writing(legacy_rows)` — an **empty book** — with
`checkpoints: Vec::new()`, on the line after the recovered journal opens. Combined with (3), the
money view resets to zero on every restart **and the reconciliation identity passes, because both
sides are zero.**

That is Law 0 inside the money path: *an instrument that cannot produce a NO is not a check.*
A reconciliation that compares 0 to 0 and reports agreement is the most dangerous green in the
release, because it is the check a bank auditor would ask for first.

### 5. The sweep half of the protocol was never built

Nothing calls `mark()`, `touch()`, `sweep` or `sweep_settle` in production. The drop guard
(`units_llm.rs:989-993`) **removes the slot instead of marking it**, so a sweeper could not see it
even if one were wired. A client disconnect mid-dispatch silently skips a charge.

`HoldCell::take` compounds it: its doc claims **two** callers, the gate spec says **three**, and
there are three (`teller.rs:1207`, `teller.rs:1305`, `tick.rs:289`). The CI gate that exists to
guarantee *"no fourth anywhere"* scans for a literal substring that **provably cannot see the third
site** — positive control: the same grep matches `teller.rs` twice and `tick.rs` zero times against
a real `take` call there.

### 6. Two durability defects that lose settled money, not just held money

- `replay()` scans **only the current segment**, so the migration marker becomes unreachable after
  the first 64 MiB roll.
- `DirectoryFactory::open` **never fsyncs the holding directory on a roll**. A new segment's
  directory entry is not durable, so a segment's settlements can vanish *after* `sync_data()`
  returned success. This is the classic fsync-the-parent-directory omission and it loses data that
  the code has already told the caller is safe.

### 7. Charged for work that was refused

`busbar-a2a/src/a2a/receive.rs:2085` meters the hop and stamps `OUTCOME_APPLIED` — and **five**
pre-flight gates that can still abort the hop are checked afterwards, at `:2131-2275`, including
the breaker's own open-circuit refusal. With a tripped breaker the caller is billed for a hop
busbar decided never to attempt. `breaker_fastfail_tests.rs` exercises exactly this and asserts
nothing about the ledger; `metered_verbs_tests.rs` proves the suite checks ledger rows when the
authors meant to.

### 8. The tamper check cannot fire

`busbar-kernel-ledger/src/verify.rs:35` declares `Finding::AnchorHeadDiffers` — the
checkpoint-versus-anchor rollback detector — renders it, and **never constructs it**: `verify()`
takes no anchor parameter at all. Positive control: the other five `Finding` variants are all
constructed and asserted in `checkpoint_tests.rs`.

### WHAT THIS MEANS

Taken together: **money is held in memory only, never journalled, never recovered, never swept,
and the restart-time check that should catch the loss compares an empty book against an empty
book and reports agreement.** The abandoned-hold defect found earlier is not an isolated bug — it
is the visible corner of a protocol that was designed, documented, and left unwired.

Under the owner's standing standard — *"would it pass a bank auditor? if not it is wrong, fix
it"* — this is a NAY, and it is the largest single body of work in Phase 2.

**These are not self-approvable and not owner questions either.** They are defects with a clear
right answer; the owner's standing money authority says fix them. The only items that go to the
owner are the two oracle cells whose FIGURES changed, above.

---

## C3 IS MEASURED, AND IT IS REFUTED — 16 cells, 12 of them product

The LLM-only build now exists and has been recorded against the published 1.5.5 golden. C3 was
"unevidenced" this morning; it is now **falsified with a number**.

```
owed 916 · diverging 16 · accepted 289 · unbaselined 1402 · weighted D/W 0.0144 · --strict rc=1
golden 84bde0a0… (published 1.5.5)      candidate 8b46bdc2… (LLM-only)
```

C3's own refutation criterion is *"one unexplained cell."* There are sixteen. Four are
environmental (no MySQL/Postgres/Valkey on this host — golden PASS, candidate SKIP), leaving
**12 product divergences**.

**The builds all compile.** Set L (LLM-only, shipped paths) 16,710,080 bytes · Set M (strict
minimum) 15,848,512 · DEFAULT (all planes) 22,488,240 — the all-planes binary carries **+41.9%**
over the strict minimum.

**CORRECTION TO MY OWN EARLIER FRAMING.** I wrote that every previous C3 recording was taken
against a binary 6.6 MB larger than the one C3 is a claim about, and implied that mattered. Both
legs have now been recorded and it did not: **the 16 diverging cells are the SAME 16 on both
builds** (D/W 0.0144 vs 0.0152). Across all 913 cells both recorded, the two builds differ on
exactly **two** — `admin.ops|DeleteOverlaySection|not-found` (4 overlay sections in 1.5.5, 6 in
LLM-only, 8 in all-planes, the extra two being MCP's `tools` and A2A's `agents`) and
`boot.refusal|BOOT-181|boot` (all-planes emits one extra WARN line, which retires the prior
document's PARK-P6 by showing it is plane-dependent). Recording the wrong binary was a real defect
in the method; it masked **zero** divergences. The divergences are HEAD's, not the feature set's.

### THE TWO MONEY CELLS — PARKED FOR THE OWNER, never self-approved (#10/#59)

| cell | 1.5.5 | 1.6.0 LLM-only |
|---|---|---|
| `billing\|rate-card\|history-mid-window` | `/spend_after_a` 25,000,000 · `/spend_after_b` 500,060,000 · `/spend_final` 750,090,000 | **2,500,000 · 27,500,000 · 277,530,000** (across `by_key`, `by_model`, `total`) |
| `billing\|key-usage\|after-upstream-down` | `/spend_cents` 250 · `/tokens` 18 | **0 · 0** |

**CORRECTION — I read this one backwards the first time.** I recorded `spend 250 → 0` as money
disappearing. The cell's own `why` says **spend 0 is the CORRECT answer**: an upstream that went
down served nothing, so nothing is owed. **1.5.5 was the one that was wrong**, billing 250 cents
for a failed request, and 1.6.0 fixes it. That makes it C4-over-C3 — the money rule beating the
byte-identity rule — and what is missing is not a fix but a **signed exception that was never
written.** It is still an owner item, for the opposite reason: a customer-visible improvement that
changes billing and has no recorded sign-off.

**The first cell settles a question the prior document could not.** `1.6.0-parity-unblock.md`
PARK-P1 offered two explanations and declined to choose: (a) a real pricing change, or (b) clock
skew, since the cell pins no clock. The measured triple is **digit-for-digit the priced-at-charge
answer that cell's own `why` names**, against a design intent that "ONLY the final read moves."
Clock skew does not reproduce three exact predicted constants. **It is (a).** And the tiebreak is
now general: **no cell in the corpus pins a clock — 0 of 2,318** — so (b) was never specific to
this cell and cannot be offered again for any cell.

### THE OTHER TEN

Ledger authorization (2): `ledger|amend|adjusting-entries` and `|refused-unsigned` move 404→403,
`not_found`→`forbidden` — an error-shape change on a signed surface.
Admin surface (4): `GetAuth|ok`, `GetConfig|ok`, `PostConfigReload|ok` flip `auth.open` true→false;
`GetAudit|ok` gains `/items/*/scheme` null→2.
Audit (1): `teller|audit-record`, the same new `scheme` field on a second surface.
Boot diagnostics (2): `BOOT-181` prints its TLS error **twice in one line**; `BOOT-135` says
"incomplete deflate stream" where 1.5.5 said "unexpected end of file".
Config grammar (1): `neutrality|NEUT-U-auth|validate` lists `operator_pub` among `auth:`'s expected
fields — a key with **zero** hits at v1.5.5 (positive control: `signing_key` has 7).

**Three of these are HEAD movement, not plane removal.** The `auth.open` and `GetAudit`
divergences are byte-identical between the LLM-only and all-planes builds, so they arrived since
the 2026-09-22 measurement and have nothing to do with C3's compile-out.

### A NEW INSTRUMENT DEFECT — the register forgives on a premise that is false for this build

The LLM-only binary still serves an admin OpenAPI document containing `A2aTrustView` and the
MCP/A2A endpoints, because the document is `include_bytes!("openapi.json.gz")` — compile-time
embedded and **entirely feature-independent**. The register forgives this under **F-011b**,
*"additive — openapi.json describes the MCP and A2A endpoints"* — a rationale premised on those
planes existing. With them compiled out, the build **advertises a surface it cannot serve**, and
the neutrality cell whose stated contract is *"the document must list only the 1.5.5 admin
surface"* is scored `accepted` rather than `diverging`.

This is Law 0 in the register rather than in a gate: **a forgiveness whose premise is not checked
is not a forgiveness, it is a blindfold.** Every `accepted-differences.json` entry needs its
premise stated as a condition the runner can evaluate against the build under test.

### THE DIFFERENTIAL ALREADY WORKS

Of the 235 cells both builds have recorded, exactly **one** differs between them:
`admin.ops|DeleteOverlaySection|not-found` — 1.5.5 lists 4 overlay sections, LLM-only lists 6, the
default build lists 8, the extra two being MCP's `tools` and A2A's `agents`. The all-planes build's
additional surface, caught in a single cell. That is the instrument C3 always needed.

### CORRECTION TO THIS PLAN'S OWN CLAIM

I recorded earlier that no divergence list existed. One does: `1.6.0-parity-unblock.md` §5 parks
**8**, measured 2026-09-22 on an all-planes build against a golden owing **913**, not the committed
916. The accurate statement is narrower and still holds: **the list for the binary C3 is actually
about has never existed, it is twice as long, and it is measured here against the committed golden.**

---

## THE ORDERING DEFECT — the plan as written contains a CYCLE, and here is the break

The executability adversary found it and it is real. Item 6 (Wave 0) must make `map-proof` and
`sweep-coverage` run. Those gates read nine `docs/design/*.md` files, each *"a document that prints
a command beside a number."* Those numbers are only true once the tree stops moving — i.e. after
Waves 2–5. And item 87 (Wave 6) collapses `docs/design/`, which is the corpus they read.

> **W0 → needs the tree final → W2–W5 → W6 rewrites the corpus → re-reds W0.**

Three more items have the same shape: item 1 arms a class whose ceiling only reaches 0 in Wave 5;
item 4 needs item 59 (Wave 4) and says so in its own text; item 17 reads ceilings that Waves 4–5 drain.

### THE BREAK: ARM AT TODAY'S NUMBER, RATCHET DOWN

The cycle is only real if these items' proofs require the numbers to be **correct**. They do not.
They require the instrument to **RUN** and to **BE ABLE TO FAIL**. Those are different, and every
other ratchet in this repo already works the second way. So each cyclic item splits in two:

| item | (a) — lands NOW, no red, breaks the cycle | (b) — lands when the tree settles |
|---|---|---|
| **6** | wire the 3 gates into `ci.yml`, classify the 2 cargo lines. Proof is `full-gate --selftest` exit 0, a **set-equality check about registration**, not about content | make the 3 gates green |
| **1** | arm `LAW0_ENFORCED_NEUTRAL_CRATES` with the ceiling **at today's measured 3084**. It now blocks every RISE — the whole Law 0 value — on day one | ratchet 3084 → 0 |
| **87** | re-point the 55 doc paths hardcoded into live tooling | delete the unbound rest |
| **17** | the 2 `ship-ready` rows that `construction --write` re-pins downward mechanically | the 2 that need Waves 4–5 drained |

**This is the single most important structural correction in the plan.** An instrument that blocks
regression is worth having immediately even while the debt stands; holding it back until the debt
is zero is what created the cycle. State it once, apply it everywhere: *arm at today's number.*

---

## ITEM 106 — RESOLVED, and it is the first item to do

Two agents returned opposite verdicts. I checked the code myself. **The test is stale; the code is
correct.**

`crates/busbar/tests/money_one_function_equivalence.rs:472-479` asserts `kernel_result.is_err()`,
on the stated grounds that *"the kernel copy has no saturation: `acc + (n as u128) * (rate as u128)`
at crates/busbar-kernel/src/cost.rs:178."* Both claims are false at HEAD:

- `cost.rs:178` is `_ => 0,` — the fallthrough arm of `reserved_rate`'s match.
- `cost.rs:199-205` and `busbar-kernel-budget/src/price.rs:112-118` are now **byte-identical**, and
  both delegate to `busbar_kernel_ledger::cost::nanos_sum`, which saturates.

It is a **red-first test whose defect was fixed in `8f083cc38` and whose assertion was never
inverted.** It fails *because the code is right*. The fix is to assert the kernel copy now agrees
with the budget copy — which is the entire point of an equivalence test.

**Do it first**, for a reason bigger than its size: items 22, 23, 24, 25, 26, 28, 32, 104, 105 and
107 all name `cargo test -p busbar --test money_one_function_equivalence` as their acceptance
command. Ten money items are currently judged by a suite that exits non-zero for a reason none of
them caused — which trains every worker to read its red as *"that one always fails."* This release
has already been burned twice by exactly that reflex (a selftest that scored every planted defect
PROOF IMPOSSIBLE; a gate green over 266 named gaps). A permanently-red money test is the third
instance of the same disease, and the next real money regression will be dismissed by the same shrug.

**A METHOD NOTE, because it cost an agent a whole round.** The agent that got this backwards read
the test's *failure message* and believed it, rather than opening `cost.rs`. A failure message is a
string literal written by a human at some past moment; it is not evidence about the present tree.
This is the sixth instance this week of **a name is not a thing**, wearing a new costume. Law 8's
positive-control rule applies to error text exactly as it applies to a grep.

---

## FIGURES CORRECTED BY SCOPING — do not plan against the originals

| item | recorded | measured | consequence |
|---|---|---|---|
| 78 | 99 zero-caller `pub fn` across 23 crates | **283 across 35 crates, a LOWER bound** (names shared with any other symbol are excluded by construction). **The "99" has no source anywhere in the repo** — the same grep for item 75's "526" also returns only the todo row. Both figures were never written down outside this list | re-baseline before assigning; **no instrument produces this number** — `dead_code` cannot see a `pub fn` in a lib crate and `unconstructed` only checks its own 19 declared rows |
| 80 | two reserved segments, "a one-line status flip" | **Both test targets DO NOT EXIST.** `qa_mcp_integrity` / `qa_a2a` appear only in the `run` lines that name them. `qa-segments.sh:236` hard-fails an active segment whose target is absent, and its selftest runs in CI | flipping the status alone turns CI RED. The suites are the item |
| 81 | 8 crates outside the shipped binary | **7.** The OUT set is 8,776 loc = **1.2% of source**. The "8" is the set that vanishes under `--no-default-features` — the DELETABLE set, transcribed as the OUTSIDE set | **≥9 folds must merge shipping crates**, not ≥8. Any fold that moves real lines moves shipping lines |
| 81 | 49 → 33, "16 folds" | **The OWNER-LOCKED roster enumerates 34 slots**, not 33; the merge that would make 33 is in prose and not applied to its rows. 4 slots have no crate on disk. **11 of the 14 crates to fold have NO execution plan at all — 315,525 loc**, including `busbar-llm-codec` at 105,589, the largest single move in the release | writing those 11 plans IS the item. Only 3 crates have plans today |
| 82 | two High advisories to fix | **BOTH SOURCE FIXES ARE ALREADY LANDED on predev**, verified at this tree and absent on all four of v1.5.5/main/qa/dev | the item is the **BACKPORT** plus two advisory records plus a CHANGELOG `Security` entry — not the fix |
| 86 | "2 of 3 #40 edges closed" | **1 of 3.** Live normal lateral transport edges = 2 (`grpc→http`, `sse→http`); 4 more became dev-only. **21 `[[dep]]` rows in `qa/kind-isolation.toml` are now DEAD** | item 69's "3 real lateral deps" is now 2 |
| 87 | 267 docs | **Matches nothing on disk.** All of `docs/` = 241 files (233 `.md`); `docs/design/` = 174. The tree contests it internally too — 90 in one doc, 203 in another. "267" appears twice more in this list with unrelated referents | re-baseline and SAY WHICH |
| 89 | 11 gates standing-red-poisoned, incl. `reachability` | **`reachability` is NOT one — 0 IMPOSS of 25 cases.** Its 2 red arms are an overlay fixture bug (item 88) and 1 uncovered row (item 90). **`config-schema` is 22 IMPOSS of 57** and is item 37b's body | remove reachability; do not schedule 89 and 37b separately |

## THE RISK IN ITEM 87 IS REWRITE, NOT DELETE

59 of the 104 parity bindings carry **155 line-number pointers** into the 8 inventory docs.
`inventory_ref.rs:9-13` states it deliberately does NOT resolve finer-grained anchors — section
numbers, row ids, line numbers. **So a reflow destroys all 155 pointers and the gate stays GREEN.**
That is the worst failure mode available in this release, and it is in the item the owner would
think of as tidying. `docs/admin-api.md` is also `include_str!`d by a test — deleting it breaks
the build.

## HAZARDS FOR ANY WORKER — read before starting an item

Found during scoping, each of which would waste a worker's round:

1. **`openapi_json_matches_committed_file` is RED in the working tree right now**, on a single
   doc-comment line (`busbar_plugin_sign::kind_restart_default` → `busbar_plugin_loader::sign::`).
   It is unrelated to any item. A worker on items 45/46 will hit it and must not mistake it for
   their own change. One-line regen.
2. **`tests/migration-corpus/refresh.sh` MUST NOT be run as-is.** Its tag filter is
   `git tag | grep -vE 'rc'`, which on this checkout matches the 36 release tags PLUS ~60 working
   tags — `backup_*`, `snapshot_wt-agent-*`, `chunk18-keep-*`. Running it verbatim grows the corpus
   from 69 files to ~270, most of them agent worktree snapshots. Its providers glob is also
   UNANCHORED and would copy the corpus into itself (63 matches instead of 1). Fix the script
   first; that is part of item 48.
3. **`cargo check -p busbar-core-admin --features openapi-schema` FAILS; `-p busbar` SUCCEEDS.**
   Single-package selection does not unify the feature down to the plane crates. The working form
   is CI's multi-package one. A worker reading the in-tree comment that says regeneration "cannot
   run today" will believe a half-truth.
4. **Two items are OWNER-GATED, not worker-gated.** Item 47 — both AUDIT NOTES end "owner decision
   needed". Item 51 — spec #59 is OWNER-LOCKED: *an agent may NEVER self-approve a gap under the
   owner's name; an agent-added gap is RED.* These produce a prepared diff, not a landed change.
5. **Item 50 cannot be completed in this repo.** Its mover, `oracle-rerecord.yml`, lives in the
   sibling `busbar-release` repo.

### Collision map — do not schedule these concurrently
`45a`↔`46` (both may touch `openapi.json`/`.gz`) · `47`↔`52` (same JSON file) ·
`50`↔`51c` (both rewrite `golden/1.5.5/**`) · `53`↔`55` (both own `prepass.rs`) ·
`55`↔`57`/`58` (same 8 kernel files) · `56a`→`56b` (serialised: b needs a's const).
Everything else in this batch is free parallelism.

## OWNER PRIORITY — THE CRATE FOLD IS THE MILESTONE

Owner, 2026-09-23: *"my number one desire is crate folding to 33ish — that will be the day i feel
we have something solid in 1.6.0 vs the 40-50-60 we have now."*

Item 81 was filed in Wave 6 under "dead weight". That was wrong as a statement of priority, and it
is corrected here: **the fold is the milestone this release is measured by, and it gets its own
track rather than a line.** 49 crates today, 33 locked by #20/#39.

**The one caveat, stated plainly because it decides the order.** A fold perturbs the crate
dependency graph, and the instruments that would notice a bad fold are keyed on crate NAMES:
`kind-isolation` carries 200 `[[cell]]` and 191 `[[dep]]` rows naming crates, `package-selectors`
resolves `-p` names across CI, and several `qa/*.toml` ratchets name crates directly. Folding while
those instruments are blind means you cannot tell a good fold from one that quietly moved a byte
the oracle covers or a function the money path depends on.

**So the fold splits by risk, not by wave:**
- **CHEAP folds — start immediately, in parallel with Wave 0.** Crates outside the shipped binary's
  dependency graph, whose code the oracle cannot see and the money path does not reach. Measured:
  only 8 crates sit outside the binary, so this is the smaller half.
- **DANGEROUS folds — need the instruments first.** Any fold that merges a SHIPPING crate, moves
  code under oracle byte-identity, or touches `busbar-kernel-ledger` / `busbar-kernel-budget` /
  `crates/api`. At least 8 folds are in this class by arithmetic alone.

The fold plan — all 49 crates with verdicts, the topological order, per-fold write sets and proof
commands, risk ranking, and the parallel batches — is being computed. Until it lands, item 81
stands as the placeholder and this section is its priority statement.

---

# NEW ITEMS 112-153 — THE AUDIT'S FINDINGS AS WORK, SLOTTED BY PHASE

Items 1-111 are the sweep's. These are the audit's. Every one carries a `file:line` verified by the
architect, and the evidence for each is in the sections above. Numbers continue rather than restart
so a citation is never ambiguous.

## Into PHASE 0 — the floor

| # | item | proof |
|---|---|---|
| 112 | **`busbar-contract` declares a `test-seal` feature**, breaking the one-surface invariant (landed `217b21cc0`). 5 tests; takes `feature-sets` RED on the real tree, cascading into 3 more. **The largest failing cluster in the release and it was on no list** | `cargo test -p busbar-contract` + `xtask gate feature-sets` |
| 113 | `qa/instance-noun-neutrality.toml:2753` names `engine_kit.rs`, now a **directory**. The `[[leak]]` row scans zero files and PASSES — a false zero inside the C1 census | the row matches ≥1 file, or is struck |
| 114 | **the walk floor lost its own proof**: `walk(min_files=1)` on an empty set returns `WalkError::Empty`, not `BelowFloor`. `min_files` is the most-repeated safety device in the gate library and is now unproven | `infra.rs:192` passes |
| 115 | `cargo test -p busbar-voice --features runtime` **does not compile** — 12 × `E0407` in `testkit/fixture_host.rs`. The Voice workflow's `boot-validate` runs this verbatim; `cargo test --workspace` never sees it | that command exits 0 |
| 116 | **`llm-conformance` is RED on every branch** — the pinned Gemini discovery doc drifted (`836bf6ea…`→`fe47cc95…`) and `validate.py` then refuses to judge anything. The drift DETECTOR is the one case that passes | `selftest.sh` exits 0 after `vendor.sh --repin gemini` and a diff review |
| 117 | `plane/registry.rs`'s `TEST_MEMO` is keyed on `(installed_len, registered_len)`, so **two registration sets of equal size alias** — 8 `units_llm` tests fail whenever `plane_decision` runs first, even single-threaded. The comment argues why a *sum* would alias and why the *pair* fixes it; the pair does not fix the equal-length case | the 8 tests pass in any order |

## Into PHASE 1 — arm the instruments

| # | item | proof |
|---|---|---|
| 118 | **`INSTANCE_VOCAB_KINDS = ["plane","transport"]`** — C1 is asserted over 7 plugin kinds and measured over 2. Arm the five missing axes for every neutral crate, seeded from the real instance vocabulary. **This single hole hides both the C1 and the C2 breach** | a planted store/secret/auth/hook/export instance name in core turns a row RED |
| 119 | **Law 10 — a forgiveness whose premise is never checked is a blindfold.** Every `accepted-differences.json` entry's `why` becomes a condition the runner evaluates against the build under test. F-011b forgives the served openapi listing MCP/A2A endpoints "because those endpoints exist", on a build where they do not | an entry whose premise is false on the build scores DIVERGING |
| 120 | `construction/rules2.rs:314` lists six kinds and **omits `transport`**, so `manifest-allowlist` emits 14 rows and no transport row at all — green while 13 crates breach the wall | a transport row exists and can go RED |
| 121 | the voice **`tool-reply` leg is run by no workflow**, and `verdict-covers-every-leg.py` checks the verdict job depends on every *job* — so a leg with no job is structurally invisible to the anti-vacuity lint | the lint's denominator is legs, not jobs |

## Into PHASE 2 — money (the bulk of the audit)

| # | item | proof |
|---|---|---|
| **123** | **THE KEYED-UNIT CONVERGENCE — ONE ITEM, FOUR LAYERS, DO NOT SPLIT.** Only the four reserved token keys can become money. Open/keyed classes are dropped at the ABI (`hot/pod.rs:1008`, a field two version bumps were made to carry and nothing decodes), at the host meter (`:272`, `Tokens` alone), at the budget gate (`reserved_rate` → `_ => 0`), and in the config grammar (`RateEntryCfg` is `deny_unknown_fields`, four floats). Fixing one layer changes nothing | an open class priced in config is charged AND capped, end to end |
| 124 | **two admission doors answer #42 in opposite directions**: `cost.rs:755`'s `if let Some(rate)` drops an unpriced model's whole spend (silently free, on the live LLM gate) while `budget/price.rs:230` returns `i64::MAX`. `money-sweep.md:752` named both; one was fixed | both refuse; `money_one_function_equivalence` green |
| **125** | **the money-authority flip is LIVE while four files call it DORMANT**, and its claimed proof (#29's oracle) does not hold. **FIRST ACTION IS A MEASUREMENT:** rebuild with `install()`'s four lines commented out and re-run the two divergent money cells. One build, two cells — it decides how much of Phases 2 and 3 is one root cause | the two cells return 1.5.5's figures, or they do not |
| 126 | **two books over one event.** The tap fills the governance ledger (what 1.5.5 bills, what `/usage` reports); the late reading posts onto a second book. One charge, two records — sound only while they agree, and the oracle shows they do not. Name which book is the invoice and make a disagreement RED | a planted divergence between the books goes RED |
| 127 | holds are **never journalled** — all six production `Entry::new` sites are at or after settle — while `recovery.rs:35` states the opposite; `recover_all` has **zero production callers**; both `replay()` calls filter `Migration` alone | a hold survives a kill -9 |
| 128 | `durability.rs:773` builds an **empty book** with empty checkpoints, so the restart reconciliation **compares 0 to 0 and passes** | the reconciliation goes RED on a book that lost a row |
| 129 | the sweep half is unbuilt: nothing calls `mark()`/`touch()`/`sweep`/`sweep_settle` in production, and the drop guard **removes** the slot instead of marking it | a disconnect mid-dispatch is charged |
| 130 | `HoldCell::take` — doc says two callers, the gate spec says three, there are three, and the gate guaranteeing "no fourth anywhere" scans a substring that **cannot see the third** | the gate sees all three and refuses a fourth |
| 131 | `identity.rs:83`'s `residual` — the books-balance verifier — missed the saturating-guard conversion its siblings announce in their headers, so it **wraps in release** and a maximally broken book verifies clean | a broken book fails verification |
| 132 | `verify.rs:35`'s `Finding::AnchorHeadDiffers` — the rollback detector — is declared, rendered, and **never constructed**; `verify()` takes no anchor parameter | a rolled-back checkpoint is detected |
| 133 | `read_count_u64(..).unwrap_or(0)` bills a **present-but-unreadable token count as $0** — all six dialects, buffered and streaming — while the crate's own `billed_count` refuses it correctly and is used only on leaf ops | a stringified count REFUSES, per #42 |
| 134 | `RerankResp::billing()` hardcodes `Billing::Flat`, discarding the `search_units` it correctly read: a 5,000-document rerank bills like a 1-document one | search_units reaches the price |
| 135 | `a2a/receive.rs:2085` meters the hop and stamps `OUTCOME_APPLIED` **before** five gates that can still abort it — a tripped breaker bills a hop never attempted | breaker-open bills nothing |
| 136 | **MCP and A2A never accrue anything into the budget ledger** (`meter_ledger`/`record_usage` → 0 hits, against 9 files for llm/voice); they meter `Queries` at amount 0, so a `budget:` cap is unreachable by those planes — while A2A's refusal says "this key's budget is spent". `Door::record_usage` has zero non-test callers | a budget cap trips on MCP and A2A traffic |
| 137 | streaming discards the open turn's audio-ms and tool-call counters on a Twilio `stop` (`plane.rs:1038`) and on a client barge-in (`:1130`); every production site discards the durable `SessionHandle` without `settle_terminal()`, and the idle-abandon callback opts OUT of the sweep | a barge-in bills what it served |
| 138 | **two voice planes are both registered by default**, metering one capability through two non-communicating pipelines (D2 nanodollar lease vs meter-class rate card) | one pipeline, or a proof they agree |
| 139 | `plane/cost.rs:221` checks its one money invariant with the **raw non-saturating** `u128` `Sum`, bypassing the saturating `Sum` the same module defines against "a hostile/buggy plugin breakdown" | an ill-formed breakdown is refused |
| 140 | the budget CAS loop (`decide.rs:283`), documented as guaranteeing racing admissions cannot jointly overshoot, has **zero multi-threaded test** | a `thread::spawn` race test exists and fails without the CAS |

## Into PHASE 4 — C1, C2 and the fold

| # | item | proof |
|---|---|---|
| 141 | **the `export` kind cannot be dropped in at all.** `config/mod.rs:1983` hard-errors on any module name outside the four built-ins, so a dropped-in export plugin cannot be NAMED; `open_export` has zero callers against four positive controls; `ExportDispatch` is never constructed. Already recorded in-tree as DOCUMENTED-NOT-IMPLEMENTED | a dropped-in export plugin serves |
| 142 | **FOLD THIS FIRST — two breakers ship and the buggy one is live.** The 578-line FSM on the kernel's admission path holds three races (`breaker.rs:400` reads `cooldown_until` before `breaker_state`; `:795` Denies a just-closed cell; `availability.rs:355` reports a 250 ms hint against a 1,800,000 ms cooldown) that the 1,198-line crate already fixes. **A fold is justified when two implementations of one concept ship and no instrument compares them — not by a crate count** | one breaker; the three races gone |
| 143 | **RESTATED — see the correction below.** The five `battery-subject` failures are real test failures, but only ONE reproduces as a product defect (the async outputSchema gap, now its own item). The rest are most likely a harness that never exercises the stdio transport | `battery-subject` green, with the harness covering both transports |

## Security and robustness — these need a phase, and they are defects not questions

| # | item | proof |
|---|---|---|
| 144 | **`auth/mod.rs:1868` FAILS OPEN.** The refusal needs all three of {no gov key, principal has roles, module has bindings}, so a caller with no roles is admitted `key: None` — no pool ACL, no budget, spend booked to `anonymous`. The guard should be the first condition alone | a roleless principal is refused |
| 145 | `HttpTransport::dial()` accepts any URI authority with **no IP-literal enforcement**, where the `tls` sibling requires `parse::<SocketAddr>()`; hyper re-resolves a hostname at connect and on every pooled reconnect — a DNS-rebinding/TOCTOU SSRF backstop gap | dial refuses a non-literal authority |
| 146 | the gRPC HTTP/2 **connection preface has zero timeout** on dial and accept, where `tls` has `HANDSHAKE_TIMEOUT` and `ws` has `HANDSHAKE_BUDGET` — an unbounded slow-loris vector | a stalled preface is dropped |
| 147 | **`limits.pool_idle_timeout_secs` is unbounded and on the admin PATCH surface.** At ≥2⁶³ it overflows `Instant + Duration` under the pool mutex, poisons it, and all nine `.expect("engine pool lock")` sites panic on every later request — **that shard's egress is dead until restart.** Sweep every other unbounded numeric on that surface | the value is bounded, and the pool recovers a poisoned lock |
| 148 | **there is no panic-catching layer anywhere in the workspace** (`CatchPanic` → 0; `tower-http` is not a dependency; control: 10 router `.layer(` calls). A panic does not 500 — it tears down the h2 connection and every request multiplexed on it. Decide the policy and write it down | stated, and enforced by a gate |
| 149 | the live admin scope gate is a verbatim 1.5.5 port and disagrees with `busbar-core-admin`'s own `required_scope()` on `POST /admin/verify` — **fails closed**, so a correctness defect rather than a hole | the two tables agree |
| 150 | both `quarantine_settle` call sites discard the durable write with `let _ =`; a failed write plus a restart **silently reverts a rug-pull quarantine** or an operator's clearance | the write is checked |
| 151 | the a2a streaming relay swallows a failed durable write of the artifact resume cursor with a bare `let _ =` and no log, while the identical sibling six lines above is logged with an error-once latch. `tasks/resubscribe` reads that cursor | the failure is surfaced |
| 152 | `ingress/byte_duplex.rs:467` leaks an `inflight` key forever if a plane callback panics, so every later drain burns the full 3 s `EOF_DRAIN` and logs phantom stragglers — its sibling at `plane/handle_engine.rs:858` wraps the equivalent callback in `catch_unwind` for exactly this | the key is released on unwind |
| 153 | no timeout bounds `client.request(req).await` in the HTTP transport; only TCP connect and HTTP/2 keepalive are bounded, so an HTTP/1.1 upstream that hangs after connecting is unbounded | the request is bounded |

**Item 122 is deliberately absent** — it was drafted for the `tool-reply` leg and merged into 121.

---

## WAVE 0 — make the instruments able to fail
*Nothing below this line can be trusted until this is done. Half the measuring apparatus currently
cannot produce a NO, so burning items would produce false green.*

| # | item | measure |
|---|---|---|
| 1 | **Instrument #2's hard clause is unenforced by BOTH instruments that claim to enforce it.** `LAW0_ENFORCED_NEUTRAL_CRATES = &[]` so the ARMED class blocks nothing on the everyday gate; the `[[cell]]` ceiling for `busbar-kernel × plane` was raised **1 → 3047**; and `plane-purity:key` is GREEN only because 11 in-source `// plane-purity: frozen-wire` pragmas delete hits before judgement. The clause bars ratchet rows and is silent about the pragma | `matrix.rs:129`, `scanner.rs:256` |
| 2 | **The pragmas' justification is false.** Three exempt `mcp`/`tools`/`agents` as "frozen since 1.5.3". **v1.5.3 and v1.5.5 both carry 26 config fields and none of the three exists at either tag.** They are 1.6.0-additive keys wearing a back-compat exemption | 11 pragmas, all in core |
| 3 | **The neutrality witnesses enumerate instances by hand, so neither can see a NEW one.** Proven by plant + positive control: a new plane, transport and dialect named in `busbar-kernel/src/lib.rs` moves **ZERO rows across 10 gates and 238 rows**; `"cohere"`/`"mcp"`/`transport_http` in the same file move 3. `PLANE_ALTERNATION` scans 4 of 5 planes and scans `voice` instead of `streaming` | `scanner.rs:29,43` |
| 4 | **Build the C1 sharp test.** Part 0 calls add-a-dialect-then-diff the command that confirms or falsifies the entire premise. No instrument exists — the only trace in the tree is a code comment. Must also assert the gate set stays green: one new `qa/field-schemas/*.json` turns all 7 `field-inventory` rows RED with zero core files touched | 0 instruments |
| 5 | **Derive every hand-maintained roster from the plugin's own declaration.** 10 dialect rosters measured, incl. `field_inventory.rs:85`, `scanner.rs:43`, `plane-grep-gate.sh:91`, `assets/readme/generate.py:118`, `voice-cross-dialect-map.json`, `providers.yaml`. Adding a 7th dialect touches ≥9 files in the root slice alone | 10 rosters |
| 6 | **Three registered gates run nowhere and carry no excuse**, which makes `full-gate --selftest` exit 1 inside a REQUIRED check. Plus two unclassified cargo invocations no item named | `unconstructed`, `map-proof`, `sweep-coverage` |
| 7 | **`--strict` has no denominator floor — the floor is ONE cell. HALF IS FIXABLE HERE TODAY.** The judge's `run.rs` is NOT in this repo — it is the pinned external `busbar-release` @ `ab88903`, so an agent scoped to busbar finds no file. **But busbar carries the identical defect independently:** `scripts/verify-1.6.0-done.sh:538` `if [ "$owed" -eq 0 ]` is the same floor-of-one, and its own selftest fixture pins `owed=913` of 2,318 as GREEN. That half needs no upstream change Proven by building the pinned judge: 1 owed cell of 2,318, 2,317 unbaselined, **exit 0**. Repo precedent: `DENOMINATOR_FLOOR=1_700`, `SCAN_FLOOR=200`, `BINDING_FLOOR=40` | `run.rs:2511` |
| 8 | **Under-matching a register entry is a `note`, not a red** — the mechanism is confirmed (`run.rs:1366` pushes to `notes` while the over-match arm 30 lines above returns `Err`). **BUT THE COUNT WAS WRONG:** recomputing the judge's own arithmetic over 2,318 ids × 41 entries gives **0 under-matching, 0 over-matching, 41 exact** (control: `D-1` matches 462). What exists is **3 entries that match zero cells and declare `expected_cells: 0`**, so they emit neither note nor red. The remedy is therefore a *"an entry that forgives nothing is refused"* rule, not an under-match rule. Also external (`run.rs`) | 3 dead, not 4 |
| 9 | **`money-invariants`' denominator is hand-written**: `MONEY_RECORDS` names 8 types, `records.rs` defines 17. `PlaneRecord` uncovered | 8 of 17 |
| 10 | **`hot-path-perf` / `hot-path-alloc` never execute a bench** — zero `Command::new`, zero non-comment `cargo` lines. They grep bench source text | 0 executions |
| ~~11~~ | ~~`kind-abi-lane` has never been shown able to go red~~ **STRUCK — my false zero.** It has 4 `prove_rows_red` cases and its selftest runs 4 RED + 2 GREEN, exit 0. I grepped `prove_red` alone; the tree's dominant helper is `prove_rows_red` (267 occurrences vs 261). **Under the full helper set every one of the 51 gates has ≥2 red proofs.** The real instrument defects are the three below | struck |
| 12 | **The migration-corpus gate computes a completeness denominator and discards it** — asserts only `n>=20` against an actual 69 | `ci.yml:1340` |
| 13 | **`.keep-proof.toml` is a keep-branch artefact on the integration branch**, narrowing `prove-remote.sh` to 161 of 2,318 cells (6.9%). Its own header says the integration tree carries no such file. Names 3 packages that do not exist | `prove-remote.sh:64` |

## WAVE 1 — switch the release engine on
*With every item in every other wave green, the train still does not move. All three stops are
plumbing, and none was on any previous list.*

| # | item | measure |
|---|---|---|
| 14 | **19 of 28 predev workflows have NEVER been registered at GitHub, and 13 of the 26 registered exist on no live branch.** The remedy "re-enable the 22" would re-enable ghosts — they were RENAMED on predev (`codeql.yml`→`qa-codeql.yml`, `security.yml`→`qa-security.yml`, `mcp-conformance.yml`→`qa-conformance-mcp.yml`). **So CodeQL on the shipping branch has never been registered and has never run once**, with `qa-security.yml`, `qa-fuzz.yml`, all three plane conformance suites and `turnstile-dispatch.yml`. The real risk is the inverse of the one recorded: when predev's content reaches a branch GitHub builds, 19 workflows register fresh and enabled, simultaneously, having never run. Separately and still true: of the 26 workflows GitHub HAS registered, 22 are `disabled_manually` — the release engine, every plane conformance suite, and CodeQL. Disable state is keyed by PATH, so promotion does not clear it. Active: CI, qa-gate, Dependabot, oracle-rebless | 22 disabled |
| 15 | **`qa → main` cannot fast-forward and cannot legally be forced.** `origin/main` holds **7 commits reachable from no other branch**. `promote.sh` refuses non-ancestors; `release.yml:530` refuses any sha qa never saw (ruling out a merge commit by design); `allow_force_pushes: false`, `enforce_admins: true`. Content is already on predev — **the remedy is an owner protection change** | 7 commits |
| 16 | **`predev → dev` has no mover — NOT AGENT-ASSIGNABLE, ZERO FILES.** `TURNSTILE_DISPATCH_TOKEN` is absent from the repo's 3 secrets and from the org's 9 (control: the org call returns 9 names). **And a larger blocker the item did not name: `turnstile-dispatch.yml` is not among the 26 workflows GitHub has registered at all.** A `workflow_dispatch` workflow must exist on the DEFAULT branch to be dispatchable, so provisioning the secret alone still leaves nothing to dispatch | owner action |
| 17 | **`ship-ready` is RED on all four rows** — measured under `XTASK_SHIP_TARGET=qa`, exit 1. **Live drift inherited by whoever takes it: `scripts/land.sh:221` carries NINE standing-red names against `gates/mod.rs`'s five**, despite mod.rs instructing that the two be kept in step under a shipping posture: `ship-twin` (10 red rows), `ceiling-slack` (2 ceilings above measurement), `ceiling-rose` (10 stale raises), `standing-reds` (5 names that must be DRAINED, not struck) | 4 rows |
| 18 | **`main`'s required context `record the staged digest` can never report** — its only producer is `release-stage.yml`, which exists at neither `main` nor `qa` and is disabled | 1 context |
| 19 | **Nothing in CI ever runs `verify-1.6.0-done.sh`** — 0 `run:` sites, all 6 hits are comments, against three positive controls. The definition of done runs only if a human types it | 0 call sites |
| 20 | **The cost-watch is healthy and never fires** — selftest green, all four bands proven ($50/$80/$100/tool-error), but its cron only runs from the default branch and the file is not there | 0 runs |
| 20b | **`predev` has ZERO workflow runs, ever** (positive control: `main` has runs). By design — `turnstile-dispatch.yml:22`: *"predev's defining rule is that it carries NO automatic CI"* — but the content is pushed to `integration/**` for CI and **every recent attempt failed or was cancelled**. Last green CI anywhere: 2026-09-13, on dependabot branches. **The 1.6.0 tree has never had a green CI run**, and `predev` is 3,219 commits ahead of dev/qa/main | 0 runs |

## WAVE 2 — money
*Binary, and the sweep found an OVERCHARGE. Under standing money authority except where marked.*

| # | item | measure |
|---|---|---|
| 21 | ~~**THE OVERCHARGE**~~ **WRONG — REFUTED, AND NO OWNER RULING IS OWED.** The refund on non-2xx is LIVE: `ingress/mod.rs:671-676` computes `is_success = matches!(status, 200..=299)` and calls `refund_request`, which decrements the billable count (`state.rs:2069`); `busbar-kernel-budget/src/decide.rs:492` is the second implementation. **Nobody is overcharged.** I read `UsageLedger`'s field doc and then pointed at a `MeteringDelta` writer — two different structs. The real, smaller defect: `state.rs:964` copies `requests` into the FinOps metering series and the admin read passes `b.requests` as the fee count (`service.rs:128,:273`), so **`/admin/usage`'s DERIVED figure over-reports the fee on failed requests. Nothing bills from it — it is a read-path reporting defect** | `service.rs:128` |
| 22 | **A rate below 0.0005 silently becomes 0, and the card reports the class PRICED** — so #42 can never fire. a2a/mcp price by BYTES: $0.10/GB = 0.00009313 micro/byte → **0** | `rate.rs:44`, `:399` |
| 23 | **THREE of four money reads silently reprice history — not two, and the arithmetic is inline here because the file that held it was deleted.** Surface enumerated from the OpenAPI document, not by grep: exactly THREE admin endpoints serve a spend figure, plus a hook projection and the Prometheus gauges. **`/admin/usage`** — DATED, #79-correct (`service_operations.rs:1481` via `card_at`). **`/groups/{name}/usage`** — CURRENT card. **`/keys/{id}/usage`** — CURRENT card **AND A DIFFERENT WINDOW** (`WINDOW_TOTAL`, all-time, not the day bucket); `keys.rs:1789` admits it verbatim: *"spend_cents = ledger x CURRENT rate card … a rate-card correction changes this number on the very next read"*. **The hook/routing projection** — CURRENT, shipped over the wire as a field literally named `spend_micros_at_current_rate` (`api/src/hooks.rs:192`). **The gauges** — CURRENT, `as f64`, self-described at `metrics.rs:434` as "reprice-on-read". Worked: 1,000,000 output tokens settled at 15 micro/tok = 15,000,000; operator publishes a forward-dated card at 12; `/admin/usage` still 15,000,000, the others 1,200 cents. **No command settles this — nothing asserts cross-surface agreement, and the acceptance test must be written first** | 3 of 4 reprice |
| 24 | **Money is served as `f64`, and the gate cannot see it** — `metrics.rs:401,462` `spend_cents as f64`; `no-float-money` does not scan the file | 2 sites |
| 25 | **THREE copies of the spend derivation**, disagreeing on the unpriced rule: ledger → 0, kernel → 0, budget → `i64::MAX` and blocks. One hardcodes `10_000` where another uses the named constant | 3 copies |
| 26 | **Nine of ten `Ledger::record_*` mutators are dead** — `drawn`, `adjustments`, `unreconciled` are structurally always 0, so the reconciliation residual is arithmetically incapable of being non-zero | 9 of 10 |
| 27 | **`with_tier` has zero PRODUCTION callers** (it does have a test caller at `money_one_function_equivalence.rs:641` — the bare "zero callers" was another name-grep overstatement) — settlement applies a tier no read applies. `/admin/usage` cannot apply a non-standard tier at all | 1 occurrence |
| 28 | **Money overflow saturates instead of refusing** — 22 sites in one slice alone, `unwrap_or(i64::MAX)` ≈ $92 quadrillion, on the SUCCESS arm too | 22 sites |
| 29 | **A negative fee is signed, sealed into the append-only history, then silently clamped to 0** | `mod.rs:1129` |
| 30 | **The rate-amendment audit trail is a volatile 1000-entry ring behind a no-op seam**, four strings, no figures, `DualControl` hard-wired `Single`. Erasable by 1000 admin calls or one restart | `entry.rs:163` |
| 31 | **The customer read prices at the CURRENT card and derives an unpriced model at 0** — the source calls that *"the designed behavior"* | `cost.rs:744` |
| 32 | **A signed sub-day amendment cannot reach its own rows.** `arrived_ms` IS captured then discarded; `history`/`arrived` are never read while the doc beside them promises the opposite | `units_llm.rs:1048` |
| 33 | **The ledger migration is dead code** — `fold_v1_ledger` has no production callers; old rows deserialize to empty; every customer's counters zero on upgrade **(PARKED — rewrites stored data)** | `usage_migration.rs:121` |
| 34 | **Item 19's currency removal is NOT done — my "done" was a false zero.** #66 bars a currency type OR SYMBOL; `busbar-kernel-audit/src/record.rs:137` carries `pub currency: String` and `recipe.rs:189` pushes it into the **signed digest**. Removal re-cuts a published cryptographic contract **(needs a migration story)** | 47 sites |
| 35 | **`plane-pricing-blindness` is RED on 5 of 11 rows, 17 findings** (not 16-wide) with a zero-row ledger**, incl. `cost_pricing_enabled` — the branch #43 outlaws — inside the LLM plane, which collides with C3 | 16 findings |
| 36 | **The ledger WRITE is gated on the rate card** — owner already ruled this a defect | `govern.rs:239` |
| 37 | Four committed green tests assert the opposite of their own decision rows (D5, D8, D9 + four llm tests proving a mid-stream cut bills zero, against #62) | 8 tests |

### Gate-census additions — three instrument defects that ARE real (replacing struck item 11)
| # | item | measure |
|---|---|---|
| 87b | **`instance-noun-neutrality` holds ZERO dialect nouns.** Its 25 nouns are 6 plane, 7 transport, 4 store, 5 auth, 1 secret, 1 hook, 1 export. It structurally cannot see `DEFAULT_PROTOCOL = "anthropic"` or any dialect leak — which is why item 57 needed a separate finding | 0 of 6 |
| 87c | **`qa/audit-ledger.json`: not one scope carries a `fixed_at`.** 146 scopes, 586 findings (112 HIGH), 33 never audited. All 7 gate rows GREEN — the row about fixes passes over an empty set | 0 fixed |
| 87d | **`plane-config-noun` is armable TODAY.** Reserved because its selftest was red; I ran it — ALL GREEN, resolves all four section nouns. Its stated blocker is stale. `--check` reports 17 config-parse debt lines across 8 kernel files | stale reason |
| 88 | **Three outright RULE REGRESSIONS — a planted defect that should be RED comes back GREEN.** These are gate bugs, not tree debt: `qa-names:glob-matches-something` (*"a config table carries a glob that matches nothing: expected Red, got Green"* — dead globs in `qa/*.toml` now pass silently) · `design-bindings` (*"a binding the ledger's own note calls UNPROVEN is refused: expected Red, got Green"*) · `reachability`'s two green arms go RED because the overlay lacks `qa/reachability-evidence.md` | 3 gates |
| 89 | **STANDING-RED POISONING — every red plant scores PROOF IMPOSSIBLE**, the exact defect `no-float-money` was just fixed for. `--selftest` exits 1 on: `map-proof` (**7 of 13 impossible**), `structure-lint` (3), `unconstructed` (2), `reachability` (2+1), `qa-names` (2), and 6 more. A gate that is already red cannot prove it can fail | 11 gates |
| 90 | **Owed rows with NO red case at all** — `sweep-coverage` 4 of 7 rows (`:denominator`, `:swept`, `:reachability-axis`, `:compiles-and-tracked`), `map-proof:floored`, `reachability:evidence` | 6 rows |

### WAVE 2 additions — the kernel slice (574 files a slice seam had dropped)
| # | item | measure |
|---|---|---|
| **99** | **A CLIENT DISCONNECT DURING TTFB LOSES THE MONEY WRITE — PROVEN BY A FAILING TEST.** `teller.rs:1159` `let _ended = terminal(...)`. The naive reading is wrong: `terminal` DOES run. What `let _ended` discards is the return value, **and the return value IS the money write.** `Posted::settle` (`hold.rs:660`) is a pure constructor that consumes the `Hold` by value and touches no book; the books move only in `Ledger::post` (`settle.rs:208`), reached only by the composition root reading `Ended`. So on abandon: hold consumed unrecoverably, cell emptied, `figures.open_holds` never decremented, **no ledger row, no WAL entry** — the reservation stays drawn against that principal's window forever. The comment at `:1156-1158` asserts three things that ARE true (audit sealed, cell empty, leases returned); the fourth — the posting — it does not name. **Fires in a burst on every deploy.** Live via `units_llm.rs:565` and `admin_mount.rs:122`. **THE TEST EXISTS** — `/tmp/hold-loss`, `crates/busbar-kernel/tests/abandoned_settlement.rs`, two cells identical but for whether the client stayed, both ending on the same four-figure assertion. Waited-for: `(1, 900, 0, 100)`. Went-away: **`(0, 0, 1000, 0)`** — no durable row, nothing settled, the reservation still drawn. It names no `Drop`, no `terminal`, no binding, so a `let _ended` → `let ended` rename still fails it. **CORRECTION A:** the reservation limb is LATENT today, not live — `record_hold_opened` has no production caller and the shipped door opens the hold at ZERO (`admit.rs:181`), so `reserved == 0`. What is lost on EVERY abandoned request regardless is the settled posting, the durable row and the WAL entry. **CORRECTION B:** the loss is BIGGER than recorded — the dropped future means `attach_late_accrual` (`units_llm.rs:617`) never runs either, so neither the terminal settlement NOR the per-token late accrual posts | `teller.rs:1162` |
| **100** | **The documented backstop has ZERO production callers — and could not help if wired.** Each recovery route checked with its own positive control: the SWEEP reads `None` because the drop empties the cell first (control: the same call returns a `Posted` with `settled == 42` in `recovery_and_ticks.rs:214`); RESTART has no journal of open holds to recover from and the abandoned unit wrote no WAL entry to be one; CHECKPOINT seals `settled=0, open_holds=1000` and signs it; **RECONCILIATION PASSES** — `residual` counts an open hold as accounted-for by being open, so `accounted == drawn` and no operator report ever names it. That is why this had to be found by reading. The drop's own doc leans on *"the cell the sweep also holds a key to"* — `tick::sweep_settle` (`tick.rs:275`), all 11 call sites are tests. And the drop empties the cell first, so a sweep would read `None` even if wired | 0 callers |
| **101** | **`plane-purity` is RED at HEAD and `plane-purity-strict` — the STRICTER twin — is GREEN over the same failure.** Strict's ceilings rest on a stated premise now false: *"Production is already 0 for every one of these (that is what makes --check green)."* A reviewer running the strict gate concludes C1 is fine. Root cause: `scanner.rs:154` decides test scope by FILENAME (`/tests/`, `_test.rs`, `_tests.rs`), and the 17 offending files are named `tests.rs` OUTSIDE a `tests/` dir — the `#[cfg(test)]` is in the parent `mod.rs`. Same shape as the cfg-above-a-pub-mod trap. The `_test.rs` arm matches ZERO files repo-wide | 21 hits, 17 mine |
| **102** | **C1's two instruments differ by two orders of magnitude and BOTH are neutralised.** Over the same crate `plane-purity` reports **0** (11 real production hits deleted by pragmas — 10 name `mcp`, 1 is `DEFAULT_PROTOCOL="anthropic"`) and `kind-isolation` reports **3047** with its ceiling written as exactly `3047` so it fires at 3048. Neither number is wrong for what it measures; **no instrument measures the claim** | 0 vs 3047 |
| **103** | **Manifests are in NEITHER `.rs` denominator, so the gate certifying "core names no instance" is structurally blind to core naming three planes.** `busbar-kernel/Cargo.toml:173` `default = [… "plane-llm","plane-mcp","plane-a2a"]`, six capability features those pull in, `openapi-schema`'s three plane forwards, and a dead optional dep on **`rmcp` — the MCP protocol SDK, referenced by ZERO `.rs` files in the crate** | 11 manifest lines |
| **104** | **SIX spend derivations, not three.** On a card silent about the model they answer `i64::MAX` (block), flat-fee-only (free), and `Err(LaneUnpriced)` (refuse). Three rounding rules (trunc / `div_ceil` / half-even), two overflow policies (`checked_` refuse vs `saturating_` bill-the-ceiling) | 6 |
| **105** | **`residual()` — the ledger's own imbalance detector — uses bare `+`/`-` on 8 `i128` columns the rest of the crate deliberately saturates.** Panics in debug, wraps in release, on values `settle_tests.rs:304` deliberately produces | `identity.rs:83,90` |
| **106** | **The money-equivalence test D3 is RED at HEAD for a STALE reason** — it asserts the kernel copy panics; the copy now saturates. A real regression here would be dismissed as "that one always fails" | `money_one_function_equivalence.rs:472` |
| **107** | **290 `pub fn` across the 9 kernel crates have no syntactic production call site — 189 high-confidence** (the name appears NOWHERE in production), 101 ambiguous, 41 with no test reference either. `dead_code` cannot see these: they are `pub` in lib crates, so the compiler's 17 warnings / 30 symbols in `busbar-kernel` are a disjoint, smaller set. **Replaces the unverified item 78.** Stated as a MEASUREMENT, not a delete list — validated against 5 known cases, not all 189 | 189 + 101 |
| **111** | **THE GATE TABLES POISON EVERY NAME-BASED MEASUREMENT IN THE REPO.** `xtask/src/gates/design_bindings/tables.rs` holds **409 quoted strings**, many of them long prose notes naming functions, files and line numbers. A census keyed on names reads them as references and scores dead code live. The worked example is the one that matters: `sweep_settle` — the unwired backstop for item 99's money loss — appears there, so **a name-grep scores it referenced**, and every other hit in the tree is in ONE test file. Worse, the note's own prose says *"nothing in crates/ calls sweep() on a production path at all"* — **the fact is already known and recorded where no instrument can act on it.** This is a repeatable false-negative generator sitting inside the gate tooling, and it is why the drop-path backstop stayed unwired without anything noticing | 409 strings |
| **108** | **`busbar-kernel-breaker` (11 files) and `busbar-kernel-scope` (5) have NEVER received a verdict row from any sweep** — not this one, not the prior 19-slice effort | 16 files |
| **109** | Ingress WS outbound queue is **unbounded** while the identical egress pump bounds both halves and records why; 4 WS pumps use raw `tokio::spawn` outside the drain grace; 7 `Mutex::lock().unwrap()` on the WS/stdio request path; 2 of 46 `extern "C-unwind"` slots lack `catch_unwind`; 3 raw buffer writes with no null check where the sibling checks | `duplex_ws.rs:74` |
| **110** | **No rustdoc gate anywhere** — `cargo doc`/`RUSTDOCFLAGS`/`broken_intra_doc_links` appear in zero workflow, xtask or script (control: `cargo clippy` hits 3 workflows). 5 broken intra-doc links live | 5 links, 0 gate |

## WAVE 3 — C3, byte-identity
*The oracle has never built the binary the claim is about. Everything else here is downstream.*

| # | item | measure |
|---|---|---|
| 37b | **The C3 CONFIG INSTRUMENT PROVES ALMOST NOTHING — 24 of 57 selftest cases are PROOF IMPOSSIBLE.** Root cause verified by me, and it is NOT the customer-facing break it first looked like: `PoolMember` has a hand-written `Deserialize` (to admit a bare name as well as the legacy rich object), and the schema RENDERER cannot see through it, so it emits `"fields": {}` and the gate reads that as 7 field REMOVALS. **Runtime parsing is intact — `RichMember` still accepts all 7 fields with `deny_unknown_fields`, so a 1.5.5 config boots.** But the gate is standing-RED on its own renderer's blind spot, and a standing-red gate cannot prove it can fail: `breaking: field REMOVAL is RED`, `field RETYPE is RED`, `newly REQUIRED is RED` are all IMPOSSIBLE. The instrument guarding the config contract is inert | `pools.rs:228`, 24 of 57 |
| 38 | **RECORD THE RIGHT BINARY.** `default` compiles in all four non-LLM planes; `parity_build_and_resolve` (`verify-1.6.0-done.sh:555`) runs a plain `cargo build`. **The oracle has recorded an all-planes build for the entire release. C3 is unevidenced, not weakly evidenced** | `Cargo.toml:192` |
| 39 | **All four non-LLM planes ship DEFAULT-ON.** Part 0 says "purely additive and off by default" in four places | 4 planes |
| 40 | **Law 7 holds for only 2 of 5 planes, and the cause is in CORE** — `boot.rs:70-77` calls every plane's `hydrate` with no slot and no config check. mcp and a2a gate on `store:`, streaming on `public_url` | 3 of 5 live |
| 41 | **The DONE script re-records its own reference** on the machine under test with no wholeness check — the exact practice both CI jobs name in their comments as the thing that went wrong | `:957` vs `ci.yml:3316` |
| 42 | **`llm.wire` is off `BODY_IS_CONTRACT`**, so `body` rates 3 there and an `improvement` can forgive it — 62 of 133 LLM cells do. All 5 `ledger` cells have `body` waived, 4 by an improvement. `billing` is off the list too | 282 of 916 |
| 43 | **The LLM plane's entire oracle surface is ONE verb** — `chat` is the only `op` in all 2,318 cells | 1 verb |
| 44 | **The admin corpus is a fixture frozen at the 1.5.5 op set** (66 ops). Nothing reconciles it against the served router, so the 11 new admin paths have zero cells AND no mechanism that could give them one | 66 ops |
| 45 | **26 admin verbs are served and in NO OpenAPI document** (18 money-governance); 11 new paths mount unconditionally because both mount loops iterate linked planes, not configured ones | 26 verbs |
| 46 | **A side-car OpenAPI exists explicitly so the pinned bytes need not move**, hand-maintained, and says so in its own source | 1 document |
| 47 | **Two `breaking` register entries carry in-file AUDIT NOTES saying their premise is false today** and still grant their classes | `F-011r`, `D-3` |
| 48 | **The migration corpus has never seen the shipped release** — no v1.5.3/1.5.4/1.5.5 entry | 3 missing |
| 49 | `BUSBAR_ORACLE_RUST_BIN` / `BUSBAR_RELEASE_CHECKOUT` **repoint the judge itself** and are the two vars missing from `assert_bless_env_empty` | 2 of 13 |
| 50 | Re-record the golden with the Rust engine so `--allow-harness-skew` can be deleted from both workflows | 2 call sites |
| 51 | 22 in-scope cells have no golden, 14 named nowhere; `owed-baseline.txt` has 917 lines for 916 cells | 22 / 1 dup |
| 52 | Sign-off/expiry field on `accepted-differences.json`; re-adjudicate the 10 `breaking` entries; repoint or delete the 4 dead ones; triage the 8 unaccepted divergences | 41 entries |

## WAVE 4 — C1, core names no instance
| # | item | measure |
|---|---|---|
| 53 | **Core holds the list of plane config verbs** and types a struct field per plane | `prepass.rs:131` |
| 54 | **The dup-claim guard watches the wrong field.** The verb is `config_section`, resolved by `.find()` — first match silently wins. The guard reads `owned_config_sections`, which 3 of 5 planes leave empty. **`pools` is claimed twice** | `registry.rs` |
| 55 | **`PlaneMeta::CONFIG_VERB` has never existed** — #49 is written around a symbol with 0 hits (control: `PlaneMeta` is used throughout; the trait has 12 consts and this is not one). **The near-miss that makes this cheap: `CONFIG_SCHEMA` already exists at `plane.rs:44` and every plane already carries its schema — `CONFIG_VERB` is the missing NAME beside the existing SHAPE.** The acceptance test also already exists: `scripts/plane-config-noun-gate.sh --check` armed by `GREP_GATE_REPORT_ONLY=0`, today exit 1 with **17 core parse-target lines** across 8 kernel files. Adding the const is one worker; draining the 17 shards by file | 0 hits · 17 to drain |
| 56 | **The transport SCHEME axis is unrepresented — but this is one field short, not unbuilt.** CORRECTED on verification: `TransportMeta` EXISTS (`busbar-contract/src/transport/mod.rs:101`) with **16 consts** and a uniform `src/meta.rs` impl in **all 7** transports. None of the 16 is a URL-scheme set — and `KEY = "http"` is a REGISTRY key, not a scheme claim, since one transport must serve `http` AND `https`. `check_composition` already exists as a pure boot check and already carries `DuplicateKey`, so the fix is ONE field on `Registered` plus two error variants, not a new module. **Where the scheme knowledge lives today and must be deleted: `crates/busbar/src/root/registry.rs:197` — `authority.starts_with("wss://")`, a raw scheme literal in the composition root choosing between two compositions of one key.** Original finding stands on the zeros: — `TRANSPORT_DECL`, `load_transport`, `scheme_claim`, `owned_schemes` → 0 hits against 532 files containing "transport". #50 is entirely unbuilt | 6 symbols, 0 hits |
| 57 | **Core names five dialects and defaults to one**; 20 `http`/`https` literals in kernel production source | `proto/mod.rs:253` |
| 58 | **3,047 plane-vocabulary hits in `busbar-kernel`**, 1,942 in the composition root | `qa/kind-isolation.toml` |
| 59 | **Law 5 is unmet**: the six llm dialects live in a separate 105,589-line crate, not inside the plane. No plane crate has a `dialects/` dir — so the sharp test has no defined boundary until this moves | 0 dirs |
| 60 | `plane-purity-strict` ceilings to 0 — SYMBOL 61 · TYPE 13 · KEY 377 · **DIALECT 456** · BACKWARDS 33 | 940 |
| 61 | 723 `[[leak]]` rows; fix `plane-purity`'s denominator (3 shell vs 21 Rust vs 31 actual); un-excuse `instance-noun-neutrality` and wire it into CI | 723 rows |

## WAVE 5 — C2, compiled-in OR dropped-in
| # | item | measure |
|---|---|---|
| 62 | **TWO unrelated `PlaneDecl` types.** `#[repr(C)]` at `hot/decl.rs:132` used by one fixture; native Rust at `registry.rs:811` used by all five real planes. **Part 3 asserts ONE seam and outranks Part 2 — the authoritative section is the wrong one** | 2 types |
| 63 | **LAND THE ABI RIDER (owner ruling 9) — HALF IS ALREADY BUILT.** `DynPlane`, the C-ABI→native adapter, is production at `plugin-loader/src/plane.rs:61`; `build_plane_host_vtable()` is production at `plane_host/vtable.rs:36` and the crossing is asserted end-to-end in a 715-line test (44 of 44 slots over live core primitives). **Genuinely owed: a REAL plane as a `cdylib` (only the `publish=false` fixture is one) and the composition-root `open_plane` call** | 2 of 4 owed |
| 64 | ~~For all 7 kinds the only `cdylib` is an unpublished test fixture~~ **WRONG — a one-repo census artefact.** Ten SIBLING repos ship real RELEASED dropped-in plugins: `store-postgres` v1.0.6 · `store-sqlite` v1.0.6 · `store-valkey` · `store-mysql` · `auth-oidc` v1.0.6 · `auth-github` · `auth-ldap` · `hashicorp-vault` v1.0.5 · `headroom-hook` v2.0.7 · `webrequest-hook`. **store, auth, secret and hook have shipped drop-in implementations. Only PLANE and TRANSPORT genuinely have none** — which is exactly where items 63 and 65 already point | 2 of 7, not 7 of 7 |
| 65 | **Transport is absent four ways**: no `TRANSPORT_DECL`, missing from `KNOWN_KINDS` (`sign.rs:70`), no `supported_abi` arm, no `load_transport` | 4 absences |
| 66 | `open_export` has **zero callers tree-wide**; `open_plane` zero production callers; the binary never mentions `load_plane`/`DynPlane` | 0 / 0 / 0 |
| 67 | **#85's acceptance clause is OWED** — the last conformance test is an empty body labelled *"this test is a landmark"*. kind:plane has no both-folds BEHAVIOURAL test, only a decl-shape compare | 2 of 7 kinds |
| 68 | `kind-isolation` RED (12 rows) + **84 `[[dep]]` rows recording not-allowed edges**. Green-by-reconciliation is not green-by-absence | 84 rows |
| 69 | 3 real lateral transport deps (4 more are dev-deps); 13 of 21 plugin crates breach the dependency wall | 3 + 60 findings |
| 70 | **`plugins.yaml`, "the single source of truth for what plugins exist", expresses 4 of 7 kinds** — export, plane and transport cannot be registered | 4 of 7 |
| 71 | **The `Dockerfile` claims "ships with ZERO plugins pre-installed"** — `auth-admin-tokens` and `hooks-ranking` are both in the default feature set | 2 baked in |
| 72 | **The decisions plane is inert and lives inside the core binary**; `busbar-plane-decision` contains 0 `PLANE_DECL`; `units_decision.rs` does not exist yet `reachability.rs:248` declares it | 308 lines |
| 73 | **`busbar-plane-llm` has ZERO `PLANE_DECL`** and one production reference repo-wide | 0 / 1 |
| 74 | **Two streaming crates both ship and both register** — `busbar-voice` (13,353 loc) and `busbar-plane-streaming` (5,135 loc) | 2 crates |

### Gate-census additions — the ratchets are far larger than the list said
| # | item | measure |
|---|---|---|
| 91 | **`ci.yml` is RED on `predev` today** — 12 `run:` steps exit non-zero, including `cargo test --workspace --locked` (`xtask/tests/cli.rs:73`, the registry-vs-workflow test) | 12 steps |
| 92 | **1,203 ratchet rows outstanding**, not the ~800 estimated: 723 leaks (**3,959 occurrences**) + 200 non-zero cells (**sum 29,572**) + 191 deps (84 not-allowed, 6 owner-pending) + 24 disagreement/question/transitional + 18 unshipped capabilities + 4 reserved segments + 10 expired raises + 33 non-zero ceilings | 1,203 |
| 93 | **852 gap rows sit UNDER GREEN GATES.** `qa/audit-ledger.json`: **586 findings** (112 HIGH), **84 scopes with findings and zero `fixed_at`**, 33 unaudited — gate passes because `fix-owes-confirmation` runs over an empty set. `qa/inventory-coverage.json`: **266 named gaps**, `CONF` 204 of 205 uncovered — gate passes because a gap is "named" | 852 |
| 94 | **`kind-isolation:matrix` is 38,394 hits** across 267 cells and 1,814 files — the single largest number in the release | 38,394 |
| 95 | **21 live findings have no declaration row at all** — 17 `plane-pricing-blindness` (not 16), 4 `reachability` | 21 |
| 96 | **`qa/teller-steps.json`'s plane axis is wrong** — `llm, mcp, a2a, voice, admin`: `decisions` absent, `admin` is not a plane, `voice` not `streaming`. Gate green | 50 cells |
| 97 | **Two more reserved segments nobody listed** — `smart-router` reserved "→ active in 1.5.6", a SHIPPED release; and `plane-config-noun`, reserved because its selftest was red — **it is GREEN today and armable now** | 2 segments |
| 98 | `full_gate.rs:405` says `kind-isolation-ship` adds "two" rows. It adds **four**, `:faces` flips PASS→FAIL, and 10 rows are red | 4 not 2 |

## WAVE 6 — dead weight, the fold, and the record
| # | item | measure |
|---|---|---|
| 75 | **526 production dead items + 117 test-scope**, measured per-crate with forced lints. `busbar` 282 · mcp 68 · a2a 48 · llm 23 · core-admin 22. **The transports have ZERO production dead code** — stdio/grpc/http reproduce 41/39/37 only under `--all-targets`, so that block is test debt and is split out here | 526 + 117 |
| 76 | **One blanket `#![allow(dead_code)]` suppresses the compiler across 38,356 lines**, and its own stated removal condition is already met (`main()` references `root::` 61 times). Six such blankets in production | 6 |
| 77 | **~8,750 lines of dark scaffolding**: ~5,100 under `root/units_*.rs` + **3,653 in `busbar-llm/src/unit/`**, the plane C3 rests on. Zero `[[dormant]]` rows declared | 8,750 loc |
| 78 | **99 `pub fn` with zero production callers** across 23 crates | 99 |
| ~~79~~ | ~~87 coverage gaps behind two `#[ignore]`s~~ **STRUCK — a false NON-zero of mine.** `qa/method-coverage.missing` and `qa/field-coverage.missing` are BOTH EMPTY: 64 and 23 are the LINE counts of files whose every line is a `#` comment (data rows: 0 and 0). Both `#[ignore]`d tests would pass today | struck |
| 80 | **Two QA segments say in-file "→ active in 1.6.0" and are still `reserved`** — the two headline planes have no live QA leg | `mcp-integrity`, `a2a` |
| 81 | Crate fold 49 → 33. Only 8 crates sit outside the shipped binary, so ≥8 folds must merge SHIPPING crates and perturb the binary's graph | 16 folds owed |
| 82 | **Two High-severity advisories — fix in 1.6.0 (ruling 3); disclosure timing is the owner's** | shipped 1.5.0–1.5.5 |
| 83 | **Rewrite the spec: 61 of 118 checkable claims are wrong** (44 stale, 17 false). 7 GOLD clauses false. Part 2: the repo's own row-by-row audit (`1.6.0-decision-audit.md`) says **37 CONTRADICTED / 28 HOLDS** — the "60 of 85" I recorded was unsourced and is withdrawn. 11 citations name paths or symbols that do not exist. Three mutually inconsistent dates survive against ruling 10 | 61 / 118 |
| 84 | **#77's "8-verb admin surface" is a 17-verb surface** — all five verbs the row says were removed are live and wire-routed (this was my withdrawn question 5; the answer is 17) | 17 verbs |
| 85 | **`#81a`/`#83a`/`#84a` are cited 12+ times including inside `no_float_money.rs`, and Part 2 has no lettered rows.** Row 81 is malformed — a stray `\|` splices a body into the verification cell | 3 broken + 1 malformed |
| 86 | **Three rows are stale OPTIMISTICALLY** — the work landed and the row still says owed: #65, #76, #79. Also done: testkit, Pass/Grant, `UnitKey::new(0)`, 2 of 3 #40 edges, the branch collapse | 3 + 6 |
| 87 | Docs collapse: 267 files → 1 spec + ~95 keep-public + ~100 trash. **Must not orphan the 104 parity bindings** (ruling 2). `#58`'s duplicate pairs named | 267 |

---

## OWNER RULING 12 — EACH PLUGIN IS ONE CRATE, AND ONLY ONE. SETTLED.

Owner, 2026-09-23: *"each plugin is 1 and only 1 crate another hard rule."*

**Consequences, both directions — this rule folds AND protects:**

It REQUIRES four folds (a plugin split across crates must become one):
`busbar-llm` + `busbar-plane-llm` + `busbar-llm-codec` → one llm plane (32,834 LOC) ·
`busbar-mcp` + `busbar-plane-mcp` → one (14,258) · `busbar-a2a` + `busbar-plane-a2a` → one
(13,825) · `busbar-voice` + `busbar-voice-codec` + `busbar-plane-streaming` → one (9,976).
`busbar-plane-decision` is already compliant. **−6 crates.**

It FORBIDS the fold I had proposed: merging the six reference plugins into one
`busbar-plugin-examples`. Each is a plugin, so each is a crate. With ruling 11's seven transports,
**17 crates are now permanently protected.**

**THE ROSTER IS 37, NOT 33**, and the difference is these two rulings. 49 − 6 (plane folds) − 1
(breaker+egress → one `route` unit) − 2 (plugin infra) − 2 (timing, connsec) − 1 (delete `api`) = 37.
Going lower means deleting the reference plugins outright, which removes the working proof of C2 —
one per kind — for a claim already refuted on three kinds.

## OWNER RULING 13 — NO CURRENCY SYMBOL. IT IS WRONG. FIX THE PAST.

Owner, 2026-09-23: *"no currency symbol its just wrong. figure out how to fix tha past."*

`pub currency: String` (`busbar-kernel-audit/src/record.rs:137`) is field 29 of 44 inside a SIGNED
published digest, under `DIGEST_RECIPE = "busbar.audit.digest.v1"` (`recipe.rs:37`), with a
committed golden and an ed25519 signature.

### THE MIGRATION — and why it is cheaper than this plan assumed

**Do NOT re-sign history.** A chain that can be re-signed at will is not a tamper record; it is a
document with a signature on it. The past is not broken — it is recorded under an older recipe, and
that is a different thing.

The code names its own remedy (`published_recipe_tests.rs:261`): *"Either restore the published
order or publish a NEW recipe version beside it."* So:

1. **v1 records stay valid under v1, unchanged and un-re-signed.**
2. **Cut recipe v2 without the field.** New records only.
3. **Tag each record with the recipe that produced it.** This is the piece that does not exist
   today — `record.rs` carries no recipe version — and it is what lets one verifier read both.
4. **Publish the v2 document beside v1's**, as v1 was published.
5. **Wire verification ONCE, recipe-aware.**

**Step 5 is why the cost estimate in this plan was wrong.** Item 34 priced this as "a v1 verifier
kept alive forever", which assumes a deployed verifier population to support. There isn't one: I
checked, and **nothing outside `busbar-kernel-audit` verifies the audit chain.** `verify_chain` at
`record.rs:529` and `legacy/chain.rs:302` are internal calls within the defining crate.

So we are not migrating a live verifier — **we are writing the first one, and it can be born
knowing two recipes.** Doing that is strictly cheaper than wiring a v1-only verifier now and
migrating it afterwards. **Item 34 is re-priced from "a recipe migration" to "tag the record, cut
v2, and wire the verifier that was owed anyway."**

### A FIFTH DUPLICATE, found on the way

`crates/busbar-kernel/src/calllog.rs:10` says it out loud: *"`verify_chain` — a second copy of what
other planes' provenance chains had and a **third** of what…"*. Confirmed: **three separate
`verify_chain` implementations** — `busbar-a2a/src/taskstore.rs:228` (live, called at `:601`,
`:947`), `busbar-kernel/src/calllog.rs` (live, called at `:451`), and `busbar-kernel-audit`'s own.
Three chain verifiers, three spellings of "was this record altered", **no instrument comparing
them.** Fifth instance of two-implementations-both-shipping, and the first at three.

## OWNER RULING 11 — EACH TRANSPORT IS A PLUGIN. SEVEN, NOT ONE. SETTLED.

Owner, 2026-09-23, verbatim: *"each transport is a plugin i set that rule and not to be discussed
again so we have 7 not 1 transports."*

**The seven transport crates are OFF the fold table permanently.** `http`, `ws`, `tls`, `grpc`,
`sse`, `stdio`, `tcp` — 5,534 lines across seven crates — each is a plugin instance of the
`transport` kind, exactly as the five planes are instances of the `plane` kind. Any future proposal
to merge them is refused by this ruling without argument. Do not re-open it; do not "measure whether
it would help".

### WHAT THE RULING COSTS — because the code does not have a transport kind

The ruling makes explicit something that was previously only a gap. If each transport is a plugin,
then each must satisfy C2 — compiled in OR dropped in, over the same contract. **Today not one of
them can be, because the plugin machinery has SIX kinds and `transport` is not among them.**
Verified, all three layers:

| layer | what it holds |
|---|---|
| `busbar-plugin/src/cold/mod.rs:90-109` | the kind constants: `STORE`, `SECRET`, `AUTH`, `HOOK`, `EXPORT`, `PLANE` — **six** |
| `plugin-loader/src/sign.rs:70` | `KNOWN_KINDS = ["store","auth","hook","secret","export","plane"]` — **six** |
| `plugin-loader/src/registry.rs:43` | `supported_abi` matches per kind and has **no transport arm** |

And `sign.rs:607` **hard-refuses any manifest whose `kind` is outside `KNOWN_KINDS`** — so a
`kind: transport` plugin cannot be signed, let alone loaded.

**The spec says seven kinds (Part 2 #3). The code implements six.** That is not a ratchet or a
count; it is a kind that exists in the architecture and not in the binary.

### This explains three findings that looked unrelated

- **`construction/rules2.rs:314`** lists six kinds and omits `transport`, so `manifest-allowlist`
  emits 14 rows and **no transport row at all** — green while thirteen crates breach the dep wall.
  It was not an oversight in the gate; the gate matches the code's six-kind reality.
- **`INSTANCE_VOCAB_KINDS = ["plane","transport"]`** — the C1 gate measures a transport kind the
  plugin system does not have. One of its two axes is aimed at something unimplemented.
- **`plugins.yaml` expresses 4 of 7 kinds.** The registry cannot express `transport` because nothing
  downstream could load one.

### The ordering this forces

Widening `plugins.yaml` or the kind vocabulary to accept `transport` **before** the loader, signer
and ABI can carry one would create a registry naming a kind no binary can load — a new false-green
surface of exactly the class those registries exist to close. **The kind must land in
`cold/mod.rs`, `KNOWN_KINDS` and `supported_abi` first**, then the registry, then the seven crates'
manifests. That sequence is now a Phase 4 item with a stated order, not a loose set of gaps.

## OWNER RULINGS OWED

~~**Item 21 — the overcharge.**~~ **STRUCK — my false conclusion.** I read `UsageLedger`'s field
doc and then indicted a `MeteringDelta` writer on a different path; the refund is live at
`ingress/mod.rs:671-676`. I had escalated this as inverting ruling 8. It does not. Corrected in
`1a642eb1d`; the entry is struck here because the plan contradicted itself, saying "NO OWNER RULING
IS OWED" in one place and listing it here in another.

**Each of the following needs a decision, not a measurement. I have changed nothing on any of them.**

1. **Item 15 — `main`'s 7 divergent commits.** Only an owner protection change resolves it; no code
   fix exists. `enforce_admins: true`, `allow_force_pushes: false`, and `qa` is not a descendant.
2. **Item 34 — the audit-chain `currency` field** is field 29 of 44 inside a SIGNED published
   digest with a committed golden and an ed25519 signature. Removing it is recipe v2 + per-record
   tagging + a re-derived golden + a v1 verifier kept alive — not a field deletion. The code names
   its own remedy at `published_recipe_tests.rs:262`.
3. **THE RATE-CARD MONEY CELL — is the new figure the intended behaviour?**
   `billing|rate-card|history-mid-window` moves `spend_final` 750,090,000 → 277,530,000 µ, caused
   by `root-llm` alone (2×2 isolated, no interaction). Two things make this a decision rather than
   a defect report: the measured triple is digit-for-digit the **priced-at-charge** answer the
   cell's own `why` names, so it looks deliberate — **but that same text says ONLY the final read
   should move, and three reads moved** (`by_key`, `by_model`, `total`). If intended, D-3 needs
   `effects.script` added to its classes AND the two earlier reads explained. If not, it is a
   money defect on the revenue plane.
4. **THE LEDGER 404 → 403 — accepted or not?** `ledger|amend|adjusting-entries` and
   `|refused-unsigned` move `not_found` → `forbidden`, caused by `root-admin` alone. **The
   register's own differ already has a rule for this**: a `status` change requires a `breaking`
   entry with a changelog line. D-4 covers `body` and `headers` and not `status`, so the rule was
   never reached. Accepting it means writing that entry; refusing it means the status is wrong.
5. **THE KEY-USAGE CELL — the exception ruling 8 already implies, but nobody wrote.**
   `billing|key-usage|after-upstream-down` moves `spend 250 → 0`, and the cell's own `why` says **0
   is correct** — an upstream that served nothing owes nothing. That is ruling 8's shape exactly
   (C4 beats C3 where 1.5.5's bytes are wrong), so it needs a **signed C3 exception**, not a fix.
   The missing exception is the defect.

## RULINGS ALREADY GIVEN
1 auth vocabulary exempt · 2 parity bindings survive the collapse · 3 fix advisories, disclosure is the owner's · 4 no currency · 5 *(withdrawn — answered by measurement: 17 verbs)* · 6 delete testkit **(done)** · 7 review then burn · 8 fix the under-billing, no restatement · 9 land the rider · 10 no date

## DONE THIS SESSION
kernel testkit deleted (−1,392 lines) · `CurrencyCode` the type removed and the 100× scale defect fixed with a demonstrated RED · `--strict` armed on three oracle callers · `inventory-ref`'s blank-binding vacuous pass closed · `no-float-money`'s `MONEY_INTAKE` declared (it had **no valid proofs at all** — every red plant scored PROOF IMPOSSIBLE)

---

# THE BACKLOG, FILED — items 160–468


309 findings from audit rounds 2–4, filed as numbered work. Every quote below was
machine-verified against the line it cites at `e1bdc6b80`; a finding whose quote no longer
matched its line was NOT filed as work — it is held in STRUCK/DRIFTED at the end.

**Provenance.** 454 raw findings across 44 lenses → 430 distinct after global dedupe on
(file, line, quote); 24 were found independently by more than one lens. Of the 430:
100 were already cited earlier in this document, 26 drifted, 1 names a file the tree no
longer has, and these **309** are new work.

**What the two content columns are.** The first is the defect. The second is the
*positive control* — the command or observation proving the instrument CAN see this,
so that a green reading is evidence and not silence (Law 0). **The exit test is owed at
fix time and is deliberately not asserted here**; inventing 309 exit tests mechanically
would be exactly the kind of unfalsifiable instrument this list exists to remove.

**The fold does not retire these.** The fold touches 63 of 1,806 tracked `.rs` files
(3.5%); 292 of these 309 sit in files that survive it. The 17 of THIS BATCH that do not are
recorded as STRUCK-BY-FOLD below rather than deleted. (Document-wide the struck total is 18 —
item 553 arrives with the later scripts batch. Both counts are correct in their own scope.)


**Totals: HIGH 70 · MEDIUM 147 · LOW 67 · NIT 25**


## `xtask — the instruments` — 77 items (HIGH 25 · MEDIUM 46 · LOW 4 · NIT 2)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 160 | HIGH | `xtask/src/cli.rs:229` | changelog-register's release arm is unreachable: ROW_VERSION is owed only when require_version is set, and the CLI hard-codes the --require-version flag to the changelog gate alone, exiting 2 for any other | The identical flag on the sibling gate IS reachable and IS wired: .github/workflows/release-stage.yml:283 runs 'cargo xtask gate changelog --require-version="$V" --require-dated-top', and cli.rs:237-243 builds ChangelogGate with it. grep -rn -- '--require-version' .github/workflows/ scripts/… |
| 161 | HIGH | `xtask/src/full_gate.rs:345` | Excuse::ReleaseScript checks only that the gate's name appears on a non-comment line of scripts/verify-1.6.0-done.sh -- never that anything runs that script, and nothing does | grep -rn 'verify-1.6.0-done' .github/workflows/*.yml returns 6 hits, every one a # comment. The identical command for a script CI really runs, grep -rn 'public-hygiene-lint.py' .github/workflows/ci.yml, returns two run: lines (:1549, :1551). The needles themselves do resolve on executed lines: grep… |
| 162 | HIGH | `xtask/src/full_gate.rs:593` | The registry-vs-workflow set equality reconciles only against ci.yml, so a gate name the registry has DROPPED is still invoked by the release oracle and still asserted by a workspace test with nothing comparing either against the registry | Extracting cargo xtask gate <name> from scripts/*.sh gives 18 names; comm against the 50 registry names leaves exactly one unregistered, audit-ledger (the other two residues, --all and --list, are flags). The 17 that do resolve -- construction, kind-isolation, teller-steps and the rest -- are the… |
| 163 | HIGH | `xtask/src/gates/config_schema/classify.rs:275` | `refusal_findings` — the arm that catches a hand-written `Deserialize` quietly accepting an input form it used to reject — returns before doing anything whenever the BASELINE node has no `refused` key, and the pinned `v1.5.3` baseline the release actually uses has zero `refused` keys on any node, so the arm can never emit a finding | ZERO-CLAIM: `git show v1.5.3:crates/busbar/src/config/config-schema.snapshot.json > /tmp/base.json; grep -c '\"refused\"' /tmp/base.json` -> 0. POSITIVE CONTROL on the identical file with the identical command form: `grep -c '\"fields\"' /tmp/base.json` -> 57 and `grep -c '\"kind\"' /tmp/base.json`… |
| 164 | HIGH | `xtask/src/gates/config_schema/schema.rs:166` | The decisions: plane grammar is outside the frozen config surface: the snapshot records the section's type NAME and never its fields, so removing or renaming one is invisible to snapshot-drift and to additive-only | The voice plane's config IS extracted because crates/busbar-voice/src/config.rs is an explicit entry at :305 -- same class of type, same kind of crate, opposite outcome. Measured against the committed snapshot: grep -o '"Decision[A-Za-z]*"'… |
| 165 | HIGH | `xtask/src/gates/conformance_sync/render.rs:308` | conformance:freshness anchors the release commit to the mode of the very verdicts it judges, so it can only ever catch a minority of stale verdicts -- and it is GREEN today over ten verdicts 647 commits old | Flip one verdict's commit and the row does fire -- the minority case works, so the mechanism and the path are right. The same circularity is identified and repaired for the sibling baseline at config_schema/mod.rs:26-32 ('HEAD IS THE WORKING TREE WEARING A REF'S CLOTHES'); it is un-repaired here.… |
| 166 | HIGH | `xtask/src/gates/construction.rs:269` | The manifest-allowlist COMPLETENESS ORACLE omits transport exactly as the rule does, so no manifest-allowlist:busbar-transport-* row is OWED, the missing rows are not even DID NOT RUN, and one of the seven transports carries a dependency the rule would score RED today | ls -d crates/busbar-transport-* \| wc -l -> 7, none of which gets a row. The same grep -nE '^\s*busbar-(kernel\|contract)\s*=' on crates/busbar-plane-llm/Cargo.toml, a kind that IS in the list, returns two busbar-contract lines, and that crate does get a row |
| 167 | HIGH | `xtask/src/gates/construction/external.rs:215` | `denylist_hits` has three return paths and all three are `Some(..)`, so the `None` the construction gate's `source-denylist` rule treats as 'the transitive-closure half never answered -> UNPROVEN -> RED' is unreachable; and the delegated scan's own `defects` channel — which external.rs:18 claims is 'folded into the rows as hits would be' — is read nowhere in the gate | ZERO-CLAIM (`denylist_hits` never returns None): `sed -n '197,226p' xtask/src/gates/construction/external.rs \| grep -n '\bNone\b'` -> no output. POSITIVE CONTROL, identical grep over the whole file: `grep -n '\bNone\b' xtask/src/gates/construction/external.rs` -> :55, :90, :150, :193 — four hits,… |
| 168 | HIGH | `xtask/src/gates/field_inventory.rs:586` | field-inventory:both-directions has a FAIL arm no possible input can reach: the loader already guarantees every (dialect, direction) pair is non-empty before the row is computed | The case at :681-692 claims to prove it red, but its naming is 'missing required key request', which is ROW_PROVENANCE's FAIL text. ROW_BOTH_DIRECTIONS appears only as the unproven(...) = Row::skip emitted at :571, and evidence_for (gates/mod.rs:1861) counts status != Pass, so a SKIP scores as red… |
| 169 | HIGH | `xtask/src/gates/hot_path_perf.rs:75` | hot-path-perf's p50/p99 rows and hot-path-alloc's pod-batch row assert that a threshold is compared, but the check is three independent substring tests over the whole bench file -- the comparison itself is never verified, and CI never runs the bench | grep -rn 'cargo bench' .github/ scripts/ qa/ matches only qa/segments.toml:238 and :256, both 'cargo bench --workspace --no-run' -- the bench binary is built and never run, so the source-marker check is the only thing behind these rows. The selftest (:211-223) strikes only claim.markers[0], the… |
| 170 | HIGH | `xtask/src/gates/hot_path_perf.rs:105` | hot-path-perf and hot-path-alloc check 'harness = false' anywhere in the manifest rather than in the bench's own block, and busbar-kernel has three bench blocks -- so removing it from the one that matters still passes | The selftest plant (hot_path_perf.rs:195-198) strikes 'name = "plane_host_vtable_perf"' instead, which IS unique in the manifest, so the case is green and the second conjunct is never exercised. grep -n '^\[\[bench\]\]' -A 3 crates/busbar-kernel/Cargo.toml returns three blocks, each carrying… |
| 171 | HIGH | `xtask/src/gates/kind_isolation.rs:5238` | Five live `prove_rows_red` cases in the `control`-kind block (kind_isolation.rs:5227-5322) demand needles that no rule in the gate can produce, so the eleven `[[registered]]`-arm findings they cover have no working red proof | ZERO-CLAIM: `grep -rn 'control-sink' xtask/src/` -> exactly two lines, kind_isolation.rs:2245 (a COMMENT saying the refusal is gone) and kind_isolation.rs:5238 (the ASSERTION). No emitter. POSITIVE CONTROL that a covered rule in this file has the two-site emitter+assertion shape, under the… |
| 172 | HIGH | `xtask/src/gates/kind_isolation.rs:6833` | Two red cases plant a dependency edge onto crates that are not in the tree, and both `rule_deps`'s drain refusal and `measure_edges` skip any declaration whose package is absent from `by_name` — so the edge is never measured and `unlisted-transitional`, the single refusal separating 'the drain' from 'the fusion', has no proof that names a resolvable subject | ZERO-CLAIM: `grep -rn '^name = "busbar-unit-audit"' --include=Cargo.toml .` -> exit 1, no output; same for `^name = "busbar-substrate"`. POSITIVE CONTROL, identical command form on the crates those names were presumably meant to be: `grep -n '^name = "busbar-kernel-audit"'… |
| 173 | HIGH | `xtask/src/gates/kind_isolation.rs:7239` | `kind-isolation:legacy-drain` has exactly two selftest cases and its only RED one plants an EMPTY overlay, which `inert_plant` refuses before the gate is ever run, and asserts a needle (`busbar-core`) that is the `from` of zero `[[transitional]]` rows in a crate that no longer exists — so the ship criterion's drain row has no working proof of falsifiability at all | ZERO-CLAIM (the needle names nothing): `grep -c 'from = "busbar-core"' qa/kind-isolation.toml` -> 0, and `ls -d crates/busbar-core` -> 'No such file or directory' (the case's own comment at line 7232 asserts '`busbar-core` exists'). POSITIVE CONTROL, identical command: `grep -c 'from =… |
| 174 | HIGH | `xtask/src/gates/kind_isolation.rs:7556` | `kind-isolation:control-path`'s zero-subject refusal is proven by `kinds_gone(cx, &["control"])`, which removes the manifest of every crate whose kind is `control` — and no crate resolves to that kind — so the overlay is empty, `inert_plant` refuses it, and the rule the case targets reads a DIFFERENT kind anyway (`rule_control` filters `c.kind == Some("cleanliness")` at line 4228) | ZERO-CLAIM: `grep -n 'kind: "control"' xtask/src/gates/kind_isolation.rs` -> exit 1, no output, so `kinds_gone(cx, &["control"])` can never match a census entry and returns `Overlay::new()` (function body at :7611-7622). POSITIVE CONTROL, the working sibling in the same file: `kinds_gone(cx,… |
| 175 | HIGH | `xtask/src/gates/kind_isolation/matrix.rs:1750` | Four red cases plant their violation under `crates/busbar-plane-admin/`, a directory with no `Cargo.toml`, and every rule that would read it keys on the manifest census — so the planted file is never opened and the assertions are doubly dead, because the ledger carries no `[[cell]]` and no `[[face]]` row for that crate either | ZERO-CLAIM: `ls crates/busbar-plane-admin/Cargo.toml` -> 'No such file or directory' (the directory itself does not exist). And the ledger has nothing to raise: `awk '/^\[\[cell\]\]/{c=1} c&&/crate/{print; c=0}' qa/kind-isolation.toml \| grep -c busbar-plane-admin` -> 0. POSITIVE CONTROL, identical… |
| 176 | HIGH | `xtask/src/gates/mod.rs:179` | The `qa-names` posture entry asserts the gate is green outright with an empty standing-red list; the gate is RED on this tree and `cargo xtask gate qa-names --posture` — the exact command ci.yml:330 runs as a blocking step — exits 1 | Positive control on path existence: `ls crates/busbar-kernel/src/teller.rs` and `ls qa/construction.toml` both resolve under the identical method, so the absence is the tree's, not the command's |
| 177 | HIGH | `xtask/src/gates/mod.rs:332` | ship-ready's REPORT_ONLY posture can never apply: Excused::ReleaseTime dereferences full_gate::REGISTRY_NOT_IN_CI, and ship-ready has no entry there because ci.yml invokes it -- so gate --all scores it and is permanently red | The same arm succeeds for kind-isolation-ship, which IS in REGISTRY_NOT_IN_CI -- the test the_release_time_posture_is_read_from_full_gate_and_expires_with_it (mod.rs:3248) asserts excused_from_all('kind-isolation-ship', &cx, &red).is_some(). Set difference of REGISTRY_NOT_IN_CI's ten names against… |
| 178 | HIGH | `xtask/src/gates/no_float_money.rs:463` | `no-float-money:count-read-shape` — the row whose title is 'a count is read with a silent zero default instead of a refusal (#81)' — only recognises silent-zero defaults spelled through a serde_json accessor, and every billed token count in this tree is now read through the crate's own `read_count_u64` helper, which the scanner has no needle for | ZERO-CLAIM (the detector sees none of them): re-ran the gate for real — `./target/debug/xtask gate --all` line 468 prints `PASS no-float-money:count-read-shape no count is read with a silent zero default`. POSITIVE CONTROL that the search path and method are right: `perl -0777 -ne 'my $c=0; $c++… |
| 179 | HIGH | `xtask/src/gates/plane_purity/scanner.rs:532` | plane-purity's forward vocabulary is four legacy crate names while the same gate derives its reverse population from the kind table's eleven plane crates, so a #[path] include reaching into any busbar-plane-* crate is invisible to the one row that exists to catch it | The gate's own selftest plant at plane_purity/mod.rs:402 is '#[path = "../../../busbar-mcp/src/witness.rs"]' and it DOES red ROW_PATH_INCLUDE. Same construct, one crate-name segment different, opposite verdict |
| 180 | HIGH | `xtask/src/gates/reachability.rs:1299` | When a roster plane's root/units_<plane>.rs is absent, both unit-path and root-reach take an Ok arm and become Row::pass -- and crates/busbar/src/root/units_decision.rs does not exist, so two of the decision plane's three rows can never say NO | The Some(m) arm for llm/mcp/a2a/streaming -- all four modules exist -- does produce Err(...), which is why gates/mod.rs:160-175's posture names units_voice, units_mcp and units_a2a as currently red. Same code, four planes red-able, one not. Both red-proof fixtures (xtask/fixtures/reachability/ and… |
| 181 | HIGH | `xtask/src/gates/seal_witness.rs:16` | The seal-witness gate's header calls itself ENFORCED (line 4) and states this row holds, but the row is UNSATISFIABLE on this tree and the gate exits 1; `.github/workflows/ci.yml:574` runs it as a BLOCKING step | The sibling row PASSED in the same run (`PASS seal-witness:no-surviving-proof-name ... 1053 files scanned`), proving the walk, the scan set and the comment-stripper all work and the path is right |
| 182 | HIGH | `xtask/src/gates/service_images.rs:834` | The one row that exists because 'a lint reading only .github/workflows/ would have declared the tree clean on the day the hole was open' reads a single hard-coded script, skips any image reference spelled as a shell variable, and floors at 3 against a script that yields exactly 4 | grep -n 'WIREMOCK_IMAGE=\\|OPENLDAP_IMAGE=' scripts/release-check.sh -> 0 matches; the identical grep on scripts/release-check-1.5.2.sh -> 2 matches (:199, :200). The covered file today yields 4 digest-pinned literals at release-check.sh:1506, :1528, :1549, :1569, which the gate's own test… |
| 183 | HIGH | `xtask/src/gates/structure_lint/roots.rs:36` | structure-lint:axis:purity -- 'nothing branches on an axis outside that axis's own arms' -- reads 7 crate prefixes out of 49; busbar-contract and all eight busbar-kernel-* workflow crates, the money path included, are outside it, and two production lines match the ban there today | The same construct INSIDE the scope is caught and required a written waiver: crates/busbar-kernel/src/registry.rs:367 reads 'left.transport == right.transport', and axis.rs:189-191 carries the AxisException ledger row for exactly that file. grammar.rs:362 is structurally the same comparison in a… |
| 184 | HIGH | `xtask/src/gates/structure_lint/roots.rs:42` | structure-lint invariant 6 -- 'a property that holds on one plane holds on all of them' -- enumerates 2 planes of the roster's 5, so a concern duplicated between any pair that does not include both mcp and a2a is invisible | The rule demonstrably fires on the mcp/a2a pair: plane_dups.rs:122-134 ships 13 signed DISTINCT rows (config.rs, transport.rs, judge, revalidate, observed_pin, Transport, contains, get, list, openapi_schemas, reresolve_gates, metadata_route, admin_view.rs), each of which had to be written down… |
| 185 | MEDIUM | `xtask/src/cli.rs:143` | Registration::tier (Fast/Full/Release) is read at exactly one place -- the --list printout -- so nothing in the runner ever acts on which tier a gate belongs to | grep -rn 'tier' xtask/src xtask/tests, excluding the tier: Tier:: literals in the registry, returns exactly one functional read: cli.rs:143 inside the --list println!. The same grep for reg.build returns the two live construction sites at cli.rs:161 and :163, proving the method and the path are… |
| 186 | MEDIUM | `xtask/src/gates/blocking_ffi.rs:396` | blocking-ffi arms only inside an 'async fn' or after an offload opener, and tokio::spawn is neither -- so a blocking plugin call inside an async block spawned from a sync fn is never tested | The identical line inside crates/busbar-transport-grpc/src/client.rs:71 ('pub(crate) async fn handshake_h2('), which also spawns at :81, IS flagged. 42 tokio::spawn( sites exist in non-test source, so the sync-fn shape is present in the tree today rather than hypothetical |
| 187 | MEDIUM | `xtask/src/gates/ci_umbrella.rs:45` | `ci-umbrella:tier` is documented as mirroring the umbrella's `FULL_TIER` expression; the code never reads `FULL_TIER` and tests two substrings, one of which names only `refs/heads/main` while the real expression names three branches | `grep -n 'FULL_TIER' xtask/src/gates/ci_umbrella.rs` returns only the two doc-comment lines (:45, :669) and no code reference, while the same grep over ci.yml returns 4 real uses — the name exists on one side only |
| 188 | MEDIUM | `xtask/src/gates/conformance_sync/render.rs:560` | `conformance:no-orphan-claim` is documented as 'no human-visible claim string outside the marker block LACKS a backing manifest entry', but the implementation only ever searches for the exact token of each REGISTERED suite — so a certification claim for a standard that is in no registry, which is precisely an unbacked claim, is structurally invisible | The token set is derived from the registry, not from a vocabulary of claim words: `render.rs:559-565` builds `let token = alt_text(s)` per registered suite and tests `outside.contains(&token)`. ZERO-CLAIM: there is no word list, no regex for 'certified\|compliant\|conformant' anywhere in the module… |
| 189 | MEDIUM | `xtask/src/gates/construction.rs:495` | The construction gate's scan-set floor — the one place that turns 'this rule had no subject' into a RED — matches only the `"vacuous: "` PREFIX, and four rule sites declare an absent subject with a different spelling the floor never sees, so two rows are measuring nothing and reporting green on HEAD | LIVE OUTPUT: `./target/debug/xtask gate construction --format=tsv \| grep -E 'slice\|unit-verbs'` -> the two PASS rows quoted above, verbatim. ZERO-CLAIM on the subjects: `find crates/busbar-kernel -name slice.rs` -> no output; `ls -d crates/busbar-unit-verbs` -> 'No such file or directory'.… |
| 190 | MEDIUM | `xtask/src/gates/construction/rules.rs:584` | `single-terminal` counts call sites of a configured NAME with no floor for 'I found none', so a renamed terminal makes the rule pass forever while printing `allowed callers seen: []` in the same sentence — three sibling rules in this same gate each spend an explicit refusal on precisely that condition | The name resolves today, so the rule is live and this is latent: `grep -rn finish_inner crates \| head -3` -> crates/busbar-kernel/src/ingress/mod.rs:491 `finish_inner(` plus a test citation. POSITIVE/NEGATIVE pair under the identical command form: `grep -rn finish_inner_zzz_absent crates` -> no… |
| 191 | MEDIUM | `xtask/src/gates/construction/rules2.rs:728` | `sealed-unit-traits` drops any configured trait whose `file` or declaration no longer resolves — not counted, not named, not a finding — and then divides by the shrunken set, which is the exact defect fixed in `no-default-bodies` forty lines earlier in the same file | The rule's subjects resolve TODAY, so this is latent rather than firing: `grep -rn 'pub trait Breaker' crates` -> crates/busbar-kernel-breaker/src/lib.rs:147 `pub trait Breaker: sealed::Sealed {`; `grep -rn 'pub trait Admission' crates` -> crates/busbar-kernel-budget/src/lib.rs:79 `pub trait… |
| 192 | MEDIUM | `xtask/src/gates/construction/rules2.rs:763` | sealed-unit-traits silently drops any configured trait whose file has moved or whose trait header no longer matches, then passes on the reduced set -- the sibling rule carries the guard this one lacks | The sibling no-default-bodies has the identical logic WITH the fix, at rules2.rs:708: 'current <= max_defaulted && missing.is_empty(),' -- and its comment (rules2.rs:697-705) documents the measured proof that the unguarded form passes on a moved file. All three configured traits resolve today… |
| 193 | MEDIUM | `xtask/src/gates/feature_sets.rs:390` | `feature-sets:every-h2-rig-is-run-by-a-named-step` asks only whether each rig's path occurs anywhere in the raw, un-comment-stripped bytes of `ci.yml`, so a commented-out step, a step behind `if: false`, or a path mentioned only in prose satisfies the row that claims the rig is RUN | ZERO-CLAIM (the rig rule never strips comments): `grep -n "strip_comments\\|starts_with('#')" xtask/src/gates/feature_sets.rs` -> one hit only, line 316, inside `job_key` (job-header detection); nothing in `rig_row`. POSITIVE CONTROL, identical command on a sibling gate: `grep -cn 'strip_comments'… |
| 194 | MEDIUM | `xtask/src/gates/field_inventory.rs:170` | `field-inventory` has exactly one floor in the whole gate — one schema FILE — and no floor on the number of FIELDS, so `--write` relays whatever the schemas currently say straight into `qa/field-inventory.json` and every row passes over the smaller number | ZERO-CLAIM: `grep -n 'min_files\\|floor\\|FLOOR' xtask/src/gates/field_inventory.rs` -> ONE hit, line 170. POSITIVE CONTROL, identical command on the sibling coverage gate: `grep -n 'min_files\\|floor\\|FLOOR' xtask/src/gates/inventory_coverage.rs` -> :50 ROW_FLOOR, :66 INVENTORY_FLOOR, :165… |
| 195 | MEDIUM | `xtask/src/gates/hot_path_perf.rs:15` | The row claims it holds the pairing of this bench's name with `harness = false`; the code runs two independent whole-file substring tests against a manifest that contains three `[[bench]]` blocks, each supplying `harness = false` | The other half of the conjunction IS red-provable: the selftest at hot_path_perf.rs:190-201 plants a manifest with the `name = ...` line struck and the row goes red, so the mechanism works for the half it exercises and has simply never been run against the `harness = false` half |
| 196 | MEDIUM | `xtask/src/gates/hot_path_perf.rs:81` | `claim_row` tests each marker independently with `bench.contains(m)` over the whole file, and the p50 and p99 claims share two of their three markers — so each row can be satisfied by the OTHER row's surviving assertion, and the selftest only ever strikes `claim.markers[0]` | The marker positions are read off the real bench: `grep -n 'p99_delta_nanos\\|p50_delta_nanos\\|HOT_PATH_BUDGET_NANOS\\|assert!' crates/busbar-kernel/benches/plane_host_vtable_perf.rs` -> :20 (doc), :59 (const), :137 p50 binding, :138 p99 binding, :140 assert!, :141 p50 compare, :146 assert!, :147… |
| 197 | MEDIUM | `xtask/src/gates/instance_noun_neutrality.rs:109` | The store kind is censused on four backends that have no crate in the tree, while the two store-kind crates that DO exist have no Noun row at all, so the store kind's real instance vocabulary is unpoliced | The gate already uses the right pattern for the sibling kinds: tokens: &['secret_example_plugin'] (:298-303) and hook_test_plugin (:308), which have 1, 4 and 2 live rows respectively in qa/instance-noun-neutrality.toml. The 'match the example plugin's own crate identifier' mechanism works and… |
| 198 | MEDIUM | `xtask/src/gates/instance_noun_neutrality.rs:512` | The instance-noun burndown ledger records a per-file count for all 723 leak rows and never compares it: the key is (noun, file), so every baselined file is an unbounded growth zone no row can see | crate::toml_doc has int_of (toml_doc.rs:91) -- the capability exists and is unused here, while str_of is called twice in the same closure. The gate's own selftest (:725-732) proves :undocumented reds for a NEW (noun, file) pair, so the row bites on a new file and structurally cannot bite on a new… |
| 199 | MEDIUM | `xtask/src/gates/kernel_token_wire_purity.rs:34` | The gate titled 'THE KERNEL NEVER RE-DERIVES A USAGE TOKEN CLASS FROM A RAW PROVIDER WIRE POINTER' reads one directory, while 'the kernel' in this tree is nine crates — and the one that computes money, `busbar-kernel-ledger`, is outside it; its floor of 8 (line 38) is doc'd as 'Eleven production files when this was written; the floor tracks the real tree rather than `> 0`' against a real tree of 199 | ZERO-CLAIM on the scope: `ls -d crates/busbar-kernel-*` -> busbar-kernel-audit, -breaker, -budget, -egress, -identity, -ledger, -scope, -wal — eight sibling crates, none of them under `crates/busbar-kernel/src`. POSITIVE CONTROL that the WIRE_FIELDS vocabulary is grep-able and the method works:… |
| 200 | MEDIUM | `xtask/src/gates/kind_isolation.rs:689` | rule_shape's exemplar for the unit kind is a crate that is not in the tree, and the missing-exemplar arm continues, so no unit crate is ever checked for no-lib, entry-count or skeleton | ls -d crates/busbar-unit-* returns only crates/busbar-unit-transport-key. The other two exemplars in the same table resolve: crates/busbar-plane-a2a and crates/busbar-transport-http both exist |
| 201 | MEDIUM | `xtask/src/gates/kind_isolation.rs:4482` | `kind-isolation:transport-registration` passes under the title 'each wire is composed in exactly one place and no plugin links one' while its only predicate is `> 1` — a wire composed NOWHERE passes, and the pass detail even prints the evidence | LIVE: `./target/debug/xtask gate --all` line 544 prints `PASS kind-isolation:transport-registration each wire is composed in exactly one place and no plugin links one`. The rule's population is real today, so this is latent rather than firing: every one of the seven wires has exactly one… |
| 202 | MEDIUM | `xtask/src/gates/kind_isolation.rs:5312` | Both arms of the registration ratchet run over an empty table, and both of their red proofs are refused one layer earlier by the load-time unknown-kind check | Table census of qa/kind-isolation.toml via grep -o '^\[\[[a-z]*\]\]' \| sort \| uniq -c: 200 [[cell]], 191 [[dep]], 119 [[edge]], 13 [[disagreement]], 6 [[question]], 5 [[transitional]], 3 [[face]] -- and no [[registered]] at all. The same command finds the other seven tables, so the method and the… |
| 203 | MEDIUM | `xtask/src/gates/kind_isolation/matrix.rs:2001` | The only case for the vendor-name leak asserts a dialect column the matrix explicitly does not have, and plants two words that are in no kind's derived vocabulary, so it moves no cell | grep -c 'kind = "dialect"' qa/kind-isolation.toml returns 0, while the file's 200 [[cell]] rows span 19 other kinds that the same grep shape does find |
| 204 | MEDIUM | `xtask/src/gates/map_proof.rs:1863` | The refusal written under the banner 'VACUITY IS NOT A PASS' requires `checkable > 0`, so a corpus document with ZERO bound expectations falls straight through to `Row::pass` — and two of the seven corpus documents are in exactly that state on HEAD | ZERO-CLAIM and POSITIVE CONTROL in one sweep of the seven corpus documents with the identical command, `grep -c '#[[:space:]]*->' <doc>`: 1.6.0-map-proof.md 231, 1.6.0-ratchet-census.md 3, 1.6.0-oracle-coverage.md 7, 1.6.0-instrument-audit.md 5, 1.6.0-gate-sweep.md 5 (five documents DO bind arrows,… |
| 205 | MEDIUM | `xtask/src/gates/mod.rs:104` | The entire justification for the `REPORT_ONLY` excuse vocabulary rests on `keep-proof.yml` running `cargo xtask gate --all` on every hand-back; that file does not exist under that name and its successor is dispatch-only | The same grep over ci.yml finds 37 distinct `cargo xtask gate <name>` invocations (e.g. ci.yml:574 `cargo xtask gate seal-witness`), so the method locates real CI invocations; `--all` simply is not one of them |
| 206 | MEDIUM | `xtask/src/gates/mod.rs:221` | The comment asserts `CONSTRUCTION_STANDING_REDS` and `land.sh`'s `land_construction_standing_reds` subtract the same rows; they differ by four rows and nothing enforces the parity | `grep -rn 'land.sh' xtask/src/` returns only the two prose mentions (mod.rs:221, mod.rs:414) and no test, so the 'keep in step' instruction has no mechanism behind it; the equivalent parity for `full_gate::CARGO_LOCAL` IS held by a selftest (documented at mod.rs:190-196), which is the shape this… |
| 207 | MEDIUM | `xtask/src/gates/mod.rs:224` | The doc block says `ceiling-rose` is on the standing-red list and describes the transaction that will strike it; the name is not in the list, and the `[gate.ceiling_raises]` entry it blames is not in qa/construction.toml either | The same posture output DOES name the five rows that are on the list, and prints `STALE `terminal-doors-in-audit-step` is named as a standing red and is not red any more`, so the stale-name check works — `ceiling-rose` simply is not a member for it to check |
| 208 | MEDIUM | `xtask/src/gates/mod.rs:286` | The comment states `plane-no-money` is carried on `CONSTRUCTION_STANDING_REDS`; it is not a member, so it is scored as a NEW RED and fails the blocking `construction --posture` step | `plane-no-money` resolves as a real construction rule — `grep -n 'plane-no-money' qa/construction.toml` finds its `[rules.plane-no-money]` stanza — so the name is live; only its membership in the excuse list is fiction |
| 209 | MEDIUM | `xtask/src/gates/mod.rs:2179` | inert_plant has exactly one caller, inside prove_red; every GREEN selftest case in every gate can therefore be taken over a plant that changed nothing, and the harness reports it as a pass | grep -n 'inert_plant' xtask/src/gates/mod.rs returns exactly two lines: the definition at :2073 and the single call at :1945 inside prove_red. The same grep for narrowed_got, a helper both paths do use, returns four lines including calls reached from prove_rows_green's siblings. ls -d… |
| 210 | MEDIUM | `xtask/src/gates/money_invariants.rs:10` | The money-invariants gate is headed ENFORCED and claims to keep #77's record invariants 'from silently regressing', but the record scan has no floor and no existence row: rename any money record struct and both rows PASS while printing a hardcoded count of 8 | The sibling row in the same gate DOES carry a floor — `SCAN_FLOOR = 200` applied through `min_files` at :92/:209 — so the author knew the pattern and the file demonstrates it one row over; the record scan simply omits it. A missing records.rs is also handled (:248-255), proving the read path reds… |
| 211 | MEDIUM | `xtask/src/gates/money_invariants.rs:274` | `money-invariants:no-plugin-keyed-money` and `:no-stored-price` print '{N} money record type(s) scanned' from the length of the CONSTANT rather than the number of structs actually found, and nothing requires that even one of the eight names matched — so both rows pass over a zero-struct population while claiming to have scanned eight | All eight names resolve TODAY, so the row is not vacuous now and nothing in the code would tell you when it became so: `for s in VirtualKey ModelTokens UsageLedger ModelTokensDelta UsageDelta MeteringDelta MeteringRow AuditRecord; do grep -c "^pub struct $s {" crates/busbar-contract/src/records.rs;… |
| 212 | MEDIUM | `xtask/src/gates/no_deferral.rs:459` | A deferral waiver's expiry is discharged by its [retires: <ID>] tracker row existing in either the open or the COMPLETED state, so there is no state in which a waiver is expired | tracker_has_row does fire correctly on a non-existent id -- the selftest at :889-896 proves [retires: ZZ999] red -- and all four in-use ids (H5, N1, N2, N3) are '- [ ]' at tracker lines 170, 193, 194, 195, so the hole is not realised today. Measured: 126 of the tracker's 155 rows are already '- [x]' |
| 213 | MEDIUM | `xtask/src/gates/no_float_money.rs:339` | A gate titled 'NO BINARY FLOATING POINT ... ON ANY MONEY PATH' detects floats by a case-sensitive whole-word match on the strings `f64`/`f32`, so any float that is never spelled that way on the offending line is invisible | ZERO-CLAIM, showing the idiom already exists in this repo: `grep -rnE '= *[0-9]+\.[0-9]+;\|< *[0-9]+\.[0-9]+\|\( *[0-9]+\.[0-9]+ *\)' --include='*.rs' crates/busbar-kernel/src \| grep -vwE 'f64\|f32' \| grep -v /tests/` -> 6 hits including `crates/busbar-kernel/src/observe.rs:312: if m.value < 0.0… |
| 214 | MEDIUM | `xtask/src/gates/no_float_money.rs:1134` | The count-read area floor sums one walk per home and compares the multiset total, but the a2a area nests one home inside another, so 34 files are counted twice and a whole home can leave the scan without tripping it | The sibling areas do not overlap and their floors bite honestly: 'the money book and the budget' = busbar-kernel-ledger/src (23) + busbar-kernel-budget/src (7) = 30 distinct against floor 24; 'the loader and the store seam' = 16 + 8 = 24 distinct against floor 18. Only the a2a group nests a home… |
| 215 | MEDIUM | `xtask/src/gates/plane_pricing_blindness.rs:166` | The money-blindness gate's scope is a hand-written crate list checked only listed-to-disk, never disk-to-listed, and its scan floor is one file across the whole workspace -- so a plane crate nobody adds is a plane crate it scans zero files of | The identical line in a LISTED dir does produce a price finding -- that is what every prove_rows_red_at case in the xtask/fixtures/plane-money selftest exercises. The sibling gate already has the right answer and uses it: plane_purity/mod.rs:161 calls kind_isolation::plane_kind_src_roots(cx) with… |
| 216 | MEDIUM | `xtask/src/gates/plane_pricing_blindness.rs:599` | A row named `plane-pricing-blindness:scan-floor` whose only floor is ONE FILE over a population of 1616, emitted as a bare `Row::pass` with the detail 'the scan cleared its floors and named nothing' on every path where the census returns Ok | ZERO-CLAIM (no other floor in the file): `grep -n 'min_files\\|FLOOR\\|floor' xtask/src/gates/plane_pricing_blindness.rs` -> line 599 is the only `min_files` and there is no numeric floor constant. POSITIVE CONTROL that a real floor is the house style and the grep finds one when it is there: the… |
| 217 | MEDIUM | `xtask/src/gates/plane_pricing_blindness.rs:698` | `plane-pricing-blindness:meter-class-declaration` is the gate's only positive control — its doc at line 23 says it exists 'because a gate that went green by the planes forgetting to meter would be measuring nothing' — and it is satisfied by the type NAME `MeterClassDecl` appearing on any production line, including a bare `use` line or an empty array | The two cases are already distinguishable in the real tree, which is what makes the weaker predicate a hole rather than a theory: `grep -rn 'MeterClassDecl' crates/busbar-plane-decision/src` -> 3 hits, of which `meta.rs:13` is a bare `use` line (`AdminVerbId, ClassDirection, MeterClassDecl,… |
| 218 | MEDIUM | `xtask/src/gates/plane_purity/freeze.rs:21` | `plane-purity:core-llm-family-freeze` says 'core names zero concrete LLM-family IR type' and walks ONE of the twenty-one neutral roots `planes::neutral_src_roots()` declares, so the freeze can be declared met by a file move between two neutral crates | ZERO-CLAIM: `ls -d crates/*/src/ir` -> crates/busbar-kernel/src/ir (the only one the witness walks), crates/busbar-llm-codec/src/ir, crates/busbar-substrate-values/src/ir, crates/busbar-voice-codec/src/ir — four `ir/` directories, one scanned. POSITIVE CONTROL that the wider list exists and is used… |
| 219 | MEDIUM | `xtask/src/gates/segregation.rs:565` | `segregation:reverse` reports 'nothing in the tree depends on xtask — the gate runner is a judge of the tree, never a part of it' while walking three roots, and three manifests in this repository are outside all of them; the companion floors are set 5-20x below their populations | ZERO-CLAIM/POSITIVE pair, one command: `find . -name Cargo.toml -not -path './target/*' -not -path './crates/*' -not -path './xtask/*' -not -path './.git/*'` -> ./Cargo.toml (walked, named root), ./fuzz/Cargo.toml, ./testing/ws-conformance/subject/Cargo.toml,… |
| 220 | MEDIUM | `xtask/src/gates/service_images.rs:63` | The gate whose subject is 'every container image this repo runs is the digest one table pins' scans exactly two locations — the workflow directory and one named script — with no discovery of `docker run` sites and no floor over them, and a third provisioning script runs three containers on floating tags today | ZERO-CLAIM: `grep -n 'shadow-oracle\\|oracle-box' xtask/src/gates/service_images.rs` -> exit 1, no output. POSITIVE CONTROL, identical command form on the paths it DOES know: `grep -c 'release-check\\|workflows' xtask/src/gates/service_images.rs` -> 35 matches. The out-of-scope floating tags: `sed… |
| 221 | MEDIUM | `xtask/src/gates/ship_ready.rs:135` | ship-ready:standing-reds decides 'nothing is standing red' from whether a Rust constant slice is empty, while run() holds the construction gate's actual verdict five lines later and reads only two ceiling rows off it | The gate DOES read the construction verdict for the facts it chose to measure: ceiling_rows(&construction) at :274 resolves ROW_SLACK/ROW_ROSE out of construction.rows and reds on a missing row (:192-200). So the verdict is in hand; only ROW_STANDING declines to consult it |
| 222 | MEDIUM | `xtask/src/gates/structure_lint/oversized.rs:64` | The grandfathered list is declared in the module doc as a shrink-only ratchet ('SHRINKING THE LIST IS THE ONLY PERMITTED EDIT. A pull request that ADDS an entry for NEW code is not a fix, it is evading the check') and no row counts it, ratchets its length, or checks an entry still names an over-cap file — the sole consumer is a `continue` | ZERO-CLAIM (nothing reads the list except the skip): `grep -n 'grandfathered' xtask/src/gates/structure_lint/oversized.rs` -> the definition at :64 and one consumer, `:106 if t.grandfathered.contains(&rel) {`, inside the scan loop. POSITIVE CONTROL that the same gate DOES ratchet its other debt… |
| 223 | MEDIUM | `xtask/src/gates/structure_lint/plane_dups.rs:159` | Invariant 6 ('one concern implemented twice is how a fix lands in one copy and not the other') compares two plane corpora, and the tree declares four plane roots plus five `PLANE_DECL` sites — so a plane-local copy of a plane helper is structurally invisible, and one such copy is in the tree with its own comment explaining why it was made | ZERO-CLAIM/POSITIVE pair, the same top-level-declaration extraction run over three plane pairs: mcp INTERSECT a2a (the pair the gate DOES scan) -> get, judge, list, metadata_route, observed_pin, openapi_schemas, reresolve_gates, revalidate, tap_join_verdict, Transport; voice INTERSECT a2a (not… |
| 224 | MEDIUM | `xtask/src/gates/structure_lint/roots.rs:13` | The header says the plane roots and protocol crates are derived rather than spelled 'because a constant is a thing to remember on the day a dialect lands'; both are spelled constants, and both have already forgotten a plane | `structure-lint:plane-roots` PASSED in the `gate --all` run ('every plane resolves to exactly one home'), which is true of the two planes it knows about — the row cannot report the two it was never given |
| 225 | MEDIUM | `xtask/src/gates/sweep_coverage.rs:416` | sweep-coverage:reachability-axis swallows a failure of its own git oracle into an empty set, so an unanswerable question and a clean tree produce the same Pass | The sibling oracle read in the same crate does the opposite and is the control: Ctx::cargo_tree returns Err rather than an empty answer, with the comment 'a tree that resolved and named NO PACKAGE AT ALL -- absent is exactly the claim an unresolved tree fakes.' And the sweep denominator row in this… |
| 226 | MEDIUM | `xtask/src/gates/sweep_coverage.rs:477` | `sweep-coverage`'s ghost detection — 'every file that compiles is a file git has' — recognises a module declaration only when it is spelled `mod X;` or `pub mod X;` (it strips the literal string `"pub "`), so `pub(crate) mod X;` and `pub(super) mod X;` are invisible, and it never looks for `#[path]` despite the doc at line 97 claiming it does | ZERO-CLAIM (the invisible spelling is common in this tree): `git grep -hnE '^\s*pub\((crate\|super)\) mod [a-z_]+;' -- '*/mod.rs' '*/lib.rs' '*/main.rs' \| wc -l` -> 84. POSITIVE CONTROL, identical command with the spelling the probe DOES see: `git grep -hnE '^\s*(pub )?mod [a-z_]+;' -- '*/mod.rs'… |
| 227 | MEDIUM | `xtask/src/gates/sweep_coverage.rs:478` | sweep-coverage:compiles-and-tracked recognises 'mod X;' and 'pub mod X;' only -- trim_start_matches('pub ') does not strip 'pub(crate) ' -- and the #[path] form its own header promises to cover is implemented nowhere | git grep -hE '^\s*pub\(crate\) mod [a-z_0-9]+;' -- '*.rs' \| wc -l -> 86 live occurrences the rule cannot see; the identical command for '^\s*(pub )?mod [a-z_0-9]+;' -> 1501 that it can |
| 228 | MEDIUM | `xtask/src/gates/tracing.rs:50` | The floor that exists because "'Every `#[instrument]` has a level' is VACUOUSLY TRUE over zero files" counts FILES (777 in production) and not SPANS (five `#[instrument]` attributes in the whole tree), so the subject can leave the tree entirely with the floor untouched | ZERO-CLAIM on the subject count, using the gate's own attribute predicate: `grep -rcE '^[[:space:]]*#\[[[:space:]]*(tracing[[:space:]]*::[[:space:]]*)?instrument' crates \| grep -v ':0'` -> crates/busbar-llm/src/arrival.rs:3, crates/busbar-kernel/src/ingress/mod.rs:2,… |
| 229 | MEDIUM | `xtask/src/gates/tracing.rs:135` | tracing:instrument-level accepts any attribute whose text contains the substring 'level', including a span FIELD named level, which does not set the span's Level at all | The selftest at :302-303 proves the LITERAL form 'name = "log_level"' IS flagged, because string literals are blanked; the identifier form is never planted. Same rule, same file, opposite verdict |
| 230 | MEDIUM | `xtask/src/gates/unconstructed.rs:629` | For a capability declared `unshipped = true`, the `unconstructed:site:<id>` row is `Row::pass` in both the no-sites and the has-sites branch — structurally incapable of failing — and eighteen of the nineteen declarations in `qa/unconstructed.toml` carry that flag, including seven money capabilities | COUNTS: `grep -c '^\[\[capability\]\]' qa/unconstructed.toml` -> 19; `grep -c '^unshipped' qa/unconstructed.toml` -> 18. LIVE: `./target/debug/xtask gate --all` lines 433-451 show two rows reading 'is constructed in production' and seventeen reading 'DECLARED UNSHIPPED, tracked', all PASS. POSITIVE… |
| 231 | LOW | `xtask/src/gates/config_schema/selftest.rs:321` | The 'a type that MOVED is not a break' green control plants into a crate that is in no manifest and on no disk, so the pair it belongs to has lost its half that says 'fine' | The paired red case at :335-344 (ov.remove(moved) alone, naming OverlayDoc) is unaffected and still works, and every other Overlay::remove plant in this slice targets a path that exists -- qa/design-bindings.json, docs/design/ARCHITECTURE.md, crates/secret-ref/src/lib.rs, crates/api/src/auth.rs,… |
| 232 | LOW | `xtask/src/gates/construction/ceilings.rs:187` | ceiling-slack computes the orphan-pin list and discards it, against the contract its own helper's doc comment states | The same two-tuple IS destructured and acted on at ceilings.rs:276, inside rewrite |
| 233 | LOW | `xtask/src/gates/kind_isolation.rs:7232` | A kind-isolation selftest case is built on the stated fact that `busbar-core` exists; the crate was absorbed into busbar-kernel and does not exist, so the paired green case's differentiating `remove` is a no-op | `crates/busbar-voice/Cargo.toml` does exist, so the second `remove` in the same overlay is doing real work and the overlay mechanism itself is sound |
| 234 | LOW | `xtask/src/gates/sweep_coverage.rs:32` | The completeness gate's header presents a five-row table as the set of NOs it can produce; the gate owes seven rows, and the two missing ones include the highest-severity check in the file | The five listed row ids all exist as constants at :79-83, so the table is not wrong about what it names; it is incomplete about what the gate owes |
| 235 | NIT | `xtask/src/gates/mod.rs:413` | The `Excused::OnlyRows` diagnostics hardcode the construction gate's constant and script function; the same code path serves `qa-names`, whose operator is told to edit two things that do not govern it |  |
| 236 | NIT | `xtask/src/gates/structure_lint/plane_dups.rs:229` | The duplicate gate's DEBT half is unreachable: Class::Debt is never constructed anywhere, so the three declared concerns and the ledger-integrity rule that validates a row's concern id can never fire | ZERO-CLAIM CONTROL: `grep -rn 'Class::Debt' --include='*.rs' xtask` returns exactly one line, plane_dups.rs:229. The SAME grep for `Class::Distinct` returns three lines including two constructions (plane_dups.rs:117 and selftest.rs:788), so the search does find constructions when they exist |

## `busbar` — 31 items (HIGH 7 · MEDIUM 15 · LOW 6 · NIT 3)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 237 | HIGH | `busbar/src/root/durability.rs:778` | Two byte-identical admin-mutation audit rings ship in one process; the kernel's persists every mutation onto the durable journal seam, the root's is constructed with a no-op seam, so admin mutations routed through the root's admin unit leave nothing on disk | ZERO-CLAIM CONTROL for 'the satellite ring persists nothing': `grep -rn 'NoSeam' --include='*.rs' crates` matches crates/busbar/src/root/durability.rs:96 (import) and :778 (injection) plus the no-op impl at crates/busbar-kernel-audit/src/legacy/entry.rs:320-325. The SAME search style for the kernel… |
| 238 | HIGH | `busbar/src/root/gauntlet_install.rs:11` | This file's own module doc says install() registers zero planes (dormant), but the function doc three lines below (line 51: "W2.b — EVERY plane is FLIPPED onto the unified kernel loop") and the function body itself (lines 63-76) unconditionally register the kernel-loop runner for mcp, a2a, llm-native and voice under their already-default-on feature flags, and this contradicts three OTHER files in the same slice that… | grep -n "flip_one_shot_to_kernel\\|flip_session_to_kernel" crates/busbar/src/root/gauntlet_install.rs shows the four unconditional calls inside pub fn install() (lines 65,67,73,75); grep -n "DORMANT" crates/busbar/src/root/*.rs main.rs shows the same string used with the opposite meaning… |
| 239 | HIGH | `busbar/src/root/policy.rs:156` | The boot check specifically written to catch a configured pool with no lane expansion -- which this file's own module doc says causes silent per-request underbilling -- has zero production callers; it is exercised only by its own unit tests | grep -rn "pools_without_expansion" crates/busbar/src/ matches only its own definition (policy.rs:156) and two calls in root/tests/policy.rs:146,154. Contrast: grep -n "root::policy::" crates/busbar/src/main.rs crates/busbar/src/root/kernel.rs finds real production callers for sibling functions in… |
| 240 | HIGH | `busbar/src/root/registry.rs:250` | The ONLY claim-overlap boot check in the codebase (`plane_claims()`/`seal_claims`, registry.rs:241-261) is built entirely from a vestigial `PlaneMeta::CLAIMS` object family (LlmPlane/McpPlane/A2aPlane/StreamingPlane/AdminPlane from the busbar-plane-* crates, always constructed `::EMPTY` in `register_all()`) that is structurally disconnected from the claims that actually decide live per-request plane routing in 1.6.0… | grep -rn "root::registry::" crates/busbar/src/ (excluding registry.rs itself and its own tests/) matches only main.rs -- proving the search method finds real call sites when they exist (contrast with e.g. `root::policy::build`/`client_settings`/`group_table`, which the same grep style finds called… |
| 241 | HIGH | `busbar/src/root/units_llm.rs:991` | The whole sweep half of the hold-cell protocol is unbuilt. crates/busbar-kernel/src/tick.rs:267 states `This is the SECOND holder of a key to a hold cell, and there is no third`, and crates/busbar-kernel/src/inflight.rs:305 states `The drop guard MARKS; it never ends a unit. A marked slot whose task is gone is what the sweep turns into TaskLost`. In production NOTHING calls `UnitSlot::mark()`, nothing calls… | `grep -rn '\.mark()\|is_marked\|\.marked' --include='*.rs' crates/ \| grep -v /target/ \| grep -v busbar-kernel/src/inflight.rs` matches ONLY crates/busbar-kernel/tests/recovery_and_ticks.rs:{189,298,380,457} and crates/busbar-kernel/src/tick.rs:144. The identical grep form against the SAME… |
| 242 | HIGH | `busbar/tests/ledger_identity.rs:476` | The one integration test that drives the shipped binary pins the served ledger view's fee-count column at 0 for every row, and it can only pass because BOTH sides of the node's own reconciliation identity hard-code that column to zero -- so `reconcile`'s `l.fee_count == g.billable_requests` is `0 == 0` on every row of every live node, forever | The RECONSTRUCTED half of the same test does assert a non-zero fee count under the identical method -- `crates/busbar/tests/ledger_identity.rs:378-383`: `let fee_total: u64 = ledger.values().map(\|r\| r.fee_count).sum(); assert_eq!(fee_total, total_requests, "one flat fee per billable request, and… |
| 243 | HIGH | `busbar/tests/plane_isomorphism.rs:110` | `installed_decls()` says it is 'the same consts the composition root pushes through install_planes' and enumerates FOUR planes; the default build installs FIVE, and the missing fifth is the one plane that is `None` on fifteen-plus hooks with no allow-row accounting for any of them -- so the asymmetry gate is green precisely because its subject is absent | `grep -n 'installed.push' crates/busbar/src/main.rs` returns five plane pushes (286, 288, 293, 300, 310) against four `v.push((` in the test. The same grep proves the method finds real linkage where it exists: `grep -n 'busbar_voice::PLANE_DECL' crates/busbar/src/main.rs… |
| 244 | MEDIUM | `busbar/src/main.rs:269` | The doc comment on `register_planes()` states A2A "is still built into core until its own extraction, so it is not pushed here yet" -- but the very function this comments describes pushes it 24 lines later | n/a (not a zero-claim; both the stale text and the contradicting code are quoted directly, file:line for each) |
| 245 | MEDIUM | `busbar/src/main.rs:513` | Boot constructs a SECOND node assembly beside ProductionUnits - its own budget door and cell store, its own credential cache, its own durability book, and an EMPTY auth chain that answers ChainVerdict::Open - of which exactly one field is ever read | ZERO-CLAIM CONTROL for 'exactly one field of the second node is read': the port it is mounted behind is `impl busbar_voice::runtime::GovernedCalls for NodeCalls` at crates/busbar/src/root/units_voice.rs:661-700, whose two methods touch only `self.node.tool_calls`. Control that the node's other… |
| 246 | MEDIUM | `busbar/src/main.rs:519` | The metering policy the usage fold prices against is built only from `MeterPolicyConfig::default()` at both production sites, so the configured pool expansions, lane prices and tolerances never reach the meter -- and the guard that would notice has no caller | ZERO for a non-default config: `grep -rn --include='*.rs' 'policy::build\(\|MeterPolicyConfig' crates/` -> every production construction is `MeterPolicyConfig::default()` (main.rs:519, kernel.rs:815); every non-default construction is under `/tests/`. CONTROL: the same grep shape over a sibling… |
| 247 | MEDIUM | `busbar/src/main.rs:2057` | The doc comment introducing `mod root;` itself -- the literal declaration of the module this whole audit lens targets -- says "Nothing in `main()` calls into it yet", which is false: `main()` and the `run()` it drives call into `root::` extensively throughout the file | grep -n "root::" crates/busbar/src/main.rs \| wc -l returns dozens of call sites spread across both `main()` and `run()`, including the one at main.rs:638 inside `main()` itself proper -- directly falsifying "Nothing in `main()` calls into it yet" |
| 248 | MEDIUM | `busbar/src/root/adapters.rs:451` | A second boot check in the same directory follows the identical dead-instrument pattern as the policy.rs finding above: `check_label_banks()` exists specifically to catch two hand-kept metric label banks drifting apart, and it is never called outside its own test | grep -rn "check_label_banks" crates/busbar/src/ matches only its definition (adapters.rs:451) and one call in root/tests/adapters.rs:415. Same positive control as the policy.rs finding: other root:: functions from this same file's neighborhood are called from main.rs/run() in production, confirming… |
| 249 | MEDIUM | `busbar/src/root/auth_bindings.rs:197` | Two line-for-line-identical credential caches ship in one process and the admin cache-flush endpoint can reach only one of them - which is precisely the failure the composition root's own doc says must not exist | ZERO-CLAIM CONTROL for 'no flush path reaches the identity cache': `grep -rn 'flush_module\|flush_all' --include='*.rs' crates` returns exactly 2 production call sites, both at handlers.rs:2197-2198 against `app.credential_cache`; every busbar-kernel-identity hit is that crate's own definition… |
| 250 | MEDIUM | `busbar/src/root/plane_decision.rs:102` | crates/busbar/Cargo.toml's `plane-decision` feature comment claims the decision plane "carries no config key, no environment variable and no boot line... so a deployment cannot tell from the wire or from the logs which way it was built" -- but this very file wires a real, validated `decisions:` config section | n/a (not a zero-claim; both the Cargo.toml text and the contradicting config-wiring code are quoted/cited directly, file:line for each) |
| 251 | MEDIUM | `busbar/src/root/registry.rs:259` | Beyond the general disconnection above, `plane_claims()` doesn't even enumerate all five of the plane-kind objects it could: it stops at llm/mcp/a2a/voice/admin and never references `busbar_plane_decision::DecisionPlane` at all, even though `plane-decision` ships default-on (crates/busbar/Cargo.toml:192,235) and `DecisionPlane` already implements `PlaneMeta` with a concrete, non-empty `CLAIMS`… | grep -rn "busbar_plane_decision\\|DecisionPlane" crates/busbar/src/root/registry.rs returns nothing, vs. the same grep for `busbar_plane_streaming`/`StreamingPlane` (voice) returning four real, feature-gated references (registry.rs:93-94,165-174,257-258,344-349,462-463) -- proving voice IS wired… |
| 252 | MEDIUM | `busbar/src/root/transports.rs:43` | The module doc names a live, unfixed multi-listener TLS hazard ("a deployment with two TLS listeners is exposed") that has already been fixed in the transport crate; the warning here was never removed | n/a (not a zero-claim; the stale doc and the fixed implementation, plus its regression test, are quoted/cited directly) |
| 253 | MEDIUM | `busbar/src/root/units_a2a.rs:540` | A2aDraft.streaming ("Whether the answer arrives as a run of events rather than one reply") is read nowhere in units_a2a.rs except the hand-written Debug impl (line 586); every money/metering function that touches the draft (fee_evidence, bytes_located, request_nanos, fee_could_land, the Units-driving A2aUnits methods) ignores it, while the sibling `op` field on the identical struct is read repeatedly for real… | grep -n "draft\.op\b" crates/busbar/src/root/units_a2a.rs returns 4 real-logic matches (lines 1281, 1345, 1598, 1627); grep -n "\.streaming\b" crates/busbar/src/root/units_a2a.rs returns only the field declaration (540) and the Debug formatter (586) — the same identical-struct, identical-file… |
| 254 | MEDIUM | `busbar/src/root/units_admin/tests/units_admin.rs:3500` | The only test that inspects the operator-signed rate-amendment payload says in its own comment that every signed field is 'still there, in order', and then checks each field with an unordered `contains` of the bare `key=` prefix -- so the payload's order and four of its five field VALUES have no witness anywhere in the tree | `grep -rn --include='*.rs' 'effective_from=' crates/` returns exactly one hit -- units_admin.rs:3494, the test's own key list -- so no byte-exact pin of the payload exists anywhere in the tree. The identical grep DOES find a real pin where one exists: `grep -rn --include='*.rs'… |
| 255 | MEDIUM | `busbar/src/root/units_mcp.rs:9` | units_mcp.rs's module doc presents itself, in the present tense with no dormancy disclosure, as the completed MCP-plane-onto-kernel switch-over, but none of its per-request money/metering functions (arrival, read_ingress, decode, authenticate, admit, route/legs, meter, settle, audit_inputs, evidence, mount) has any caller anywhere in crates/busbar/src outside the module's own test file; the only production call site… | grep -rn "units_mcp::seal" crates/busbar/src finds the one real production caller (main.rs:354); grep -rn "units_llm::PATH_INGRESS\\|units_llm::BODY_INGRESS" crates/busbar/src finds units_llm.rs's equivalent top-level entry point IS wired into main.rs's register_protocols() (main.rs:219,242) and is… |
| 256 | MEDIUM | `busbar/src/root/units_mcp.rs:200` | Decoded.streaming (set to false/true at units_mcp.rs:231/236 depending on whether the plane's ingress was OneShot or Open) is never read by any consumer: units_mcp.rs's own decode() function at line 256 destructures only `decoded.op` (`Ok(Read::Unit(decoded)) => Decision::proceed(token, decoded.op)`), discarding `streaming` and `facts` from the same value, and no other file in the slice reads it either | grep -n "\.streaming\b" crates/busbar/src/root/units_mcp.rs and grep -rn "streaming" crates/busbar/src (excluding the two assignment sites and one test assertion at root/tests/units_mcp.rs:310) return zero consuming reads; grep -n "decoded\.op\b" crates/busbar/src/root/units_mcp.rs:256 shows the… |
| 257 | MEDIUM | `busbar/tests/capability_equality.rs:986` | `every_workspace_plane_crate_maps_to_at_least_one_ledger_column` promises to go 'RED the moment a plane crate joins the workspace with no row in PLANE_CRATE_LEDGER_COLUMNS'; it enumerates a four-entry shell variable, and two plane crates that are already workspace members map to zero ledger columns and are invisible to it | Direct reads of the three inputs: `grep -n 'PLANE_KEYS' scripts/plane-keys.sh` returns line 46 (`llm mcp a2a voice`) and line 65 (`llm mcp a2a streaming decisions`); `grep -n 'crates/busbar-plane-' Cargo.toml` returns five members including streaming and decision; and the ledger's own `planes` keys… |
| 258 | MEDIUM | `busbar/tests/money_never_ends_an_admitted_unit.rs:156` | The scan the file calls 'the only way a reason becomes a refusal' matches 57 of the 100 production `Refusal::new(` occurrences in crates/; the other 43 -- including nine in the composition root's own kernel.rs and every site that passes the reason in a variable -- are invisible, so 'OverdraftCeiling and StaleSlice are refusal reasons nothing refuses with' is asserted over a partial census | The identical re-implementation of the scanner DOES find the 57 matching occurrences and returns the 17 distinct reasons the test's sibling assertion relies on (ChallengeExhausted, DeadlineExceeded, DecodeFailed, DurabilityUnavailable, GroupFrozen, HandoffMismatch, HookVeto, MeterDisputed,… |
| 259 | LOW | `busbar/src/main.rs:1372` | "Every plane's exit arm settles onto it" carries no qualifier, but today only two of five planes (llm, admin) actually bind to this book in `crates/busbar/src/`; mcp/a2a/voice have no equivalent binding here | grep -rn "fn bind_book" crates/busbar/src/ (1 file, 2 lines, both units_llm.rs) contrasted with grep -rn "fn new\b" crates/busbar/src/root/kernel.rs (many real hits) shows the search method distinguishes a genuinely single-plane mechanism from a widely-implemented one |
| 260 | LOW | `busbar/src/root/money_book.rs:44` ⟂FOLD | `crates/api` (`busbar-api`) is documented as the legacy pre-#35 plugin-contract surface being retired, but it is still directly depended on by 20 other crates (not just third-party-shaped plugins) — including the core engine (`busbar-kernel`) and the shipped binary (`busbar`) itself — and its re-exported types sit directly on the live money path (the `MoneyBook` seam) and the live admin-auth path (the operator… | `grep -rl busbar-api --include=Cargo.toml crates/ \| wc -l` returns 21 (including crates/api itself); every quoted call site above was read directly at the given file:line in the pinned tree |
| 261 | LOW | `busbar/src/root/money_book.rs:118` ⟂FOLD | Two different traits named MoneyBook live in one crate, each documented as THE money-settling seam; the live one has a single method, the dead one has five, and a reader who follows the name reaches the wrong seam | ZERO-CLAIM CONTROL: `grep -rn 'PassThroughBook' --include='*.rs' crates xtask` returns 13 lines of which every constructor is in crates/busbar/src/root/tests/money_book.rs (7 sites), the rest being its own declaration and doc comments. The SAME command for the sibling implementor - `grep -rn… |
| 262 | LOW | `busbar/tests/field_coverage.rs:256` | `every_carried_claim_names_a_real_test` -- described at line 204 as 'the load-bearing assertion of the whole gate' -- accepts any occurrence of the text `fn NAME(` anywhere in a concatenation of eight source trees, with no test-attribute check, no test-module check and no assertion check, and one of those eight roots is a directory that no longer exists | The upgrade this gate never got is written out in full in the sibling file, for the identical problem: capability_equality.rs:178-189 -- 'It used to mean `src.contains("fn NAME(")` -- a substring. Under that rule a cell stays proven when its named function is a plain helper, or a `fn` in a doc… |
| 263 | LOW | `busbar/tests/money_never_ends_an_admitted_unit.rs:108` | The comment says the normalisation makes a rustfmt-broken construction read the same as a one-line one; the normalisation is what BREAKS the match, because collapsing the newline and indent leaves a single space between `Refusal::new(` and `ReasonCode::` that the marker cannot cross | The same normalisation applied to the one-line spelling `Refusal::new(ReasonCode::OverdraftCeiling)` DOES match the marker, which is why the 57 single-line sites are found and these are not -- same function, same marker, different spelling |
| 264 | LOW | `busbar/tests/plane_isomorphism.rs:438` | Inside a `for &col in *cols` loop, the 'is this column driven by a leg that proves anything' check reads a per-LEG counter rather than a per-(leg, column) one, so a whole directional column's root-loop evidence can drop to zero while the assertion whose message names that column stays green | Counting the ledger by (leg, plane, state) gives ('root-mcp','mcp-client','proven')=7 and ('root-mcp','mcp-server','proven')=5 -- two columns behind one leg key, which is the shape that makes the per-leg counter lossy. Positive control that a per-column key is available and simply is not used: the… |
| 265 | NIT | `busbar/src/root/units_a2a.rs:1220` | The comment justifying a hand-written FinishClass mapping states two facts that are both false: the crates do depend on each other, and the mapping is written three times rather than once | ZERO-CLAIM CONTROL for 'written three times': `grep -rn 'busbar_kernel_audit::FinishClass\|busbar_kernel_audit::record::FinishClass' --include='*.rs' crates` returns the three mapping functions above plus 4 assertion sites in crates/busbar/src/root/tests/units_a2a.rs (713,717,721,725) - production… |
| 266 | NIT | `busbar/tests/plane_transport_neutrality.rs:15` | The test demotes itself to 'belt-and-suspenders' on the strength of a blocking shell gate at `scripts/plane-transport-neutrality.sh`, which does not exist |  |
| 267 | NIT | `busbar/tests/voice_boot.rs:13` | A copy of `CORE_OWNED_CONCRETE_SECTIONS` is kept in this test file on the stated grounds that the real const is `pub(crate)`; it is `pub`, so the mirror is unnecessary and can silently diverge from the list the real boot guard uses | `grep -n 'CORE_OWNED_CONCRETE_SECTIONS' crates/busbar-kernel/src/plane/registry.rs` returns line 411 declaring it `pub const`, not `pub(crate)`. Positive control that the backstop the comment names does exist (which is why this is a NIT and not a missing test): the referenced… |

## `busbar-kernel` — 33 items (HIGH 6 · MEDIUM 13 · LOW 8 · NIT 6)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 268 | HIGH | `busbar-kernel/src/governance/state.rs:803` | MCP and A2A traffic never accrues a single unit into the budget token ledger, so the only thing their traffic contributes to a `budget:` cap is `price_per_request_cents x billable_requests` — and that fee defaults to 0, which makes a spend cap unreachable by those planes entirely | Zero-claim: `grep -rn --include="*.rs" -e 'meter_ledger' -e 'record_usage' crates/busbar-mcp/ crates/busbar-a2a/` returns NOTHING (exit 1). Control proving the identical command matches where the call does exist: the same pattern over `crates/busbar-llm/ crates/busbar-voice/` returns 9 files… |
| 269 | HIGH | `busbar-kernel/src/governance/state.rs:1970` | The live budget gate derives spend through a fixed four-element RESERVED_UNITS fold, while every other operation on the same ledger cell (accrue, total_tokens, flush, hydrate) is open-keyed, so any meter class a plane declares outside input/output/cache_read/cache_write prices at exactly zero against a `budget:` cap and can never move it | Zero-claim is that `reserved_rate` prices nothing outside the four. Control showing the method and the path: `grep -n 'UNIT_INPUT => self.input' crates/busbar-kernel/src/cost.rs` matches line 171 (a key that IS priced) under the same read, while `grep -n '_ => 0,' crates/busbar-kernel/src/cost.rs`… |
| 270 | HIGH | `busbar-kernel/src/plane/config.rs:325` | Plane 4's canonical key streams (and plane 5's decisions) appears in no banned-vocabulary list in any neutrality gate, and both are live and unpragma'd in the neutral kernel crate right now | The MCP twin of the identical construct, 53 lines later in the same file, needed a documented carve-out to get past the gate: config.rs:378 'pub struct McpEndpointSection(...); // plane-purity: frozen-wire ...' and :401 'ENDPOINT_SECTION_KEY: &str = "mcp"; // plane-purity: frozen-wire ...'. grep -n… |
| 271 | HIGH | `busbar-kernel/src/recovery.rs:115` | LEAD 1 CONFIRMED, and wider than claimed: not only `recover_all` but EVERY public item of `busbar_kernel::recovery` (materialize, settle, recover_all, truncate_torn_tail, frame, crc32, owed_after, voids_claim, HoldRecord, TailVerdict, KillPoint, RECORD_HEADER_BYTES) is referenced from exactly one place in the whole workspace -- the integration test crates/busbar-kernel/tests/recovery_and_ticks.rs. `pub mod… | Same command form, same tree: `grep -rn 'recover_all\|recovery::' --include='*.rs' crates/ \| grep -v /target/` returns ONLY crates/busbar-kernel/tests/recovery_and_ticks.rs:{13,40,58,793} and the definition itself. The identical grep for a kernel function that IS production-wired -- `grep -rn… |
| 272 | HIGH | `busbar-kernel/src/teller.rs:786` | The two-sided canary's identity `drafts == holds + accruals == settlements` is arithmetically unsatisfiable for every zero-hold admission and every refusal, because the loop increments `drafts` four steps before the door and `settled` for units that opened neither a hold nor an accrual | `grep -rn --include='*.rs' '\.balanced()' crates/` matches ONLY test files: busbar-contract/src/caps/tests/mod.rs:{754,759,771}, caps/tests/what_the_record_reads.rs:142, busbar-kernel/tests/table_and_pump.rs:{885,891,897,903}, busbar-kernel/tests/loop_battery.rs:{373,443,699} -- 11 test sites, 0… |
| 273 | HIGH | `busbar-kernel/src/tick.rs:320` | The sweep's settle and the recovery settle both build a `Posted` WITHOUT first applying the accrual to the hold, so an overdraft they settle is posted as zero while the same unit through the exit path posts it in full | `grep -rn --include='*.rs' '\.spend(' crates/ \| grep -v '/tests/\\|_tests\.rs'` returns 3 Hold-spend production sites -- busbar-kernel/src/teller.rs:1323, busbar-kernel-budget/src/lib.rs:271, busbar-contract/src/caps/hold.rs:502 (the child-accrual arm) -- and ZERO in busbar-kernel/src/tick.rs or… |
| 274 | MEDIUM | `busbar-kernel/src/audit/journal.rs:367` | `Journal::forget` drops a scope's cached chain position WITHOUT latching `overflowed`, but both resume paths short-circuit to a fresh seq-1 chain when `overflowed` is false -- so on an ABI-registered stream (cap = usize::MAX, where `overflowed` can never latch) a forget-then-append FORKS the durable hash chain at seq 1 even with a sink attached, which is the exact opposite of what `forget`'s own doc promises | Zero-claim is 'no in-tree plane calls journal_forget today (so this is a wired trap, not a live regression)'. Control -- the same command finds the sibling ABI entry points that DO have in-tree reachable callers: `grep -rn "journal_forget" crates/ \| grep -v "busbar-plugin/"` -> only… |
| 275 | MEDIUM | `busbar-kernel/src/auth/mod.rs:505` ⟂FOLD | The two auth-chain walks differ at the keys arm: the identity crate's commits the buffered Pass rows when the keys arm identifies, the kernel's returns without committing them, so one chain and one credential leave the two caches in different states | ZERO-CLAIM CONTROL for 'nothing compares the two walks': the set of .rs files naming `busbar_kernel_identity` AND `crate::auth_cache`/`busbar_kernel::auth` is exactly one file, crates/busbar-kernel/src/drain.rs, where the mention is a doc comment at drain.rs:22 rather than a test. Control that the… |
| 276 | MEDIUM | `busbar-kernel/src/auth/tests/token_tests.rs:798` | `execute_hop_refuses_non_allowlisted_host` claims '(secret never sent)' but inspects only the returned Err; the endpoint it points at is an echoing mock with no hit counter, so the test cannot tell 'refused before the request was built' from 'sent the secret, then refused' | The machinery to see it exists and is used earlier in the same file against the same mock: `client_secret_is_core_injected_only` (token_tests.rs:441-449) reads the echoed body back out of `mock_echo_endpoint()` and asserts on its contents. So the endpoint's echo IS readable from a test and IS read… |
| 277 | MEDIUM | `busbar-kernel/src/cost.rs:227` ⟂FOLD | The function the kernel's own doc calls 'THE ONE ENFORCEMENT PRICER every plane's neutral Usage reaches' has zero production callers; the pricers production actually reaches are three other functions in two other crates | ZERO-CLAIM CONTROL: `cat prod_files.txt \| xargs grep -n '\bprice('` over the 927 non-test files returns cost.rs:227 only as a declaration and never as a call, while the SAME command returns real production calls at crates/busbar/src/root/units_llm.rs:760 and… |
| 278 | MEDIUM | `busbar-kernel/src/cost.rs:783` ⟂FOLD | `CostModel::derive_spend_micros` carries the same unpriced-model drop as its cents twin, and it feeds the figure an operator reads as 'spend so far' and the figure they read as 'budget remaining' | `grep -rn --include="*.rs" "derive_spend_micros" . \| grep -v /tests/` matches the definition at cost.rs:775 AND the live callers `crates/busbar-kernel/src/governance/state.rs:1691` and `crates/busbar-core-admin/src/v1/service.rs:128,291` -- the same command matching those three is the control that… |
| 279 | MEDIUM | `busbar-kernel/src/failover/mod.rs:703` ⟂FOLD | The pin check in `walk_with` ABORTS the entire walk on the first member whose `interchange_key` disagrees with the primary's, instead of skipping that candidate -- so one pending or drifted member in the middle of a pool makes every healthy verified twin after it unreachable | Not a zero-claim. Control for the reachability half: `grep -rn "fn interchange_key" crates/` -> 3 production impls (busbar-llm/src/engine/select.rs:445 returns `Some(pool)` so the model plane is immune; busbar-a2a/src/a2a/route.rs:50 and busbar-mcp/src/mcp/reroute.rs:60 both return… |
| 280 | MEDIUM | `busbar-kernel/src/inflight.rs:435` | The in-flight table's provider reserve -- which the module doc calls money -- is unreachable: its enabling input `provider_of_open_session` is never set to `true` anywhere in the tree, `reserve_for` has no production caller, and every production construction passes a literal reserve of 0 | COUNTS, CLASSIFIED. `grep -rn --include='*.rs' 'provider_of_open_session: true' crates/` returns ZERO hits; `grep -rn --include='*.rs' 'provider_of_open_session: false' crates/ \| wc -l` returns 19 (production: units_llm.rs:534, units_admin/admin_mount.rs:99, gauntlet_kernel.rs:{331,430}; the… |
| 281 | MEDIUM | `busbar-kernel/src/metrics.rs:527` | `busbar_lane_state` mixes a PER-POOL cooldown with a LANE-GLOBAL `usable`, so when a lane is shared by two pools and one pool's breaker cell is fully Open the gauge for that pool reports 1 (`half-open`) instead of 2 (`tripped`) -- while `busbar_lane_available` on the very same label pair, rendered two lines later from the real per-pool `classify`, reports 0 | Not a zero-claim. The lane-global vs per-pool split is quoted from the store itself: `sed -n '717,731p' crates/busbar-kernel/src/store/in_memory/mod.rs` shows `lane_usable_any_cell` folding `per_lane.iter().any(...)` across every pool cell, and `crates/busbar-kernel/src/store/vocab.rs:347-359`… |
| 282 | MEDIUM | `busbar-kernel/src/plane_host/journal.rs:463` | journal_register - the only durable-stream registration reachable over the ABI - always passes usize::MAX, which disables the position-cache LRU, and the descriptor POD has no field that could express a cap, so any third-party plane is structurally stuck with an unevictable position cache | `grep -rn 'journal_register_capped' crates/busbar-kernel/src/` finds the definition (journal.rs:472) and exactly one real caller, calllog.rs:195 - proving the bounded path exists, is used, and that the grep resolves callers; the absence of any cap field is confirmed by reading JournalStreamDesc in… |
| 283 | MEDIUM | `busbar-kernel/src/registry.rs:509` | Two implementations of claim-selector overlap disagree on ALPN case: the kernel's boot seal folds ALPN case, the contract crate's published rule compares ALPN byte-exactly | ZERO-CLAIM CONTROL for 'no instrument compares them': `grep -rn 'overlaps\|SelectorFamily\|transport_overlaps' xtask/src/gates/` returns only prose hits in release_order.rs and design_bindings/tables.rs. Control that the gates tree is searchable and does hold real rules under the same idiom: `grep… |
| 284 | MEDIUM | `busbar-kernel/src/tick.rs:87` | All three `Close` arms of the session tick drop the checkpoint AND the outstanding priced interval, reintroducing on the close path exactly the defect the tree's own regression test says was fixed on the accrue path | `grep -rn --include='*.rs' 'session_tick' crates/` matches only crates/busbar-kernel/src/tick.rs (the definition) and crates/busbar-kernel/tests/recovery_and_ticks.rs:{19,719,722,727,732,742,752,771,806,812,818} -- 0 production callers, 11 test sites, 0 doc-comments. `grep -rn --include='*.rs'… |
| 285 | MEDIUM | `busbar-kernel/src/tick.rs:304` | The sweep and recovery hard-code the kernel's own meter class while the exit path honours `Evidence.class`, so the same unit's usage line lands on a different meter depending on which of its two ends settled it -- and the constant's own doc says all three post against the same class | `grep -rn --include='*.rs' 'evidence.class\\|\.class\.unwrap_or' crates/` matches exactly two sites: busbar-kernel/src/teller.rs:1324 (production read) and busbar/src/root/tests/units_voice.rs:1618 (a test that asserts the plane still declares it) -- so the field has ONE production reader. The… |
| 286 | MEDIUM | `busbar-kernel/tests/loop_battery.rs:566` | `the_loop_has_no_early_exits` scans the Teller's source for `"return "` with a trailing space; the file it scans already contains a bare `return;`, and the test is green | Reproducing the test's exact logic (strip lines whose trim_start begins with `//`, join, search) over the pinned teller.rs gives `'return ' in body == False` while `'return;' in body == True` -- the marker misses and the defect is present. Positive control that the grep method works at all on this… |
| 287 | LOW | `busbar-kernel/src/audit/mod.rs:16` ⟂FOLD | The audit module's core claim - one append, one digest, one verifier, and that several independent chain implementations would falsify the tamper-evidence product claim - is stated in a file that has a byte-identical twin shipping in the same binary | ZERO-CLAIM CONTROL for 'no gate or test names both': the set of .rs files containing both `busbar_kernel_audit` and `audit_ring` is empty, and the dependency graph forbids it - crates/busbar-kernel-audit/Cargo.toml does not name busbar-kernel. Control that the left-hand grep is not vacuous: 9 files… |
| 288 | LOW | `busbar-kernel/src/auth/token.rs:344` | `GET /auth/token?method=X&refresh=1` is an unauthenticated, exact-match-bypassed route that starts a login whose completion ROTATES the caller's issued busbar key (the prior token stops verifying); the destructive `refresh` flag is chosen by the query string and nothing binds the request to user intent | Zero-claim (no CSRF/state material is consulted anywhere in `fn begin`, token.rs:369-474): `sed -n '369,474p' crates/busbar-kernel/src/auth/token.rs \| grep -c "FORM_STATE_FIELD\\|read_cookie\\|cookie_raw"` -> 0. Positive control, same symbol in the same file where it IS used: `grep -c… |
| 289 | LOW | `busbar-kernel/src/drain.rs:60` | The drain facade whose stated purpose is to make relocating a step a one-line edit still points at the kernel's auth module, while the crate meant to replace it is already live in the composition root - so both steps ship and the facade documents a swap that never happened | ZERO-CLAIM CONTROL: `grep -rn 'busbar_kernel_identity' --include='*.rs' crates xtask \| grep -v '^crates/busbar-kernel-identity/'` returns production lines across crates/busbar/src/root/*.rs and crates/busbar/src/main.rs:513; the SAME command finds exactly one mention inside busbar-kernel, the doc… |
| 290 | LOW | `busbar-kernel/src/egress/engine/pool.rs:467` | The egress pool mutex is the only lock in the slice that is NOT poison-recovering: 5 production sites take it with `.expect("engine pool lock")` while `DialGuard::drop` alone silently skips on poison, so one panic under the guard would both brick every later checkout AND permanently leak the dial budget | `grep -rn 'lock_recover(' crates/busbar-kernel/src/store/ crates/busbar-kernel-breaker/src/ \| grep -v 'fn lock_recover' \| wc -l` returns 45 (the house pattern exists and is used), while `grep -n 'expect("engine pool lock")' crates/busbar-kernel/src/egress/engine/pool.rs… |
| 291 | LOW | `busbar-kernel/src/inflight.rs:9` | The in-flight table's module header is the third place in the tree to state the wrong take-site count | The sibling claim in the same paragraph — that the cell makes the second one lose — checks out at crates/busbar-contract/src/caps/hold.rs:470-473 (`std::mem::replace(&mut *slot, Slot::Taken)` under the lock), so the paragraph is not uniformly wrong |
| 292 | LOW | `busbar-kernel/src/preflight.rs:120` | The identity-provider exemption is gated on `cfg!(test)` while its sibling 22 lines above is gated on `cfg!(any(test, feature = "test-support"))`, and the stand-in modules it exempts are registered under the WIDER gate -- so in a `test-support` build `--validate`/boot refuse an `identity-providers:` definition that `AuthPlugins::build` would have accepted | Not a zero-claim. Both halves are direct reads: `sed -n '98p;120p' crates/busbar-kernel/src/preflight.rs` shows the two different gates, and `grep -n "test-groups-module\\|test-scope-module" -B 2 crates/busbar-kernel/src/auth/mod.rs` shows `#[cfg(any(test, feature = "test-support"))]` immediately… |
| 293 | LOW | `busbar-kernel/src/tick.rs:98` | The priced-seconds branch clips the catch-up interval at the five-minute idle bound and drops the remainder, on the line after the field doc promises priced time is never simply dropped | Same zero-claim as the session-tick finding above: `grep -rn --include='*.rs' 'session_tick' crates/` matches only busbar-kernel/src/tick.rs and busbar-kernel/tests/recovery_and_ticks.rs -- 0 production callers, 11 test sites. The clip itself is pinned by the tree's own test at… |
| 294 | LOW | `busbar-kernel/tests/registry_cross_plane.rs:530` | `dup_claim_guard_passes_for_the_shipped_empty_registry` asserts that every shipped plane claims no config section, over a file-local three-entry array (llm, mcp, a2a) that excludes exactly the two shipped planes for which the claim is false | `grep -rn 'owned_config_sections' crates/busbar-voice/src/lib.rs crates/busbar/src/root/plane_decision.rs crates/busbar-llm/src/lib.rs crates/busbar-mcp/src/mcp/mod.rs crates/busbar-a2a/src/a2a/mod.rs` shows the first two non-empty and the last three empty -- same field, same grep, both answers… |
| 295 | NIT | `busbar-kernel/src/auth/tests/self_keys_tests.rs:714` | `gov_disabled` is constructed at line 707 and read at line 714 and nowhere else; the `resolve_exchange` call between them takes no store, so 'a refused self-mint writes NO VirtualKey row' is asserted against a store nothing could have written to | The paired PRECONDITION half of the same test is wired correctly and does observe a write: `gov_open` at line 690 is threaded through `DeterministicEd25519Keys::new(gov_open.clone(), team, pools, ..)`, a key is minted through it, and line 699-703 asserts `self_rows(&gov_open, "sam").len() == 1`.… |
| 296 | NIT | `busbar-kernel/src/egress_auth/tests/jwt_bearer_tests.rs:387` | The helper-level key-leak check feeds `pem_to_pkcs8_der` the UNPADDED `Az` and asserts on the DECIMAL byte 122 -- the two spellings this cell's own docstring says can never catch the leak, after the `cases` table above it was repaired to `Az==` / "'z'" | The test diagnoses itself at lines 333-336: 'The unpadded `Az` this case used to carry never reached that variant at all: `STANDARD` requires canonical padding, so it failed earlier with a length/padding error that names no key byte -- the case passed while leaking nothing, which is a test that… |
| 297 | NIT | `busbar-kernel/src/handlers/tests/chat_tests.rs:16` ⟂FOLD | `matches!(ir, _)` is true for every possible value of `ir`, so this line asserts nothing about which IR variant the chat codec produced | `grep -rn 'matches!(' crates --include='*.rs' \| grep ', _)'` over the whole tree returns this one line as the only `matches!` whose pattern is the bare wildcard; every other `matches!` in the slice names a variant (e.g. recompute_tests.rs:159 `matches!(f.divergence, Divergence::Priced { .. })`),… |
| 298 | NIT | `busbar-kernel/src/store/in_memory/mod.rs:17` | `restore_health_impl` documents itself as 'shared by the state-carrying constructor (config apply) and the in-place boot restore'. There is no in-place boot restore: the function has exactly one caller, `HealthState::new_with_limits_restored` (in_memory/mod.rs:315), which is itself reached only from the config-apply arm at crates/busbar-kernel/src/appbuild.rs:939 (`match prior { Some(p) => ... , None =>… | `grep -rn 'export_health\|new_with_limits_restored\|restore_health_impl' --include='*.rs' crates/ \| grep -v /target/ \| grep -v tests` returns appbuild.rs:{939,943}, vocab.rs:{784,787}, in_memory/mod.rs:{5,19,307,315} and availability.rs:806 -- one construction site, in the config-apply arm. The… |
| 299 | NIT | `busbar-kernel/src/teller.rs:221` | The enumeration that exists so 'the source scan that accounts for every mint sees this one too' names five tokens minted outside the loop and gives the seal accessor two purposes, but the seal actually mints six tokens across the two modules -- including two of the tree's five production `WriteMoney` mints, which the enumeration never mentions | `grep -rn --include='*.rs' 'WriteMoney>::mint' crates/` returns 50 hits, classified: 5 PRODUCTION (busbar-kernel/src/teller.rs:199 the `ledger_token` factory, teller.rs:1218, teller.rs:1348, tick.rs:318, recovery.rs:103), 2 DOC-COMMENT (busbar-contract/src/caps/hold.rs:29, caps/unit_end.rs:29), 43… |
| 300 | NIT | `busbar-kernel/src/tick.rs:296` | `LeaseSet`'s doc says the leases are released on the exit path and names one releaser; there are three production releasers, and the type's own sibling doc twelve lines below says two | `grep -rn --include='*.rs' 'release_all(' crates/` returns 9 hits, classified: 3 definitions/internal (busbar-contract/src/slice.rs:521, :614, :619), 3 test sites (busbar-kernel/tests/slices_and_leases.rs:286, tests/recovery_and_ticks.rs:551, :554), and 3 PRODUCTION callers… |

## `busbar-llm-codec` — 7 items (HIGH 6 · MEDIUM 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 301 | HIGH | `busbar-llm-codec/src/cohere/reader.rs:1141` | Cohere's streaming read_response_events has no ET_CITATION_START/ET_CITATION_END match arm at all -- both fall into this catch-all and produce zero IR events -- even though the IR fully supports streamed citations, the crate's own constants ET_CITATION_START/ET_CITATION_END exist (cohere/mod.rs:312-321), and this crate's OWN Cohere writer emits native citation-start/citation-end frames from IrDelta::CitationsDelta… | cohere/tests/tests.rs:2158-2167 (test_stream_unknown_event_is_noop) explicitly feeds {"type":"citation-start","index":0} into the reader and asserts the result is empty, using a well-known, well-documented event type as its example of an 'unknown' one -- confirming this is the reader's actual,… |
| 302 | HIGH | `busbar-llm-codec/src/cohere/tests/tests.rs:4739` | Cohere's BUFFERED `read_response` path for `finish_reason: "ERROR"` (the confirmed 'failed generation billed as completed' defect, r3-codec.json) is covered by two tests -- `read_write_response_error_finish_reason_round_trips` (this line) and `test_generic_error_does_not_fold_into_safety_and_round_trips` (line 1185) -- both of which require `read_response` to return `Ok(...)` and treat the ERROR body as a normal,… | `grep -n IrStreamEvent::Error crates/busbar-llm-codec/src/cohere/tests/tests.rs` finds 8 hits, including the correctly-designed streaming test at line 5283-5340 that explicitly checks for the Error-event push -- proving the crate's authors know how to write this exact assertion, and that… |
| 303 | HIGH | `busbar-llm-codec/src/gemini/tests/tests.rs:4852` | `finish_reason_malformed_function_call_stream_is_error` -- the test whose own doc comment says it guards the exact streaming call site where Gemini's MALFORMED_FUNCTION_CALL (a failed generation, per gemini/mod.rs's own doc comment) is supposed to suppress billing -- only inspects the `stop_reason` field on the `MessageDelta` event. It never checks whether an `IrStreamEvent::Error` was also pushed, which is the… | `grep -n IrStreamEvent::Error crates/busbar-llm-codec/src/gemini/tests/tests.rs` finds 7 hits elsewhere in the file (lines 2263, 2292, 3950, 3958, 3971, 3982, and the doc comment at 3934), proving both that the crate's own test idiom for asserting this event's presence exists and that… |
| 304 | HIGH | `busbar-llm-codec/src/ir/tests/rerank_tests.rs:24` | `rerank_resp_billing_flat_regardless_of_search_units` does not merely fail to catch the confirmed rerank-billing defect (r2-codec.json: `RerankResp::billing()` always returns `Billing::Flat` and discards `search_units`) -- it REQUIRES it: the test constructs a response with `search_units: Some(3)` specifically to assert that `.billing()` ignores it. Fixing the confirmed defect (making billing vary with search_units)… | `cargo test -p busbar-llm-codec --lib ir::rerank::tests::rerank_resp_billing_flat_regardless_of_search_units` -> 'test result: ok. 1 passed', confirming the test both exists, targets exactly this behavior, and currently passes by requiring the defect. `grep -rn billing… |
| 305 | HIGH | `busbar-llm-codec/src/openai_responses/reader.rs:1161` | The Responses streaming read_response_events never handles annotations/citations: there is no `response.output_text.annotation.added` event constant anywhere in openai_responses/mod.rs's EVT_* set, and the terminal response.completed handler only scans each output content part for type=="refusal" (this line), never for the sibling `annotations` array on an output_text part it already has in hand. The buffered… | gemini/reader.rs:1188-1235 is this exact bug class already found and fixed for a sibling dialect -- its own comment states 'Without this arm a streamed Gemini citation was silently dropped' and it now emits IrDelta::CitationsDelta mid-stream -- proving the team has fixed this precise failure mode… |
| 306 | HIGH | `busbar-llm-codec/src/tests/usage_count_tests.rs:227` | The two source-scanning regression guards that this file's own doc comments say exist specifically to stop the confirmed 'unreadable count billed as zero' defect from recurring (`no_dialect_reads_a_count_with_the_bare_defaulting_idiom`, `no_dialect_reads_a_billed_count_field_with_a_bare_as_u64`) both search for the string `as_u64`/`as_i64` -- but the confirmed-live defect (r2-codec.json: anthropic/reader.rs:732,… | `grep -c as_u64 <<< read_count_u64` = 0 (the banned string is not a substring of the primitive's own name, so the scan is structurally blind to any call that goes through it). Flattened-line regex scan for `read_count_u64)\s*\.\s*unwrap_or\(` and `u64_field\("[a-zA-Z_]+"\)\s*\.\s*unwrap_or\(` over… |
| 307 | MEDIUM | `busbar-llm-codec/src/wire_shim.rs:96` | `tier_usage()` -- the function r3-codec.json already flagged (wire_shim.rs:103) for collapsing a cache field that is `None` because an upstream `read_count_u64` refused an unparseable value into the same sparse-map omission as a field that legitimately reported nothing -- has no dedicated test file or `#[cfg(test)]` module of its own, and is exercised by exactly two tests anywhere in the crate, neither of which ever… | `grep -n 'mod tests' crates/busbar-llm-codec/src/wire_shim.rs` -> 0 hits (confirms no test module declared in the file itself, in contrast to every dialect mod.rs/reader.rs/handler.rs and usage_count.rs, which all declare one). `grep -rn tier_usage crates/busbar-llm-codec/src --include='*.rs'`… |

## `busbar-mcp` — 6 items (HIGH 5 · MEDIUM 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 308 | HIGH | `busbar-mcp/src/mcp/client/stdio.rs:800` | ChildSlot::guarded_call keeps (does not retire) a stdio child whenever StdioChild::call() returns Ok, but call()'s read loop (stdio.rs:510-515, delegating to peer::classify at peer.rs:231-235) returns the FIRST line that merely LOOKS like a response (any object carrying `result` or `error`) as 'the answer', with no check that its `id` is the one busbar actually sent -- id correlation happens only one layer up, in… | The exact 'one-ahead wedge' this produces is independently documented and tested for a DIFFERENT trigger -- cancellation -- in crates/busbar-mcp/src/mcp/client/tests/stdio_tests.rs:388-434 (`a_cancelled_dispatch_retires_the_child_rather_than_wedging_the_next_call`), whose own doc comment states… |
| 309 | HIGH | `busbar-mcp/src/mcp/envelope.rs:73` | The comment says a second spelling of these MCP wire words anywhere in the tree is RED; a second production spelling of `"mcp-protocol-version"` exists today and the census row is green, because the row's scope excludes every `busbar-plane-*` crate | In the same `gate --all` run the row reported PASS: `PASS structure-lint:census:count every shared decision and wire word occurs exactly the declared number of times`, so the row ran and counted — it counted in a scope that cannot see the duplicate |
| 310 | HIGH | `busbar-mcp/src/mcp/tasks.rs:968` ⟂FOLD | The SEP-2663 async task-completion path never calls busbar_plane_mcp::outputschema::check, so a tool that is BOTH task-capable and has a published output_schema gets zero structured-output validation when it finishes as a task, even though the identical tool calling synchronously (method.rs:2047-2078) IS validated | grep -n "output_schema\\|outputschema\\|structuredContent" crates/busbar-mcp/src/mcp/tasks.rs returns ZERO matches; the identical grep against crates/busbar-mcp/src/mcp/method.rs returns hits at lines 2047-2054 showing the check IS present and wired there (`if let Some(schema) =… |
| 311 | HIGH | `busbar-mcp/src/mcp/tests/stdio_client_leg_tests.rs:280` | No test anywhere in crates/busbar-mcp/src/mcp/client/tests/stdio_tests.rs or crates/busbar-mcp/src/mcp/tests/{stdio_dispatch_tests.rs,stdio_client_leg_tests.rs} ever constructs a stdio child that emits a spurious/duplicate/extra response-shaped line, so the exact cross-caller desynchronisation described in the companion stdio.rs:800 finding above has zero test coverage -- and one of the two files that comes closest… | The identical hazard IS correctly tested on the sibling HTTP transport: crates/busbar-mcp/src/mcp/client/tests/transport_tests.rs:236-268 (`an_answer_naming_a_different_request_is_refused_and_its_payload_never_surfaces`) constructs a response naming the WRONG id, asserts `RpcOutcome::Uncorrelated`,… |
| 312 | HIGH | `busbar-mcp/src/mcp/upstream.rs:594` | upstream::call() sets the per-member breaker outcome to Success as soon as the HTTP/transport status is 2xx (line 594), before the JSON-RPC body is parsed at all; when jsonrpc::parse_response then classifies that same body as RpcOutcome::Malformed or RpcOutcome::Uncorrelated, call() still returns Err(LegFailure) to its own caller (lines 615-627) but never revises *outcome back down, so the breaker is told every one… | crates/busbar-mcp/src/mcp/client/issue.rs handles the identical RpcOutcome::Malformed (line 261) and RpcOutcome::Uncorrelated (line 269) arms for every OTHER verb (prompts/list, resources/read, etc.) and correctly records `OUTCOME_DISPATCHED` + `REASON_UPSTREAM_FAILED` -- a failure, not a success… |
| 313 | MEDIUM | `busbar-mcp/src/mcp/method.rs:2048` ⟂FOLD | Even on the synchronous tools/call path, the outputSchema conformance check only runs when the upstream's result carries a `structuredContent` key at all; an upstream that has an operator-published output_schema and simply omits structuredContent entirely (rather than sending a type-violating value) skips the whole check and is served to the caller as an ordinary success | grep -rn "structured_output_violation" crates/busbar-mcp/src shows the concept is named only in a comment (catalogue.rs:1356) pointing at this exact block; there is no second check anywhere in method.rs or tasks.rs for a MISSING structuredContent when output_schema is Some -- confirmed by grepping… |

## `busbar-contract` — 17 items (HIGH 4 · MEDIUM 7 · LOW 5 · NIT 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 314 | HIGH | `busbar-contract/src/caps/fixtures/lint_rules.rs:95` | The seal-site rule that is supposed to prove 'no fourth take site anywhere' searches for the literal `take(&Grant::<Exit>::mint(`, which cannot match the tick's spelling — so the one non-teller take site in the tree is invisible to the scan that exempts it | The same literal DOES match and IS reported for the two teller.rs sites (`grep -c 'take(&Grant::<Exit>::mint(' crates/busbar-kernel/src/teller.rs` -> 2), so the needle and the scan set are right; only the tick's spelling escapes |
| 315 | HIGH | `busbar-contract/src/caps/hold.rs:55` | hold.rs asserts HoldCell::take has exactly two legitimate callers ('the exit path and the node's sweep'), but the crate's own construction-gate spec (caps/fixtures/lint_rules.rs:95-96) says there are three ('the exit path, the sweep and the tick'), the real kernel code has three distinct call sites (busbar-kernel/src/teller.rs:1207, teller.rs:1305, tick.rs:289), and the CI gate that is supposed to make 'no fourth… | grep -n 'take(&Grant::<Exit>::mint(' crates/busbar-kernel/src/teller.rs matches lines 1207 and 1305 (2 hits), proving the literal CAN and does match a real take call when spelled inline. The identical grep against crates/busbar-kernel/src/tick.rs returns 0 hits (exit code 1) even though tick.rs:289… |
| 316 | HIGH | `busbar-contract/src/caps/token.rs:14` | busbar-contract documents that only the kernel crate can obtain a `KernelSeal` -- "There are exactly two proof types and one root minter, and no others survive" (token.rs:19) and "Only the kernel mints tokens \| one audited symbol ... \| CI" (caps/mod.rs:51) -- confined by a CI symbol scan, but the actual enforcing gate is RED against this exact tree right now, and so are its sibling rows for the crate's other… | `cargo run -p xtask --quiet -- gate seal-witness` (run from /Users/matthew/.cache/busbar-codeaudit/round2) exits 1 and prints `FAIL seal-witness:single-minter ... 135 site(s) of KernelSeal::acquire_for_kernel( outside crates/busbar-kernel/src/: ...`; the paired row in the same run,… |
| 317 | HIGH | `busbar-contract/src/caps/token.rs:81` | The one deliberate cross-crate hole in the capability model is documented as held by 'CI's symbol scan'; both scans that watch it (`seal-witness:single-minter` and `construction`'s `token-sealed:kernel-seal`) are RED on this tree, so nothing is actually holding it | `grep -rn 'KernelSeal::acquire_for_kernel(' crates/busbar-kernel/src/` returns exactly 1 hit (teller.rs), confirming the needle and the path both work |
| 318 | MEDIUM | `busbar-contract/src/caps/hold.rs:677` | `Posted::settle` recognises two ways to be overdrawn but records only one: the second sets the FLAG and never the overdraft FIGURE, so a posting that settles above its reservation moves the books into an imbalance the overdraft column cannot explain | Not a zero-claim. `released()` at hold.rs:769 uses the same comparison (`self.reserved.saturating_sub(self.settled)`), proving the struct does compute the S>R case -- it just never writes it to `overdraft` |
| 319 | MEDIUM | `busbar-contract/src/caps/mod.rs:39` | mod.rs's "What Rust actually enforces here, honestly" table -- explicitly billed as the file that separates real guarantees from imagined ones (mod.rs:29-32) -- names two Rust types, `AdmitToken` and `LedgerToken` (mod.rs:39-40), that do not exist anywhere in the codebase: the #65/#73 refactor renamed them to `Grant<Admittance>` and `Grant<WriteMoney>` respectively, and `Hold::open`/`Posted::settle`… | `grep -rn "AdmitToken\b" crates/busbar-contract/src/ crates/busbar-kernel/src/` returns only comment-only hits (`caps/token.rs:215`, `caps/capability.rs:48`, and mod.rs:39 itself) -- zero live type/fn uses -- while the identical grep for `Grant<Admittance>` / `Grant::<Admittance>` returns real… |
| 320 | MEDIUM | `busbar-contract/src/caps/mod.rs:53` | The capability crate's 'what Rust actually enforces, honestly' table states two take sites held by a CI source scan; the tree has three, the crate's own fixture says three, and the cited scan counts confinement rather than sites | The same table's compile-time row 'Only ten steps exist' checks out exactly — `crates/busbar-contract/src/caps/step.rs:129-149` declares precisely ten step markers through `step_markers!` — so the table is not uniformly wrong and the method distinguishes the rows that hold |
| 321 | MEDIUM | `busbar-contract/src/kinds.rs:459` | busbar-contract declares itself 'the one base trait every plugin implements, and the closed set of kinds', but five of its seven kind traits (Store, Secret, Hook, Export, AuthScheme) have zero implementors in the workspace; every real plugin on those kinds, compiled-in and dropped-in alike, implements the older busbar_api traits instead | grep -rn 'fn ref_grammar' crates/ returns ONLY the declaration at kinds.rs:461; grep -rn 'ExportItem' crates/ returns only busbar-contract's own src and tests. The control proving the method-name search finds real out-of-crate implementors is grep -rn 'fn epoch' crates/ \| grep -v… |
| 322 | MEDIUM | `busbar-contract/src/tests/records_tests.rs:496` | `usage_ledger_applies_deltas_and_floors_at_zero`, whose doc says 'negative deltas floor every counter at 0', drives billable_requests through +2, +1 and -10 and never once asserts `l.billable_requests` -- and no test anywhere in the tree asserts that counter after a negative delta | Same file, same method, same object: `assert_eq!(l.requests, 0);` at records_tests.rs:502 is the paired assertion for the sibling counter after the identical -10 delta -- so the value IS reachable and IS assertable here. Zero-claim classification: `grep -rn 'billable_requests' crates… |
| 323 | MEDIUM | `busbar-contract/src/verb_store.rs:19` | verb_store.rs claims the per-node, in-process idempotency-replay cache in the admin verb-execution unit ages its slots against this one exported constant, but `busbar-core-admin/src/keys.rs` -- which owns exactly that cache for the admin's key-mint/key-rotate idempotency replay -- defines its own separate, unimported `const IDEMPOTENCY_TTL_SECS: u64 = 600` rather than referencing… | `grep -rn 'IDEMPOTENCY_TTL_SECS' crates` shows both definitions: `crates/busbar-contract/src/verb_store.rs:24: pub const IDEMPOTENCY_TTL_SECS: u64 = 600;` and, independently, `crates/busbar-core-admin/src/keys.rs:470: const IDEMPOTENCY_TTL_SECS: u64 = 600;` (module-private, no `use` importing the… |
| 324 | MEDIUM | `busbar-contract/tests/mutation_hardening.rs:129` | `a_surrogate_pairs_second_escape_needs_both_the_backslash_and_the_u` exists to pin the `\|\|` in next_json_char's low-surrogate guard against becoming `&&`, and returns `Resolved::Missing` under BOTH the correct code and that mutation, because a key that decodes to a different character and a key that fails to decode are the same answer to this pointer | The sibling cells in the same file DO discriminate, under the identical method: `a_lone_garbage_byte_is_malformed_not_an_empty_number` (mutation_hardening.rs:58-60) asserts `Resolved::Malformed`, a value the correct code produces and the mutant would not;… |
| 325 | LOW | `busbar-contract/src/caps/fixtures/lint_rules.rs:9` | The lint-rule data file says the workspace's source scan reads it; nothing outside busbar-contract does — the scan reads a hand-maintained duplicate in qa/construction.toml | The join test itself is real and does fire: `grep -n 'qa/construction.toml' crates/busbar-contract/src/caps/tests/mod.rs` -> :816 `include_str!("../../../../../qa/construction.toml")`, so the cross-check exists; it is the DIRECTION of the sentence that is wrong |
| 326 | LOW | `busbar-contract/src/caps/hold.rs:467` | `HoldCell::take`'s own doc restates the wrong take-site count at the point of the API, naming two callers where the tree has three | `grep -c 'take(&Grant::<Exit>::mint(' crates/busbar-kernel/src/teller.rs` -> 2 under the same method that returns 0 for tick.rs, so both the count and the blind spot are measured, not assumed |
| 327 | LOW | `busbar-contract/src/caps/mod.rs:39` | The honesty table names `AdmitToken` and `LedgerToken` as the types that enforce two of its compile-time rows; both were deleted in the #73 token unification and survive nowhere in code | The same grep finds `Grant<Admittance>` and `Grant<WriteMoney>` in real signatures at crates/busbar-contract/src/caps/hold.rs, so the search reaches live code; the zoo names simply are not there |
| 328 | LOW | `busbar-contract/src/caps/tests/what_the_record_reads.rs:70` | `a_holds_debug_shows_the_four_figures_a_reader_has_to_reconcile` checks that four numbers appear somewhere in the rendering and never that each sits under its own label, so two of them can be swapped and the test stays green | Exact renderings ARE asserted by equality in this same file whenever the pairing matters: `grep -n 'assert_eq!(format!' crates/busbar-contract/src/caps/tests/what_the_record_reads.rs` returns lines 197-224 (e.g. the full `Grant<write-money>` string), and caps/tests/mod.rs asserts… |
| 329 | LOW | `busbar-contract/src/caps/token.rs:92` | The sealed-trait argument states the only way to obtain a `&dyn KernelSeal` is to hold a real Pass or Grant; a third in-crate implementor exists 186 lines below | The claim's own compile_fail fixture at plugin.rs:196-213 is a real doc test, so the sealing mechanism exists; it is the enumeration of implementors that is short by one |
| 330 | NIT | `busbar-contract/src/civil.rs:12` | The civil-date module's stated reason for existing - that it is the one copy so no plane can drift - is false: three production crates carry the same algorithm, plus two more in xtask | ZERO-CLAIM CONTROL: `grep -rn 'fn civil_from_days' --include='*.rs' crates xtask` returns 5 declarations, 3 in crates/ and 2 in xtask/. Control that the search separates declarations from calls: the same command with `civil_from_days(` additionally returns call sites inside window.rs:75 and… |

## `workflows` — 31 items (HIGH 3 · MEDIUM 7 · LOW 16 · NIT 5)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 331 | HIGH | `.github/workflows/qa-security.yml:13` | Twenty-one of predev's twenty-eight workflows do not exist on the default branch `main`, and GitHub resolves `schedule`, `workflow_run`, `workflow_dispatch`, `release` and `branch_protection_rule` against the default branch only - so none of them can fire by those triggers today | The same `git ls-tree` command against origin/predev returns all 28 workflow files, and against origin/main returns 10 - so the command and the paths are right. `grep -rn predev .github/` matches 16 lines (required-status-checks.md:42, turnstile-dispatch.yml:22,29,34,… |
| 332 | HIGH | `.github/workflows/verify-deploy.yml:13` | The fourteen release checks (a)-(n) are sequential steps of ONE job and not one of them carries `if: always()`, so the first failing check skips every check after it - the opposite of the 'each independently PASS/FAIL' guarantee on line 13 | `grep -n 'if: always()\\|!cancelled()' verify-deploy.yml` returns ZERO lines; `grep -c 'if: always()' ci.yml` returns 11 under the identical command, proving the pattern and the grep both work. Every step-level `if:` in verify-deploy.yml (lines 805, 841, 867, 1065, 1169, 1211, 1238, 1358, 1486,… |
| 333 | HIGH | `.github/workflows/verify-deploy.yml:1395` | Six of the fourteen release checks become green no-ops under the edge-block condition the file says is currently live: checks (h), (i), (k) and (n) `exit 0`/`return 0` on HTTP 403/429, and the (g), (j) and (l.1) halves downgrade to `::warning::`/VISIBLE SKIP without touching `fail` | The same file DOES fail closed elsewhere under the identical grep: verify-deploy.yml:1075 `[ -f "$bin" ] \|\| { echo "::error::(c2) ..."; exit 1; }` and 1670 `[ "$fail" = 0 ]` - so `exit 0` versus `exit 1` on the 403/429 arms is a real asymmetry, not a search artefact |
| 334 | MEDIUM | `.github/workflows/docker.yml:323` | An unreadable compat digest is indistinguishable from an absent one, and both skip all four armv8.0 partial-promote assertions with a warning while `fail` stays 0 - the step then exits green | The same step DOES fail closed on the main four names: line 303-305 `if [ "$got" != "$hub_v" ] \|\| [ -z "$got" ]; then ... fail=1`, i.e. an empty digest there IS red. The compat branch is the only place emptiness is forgiven |
| 335 | MEDIUM | `.github/workflows/qa-conformance-mcp.yml:431` | `battery-subject` publishes `armed=true` when a repository VARIABLE is non-empty, even with no busbar binary present - directly contradicting the adjacent design note eleven lines earlier that says no repository variable arms this any more | The sibling A2A workflow arms from a built artifact only and its `arm` step writes armed=false rather than reading a variable (qa-conformance-a2a.yml:674-689, guarded at :707) - so the file-based arm the MCP comment describes does exist in this tree, and the MCP leg is the divergent one |
| 336 | MEDIUM | `.github/workflows/qa-fuzz.yml:51` | The whole Fuzz gate is a `for` loop over an unasserted command substitution: if `cargo +nightly fuzz list` prints nothing or exits non-zero, the body never runs, the step exits 0, and the workflow reports green having fuzzed nothing | Measured, not assumed: `bash -euo pipefail -c 'for x in $(sh -c "echo err >&2; exit 3"); do echo iter; done; echo REACHED AFTER LOOP'` prints 'REACHED AFTER LOOP' and exits 0. The same tree DOES guard this shape elsewhere - ci.yml:3428 `[ -s "$led" ] \|\| { echo "::error::no candidate ledger...";… |
| 337 | MEDIUM | `.github/workflows/qa-gate.yml:402` | The qa-gate umbrella scores `needs.slow.result`, which is identical whether one matrix leg ran and passed or all ten did, and nothing in the workflow asserts the leg set or count against qa/segments.toml | release-stage.yml DOES floor its equivalent matrices: line 908 `if len(inc) < 6:`, line 957 `if len(images) != 2:`, line 1321 `if len(declared) < 6:` - so a count floor on a fromJSON matrix is an established pattern in this slice and its absence in qa-gate.yml is a real difference |
| 338 | MEDIUM | `.github/workflows/release-stage.yml:230` | The Docker Hub tag-immutability pre-flight treats every non-200 - including the `000` its own `\|\| echo 000` manufactures on a network failure, and 429/5xx - as 'the tag is free', and a failed token fetch skips the pre-flight entirely with a warning | The rc-tag arm twenty lines below has the identical defect (line 239 `\|\| echo 000`, line 240 `if [ "$rccode" = "200" ]`, line 244 prints 'is free'), while `branch-green` in the same file does fail closed on an unreadable API (line 379-381 `if ! runs="$(api ...)"; then ... exit 1`), proving the… |
| 339 | MEDIUM | `.github/workflows/release-stage.yml:424` | Gate 0's red sweep reads `/tmp/runs.json`, which line 386-391 has already filtered down to `REQUIRED_WORKFLOWS: "CI"` alone - so the sweep whose error text says 'no exception list' can only ever name the CI workflow, and every other workflow red on the same commit is structurally invisible to it | The sibling gate in release.yml filters by EXCLUSION instead (release.yml:341 `NOT_COMMIT_CHECKS: "Release,Verify deploy"` and 379 `map(select(.id != (env.GITHUB_RUN_ID \| tonumber) and (.name \| IN($not[]) \| not)))`), so its line-434 sweep really does see every run on the sha - proving the… |
| 340 | MEDIUM | `.github/workflows/verify-deploy.yml:983` | The expected-asset floor is 5 while `expected` is built from the platform targets AND the two metadata assets, which the adjacent comment itself says should total 7 - so the SBOM and the OpenAPI document can silently go unchecked | The same file DOES floor a set correctly at line 1556 in check (j) - `[ -n "$assets" ] \|\| { echo "::error::(j) could not parse any asset filenames out of the tap formula..."; exit 1; }` - so a floor assertion is the house pattern; the (c) floor is simply off by the two metadata assets |
| 341 | LOW | `.github/workflows/ci.yml:157` | The `gate tier (what this run actually proved)` job names 8 of the 14 full-tier jobs in its 'It did NOT run' list, omitting deletion-test-matrix, feature-sets, plane-rigs, coverage, perf-build-gate and teller-steps | The umbrella's RESULTS ledger at ci.yml:3812-3840 is the authoritative tier map and does tag all fourteen with `\|full\|`: openapi-schema, migration-corpus, executable-config-lint, no-default-features, deletion-test-matrix, feature-sets, plane-rigs, no-plugins-gate, txn-guards, timing, windows,… |
| 342 | LOW | `.github/workflows/ci.yml:246` | Two of CI's heaviest jobs unconditionally take `fetch-depth: 0` plus an extra remote ref fetch to serve an `audit-ledger` gate and a `gates::audit_ledger` test module that no longer exist anywhere in the tree | `grep -rn audit-ledger .github/` returns six lines in ci.yml, all inside `#` comments and none a `run:` step. The identical grep for a gate that IS wired - `grep -rn inventory-coverage .github/` - returns ci.yml:614 `run: cargo xtask gate inventory-coverage --selftest` and ci.yml:616 `run: cargo… |
| 343 | LOW | `.github/workflows/ci.yml:1049` | The backticks in this double-quoted shell string are command substitution, not markdown - so the diagnostic for a blind test census actually RUNS `cargo test --no-run --message-format=json` and splices its JSON output into the error annotation | This is the ONLY unescaped backtick pair inside a double-quoted shell string in all 29 workflow files. `grep -n 'echo "[^"]*`' *.yml` returns 6 matches; the other 5 (ci.yml:146, 152, 161, 2660, 2668) all use `\``, and so does every one of the ~30 further matches in docker.yml, release.yml,… |
| 344 | LOW | `.github/workflows/gate-mutants.yml:308` | This file declares itself THE required check that branch protection holds and claims it runs on every event; the workflow is `workflow_dispatch`-only, and required-status-checks.md says it must be stripped OUT of branch protection | xtask/src/gates/ship_ready.rs:19-22 records the same ruling from the third direction - 'The `gate-mutants` job used to be a fifth row here ... Per owner ruling it is now MANUAL-ONLY and entirely OPTIONAL ... branch protection no longer requires the `gate-mutants` check' - so two of the three… |
| 345 | LOW | `.github/workflows/plugin-ci.yml:29` | The claim that no CI step uses `--allow-unsigned` any more is false: plugin-functional.yml packs the plugin unsigned in the mode every plugin repo's functional workflow uses | `grep -rn -- '--allow-unsigned' .github/workflows/` returns plugin-ci.yml:29 (the comment asserting it is gone) and plugin-functional.yml:274 (a live `run:` argument) - the two halves of the contradiction, found by one command |
| 346 | LOW | `.github/workflows/plugin-ci.yml:924` | `plugin-ci.yml` is `workflow_call`-only and has no `secrets:` block, so a caller that passes explicit secrets rather than `secrets: inherit` hands the coverage upload an empty token - and three independent softenings make that silent | docker.yml DOES fail closed for one of its four secrets - `BUSBAR_SIGN_KEY` has an explicit guard at docker.yml:702 - so a named refusal on a missing inherited secret is an available pattern in this slice, used once out of five |
| 347 | LOW | `.github/workflows/plugin-consumer-verify.yml:260` | `targets` is a free-text workflow_call input with no floor and no validation; an empty or typo'd value makes the archive and manifest half of the consumer check iterate zero times while the step still prints its success prose | release-stage.yml floors every equivalent derivation - line 908 `if len(inc) < 6:`, line 957 `if len(images) != 2:`, line 1321 `if len(declared) < 6:` - so a count floor is the house pattern for exactly this shape and plugin-consumer-verify.yml has none |
| 348 | LOW | `.github/workflows/qa-codeql.yml:15` | The enforcement mechanism this comment names does not exist in the tree; the real gate is `cargo xtask gate qa-gate-dispatch` | `find . -name 'qa-gate-dispatch-lint.py' -not -path './.git/*'` returns nothing; the identical `find . -name 'release-script-lint.sh'` returns ./scripts/release-script-lint.sh, proving the search works. qa-gate.yml:129 names the gate correctly, so the two files disagree about their own enforcer |
| 349 | LOW | `.github/workflows/qa-conformance-a2a.yml:34` | None of the ten jobs in this file runs on `ubuntu-latest`; every one uses a self-hosted `latchkey-*` label, so the load-bearing half of the claim - that nothing needs provisioning - is false | `grep -n 'runs-on:' .github/workflows/qa-conformance-*.yml` returns only `latchkey-small`/`latchkey-large`/`latchkey-xlarge` - zero `ubuntu-latest`. Across the whole slice `ubuntu-latest` does appear twice (in other files), proving the string would have matched had it been there |
| 350 | LOW | `.github/workflows/qa-conformance-a2a.yml:284` | The second independent A2A oracle captures the battery's exit status into `rc` with `\|\| rc=$?` and then only prints it - including the exit-3 'never reached the peer' state the same file treats as catastrophic elsewhere | `grep -n 'rc=\$?' qa-conformance-a2a.yml` finds the capture at 282; `grep -n '\$rc' qa-conformance-a2a.yml` finds only line 284's echo - no test, no comparison, no exit |
| 351 | LOW | `.github/workflows/qa-gate.yml:47` | The header's dependency narrative and its ASCII graph show `build` and `fast` as siblings; `fast` now declares `needs: [build]`, so the cheap tier is fully serialised in front of the ~2h real-money tier - the thing the comment says was avoided | The same file's corrected rationale is present at lines 147-153, immediately above the `needs: [build]` it explains - so the change was deliberate and only the header was left behind |
| 352 | LOW | `.github/workflows/release-stage.yml:589` | The release gate's `run:` block has no `shell:` key, so GitHub runs it as `bash -e {0}`; `set -uo pipefail` does not clear errexit, so when `cargo test` fails the pipeline at line 588 aborts the step and lines 589-609 - including the `::error::cargo test --workspace FAILED in the release gate` message - never execute | ci.yml's `check` job solves the identical problem correctly at ci.yml:954 - `rc=0; wait "$cargo_pid" \|\| rc=$?` - by backgrounding cargo so its exit is captured through `wait` rather than through a pipeline errexit can kill. And the repo states the rule explicitly in two places:… |
| 353 | LOW | `.github/workflows/release-stage.yml:1358` | `scripts/verify-artifact.py --coverage` is cited twice as the detector for artifact-contract rows that no matrix executes, and no workflow ever passes `--coverage` | `grep -rn -- '--coverage' .github/workflows/*.yml` returns exactly two lines, both `#` comments (release-stage.yml:921 and :1358). `grep -rn 'verify-artifact.py' .github/workflows/ scripts/` returns the three real invocation sites listed above, proving the grep finds `run:` invocations when they… |
| 354 | LOW | `.github/workflows/release.yml:346` | The step declares `shell: bash`, so GitHub runs it with `-eo pipefail`; `set -uo pipefail` leaves errexit on, which makes the two `if [ $? -ne 0 ]` API-failure handlers below unreachable - the step dies with a bare exit 1 and no annotation | Measured both shapes: `bash -eo pipefail -c 'f(){ for a in 1 2 3; do out="$(sh -c "exit 7")"; rc=$?; echo "attempt $a rc=$rc" >&2; done; return 1; }; runs="$(f)"; echo REACHED'` prints all three attempts (the retry loop DOES run, because command substitution does not inherit errexit without `shopt… |
| 355 | LOW | `.github/workflows/sched-cost-watch.yml:59` | Exit codes 2 (soft-alarm) and 3 (review) are documented as warnings that do not fail the job, but errexit is on by default and `set -uo pipefail` does not clear it, so both turn the scheduled cost gate RED | plugin-consumer-verify.yml:174-178 states the rule this file breaks, verbatim: '`bash -e {0}`, so errexit is ALREADY ON before line 1 and `set -uo pipefail` does not' turn it off - and that file writes `set +e` on line 177 before its rc-capturing block. Measured too: `bash -eo pipefail -c… |
| 356 | LOW | `.github/workflows/verify-deploy.yml:763` | The version-resolution step pastes `github.event.workflow_run.head_branch`, `github.event.release.tag_name` and `inputs.version` directly into the `run:` body, before the X.Y.Z validation on line 766 can see them - violating both the repo's stated rule and this file's own | The `pointers` job in the same file does it correctly - verify-deploy.yml:322 `SUPPLIED_VERSION: ${{ inputs.version \|\| github.event.release.tag_name \|\| '' }}` hoisted into `env:` - so the safe shape exists here and the `verify` job simply does not use it |
| 357 | NIT | `.github/workflows/ci.yml:470` | Two comment blocks in ci.yml end mid-sentence and run straight into the next block's heading, which is what a partial deletion leaves behind - and in both cases the amputated block is the one that documented a step that no longer exists | Every other section heading in ci.yml sits immediately above the job or step it introduces (e.g. ci.yml:230-237 above `structure-lint:`, ci.yml:2076-2095 above `build-release:`), so the two truncations are departures from a consistent layout, not the file's style |
| 358 | NIT | `.github/workflows/ci.yml:2071` | The fourth entry of the `plane:` paths-filter is a nested sequence rather than a string, unlike its six siblings - a paste accident that survives only because dorny/paths-filter happens to flatten nested arrays | `python3 -c "import yaml; ..."` over ci.yml's `jobs.changes.steps[1].with.filters` prints the nested list explicitly; all six sibling entries parse as plain strings under the identical call |
| 359 | NIT | `.github/workflows/sched-cost-watch.yml:34` | The unquoted `#` starts a YAML comment, so the check-run name is actually `cost-watch (DECISIONS` - the three thresholds the name exists to advertise are discarded by the parser | Every other `name:` in the 29 workflow files parses to its full written text under the same `yaml.safe_load` pass; this is the only truncation, and the only job name containing an unquoted ` #` |
| 360 | NIT | `.github/workflows/turnstile-dispatch.yml:35` | This is the only workflow file in the slice with no top-level `name:` key, so GitHub displays it by file path and it cannot be referenced by name from a `workflow_run` trigger or from either release gate's workflow-name filter | `yaml.safe_load` over all 28 workflow files returns a non-null `name` for 27 of them (e.g. 'CI', 'qa-gate', 'Release stage', 'release-fleet') and `None` only for turnstile-dispatch.yml |
| 361 | NIT | `.github/workflows/verify-deploy.yml:704` | `GHCR_IMAGE` is declared in the `verify` job's env and referenced by nothing; every GHCR assertion in the file hardcodes `getbusbar/busbar` instead | `grep -n GHCR_IMAGE verify-deploy.yml` returns exactly one line (704, the definition). `grep -c DOCKERHUB_IMAGE verify-deploy.yml` returns 27 under the identical command, proving env vars that ARE read match many times |

## `busbar-llm` — 8 items (HIGH 2 · MEDIUM 3 · LOW 2 · NIT 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 362 | HIGH | `busbar-llm/src/engine/attempt/assemble.rs:252` | A client can send a syntactically-valid-but-wrong-typed `stream_options` field (e.g. `"stream_options": "x"`) on a streaming OpenAI-family chat request; busbar's own forced include_usage injection explicitly detects this and gives up rather than fixing it, on the documented assumption that the upstream will reject the request -- an assumption the code does not verify and that does not hold for every… | The developers' own comment at assemble.rs:206-210 states the threat model explicitly ('an OpenAI Chat Completions upstream only emits that usage ... when the request carried stream_options.include_usage: true. A client that did not opt in would otherwise leave the upstream silent on tokens and… |
| 363 | HIGH | `busbar-llm/src/unit/walk.rs:463` | On the live (default-on, root-llm) request path, a delivered LLM response's token usage is posted to two independently-priced money ledgers for the same event: the legacy synchronous accrual (`ledger_and_meter` -> `EngineHost::meter_ledger`/`meter_series`, fired inside `FirstByteBody`/`deliver_buffered` at stream end) and the new asynchronous `LateAccrual` posting in `busbar/src/root/units_llm.rs`… | grep -n 'host.meter_ledger(' crates/busbar-llm/src/engine/usage.rs (matches, old ledger) and grep -n 'reported_after_terminal' crates/busbar-llm/src/unit/walk.rs (matches, new-ledger feed) both resolve against the SAME `TapCell`/`UsageSink` fields (`crate::engine::TapReport`,… |
| 364 | MEDIUM | `busbar-llm/src/engine/tests/forward_pool_integration_tests.rs:861` | The doc states `/healthz` is the sole always-open route; 14 production route declarations carry `RouteAuth::None`, across five files, including the token-exchange endpoint, the OAuth2 endpoints and the A2A push-callback receiver | The same command matched 14 lines and correctly excluded the comment-only mentions at busbar-a2a/src/a2a/pushback.rs:59 and busbar-oauth2/src/routes.rs:23, so the classifier separates code from prose |
| 365 | MEDIUM | `busbar-llm/src/engine/tests/ingress_integration_tests.rs:3398` | Two comments in the same test file describe the identical flat-fee refund mechanism (`refund_request`, fired by `finish`/`finish_admitted` on a charged-but-non-2xx end) in directly contradictory terms: one says it is now purely synchronous in-memory accounting with no store offload, the other -- describing the exact same charged=true/non-2xx case -- calls it 'offloaded/async' | `grep -n 'no store offload\\|now decrements the AUTHORITATIVE\\|offloaded/async\\|is observable synchronously' crates/busbar-llm/src/engine/tests/ingress_integration_tests.rs` matches both comments (lines ~228-229 and 3398) in the one file, both discussing the same… |
| 366 | MEDIUM | `busbar-llm/src/unit/meter.rs:432` | The Meter step computes and exhaustively unit-tests a `refund` decision ("a non-2xx end refunds the fee base") that is exposed via `Walk::refund()` but has zero callers anywhere in the workspace, including the one composition-root file (`busbar/src/root/units_llm.rs`) that drives this plane's steps | Sibling accessors on the same `Walk` type DO have live callers: `grep -n '.reported_at_step()\\|.posted_here()' crates/busbar-llm/src/unit/tests/chain.rs` matches (used by the rehearsal harness), and `grep -n 'report.fee_count' crates/busbar/src/root/units_llm.rs` matches (used in… |
| 367 | LOW | `busbar-llm/src/engine/response_body.rs:953` | A same-protocol, non-streaming (`!is_sse`, `translate == None`) JSON response that is cut short by a client disconnect mid-buffer (e.g. a large non-streamed chat completion delivered across several TCP reads) is documented and coded to bill exactly zero tokens for the bytes already generated and relayed to the client, unlike every other delivery path (streaming SSE, cross-protocol) whose `Drop` impl bills the tap's… | The streaming sibling of this exact scenario (`translate` is `Some`) DOES bill the tap's partial usage on the identical drop path: `grep -n 'crate::engine::usage::ledger_and_meter(host, &sink' crates/busbar-llm/src/engine/response_body.rs` matches at line 984, inside the same `Drop::drop`,… |
| 368 | LOW | `busbar-llm/src/unit/route.rs:51` | `route.rs` and `audit.rs` are both marked "BUILT DARK ... has no production caller" with a file-scoped `#![allow(dead_code)]`, but both are demonstrably live: `busbar/src/root/units_llm.rs:1542` calls `self.walk.route(token, &destination).await`, which calls `route_parts` in this file, and `units_llm.rs` imports and calls `audit::audit`/`audit::audit_refused` via `Walk::audit`/`Walk::audit_refused`. `main.rs` wires… | grep -n 'busbar_llm::unit::walk::\\|self.walk.route(' crates/busbar/src/root/units_llm.rs matches at lines 93-94 and 1542; grep -n 'root-llm\\|teller-waist' crates/busbar/Cargo.toml shows `root-llm` is in the crate's `default = [...]` feature list and forwards to `busbar-llm?/teller-waist`, which… |
| 369 | NIT | `busbar-llm/src/engine/select.rs:322` | The comment explaining why an `AfterDispatch` failover retry is safe reads as if a repeat dispatch 'charges the caller twice,' the opposite of the invariant the surrounding code and every sibling comment on this exact property actually state -- a dropped negation inverts the sentence documenting precisely the money property (no double-charge on retry) this audit round was asked to check | The sibling comment two lines above and the model plane's actual behavior (`Repeatable::Yes` unconditionally, contrasted with other planes' `Repeatable::No` unless configured, per the same comment block) both describe a plane deliberately repeat-SAFE because a completion is idempotent to request… |

## `busbar-core-admin` — 6 items (HIGH 2 · MEDIUM 2 · LOW 1 · NIT 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 370 | HIGH | `busbar-core-admin/src/tests/tests.rs:4463` | The only test claiming to guard router/OpenAPI-doc parity for the admin surface (`test_admin_v1_openapi_paths_all_resolve`) iterates just the 14-entry `V1_GET_PATHS` constant, not the actual document -- leaving 46 of 60 documented paths and 67 of 81 documented operations (every non-GET verb, every named-map section GET/{name}/settings route, every a2a/mcp plane trust verb) with zero proof they resolve, and checks… | python3 diff of the openapi.json `paths` object (60 paths / 81 get+post+put+patch+delete entries, counted programmatically) against a manually-reconstructed router set built from mod.rs + named_map.rs + busbar-a2a/mod.rs's admin_routes()+openapi_fragment() + busbar-mcp/mod.rs's… |
| 371 | HIGH | `busbar-core-admin/src/v1/json/handlers.rs:2746` | `limits.upstream_request_timeout_secs` is validated only for a floor (`< 1`, config_validate/mod.rs:1251) with NO ceiling, and reaches a raw, panicking `tokio::time::Instant + Duration` add on the LLM engine's per-attempt hot path (`crates/busbar-llm/src/engine/attempt/send.rs:66-67`, `tokio::time::Instant::now() + std::time::Duration::from_secs(client_settings.upstream_request_timeout_secs.max(1))`) -- the same… | `grep -n "upstream_request_timeout_secs" crates/busbar-kernel/src/config_validate/mod.rs` shows exactly one guard (`< 1`, line 1251) -- no ceiling -- in contrast to `request_body_max_bytes` in the SAME file, which has both a floor (line ~1305) AND a ceiling (line ~1312) check, proving the codebase… |
| 372 | MEDIUM | `busbar-core-admin/src/rate.rs:73` | The two mutation classifiers derive the named-map sections differently - the kernel folds them from the live plane registry, core-admin hardcodes two - so a mutation under a plane's own named-map root is Config (10/min) to one and Crud (60/min) to the other | ZERO-CLAIM CONTROL: the claim that core-admin lists only two prefixes is a read of crates/busbar-core-admin/src/rate.rs:65-74, where the CONFIG_CLASS_RULES array terminates at `];` on line 74 with exactly those two Prefix rows; the same read of crates/busbar-kernel/src/ratelimit.rs:121-126 DOES… |
| 373 | MEDIUM | `busbar-core-admin/src/verbs.rs:242` | A second admin mutation rate limiter, byte-identical in arithmetic to the kernel's, is constructed fresh inside the per-request Route step, so its counters are always zero and it can never deny | ZERO-CLAIM CONTROL for 'no instrument compares the two limiters': the only file naming both is crates/busbar-core-admin/src/tests/rate_tests.rs, where `classify_mutation` appears solely in a doc comment (:98) and a test NAME (:103) and never in an assertion. Control that cross-crate calls from… |
| 374 | LOW | `busbar-core-admin/src/v1/service.rs:278` | The owner-signed PARK that lets the admin usage read fall back to the legacy projection enumerates three of MoneyError's four variants; the wildcard arm also swallows NoCardInForce, so a hole in the dated rate-card history prices at the caller's pinned card instead of refusing | Not a zero-claim. `MoneyError`'s four variants are enumerated at crates/busbar-kernel-ledger/src/cost/view.rs:232-261; the PARK doc at service.rs:245-247 names three |
| 375 | NIT | `busbar-core-admin/src/v1/service.rs:42` | The doc comment guarding admin settings redaction cites a lint script that was deleted; the rule survives under `cargo xtask gate settings-leak` |  |

## `construction.toml` — 6 items (HIGH 2 · MEDIUM 4)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 376 | HIGH | `qa/construction.toml:373` | The eight ports-only / ports-only-tests rows -- the wall between the plane crates and core internals -- search for busbar_core::, a crate-path spelling the tree cannot contain, so all eight measure 0 against a ceiling of 0 forever | Identical grep -rho '<needle>' crates/<p>/ \| wc -l per plane crate: busbar_kernel:: -> 1345 / 611 / 487 / 153; busbar_core:: -> 0 / 3 / 3 / 0, and all six remaining hits are # comment lines inside Cargo.toml (crates/busbar-mcp/Cargo.toml:31,35,169 and crates/busbar-a2a/Cargo.toml:13,18,160), which… |
| 377 | HIGH | `qa/construction.toml:1764` | Two of plane-no-money's three forbidden module paths name a crate that was deleted, and neither live successor path is listed, so the module-path half of the money ban is two-thirds dead and blind to the seam it was written about | grep -rl 'busbar_kernel_ledger::cost::' crates/ (the one live entry) -> 26 files; the identical command for busbar_core::cost:: and busbar_core::plane_host::cost_host -> 0 files each; for the unlisted busbar_kernel::cost:: -> 11 files |
| 378 | MEDIUM | `qa/construction.toml:795` | `cargo xtask gate construction`'s own `ceiling-slack` row ('every ratcheted ceiling is pinned to today's measurement') is currently FAIL and names exactly two ceilings with slack: 'loc-ceilings:kernel measures 51515 against ceiling 52950 (rules.loc-ceilings.kernel_ceiling = 52950, slack 1435); loc-ceilings:union measures 58619 against ceiling 58870 (rules.loc-ceilings.union_ceiling = 58870, slack 251)'. Both… | `cargo xtask gate construction --report` prints the exact FAIL line quoted above with both measured/ceiling pairs; as a contrast, `loc-ceilings:kernel:pump` and `loc-ceilings:unit-total` PASS with no slack reported in the same run, showing the check does distinguish tight ceilings from slack ones… |
| 379 | MEDIUM | `qa/construction.toml:1090` | lean-core is scoped to the same one-crate dead glob while its two sibling unit rules were repointed at busbar-kernel-*, so 167 files in the eight unit crates are unscanned at max_hits = 0 | Under fnmatch, crates/busbar-kernel-*/src/* resolves to 167 files and crates/busbar-unit-*/src/* to 2. The scan set does bite where it reaches: the same word class in crates/busbar-kernel/src/config/ is carried on known_sites at toml:1127-1171 |
| 380 | MEDIUM | `qa/construction.toml:1233` | hold-discipline's non-kernel scope is a glob that matches one crate with no holds in it, so the money settle site and the budget crate's hold cell are both outside the scan set | The same .take( pattern inside the scan set does resolve: crates/busbar-kernel/src/teller.rs:1207. The selftest only ever plants the kernel half (selftest.rs:1152-1159 writes crates/busbar-kernel/src/zz_planted_hold.rs). ls -d crates/busbar-unit-* -> 1 dir; ls -d crates/busbar-kernel-* -> 8 dirs |
| 381 | MEDIUM | `qa/construction.toml:1985` | Two of legacy-reach's three prefixes name deleted crates and measure 0 against a figure of 0 forever, while the three live retiring engines the ratchet is about have no prefix at all | Identical grep -rn '<prefix>' crates/busbar/src/root/ crates/busbar/src/main.rs: busbar_llm:: -> 58, busbar_core:: -> 0, busbar_substrate:: -> 0 (and 0 across the whole of crates/) |

## `plugin-loader` — 13 items (HIGH 1 · MEDIUM 9 · LOW 3)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 382 | HIGH | `plugin-loader/src/lib.rs:1046` | MeteringDelta/MeteringRow gained `priced_from_ms` -- which the engine makes part of the metering ACCRUAL KEY -- with no abi_version bump and no legacy adapter, while supported_abi("store") still admits abi_version 2; a published store plugin has no such column, so it merges a rate-card-split day back into one cell and the whole day is then re-priced at the midnight card | ZERO-CLAIM (no metering adapter exists): `grep -n 'legacy_usage_wire' crates/plugin-loader/src/lib.rs` returns 822 (the definition) and 951/988/1018 -- get_usage, put_usage, add_usage. It does NOT appear in add_metering (1045-1050) or list_metering (1052-1057). POSITIVE CONTROL that the adapter… |
| 383 | MEDIUM | `plugin-loader/src/lib.rs:143` | No test anywhere exercises ffi_guard catching a panic that actually unwinds out of a dlopened cdylib, and for the population the doc names (a Rust plugin NOT built with the busbar SDK) Rust's catch_unwind treats an unwind raised by the plugin's own statically-linked std as a foreign exception and ABORTS | grep -rn 'ffi_guard\b' crates/plugin-loader/src/tests/ crates/plugin-loader/tests/ returns nothing; the control that the same grep over the same directories does match real symbols is grep -rn 'free_guarded' crates/plugin-loader/src/tests/, which matches ffi_guard_tests.rs:51 - and that test's… |
| 384 | MEDIUM | `plugin-loader/src/lib.rs:1445` | The SDK was changed so SecretRequest::Resolve's `deadline_ms` finally reaches the module via resolve_with_deadline, but the ONLY host-side producer of that request hardcodes None, so the field is still dead -- the fix closed the plugin half of the seam and left the host half exactly as it was | ZERO-CLAIM: `grep -rn 'resolve_with_deadline' crates/ --include='*.rs'` returns exactly five sites -- plugin-sdk/src/lib.rs:620 (the plugin-side call), plugin-sdk/src/tests/lib_tests.rs:127, api/src/secret.rs:114 (the defaulted trait method), and… |
| 385 | MEDIUM | `plugin-loader/src/plane.rs:30` | There are two structurally unrelated types both named PlaneDecl - the repr(C) drop-in one and the native Rust compiled-in one - with no adapter between them, and DynPlane has no production consumer, so a dropped-in plane contributes no config section, routes, claims, admission or scope kinds | grep -rn 'DynPlane' crates/ \| grep -v 'plugin-loader/src' returns only crates/busbar-kernel/tests/plane_abi_rider.rs (an integration test). Control that the compiled-in fold IS wired: grep -rn 'install_planes' crates/busbar/src/main.rs matches at line 311 with five &PLANE_DECL pushes above it, and… |
| 386 | MEDIUM | `plugin-loader/src/plane.rs:96` | DynPlane unmaps the plugin image on Drop but exposes no way to free the OpaqueState its build() handed out, so a built plane's state leaks and both its ptr and its free fn-pointer dangle into unmapped memory the moment DynPlane drops | grep -n 'free' crates/plugin-loader/src/plane.rs returns nothing outside the doc prose; the control that the grep and path are right is grep -n 'free' crates/plugin-loader/src/tests/plane_conformance_tests.rs, which matches at line 671 where the TEST itself calls `free(p.ptr)` by hand for the… |
| 387 | MEDIUM | `plugin-loader/src/plane.rs:163` | DynPlane has slot readers for config_validate, build, hydrate, start and dispatch and NONE for admin_routes or openapi, so the two PlaneDecl slots the ABI reserved for a plane's admin and OpenAPI contribution -- and attached a documented 'non-vacuity invariant' to -- are read by nothing, while the compiled-in native decl's equivalents ARE called | ZERO-CLAIM: `grep -rn 'admin_routes' crates/plugin-loader/src/ crates/busbar-kernel/src/plane_host/` returns nothing; same for `openapi`. POSITIVE CONTROL, same command, same directory: `grep -rn 'provided_carriers' crates/plugin-loader/src/plane.rs` matches :75, :107, :136-137, :142, :387-389 and… |
| 388 | MEDIUM | `plugin-loader/src/plane.rs:227` | Every per-LOAD hot-lane crossing (config_validate, build, hydrate, start) runs on the CALLER's thread via the inline ffi_guard, while the cold lane's identical per-load crossing (busbar_open, the plugin's constructor) is confined to a never-retiring worker | grep -n 'ffi_guard_confined' crates/plugin-loader/src/plane.rs returns lines 335 and 362 only (abi, plane_decl); grep -n 'crate::ffi_guard(' crates/plugin-loader/src/plane.rs returns 178, 227, 273 and 289 - config_validate, build, dispatch and hydrate/start. Control that ffi_guard_confined IS used… |
| 389 | MEDIUM | `plugin-loader/src/plane.rs:396` | wire_up_plane CLAMPS a PlaneDecl that attests a size larger than this build's struct, where PlaneHostVtable::check REFUSES the identical over-claim - and PlaneDecl slots are fn-pointers the host then CALLS | grep -n 'SizeOverBuild' crates/busbar-plugin/src/hot/tests/host_tests.rs matches (the host-vtable over-claim IS tested and refused); grep -n 'advertis\\|SizeOver' crates/plugin-loader/src/tests/plane_conformance_tests.rs returns only the vocabulary-length test at line 580, i.e. no decl size… |
| 390 | MEDIUM | `plugin-loader/src/store_adapter.rs:101` | speaks_new_ops(abi) requires abi_version >= 5, but supported_abi("store") is [2,4] and a bare path load assumes 4, so the predicate is structurally always false and the ten operations this release adds are answered by a node-local in-memory shim on every deployment this binary can produce | grep -n 'speaks_new_ops' crates/plugin-loader/src/ -r shows the definition plus its call sites; the control that the SAME kind of schema predicate IS reachable in this window is needs_legacy_usage_wire (crates/plugin-loader/src/legacy_usage.rs:38, abi_version < 4), which is true for the published… |
| 391 | MEDIUM | `plugin-loader/src/tests/lib_tests.rs:24` | The loader-mechanism tests for the `kind: store` dlopen seam are documented as covered on every push to dev by `dev-gate.yml`; that workflow does not exist, and the only thing that sets the `DEV_GATE` variable the hard-failure needs runs on the `qa` branch | `ls .github/workflows/` lists 29 files and `ls .github/workflows/qa-gate.yml` resolves, so the directory and the method are right; `dev-gate.yml` is genuinely absent |
| 392 | LOW | `plugin-loader/src/lib.rs:760` | busbar_plugin_kind() returns a bare *const u8 with no length and the loader walks it with CStr::from_ptr, so a plugin that returns a non-NUL-terminated pointer causes an unbounded out-of-bounds read at the very first crossing of every load - the one plugin-supplied buffer in this ABI that skips the cap every other one gets | grep -rn 'MAX_PLUGIN_RESPONSE_LEN\\|MAX_PLANE_VOCAB_LEN\\|MAX_LOG_LEN' crates/plugin-loader/src returns a cap for every other plugin-supplied length (response, open error text, plane vocabulary, log record); the kind pointer is the only one with no constant, because the ABI gives it no length to cap |
| 393 | LOW | `plugin-loader/src/registry.rs:90` | busbar-contract declares seven plugin kinds, but supported_abi has arms for only six; a kind:transport tarball falls into the catch-all and is rejected at scan, so transport is a kind that exists only compiled in | grep -n '"store" =>\\|"secret" =>\\|"auth" =>\\|"hook" =>\\|"export" =>\\|"plane" =>\\|"transport" =>' crates/plugin-loader/src/registry.rs returns six arms and no transport arm; the control that the grep spells the arms the way the file does is that the six it finds are exactly the six kind… |
| 394 | LOW | `plugin-loader/src/tests/export_conformance_tests.rs:268` | `the_pre_envelope_path_loses_a_dropped_in_plugins_counters`, presented at line 234 as 'THE RED ARM -- what the old arrangement did, kept as the witness', increments two local `std::cell::Cell<u64>`s and asserts their values; it touches no production symbol and passes unconditionally | No `crate::`-qualified call, `run_compiled_in`, `run_dropped_in` or `observe::` appears anywhere between lines 245 and 270. The same search finds real production calls in the sibling cells of this very file: `compiled_in_and_dropped_in_report_identical_observations` (line 183) calls… |

## `busbar-plane-decision` — 9 items (HIGH 1 · MEDIUM 4 · LOW 3 · NIT 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 395 | HIGH | `busbar-plane-decision/src/plane.rs:401` | A successful (non-error) `systemone` decision response whose `/usage/units` field is missing, fractional, negative, or otherwise fails `u64` text parsing is billed as nothing at all — not zero, no line at all — because `meter()` only emits a usage locator when `FACT_USAGE_UNITS` happens to be set | The crate's own test suites (crates/busbar-plane-decision/tests/jev.rs:118 `billable_success_metering_surface_reports_usage_on_success_and_nothing_on_error`, tests/conformance.rs:88/112, src/tests/codec.rs:13) only assert two cases: a success WITH a valid integer usage count, and a 422 WITH no… |
| 396 | MEDIUM | `busbar-plane-decision/Cargo.toml:25` | This crate's own manifest comment and tests/invariance.rs:20-21 both assert in strong prose that busbar-plane-decision 'is not even in the binary's dependency graph' / 'the binary does not even depend on this crate' -- false in this pinned tree: crates/busbar/Cargo.toml:132 declares `busbar-plane-decision = { path = "../busbar-plane-decision", optional = true }`, line 192's `default = [...]` feature list includes… | grep -n 'busbar-plane-decision\\|plane-decision' crates/busbar/Cargo.toml returns the three matches quoted above (path dependency, default feature list, feature-to-dep mapping); `findings/r2-instruments.json`, `r2-governance.json`, `r3-instruments.json` and a sibling round-4 lens's… |
| 397 | MEDIUM | `busbar-plane-decision/src/facts.rs:37` | facts.rs:34-36 states 'the provider name IS the session-scoped fact, because a session's provider changing mid-flight would be a different priced thing' -- but the very next line declares `SESSION_FACTS` containing only `FACT_REQUEST_ID`; no provider-name fact exists anywhere in this crate's declared fact vocabulary (facts.rs declares exactly 4 FACT_ constants total: FACT_OP, FACT_REQUEST_ID, FACT_HAS_ERROR,… | grep -n 'provider' crates/busbar-plane-decision/src/*.rs returns 26 hits, all either doc prose or the unrelated `ResourceLocator{kind:"decision_provider",..}` authorization/audit resource in plane.rs:359-362 (a scope-approval fact, not a `Facts`/session fact); grep -n 'FACT_'… |
| 398 | MEDIUM | `busbar-plane-decision/src/lib.rs:57` | lib.rs:56-58 states in the present tense that 'the composition root builds one [a Registration] at boot, interns every config-derived key through it exactly once' -- but crates/busbar/src/root/plane_decision.rs, the only composition-root file for this plane, says the opposite of itself in its own doc: 'build' is `None` and 'the plane has no unit path in `root/` yet to drive it' (plane_decision.rs:39-40, 80). Across… | grep -rn 'DecisionProvider' across every crate in the workspace returns exactly 4 hits, all at the type's own definition/accessor site in lib.rs (lines 60, 77, 83, 96) -- zero construction sites anywhere else; the identical search technique DOES find real construction sites for this plane's shipped… |
| 399 | MEDIUM | `busbar-plane-decision/src/plane.rs:63` | DecisionPlane::upstream_destination() (plane.rs:61-75, backing both verify() and route()'s upstream_leg()) and DecisionPlane::approve() (plane.rs:356-365) both resolve the SAME thing -- self.providers().first() -- regardless of how many providers are actually configured on `providers: &'static [DecisionProvider]`, and there is no other code anywhere in this crate (codec.rs declares REQUEST_PTRS as empty, ops.rs's… | grep -n 'providers().first()' crates/busbar-plane-decision/src/plane.rs matches both call sites (lines 62 and 358) with no third call site anywhere resolving a provider by name; grep -rn 'model\b' crates/busbar-plane-decision/src/codec.rs crates/busbar-plane-decision/src/plane.rs finds no… |
| 400 | LOW | `busbar-plane-decision/src/meta.rs:48` | meta.rs's CONFIG_SCHEMA (a hand-written JSON Schema string, lines 48-73) is a manually duplicated copy of busbar_contract::config::ModelCfg's field list (config.rs:20-70+, #[serde(deny_unknown_fields)]) with no test or generation step keeping the two in sync -- src/tests/meta.rs's own schema test only checks top-level keys and the absence of the two reserved member names, and never enumerates ModelCfg's real fields… | Direct side-by-side field comparison (this finding's own verification) confirms today's 8 CONFIG_SCHEMA properties match ModelCfg's 8 real fields one-for-one -- proving the comparison technique detects real divergence when present; none exists yet, so this is filed as a structurally-undefended… |
| 401 | LOW | `busbar-plane-decision/tests/jev.rs:66` | Every test in busbar-plane-decision's entire suite (10 test files, all of tests/{alloc_gate,purity,jev,conformance}.rs plus src/tests/*.rs) constructs the plane via `DecisionPlane::EMPTY` -- not one test anywhere in the crate ever calls `DecisionPlane::new(&[...])` with a real provider -- so DecisionPlane::route(), ::verify(), ::approve(), ::upstream_destination() and ::upstream_leg() (the entire… | grep -rn 'DecisionPlane::new(' crates/busbar-plane-decision/{src,tests}/ returns zero matches, while the identical grep for 'DecisionPlane::EMPTY' returns 12 matches across tests/alloc_gate.rs, tests/purity.rs (x3), tests/jev.rs (x4) and tests/conformance.rs (x6) -- proving every single test in the… |
| 402 | LOW | `busbar-plane-decision/tests/purity.rs:261` ⟂FOLD | The 'a plane never hands back a decision, an amount or a credential' scan forbids eleven spellings, three of which occur nowhere in the workspace except inside the three copies of this list, and it omits every spelling the tree actually uses for money | The same file's sibling scans are built on vocabulary the tree really uses and would really fire: `the_plane_names_no_kernel_side_crate` (purity.rs:139) forbids `busbar_kernel`, which this crate's own doc (purity.rs:11-14) records as having been RED on two real source lines until those files were… |
| 403 | NIT | `busbar-plane-decision/src/lib.rs:36` | The crate's own module doc (lib.rs:36) and facts.rs:49 both cite a standalone file `tests/pii_witness.rs` (and facts.rs additionally implies a mirrored copy under src/tests/) as the home of the PII-witness guarantee; no such file exists anywhere in the crate (confirmed by `find crates/busbar-plane-decision -iname '*pii*'`, zero results). The actual test is a single function,… | grep -rn 'pii_witness' crates/busbar-plane-decision/ finds the two false file-name references (lib.rs:36, facts.rs:49) plus the real test's function name and module-doc mentions (tests/jev.rs:3,17,32,45,174; tests/conformance.rs:7,26) -- the same grep that surfaces the stale reference also surfaces… |

## `busbar-kernel-audit` — 6 items (HIGH 1 · MEDIUM 2 · LOW 2 · NIT 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 404 | HIGH | `busbar-kernel-audit/src/amend.rs:227` | The amendment journal -- which the crate's own doc calls the ONLY way a recorded money figure may change, and the only record of a hook or export plugin reading content -- is entirely unconstructed: nothing outside the module names AmendChain, Amendment, AmendBody, correction, content_access or amends | ZERO: `grep -rn --include='*.rs' 'AmendChain\|amend::correction\|content_access\(\|AmendBody\|Amendment' crates/ \| grep -v 'busbar-kernel-audit/src/amend.rs' \| grep -v tests` -> only crates/busbar-kernel-audit/src/lib.rs:89-90, the re-export lines. CONTROL: the identical command for `AuditInputs`… |
| 405 | MEDIUM | `busbar-kernel-audit/src/record.rs:505` | Four chain verifiers ship; the one that verifies the NEW production audit record has no scope check at all and collapses two distinguishable tampers into one, so a run mixing two subjects' records verifies clean where the other three report ForeignScope | ZERO-CLAIM CONTROL for 'record.rs's walk has no scope arm': reading record.rs:493-519 in full shows the loop body is the link/seq check, then the digest check, then the advance - no scope_of. Control that scope_of exists and is findable by the same read: `grep -rn 'scope_of'… |
| 406 | MEDIUM | `busbar-kernel-audit/src/tests/amend_tests.rs:91` | `an_export_access_is_recorded_the_same_way_a_hook_access_is` never records a hook access to compare against -- it checks the amendment class and that the hex digest is 64 characters -- and `Reader::Export`'s digested word is pinned nowhere in the crate | `grep -rn '"export"' crates/busbar-kernel-audit/` returns exactly one line -- amend.rs:75, the definition itself -- so nothing pins it. The identical command over the sibling vocabulary proves the method and the path: `grep -rn '"adjust"' crates/busbar-kernel-audit/` returns two lines, amend.rs:50… |
| 407 | LOW | `busbar-kernel-audit/src/tests/legacy_ring_tests.rs:267` | `a_restore_at_the_top_of_the_range_saturates_instead_of_panicking` asserts only that the restored ring holds one entry; it never reads the resume position the saturation exists to produce, so only the panic half of the name is pinned | `grep -n 'u64::MAX' crates/busbar-kernel-audit/src/tests/legacy_ring_tests.rs` returns exactly one hit, line 262 inside this test, so this is the only cell that can reach the saturation branch. Resumed positions ARE assertable and ARE asserted in the same file by the same method:… |
| 408 | LOW | `busbar-kernel-audit/src/tests/sign_tests.rs:551` | The cell's doc claims the head history has 'no pruning method, no cutoff, no capacity', but the body observes 50 records and asserts the series reached 50 -- so any capacity bound larger than 50 is invisible to it and to every other test in the crate | The series length IS assertable and IS asserted, so the method and path are right: sign_tests.rs:564 `assert_eq!(history.series().len(), 50);` and sign_tests.rs:529 `assert_eq!(chain.heads().series().len(), 1);`. Zero-claim: those are also the LARGEST series any test in the crate builds -- `grep… |
| 409 | NIT | `busbar-kernel-audit/src/tests/digest_framing_tests.rs:288` | The cell's heading says the framing is a property of the record type and 'there is no per-record scheme byte', while `AuditEntry` carries exactly that -- a serialized `pub scheme: u8` that its own `framing()` override reads per record | `grep -rn 'FRAMING' crates/busbar-kernel-audit/` shows the const's only production reader is the default method body in chain.rs; `grep -rn '\.scheme' crates/busbar-kernel-audit/` matches entry.rs:230 plus nine sites in digest_framing_tests.rs itself (533-534, 542-543, 573, 592-594, 630, 651, 673),… |

## `busbar-plugin` — 6 items (HIGH 1 · MEDIUM 3 · LOW 2)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 410 | HIGH | `busbar-plugin/src/lib.rs:82` | The 20->21 airlock bump RESIZED an INTERIOR field (BuildCtx.host_ctx, 8 -> 16 bytes) and shifted the four fields after it by 8 bytes, but ABI_MAJOR stayed 1 and check_preamble refuses only magic and MAJOR -- so a dropped-in plane built at any older minor is ACCEPTED and then reads config_ptr/config_len/resolved_refs_* from the wrong offsets | ZERO-CLAIM (nothing compares the minor): `grep -rn 'abi_minor' crates/ --include='*.rs' \| grep -v '/tests/'` returns ONLY lib.rs:25 (a doc), :85 (a doc), :97 (the field declaration) and :105 (an assignment) -- no comparison anywhere. POSITIVE CONTROL, identical command shape: `grep -rn 'abi_major'… |
| 411 | MEDIUM | `busbar-plugin/src/hot/host.rs:339` | JournalReframeFn is the one PLANE-IMPLEMENTED fn-pointer in the host vtable family and it returns the bare StatusClass enum by value (line 351) instead of the RawStatus carrier this ABI invented for exactly that direction | grep -n 'RawStatus' crates/busbar-plugin/src/hot/decl.rs matches on all seven PlaneDecl slot types, proving the RawStatus discipline is applied to plugin-implemented slots; grep -n 'PLANE-PROVIDED' crates/busbar-plugin/src/hot/host.rs returns exactly one hit (line 332), so JournalReframeFn is the… |
| 412 | MEDIUM | `busbar-plugin/src/hot/host.rs:449` | The RawStatus/RawFault/RawEgressKind/RawUsageComponent/RawFraming hardening covers only plugin->host bytes; six append-only #[repr(u8)] enums still cross host->plane as bare enums, which is undefined behaviour in an older-minor plane the airlock deliberately accepts | grep -nE 'pub [a-z_]+: (StatusClass\|Decision\|EgressFailClass\|VerifyOutcome\|IdentityOutcome\|Unavailability),$' crates/busbar-plugin/src/hot/pod.rs returns the five bare-enum POD fields; the control that the same grep finds the HARDENED fields as Raw carriers is grep -n 'pub component:… |
| 413 | MEDIUM | `busbar-plugin/src/hot/pod.rs:1089` | decode_usage_units forms a slice over a plugin-attested units_len with no bound - the only from_raw_parts on a plugin-supplied length in the whole slice that skips the cap every sibling path enforces | grep -rn 'from_raw_parts' crates/plugin-loader/src crates/busbar-plugin/src \| grep -v tests returns 5 slicing sites; four are preceded by an explicit cap and this one is not. The control that the grep finds guarded sites too is crates/plugin-loader/src/plane.rs:486, which appears in the same… |
| 414 | LOW | `busbar-plugin/src/cold/endpoint.rs:133` | safe_relay_status and both its wrappers (EndpointResponse::safe_status, HttpResponse::safe_status) have zero production callers; the one real relay clamps with StatusCode::from_u16(..).unwrap_or(BAD_GATEWAY), which accepts 100..=999 where the ABI helper accepts only 100..=599, so a plugin-chosen status of 700-999 is relayed verbatim to an external client | ZERO-CLAIM: `grep -rn 'safe_relay_status\|safe_status' crates/ --include='*.rs'` matches only cold/tests/endpoint_tests.rs:81/90/92/96, cold/auth.rs:260/265-266 (the wrapper defining itself), and the definition here -- no caller in busbar-kernel, busbar-core-admin or plugin-loader. POSITIVE… |
| 415 | LOW | `busbar-plugin/src/hot/pod.rs:21` | The `version` field every hot POD struct leads with is STAMPED in about forty places and COMPARED in zero, so POD_VERSION's own 2->3 bump signals nothing to any receiver -- the same inert state PlaneHostVtable.size was in before ::check was written | ZERO-CLAIM: `grep -rn 'POD_VERSION' crates/ --include='*.rs' \| grep -v '/tests/'` returns 45 lines; every one is a `use`, a doc, the definition, or `version: POD_VERSION` on a struct literal -- there is no `== POD_VERSION`, no `!=`, no `<`, no match. POSITIVE CONTROL that a comparison of this… |

## `busbar-kernel-egress` — 2 items (HIGH 1 · NIT 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 416 | HIGH | `busbar-kernel-egress/src/attempt.rs:501` | Transport::close() is reachable from exactly one branch of the whole attempt lifecycle (full success through deliver(), attempt.rs:741-742); every other exit -- a write failure (this line), an attempt-timeout, a budget-timeout, an empty/erroring frame stream, and a non-success upstream status via classify_failure (line 354) -- drops the dialled Conn without ever calling close() on it | The same function scrupulously guards every OTHER resource on every exit including a dropped future: JournalGuard (attempt.rs:211-247, Drop calls journal.abandoned), BudgetGuard (attempt.rs:676-680/750-760, Drop refunds the budget), ProbeGuard (select.rs:199-256, Drop calls release_probe… |
| 417 | NIT | `busbar-kernel-egress/Cargo.toml:21` | The crate's own header comment, a test's dependency allowlist, and a second test's doc comment all describe capability tokens as coming from a separate `busbar-caps` workspace crate; no such crate exists anywhere in the workspace (caps is a module, busbar_contract::caps, inside busbar-contract) and the crate's actual [dependencies] section names only busbar-contract and futures | `find . -maxdepth 3 -iname "*caps*" -type d` (excluding target/) and `grep -n caps Cargo.toml` (workspace root) both return nothing; crates/busbar-kernel-breaker/Cargo.toml's actual [dependencies] section reads only `busbar-contract = { path = "../busbar-contract" }`;… |

## `busbar-plane-streaming` — 2 items (HIGH 1 · LOW 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 418 | HIGH | `busbar-plane-streaming/src/twilio.rs:322` | The live Twilio media decoder carries its own private base64 decoder that strips '=' anywhere in the string; the workspace's shared decoder rejects data resuming after padding, and the byte count the private copy produces is a billed ledger quantity | ZERO-CLAIM CONTROL for 'the live path does not use the shared decoder': `grep -rn 'base64_decode(' --include='*.rs' crates \| grep -v '/tests/' \| grep -v 'fn base64_decode'` returns crates/busbar-plane-streaming/src/twilio.rs:160 calling the LOCAL fn, while the SAME command returns… |
| 419 | LOW | `busbar-plane-streaming/src/twilio.rs:268` | The comment that exists to justify the private base64 copy asserts the two implementations carry the identical algorithm; they do not, and that sentence is what would stop a reviewer looking | ZERO-CLAIM CONTROL: `grep -rn 'base64_decode\|base64_encode' crates/busbar-voice-codec/src/topology/twilio.rs` returns only the import at :31 and the call at :214 - no local implementation. Control that the same grep DOES find an implementation when there is one: over… |

## `busbar-transport-ws` — 1 items (HIGH 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 420 | HIGH | `busbar-transport-ws/src/conn.rs:81` | Five transport crates implement the same close fence; four pair the closed flag with a tokio Notify that wakes a parked reader, ws ships the flag alone - and ws's own doc claims the flag reaches a pump already suspended in a read, which an AtomicBool cannot do | ZERO-CLAIM CONTROL: `grep -rn 'tokio::sync::Notify\|notify_waiters\|begin_close' --include='*.rs' crates/busbar-transport-*/src` returns 19 lines across busbar-transport-{stdio,tcp,tls,http,grpc} and ZERO in busbar-transport-ws. Four sibling crates matching under the identical pattern is the… |

## `unconstructed.toml` — 1 items (HIGH 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 421 | HIGH | `qa/unconstructed.toml:231` | The `money-one-function-view` capability (id at line 226) is declared `unshipped = true` on the premise that 'no production code ever builds a LedgerEntry to feed it', with a `switch` field (line 233) that explicitly gates striking this row behind a human owner: 'STRIKE THIS ROW in the commit that gives the production money path a LedgerEntry producer... MONEY: PARKED-OWNER — an owner signs, not an agent.' `cargo… | `grep -rn "LedgerEntry::new(" --include="*.rs" crates/ \| grep -v /tests/ \| grep -v _test` finds exactly one production hit (service.rs:270), matching the gate's own count of 1; as a negative control, the eighteen other `[[capability]]` rows in qa/unconstructed.toml marked `unshipped = true` all… |

## `busbar-a2a` — 12 items (MEDIUM 9 · LOW 3)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 422 | MEDIUM | `busbar-a2a/src/a2a/grpc.rs:214` | The gRPC binding discards the exact numeric Retry-After cooldown the HTTP and JSON-RPC legs attach to the identical breaker-open refusal (`parts.headers` is destructured here but never read for Retry-After anywhere in the file), and maps that refusal to a gRPC code carrying no backoff semantics, so a gRPC client has no signal to slow down against a tripped breaker | `grep -n 'RetryInfo\\|retry_after\\|Retry-After\\|parts.headers' crates/busbar-a2a/src/a2a/grpc.rs` returns zero hits. The search term is proven correct by the matching control `grep -n 'Retry-After\\|retry_after' crates/busbar-a2a/src/a2a/receive.rs`, which DOES match receive.rs:2260, :3135,… |
| 423 | MEDIUM | `busbar-a2a/src/a2a/jws.rs:208` | `verify_card`'s loop over a card's `signatures[]` array keeps running after finding a genuine match against the pinned issuer key (setting `verified = Some(...)`) rather than short-circuiting, and every per-entry step (`check_algorithm`, `check_critical_headers`, `decode_signature`) is guarded with `?`; a LATER entry that is malformed, names an unsupported algorithm, or declares an unknown `crit` header aborts the… | `a2a/tests/jws_tests.rs`'s `a_rotation_carrying_two_signatures_reports_which_one_verified` (line 258) drives a real two-entry array of WELL-FORMED signatures and asserts the reported index, and `a_malformed_signature_is_refused_rather_than_skipped` (line 292) covers a card whose ONLY signature is… |
| 424 | MEDIUM | `busbar-a2a/src/a2a/originate.rs:260` | `mirror_push_config`'s reconciliation only ever re-arms busbar's own callback registration at the backend when the mirrored hop was a READ (GetPushConfig/ListPushConfigs); a failed `CreateTaskPushNotificationConfig` or `DeleteTaskPushNotificationConfig` mirror hop is logged once at bare `tracing::info!` and then permanently dropped, with no retry and no periodic sweep that ever revisits it -- unlike the read path,… | `grep -n 'A2A_PUSH_REARM_FAILED' crates/busbar-a2a/src/a2a/originate.rs crates/busbar-a2a/src/diagnostics.rs` matches the re-arm path and its diagnostic definition, proving the codebase has a named, retried reconciliation for the read case. The same search shape for an initial-arm-failure… |
| 425 | MEDIUM | `busbar-a2a/src/a2a/pushback.rs:517` | When a backend's pushed state report fails to durably `transition()` for a real store I/O reason (not the benign 'already terminal' case), pushback.rs collapses both error kinds with `.map_err(\|e\| e.to_string())`, logs at bare `tracing::info!` with no diagnostic code and no error-once latch, and answers the backend `202 Accepted` -- before the `push_callback.is_some()` delivery gate is ever reached, so the… | `grep -n 'A2A_INBOUND_TASK_UNRECORDED\\|A2A_DISPATCH_UNRECORDED\\|A2A_PIN_REFUSAL_UNRECORDED' crates/busbar-a2a/src/a2a/receive.rs crates/busbar-a2a/src/a2a/route.rs` matches every sibling failure site on this same plane that can lose a durable write, each using `diag_error!`/`diag_debug!` with a… |
| 426 | MEDIUM | `busbar-a2a/src/a2a/pushdeliver.rs:373` | Outbound webhook delivery makes exactly one attempt per task-state transition with no retry, no backoff and no dead-letter; when the destination answers with a non-2xx HTTP status (rather than a connection-level failure), `attempt()` still calls `remember`/`forget` (pushdeliver.rs:474-477) BEFORE checking the status code, so on a terminal transition the caller's delivery pin and credential are cleared even though… | The no-retry claim is a zero-claim: `grep -n 'fn .*retry\\|RetryPolicy\\|retry_count\\|backoff' crates/busbar-a2a/src/a2a/pushback.rs crates/busbar-a2a/src/a2a/pushdeliver.rs` returns zero. The method is proven by the contrasting control on the same vocabulary in the sibling hop path: `grep -n… |
| 427 | MEDIUM | `busbar-a2a/src/taskstore.rs:437` ⟂FOLD | The retention sweep's abandon transition (`plan_abandon`) moves a stalled task straight to `canceled` via a closure that only builds a durable `Mutation` -- it never calls `notify_push`, unlike every other state transition in this crate (route.rs:348, receive.rs:2969/3023/3155/3311, originate.rs:419) -- and `TaskRegistry::sweep_now`/`submit` give the caller no way to even learn which task(s) got swept-abandoned, so… | `grep -n notify_push crates/busbar-a2a/src/a2a/{route,receive,originate}.rs` (excluding tests) matches 7 times, all immediately after a `.transition(...)` call, proving the codebase's own convention; `grep -n abandon crates/busbar-a2a/src/a2a/pushback.rs crates/busbar-a2a/src/a2a/pushdeliver.rs… |
| 428 | MEDIUM | `busbar-a2a/src/taskstore.rs:574` ⟂FOLD | Boot rehydrate returns immediately for any TERMINAL task row without ever fetching or verifying its provenance chain (only non-terminal rows reach the event-listing + `verify_chain` path). Combined with a documented residual in the shared kernel engine (busbar-kernel/src/plane/handle_engine.rs:528-548: a rehydrated handle has no in-memory pre-image, so if a row upsert succeeds but the paired chain-event append then… | The analogous bug for a handle that arrived via `submit` (which DOES carry a pre-image) is proven both fixed and tested at busbar-kernel/src/plane/tests/handle_engine_tests.rs:1224 (`a_mutation_whose_event_append_fails_does_not_leave_the_durable_row_ahead_of_the_chain`), proving the authors… |
| 429 | MEDIUM | `busbar-a2a/src/taskstore.rs:912` ⟂FOLD | `TaskRegistry::compact` is the only code path that can purge terminal `task`/`task_event` rows from a configured durable store, and it has zero production callers anywhere in the workspace; the submit-time sweep (`sweep_now`) only evicts entries from the in-memory working set, so once a terminal task ages out of that working set it becomes permanently unqueryable while its durable row and full hash-chained… | `grep -rn --include='*.rs' '\.compact(' crates/` returns only the two internal self-delegations (taskstore.rs:912/913, calllog.rs:1003) and test call sites -- no production caller. The identical search shape for the known-live sibling on the same struct, `sweep_now(`, finds a genuine production… |
| 430 | MEDIUM | `busbar-a2a/src/taskstore.rs:937` ⟂FOLD | The only on-demand verifier of an A2A task's provenance chain has no production caller anywhere in the workspace -- no admin verb, no route, no scheduled job -- so once a task reaches a terminal state, its tamper-evidence chain is never checked again by anything in a running deployment. This is the missing-detection half of the taskstore.rs:574 finding above: that one can create a state/chain divergence, this one… | `grep -rn 'verify_task_chain' --include='*.rs' .` returns 10 hits: the definition, five calls all inside `a2a/tests/taskstore_tests.rs`, and two doc-comment cross-references in other crates -- zero production callers. Corroborated by the codebase's own acknowledgement of this identical gap for the… |
| 431 | LOW | `busbar-a2a/src/a2a/pushback.rs:526` | When a backend re-reports a state busbar already holds, the handler correctly skips the durable transition (recognizing it as a retry) but the onward caller-facing delivery is gated only on `moved.push_callback.is_some()`, unaffected by that decision -- so a real outbound POST to the caller's webhook still fires and another delivery event is appended to the task's durable chain for an event that moved nothing | `sed -n '496,534p' crates/busbar-a2a/src/a2a/pushback.rs` shows the `reported == task.state` branch binding `moved = task` unchanged, and the delivery block testing only `push_callback.is_some()` with no reference to whether a transition occurred. `grep -n… |
| 432 | LOW | `busbar-a2a/src/a2a/pushdeliver.rs:475` | `forget()` -- the only path that clears a task's entry from the process-local map holding the caller's plaintext webhook delivery credential -- sits after the `?` on the transport send, so a CONNECTION-level transport failure (as opposed to the HTTP-status failure covered by the finding above) on a terminal task's final delivery returns early and the credential is never dropped, contradicting the module's own stated… | `forget()` is reachable and genuinely clears both maps on other paths -- called from the push-config delete verb at `local.rs:756`, and `a2a/tests/pushdeliver_tests.rs` asserts the clearing in `a_terminal_delivery_drops_the_credential_as_well_as_the_pin`. That test drives delivery through a… |
| 433 | LOW | `busbar-a2a/src/taskstore.rs:465` ⟂FOLD | After the first retention-sweep abandon failure, every subsequent one is entirely silent for the rest of the process's life -- this error-once latch has no `else` branch, unlike three sibling latches in this same crate (`DISPATCH_UNRECORDED_WARNED`, `PUSH_CALLBACK_UNPERSISTED_WARNED`, `STREAM_OUTCOME_UNRECORDED_WARNED`) which keep emitting at debug after the first occurrence | `grep -n -A 6 'DISPATCH_UNRECORDED_WARNED' crates/busbar-a2a/src/a2a/receive.rs` matches receive.rs:2040-2046, which uses the identical `AtomicBool` swap pattern but adds `} else { diag_debug!(...) }`; the same shape appears again at receive.rs:2059-2065 and :2782-2796. Three in-crate siblings keep… |

## `busbar-kernel-ledger` — 6 items (MEDIUM 3 · LOW 2 · NIT 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 434 | MEDIUM | `busbar-kernel-ledger/src/cost/rate.rs:66` | `nanos_sum`'s headline claim is false at HEAD: three more multiply-and-sum folds exist on the money path, two of them in the same crate, and one of them does not saturate the way the claim says the single fold does | Not a zero-claim. All four sites quoted with file:line; `grep -rn --include='*.rs' 'nanos_sum' crates/` shows the callers that DO route through it (cost/rate.rs:413-418, busbar-kernel-budget/src/price.rs:113, busbar-kernel-budget/src/estimate.rs:94, busbar-kernel/src/cost.rs:200), proving the… |
| 435 | MEDIUM | `busbar-kernel-ledger/src/recompute.rs:606` | The recompute prices a line at the ARCHIVE's sealed tier and treats the line's own tier as the thing that can be forged; the production statement path prices at the LINE's own tier and documents the opposite rule. The only path that would catch a forged tier is the unwired one | Not a zero-claim for the arithmetic; the zero-caller half is covered by finding 1's control (`SealedHistory\|HistoryArchive\|recompute(` matches only the lib.rs re-export, while `totals_as_of` matches production at units_admin/mod.rs:1449 under the identical command) |
| 436 | MEDIUM | `busbar-kernel-ledger/src/usage/settlement.rs:130` | There are two settlement tables. The one the ledger crate ships and documents as 'One table, every end' is unreachable from production; the live one is busbar-kernel/src/teller.rs:372, and the two disagree about what a durability-lost unit bills | ZERO: `grep -rn --include='*.rs' 'UnitEndKind\|usage::settle\|usage::Evidence\|SettleFlag\|requests_settled\|posting_flags' crates/ \| grep -v 'busbar-kernel-ledger/src'` returns no `busbar_kernel_ledger` hit -- the only `requests_settled` matches outside the crate are teller.rs:485/1314 and… |
| 437 | LOW | `busbar-kernel-ledger/src/cost/posting.rs:422` | The posting lookup applies the tier at nano scale and the 'ONE FUNCTION' view lookup applies it at scale 15, so the two can answer different money for the same consumption at any non-neutral tier -- and the test that certifies they agree is pinned to the one tier where they cannot disagree | Not a zero-claim; the arithmetic is hand-derived from checked_apply_tier (posting.rs:291-318) and price_in_view (view.rs:310-315). `grep -rn --include='*.rs' 'with_tier\(' crates/` -> one non-test hit (the setter at view.rs:224), confirming no production caller sets a non-neutral tier on a… |
| 438 | LOW | `busbar-kernel-ledger/src/digest.rs:6` | The digest module states one hash function with exactly one caller as the thing that keeps two preimages from drifting; there are four callers in four different functions, including the seal/verify pair the sentence warns about | `grep -rn 'digest::sha256' crates/busbar-kernel-ledger` returns all five sites including the one inside digest.rs itself, so the needle and the crate path are right |
| 439 | NIT | `busbar-kernel-ledger/src/usage/tests/meter_tests.rs:372` | `the_fold_takes_its_policy_and_its_legs`'s doc opens with an unterminated sentence about 'the short form on the report type', which lines 375-378 then explain was removed -- so the cell's first line documents the thing its body exists to say no longer exists | The very next line of the same doc block (meter_tests.rs:373-378) states the correct claim -- 'The fold takes its policy and its legs, and there is no second way in. There used to be a shorter form that took neither ... Its absence is the point of this cell' -- so the intended text is present and… |

## `busbar-oauth2` — 4 items (MEDIUM 1 · LOW 2 · NIT 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 440 | MEDIUM | `busbar-oauth2/src/tests/flow_tests.rs:887` | The consent screen's only content test asserts the client id (887) and the requested scope (891) with a bare `page.contains`, and both strings are present in the hidden `<input name=return value="...">` that round-trips the whole authorization request -- the exact false green the SAME test strips out, deliberately and with a comment saying why, nine lines later for the redirect-host assertion | The method that works is used on the third assertion of the very same test: flow_tests.rs:900 `let visible = strip_hidden_return(&page);` then line 902 `visible.contains("client.example")`, with a comment at lines 895-899 stating that 'a bare `page.contains(...)` is satisfied by a field the… |
| 441 | LOW | `busbar-oauth2/src/routes.rs:171` | The authorization server's consent screen mints the `busbar-operator` resource-owner session behind `RouteAuth::Admin` alone, and the explicit open-admin posture (`auth.admin_auth: []`) turns that into an anonymous, full-scope route with no boot warning - the analogous warning exists only for `auth.chain: [keys]` | Zero-claim (config_validate's rule body never mentions the AS plane): `grep -c "oauth_as" crates/busbar-kernel/src/config_validate/mod.rs` -> 0. Positive control, identical command against another plane section name in the same file: `grep -c "mcp" crates/busbar-kernel/src/config_validate/mod.rs`… |
| 442 | LOW | `busbar-oauth2/src/routes.rs:289` | The consent screen renders the redirect host as one of the three facts the operator is approving ("Sends the credential to: <host>"), but the staked approval key is only `client_id\u{1f}scope` - the redirect URI is discarded into `_redirect_host` and is never re-checked when the approval is spent | Zero-claim (the approval resolver never reads the redirect URI): `grep -n "redirect_uri" crates/busbar-oauth2/src/consent.rs` -> exactly 1 hit, line 267, inside a comment about `access_denied`; no code reads `request.redirect_uri`. Positive control, same file and same command shape for a field that… |
| 443 | NIT | `busbar-oauth2/src/tests/cimd_tests.rs:309` | `Result::expect_err` takes a plain `&str`, so `{client_id}` is emitted literally; when this seven-entry loop's guard regresses, the panic names no target | The correct form is two lines below, in the same loop body: cimd_tests.rs:310-313 uses `assert!(why.contains(expected), "`{client_id}` was refused, but not as `{expected}`: {why}")`, where the message IS a format string and does interpolate. Same identifier, same scope, one interpolates and one… |

## `busbar-kernel-wal` — 3 items (MEDIUM 3)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 444 | MEDIUM | `busbar-kernel-wal/src/backend.rs:389` | A new segment file is created here with no fsync of the holding directory. `sync_holding_dir` exists in this very file (backend.rs:328) and is called from exactly one place, `DirectoryFactory::new` (backend.rs:363), and only when the DATA DIRECTORY itself was newly created. Every subsequent segment created by `Wal::roll` (wal.rs:513-521) gets its data fsynced by `FileSegment::sync` but its directory entry never is | `grep -rn 'sync_holding_dir' --include='*.rs' crates/ \| grep -v /target/` returns backend.rs:328 (definition) and backend.rs:363 (its single caller, inside `DirectoryFactory::new` under `if fresh`). The same grep form shows the pattern IS applied on the other durable-write path in this tree:… |
| 445 | MEDIUM | `busbar-kernel-wal/src/journal.rs:581` | A fifth chain verifier, in the WAL, seeds its expectation from the first record it is handed, so it is permanently a window walk with no genesis anchor and cannot detect a head truncation - the case the audit family's Anchor::Genesis exists for | ZERO-CLAIM CONTROL for 'verify takes no anchor': its full signature at journal.rs:580 has one parameter. Control that the anchor concept is findable across crate boundaries by the same search: `grep -rn 'Anchor::Genesis' crates` matches crates/busbar-kernel-audit/src/record.rs:498 and… |
| 446 | MEDIUM | `busbar-kernel-wal/src/wal.rs:485` | `Journal::replay()` (journal.rs:960) is the only read-back path, and it delegates to `Wal::read_back()`, which scans ONLY the currently-open segment. Segments roll at `SEGMENT_BYTES = 64 MiB` (segment.rs:27), i.e. every 131,072 frames. So every record written before the first roll -- including the one-and-only `RecordClass::Migration` marker, which by construction is written on the first boot after the upgrade and… | The crate's own test helper proves the distinction: crates/busbar-kernel-wal/src/tests/restart_after_a_roll.rs:52-60 defines `fn whole_log(segments: Vec<Box<dyn SegmentBackend>>)` which loops over EVERY segment calling `scan(&segment)`, with the comment 'which is what "the chain" means once the log… |

## `busbar-substrate-values` — 3 items (MEDIUM 1 · LOW 1 · NIT 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 447 | MEDIUM | `busbar-substrate-values/src/json.rs:12` | json.rs claims ALL body JSON, including the error-envelope path, goes through this crate's single sonic-rs seam and that "only config (YAML) and tests use a JSON library directly" -- but `busbar-llm/src/engine/wire.rs:838` and `busbar-kernel/src/proxy/mod.rs:140` both call `.to_string()` directly on a `serde_json::Value` built by this crate's own `agnostic_error_envelope` (`proxy/mod.rs:388`, via… | `grep -rln "serde_json::\\|sonic_rs::" crates/busbar-substrate-values/src \| grep -v json.rs` lists ~20 files, but nearly all only name the `serde_json::Value` TYPE (consistent with json.rs's own allowance, line 13); narrowing to actual parse/serialize calls surfaces the two real bypasses cited… |
| 448 | LOW | `busbar-substrate-values/src/breaker.rs:199` | The breaker's status ladder ships twice; the documented fix for 'the upstream never answered' landed only in the copy with no live caller, and the copy every plane calls still maps that case to record-nothing | ZERO-CLAIM CONTROL for 'no test covers status 0 on either side': grepping `from_status(0)`, `http_status: 0` and `status: 0` over crates/busbar-kernel-breaker/src/tests/mod.rs and crates/busbar-substrate-values/src/tests/breaker_tests.rs returns nothing; the SAME two files DO match `grep -n… |
| 449 | NIT | `busbar-substrate-values/src/testkit/warn_capture.rs:6` | The duplicated warn-capture fixture's safety argument is that a test binary links only one of the two copies; the kernel's own test-support feature turns both on, so a binary can mint two process-global capture gates - the defect the substrate crate says it already fixed | ZERO-CLAIM CONTROL that both are live modules in live crates and not the deleted testkit crate: crates/busbar-substrate-values/src/lib.rs:114-115 declares `#[cfg(any(test, feature = "test-support"))] pub mod testkit {` and crates/busbar-kernel/src/lib.rs:323-324 declares `#[cfg(any(test, feature =… |

## `busbar-plane-a2a` — 2 items (MEDIUM 2)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 450 | MEDIUM | `busbar-plane-a2a/src/plane.rs:101` | In the busbar-plane-a2a crate's successor `Plane` implementation, `upstream_destination()` resolves the hop target by taking `self.agents().first()` rather than the agent the request actually named, and `approve()` (line 800) picks the `ResourceLocator` the same way -- so with more than one agent configured, every hop-carrying operation would be dialled, scoped and lane-priced against the first configured agent… | `grep -rn --include='*.rs' 'FACT_AGENT_ID' crates/` returns exactly two hits -- its declaration and its listing in `SESSION_FACTS` -- and nothing that sets or reads it. The identical search for its sibling on the same list, `FACT_VERSION`, finds a genuine consumer at plane.rs:557, proving the… |
| 451 | MEDIUM | `busbar-plane-a2a/src/plane.rs:658` | A2aPlane::decode_response (the busbar-plane-a2a crate's generic `Plane` implementation) decides whether a response is the last frame of a streamed exchange by reading `codec.streaming` off `PlaneSessionState`; that flag is only ever written by `encode_egress` when handed `Some(state)`, but the only two production call sites of this seam (busbar-kernel-egress/src/attempt.rs:381, :717) hardcode `None`, so `streaming`… | `grep -rn '\.encode_egress(\\|\.decode_response(' --include='*.rs' crates/ \| grep -v '/tests/'` returns exactly two call sites, both `None`. `grep -rn 'A2aUnits::new' --include='*.rs' crates/` returns exactly one line, under `crates/busbar/src/root/tests/`. Contrasting matches in… |

## `plugin-sdk` — 2 items (MEDIUM 2)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 452 | MEDIUM | `plugin-sdk/src/lib.rs:816` | The hook half of the endpoint ABI has a plugin side and no host side: HookRequest::Routes/Endpoint, HookReply::Routes/Endpoint, HookHandler::routes() and HookHandler::handle_http() all exist and the SDK answers them, but DlopenPolicy never emits either op, so a dropped-in hook's declared HTTP routes are collected by nobody | ZERO-CLAIM: `grep -rn 'HookReply::Routes\|HookReply::Endpoint\|HookRequest::Routes\|HookRequest::Endpoint' crates/ conformance/ examples/` matches ONLY two doc lines in cold/hook.rs (:68, :72) and the two SDK dispatch arms (plugin-sdk/src/lib.rs:816, :817) -- no host-side emitter. POSITIVE CONTROL,… |
| 453 | MEDIUM | `plugin-sdk/src/lib.rs:915` | The #85 observability envelope is documented as 'the ONE response shape, every kind' and the host decoder is genuinely kind-neutral, but drain_observations exists only on ExportHandler -- the other four cold kinds' SDK dispatchers return the bare response and have no trait method on which a plugin could report anything at all | ZERO-CLAIM: `grep -rn 'drain_observations\|into_envelope\|Observations' crates/ --include='*.rs' \| grep -v '/tests/' \| grep -v 'cold/observe.rs'` matches only crates/export-example-plugin/src/lib.rs:53-58, plugin-sdk/src/lib.rs:860/915/967, and one busbar-kernel doc comment -- every one on the… |

## `required-status-checks.md` — 2 items (MEDIUM 2)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 454 | MEDIUM | `.github/required-status-checks.md:11` | The cross-workflow required-status ledger never names `Voice conformance verdict` or `cargo-fuzz (bounded, qa boundary)` - neither in the required tables nor in the 'Intentionally NOT branch-protection-required' section - so a 9-job Voice conformance battery and the fuzz gate can go red on a qa promotion and block nothing | `grep -n 'Voice\\|voice\\|fuzz\\|Fuzz' .github/required-status-checks.md` returns nothing. The identical grep for `'MCP conformance verdict\\|A2A conformance verdict'` returns lines 54 and 55, proving the file, the path and the grep are correct |
| 455 | MEDIUM | `.github/required-status-checks.md:53` | The required context name carries a ` + cargo-audit` suffix that exists only in predev's qa-security.yml; the job that actually reports on main/dev/qa today is named `cargo-deny (advisories · licenses · sources · bans)`, so the required context never appears | The other three names in the same table DO match their jobs verbatim: `MCP conformance verdict` == qa-conformance-mcp.yml's verdict `name:`, `A2A conformance verdict` == qa-conformance-a2a.yml's, `CodeQL (rust)` == qa-codeql.yml:65 - so the comparison method is sound and the cargo-deny row is the… |

## `store-example-plugin` — 2 items (MEDIUM 2)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 456 | MEDIUM | `store-example-plugin/src/lib.rs:574` | The two in-tree reference `Store` implementations disagree at the exact expiry instant for `plane_token_live` (the multi-use push-notification capability check): `store-memory` treats `now == expires_at` as EXPIRED, `store-example-plugin`'s durable `FileStore` treats the identical input as LIVE — and each crate's own test suite locks in its own answer, so nothing catches the drift | Both quoted call sites and both quoted test assertions were read directly from the pinned tree at the line numbers given; `grep -n 'plane_token_live' crates/store-memory/src/lib.rs crates/store-example-plugin/src/lib.rs` returns exactly these two comparison operators as the full set of… |
| 457 | MEDIUM | `store-example-plugin/src/tests/mod.rs:749` | The copy-me plugin template tells contributors the `manifest-allowlist` gate is report-only because the construction-gate job carries `continue-on-error: true`; there is no such key in ci.yml and the job is a blocking required check | The same grep DOES find real YAML keys elsewhere when they exist — e.g. `grep -nE '^\s*needs:' .github/workflows/ci.yml` returns many — so the anchored-key method distinguishes keys from prose |

## `actionlint.yaml` — 1 items (MEDIUM 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 458 | MEDIUM | `.github/actionlint.yaml:15` | actionlint is configured here and is invoked by NO workflow; its only caller is scripts/land.sh, behind `if command -v actionlint`, so the runner-label check this file claims 'says so before it is pushed' never runs in CI at all | `grep -rn actionlint .github/ scripts/ xtask/src` returns only actionlint.yaml's own comments plus scripts/land.sh:697-698. The identical command for a lint that IS wired - `grep -rn release-script-lint .github/workflows/*.yml` - returns ci.yml:337 and ci.yml:339 as `run:` steps, proving the method… |

## `busbar-kernel-breaker` — 1 items (LOW 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 459 | LOW | `busbar-kernel-breaker/src/classify.rs:9` | The breaker classifier's module doc claims it was moved byte-identical from busbar-substrate with exactly two adaptations that change no arithmetic; there is a third adaptation and it changes the arithmetic, plus a fourth in the date parser | ZERO-CLAIM CONTROL: the doc's own enumeration at classify.rs:11-14 lists the &str signature and parse_imf_fixdate and then stops, while reading classify.rs:380-400 shows the third arm present. Control that the file the header names as the source of truth exists and holds the same function:… |

## `busbar-kernel-budget` — 1 items (MEDIUM 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 460 | MEDIUM | `busbar-kernel-budget/src/decide.rs:530` | `Door::record_usage` and `Door::record_usage_unchained` have no caller outside this crate's own tests, so every unit built on the new admission door charges requests at the door and then ledgers nothing — leaving all five token caps and the token half of every budget cap on that stack permanently at zero | Zero-claim: `grep -rn --include="*.rs" '\.record_usage(' crates/ \| grep -v '^crates/busbar-kernel-budget/src/tests/'` leaves only `GovState::record_usage` sites (plane_host/mod.rs:981 plus comments and busbar-core-admin tests) — no `Door::record_usage` caller at all. Control proving the same grep… |

## `busbar-kernel-identity` — 1 items (LOW 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 461 | LOW | `busbar-kernel-identity/src/tests/chain_tests.rs:58` | `test_reject_stops_the_chain` builds a second module that would identify and never reads its call counter; it asserts only that the verdict is Denied, which a chain that consulted every module and merely let the Reject dominate would also produce | The identical property is pinned correctly twenty lines later in the same file, by the same method: chain_tests.rs:77 `let second_calls = second.calls.clone();` and chain_tests.rs:93-98 `assert_eq!(second_calls.load(..), 0, "the chain returns on the first identification; nothing after it is… |

## `busbar-plane-llm` — 1 items (LOW 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 462 | LOW | `busbar-plane-llm/src/dialect.rs:92` | The OpenAI Chat Completions dialect table asserts (comment at line 91) that this dialect 'reports no separate written-to-cache quantity,' but the crate's own sibling codec genuinely reads and separately prices a cache-write quantity for this exact dialect (`usage.prompt_tokens_details.cache_write_tokens`), so this table's cache accounting silently diverges from both the provider's real wire format and busbar's own… | The `bedrock` and `responses` entries in the SAME file DO carry a real, codec-verified `cache_write_pointer` (line 118: `"/usage/cacheWriteInputTokens"`; line 130: `"/usage/input_tokens_details/cache_write_tokens"`), proving the table is capable of stating this field correctly and that the `openai`… |

## `busbar-transport-http` — 1 items (LOW 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 463 | LOW | `busbar-transport-http/tests/no_plane_names.rs:192` ⟂FOLD | In the test whose entire job is to prove the plane/core-name scanner (contains_word) is not vacuous, one assertion's second OR-arm is a compile-time-constant true (a string literal containing its own uppercase substring), so this specific assertion passes even if contains_word were deleted or always returned false. The identical line exists in crates/busbar-transport-grpc/tests/no_plane_names.rs:192 | Every other assertion in the same test function (the_scan_would_catch_a_planted_name, lines 191-207) has no such OR-escape and does correctly exercise contains_word alone -- e.g. `assert!(contains_word("this is the a2a binding", "a2a"));` at line 193 has no fallback and would fail if contains_word… |

## `busbar-transport-stdio` — 1 items (LOW 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 464 | LOW | `busbar-transport-stdio/src/transport.rs:184` | The absolute-path-only guard on child-process spawn (no shell, no relative path, refusing anything that does not start with '/') has zero exercising test anywhere in busbar-transport-stdio's own test files | Grepped both crates/busbar-transport-stdio/src/tests/battery.rs and crates/busbar-transport-stdio/src/tests/mutation_hardening.rs for AddressRefused, starts_with, and relative -- zero hits in either. The sibling crates/busbar-transport-tcp/src/tests/mod.rs:320-323 DOES exercise its own analogous… |

## `loc.toml` — 1 items (LOW 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 465 | LOW | `qa/loc.toml:17` | The comment (lines 17-18) claims only two crate directories disagree with their `package.name`: `crates/secret-ref` and `crates/hooks-ranking`. A full sweep of every `crates/*/Cargo.toml` (`grep -m1 "^name"` per directory, compared to the directory's basename) finds THIRTEEN mismatches, not two: api->busbar-api, auth-admin-tokens->busbar-auth-admin-tokens, auth-static-plugin->busbar-auth-static-plugin,… | The comment's own two named examples (crates/secret-ref -> busbar-secret-ref, crates/hooks-ranking -> busbar-hooks-ranking) ARE confirmed true positives by the same method, showing the check technique is sound; it simply undercounts the population by 11 |

## `plane-purity-strict.toml` — 1 items (MEDIUM 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 466 | MEDIUM | `qa/plane-purity-strict.toml:33` | This comment (part of the `[test-reach]` section's rationale) asserts that `qa/construction.toml` currently carries `ports-only-tests` ceilings of llm=565, mcp=1, a2a=3, voice=0, and claims the two files' figures 'agree to within a single line.' The actual content of qa/construction.toml (`[rules.ports-only-tests.max_per_crate]`, lines 384-388) is `busbar-llm = 0`, `busbar-mcp = 0`, `busbar-a2a = 0`, `busbar-voice =… | `sed -n '384,388p' qa/construction.toml` prints the four `= 0` lines directly, and `cargo xtask gate construction --report` shows `ports-only-tests:busbar-llm`, `:busbar-mcp`, `:busbar-a2a`, `:busbar-voice` all currently PASS at ceiling 0 — confirming the cited 565/1/3/0 figures are not what… |

## `secret-accessor-seal-witness.sh` — 1 items (MEDIUM 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 467 | MEDIUM | `scripts/secret-accessor-seal-witness.sh:13` | The secret-accessor witness justifies itself on a dependency wall around a crate named `busbar-caps`; no such crate exists, the seal lives in `busbar-contract`, and the manifest allow-list explicitly permits every plugin-kind crate to name `busbar-contract` freely | `grep -rn '^name = "busbar-contract"' crates/*/Cargo.toml` resolves, proving the manifest-name method works; `busbar-caps` returns nothing under the identical command |

## `verify-1.6.0-done.sh` — 1 items (MEDIUM 1)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 468 | MEDIUM | `scripts/verify-1.6.0-done.sh:1043` | The release DONE script's audit-coverage group invokes a gate named `audit-ledger` that is not registered, inside an `if` guarded on a file that does not exist — so the group that proves 'nothing open at HIGH/MEDIUM' is silently skipped at release time | `grep -c 'audit-ledger' /tmp/gates.txt` (the extracted REGISTRY names) -> 0, while the same file contains `seal-witness`, `money-invariants` and 48 others, so the extraction is complete and the absence is real |

---

## STRUCK — recorded with reason, not deleted

### ⟂ STRUCK-BY-FOLD — 18 items

They sit in one of the 63 files the fold deletes or merges, so the fold is their fix. They are
struck *by the fold landing*, not by judgement — if the fold changes, they come back.

**17 and 18 are both right, in different scopes.** 17 is the count within the 309-item batch
this heading sits under; the eighteenth is item 553, filed later in the scripts batch. The
document-wide total is 18 and the authoritative id list is in THE WAVES at the end of this file.

These items hold **no slot**. Verified by measurement, not assertion: the intersection of the
18 struck ids with the 394 ids holding a slot is empty, and 18 + 394 = 412 filed.

### ✎ DRIFTED — 26 findings, NOT filed as work

The quote no longer matches the cited line. Per the audit rule a mismatch is demoted to
needs-measurement, never dropped and never promoted: the lead may be real elsewhere in the
file. Each is re-measurable by re-running its lens against the named file.

| lens | where | quote now at | the claim |
|---|---|---|---|
| `r4-ci` | `.github/workflows/ci.yml:3728` | 3797 | `ship-ready` is described as reading a mutation verdict from the GitHub checks API; the gate has four rows, none of them `gate-mutants`, and it reads no API - so the `GH_TOKEN` granted three lines… |
| `r3-a2a` | `crates/busbar-a2a/src/a2a/relay.rs:1221` | GONE | busbar's outbound A2A gRPC client never inspects a `grpc-status` signal at all -- it infers success purely from the HTTP status and requires the body to already carry a complete length-prefixed… |
| `r2-config` | `crates/busbar-kernel/src/config/mod.rs:1983` | GONE | resolve_export() matches export.<name>.module against the closed 4-entry EXPORT_MODULES list (sections.rs:435-439) with no fallback arm to the plugin registry, so a kind:export plugin can never be… |
| `r2-codec` | `crates/busbar-llm-codec/src/anthropic/mod.rs:567` | GONE | Anthropic's streaming block-index DoS hardening clamps any index above 1023 down to 1023 instead of rejecting it, and at least the Gemini egress writer treats a repeated index as "the same block… |
| `r2-codec` | `crates/busbar-llm-codec/src/anthropic/reader.rs:732` | GONE | The primary chat-completion usage read, for all six dialects, reads input/output token counts via read_count_u64(..).unwrap_or(0) instead of the crate's own usage_count::billed_count, so a… |
| `r3-codec` | `crates/busbar-llm-codec/src/anthropic/reader.rs:471` | GONE | A streaming Anthropic content_block_start for a tool_use block missing id or name makes the whole event silently vanish (the `?` bails the match to None, and read_response_events turns None into an… |
| `r3-codec` | `crates/busbar-llm-codec/src/anthropic/reader.rs:12` | GONE | recover_truncated_usage -- the last-chance usage-salvage path for a response too large to fit the reassembly buffer, wired directly into production billing… |
| `r3-codec` | `crates/busbar-llm-codec/src/bedrock/handler.rs:531` | GONE | The Titan-embeddings response reader collapses 'absent' and 'present-but-unreadable' inputTextTokenCount into the same None (via read_count_u64, not the crate's own usage_count::billed_count), and… |
| `r3-codec` | `crates/busbar-llm-codec/src/bedrock/reader.rs:837` | GONE | Bedrock's contentBlockStart{toolUse} arm unconditionally (re-)opens and emits a BlockStart for the index with no check that it is already open, unlike its sibling arms in the same match block… |
| `r3-codec` | `crates/busbar-llm-codec/src/bedrock/reader.rs:844` | GONE | A streaming Bedrock toolUse block with an absent/blank toolUseId silently becomes id "" and is still emitted as a normal BlockStart (and the buffered response reader at reader.rs:1296-1298 makes the… |
| `r3-codec` | `crates/busbar-llm-codec/src/cohere/mod.rs:623` | GONE | state.open_tools/state.tool_ir_index are, by explicit design, never shrunk for a Cohere stream's lifetime (mod.rs comment: 'Neither ... is ever shrunk for the stream's lifetime'), so… |
| `r3-codec` | `crates/busbar-llm-codec/src/cohere/reader.rs:1157` | GONE | The buffered read_response only checks that the `message` KEY is present, never that its VALUE is an object. Cohere's own documented error-response shape is `{"message": "<string>"}` -- a top-level… |
| `r3-codec` | `crates/busbar-llm-codec/src/cohere/reader.rs:1241` | GONE | Cohere's BUFFERED read_response maps a `finish_reason: "ERROR"` body straight into `stop_reason: Some(IrStopReason::Error)` inside a normal `Ok(IrResponse{...})` (built at line 1345) with no… |
| `r3-codec` | `crates/busbar-llm-codec/src/gemini/reader.rs:1319` | GONE | When a Gemini streaming candidate's finishReason is MALFORMED_FUNCTION_CALL (the model emitted an unparseable tool call -- per gemini/mod.rs:1268's own doc comment, 'generation FAILED, there is NO… |
| `r2-codec` | `crates/busbar-llm-codec/src/ir/rerank.rs:97` | GONE | RerankResp.search_units is correctly read off the wire from Cohere's meta.billed_units.search_units (and shared by Bedrock rerank), but RerankResp::billing() -- the one method both dialects' rerank… |
| `r3-codec` | `crates/busbar-llm-codec/src/openai_chat/reader.rs:850` | GONE | serde_json::Value::as_u64() returns None for a negative number, a non-integer float, or an absent index key; all three collapse via map_or(0, ...) to oai_idx = 0 -- the SAME slot a legitimate,… |
| `r3-codec` | `crates/busbar-llm-codec/src/openai_responses/reader.rs:868` | GONE | Reasoning (Thinking) and function-call (ToolUse) items share ONE open_tools key namespace keyed on raw output_index (only text gets a disjoint TEXT_INDEX_KEY_OFFSET). The reasoning-delta arm's guard… |
| `r3-codec` | `crates/busbar-llm-codec/src/proto_stream.rs:767` | GONE | Inside the Bedrock event-stream drain loop, a validly length/CRC-framed event whose JSON payload fails to parse is silently skipped via a bare `continue` -- no error frame, no abort, no metric, no… |
| `r3-codec` | `crates/busbar-llm-codec/src/wire_shim.rs:103` | GONE | tier_usage() -- the function every dialect's usage funnels through before the ledger/metering write -- collapses a cache-token field that is None purely because an upstream read_count_u64 call… |
| `r3-mcp` | `crates/busbar-mcp/src/mcp/client/peer.rs:312` | GONE | On the stdio client transport, EVERY server-initiated authority ask (sampling/createMessage, elicitation/create, roots/list) that the operator has granted is unconditionally answered `Unsatisfiable`… |
| `r4-mcp-planes.backup1` | `crates/busbar-mcp/src/mcp/stdio_serve.rs:714` | 715 | busbar's own stdio-server session's `resources/subscribe` ceiling check (`MAX_RESOURCE_SUBS = 256`) reads the subscription-set size and releases the lock (stdio_serve.rs:715-718) before the matching… |
| `r4-transport-egress` | `crates/busbar-transport-ws/src/transport.rs:0` | GONE | The 'a transport crate must never name a plane or the kernel core' architecture test exists in only 2 of the 7 sibling transport crates in this slice (busbar-transport-http and… |
| `r4-qa-registers` | `qa/design-bindings.json:2785` | GONE | PB-58's note (a money-guarantee binding: admitted units are never ended for money, overspend is carried as an overdraft) closes with a citation to… |
| `r4-qa-registers` | `qa/kind-isolation.toml:2888` | 2894,2904,2924 | This [[dep]] row (busbar-transport-grpc -> busbar-transport-http, and its twin at line 2948 for busbar-transport-sse -> busbar-transport-http) declares verdict = "allowed" citing a… |
| `r4-qa-registers` | `qa/kind-isolation.toml:2428` | GONE | `busbar-admin` was folded into `busbar-core-admin` (xtask/src/gates/kind_isolation.rs:3169: '`busbar-admin` folded into `busbar-core-admin` under #37'); `crates/busbar-admin` does not exist in this… |
| `r4-qa-registers` | `qa/kind-isolation.toml:2718` | GONE | `busbar-plugin-sign` was folded whole into `busbar-plugin-loader` by DECISIONS #33 (crates/plugin-loader/src/lib.rs:45: 'The former `busbar-plugin-sign` crate, folded in whole (DECISIONS #33)';… |

### ∅ FILE GONE — 1

| lens | path | the claim |
|---|---|---|
| `r4-qa-registers` | `qa/audit-ledger.json` | `qa/audit-ledger.json` is the register this very 1.6.0 audit's round-by-round ledger is supposed to live in (xtask/src/audit.rs, REGISTER_REL at line 49; paired with docs/design/AUDIT-STATUS.md,… |

---

# THE BACKLOG, FILED — items 469–565 (the scripts round)


97 further findings, landed after items 160–468 were filed and deduped against them:
**83 from the release-scripts lens, 14 from the kernel tick lens.** Same contract as
above — every quote machine-verified against `e1bdc6b80`, second column is the positive
control, exit test owed at fix time.

**Why this batch is different.** It is the first lens to audit `scripts/` — 109 files,
43,884 LOC — and the release engine had never been read. Its denominator is stated and
high: 93 of 109 read line-by-line. The lens ran the scripts rather than reading them,
so several rows below are reproduced failures, not inferences.


**Totals: HIGH 18 · MEDIUM 50 · LOW 18 · NIT 11**


## `scripts/ — the release engine` — 83 items (HIGH 17 · MEDIUM 45 · LOW 13 · NIT 8)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 469 | HIGH | `scripts/a2a-subject/boot.sh:1026` | assert_tck_number's only failing condition is a non-empty `unwaived` list (FAIL-status MUSTs outside the pin). `not_tested` is computed at :999, printed at :1006-1011, and gated on nowhere -- so a TCK run in which every MUST degrades to NOT TESTED exits 0 | The gate is alive for the shape it was written against: flipping one fixture status from 'NOT TESTED' to 'FAIL' makes the same code exit 1 with 'busbar failed 1 MUST requirement(s) outside the pinned waiver set'. :938 states the policy in prose -- '`NOT TESTED` requirements are reported, never… |
| 470 | HIGH | `scripts/branch-symbol-sweep.py:1021` | The default trap symbols are string literals in scripts/branch-symbol-sweep.py itself, which is a tracked non-doc file, so build_haystack indexes them into trunk_hay.code -- the one set the T3 case (:1023) requires them to be ABSENT from | grep -rl 'RecordSink' crates/ xtask/ scripts/ -> scripts/branch-symbol-sweep.py ONLY; grep -rn 'sink_key' crates/ xtask/ -> 0 hits. All three symbols exist in the tree solely inside this script's own fixtures. POSITIVE CONTROL: is_doc('docs/design/1.6.0-branch-sweep.md') -> True,… |
| 471 | HIGH | `scripts/cost-watch.py:660` | `known` excludes every job whose runner label priced to None (:607), so an unrecognised runner contributes $0 to the number the exit code bands on; unknown_job_count is computed at :722, printed, and compared to nothing | The sibling risk IS floored elsewhere in the same slice: ci-images.py:205 refuses on `if len(upstreams) < 2` and verify-artifact.py:1210 raises 'Zero rows is not a pass'. grep -n 'unknown_job_count\\|unknown_labels' scripts/cost-watch.py -> construction and printing only, no comparison anywhere |
| 472 | HIGH | `scripts/cost-watch.py:870` | The per-run job fetch catches RuntimeError, appends to fetch_errors and continues; fetch_errors is printed and compared to nothing, and main() returns the money band (:890) rather than the tool-failure code the file's own contract reserves | The intended shape exists 20 lines above: the fetch_repo_is_private call site at :852-856 does `except RuntimeError: ... return 4`. grep -n 'fetch_errors' scripts/cost-watch.py -> construction and printing only; never compared. --selftest has no fixture for either swallowed path |
| 473 | HIGH | `scripts/executable-config-lint.py:171` | crates/busbar-core/ was absorbed into crates/busbar-kernel/, so snapshot_root_keys() finds no file and returns None (:186), assert_key_mirror() returns [] (:200) = 'the mirror holds', and the anti-rot guard is dead in the only tree where it can run. Measured against the real snapshot the mirror has already rotted by one key | python3: os.path.isfile('crates/busbar-core/src/config/config-schema.snapshot.json') -> False; os.path.isfile('crates/busbar-kernel/src/config/config-schema.snapshot.json') -> True (positive control, same call); set(snapshot DeployCfg fields) - CONFIG_ROOT_KEYS -> ['decisions'] |
| 474 | HIGH | `scripts/mcp-fixture-absence-gate.sh:295` | Two defects on one line. (a) The real gate keeps only underscore names, discarding 37 of the 43 identifiers the file's headline widening exists to cover. (b) The floor's `die` runs inside a process substitution -- a CHILD -- so the parent reads zero lines and both axes loop over an empty array | grep -rhoE '"test[-_][A-Za-z0-9_-]+"' crates/*/src \| tr -d '"' \| sort -u \| wc -l -> 43; the same pipeline \| grep -cE '^test_' -> 6. grep -n 'load_forbidden_set' scripts/mcp-fixture-absence-gate.sh -> :123 (definition) and :392 (inside run_selftest) only, never run_gate |
| 475 | HIGH | `scripts/plane-keys.sh:130` | The shell neutral-crate list is 3 roots; the repo's own authority (xtask/src/planes.rs::neutral_src_roots) is 21, all 21 on disk -- so the two shell meters that consume it scan 220 of 427 neutral files and print 0 for needles that are non-zero | awk over xtask/src/planes.rs:116-153 \| grep -c '"crates/' -> 21; `wc -w` on NEUTRAL_ROOTS_LIST -> 3; every one of the 21 passes `[ -d ]`. And xtask/tests/infra.rs:481-484 does assert_eq!(planes::neutral_src_roots(), shell_neutral) where shell_neutral is parsed out of this very line (infra.rs:470)… |
| 476 | HIGH | `scripts/proof-manifest.py:147` | The byte-identity verdict runs `cargo test -p busbar-llm --lib translate_parity`, and busbar-llm contains zero files mentioning translate_parity -- all of them are in busbar-llm-codec. cargo exits 0 on an empty filter, so golden_status is set to 'pass' | grep -rl translate_parity crates/busbar-llm/ \| wc -l -> 0; grep -rl translate_parity crates/busbar-llm-codec/ \| wc -l -> 5 (positive control: the identical command finds the tests one crate over). grep -n GOLDEN_CRATE scripts/proof-manifest.py -> line 114 only, the definition |
| 477 | HIGH | `scripts/proof-manifest.py:840` | `shutil` is used at lines 692, 707 and 840 and is imported nowhere, so the release proof collator raises NameError before it computes a single verdict whenever --hits-dir is omitted | grep -n '^import ' scripts/proof-manifest.py -> argparse, atexit, hashlib, json, os, re, subprocess, sys, tempfile (no shutil). ast-parse confirms: shutil imported=False, shutil referenced=True, tempfile imported=True (control) |
| 478 | HIGH | `scripts/proof-manifest.py:870` | mark_sources() (:551) implements 'no evidence, no pass' and refuses to fan one job result out across a sub-map, but it is called only from the unreachable selftest(); main() re-implements marking at :865-875 with neither guard | ls on the five oracle paths: crossproto_delivery_billing_tests.rs MISSING, crates/busbar-core/src/ingress/tests/tests.rs MISSING, crates/busbar-llm/src/engine/tests/on_exhausted_tests.rs EXISTS (positive control, same ls, same list). grep -n 'mark_sources' -> definition at 551 and calls only inside… |
| 479 | HIGH | `scripts/reap-worktrees.sh:401` | The cold reaper samples the CANDIDATE's CPU time twice (:385, :390) and then kill -9's all of its CHILDREN, which were never sampled. An orphaned shell parked waiting on a live build is indistinguishable from a stalled one | The sampling loop at :384-393 reads `ps -o time=` for $p in cold_candidates only; :401's `pgrep -P "$p"` is the first and only mention of the children, and no `ps -o time=` is ever taken for them. POSITIVE CONTROL that the file knows the correct shape: the hot rule at :216-217 samples $cpu for the… |
| 480 | HIGH | `scripts/release-check-1.5.2.sh:859` | Phase B's POST /auth/token round-trip and Phase C's admin read-only-vs-full authorization matrix (:1432) are both behind BUSBAR_1_5_2_RUN_OIDC_BOOT, which nothing in the repository ever sets -- and this script never calls record_phase_skip, so the parent's gap accounting cannot see it | grep -rn 'BUSBAR_1_5_2_RUN_OIDC_BOOT' over the whole tree -> 5 hits, ALL FIVE inside scripts/release-check-1.5.2.sh itself (:725, :859, :910, :1432, :1470); no workflow, no wrapper. POSITIVE CONTROL, same grep shape on a var that IS wired externally: BUSBAR_RELEASE_CHECK_REQUIRE_SIBLINGS ->… |
| 481 | HIGH | `scripts/release-check.sh:631` | print_verdict computes gaps only from statuses that were written; it never diffs PHASE_RUN_IDS against all_phase_ids / SELECTED_PHASES, so a run in which ZERO phases executed prints a clean PASS and exits 0 -- even under --require-siblings | Same harness, one status flipped to a gap: '=== RELEASE GATE INCOMPLETE ... 1 PHASE(S) DID NOT RUN ===', rc 1 -- the accounting works on the inputs it was designed for; it is the absence of input it cannot see. The sibling gate one directory over states the opposite principle by name:… |
| 482 | HIGH | `scripts/release-gate/gate.sh:234` | The release gate has no RUNTIME floor on how many ids the contract owes: GATE_EXPECTED_FLOOR_DEFAULT is read only inside selftest() (:68, inside the function defined at :35 and closed at :230), and GATE_EXPECTED_FLOOR -- the name the comment and ledger-selftest.sh use -- has zero readers anywhere | grep -rn 'GATE_EXPECTED_FLOOR' scripts/ -> 4 hits: gate.sh:68 and :234 (both _DEFAULT, inside selftest), ledger-selftest.sh:95 (comment) and :105 (the setter). Zero readers of GATE_EXPECTED_FLOOR. POSITIVE CONTROL, same grep shape on a variable that IS read: grep -rn 'SKIP_ALLOWED'… |
| 483 | HIGH | `scripts/release-gate/gate.sh:288` | Line 282 filters $ALL down to rows whose version equals $VERSION into $MINE, :288 assigns ALL="$MINE", and :314 then asks ledger_foreign_rows "$ALL" for rows whose version differs -- which is empty by construction. The WRONG RELEASE refusal at :316 (the only `exit 1` for a ledger about another release) can never fire; the surviving signal at :286 is a GitHub ::error:: annotation with no exit, which does not fail a… | grep -n 'WRONG RELEASE' scripts/release-gate/*.sh -> gate.sh:316 (the unreachable echo) plus ledger-selftest.sh:192 and :204 (the two assertions on it). Running the selftest prints 19 [ok] lines and 2 [FAILED] lines and 'release-gate ledger selftest: FAILED'. grep -rn 'ledger-selftest'… |
| 484 | HIGH | `scripts/secret-hygiene-gate.sh:1361` | The secret-hygiene gate is the only gate in the plane/secret slice with no require_roots / require_files floor: when prod_files returns empty, all three checks are skipped by their `[ -n "$files" ] &&` guards, the totals are 0+0+0 and the gate prints its green PASS line | sed -n '262p' scripts/plane-grep-gate.sh -> 'red "plane-grep gate: FAIL -- the $label group scanned $n production .rs file(s); zero is RED"'. Same shape exists in plane-noun-gate.sh:154 and plane-config-noun-gate.sh:193. grep -n 'require_roots\\|require_files\\|zero is RED'… |
| 485 | HIGH | `scripts/verify-1.6.0-done.sh:365` | assert_bless_env_empty declares itself to be 'EVERY VARIABLE THAT CAN MAKE A COMPARISON COMPARE NOTHING' (:319) but omits the two variables bin/oracle reads to choose the JUDGE binary -- BUSBAR_ORACLE_RUST_BIN (bin/oracle:50) and BUSBAR_RELEASE_CHECKOUT (bin/oracle:57) | grep -n 'BUSBAR_ORACLE_RUST_BIN\\|BUSBAR_RELEASE_CHECKOUT' scripts/verify-1.6.0-done.sh -> rc=1, zero hits. POSITIVE CONTROL, identical command, a var that IS in the list: grep -n 'BUSBAR_ORACLE_TOOL_DIR' scripts/verify-1.6.0-done.sh -> :345, :365, :586. And grep -n on bin/oracle -> :50 and :57… |
| 486 | MEDIUM | `scripts/a2a-subject/h2-route-failover.sh:68` | The leg's header (:6-9) and its PASS string claim per-attempt 502s and 'an exact Retry-After'. Neither is asserted anywhere: statuses is accumulated at :42 and read only inside the failure text at :49, no 502 is ever compared, and h2_call discards all response headers | grep -n '502\\|Retry-After' scripts/a2a-subject/h2-route-failover.sh -> :7, :9, :64, :68 only -- all comments plus the PASS string, no comparison. POSITIVE CONTROL that the grep finds real assertions when they exist: the same grep for 'UNSUPPORTED_OPERATION' matches :53, a real `case` arm |
| 487 | MEDIUM | `scripts/build-provenance-gate.sh:123` | Every release-expecting reject fixture in --selftest carries opt-level=3, so the opt-level comparison can be deleted outright and the self-test still passes clean -- the exact bundled-fixture defect scripts/profile-lock.sh:100-105 documents having already fixed in its sibling | Mutated a copy (`if [ "$opt" != "3" ]` -> `if false`) into a temp tree symlinked to the real crates/ and Cargo.toml: control selftest exit=0, mutant selftest exit=0 -- identical green. POSITIVE CONTROL: every other release rule (pgo, debug-assertions, lto=thin, lto=off, lto absent, profile-table… |
| 488 | MEDIUM | `scripts/ci-branch-protection.sh:119` | Two of the four contexts this script writes onto qa and main -- 'ship-ready' and 'construction gate (how the tree is built vs ARCHITECTURE.md -- BLOCKING, on its posture)' -- do not exist as job names in ci.yml on dev, qa or main, and promote.sh treats a context that never reported as a refusal | MEASURED: for b in dev qa main predev; do git show "origin/$b:.github/workflows/ci.yml" \| grep -cE 'name: ship-ready\|name: construction gate'; done -> dev 0, qa 0, main 0, predev 2. POSITIVE CONTROL: the same grep on the pinned checkout's own .github/workflows/ci.yml -> 2, so the pattern does… |
| 489 | MEDIUM | `scripts/ci-runners-down.sh:87` | Under --all the sweep deletes EVERY offline runner registration in the org, with no name, label or instance filter -- and the org pool explicitly serves sibling repos, not just busbar's fleet | grep -n 'select(.status==' scripts/ci-runners-down.sh -> :87, one select, with no conjunct on .name or .labels. POSITIVE CONTROL that a narrower filter exists in the same file: the else branch at :100 calls sweep_ghost_runners, described at :79-82 as only removing a registration whose INSTANCE EC2… |
| 490 | MEDIUM | `scripts/ci-runners-selftest.sh:54` | Case B is three source greps, none of which looks at the exit-status handling -- reintroducing the exact `\|\| true` the file was written for leaves the selftest fully GREEN | Reproduced by copying ci-runners-reconcile.sh, applying that one sed, and re-running case B's three greps verbatim -> ok / ok / ok. POSITIVE CONTROL: case C in the same file plants a real condition in a real code path and does red |
| 491 | MEDIUM | `scripts/ci-runners-selftest.sh:86` | Case D asserts 'the registration never prints the token' but runs only with CI_RUNNER_DRY_RUN=1, and the dry branch of register_agents returns before `token` is ever assigned -- so the assertion cannot fail no matter what the live path does | POSITIVE CONTROL: case C in the same file plants a real empty-token condition and does go red, so the harness can fail; it is this case's subject that is absent |
| 492 | MEDIUM | `scripts/ci-service-endpoints.sh:35` | The Go template ranges over EVERY network with no separator, so a container on two networks yields two concatenated IPs -- non-empty, so the `[ -n "$ip" ]` guard passes and a garbage URL is written to $GITHUB_ENV | grep -n 'range' scripts/ci-service-endpoints.sh -> :35 only; no printf separator, no len check, no single-network assertion anywhere. POSITIVE CONTROL that the file CAN hard-fail a bad lookup: :46-50 exit 1 and dump {{json .NetworkSettings.Networks}} for the empty case |
| 493 | MEDIUM | `scripts/documented-claims-check.py:55` | The claim ids are line addresses into docs/design/inventory/1.5.5-ops-observability.md and the docstring at :22-25 says 'a dropped claim is a hole in the run' -- but check() only validates the register's internal shape and never opens the document. Every one of the 56 ids has drifted by one | awk 'NR>=1046 && NR<=1049' on the doc vs the register's quotes for README:1047 and README:1061; the offset is +1 on every claim that matches at all. POSITIVE CONTROL that the register is readable and the quotes are real: grep -n 'Six wire protocols, first class on both sides'… |
| 494 | MEDIUM | `scripts/gate-mutants.sh:241` | gm_verdict inspects cargo-mutants' exit status only for the value 4; every other non-zero status is ignored and the verdict is read entirely from result-file CONTENTS, so a shard that died mid-run with empty result files is GREEN. n_caught is computed at :224 and compared to nothing | Ran gm_verdict (extracted verbatim) against a fixture of four empty result files across every plausible tool rc: rc=0 -> 0, rc=1 -> 0, rc=2 -> 0, rc=3 -> 0, rc=5 -> 0, rc=4 -> 1. POSITIVE CONTROL: rc=4 is the one value it does refuse, so the function is alive. The selftest's fixture list (:428-445)… |
| 495 | MEDIUM | `scripts/history-rewrite-empty-bodies.sh:435` | The only assertion that a non-listed commit was left alone tests object-database EXISTENCE, which survives any rewrite -- the orphaned original is still in the ODB, including in the dry run, which explicitly writes rebuilt objects into it | Reproduced in a throwaway repo: commit, capture $C1, `git commit --amend`, then `git cat-file -e "$C1"` -> rc 0 SUCCESS, while `git rev-list main \| grep -q "$C1"` -> NOT on main. Same repo, same object, two different answers; the selftest asks the one that cannot say no |
| 496 | MEDIUM | `scripts/land-remote.sh:185` | The comment eight lines above says a missing .result file 'is worse than a red', and the failure branch prints a WARNING without touching RC, so the run exits 0 | POSITIVE CONTROL: twenty lines down the landed-tip branch escalates the identical shape correctly -- :206-209 print the same style of WARNING and then `[ "$RC" = 0 ] && RC=2`. grep -n 'RC=2' scripts/land-remote.sh shows the landed-tip failure sets it; the .result failure branch does not |
| 497 | MEDIUM | `scripts/mcp-conformance.sh:11` | battery_subject -- the leg that judges busbar -- ends on an exit code and nothing else: no report-existence check, no executed-test floor, no stale-report deletion. Its sibling battery_control, twenty lines above, has all three and says in a comment why it does not trust the callee | grep -nE 'assert_covered\|report\|-f \|len\(' over mcp-conformance.sh:418-568 matches only comment lines; the same grep over :380-416 matches :396 (report path), :397 (rm -f), :401 ([ -f ] \|\| die) and :409 (len(ran) < 20). grep -n 'rm -f\\|rm -rf' testing/mcp-conformance/scripts/run-subject.sh ->… |
| 498 | MEDIUM | `scripts/mcp-subject/h2-card-epoch.sh:100` | The leg's decisive dated-card money assertion is written in the negated `if` form, which fails OPEN on a non-integer read; its own siblings at :74 and :86 use the `[ ... ] \|\| { fail }` form, which fails closed on the identical input. Identical line in scripts/a2a-subject/h2-card-epoch.sh:103 | Ran both forms in /bin/bash with no redirection: `if [ "" -ne 8 ]` -> stderr '[: : integer expression expected' and VERDICT PASS; `[ "" -eq 1 ] \|\| { f=1; }` -> failures=1. Same shell, same bad data, opposite outcomes, purely because of the if/\|\| choice |
| 499 | MEDIUM | `scripts/mcp-subject/h2-class-price.sh:94` | Money check (2) -- 'THE PLANE MUST REPORT COUNTS' -- passes silently whenever h2_meter_row_quantity returns anything non-integer, because `[ "" -le 0 ]` exits 2 and the redirection swallows the diagnostic. Identical line in scripts/a2a-subject/h2-class-price.sh:94 | POSITIVE CONTROL, same construct in the same shell: quantity='-' and quantity='0' both set failures=1, so the check is alive for the two shapes it was written against and blind to every other |
| 500 | MEDIUM | `scripts/mcp-subject/h2-unpriced-refuses.sh:128` | ARM 2 of the money-sacred #42 switch records a failure only on an exact 200; every other outcome, including curl's 000 for 'no connection', is silently accepted as a compliant refusal. Same shape in scripts/a2a-subject/h2-unpriced-refuses.sh:127-131 | POSITIVE CONTROL: ARM 1 in the same file uses the strict form at :109 -- `if [ "$off_status" != "200" ]; then failures=...` -- which DOES catch 000, boot-failed, and every other non-200. The two arms of one leg use opposite polarities and only the permissive one guards the money-sacred branch |
| 501 | MEDIUM | `scripts/no-plugins-gate.sh:630` | The assertion that the two axes are genuinely different binaries has no else arm -- an empty or absent build.txt skips it in silence -- while the probe-vector block directly above (:623-625) correctly fails closed | sed -n '620,627p' shows the probe-vector guard's else arm: fail "A7: one or both axes produced no probe vector (an earlier assertion failed hard)". sed -n '630,639p' shows the build-block guard ending at `fi` with no else. Same [ -s ... ] premise, opposite failure posture, ten lines apart |
| 502 | MEDIUM | `scripts/pgo-drift-check.sh:531` | When every production-hot symbol is classified instrumentation-invisible, MISS_SHARE is 0 and the gate exits 0 -- the file names this exact case at :490-494 and fixes only the WORDING of the message above it | grep -n 'INVIS_COUNT' scripts/pgo-drift-check.sh -> :473 (write) then :487, :495, :496, :503 -- all reads are echo/if for PRINTING; none feeds DRIFTED. POSITIVE CONTROL: grep -n 'DRIFTED' -> :511, :516, :528, :530; the only two things that can set it are OVERLAP_PCT and MISS_SHARE |
| 503 | MEDIUM | `scripts/plane-delete-test.sh:234` | make_scratch is invoked only as `s="$(make_scratch)"`, so the SCRATCHES append runs in a subshell and the parent's variable is always empty; the EXIT/INT/TERM/HUP trap's `for d in $SCRATCHES` iterates an empty list forever | bash -c 'S=""; f(){ S="$S /tmp/x"; echo /tmp/x; }; s="$(f)"; echo "s=$s SCRATCHES=[$S]"' -> s=/tmp/x SCRATCHES=[] . grep -n 'make_scratch' scripts/plane-delete-test.sh -> every call site is of the form s="$(make_scratch)" |
| 504 | MEDIUM | `scripts/plane-delete-test.sh:244` | The 'strong form' removes crates/busbar-<P> and never crates/busbar-<P>-codec, while scripts/plane-keys.sh:80-103 defines a plane as BOTH crates since the codec split and warns that omitting one means 'the bulk of a plane stops being scanned'. The verdict text carries no such qualification | grep -n 'crates/busbar-llm-codec\\|crates/busbar-voice-codec' Cargo.toml -> both are still workspace members after the simulated deletion. POSITIVE CONTROL that the script CAN remove a directory: remove_crate_dir is what makes leg 2 red when the bin still depends on the plane |
| 505 | MEDIUM | `scripts/plane-delete-test.sh:1038` | Leg 1b -- the --all-targets compile of the contract plane crate -- is guarded by a bare -d test with no else, no note and no counter, and crates/busbar-plane-voice does not exist, so the leg silently does nothing for 1 of the 4 planes the gate reports PASS for | ls -d crates/busbar-plane-* -> busbar-plane-a2a, busbar-plane-decision, busbar-plane-llm, busbar-plane-mcp, busbar-plane-streaming. POSITIVE CONTROL, same test: `[ -d crates/busbar-plane-llm ]` succeeds, `[ -d crates/busbar-plane-voice ]` fails, and PLANE_KEYS (plane-keys.sh:46) is 'llm mcp a2a… |
| 506 | MEDIUM | `scripts/plane-grep-gate.sh:271` | The exclusion requires a literal underscore before 'tests'. This repo's module-style test files are named tests.rs, so they are scanned as production and counted as debt -- while the header at :39 says test code is excluded, naming '*_test(s).rs' | PLANE_GREP_HITS_OUT=/tmp/h.txt bash scripts/plane-grep-gate.sh --report >/dev/null; wc -l </tmp/h.txt -> 271; awk -F'\t' '{split($2,a,":");print a[1]}' /tmp/h.txt \| grep -c '/tests\.rs$' -> 102. POSITIVE CONTROL that the pattern CAN exclude: a file named foo_tests.rs is dropped by the same grep |
| 507 | MEDIUM | `scripts/plane-noun-gate.sh:72` | This gate hand-adds crates/busbar-plugin/src on the stated ground (:66-68) that 'the plugin ABI is the surface a third-party plugin compiles against, so an LLM noun frozen into it is the same leak' -- and omits crates/busbar-contract/src, which is that same surface and carries 14 real leak lines the meter reports as zero | PLANE_NOUN_NEUTRAL_ROOTS='crates/busbar-contract/src' PLANE_NOUN_HITS_OUT=/tmp/c.txt bash scripts/plane-noun-gate.sh --report >/dev/null; wc -l </tmp/c.txt -> 14, first lines crates/busbar-contract/src/config.rs:44 and records.rs:645. POSITIVE CONTROL: the default run over the same script yields… |
| 508 | MEDIUM | `scripts/plugin-ci-refs.sh:87` | resolve() passes any 40-hex string through untouched (:47) and :77 hard-fails anything that is not 40-hex, so pin_sha is never empty: the 'a pin busbar does not have is a release that cannot build' hard fail at :90 is unreachable | Driven with a stubbed resolver: a 40-hex pin busbar does not have emits the matrix JSON and exits 0; the non-hex fixture emits :78's message. grep -c 'which busbar does not have' scripts/plugin-ci-refs.sh -> 1 (source only, never printed) |
| 509 | MEDIUM | `scripts/plugin-registry-check.sh:89` | The selftest exits 0 here on the happy path, so the 54-line check-2 fail-injection block at :90-143 never runs; and on the unhappy path its verdict cannot change the outcome either, so it is inert in both directions | RAN IT on the pinned tree: `bash scripts/plugin-registry-check.sh --selftest` prints only check 5's four [ok] lines and then :89's message; the :99 header 'plugin-registry-check selftest (check 2, the registry-driven sibling loop)' and the longer :142 success message never appear. The :142 message… |
| 510 | MEDIUM | `scripts/plugin-registry-check.sh:365` | The reverse org sweep reads a single page of at most 100 repos with no --paginate, while the floor it is checked against (ORG_REPO_FLOOR at :363) is len(plugins)+len(excluded) = 11 -- so a truncated answer is indistinguishable from a complete one | The floor is computable from the tree: grep -c ' - repo:' plugins.yaml -> 10, excluded_repos holds 1, so ORG_REPO_FLOOR = 11. Any single page of >=11 clears it and a page can carry 100. POSITIVE CONTROL: the selftest's CASE 3/4 stubs return 41 and 40 repos -- both above the floor and both well… |
| 511 | MEDIUM | `scripts/pr-land.sh:241` | --wait's GREEN verdict is rendered against the hardcoded PR_LAND_CHECKS default at :39, never against branch protection, so the list can silently drift and 'every required context' stops meaning it | grep -n 'protection\\|required_status' scripts/pr-land.sh -> :10, :33, :36, all inside comments, no code. POSITIVE CONTROL, same grep on the sibling: scripts/promote.sh:144 -> `gh api "repos/${REPO}/branches/${TO}/protection/required_status_checks"`, live code, with a header that says 'never a… |
| 512 | MEDIUM | `scripts/pr-queue.sh:185` | --dry-run discards the lander's refusal with `\|\| true` and then counts the line as `opened`, so a rehearsal always reports 'N PR(s) opened, 0 red' and exits 0 even when every line would refuse | grep -n 'dry' scripts/pr-queue-selftest.sh -> NO MATCH; the selftest's seven cases never invoke --dry-run. POSITIVE CONTROL, same grep on the sibling: grep -n 'dry' scripts/pr-land-selftest.sh -> 5 hits including 'case E -- --dry-run touches nothing' |
| 513 | MEDIUM | `scripts/preflight.sh:116` | Line 2 claims preflight 'Mirrors .github/workflows/ci.yml EXACTLY so a tag can never ship red CI' and :17 'Exit 0 only if every mirrored job is green', but three legs -- the windows job, cargo-deny, and the local audit -- print a warning glyph and leave fail=0 | grep -n 'fail=1' scripts/preflight.sh -> :41, :58, :83, :99, :114 only. Neither the warning at :86-88 (cargo-deny absent), :102 (audit unset), :116 nor :120-121 sets it. POSITIVE CONTROL that the pattern CAN set it: :114 sets fail=1 inside the same if/elif/else chain as :116 |
| 514 | MEDIUM | `scripts/proof-manifest.py:226` | Three evidence scripts named by the neutrality verdict (:226, :242, :278) were converted to `cargo xtask gate ...` and deleted, so evidence_present is permanently false for three gates that actually ran and passed | ls scripts/plane-purity-lint.sh scripts/g6-freeze-witness.sh scripts/plane-abi-neutrality.sh -> all three MISSING. POSITIVE CONTROL, same ls over the other four evidence names in the same functions: scripts/plane-grep-gate.sh, scripts/no-plugins-gate.sh, scripts/plane-delete-test.sh,… |
| 515 | MEDIUM | `scripts/proof-manifest.py:653` | The 125-line battery whose docstring reads 'Prove the manifest cannot publish a claim with nothing behind it' is wired to no CLI flag and called by nothing -- which is why the three other proof-manifest findings above all shipped | grep -n 'add_argument' scripts/proof-manifest.py \| grep -i selftest -> rc=1, no match. POSITIVE CONTROL, identical command on the sibling script: grep -n 'add_argument.*selftest' scripts/verify-artifact.py -> :1144. grep -rn 'proof-manifest.py --selftest' .github/ -> no match |
| 516 | MEDIUM | `scripts/proto-deletion-gate.sh:146` | The level-1 needles come from six hardcoded globs, so a protocol/plane crate whose name matches none of them is not a needle -- contradicting the comment's claim that a crate added or renamed by the next split is covered without an edit here | Reproducing the gate's own derivation yields 11 needles (busbar_a2a, busbar_llm, busbar_llm_codec, busbar_mcp, busbar_plane_a2a, busbar_plane_decision, busbar_plane_llm, busbar_plane_mcp, busbar_plane_streaming, busbar_voice, busbar_voice_codec). On disk and matching NO glob: crates/busbar-oauth2,… |
| 517 | MEDIUM | `scripts/proto-deletion-gate.sh:552` | The set of dialects level 3a proves refused is a hand-typed string; nothing derives it from busbar-llm-codec's DECLS and nothing counts it, yet the gate's closing line asserts 'all six dialects it carries are refused' | The source of truth exists and is machine-readable: crates/busbar-llm-codec/src/lib.rs's DECLS carries exactly six `&...::DECL` entries. grep -n 'DECLS' scripts/proto-deletion-gate.sh -> no match; the only tree-derived list in the file is proto_crate_names at :144, which feeds level 1's needles,… |
| 518 | MEDIUM | `scripts/proto-deletion-gate.sh:832` | The only arm is 404); every other value -- including 000, which curl prints on connection refused or an empty reply -- is accepted as 'the door mounted and routes', so the mounted-vs-absent discriminator can be satisfied by a dead process | POSITIVE CONTROL: the strict form exists elsewhere in the same file, so the author knew the difference -- MCP_META_OFF uses `[ "$MCP_META_OFF" = "404" ] \|\| die` and mcp-d uses `[ "$MCP_D_MCP" = "404" ] \|\| die`. Those reject 000; :832 and :516 do not |
| 519 | MEDIUM | `scripts/prove-remote.sh:152` | The shadow-oracle leg -- one of the eight the header promises -- is skipped on an [ -x ] test, and the verdict five lines later is an unconditional 'PROVE-REMOTE: GREEN' | git ls-files -s bin/oracle -> 100755 (healthy today, so the hazard is latent). POSITIVE CONTROL that mode loss is the live class in this very tree: git ls-files -s scripts/*.sh \| awk '$1!="100755"' -> scripts/plane-keys.sh and scripts/plane-roots.sh are tracked 100644. preflight.sh:47 solved the… |
| 520 | MEDIUM | `scripts/qa-gate-run.sh:454` | The `loader` job exists to run the loader-mechanism tests against the real sibling-built sqlite cdylib (:38), but a missing or unclonable sibling is a GitHub ::warning:: -- which does not fail a job -- and the scoped loader test then runs against whatever fallback it finds | POSITIVE CONTROL: the hard-fail form is used freely in this same file for less consequential gaps -- cmd_matrix dies on 'qa-segments.sh --list returned an EMPTY feed', cmd_fast dies when the live-mock slots cannot be enumerated, cmd_siblings dies on 'empty registry feed', cmd_hydrate dies on a… |
| 521 | MEDIUM | `scripts/qa-segments.sh:252` | run_all iterates feed rows and re-looks-up each by id, so two rows sharing an id both resolve to the FIRST row's run command; the second segment's command never executes, yet both count toward `executed` and both print PASS | Executed the real script against two fixture manifests whose second segment's run command was identical in both: with ids ('dup','dup') the output is 'PASS dup / PASS dup / COVERED ...: dup, dup / qa-gate umbrella: GREEN' and the second segment's sentinel file is NOT created; POSITIVE CONTROL with… |
| 522 | MEDIUM | `scripts/reap-worktrees.sh:143` | The comment two lines above says 'every column the `load:` line reports is counted, by the same live() helper' -- but the load: line reports five columns and this guard sums three, omitting tool-bins and product | sed -n '481,487p' lists five live() columns (rustc, cargo, test-bins, tool-bins, product); sed -n '140,143p' computes three. POSITIVE CONTROL: the tool-bins pattern '/(debug\|release)/[a-z_-]+$' exists in the file and is used for the load: line, and is assigned to no counter feeding n_busy |
| 523 | MEDIUM | `scripts/reap-worktrees.sh:487` | The comment block at :466-478 says 'every one of those counters COUNTED MENTIONS, NOT PROCESSES ... The fix is to match the EXECUTABLE, not the sentence' and names pgrep -fc as the rejected instrument -- yet the product column still uses pgrep -fc | grep -n 'pgrep -fc' scripts/reap-worktrees.sh -> :487 only. POSITIVE CONTROL that the fix WAS applied elsewhere: sed -n '479p' shows the other four columns going through awk '{print $1}' \| grep -cE, i.e. field 1 = executable |
| 524 | MEDIUM | `scripts/release-script-lint.sh:422` | If the glob matches nothing, rules 1 and 3 print 'nothing to scan' and leave fail=0 -- the exact vacuous-scan hole this same file refuses for rule 4 fifty lines later | POSITIVE CONTROL: rule 4 treats the identical input as RED and says why at :474-476 -- 'A count of zero is not a clean scan; it is a scan that did not happen' -- and has a dedicated selftest fixture at :241-263. No equivalent floor or fixture exists for rules 1, 2 or 3. The glob currently matches 3… |
| 525 | MEDIUM | `scripts/rooms.sh:33` | The sweep-denominator tool defaults its SUBJECT repository to a hard-coded absolute path inside one developer's home directory, so running it from any checkout computes the denominator of a different tree unless REPO is set | The same class is measured live elsewhere in the slice by the repo's own gate: `python3 scripts/public-hygiene-lint.py --root .` reports '[machine-path] a developer home directory -- 4 hit(s)' naming scripts/branch-symbol-sweep.py:1171, scripts/reap-worktrees.sh:208 and… |
| 526 | MEDIUM | `scripts/secret-accessor-seal-witness.sh:110` | The gate refuses an empty CRATE list at :105 but declares an empty FILE list a green PASS -- the exact condition every sibling in this slice calls RED. It also prints crate names and no file count, so a partial scan and a full one are indistinguishable | POSITIVE CONTROL, the same condition treated as RED by three siblings: plane-grep-gate.sh:262, plane-noun-gate.sh:154, plane-config-noun-gate.sh:193 all print FAIL and exit non-zero on a zero-file scan. `bash scripts/secret-accessor-seal-witness.sh --check` lists 6 crates and prints no file count… |
| 527 | MEDIUM | `scripts/snapshot-refs.sh:19` | The backup script's REPOS array hard-codes two absolute paths under a named developer's home directory, with no environment override -- and the done-oracle's PUBLIC-HYGIENE group requires public-hygiene-lint.py to report ZERO hits over a scope that explicitly includes scripts/ | public-hygiene-lint.py:52 states the scope ('What ships or is publicly visible: source and its comments, `scripts/`, .github/, docs/') and :334-340 is the machine-path rule with an allowlist for runner/Shared. POSITIVE CONTROL that the rule discriminates: /Users/runner/... and /home/runner/... are… |
| 528 | MEDIUM | `scripts/snapshot-refs.sh:60` | The tag that anchors the `git stash create` commit against gc is created with its status discarded, and the next line prints 'uncommitted work preserved as tag ...' unconditionally | Reproduced in a throwaway repo: `git tag snapshot HEAD` then `git tag -f "snapshot/uncommitted-2026-09-23-1830" HEAD >/dev/null 2>&1` -> rc 128, no output. POSITIVE CONTROL, same command after `git tag -d snapshot` -> rc 0 |
| 529 | MEDIUM | `scripts/verify-1.6.0-done.sh:669` | When qa/full-gate.toml is absent the BUILD group falls through (:668) to three plain `cargo build` invocations -- no clippy, no test tier -- and still reports GREEN, prints the unqualified 'busbar 1.6.0 is DONE' banner and exits 0. That is the exact downgrade --fast was demoted to PROVISIONAL / exit 3 for | sed -n '665p;668,671p' scripts/verify-1.6.0-done.sh shows the elif guard and the three-build else arm; sed -n '202,207p' shows the --fast arm returning 3 with a PROVISIONAL banner, which the fall-through does not. (Also recorded, unfixed, at docs/design/1.6.0-instrument-audit.md row 8.) |
| 530 | MEDIUM | `scripts/verify-1.6.0-done.sh:827` | The CONFORMANCE group is titled '... + voice legs =ready' (:815) and the header repeats it (:25), but the only voice step runs --selftest, whose final real-leg check returns 0 for a battery in which every leg is LEG_STATUS=pending | sed -n '263,268p' testing/voice-conformance/voice-conformance.sh shows the zero-ready branch containing only `say` calls and no die/return; sed -n '389p' shows the stdout being discarded. POSITIVE CONTROL that the function CAN refuse: :256-261 die on problems>0 and conformance_fail>0. grep -n… |
| 531 | LOW | `scripts/branch-symbol-sweep.py:629` | `rows` is capped at MAX_ABSENT_IN_ROW = 500; the money loop two lines above deliberately continues past the cap with a tail loop, the docs-only count does not | POSITIVE CONTROL: the money counter two lines above at :624 is the correct shape (`for n in absent[MAX_ABSENT_IN_ROW:]`), proving the author knew the cap needed a tail loop and wrote one for only one of the two metrics |
| 532 | LOW | `scripts/ci-images.py:87` | consumer_state() returns 'upstream' when ci.yml does not exist, and the --consumer-state branch of main() never calls collect(), so the 'is ci.yml missing?' problem is never raised on that path | POSITIVE CONTROL: collect() at :102-104 handles the same missing file correctly, appending '%s is missing, so its service-container pins cannot be derived.', and main()'s default path at :385 returns 1 on it. The refusal exists; the --consumer-state branch routes around it |
| 533 | LOW | `scripts/extract-inline-tests.py:489` | `refused` is accumulated, printed under 'REFUSED (a block this tool can SEE but will not move -- each needs a human)', and then main() returns 0 regardless; targets() silently yields nothing for a path that is neither a file nor a directory | POSITIVE CONTROL: the same invocation against a real crate path prints non-zero movable lines, so the walk works; it is the missing-path case that is indistinguishable from the clean one |
| 534 | LOW | `scripts/mcp-conformance.sh:367` | The official-subject leg's failure baseline is picked up on a bare -f test with no size, count, or reason check, and assert_covered proves only that scenarios EXECUTED, never that any passed | ls qa/mcp-conformance-baseline.yml -> No such file (positive control: ls qa/teller-steps.json -> exists, same command). grep -n 'baseline' scripts/mcp-conformance.sh -> :366, :367, :371, :376 only; no size, count or schema assertion anywhere |
| 535 | LOW | `scripts/method-inventory.py:536` | build() constructs cells with nested `for m ... for transport ... for role in ROLES`, so every (protocol, method, transport) key necessarily collects exactly set(ROLES); the check asserts a property of its own loop and can never report otherwise | POSITIVE CONTROL: the genuinely discriminating cases in the same selftest (:501-519) mutate the INPUT and assert a refusal; this one does not touch the input at all |
| 536 | LOW | `scripts/public-hygiene-lint.py:488` | The skip assumes long lines are machine output; docs/admin-api.md:199 is 4,292 characters of hand-written markdown table prose in the operator-facing API document | awk 'length($0)>4000 {print FILENAME":"NR}' docs/admin-api.md -> docs/admin-api.md:199. POSITIVE CONTROL: the adjacent table rows 198 and 200 are under the limit and ARE scanned; the generated openapi.json the docstring also names has a max line length of 3,417 and is therefore fully scanned |
| 537 | LOW | `scripts/qa-gate-run.sh:396` | A sibling directory that already exists is reported 'already present' and reused -- its branch, commit and freshness are never checked, which bypasses the entire dev-not-main policy the forty lines above it establish | POSITIVE CONTROL: the same function checks the ref carefully on every other path -- the clone arm at :398 passes --branch "${ref}" and the fallback arm at :400 warns explicitly that a default branch 'may predate the core change this gate is testing'. Only the reuse arm makes no ref claim at all |
| 538 | LOW | `scripts/release-check-1.5.2.sh:448` | The one check that claims to prove the fetched plugin actually LOADED is unfalsifiable twice over: `loaded` is a substring of `downloaded`, which :442 has already required to be in the log, and even if it did fail, :449 only emits a note | printf 'plugins.fetch: downloaded + verified\n' \| grep -Eiq "validated\|loaded\|hooktest\|busbar-hook-test" -> rc 0. POSITIVE CONTROL that the pattern CAN fail: printf 'plugins.fetch: staged artifact ok\n' \| same grep -> rc 1 |
| 539 | LOW | `scripts/release-check.sh:207` | setup_fail is called at :207 but defined at :527, and the ERR trap is not installed until :542, so the one guard against an empty registry dies as a bare 'command not found' with exit 127 instead of the HARNESS/ENVIRONMENT verdict and exit 78 | bash -c 'set -eEuo pipefail; trap "echo TRAP-FIRED rc=$?" ERR; X=""; [ -n "$X" ] \|\| setup_fail "msg"; echo REACHED' -> 'setup_fail: command not found', TRAP-FIRED rc=127, REACHED never printed. POSITIVE CONTROL with the function defined: rc 78 and the [SETUP] banner |
| 540 | LOW | `scripts/release-check.sh:1085` | The poll loop's own fallback (`\|\| echo '{}'` at :1084) feeds jq an object with no .lanes, which is a jq ERROR rather than an empty result; under `set -eEuo pipefail` that kills the command substitution and fires the ERR trap | echo '{}' \| jq -r '.lanes[] \| select(.model=="slow-model") \| .at_capacity' -> 'jq: error (at <stdin>:1): Cannot iterate over null (null)', rc 5. POSITIVE CONTROL: echo '{"lanes":[]}' \| same jq -> rc 0 and empty output, which is the behaviour the loop assumes |
| 541 | LOW | `scripts/release-check.sh:1382` | Three phase ids are hardcoded (phase-1-sqlite-binary, phase-5-smoke-headroom, phase-5-smoke-webrequest) while the registry derives them from the plugins.yaml ALIAS (:222), so one alias rename makes every segmented run skip the phase silently | grep -n 'phase-1-sqlite-binary\\|phase-5-smoke-headroom\\|phase-5-smoke-webrequest' scripts/release-check.sh -> 13 hits, all literal, none inside a ${...} expansion. POSITIVE CONTROL, same grep for the suite family: grep -n 'phase-2-suite-\${' -> 5 hits, derived on both sides, which is why that… |
| 542 | LOW | `scripts/release-gate/lib.sh:135` | digest_matches, staged_asset_sha256, staged_image_digest and staged_compat_digest are called only by gate.sh --selftest; the production checks hand-roll the same lookup with an incompatible convention (lib.sh requires STAGED_RECORD to be a PATH; the workflow and both checks treat it as JSON CONTENT) | grep -rn 'staged_image_digest\\|staged_compat_digest\\|digest_matches' scripts/ -> every call site is gate.sh:100-185 (the selftest) or the definition in lib.sh; zero in docker-checks.sh / platform-checks.sh / channel-checks.sh / fleet-checks.sh, which instead carry their own `printf '%s'… |
| 543 | LOW | `scripts/verify-artifact.py:1116` | coverage_main() returns 1 on a gap, but no workflow calls --coverage; it appears in .github/ only inside comments, so the only refusal in the file is unreachable | grep -rn 'verify-artifact.py' .github/workflows/ -> three real invocations (ci.yml --selftest, release-stage.yml x2) and two comment-only mentions of --coverage. POSITIVE CONTROL: the sibling flag --selftest IS wired, so the wiring mechanism exists and this flag was left out |
| 544 | NIT | `scripts/a2a-subject/boot.sh:547` | The first pid is concatenated to SUBJECT_PIDS without a separator, unlike every other append in the file | grep -n 'SUBJECT_PIDS' scripts/a2a-subject/boot.sh -> :443, :547, :564, :593, :715, :789. POSITIVE CONTROL: :564, :593 and :715 all use the correct `SUBJECT_PIDS="$SUBJECT_PIDS $!"` with a space; :547 is the only one without |
| 545 | NIT | `scripts/gate-mutants.sh:28` | --scope exits 2 when the base ref does not resolve, and the selftest asserts exactly that; the USAGE line still promises 'exit 0 always' | grep -n 'refuses the SCOPE too' scripts/gate-mutants.sh -> :367, a selftest case pinning rc 2 for the case the header says is always 0 |
| 546 | NIT | `scripts/mcp-subject/boot.sh:1` | This 1,186-line file declares no `set` line at all, while its sibling scripts/a2a-subject/boot.sh:149 and its only caller scripts/mcp-conformance.sh:78 both set -euo pipefail | grep -c '^set ' scripts/mcp-subject/boot.sh -> 0. POSITIVE CONTROL, identical pattern: scripts/mcp-conformance.sh:78, scripts/a2a-subject/boot.sh:149, scripts/mcp-subject/h2-lib.sh:20 and scripts/mcp-subject/client-arm.sh:42 all match. (Also recorded in-tree as X-1527.) |
| 547 | NIT | `scripts/mcp-subject/h2-lib.sh:363` | h2_verdict returns a status; it does not exit. The a2a twin at scripts/a2a-subject/h2-lib.sh:359-363 is the same code with no comment, so the lie is mcp-only | sed -n '364,368p' scripts/mcp-subject/h2-lib.sh shows the body is `printf ...; [ "$outcome" = PASS ]` -- a return, not an exit. POSITIVE CONTROL: every h2_verdict FAIL outside a final if block carries its own `; exit 1` (e.g. scripts/mcp-subject/h2-meter-row.sh:52), which is exactly the workaround… |
| 548 | NIT | `scripts/plane-config-noun-gate.sh:96` | The 'CANDIDATES CONSIDERED AND REJECTED' block (:87-106) -- the entire defence of CORE_ROOTS being one directory -- names three crates that do not exist under those names: busbar-core-oauth2 (the tree has busbar-oauth2), busbar-core-hooks, and busbar-admin (the tree has busbar-core-admin) | ls -d crates/busbar-core-oauth2 crates/busbar-core-hooks crates/busbar-admin -> all three 'No such file'. POSITIVE CONTROL, same ls: crates/busbar-oauth2 and crates/busbar-core-admin both exist |
| 549 | NIT | `scripts/plane-roots.sh:7` | 235 lines, two functions and six RED/GREEN self-test cases, and nothing in the tree sources the file or calls either function -- while :59 claims 'plane_roots_selftest (below) is what exercises this' | grep -rn 'plane_roots_resolve\\|plane_roots_selftest' scripts xtask -> hits only inside scripts/plane-roots.sh itself. POSITIVE CONTROL, same grep shape on a shell function that IS sourced across files: grep -rn 'plane_ondisk_key' scripts -> plane-keys.sh (definition) and verify-1.6.0-done.sh:757… |
| 550 | NIT | `scripts/release-script-lint.sh:402` | The failure count is a literal 0, printed before the fail check, so a failing selftest reports '0 failed' on the line immediately above 'SELF-TEST FAILED' | grep -n 'fail=1' scripts/release-script-lint.sh -> 10 hits, all `fail=1`, never `fail=$((fail+1))`; grep -n 'pass=$((pass+1))' -> 10 hits, a real counter. The two halves of the tally are not symmetric |
| 551 | NIT | `scripts/verify-1.6.0-done.sh:95` | `--help` prints lines 2-66 of the file, and the FLAGS block runs from line 72 to line 79 -- so the help output never mentions --fast, --selftest, or the rule that a --fast run is PROVISIONAL and exits 3 | sed -n '2,66p' scripts/verify-1.6.0-done.sh ends at the LOCKED EXCLUSION paragraph; sed -n '72,79p' is the '# FLAGS:' block. POSITIVE CONTROL: the help range DOES reach the group list at lines 9-58, so the range is not empty, only short |

## `busbar-kernel` — 14 items (HIGH 1 · MEDIUM 5 · LOW 5 · NIT 3)

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 552 | HIGH | `busbar-kernel/src/state.rs:1004` | `AppHandle::swap`'s doc says it re-spawns the health probers on every swap and that doing it there makes it impossible to forget; the body retires the host the probers hold a Weak to and delegates the re-spawn to a `PlaneDecl::on_swap` hook that the plane owning the probers does not implement -- so the first admin mutation kills active health probing for the life of the process | `grep -rn --include='*.rs' 'spawn_probers' crates/` returns exactly ONE production call site -- crates/busbar/src/main.rs:1343 `busbar_llm::spawn_probers(&boot_host);`, boot only -- with the rest classified as: 1 definition (health.rs:137), 1 re-export (busbar-llm/src/lib.rs:291), 4 doc/comment… |
| 553 | MEDIUM | `busbar-kernel/src/audit_ring.rs:17` ⟂FOLD | `AuditLog::record_by` is documented twice as the ONE place an admin mutation is recorded, but a second production path -- `JournalHost::audit_emit` -- reaches the same durable admin chain from 27 plane call sites without entering `record_by`, the in-process ring, or `record_by`'s `&'static str` outcome constraint | COUNTS, CLASSIFIED. `grep -rn --include='*.rs' '\.audit_emit(' crates/` returns 27 hits, ALL production plane code and none under a tests path: busbar-a2a/src/a2a/receive.rs x9 (:1216,1233,1342,1429,1476,1533,1691,1716,2093), busbar-a2a/src/a2a/route.rs:297, busbar-mcp/src/mcp/tasks.rs:694,… |
| 554 | MEDIUM | `busbar-kernel/src/calllog.rs:625` | Restore seeds the per-principal chain position from the DECODABLE records only, so a principal whose tail rows will not decode has its chain reopened below the store's real tail -- the forked log the same file and its journal both name as the one outcome to avoid | Not a zero-claim: every step is a read of live code. The two forbidding statements are at calllog.rs:829-831 and audit/journal.rs:363-365, and the seed is at audit/journal.rs:350-358, all quoted above from the pinned tree. `grep -n 'fn seed_position' crates/busbar-kernel/src/audit/journal.rs`… |
| 555 | MEDIUM | `busbar-kernel/src/calllog.rs:935` | `emit_hostless` is a second production per-call emitter whose own doc claims parity with `emit`, but it raises a durable-write failure only at debug -- never at error, with no latch and no success line -- so on the MCP client-leg path an operator at normal log levels sees nothing when per-call evidence is being lost | `grep -rn --include='*.rs' 'emit_hostless\\|call_log_emit_hostless' crates/` returns 12 hits, classified: 2 production definitions/impls (calllog.rs:927, plane_host/mod.rs:736), 1 production CALLER through the seam (busbar-mcp/src/mcp/client/issue.rs:105, inside `pub(crate) async fn issue`, not… |
| 556 | MEDIUM | `busbar-kernel/src/observe.rs:266` | The hook-kind early return silences `fold_diagnostics` as well as `fold_metrics`, but the thirteen-line justification above it argues exclusively about metrics, and no second path renders a hook's diagnostics anywhere | `grep -c 'diagnostic' crates/busbar-kernel/src/hooks/scrape.rs` returns 0 -- the module the justification names as the hook kind's alternative path has no notion of a diagnostic. Positive control under the identical command form: `grep -c 'metric' crates/busbar-kernel/src/hooks/scrape.rs` returns… |
| 557 | MEDIUM | `busbar-kernel/src/pump.rs:201` | One-shot permits are taken inside `dispatch` with no production release, no `#[must_use]` handle and no `Drop` -- unlike the nested-pool permit in the same file -- and exhaustion answers `Wait` on a frame that has nothing left to read, so the frame is neither served, refused, dropped nor counted | `grep -rn --include='*.rs' 'start_one_shot\\|finish_one_shot' crates/` returns 4 hits, fully classified: definitions at pump.rs:145 and pump.rs:164 (0 production callers for the release), ONE production call of the acquire at pump.rs:198 (inside `Scheduler::dispatch`), and ONE test call of the… |
| 558 | LOW | `busbar-kernel/src/ratelimit.rs:71` | `CONFIG_CLASS_RULES` is documented as the single source of truth that nothing else decides class membership against, but three further deciders run ahead of it inside its own classifier, and a second production table of the same name in a second crate is separately live on the admin-verb path | `grep -rn --include='*.rs' 'classify_mutation\\|MutationClass::for_verb\\|CONFIG_CLASS_RULES' crates/ \| grep -v '_tests\.rs\\|/tests/'` returns both live classifiers with their production drivers: the kernel one at busbar-kernel/src/ratelimit.rs:84/108, driven from… |
| 559 | LOW | `busbar-kernel/src/registry.rs:11` | The registry's generation story -- and the `UnitCtx.generation` the loop pins because of it -- has no production driver: both mutators are test-only, `entries` is push-only with no removal path, and the tree's real config reload never touches the registry at all | `grep -rn --include='*.rs' -E 'registry\.(replace\|retire\|count)\(' crates/` matches ONLY busbar-kernel/tests/registry_and_claims.rs:{293,304,326,332,334} and busbar/src/root/tests/registry.rs:{78,81} -- all tests, 0 production. The identical grep form for the constructors, `grep -rn… |
| 560 | LOW | `busbar-kernel/src/router.rs:214` | The 413-reshape doc says the axum body-limit response is matched by byte-equality against the marker; the implementation matches a substring, and the constant's own doc says byte-equality is precisely what was broken | Not a zero-claim: both sides are direct reads of the pinned file. `sed -n '248,254p' crates/busbar-kernel/src/router.rs` shows the `.windows(...).any(...)` substring match; `sed -n '88,92p'` shows the constant's 'SUBSTRING, NOT BYTE-EQUALITY' doc. The live-stack test the fix doc names is real:… |
| 561 | LOW | `busbar-kernel/src/router.rs:269` | `build_router`'s doc says production calls `build_router_with_limits`; it has zero production callers, and the very next doc block twenty lines below says the opposite | `grep -rn --include='*.rs' 'build_router_with_limits' crates/ \| grep -v '_tests\.rs\\|/tests/\\|test_support'` returns only: two `lib.rs` re-export lines (369/373/375), the definition (router.rs:293), the doc line quoted here (router.rs:269), and the internal call from the test-only `build_router`… |
| 562 | LOW | `busbar-kernel/src/state.rs:167` | `App::client_settings` is written at both construction sites and read at none, and its doc explains it as the input to a reuse decision that moved in-plane, against an `App::client` field the struct does not have | `grep -rn --include='*.rs' 'client_settings' crates/` returns 3 references to the `App` field: state.rs:167 (declaration), appbuild.rs:1729 (write), test_support/mod.rs:1895 (write). Reads: 0. Every other hit is the unrelated `ClientSettings` / `ClientSettingsInput` types in busbar/src/root and… |
| 563 | NIT | `busbar-kernel/src/catalogue.rs:5` | `catalogue.rs`'s header describes the module as a glob re-export shim over a relocated catalogue; the file contains no `pub use` at all -- the code was merged back inline and the header was not updated | `grep -n 'pub use' crates/busbar-kernel/src/catalogue.rs` exits 1 with no matches -- not a glob, not a named re-export, nothing. Positive control under the identical command form: `grep -n 'pub use' crates/busbar-kernel/src/state.rs` returns 2 hits (lines 28 and 74), so the grep does find `pub use`… |
| 564 | NIT | `busbar-kernel/src/router.rs:812` | The eleven-line inbound-concurrency doc block runs straight into `project_auth_scope_caps`'s own doc with no item between them, so all of it binds to the wrong function and `apply_inbound_concurrency_limit` ships undocumented | Not a zero-claim: `sed -n '801,845p' crates/busbar-kernel/src/router.rs` shows the contiguous doc lines 801-824 followed by `pub(crate) fn project_auth_scope_caps(` at :825 and `pub(crate) fn apply_inbound_concurrency_limit(` at :839 with no doc line between :825's body and :839 |
| 565 | NIT | `busbar-kernel/src/state.rs:256` | The doc block written for `hook_registry` runs into the one written for `hook_env` with no field between them, so both bind to `hook_env` and `hook_registry` -- read by the admin hooks surface and by six resolve facades -- documents nothing | Not a zero-claim: `sed -n '243,258p' crates/busbar-kernel/src/state.rs` shows the two doc blocks running contiguously into `pub hook_env` at :255 and the undocumented `pub hook_registry` at :256 |

## THE BACKLOG, FILED — items 566–571 (final sweep)

The last six, swept out of the two lenses still writing when the audit loop was closed.

| # | sev | where | the defect | positive control |
|---|---|---|---|---|
| 566 | MEDIUM | `busbar-kernel/src/telemetry.rs:113` | `busbar_plane_request_duration_seconds` can never receive a sample for the voice plane: the voice front door emits the counter itself and marks the response Counted, and the marker makes the core boundary skip `request_finished`, which is the only production caller of the duration recorder | `grep -rn --include='*.rs' 'record_plane_request_duration\|PLANE_REQUEST_DURATION' crates/` (extended-regex form) returns one production caller (telemetry.rs:294), the definition and constant (metrics.rs:267, :829), three doc/emit-site mentions inside metrics.rs, and two test references… |
| 567 | MEDIUM | `busbar-kernel/src/telemetry.rs:683` | The per-thread telemetry bank registry is append-only with no removal path, and banked emission is reachable from tokio blocking-pool threads, so a gateway doing bursty large cross-protocol requests retains one ThreadBank per reaped blocking thread for the process lifetime and pays O(threads-that-ever-emitted) on every flush | `grep -nE '\.retain\(\|\.remove\(\|\.clear\(\)\|\.pop\(\)\|\.drain\(' crates/busbar-kernel/src/telemetry.rs` returns NO matches -- there is no removal path of any shape in the file. Positive control under the identical command form: `grep -rn --include='*.rs' '\.retain(' crates/busbar-kernel/src`… |
| 568 | MEDIUM | `busbar-mcp/src/mcp/connect.rs:73` | client/jsonrpc.rs:362 documents the outbound JSON-RPC id space as 'ONE JSON-RPC id space, split into two ranges that CANNOT MEET', backed by a compile-time assertion (jsonrpc.rs:407, `const _: () = assert!(HANDSHAKE_REQUEST_ID > dispatch_request_id(u32::MAX));`) that relates ONLY the handshake id to the dispatch range -- but a THIRD id source, connect.rs's `REFRESH_REQUEST_ID = 1`, is absent from that accounting… | `grep -rn "REFRESH_REQUEST_ID" crates/busbar-mcp/src` finds exactly the three lines in connect.rs (the definition plus its two uses building and correlating the refresh request) and nothing anywhere relating it to `dispatch_request_id` -- in the SAME file (jsonrpc.rs) that DOES carry a compile-time… |
| 569 | LOW | `busbar-kernel/src/endpoints.rs:25` | `/stats`'s doc says an empty allowed-scopes list means "all pools"; the frozen governance primitive says an explicit empty list is NO scopes at all and names the opposite reading as fail-open. The code is correct; only the doc points the wrong way | Not a zero-claim: both halves are direct reads of the pinned tree -- `sed -n '22,40p' crates/busbar-kernel/src/endpoints.rs` for the header and the implementation, `sed -n '385,412p' crates/busbar-contract/src/records.rs` for `scope_allowed`'s frozen doc and its `None => true, Some(list) =>… |
| 570 | LOW | `busbar-kernel/src/observability.rs:485` | The "OTLP tracing enabled" info line is gated on the endpoint being configured, not on a provider having been installed, so it is printed verbatim after the exporter failed to build and no spans will ever be exported | Not a zero-claim: `sed -n '455,460p;478,490p;805,815p' crates/busbar-kernel/src/observability.rs` shows the three regions -- the `and_then(build_otlp)` build at :457, the endpoint-gated info line at :481-485, and `build_otlp`'s `return None` failure arm at :809-812 -- in the pinned tree |
| 571 | LOW | `busbar-kernel/src/profile.rs:137` | The profiler's bucket array is sized by a hand-written literal whose only guard is a comment, and `record` indexes it with the enum's position unchecked, so adding a 23rd Stage panics the first sample taken under a profiling run | `grep -rn --include='*.rs' 'STAGE_COUNT' crates/` returns exactly 2 hits, both in profile.rs: the definition at :137 and the single use at :219. No test names it, and no `const _: () = assert!(...)` references it. Positive control under the identical command form: `grep -rn 'BUCKET_CAP' crates/`… |

---

# THE WAVES — 394 items, 100 slots, and the partition PROVED


A **slot** is one agent's work. A **wave** is every slot in a phase; they run simultaneously.
**Phases run in order.**

## The partition, proved rather than asserted (Law 8)

**One DIRECTORY = one slot.** A directory's `mod.rs` therefore has exactly one owner, so the
slot that adds an exit test can declare its new module without racing a neighbour. A directory
larger than 14 items splits **by file**, and the split is declared in the table — the first
sub-slot owns the directory's shared module files.

**The phase is a property of the DIRECTORY, not the item.** A directory travels whole, in the
earliest phase any of its items demands. The earlier attempt assigned phase per item, which put
one directory in two phases and handed one `mod.rs` to two slots — measured, 13 files collided.

Re-measured after the fix:

| check | result |
|---|---|
| work items placed | **394 of 394** |
| duplicate item ids | **0** |
| files claimed by more than one slot | **0** |
| directories split without a declared owner | **0** |
| largest slot | **14 items** |

## Model per slot

| model | assigned when | slots |
|---|---|--:|
| **opus** | the slot touches **money** or **the oracle**, or carries any **HIGH** | 67 |
| **sonnet** | worst severity is **MEDIUM** | 19 |
| **haiku** | all **LOW / NIT** | 14 |

**Money is matched on the defect statement, not only the path.** The path tokens
(`ledger`, `cost`, `budget`, `price`, `meter`, `settle`, `hold`, `audit`) miss read-time money —
an item that changes what a customer is charged while living in a file whose name says nothing
about money. The text match adds *billed · invoice · charged · priced · rate-card · spend ·
token-count · metered · overdraft · refund · settles · unpriced*. That reclassification moved
**56 items** into the money tier, including three that the path-only rule had sitting on haiku.


## PHASE 0 — THE FLOOR

*CI can be green at all, and the three §12 breakages are fixed*

**181 items · 19 parallel slots · opus 18 · sonnet 1 · haiku 0**

| slot | model | directory (the slot OWNS it) | items | HIGH | tags | item numbers |
|---|---|---|--:|--:|---|---|
| **W0.1** | `opus` | `scripts/` | 14 | 5 | 🔍oracle ⑂split ⊕shared | 468, 476–478, 481, 485, 514–515, 529–530, 539–541, 551 |
| **W0.2** | `opus` | `scripts/` | 13 | 2 | ⑂split | 470, 479, 490–491, 503–505, 516–518, 522–523, 531 |
| **W0.3** | `opus` | `scripts/` | 14 | 3 | 💰money 🔍oracle ⑂split | 471–472, 480, 494, 497, 509–510, 520, 524, 534, 537–538, 545, 550 |
| **W0.4** | `opus` | `scripts/` | 14 | 1 | 🔍oracle ⑂split | 467, 473, 487–489, 492–493, 495–496, 526–528, 532–533 |
| **W0.5** | `opus` | `scripts/` | 14 | 2 | 🔍oracle ⑂split | 474–475, 501–502, 506–508, 511–513, 519, 535, 548–549 |
| **W0.6** | `opus` | `scripts/` | 5 | 1 | ⑂split | 484, 521, 525, 536, 543 |
| **W0.7** | `opus` | `xtask/src/gates/` | 8 | 4 | ⑂split ⊕shared | 171–174, 200–202, 233 |
| **W0.8** | `opus` | `xtask/src/gates/` | 12 | 4 | ⑂split | 169–170, 176–177, 195–196, 205–209, 235 |
| **W0.9** | `opus` | `xtask/src/gates/` | 14 | 3 | 💰money 🔍oracle ⑂split | 166, 168, 178, 189, 194, 213–217, 225–227, 234 |
| **W0.10** | `opus` | `xtask/src/gates/` | 14 | 1 | 💰money ⑂split | 182, 186–187, 193, 197–199, 204, 210–212, 220, 228–229 |
| **W0.11** | `opus` | `xtask/src/gates/` | 5 | 2 | ⑂split | 180–181, 219, 221, 230 |
| **W0.12** | `opus` | `.github/workflows/` | 14 | 2 | 🔍oracle ⑂split ⊕shared | 332–333, 338–343, 352–353, 356–358, 361 |
| **W0.13** | `opus` | `.github/workflows/` | 14 | 0 | 🔍oracle ⑂split | 334–337, 344–351, 355, 359 |
| **W0.14** | `opus` | `.github/workflows/` | 3 | 1 | ⑂split | 331, 354, 360 |
| **W0.15** | `opus` | `crates/busbar-contract/src/caps/` | 9 | 3 | 💰money | 315–320, 326–327, 329 |
| **W0.16** | `opus` | `scripts/mcp-subject/` | 5 | 0 | 💰money | 498–500, 546–547 |
| **W0.17** | `sonnet` | `.github/` | 3 | 0 | — | 454–455, 458 |
| **W0.18** | `opus` | `scripts/a2a-subject/` | 3 | 1 | — | 469, 486, 544 |
| **W0.19** | `opus` | `scripts/release-gate/` | 3 | 2 | — | 482–483, 542 |

## PHASE 1 — ARM THE INSTRUMENTS

*Law 0: an instrument that cannot produce a NO is not a check*

**33 items · 8 parallel slots · opus 8 · sonnet 0 · haiku 0**

| slot | model | directory (the slot OWNS it) | items | HIGH | tags | item numbers |
|---|---|---|--:|--:|---|---|
| **W1.1** | `opus` | `qa/` | 9 | 3 | 💰money | 376–381, 421, 465–466 |
| **W1.2** | `opus` | `xtask/src/gates/structure_lint/` | 6 | 2 | — | 183–184, 222–224, 236 |
| **W1.3** | `opus` | `xtask/src/gates/construction/` | 5 | 1 | 💰money | 167, 190–192, 232 |
| **W1.4** | `opus` | `xtask/src/` | 4 | 3 | 🔍oracle | 160–162, 185 |
| **W1.5** | `opus` | `xtask/src/gates/config_schema/` | 3 | 2 | — | 163–164, 231 |
| **W1.6** | `opus` | `xtask/src/gates/conformance_sync/` | 2 | 1 | 🔍oracle | 165, 188 |
| **W1.7** | `opus` | `xtask/src/gates/kind_isolation/` | 2 | 1 | — | 175, 203 |
| **W1.8** | `opus` | `xtask/src/gates/plane_purity/` | 2 | 1 | — | 179, 218 |

## PHASE 2 — MONEY

*bank-auditor standard, binary*

**111 items · 28 parallel slots · opus 28 · sonnet 0 · haiku 0**

| slot | model | directory (the slot OWNS it) | items | HIGH | tags | item numbers |
|---|---|---|--:|--:|---|---|
| **W2.1** | `opus` | `crates/busbar-kernel/src/` | 13 | 2 | 💰money ⑂split ⊕shared | 273, 284–285, 293, 300, 552, 554–555, 560–562, 564–565 |
| **W2.2** | `opus` | `crates/busbar-kernel/src/` | 14 | 1 | 💰money ⑂split | 272, 280–281, 283, 289, 291, 299, 556, 559, 563, 566–567, 569–570 |
| **W2.3** | `opus` | `crates/busbar-kernel/src/` | 5 | 1 | 💰money ⑂split | 271, 292, 557–558, 571 |
| **W2.4** | `opus` | `crates/busbar/src/root/` | 14 | 5 | 💰money | 237–241, 248–253, 255–256, 265 |
| **W2.5** | `opus` | `crates/plugin-loader/src/` | 11 | 1 | 💰money 🔍oracle | 382–390, 392–393 |
| **W2.6** | `opus` | `crates/busbar/tests/` | 9 | 2 | 💰money | 242–243, 257–258, 262–264, 266–267 |
| **W2.7** | `opus` | `crates/busbar-plane-decision/src/` | 6 | 1 | 💰money | 395, 397–400, 403 |
| **W2.8** | `opus` | `crates/busbar/src/` | 5 | 0 | 💰money | 244–247, 259 |
| **W2.9** | `opus` | `crates/busbar-kernel-audit/src/tests/` | 4 | 0 | 💰money | 406–409 |
| **W2.10** | `opus` | `crates/busbar-llm/src/unit/` | 3 | 1 | 💰money | 363, 366, 368 |
| **W2.11** | `opus` | `crates/busbar-core-admin/src/v1/` | 2 | 0 | 💰money | 374–375 |
| **W2.12** | `opus` | `crates/busbar-kernel-audit/src/` | 2 | 1 | 💰money | 404–405 |
| **W2.13** | `opus` | `crates/busbar-kernel-ledger/src/` | 2 | 0 | 💰money | 435, 438 |
| **W2.14** | `opus` | `crates/busbar-kernel-ledger/src/cost/` | 2 | 0 | 💰money | 434, 437 |
| **W2.15** | `opus` | `crates/busbar-kernel/src/governance/` | 2 | 2 | 💰money | 268–269 |
| **W2.16** | `opus` | `crates/busbar-llm/src/engine/` | 2 | 0 | 💰money | 367, 369 |
| **W2.17** | `opus` | `crates/busbar-llm/src/engine/tests/` | 2 | 0 | 💰money | 364–365 |
| **W2.18** | `opus` | `crates/busbar-plane-a2a/src/` | 2 | 0 | 💰money | 450–451 |
| **W2.19** | `opus` | `crates/busbar-plane-streaming/src/` | 2 | 1 | 💰money | 418–419 |
| **W2.20** | `opus` | `crates/busbar-kernel-budget/src/` | 1 | 0 | 💰money | 460 |
| **W2.21** | `opus` | `crates/busbar-kernel-ledger/src/usage/` | 1 | 0 | 💰money | 436 |
| **W2.22** | `opus` | `crates/busbar-kernel-ledger/src/usage/tests/` | 1 | 0 | 💰money | 439 |
| **W2.23** | `opus` | `crates/busbar-kernel/src/audit/` | 1 | 0 | 💰money | 274 |
| **W2.24** | `opus` | `crates/busbar-llm-codec/src/cohere/tests/` | 1 | 1 | 💰money | 302 |
| **W2.25** | `opus` | `crates/busbar-llm-codec/src/gemini/tests/` | 1 | 1 | 💰money | 303 |
| **W2.26** | `opus` | `crates/busbar-llm-codec/src/ir/tests/` | 1 | 1 | 💰money | 304 |
| **W2.27** | `opus` | `crates/busbar-llm-codec/src/tests/` | 1 | 1 | 💰money | 306 |
| **W2.28** | `opus` | `crates/busbar-llm/src/engine/attempt/` | 1 | 1 | 💰money | 362 |

## PHASE 3 — C3, BYTE-IDENTITY

*the oracle, never waived*

**6 items · 5 parallel slots · opus 4 · sonnet 1 · haiku 0**

| slot | model | directory (the slot OWNS it) | items | HIGH | tags | item numbers |
|---|---|---|--:|--:|---|---|
| **W3.1** | `opus` | `crates/plugin-loader/src/tests/` | 2 | 0 | 🔍oracle | 391, 394 |
| **W3.2** | `opus` | `crates/busbar-core-admin/src/tests/` | 1 | 1 | 🔍oracle | 370 |
| **W3.3** | `sonnet` | `crates/busbar-llm-codec/src/` | 1 | 0 | — | 307 |
| **W3.4** | `opus` | `crates/busbar-llm-codec/src/cohere/` | 1 | 1 | — | 301 |
| **W3.5** | `opus` | `crates/busbar-llm-codec/src/openai_responses/` | 1 | 1 | — | 305 |

## PHASE 4 — C1, C2 AND THE FOLD

*the wide parallel drain*

**63 items · 40 parallel slots · opus 9 · sonnet 17 · haiku 14**

| slot | model | directory (the slot OWNS it) | items | HIGH | tags | item numbers |
|---|---|---|--:|--:|---|---|
| **W4.1** | `sonnet` | `crates/busbar-a2a/src/a2a/` | 7 | 0 | — | 422–426, 431–432 |
| **W4.2** | `sonnet` | `crates/busbar-plugin/src/hot/` | 4 | 0 | — | 411–413, 415 |
| **W4.3** | `sonnet` | `crates/busbar-contract/src/` | 3 | 0 | — | 321, 323, 330 |
| **W4.4** | `sonnet` | `crates/busbar-kernel-wal/src/` | 3 | 0 | — | 444–446 |
| **W4.5** | `opus` | `crates/busbar-mcp/src/mcp/` | 3 | 2 | — | 309, 312, 568 |
| **W4.6** | `opus` | `crates/busbar-contract/src/caps/fixtures/` | 2 | 1 | — | 314, 325 |
| **W4.7** | `sonnet` | `crates/busbar-core-admin/src/` | 2 | 0 | — | 372–373 |
| **W4.8** | `sonnet` | `crates/busbar-kernel/src/auth/tests/` | 2 | 0 | — | 276, 295 |
| **W4.9** | `sonnet` | `crates/busbar-kernel/tests/` | 2 | 0 | — | 286, 294 |
| **W4.10** | `haiku` | `crates/busbar-oauth2/src/` | 2 | 0 | — | 441–442 |
| **W4.11** | `sonnet` | `crates/busbar-oauth2/src/tests/` | 2 | 0 | — | 440, 443 |
| **W4.12** | `sonnet` | `crates/busbar-substrate-values/src/` | 2 | 0 | — | 447–448 |
| **W4.13** | `sonnet` | `crates/plugin-sdk/src/` | 2 | 0 | — | 452–453 |
| **W4.14** | `haiku` | `crates/busbar-contract/src/caps/tests/` | 1 | 0 | — | 328 |
| **W4.15** | `sonnet` | `crates/busbar-contract/src/tests/` | 1 | 0 | — | 322 |
| **W4.16** | `sonnet` | `crates/busbar-contract/tests/` | 1 | 0 | — | 324 |
| **W4.17** | `opus` | `crates/busbar-core-admin/src/v1/json/` | 1 | 1 | — | 371 |
| **W4.18** | `haiku` | `crates/busbar-kernel-breaker/src/` | 1 | 0 | — | 459 |
| **W4.19** | `haiku` | `crates/busbar-kernel-egress/` | 1 | 0 | — | 417 |
| **W4.20** | `opus` | `crates/busbar-kernel-egress/src/` | 1 | 1 | — | 416 |
| **W4.21** | `haiku` | `crates/busbar-kernel-identity/src/tests/` | 1 | 0 | — | 461 |
| **W4.22** | `haiku` | `crates/busbar-kernel/src/auth/` | 1 | 0 | — | 288 |
| **W4.23** | `haiku` | `crates/busbar-kernel/src/egress/engine/` | 1 | 0 | — | 290 |
| **W4.24** | `haiku` | `crates/busbar-kernel/src/egress_auth/tests/` | 1 | 0 | — | 296 |
| **W4.25** | `opus` | `crates/busbar-kernel/src/plane/` | 1 | 1 | — | 270 |
| **W4.26** | `sonnet` | `crates/busbar-kernel/src/plane_host/` | 1 | 0 | — | 282 |
| **W4.27** | `haiku` | `crates/busbar-kernel/src/store/in_memory/` | 1 | 0 | — | 298 |
| **W4.28** | `opus` | `crates/busbar-mcp/src/mcp/client/` | 1 | 1 | — | 308 |
| **W4.29** | `opus` | `crates/busbar-mcp/src/mcp/tests/` | 1 | 1 | — | 311 |
| **W4.30** | `sonnet` | `crates/busbar-plane-decision/` | 1 | 0 | — | 396 |
| **W4.31** | `haiku` | `crates/busbar-plane-decision/tests/` | 1 | 0 | — | 401 |
| **W4.32** | `haiku` | `crates/busbar-plane-llm/src/` | 1 | 0 | — | 462 |
| **W4.33** | `opus` | `crates/busbar-plugin/src/` | 1 | 1 | — | 410 |
| **W4.34** | `haiku` | `crates/busbar-plugin/src/cold/` | 1 | 0 | — | 414 |
| **W4.35** | `haiku` | `crates/busbar-substrate-values/src/testkit/` | 1 | 0 | — | 449 |
| **W4.36** | `haiku` | `crates/busbar-transport-stdio/src/` | 1 | 0 | — | 464 |
| **W4.37** | `opus` | `crates/busbar-transport-ws/src/` | 1 | 1 | — | 420 |
| **W4.38** | `sonnet` | `crates/busbar/src/root/units_admin/tests/` | 1 | 0 | — | 254 |
| **W4.39** | `sonnet` | `crates/store-example-plugin/src/` | 1 | 0 | — | 456 |
| **W4.40** | `sonnet` | `crates/store-example-plugin/src/tests/` | 1 | 0 | — | 457 |

---

## ITEM 572 — `oauth-as` 0.9.3 → 1.0.0, and it runs BEFORE the waves

**Owner-added. This is not an audit finding; it is a dependency the release must carry.**

| | |
|---|---|
| pinned today | `oauth-as = "0.9.3"` (`Cargo.toml:201`) |
| latest | **1.0.0**, published 2026-09-24 |
| versions crossed | 0.9.5 · 0.10.0 · 0.11.0 · 0.11.1 · 0.11.2 · **1.0.0** |
| `cargo update` would give | 0.9.5 — semver-compatible only, **not** what is wanted |
| features enabled | `http` · `jwt` · `consent` · `resource-metadata`, plus `test-util` in two dev-deps |
| consumers | `busbar-oauth2` · `busbar-kernel` (both normal and dev) |
| call sites | **93** non-comment `oauth_as::` references across `crates/` |

**Why it is a serialization point and not a slot.** Under 0.x, every minor is a breaking change,
so this crosses **three** breaking boundaries before the major. It edits the workspace root
`Cargo.toml` (owned by nobody) *and* the manifests and call sites of `busbar-oauth2` and
`busbar-kernel` — directories owned by W2.1–W2.3, W4.10 and W4.11. Running it as a parallel slot
would violate §4.2 outright.

So it does not run in a wave. **It runs alone, before wave 0**, and every other slot builds on
top of the result. A dependency bump that changes what compiles is not something ninety-nine
agents should discover one at a time.

**Model: `opus`.** It is not money, but `oauth_as` mints tokens, and §9.5 holds customer-visible
behaviour identical to 1.5.5. A major bump of the authorization server is exactly where an
OAuth-visible change enters without anyone deciding to make one.

**Done means:**

1. `oauth-as = "1.0.0"` in `Cargo.toml`, lockfile regenerated, `cargo check --workspace` clean.
2. **Every one of the 93 call sites re-verified** — a bump that compiles is not a bump that is
   correct. Compilation proves the shapes match, not that the semantics did not move.
3. The feature list re-derived against 1.0.0's own feature table, not carried across verbatim.
   Features are renamed and split across a major.
4. **The upstream changelog read, and every behavioural change between 0.9.3 and 1.0.0 written
   into the slot-log row** — named, not summarised. This is the step that catches a
   customer-visible change before a customer does.
5. Any behavioural change that reaches a client is **queued per §1.8**, not absorbed. It is a
   §9.5 decision and it is the owner's.
6. Exit test: the OAuth conformance leg green on the new version, plus a test that pins one
   observable the bump could plausibly have moved — proved RED against 0.9.3 per §16.

---

## ⟂ STRUCK — 18 items, OUT of the work


These sit in one of the 63 files the fold deletes or merges. **The fold is their fix.** They hold
no slot and no agent should open them — doing so burns a model on a file that is going away and
manufactures a merge conflict against the fold.

They are recorded, not deleted: if the fold changes, they return to the work.


`260–261, 275, 277–279, 287, 297, 310, 313, 402, 427–430, 433, 463, 553`


**394 work items in slots + 18 struck = 412 filed.**

