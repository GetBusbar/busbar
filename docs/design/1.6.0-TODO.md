# BUSBAR 1.6.0 — THE TODO LIST

**STATUS: DRAFT v2, post-adversarial-audit, awaiting owner sign-off.** When every item here is
done and the four claims in `BUSBAR-1.6.0.md` Part 0 return YES, 1.6.0 is done. Nothing else counts.

Every number was measured on `predev`, not estimated. Where a gate already measures an item, the
gate is named — that gate IS the progress bar. The list does not need maintaining; it needs
re-measuring.

> **v2 changed two things that matter.** C4 is **NOT** passing — v1 said it was. And v1 was a
> trap: on the day all 25 of its items went green, promotion to `qa` would still have refused,
> because the check that blocks promotion was named on no item. Both are fixed below.

---

## C1 — core names no instance
*Instrument: `instance-noun-neutrality` (correct, RED with 24 rows, excused, in `ci.yml` ZERO times).*

| # | item | measure |
|---|---|---|
| 1 | Clear instance-noun leaks in planes / transports / stores | `qa/instance-noun-neutrality.toml` carries **723** `[[leak]]` rows — v1 said ~436, re-measure before signing |
| 2 | Replace 55 hardcoded plane-section literals with `config_sections_from()` | 55 sites |
| 3 | Replace core's hardcoded URL-scheme literals — **blocked on #6** | 10+ sites |
| 4 | Un-excuse the gate and wire it into `ci.yml` | 0 mentions today |
| 5 | Fix `plane-purity`'s denominator | 3 roots (shell) vs 21 (Rust) vs 31 (actual Neutral crates) |
| **26** | **Drive `plane-purity-strict`'s ceilings to 0.** Part 0 instrument #2 demands ZERO instance vocabulary in Neutral crates, ceiling 0, ARMED. Five of six ceilings are non-zero. Item 5 makes these numbers BIGGER (21→31 roots), never smaller | SYMBOL 61 · TYPE 13 · KEY 377 · **DIALECT 456** · BACKWARDS 33 = **940** |
| **27** | **Core still holds the list of plane config verbs, hidden from its own gate by 11 in-source pragmas — all 11 in core.** And the dup-claim guard the ratified contract rests on is vacuous: 3 of 5 planes declare `owned_config_sections: &[]`, so `registry.rs:385` is "unconditionally `Ok(())`". Core concretely owns 5 sections incl. `pools` — which IS the llm plane's own verb | 11 pragmas · guard inert for 3/5 planes |
| **28** | **Core names five dialects and defaults to one.** `proto/mod.rs:253-257`, `providers.rs:79 DEFAULT_PROTOCOL="anthropic"`. Item 1's gate cannot see these — it knows plane/transport nouns, zero dialect terms | 6 lines |
| **29** | **BUILD THE C1 SHARP TEST.** Part 0: *"add a dialect, and `git diff --stat` must show zero files changed under core. That single command falsifies or confirms the entire premise of the release."* **No such instrument exists** — not a gate, not a script, not one of the 23 groups. The only hit in the tree is a code comment at `construction/rules2.rs:539` | 0 instruments |

## C2 — every plugin compiles in OR drops in
*2 of 7 kinds proven. `export` and `plane` are the working reference.*

| # | item | measure |
|---|---|---|
| 6 | **Make transport a real kind**: `TRANSPORT_DECL`, scheme-claim + dup-guard, `cdylib`, `load_transport` | ABI enumerates 6 kinds; `load_transport` 0 hits |
| 7 | Cut the lateral transport→transport edges | 7 edges |
| 8 | `store`, `secret`, `hook`: `open` pub, `dispatch_compiled_in`, conformance test | 3 kinds, mechanical |
| 9 | `auth`: as #8 plus `dispatch_auth_enveloped`. `token-auth` has NEVER existed as code — not a proof | 1 kind |
| 10 | `kind-isolation`'s RED rows | deps, test-deps, closure, registry, matrix |
| **30** | **`kind-isolation-ship`'s two extra rows, RED by design**: no kind states a `Unit` entry, no plane/transport/unit kind has a shared battery, skeletons diverge from exemplars. This IS the C2 clause | 2 rows + 13 `[[disagreement]]`, 6 `[[question]]`, 5 `[[transitional]]` |
| **31** | **The decisions plane — 5th of the GOLD's 5 — is inert and lives inside the core binary.** 308 lines at `crates/busbar/src/root/plane_decision.rs`; `claims`/`admission`/`build` all return empty. `busbar-plane-decision` contains **0** `PLANE_DECL`. `units_decision.rs` does not exist, yet `reachability.rs:248` declares it | 4 planes serve, 1 is a name |
| **32** | **`kind-abi-lane` has never been shown able to go red** — the sole instrument for "each kind is bound to exactly one lane", running on every push | 0 `prove_red` vs 8 `Row::fail` |

## C3 — LLM-only is byte-identical to published 1.5.5
*REFUTED. Denominator is 916 owed cells of 2,318 — 60% of the corpus cannot fail. 545 of the 916 are pre-waived class-wise.*

| # | item | measure |
|---|---|---|
| 11 | Give `accepted-differences.json` a sign-off/date/expiry field | 41 entries, 0 have one |
| 12 | Repoint or delete register entries matching zero cells | 4 entries |
| 13 | Triage the unaccepted divergences | 8 |
| 14 | Pin the clock on `billing\|rate-card\|history-mid-window`, re-run **(MONEY, parked)** | 1 cell, 10x spend delta |
| 15 | Re-run the oracle on clean HEAD | token, containers, ~40–60 min |
| 16 | Land the oracle judge on `main` | unmerged feature branch |
| 25 | **Streaming plane is NOT off by default** — `voice_build()` gates on `public_url`, never reads `cfg.streams` | `mount.rs:487` |
| **33** | **Fix the DENOMINATOR, not just the numerator.** A corpus where 60% of cells cannot fail is not a corpus | 916 of 2,318 |
| **34** | **87 coverage gaps sit behind two `#[ignore]`s and nothing watches them** | `method_coverage.rs:552` → **64** cells · `field_coverage.rs:305` → **23** fields |
| **35** | **Two QA segments say in-file "→ active in 1.6.0" and are still `reserved`** — so the two headline planes have no live QA leg. Reserved segments are "INERT and EXCLUDED FROM THE GREEN CLAIM" | `segments.toml:302 mcp-integrity`, `:308 a2a` |

## C4 — money is a view
> **v1 said C4 PASSES. That was wrong.** Zero floats on the money path still holds, and no dollar
> crosses the store boundary. But the witness for #43 is 16-wide red with an empty ledger, and one
> of its findings is the literal branch #43 outlaws, inside the LLM plane.

| # | item | measure |
|---|---|---|
| 17 | `derive_spend_minor` reports an unpriced fee as ZERO rather than refusing — NAY | `cost/project.rs:75-129` |
| 18 | Rate-card history has no `Serialize` — confirm it survives restart or make it | 0 derives |
| 19 | **Remove `CurrencyCode`** — RULED. An admin `amend_rate_history` body carrying `"currency":"JPY"` shifts the scale 10,000,000 → 1,000,000,000: same figures, **100× different money**. At `v1.5.5` the type, the verb and the openapi field are all absent, so removal RESTORES parity | `units_admin/mod.rs:1113` |
| 24 | **Ledger migration is dead code** — `fold_v1_ledger` has ZERO production callers; old rows deserialize to empty; every customer's counters zero on upgrade — NAY **(MONEY, parked)** | `usage_migration.rs:121` |
| **36** | **`plane-pricing-blindness` is fully RED with a zero-row ledger.** `engine/usage.rs:113` and `unit/verify.rs:317` call `host.cost_pricing_enabled(..)` — the branch #43 outlaws. Collides with C3: these sites are on the byte-identical LLM path | 16 findings, 11 files, 0 `[[debt]]` rows |
| ~~23~~ | ~~`no-float-money` misses `card_from_config`~~ **DONE.** Worse than stated: the gate was standing-RED, so every red plant in its selftest scored PROOF IMPOSSIBLE — **it had no valid proofs at all.** A third intake was found (`busbar-kernel-budget/src/price.rs:76`). Now a declared `MONEY_INTAKE` table, return types checked, RED demonstrated | 27 selftest cases, exit 0 |

## ACCEPTANCE — what actually lets 1.6.0 ship
> **This section is why v1 was a trap.** `ship-ready` is a REQUIRED status check on `qa` and `main`
> (`scripts/ci-branch-protection.sh:119`), it is **RED on purpose**, and it is deliberately NOT in
> the umbrella's `needs` — verified: 0 occurrences. So it blocks promotion while CI stays green.

| # | item | measure |
|---|---|---|
| **37** | **`ship-ready`'s four rows**: `ship-twin`, `ceiling-slack`, `ceiling-rose`, `standing-reds` | 4 rows |
| **38** | **The five standing reds must be EMPTY** — `hold-discipline:cancellation-before-await`, `one-pick-site`, `ports-only-tests:busbar-llm`, `request-path-fn-size`, `terminal-doors-in-audit-step`. My own gate run confirms `one-pick-site` FAILs today | `gates/mod.rs:229-235` |
| **39** | **Three registered gates run nowhere and carry no excuse**, which makes `full-gate --selftest` red — and `ci.yml:454` runs it | `unconstructed`, `map-proof`, `sweep-coverage` |
| **40** | **~5,100 lines of plane unit-path scaffolding never run, with ZERO `[[dormant]]` rows declared.** This is the module the three 2026-09-22 money faults were found in — no corpus cell could have caught any of them, because none of it runs | `units_mcp.rs` 1,884 · `units_a2a.rs` 1,784 · `units_voice.rs` ~1,000 · `money_book.rs` 239 · `vocabulary.rs` 225 |

| **42** | **`money-invariants`' denominator is hand-maintained and nothing forces it to stay complete.** `MONEY_RECORDS` names 8 types; `records.rs` defines 17. `PlaneRecord` is uncovered. The gate CAN say no (it prints real found-counts) — but only about the 8 it was told to look at | 8 of 17 |

## Not covered by any claim
| # | item | measure |
|---|---|---|
| 20 | Crate fold | 49 → 33 locked |
| 21 | Docs collapse — **must not orphan the 104 parity bindings** (owner ruling #2) | 267 classified: 1 spec, ~95 keep, ~100 trash |
| 22 | Two High-severity advisories — **fix in 1.6.0; disclosure timing is the owner's** | shipped in 1.5.0–1.5.5 |
| **41** | Part 0 says the done script has 22 groups. It has **23** | `verify-1.6.0-done.sh:153` |
| **43** | **`hot-path-perf` and `hot-path-alloc` never execute a bench** — zero `Command::new`, zero non-comment `cargo` lines. They grep bench SOURCE TEXT. (The <1µs HOT-lane budget itself IS asserted, by the `benches` segment at qa cadence — these two gates are simply not what asserts it, and their names say otherwise) | 0 executions |
| **44** | **26 of 51 registered gates serve none of the four claims.** Not itself a defect — but it means `cargo xtask gate --all` being green is not evidence for the release, and roughly half of today's 16 reds bear on nothing that blocks 1.6.0 | 51 gates, ~26 orphan |

---

## Owner rulings — settled 2026-09-23
1. Auth vocabulary (`bearer`/`spki`/`mtls`) = protocol vocabulary, not instance nouns. `sigv4` stays.
2. Something must still run 1.5.5 and 1.6.0 and prove sameness → constrains item 21.
3. Advisories: fix in 1.6.0, disclosure is the owner's call.
4. No currency. #66 unamended — the code was wrong, not the row.
5. *(withdrawn — admin verb count is a measurement, not an owner decision)*
6. Delete testkit. Second ruling; #33 has said so since 2026-09-18.
7. Review the list, then burn at full pace.
