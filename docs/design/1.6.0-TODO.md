# BUSBAR 1.6.0 — THE PLAN FOR DONE

**STATUS: complete after a 10-slice sweep over all 3,915 tracked files. Awaiting owner sign-off.**

When every item here is done, the four claims in `BUSBAR-1.6.0.md` Part 0 return YES and the train
moves. Nothing else counts. Ordered by **dependency**, not by date — owner ruling 10: *"forget a
date, i want a plan for DONE."*

Every figure was measured, not estimated, and independently re-verified by the architect before
landing here. Coverage proof: `1.6.0-SWEEP-COVERAGE.md`. Raw findings: `1.6.0-SWEEP-FINDINGS.md`.

> **Why the order is what it is.** Waves 0 and 1 come first because without them every later wave
> is unmeasurable or unshippable. A gate that cannot fail turns a green board into no evidence at
> all, and a switched-off release engine means a 100%-green tree still never leaves the shed. The
> sweep found both conditions live.

---

## WAVE 0 — make the instruments able to fail
*Nothing below this line can be trusted until this is done. Half the measuring apparatus currently
cannot produce a NO, so burning items would produce false green.*

| # | item | measure |
|---|---|---|
| 1 | **Instrument #2's hard clause is unenforced by BOTH instruments that claim to enforce it.** `LAW0_ENFORCED_NEUTRAL_CRATES = &[]` so the ARMED class blocks nothing on the everyday gate; the `[[cell]]` ceiling for `busbar-kernel × plane` was raised **1 → 3047**; and `plane-purity:key` is GREEN only because 11 in-source `// plane-purity: frozen-wire` pragmas delete hits before judgement. The clause bars ratchet rows and is silent about the pragma | `matrix.rs:129`, `scanner.rs:256` |
| 2 | **The pragmas' justification is false.** Three exempt `mcp`/`tools`/`agents` as "frozen since 1.5.3". **v1.5.3 and v1.5.5 both carry 26 config fields and none of the three exists at either tag.** They are 1.6.0-additive keys wearing a back-compat exemption | 11 pragmas, all in core |
| 3 | **The neutrality witnesses enumerate instances by hand, so neither can see a NEW one.** Proven by plant + positive control: a new plane, transport and dialect named in `busbar-kernel/src/lib.rs` moves **ZERO rows across 10 gates and 238 rows**; `"cohere"`/`"mcp"`/`transport_http` in the same file move 3. `PLANE_ALTERNATION` scans 4 of 5 planes and scans `voice` instead of `streaming` | `scanner.rs:29,43` |
| 4 | **Build the C1 sharp test.** Part 0 calls add-a-dialect-then-diff the command that confirms or falsifies the entire premise. No instrument exists — the only trace in the tree is a code comment. Must also assert the gate set stays green: one new `qa/field-schemas/*.json` turns all 7 `field-inventory` rows RED with zero core files touched | 0 instruments |
| 5 | **Derive every hand-maintained roster from the plugin's own declaration.** 10 dialect rosters measured, incl. `field_inventory.rs:85`, `scanner.rs:43`, `plane-grep-gate.sh:91`, `assets/readme/generate.py:118`, `voice-cross-dialect-map.json`, `providers.yaml`. Adding a 7th dialect touches ≥9 files in the root slice alone | 10 rosters |
| 6 | **Three registered gates run nowhere and carry no excuse**, which makes `full-gate --selftest` exit 1 inside a REQUIRED check. Plus two unclassified cargo invocations no item named | `unconstructed`, `map-proof`, `sweep-coverage` |
| 7 | **`--strict` has no denominator floor — the floor is ONE cell.** Proven by building the pinned judge: 1 owed cell of 2,318, 2,317 unbaselined, **exit 0**. Repo precedent: `DENOMINATOR_FLOOR=1_700`, `SCAN_FLOOR=200`, `BINDING_FLOOR=40` | `run.rs:2511` |
| 8 | **Under-matching a register entry is a `note`, not a red** — coverage shrink is silent by construction, and has already happened (4 dead entries) | `run.rs:1360` |
| 9 | **`money-invariants`' denominator is hand-written**: `MONEY_RECORDS` names 8 types, `records.rs` defines 17. `PlaneRecord` uncovered | 8 of 17 |
| 10 | **`hot-path-perf` / `hot-path-alloc` never execute a bench** — zero `Command::new`, zero non-comment `cargo` lines. They grep bench source text | 0 executions |
| 11 | **`kind-abi-lane` has never been shown able to go red** — the sole instrument for one-kind-one-lane, running on every push | 0 `prove_red` |
| 12 | **The migration-corpus gate computes a completeness denominator and discards it** — asserts only `n>=20` against an actual 69 | `ci.yml:1340` |
| 13 | **`.keep-proof.toml` is a keep-branch artefact on the integration branch**, narrowing `prove-remote.sh` to 161 of 2,318 cells (6.9%). Its own header says the integration tree carries no such file. Names 3 packages that do not exist | `prove-remote.sh:64` |

## WAVE 1 — switch the release engine on
*With every item in every other wave green, the train still does not move. All three stops are
plumbing, and none was on any previous list.*

| # | item | measure |
|---|---|---|
| 14 | **22 of 26 workflows are `disabled_manually` at GitHub.** Active: CI, qa-gate, Dependabot, oracle-rebless. Disabled: the entire release engine (Release, Release stage, Prepare release, tag-on-main, Verify deploy, Docker), **every plane conformance workflow** (mcp, a2a, voice, oauth — whose conformance is a C2 acceptance criterion), and **CodeQL**. Disable state is keyed by PATH, so promotion does not clear it | 22 disabled |
| 15 | **`qa → main` cannot fast-forward and cannot legally be forced.** `origin/main` holds **7 commits reachable from no other branch**. `promote.sh` refuses non-ancestors; `release.yml:530` refuses any sha qa never saw (ruling out a merge commit by design); `allow_force_pushes: false`, `enforce_admins: true`. Content is already on predev — **the remedy is an owner protection change** | 7 commits |
| 16 | **`predev → dev` has no mover** — `TURNSTILE_DISPATCH_TOKEN` absent from the repo's 3 secrets | owner PAT |
| 17 | **`ship-ready` is RED on all four rows** under a shipping posture: `ship-twin` (10 red rows), `ceiling-slack` (2 ceilings above measurement), `ceiling-rose` (10 stale raises), `standing-reds` (5 names that must be DRAINED, not struck) | 4 rows |
| 18 | **`main`'s required context `record the staged digest` can never report** — its only producer is `release-stage.yml`, which exists at neither `main` nor `qa` and is disabled | 1 context |
| 19 | **Nothing in CI ever runs `verify-1.6.0-done.sh`** — 0 `run:` sites, all 6 hits are comments, against three positive controls. The definition of done runs only if a human types it | 0 call sites |
| 20 | **The cost-watch is healthy and never fires** — selftest green, all four bands proven ($50/$80/$100/tool-error), but its cron only runs from the default branch and the file is not there | 0 runs |

## WAVE 2 — money
*Binary, and the sweep found an OVERCHARGE. Under standing money authority except where marked.*

| # | item | measure |
|---|---|---|
| 21 | **THE OVERCHARGE — the flat fee is billed on failed requests.** `records.rs:683` defines `billable_requests` as *"admissions MINUS non-2xx refunds — the flat-fee base (the fee bills 2xx only)"*. The live writer `governance/state.rs:963` sets `billable_requests: counts.requests`. The correct number is never computed. **Overcharge = fee × failures per key per window. OWNER RULING OWED — this has the opposite sign to ruling 8** | `state.rs:963` |
| 22 | **A rate below 0.0005 silently becomes 0, and the card reports the class PRICED** — so #42 can never fire. a2a/mcp price by BYTES: $0.10/GB = 0.00009313 micro/byte → **0** | `rate.rs:44`, `:399` |
| 23 | **Four live endpoints answer four different figures** for one window after a rate change; two silently reprice history #79 forbids. The Prometheus gauges make a fifth, disagreeing with the invoice by 20% | worked arithmetic in FINDINGS |
| 24 | **Money is served as `f64`, and the gate cannot see it** — `metrics.rs:401,462` `spend_cents as f64`; `no-float-money` does not scan the file | 2 sites |
| 25 | **THREE copies of the spend derivation**, disagreeing on the unpriced rule: ledger → 0, kernel → 0, budget → `i64::MAX` and blocks. One hardcodes `10_000` where another uses the named constant | 3 copies |
| 26 | **Nine of ten `Ledger::record_*` mutators are dead** — `drawn`, `adjustments`, `unreconciled` are structurally always 0, so the reconciliation residual is arithmetically incapable of being non-zero | 9 of 10 |
| 27 | **`with_tier` has ZERO callers** — settlement applies a tier no read applies. `/admin/usage` cannot apply a non-standard tier at all | 1 occurrence |
| 28 | **Money overflow saturates instead of refusing** — 22 sites in one slice alone, `unwrap_or(i64::MAX)` ≈ $92 quadrillion, on the SUCCESS arm too | 22 sites |
| 29 | **A negative fee is signed, sealed into the append-only history, then silently clamped to 0** | `mod.rs:1129` |
| 30 | **The rate-amendment audit trail is a volatile 1000-entry ring behind a no-op seam**, four strings, no figures, `DualControl` hard-wired `Single`. Erasable by 1000 admin calls or one restart | `entry.rs:163` |
| 31 | **The customer read prices at the CURRENT card and derives an unpriced model at 0** — the source calls that *"the designed behavior"* | `cost.rs:744` |
| 32 | **A signed sub-day amendment cannot reach its own rows.** `arrived_ms` IS captured then discarded; `history`/`arrived` are never read while the doc beside them promises the opposite | `units_llm.rs:1048` |
| 33 | **The ledger migration is dead code** — `fold_v1_ledger` has no production callers; old rows deserialize to empty; every customer's counters zero on upgrade **(PARKED — rewrites stored data)** | `usage_migration.rs:121` |
| 34 | **Item 19's currency removal is NOT done — my "done" was a false zero.** #66 bars a currency type OR SYMBOL; `busbar-kernel-audit/src/record.rs:137` carries `pub currency: String` and `recipe.rs:189` pushes it into the **signed digest**. Removal re-cuts a published cryptographic contract **(needs a migration story)** | 47 sites |
| 35 | **`plane-pricing-blindness` is 16-wide RED with a zero-row ledger**, incl. `cost_pricing_enabled` — the branch #43 outlaws — inside the LLM plane, which collides with C3 | 16 findings |
| 36 | **The ledger WRITE is gated on the rate card** — owner already ruled this a defect | `govern.rs:239` |
| 37 | Four committed green tests assert the opposite of their own decision rows (D5, D8, D9 + four llm tests proving a mid-stream cut bills zero, against #62) | 8 tests |

## WAVE 3 — C3, byte-identity
*The oracle has never built the binary the claim is about. Everything else here is downstream.*

| # | item | measure |
|---|---|---|
| 38 | **RECORD THE RIGHT BINARY.** `default` compiles in all four non-LLM planes; `parity_build_and_resolve` (`verify-1.6.0-done.sh:555`) runs a plain `cargo build`. **The oracle has recorded an all-planes build for the entire release. C3 is unevidenced, not weakly evidenced** | `Cargo.toml:192` |
| 39 | **All four non-LLM planes ship DEFAULT-ON.** Part 0 says "purely additive and off by default" in four places | 4 planes |
| 40 | **Law 7 holds for only 2 of 5 planes, and the cause is in CORE** — `boot.rs:70-77` calls every plane's `hydrate` with no slot and no config check. mcp and a2a gate on `store:`, streaming on `public_url` | 3 of 5 live |
| 41 | **The DONE script re-records its own reference** on the machine under test with no wholeness check — the exact practice both CI jobs name in their comments as the thing that went wrong | `:957` vs `ci.yml:3316` |
| 42 | **`llm.wire` is off `BODY_IS_CONTRACT`**, so `body` rates 3 there and an `improvement` can forgive it — 62 of 133 LLM cells do. All 5 `ledger` cells have `body` waived, 4 by an improvement. `billing` is off the list too | 282 of 916 |
| 43 | **The LLM plane's entire oracle surface is ONE verb** — `chat` is the only `op` in all 2,318 cells | 1 verb |
| 44 | **The admin corpus is a fixture frozen at the 1.5.5 op set** (66 ops). Nothing reconciles it against the served router, so the 11 new admin paths have zero cells AND no mechanism that could give them one | 66 ops |
| 45 | **26 admin verbs are served and in NO OpenAPI document** (18 money-governance); 11 new paths mount unconditionally because both mount loops iterate linked planes, not configured ones | 26 verbs |
| 46 | **A side-car OpenAPI exists explicitly so the pinned bytes need not move**, hand-maintained, and says so in its own source | 1 document |
| 47 | **Two `breaking` register entries carry in-file AUDIT NOTES saying their premise is false today** and still grant their classes | `F-011r`, `D-3` |
| 48 | **The migration corpus has never seen the shipped release** — no v1.5.3/1.5.4/1.5.5 entry | 3 missing |
| 49 | `BUSBAR_ORACLE_RUST_BIN` / `BUSBAR_RELEASE_CHECKOUT` **repoint the judge itself** and are the two vars missing from `assert_bless_env_empty` | 2 of 13 |
| 50 | Re-record the golden with the Rust engine so `--allow-harness-skew` can be deleted from both workflows | 2 call sites |
| 51 | 22 in-scope cells have no golden, 14 named nowhere; `owed-baseline.txt` has 917 lines for 916 cells | 22 / 1 dup |
| 52 | Sign-off/expiry field on `accepted-differences.json`; re-adjudicate the 10 `breaking` entries; repoint or delete the 4 dead ones; triage the 8 unaccepted divergences | 41 entries |

## WAVE 4 — C1, core names no instance
| # | item | measure |
|---|---|---|
| 53 | **Core holds the list of plane config verbs** and types a struct field per plane | `prepass.rs:131` |
| 54 | **The dup-claim guard watches the wrong field.** The verb is `config_section`, resolved by `.find()` — first match silently wins. The guard reads `owned_config_sections`, which 3 of 5 planes leave empty. **`pools` is claimed twice** | `registry.rs` |
| 55 | **`PlaneMeta::CONFIG_VERB` has never existed** — #49 is written around a symbol with 0 hits | 0 hits |
| 56 | **No transport dup-guard exists at all** — `TRANSPORT_DECL`, `load_transport`, `scheme_claim`, `owned_schemes` → 0 hits against 532 files containing "transport". #50 is entirely unbuilt | 6 symbols, 0 hits |
| 57 | **Core names five dialects and defaults to one**; 20 `http`/`https` literals in kernel production source | `proto/mod.rs:253` |
| 58 | **3,047 plane-vocabulary hits in `busbar-kernel`**, 1,942 in the composition root | `qa/kind-isolation.toml` |
| 59 | **Law 5 is unmet**: the six llm dialects live in a separate 105,589-line crate, not inside the plane. No plane crate has a `dialects/` dir — so the sharp test has no defined boundary until this moves | 0 dirs |
| 60 | `plane-purity-strict` ceilings to 0 — SYMBOL 61 · TYPE 13 · KEY 377 · **DIALECT 456** · BACKWARDS 33 | 940 |
| 61 | 723 `[[leak]]` rows; fix `plane-purity`'s denominator (3 shell vs 21 Rust vs 31 actual); un-excuse `instance-noun-neutrality` and wire it into CI | 723 rows |

## WAVE 5 — C2, compiled-in OR dropped-in
| # | item | measure |
|---|---|---|
| 62 | **TWO unrelated `PlaneDecl` types.** `#[repr(C)]` at `hot/decl.rs:132` used by one fixture; native Rust at `registry.rs:811` used by all five real planes. **Part 3 asserts ONE seam and outranks Part 2 — the authoritative section is the wrong one** | 2 types |
| 63 | **LAND THE ABI RIDER (owner ruling 9).** C-ABI→native adapter, loop through `PlaneHostVtable`, one real plane as `cdylib`, `open_plane` called from the composition root. This is TRACKER H6 | 0 production callers |
| 64 | **For all 7 kinds the only `cdylib` is an unpublished test fixture.** Every shipped implementation is compiled-in only | 6 fixtures, `publish=false` |
| 65 | **Transport is absent four ways**: no `TRANSPORT_DECL`, missing from `KNOWN_KINDS` (`sign.rs:70`), no `supported_abi` arm, no `load_transport` | 4 absences |
| 66 | `open_export` has **zero callers tree-wide**; `open_plane` zero production callers; the binary never mentions `load_plane`/`DynPlane` | 0 / 0 / 0 |
| 67 | **#85's acceptance clause is OWED** — the last conformance test is an empty body labelled *"this test is a landmark"*. kind:plane has no both-folds BEHAVIOURAL test, only a decl-shape compare | 2 of 7 kinds |
| 68 | `kind-isolation` RED (12 rows) + **84 `[[dep]]` rows recording not-allowed edges**. Green-by-reconciliation is not green-by-absence | 84 rows |
| 69 | 3 real lateral transport deps (4 more are dev-deps); 13 of 21 plugin crates breach the dependency wall | 3 + 60 findings |
| 70 | **`plugins.yaml`, "the single source of truth for what plugins exist", expresses 4 of 7 kinds** — export, plane and transport cannot be registered | 4 of 7 |
| 71 | **The `Dockerfile` claims "ships with ZERO plugins pre-installed"** — `auth-admin-tokens` and `hooks-ranking` are both in the default feature set | 2 baked in |
| 72 | **The decisions plane is inert and lives inside the core binary**; `busbar-plane-decision` contains 0 `PLANE_DECL`; `units_decision.rs` does not exist yet `reachability.rs:248` declares it | 308 lines |
| 73 | **`busbar-plane-llm` has ZERO `PLANE_DECL`** and one production reference repo-wide | 0 / 1 |
| 74 | **Two streaming crates both ship and both register** — `busbar-voice` (13,353 loc) and `busbar-plane-streaming` (5,135 loc) | 2 crates |

## WAVE 6 — dead weight, the fold, and the record
| # | item | measure |
|---|---|---|
| 75 | **643 dead items repo-wide**, measured per-crate with forced lints (a multi-`-p` run masks it). `busbar` 282 · mcp 71 · a2a 48 · transports 117 | 643 |
| 76 | **One blanket `#![allow(dead_code)]` suppresses the compiler across 38,356 lines**, and its own stated removal condition is already met (`main()` references `root::` 61 times). Six such blankets in production | 6 |
| 77 | **~8,750 lines of dark scaffolding**: ~5,100 under `root/units_*.rs` + **3,653 in `busbar-llm/src/unit/`**, the plane C3 rests on. Zero `[[dormant]]` rows declared | 8,750 loc |
| 78 | **99 `pub fn` with zero production callers** across 23 crates | 99 |
| 79 | **87 coverage gaps behind two `#[ignore]`s** — 64 method cells, 23 fields | 87 |
| 80 | **Two QA segments say in-file "→ active in 1.6.0" and are still `reserved`** — the two headline planes have no live QA leg | `mcp-integrity`, `a2a` |
| 81 | Crate fold 49 → 33. Only 8 crates sit outside the shipped binary, so ≥8 folds must merge SHIPPING crates and perturb the binary's graph | 16 folds owed |
| 82 | **Two High-severity advisories — fix in 1.6.0 (ruling 3); disclosure timing is the owner's** | shipped 1.5.0–1.5.5 |
| 83 | **Rewrite the spec: 61 of 118 checkable claims are wrong** (44 stale, 17 false). 7 GOLD clauses false. **60 of 85 Part 2 rows** do not match the code. 11 citations name paths or symbols that do not exist. Three mutually inconsistent dates survive against ruling 10 | 61 / 118 |
| 84 | **#77's "8-verb admin surface" is a 17-verb surface** — all five verbs the row says were removed are live and wire-routed (this was my withdrawn question 5; the answer is 17) | 17 verbs |
| 85 | **`#81a`/`#83a`/`#84a` are cited 12+ times including inside `no_float_money.rs`, and Part 2 has no lettered rows.** Row 81 is malformed — a stray `\|` splices a body into the verification cell | 3 broken + 1 malformed |
| 86 | **Three rows are stale OPTIMISTICALLY** — the work landed and the row still says owed: #65, #76, #79. Also done: testkit, Pass/Grant, `UnitKey::new(0)`, 2 of 3 #40 edges, the branch collapse | 3 + 6 |
| 87 | Docs collapse: 267 files → 1 spec + ~95 keep-public + ~100 trash. **Must not orphan the 104 parity bindings** (ruling 2). `#58`'s duplicate pairs named | 267 |

---

## OWNER RULINGS OWED
1. **Item 21 — the overcharge.** Ruling 8 allowed no restatement because the error was an UNDER-charge. This one has the opposite sign: customers are charged for requests that failed. Same question, inverted facts.
2. **Item 15 — `main`'s 7 divergent commits.** Only an owner protection change can resolve it; no code fix exists.
3. **Item 34 — the audit-chain currency field** sits inside a signed digest. Removing it needs a migration story.

## RULINGS ALREADY GIVEN
1 auth vocabulary exempt · 2 parity bindings survive the collapse · 3 fix advisories, disclosure is the owner's · 4 no currency · 5 *(withdrawn — answered by measurement: 17 verbs)* · 6 delete testkit **(done)** · 7 review then burn · 8 fix the under-billing, no restatement · 9 land the rider · 10 no date

## DONE THIS SESSION
kernel testkit deleted (−1,392 lines) · `CurrencyCode` the type removed and the 100× scale defect fixed with a demonstrated RED · `--strict` armed on three oracle callers · `inventory-ref`'s blank-binding vacuous pass closed · `no-float-money`'s `MONEY_INTAKE` declared (it had **no valid proofs at all** — every red plant scored PROOF IMPOSSIBLE)
