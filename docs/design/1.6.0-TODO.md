# BUSBAR 1.6.0 — THE TODO LIST

**STATUS: DRAFT, awaiting owner agreement.** When every item here is done and the four
claims in `BUSBAR-1.6.0.md` Part 0 return YES, 1.6.0 is done. Nothing else counts.

Every number below was measured on `predev` on 2026-09-23, not estimated. Where a gate
already measures an item, the gate is named — that gate IS the progress bar, and re-running
it is how you know where this list stands. The list does not need maintaining; it needs
re-measuring.

---

## C1 — core names no instance
*Instrument: `cargo xtask gate instance-noun-neutrality`. The gate is already correct
(per-instance families, not per-kind). It is RED with 24 rows, carries
`Excused::OnlyAbout("tracked known-debt")`, and appears in `ci.yml` ZERO times.*

| # | item | measure |
|---|---|---|
| 1 | Clear instance-noun leaks in planes / transports / stores | ~436 file-mentions |
| 2 | Replace 55 hardcoded plane-section literals in core with `config_sections_from()`, which already exists | 55 sites |
| 3 | Replace core's hardcoded URL-scheme literals — **blocked on #6** | 10+ sites |
| 4 | Un-excuse the gate and wire it into `ci.yml` | 0 mentions today |
| 5 | Fix `plane-purity`'s denominator | 3 roots (shell) vs 21 (Rust) vs 31 (actual Neutral crates) |

## C2 — every plugin compiles in OR drops in
*2 of 7 kinds proven. `export` and `plane` are the working reference.*

| # | item | measure |
|---|---|---|
| 6 | **Make transport a real kind**: `TRANSPORT_DECL` in the ABI, scheme-claim + dup-guard mirroring `check_owned_config_claims`, `cdylib` crate-type, `load_transport` in the loader | ABI enumerates 6 kinds; `load_transport` has 0 hits |
| 7 | Cut the lateral transport→transport edges | 7 edges |
| 8 | `store`, `secret`, `hook`: make `open` pub, add `dispatch_compiled_in`, write the conformance test | 3 kinds, mechanical |
| 9 | `auth`: as #8 plus `dispatch_auth_enveloped` in the sdk | 1 kind |
| 10 | `kind-isolation`'s RED rows | deps, test-deps, closure, registry, matrix |

## C3 — LLM-only is byte-identical to published 1.5.5
*REFUTED. Denominator is 916 owed cells, not 2,318 — 60% of the corpus cannot fail.
545 of the 916 are pre-waived class-wise. Last real run: 8 unaccepted divergences.*

| # | item | measure |
|---|---|---|
| 11 | Give `accepted-differences.json` a sign-off/date/expiry field — DoD demands re-signing and there is nowhere to record it | 41 entries, 0 have one |
| 12 | Repoint or delete register entries that match zero cells | 4 entries |
| 13 | Triage the unaccepted divergences | 8 |
| 14 | Pin the clock on `billing\|rate-card\|history-mid-window` and re-run — separates a register class-gap from a real divergence **(MONEY, parked)** | 1 cell, 10x spend delta |
| 15 | Re-run the oracle on clean HEAD | needs private-repo token, containers, ~40–60 min |
| 16 | Land the oracle judge on `main` | currently an unmerged feature branch |
| 25 | **Streaming plane is NOT off by default** — `voice_build()` gates on `public_url`, never reads `cfg.streams`. Refutes the ratified statement and Law 7 | `mount.rs:487` |

## C4 — money is a view
*PASSES. Zero floats on the money path — all 37 are at the one config-intake conversion
or in tests. `UsageLedger` carries only counts; no dollar crosses the store boundary.*

| # | item | measure |
|---|---|---|
| 17 | `derive_spend_minor` reports an unpriced fee as ZERO rather than refusing — ruled NAY | `cost/project.rs:75-129` |
| 18 | Rate-card history has no `Serialize` — confirm it survives restart or make it | `History`/`CardEntry`/`RateCard`, 0 derives |
| 19 | `CurrencyCode` exists vs #66 "no currency type" — **owner ruling owed** | live type in the ledger |
| 24 | **Ledger migration is dead code.** `ModelTokens` changed shape; `fold_v1_ledger` has ZERO production callers; old rows silently deserialize to empty. Every customer's accumulated counters zero on upgrade — ruled NAY **(MONEY, parked)** | `usage_migration.rs:121` |

## Not covered by any claim
| # | item | measure |
|---|---|---|
| 20 | Crate fold | 49 today → 33 locked |
| 21 | Docs collapse | 267 classified: 1 spec, ~95 keep-public, ~100 trash |
| 22 | Two High-severity advisories — **owner disclosure decision owed** | shipped in 1.5.0–1.5.5, 13 days on STOP |
| 23 | `no-float-money` does not scan `kernel.rs::card_from_config` — where money enters from config | undeclared second boundary |

---

## Owner rulings owed
1. Auth vocabulary — are `bearer`/`spki`/`mtls` instance nouns? (265 files swing)
2. Two documents or three (`ARCHITECTURE.md` holds 104 parity bindings nothing else covers)
3. The two security advisories
4. `CurrencyCode` vs #66
5. Admin verbs — 12 or 8
6. `testkit` — #33 says it doesn't exist; it does
7. Does this list match "done"?
