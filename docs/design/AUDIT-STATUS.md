# busbar audit status

GENERATED by `cargo xtask ledger status` from `qa/audit-ledger.json`. Do not hand-edit.

An audit result describes one tree. When a scope's tree hash moves, its result expires and
the scope reads `stale` -- the code must be looked at again. Nothing here is a wall clock;
`age` is commits between the audited commit and HEAD.

HEAD at generation: `7403108f56e857084ead2511164b10210816db3b`

## Totals over production LOC

| status | LOC | share |
| --- | ---: | ---: |
| clean | 0 | 0.0% |
| unconfirmed | 190 | 0.0% |
| fixed | 0 | 0.0% |
| in_progress | 1055 | 0.2% |
| stale | 555249 | 84.6% |
| open | 3474 | 0.5% |
| unaudited | 96116 | 14.6% |
| invalid | 0 | 0.0% |

## production scopes (71)

| scope | status | round | age (commits) | LOC | result | auditor | report |
| --- | --- | ---: | ---: | ---: | --- | --- | --- |
| `crates/api/src` | stale | 1 | 1179 | 3583 | findings (MEDIUM=2, LOW=6, NIT=4) | opus-pass1 | `foreign wire field bricks a VirtualKey row (fixed); redeem_plane_token defaulted to first-redemption (fixed); put_key tombstone precondition unenforceable (ABI, post-1.6.0); all 10 production files read` |
| `crates/auth-admin-tokens/src` | unconfirmed | 1 | 663 | 63 | zero | opus-examples-auditor-a | `zero; read fully` |
| `crates/auth-static-plugin/src` | unconfirmed | 1 | 663 | 127 | zero | opus-examples-auditor-a | `zero; read fully` |
| `crates/busbar-a2a-codec/src` | stale | 7 | 494 | 1584 | findings (LOW=1, NIT=1) | opus-plane-auditor-b | `the crate does not forbid unsafe while every plane crate does; one wire error code is declared twice, once here in the shared table and once again in the plane module that reads this table by identity` |
| `crates/busbar-a2a/src` | stale | 1 | 742 | 46850 | findings (HIGH=2, MEDIUM=3, LOW=3) | opus-prod-auditor-a2a | `audits/prod-a2a.md — PARTIAL first read (wind-down): backend URL leaks into refusals; legacy pushback token never revoked; unmetered GetExtendedAgentCard/ListTasks` |
| `crates/busbar-admin/src` | unaudited | - | - | 25525 | unaudited | - | - |
| `crates/busbar-caps/src` | stale | 8 | 1210 | 2300 | findings (MEDIUM=1, LOW=4, NIT=2) | opus-p3-caps-contract-kernel | `challenge round dropped its accrual, Sessions::open displaced a live session, NestedPool::leave unbounded (all fixed); canary identity mis-specified (owner); 18/18 plants caught, 4 survivors named` |
| `crates/busbar-contract-transport/src` | stale | 6 | 1179 | 2148 | findings (HIGH=1) | opus-pass3 | `transport_registry.rs real-stack copy drifted from production (ws over http,tcp,tls; grpc over http); read fully` |
| `crates/busbar-contract/src` | stale | 8 | 1272 | 5011 | findings (MEDIUM=3, LOW=4) | opus-p3-caps-contract-kernel | `challenge round dropped its accrual, Sessions::open displaced a live session, NestedPool::leave unbounded (all fixed); canary identity mis-specified (owner); 18/18 plants caught, 4 survivors named` |
| `crates/busbar-core-config/src` | unaudited | - | - | 92 | unaudited | - | - |
| `crates/busbar-core-hooks/src` | unaudited | - | - | 2697 | unaudited | - | - |
| `crates/busbar-core/src` | stale | 2 | 1179 | 112249 | findings (HIGH=3, MEDIUM=10, LOW=6, NIT=3) | opus-pass2 | `rotate_key unbounded id (fixed), auth.mode replaced the chain (fixed), malformed pools/export replaced with empty (fixed), consent decoder panic (fixed); validate cannot see boot-only checks, inferred pool knobs discarded, oauth_as task leak per apply, overlay no interpolation reported` |
| `crates/busbar-grammar/src` | open | 6 | 1179 | 568 | findings (MEDIUM=1, LOW=1, NIT=2) | opus-pass2 | `span not readable back from the journal (fixed); skip_container punctuation; read fully` |
| `crates/busbar-kernel/src` | stale | 8 | 1210 | 5530 | findings (MEDIUM=3, LOW=3) | opus-p3-caps-contract-kernel | `challenge round dropped its accrual, Sessions::open displaced a live session, NestedPool::leave unbounded (all fixed); canary identity mis-specified (owner); 18/18 plants caught, 4 survivors named` |
| `crates/busbar-llm-codec/src` | stale | 8 | 494 | 85928 | findings (HIGH=1, LOW=1) | opus-plane-auditor-b | `a detected provider usage shortfall is computed and threaded through the whole streaming fold and then read by nobody, so a unit whose provider reported more tokens than the buckets account for is metered on the smaller number and the note saying so goes nowhere; the crate does not forbid unsafe` |
| `crates/busbar-llm/src` | stale | 1 | 742 | 59273 | findings (HIGH=2, MEDIUM=5, LOW=3) | opus-prod-auditor-llm | `audits/prod-llm.md — PARTIAL first read (wind-down): Meter step never receives a sink/hold on live units; non-SSE post-first-byte cut refunds` |
| `crates/busbar-mcp-codec/src` | stale | 8 | 494 | 2434 | findings (MEDIUM=1, LOW=1) | opus-plane-auditor-b | `the markup-normalisation floor is applied by the retiring engine and by nothing in the plane crate that replaces it, so the migration drops a control the module header calls universal and security-critical; the crate does not forbid unsafe` |
| `crates/busbar-mcp/src` | stale | 1 | 1179 | 55 | findings (HIGH=1, MEDIUM=11, LOW=12) | opus-pass1 | `bare-host format carried a url past the ssrf guard (fixed), handshake id collided with the opening handshake (fixed), call-log dispatched for calls that never left (fixed), token endpoint body unbounded (fixed), secret sources leaked to callers (fixed); notify not cancel-safe, supervisor reset never fires, listen ungated, sampling input unbounded, demotion row skipped silently reported` |
| `crates/busbar-oauth2/src` | unaudited | - | - | 2203 | unaudited | - | - |
| `crates/busbar-plane-a2a-host/src` | unaudited | - | - | 700 | unaudited | - | - |
| `crates/busbar-plane-a2a/src` | stale | 8 | 494 | 2724 | findings (HIGH=2, MEDIUM=4, LOW=1, NIT=1) | opus-plane-auditor-b | `an ordinary non-streaming answer never ends its unit (the terminal test asks only whether the result carries a kind discriminator, so every Task and Message answer reads as a mid-stream frame and an envelope with neither result nor error reads as complete); refusals outside the first twenty reasons all render as internal error; a message with no identifier is answered; approve names the first configured agent whatever the unit asked for and never the task` |
| `crates/busbar-plane-admin/src` | stale | 6 | 1179 | 0 | findings (HIGH=1, MEDIUM=1, NIT=4) | opus-pass2 | `unauthenticated admin caller answered 403 not 401 (fixed, root arm); split envelope never decodes (fixed); all src read fully` |
| `crates/busbar-plane-llm/src` | stale | 8 | 494 | 1861 | findings (MEDIUM=2, LOW=1) | opus-plane-auditor-b | `the crate carries none of the kind skeleton (no conformance module, no src tests directory, the plane implementation lives in the codec file, every submodule is public) and it names its own dialect crate as a dependency; a comment claims an allocation gate holds the answer-relay borrow and the only gate in the crate measures the request direction` |
| `crates/busbar-plane-mcp-host/src` | unaudited | - | - | 47267 | unaudited | - | - |
| `crates/busbar-plane-mcp/src` | stale | 8 | 494 | 2447 | findings (HIGH=1, MEDIUM=5, LOW=1) | opus-plane-auditor-b | `refusals outside the first twenty reasons all render as internal error, so a rate limit, an open breaker and a drain reach the caller as a node fault; an answer carrying neither result nor error is reported and billed as complete; the markup-normalisation floor the retiring engine applies has no call site here; the plane names another plane as a string literal; the round cap the session counter exists for has no reader` |
| `crates/busbar-plane-streaming/src` | unaudited | - | - | 2707 | unaudited | - | - |
| `crates/busbar-plugin/src` | stale | 2 | 663 | 8589 | findings (HIGH=0, MEDIUM=0, LOW=1) | opus-plugins-auditor-a | `11 Sonnet lens finders at e36685644; 6 raw -> 4 confirmed (0 HIGH, 1 MEDIUM, 3 LOW/NIT), 33% reject rate, 2 quote mismatches; NOT CONVERGED (no lens retired, hot/pod.rs second half unread)` |
| `crates/busbar-streaming-codec/src` | unaudited | - | - | 5534 | unaudited | - | - |
| `crates/busbar-streaming/src` | unaudited | - | - | 9153 | unaudited | - | - |
| `crates/busbar-substrate-values/src` | stale | 6 | 1179 | 10427 | findings (MEDIUM=1, LOW=1, NIT=5) | opus-pass2 | `interned-name pointer match without length (fixed); base64 trailing bits; all production files read, diagnostics catalogue checked mechanically, unit tests unread` |
| `crates/busbar-substrate/src` | stale | 8 | 1179 | 42622 | findings (MEDIUM=1, LOW=5) | opus-pass2 | `inbound ws acceptor had no message or frame ceiling (fixed); outbound ws mirror, limits install ordering, retry header restore reported; test files unread` |
| `crates/busbar-timing/src` | stale | 6 | 1179 | 923 | findings (LOW=1) | opus-pass2 | `atexit catch site named; read fully` |
| `crates/busbar-transport-grpc/src` | stale | 7 | 565 | 1895 | findings (MEDIUM=3) | opus-wire-auditor-b | `no configured body cap is read at listen or dial, so every message is pinned to the codec library default rather than the operator limit the sibling websocket wire reads for exactly this reason; a decoder failure on a served request body is reported as a network reset, discarding the status and collapsing a caller fault into the variant reserved for a broken connection when a framing variant exists; no meta, claims or conformance module exists, and the frame-meta honesty check is asserted against one correct value with no inflating or deflating fixture to prove it discriminates` |
| `crates/busbar-transport-http/src` | stale | 7 | 565 | 2494 | findings (MEDIUM=3, LOW=2) | opus-wire-auditor-b | `the chunked ingress body cap is checked one read late and is skipped entirely when the pre-loop feed completes the decode, so a cap set below the read chunk size is not enforced on that path; the egress re-segmenter omits the chunked-plus-content-length refusal its ingress twin performs, forwarding the request-smuggling shape it refuses inbound; serve is public on the mount seam, the vocabulary the kind rules forbid on a plugin surface; no meta, claims or conformance module exists; the plane-name guard duplicates its sibling byte for byte and one self-test line is always true` |
| `crates/busbar-transport-sse/src` | stale | 8 | 565 | 776 | findings (LOW=2, NIT=1) | opus-wire-auditor-b | `a line carrying no colon is treated as carrying no field, but the event-stream grammar reads such a line as a field whose value is empty, so a legal frame whose only line is a bare field name is dropped as if it were a comment keepalive; this wire names a model vendor in a doc comment and in a test name while carrying no vocabulary guard at all; the test-only reference parser trims all whitespace where the grammar strips a single leading space` |
| `crates/busbar-transport-stdio/src` | stale | 8 | 565 | 872 | findings (MEDIUM=1, LOW=1) | opus-wire-auditor-b | `the frame-meta honesty check is asserted against a single correct value with no inflating or deflating fixture to prove it discriminates a wrong byte count from a right one; two battery tests assert a reader is parked by sleeping and then checking the task is unfinished, which is equally true of a task the runtime has not polled yet, so under contention the test passes without the interleaving it documents; no meta, claims or conformance module exists` |
| `crates/busbar-transport-tcp/src` | stale | 8 | 565 | 616 | findings (LOW=2) | opus-wire-auditor-b | `no meta, claims or conformance module exists, so this wire diverges from the uniform skeleton its kind requires of every sibling; the read chunk constant is a public name shared with two siblings that give it a different value and a different meaning, one of them a contract-derived cap and this one a plain read size` |
| `crates/busbar-transport-tls/src` | stale | 8 | 565 | 925 | findings (MEDIUM=2, LOW=1) | opus-wire-auditor-b | `a peer that vanishes without the close alert surfaces as an unexpected end of file, and the io mapper has no arm for it, so it falls through to the same generic closed value a tidy shutdown produces even though this crate own doc names that signature as what a truncation looks like from the inside; no meta, claims or conformance module exists and the frame-meta honesty check has no inflating or deflating fixture; the read chunk constant is a public name shared with two siblings that give it a different value and a different meaning` |
| `crates/busbar-transport-ws/src` | stale | 8 | 565 | 1103 | findings (MEDIUM=2, LOW=1) | opus-wire-auditor-b | `no meta, claims or conformance module exists; the frame-meta honesty check is asserted against a single correct value with no inflating or deflating fixture to prove it can go red, unlike three of its siblings; a battery test asserts a reader is parked by sleeping and then checking the task is unfinished, which is also true of a task that was never polled, so the interleaving it claims to establish may never occur` |
| `crates/busbar-unit-admission/src` | stale | 6 | 565 | 2083 | findings (MEDIUM=2, LOW=2) | opus-units-auditor-b | `the door keeps a second hand-written copy of the cents projection and of the nano-per-cent constant while already delegating the rate conversion to the crate that owns the pricing law; the shipped cell store implements none of the three duties its own trait doc assigns a store, so its per-bucket map holds an entry per principal ever seen and the last-used field provided for eviction is read by nothing` |
| `crates/busbar-unit-audit/src` | stale | 8 | 565 | 2837 | in_progress | opus-units-auditor-b | `chain digesting, amendment and the legacy ring were read and nothing survived triage; three lenses left scope unread, so this is a sample` |
| `crates/busbar-unit-auth/src` | stale | 6 | 565 | 1280 | findings (MEDIUM=2, LOW=1) | opus-units-auditor-b | `the admin scope mirror re-implements the scope unit's rungs with no cross-crate test holding the two answers together, unlike the breaker mirror next door which has one; the credential cache builds an owned key on every lookup including the hit it exists to make cheap; the crate's dialect ladder spells one dialect differently from the plane's claim table and the egress-auth header table` |
| `crates/busbar-unit-breaker/src` | stale | 6 | 565 | 2102 | findings (HIGH=1, MEDIUM=1, LOW=1) | opus-units-auditor-b | `an upstream failure the path could put no number on falls through the status ladder into the arm meant for an unexpected success and is recorded as the caller's fault, so a destination failing that way never trips its cell and keeps taking full traffic; the four per-destination maps insert and never remove, so retired destinations stay resident for the life of the process; a reopen journals a cooldown re-read after its own transition lock was released` |
| `crates/busbar-unit-cost/src` | stale | 6 | 565 | 1510 | findings (MEDIUM=1) | opus-units-auditor-b | `pricing one settled unit heap-allocates a string per usage line and one more for the fee line, from a class name that is already static, on a path the crate's own allocation case does not cover` |
| `crates/busbar-unit-egress-auth/src` | stale | 6 | 565 | 1145 | in_progress | opus-units-auditor-b | `the signer's canonicalization and key derivation were checked against the published algorithm and matched; the conformance reading covered a fifth of the scope, so this is a sample` |
| `crates/busbar-unit-egress/src` | stale | 6 | 565 | 3235 | findings (MEDIUM=1, LOW=2) | opus-units-auditor-b | `the pool name is copied into an owned string on every attempt and again on delivery, so a failover pays for it several times per request; the outbound request that carries the substituted cleartext credential derives its debug output where the sibling credential type hand-writes one that prints presence only; the pick rebuilds the whole candidate list on every retry` |
| `crates/busbar-unit-ledger/src` | stale | 8 | 565 | 3240 | findings (HIGH=1, MEDIUM=1) | opus-units-auditor-b | `the book of balances grows a row per bucket, dimension, scope and window for the life of the process and the one function that retires a sealed window has no caller outside its own cases; separately every mutation and every read clones a string-owning key to reach a map entry that in the common case inserts nothing, justified by a comment saying the identifier has no total order when it does` |
| `crates/busbar-unit-scope/src` | stale | 6 | 565 | 829 | in_progress | opus-units-auditor-b | `read as the authority the auth unit's admin mirror defers to; nothing survived triage in what was opened` |
| `crates/busbar-unit-transport-key/src` | in_progress | 8 | 565 | 1055 | in_progress | opus-units-auditor-b | `the default protocol list and the config builder's own literal are pinned by two cases that would go red on a change, so the documented identity between them holds; nothing survived triage` |
| `crates/busbar-unit-trust/src` | stale | 6 | 565 | 2986 | findings (LOW=1) | opus-units-auditor-b | `the per-hop candidate list is rebuilt in full on every retry inside the pick loop; the weighted walk's credits are bounded by the algorithm's own invariant, so the plain addition there is not the hazard it looks like` |
| `crates/busbar-unit-usage/src` | stale | 6 | 565 | 949 | findings (LOW=1) | opus-units-auditor-b | `the locator floor's lower band truncates to zero and can never fire for a kernel floor smaller than the configured ratio, while the upper band works at every size` |
| `crates/busbar-unit-verbs/src` | stale | 8 | 565 | 2497 | in_progress | opus-units-auditor-b | `the mint plan, idempotency probe, posture and rate paths were read and nothing survived triage; the group-name ceiling sits at the provisioning door by design` |
| `crates/busbar-unit-wal/src` | open | 8 | 565 | 2906 | in_progress | opus-units-auditor-b | `segment, journal, recovery and shipping were read and nothing survived triage; three lenses left scope unread, so this is a sample` |
| `crates/busbar/build.rs` | stale | 1 | 663 | 90 | findings (MEDIUM=1, LOW=1) | opus-examples-auditor-a | `pgo=true in the build-provenance stamp can be faked by setting an env var that no shipping build path sets any more, and the stamp's target-cpu/target-features/lto substrings are spliced in unvalidated despite the file's own no-spaces invariant` |
| `crates/busbar/src` | unaudited | - | - | 42 | unaudited | - | - |
| `crates/busbar/src/main.rs` | stale | 6 | 1179 | 2379 | findings (HIGH=1, MEDIUM=1, LOW=1) | opus-pass2 | `shipped voice stub table refuses every client tool reply (critical, fix dispatched); mount_root_voice discards the seal; three load-bearing doc comments false; read fully` |
| `crates/busbar/src/root` | stale | 7 | 1179 | 33358 | findings (HIGH=8, MEDIUM=5, LOW=2) | opus-pass3 | `node_book ships a null shipper with no data_dir (critical); migration::run never called; identity only proven at the standard tier; no verifier or revocation view bound; transport provisioning has no caller; late-accrual path has zero coverage; a2a redeem passes ts as expiry; fix agents dispatched` |
| `crates/export-example-plugin/src` | stale | 1 | 663 | 73 | findings (LOW=1) | opus-examples-auditor-a | `header falsely claims to mirror the store example's config-less posture, but that store parses and acts on a config key that switches its backend to a disk-backed store` |
| `crates/hook-test-plugin/src` | stale | 1 | 663 | 587 | zero | opus-examples-auditor-a | `zero; read fully` |
| `crates/hooks-ranking/src` | stale | 1 | 663 | 259 | zero | opus-examples-auditor-a | `zero; read fully` |
| `crates/plane-example/src` | unaudited | - | - | 196 | unaudited | - | - |
| `crates/plugin-loader/src` | stale | 7 | 742 | 6795 | findings (HIGH=4, MEDIUM=15, LOW=24) | opus-prod-auditor-plugins | `audits/prod-plugins.md — PARTIAL first read of the plugin toolchain, counts are the combined toolchain total: bare repr(u8) enums decoded from plugin memory; one-sided sized-struct guard; docs promise an anti-downgrade floor evaluate() lacks` |
| `crates/plugin-pack/src` | stale | 2 | 663 | 603 | findings (HIGH=0, MEDIUM=0, LOW=1) | opus-plugins-auditor-a | `11 Sonnet lens finders at e36685644; 6 raw -> 4 confirmed (0 HIGH, 1 MEDIUM, 3 LOW/NIT), 33% reject rate, 2 quote mismatches; NOT CONVERGED (no lens retired, hot/pod.rs second half unread)` |
| `crates/plugin-sdk/src` | stale | 7 | 663 | 1540 | findings (HIGH=0, MEDIUM=0, LOW=1) | opus-plugins-auditor-a | `11 Sonnet lens finders at e36685644; 6 raw -> 4 confirmed (0 HIGH, 1 MEDIUM, 3 LOW/NIT), 33% reject rate, 2 quote mismatches; NOT CONVERGED (no lens retired, hot/pod.rs second half unread)` |
| `crates/plugin-sign/src` | stale | 2 | 663 | 893 | zero (HIGH=0, MEDIUM=0, LOW=0) | opus-plugins-auditor-a | `11 Sonnet lens finders at e36685644; 6 raw -> 4 confirmed (0 HIGH, 1 MEDIUM, 3 LOW/NIT), 33% reject rate, 2 quote mismatches; NOT CONVERGED (no lens retired, hot/pod.rs second half unread)` |
| `crates/plugin-testkit/src` | stale | 2 | 663 | 984 | findings (HIGH=0, MEDIUM=1, LOW=0) | opus-plugins-auditor-a | `11 Sonnet lens finders at e36685644; 6 raw -> 4 confirmed (0 HIGH, 1 MEDIUM, 3 LOW/NIT), 33% reject rate, 2 quote mismatches; NOT CONVERGED (no lens retired, hot/pod.rs second half unread)` |
| `crates/secret-example-plugin/src` | stale | 1 | 663 | 160 | zero | opus-examples-auditor-a | `zero; read fully` |
| `crates/secret-ref/src` | stale | 1 | 742 | 431 | findings (HIGH=4, MEDIUM=15, LOW=24) | opus-prod-auditor-plugins | `audits/prod-plugins.md — PARTIAL first read of the plugin toolchain, counts are the combined toolchain total: bare repr(u8) enums decoded from plugin memory; one-sided sized-struct guard; docs promise an anti-downgrade floor evaluate() lacks` |
| `crates/store-example-plugin/src` | stale | 1 | 663 | 999 | findings (MEDIUM=2, NIT=1) | opus-examples-auditor-a | `depends on a sibling instance of its own kind even though the normative spec forbids that absolutely for any kind ('no sibling instance, ever'), which only passes because the gate's manifest allowlist waives it by name; redeem_plane_token makes every plane token except 'ask' infinitely replayable via the trait's default, the opposite of the in-tree store backend's own documented anti-replay stance; and its tests live at a different path than every other crate in the unit uses` |
| `crates/store-memory/src` | stale | 1 | 663 | 871 | findings (MEDIUM=1) | opus-examples-auditor-a | `the map backing the contract's record verbs has no sweep, ticker, TTL or delete verb -- unlike every other map in the file -- so it grows without bound for the life of a long-lived process` |
| `xtask/src` | stale | 3 | 565 | 75315 | findings (HIGH=5, MEDIUM=10, LOW=7, NIT=1) | opus-gates-auditor-c | `ten lens finders at this hash; 35 raw -> 33 distinct -> 22 confirmed (5 HIGH), 15 percent reject rate, 0 quote mismatches; a mistyped gate flag is ignored so a release arm reports green having checked less; three walks read a failed or empty scan as a clean tree; two sites abort the process where the design says refuse; a corrupt register makes ledger sync overwrite the audit history on exit 0; four defects are repeats from earlier rounds; NOT CONVERGED, no lens retired, the four largest gates unread` |

## test scopes (81)

| scope | status | round | age (commits) | LOC | result | auditor | report |
| --- | --- | ---: | ---: | ---: | --- | --- | --- |
| `crates/api/src/tests` | stale | 1 | 929 | 1917 | findings (severities unrecorded) | opus-test-auditor-root | `busbar-landq-state/gate/audits/tests-root.md` |
| `crates/api/tests` | unaudited | - | - | 883 | unaudited | - | - |
| `crates/auth-admin-tokens/src/tests` | unconfirmed | 1 | 663 | 56 | zero | opus-examples-auditor-a | `zero; read fully` |
| `crates/auth-admin-tokens/tests` | unaudited | - | - | 104 | unaudited | - | - |
| `crates/auth-static-plugin/src/tests` | stale | 1 | 663 | 129 | zero | opus-examples-auditor-a | `zero; read fully` |
| `crates/busbar-a2a-codec/src/tests` | stale | 1 | 929 | 208 | findings (severities unrecorded) | opus-test-auditor-planes | `busbar-landq-state/gate/audits/tests-planes.md` |
| `crates/busbar-admin/src/tests` | unaudited | - | - | 17166 | unaudited | - | - |
| `crates/busbar-caps/src/tests` | unaudited | - | - | 2193 | unaudited | - | - |
| `crates/busbar-contract-transport/src/tests` | unaudited | - | - | 34 | unaudited | - | - |
| `crates/busbar-contract-transport/tests` | stale | 1 | 929 | 1497 | findings (severities unrecorded) | opus-test-auditor-kernel | `busbar-landq-state/gate/audits/tests-kernel.md` |
| `crates/busbar-contract/tests` | stale | 1 | 929 | 3256 | findings (severities unrecorded) | opus-test-auditor-kernel | `busbar-landq-state/gate/audits/tests-kernel.md` |
| `crates/busbar-core-hooks/src/tests` | unaudited | - | - | 4157 | unaudited | - | - |
| `crates/busbar-core/benches` | stale | 1 | 930 | 495 | findings (severities unrecorded) | opus-test-auditor-core | `busbar-landq-state/gate/audits/tests-core.md` |
| `crates/busbar-core/src/tests` | stale | 1 | 930 | 11174 | findings (HIGH=0, MEDIUM=4, LOW=9) | opus-test-auditor-core | `busbar-landq-state/gate/audits/tests-core.md` |
| `crates/busbar-core/tests` | stale | 1 | 930 | 921 | findings (severities unrecorded) | opus-test-auditor-core | `busbar-landq-state/gate/audits/tests-core.md` |
| `crates/busbar-grammar/tests` | stale | 1 | 929 | 876 | findings (severities unrecorded) | opus-test-auditor-kernel | `busbar-landq-state/gate/audits/tests-kernel.md` |
| `crates/busbar-kernel/tests` | stale | 1 | 929 | 5775 | findings (HIGH=0, MEDIUM=12, LOW=5) | opus-test-auditor-kernel | `busbar-landq-state/gate/audits/tests-kernel.md` |
| `crates/busbar-llm-codec/src/tests` | stale | 1 | 929 | 17725 | findings (HIGH=8, MEDIUM=25, LOW=9) | opus-test-auditor-llm-codec | `busbar-landq-state/gate/audits/tests-llm-codec.md` |
| `crates/busbar-llm/src/tests` | stale | 1 | 929 | 649 | findings (severities unrecorded) | opus-test-auditor-llm-codec | `busbar-landq-state/gate/audits/tests-llm-codec.md` |
| `crates/busbar-mcp-codec/src/tests` | stale | 1 | 929 | 726 | findings (severities unrecorded) | opus-test-auditor-planes | `busbar-landq-state/gate/audits/tests-planes.md` |
| `crates/busbar-oauth2/src/tests` | unaudited | - | - | 1871 | unaudited | - | - |
| `crates/busbar-plane-a2a-host/src/tests` | unaudited | - | - | 160 | unaudited | - | - |
| `crates/busbar-plane-a2a/src/tests` | stale | 1 | 494 | 1534 | findings (LOW=1) | opus-plane-auditor-b | `the session-counter test increments the field itself and then asserts the result, so it passes with the production increment deleted; the refusal totality test enumerates twenty of forty-two reasons by hand against a match that carries a catch-all arm, so it can never force the list to grow` |
| `crates/busbar-plane-a2a/tests` | stale | 2 | 494 | 1957 | findings (LOW=1) | opus-plane-auditor-b | `the only allocation gate measures the request direction, so nothing holds the per-frame relay path the arena reset is pinned for; no test feeds an ordinary successful answer through the response decoder, which is what let the terminal test ship wrong in both directions` |
| `crates/busbar-plane-admin/src/tests` | unaudited | - | - | 0 | unaudited | - | - |
| `crates/busbar-plane-llm/tests` | stale | 2 | 494 | 3222 | findings (LOW=1, NIT=1) | opus-plane-auditor-b | `the only allocation gate measures the request direction while a comment in the source claims it holds the answer relay; the dialect-envelope test covers six of forty-two refusal reasons, which is coverage only because the mapping it guards is exhaustive` |
| `crates/busbar-plane-mcp-host/src/tests` | unaudited | - | - | 233 | unaudited | - | - |
| `crates/busbar-plane-mcp/src/tests` | stale | 1 | 494 | 1211 | findings (LOW=1) | opus-plane-auditor-b | `the session-counter test increments the field itself and then asserts the result, so it passes with the production increment deleted; the refusal totality test enumerates twenty of forty-two reasons by hand against a match that carries a catch-all arm, so it can never force the list to grow` |
| `crates/busbar-plane-mcp/tests` | stale | 2 | 494 | 1943 | findings (LOW=1) | opus-plane-auditor-b | `the only allocation gate calls no plane method at all, so nothing holds the per-frame relay path the arena reset is pinned for` |
| `crates/busbar-plane-streaming/src/tests` | unaudited | - | - | 1723 | unaudited | - | - |
| `crates/busbar-plane-streaming/tests` | unaudited | - | - | 186 | unaudited | - | - |
| `crates/busbar-plugin/src/tests` | unconfirmed | 2 | 663 | 166 | zero (HIGH=0, MEDIUM=0, LOW=0) | opus-plugins-auditor-a | `tests lens (Sonnet) read all 4,592 test lines in full; zero findings IN the test modules (the one MEDIUM is in plugin-testkit/src production battery). Not retired: Sonnet, not opus` |
| `crates/busbar-plugin/tests` | open | 1 | 929 | 1104 | findings (severities unrecorded) | opus-test-auditor-planes | `busbar-landq-state/gate/audits/tests-planes.md` |
| `crates/busbar-streaming-codec/tests` | unaudited | - | - | 110 | unaudited | - | - |
| `crates/busbar-streaming/src/tests` | unaudited | - | - | 2493 | unaudited | - | - |
| `crates/busbar-substrate-values/src/tests` | stale | 1 | 929 | 3179 | findings (severities unrecorded) | opus-test-auditor-substrate | `busbar-landq-state/gate/audits/tests-substrate.md` |
| `crates/busbar-substrate/src/tests` | stale | 1 | 929 | 1140 | findings (HIGH=1, MEDIUM=3, LOW=3) | opus-test-auditor-substrate | `busbar-landq-state/gate/audits/tests-substrate.md` |
| `crates/busbar-substrate/tests` | stale | 1 | 929 | 337 | findings (severities unrecorded) | opus-test-auditor-substrate | `busbar-landq-state/gate/audits/tests-substrate.md` |
| `crates/busbar-timing/src/tests` | open | 1 | 929 | 66 | findings (severities unrecorded) | opus-test-auditor-kernel | `busbar-landq-state/gate/audits/tests-kernel.md` |
| `crates/busbar-timing/tests` | stale | 1 | 929 | 51 | findings (severities unrecorded) | opus-test-auditor-kernel | `busbar-landq-state/gate/audits/tests-kernel.md` |
| `crates/busbar-transport-grpc/src/tests` | stale | 2 | 565 | 2366 | findings (MEDIUM=1) | opus-wire-auditor-b | `read fully; the frame-meta honesty check is a single equality against one hardcoded payload length, so a metering regression returning a constant that happens to match it would ship green, and no test perturbs a real frame to prove the check discriminates; half-close, two-level composition with a bottom-layer credential, in-band upgrade with cleared facts and the emission clock have no test` |
| `crates/busbar-transport-grpc/tests` | stale | 1 | 565 | 208 | findings (MEDIUM=1, LOW=1) | opus-wire-auditor-b | `the guard that keeps a wire from naming a plane holds a word list with no model vendor name and no billing word, so it cannot fail on the dialect class it is named for; one self-test line is a disjunction whose right half is a literal that is always true, so that case cannot fail whatever the matcher returns; the file is byte-identical to another crate copy rather than shared from one place` |
| `crates/busbar-transport-http/src/tests` | stale | 1 | 565 | 2849 | findings (MEDIUM=1) | opus-wire-auditor-b | `read fully; this crate is one of only three whose frame-meta honesty test perturbs a real frame both ways and asserts the check goes red, and that shape is the one four sibling wires lack; the battery leaves half-close, backpressure, two-level composition with a bottom-layer credential, in-band upgrade with cleared facts, multiplexed streams, K writers, overlap cases and the emission clock with no test` |
| `crates/busbar-transport-http/tests` | stale | 1 | 565 | 212 | findings (MEDIUM=1, LOW=1) | opus-wire-auditor-b | `the guard that keeps a wire from naming a plane holds a word list that contains no model vendor name and no billing word, so it cannot fail on the dialect class it is named for, and a live instance of that class sits in a sibling wire; one self-test line is a disjunction whose right half is a literal that is always true, so that case cannot fail; the file is byte-identical to another crate copy rather than shared` |
| `crates/busbar-transport-sse/src/tests` | stale | 1 | 565 | 1041 | findings (NIT=1) | opus-wire-auditor-b | `read fully; this crate is one of three that does perturb a real frame both ways to prove the frame-meta honesty check goes red; a test name and a doc comment in the crate under test name model vendors, which no vocabulary guard here would catch because this wire carries none; half-close, cancel mid-frame, K writers, multiplexed streams, overlap cases and the emission clock have no test` |
| `crates/busbar-transport-stdio/src/tests` | stale | 2 | 565 | 1038 | findings (MEDIUM=1, LOW=1) | opus-wire-auditor-b | `read fully; two tests establish that a reader is parked by sleeping fifty milliseconds and asserting the task is unfinished, which is equally true of a task the runtime has not polled, so on a contended runner the fixture the message calls honest never holds and the test still passes; the frame-meta honesty check has no inflating or deflating fixture; in-band upgrade with cleared facts, multiplexed streams, overlap cases and the emission clock have no test` |
| `crates/busbar-transport-tcp/src/tests` | unaudited | - | - | 958 | unaudited | - | - |
| `crates/busbar-transport-tls/src/tests` | unaudited | - | - | 1963 | unaudited | - | - |
| `crates/busbar-transport-ws/src/tests` | stale | 2 | 565 | 1327 | findings (MEDIUM=1, LOW=1) | opus-wire-auditor-b | `read fully; the frame-meta honesty check is a single equality with no fixture proving it discriminates an inflated or deflated count; a test presumes the pump is suspended in its socket read purely because twenty milliseconds elapsed after spawning it, with no primitive proving it was ever polled, so the wake-a-suspended-read path it documents may not be exercised; two-level composition with a bottom-layer credential, overlap cases and the emission clock have no test` |
| `crates/busbar-unit-admission/src/tests` | open | 2 | 565 | 3107 | in_progress | opus-units-auditor-b | `read for tautologies and shared-fixture races; none found in what was opened` |
| `crates/busbar-unit-audit/src/tests` | stale | 2 | 565 | 3685 | in_progress | opus-units-auditor-b | `read for tautologies and shared-fixture races; none found in what was opened` |
| `crates/busbar-unit-auth/src/tests` | stale | 2 | 565 | 1556 | in_progress | opus-units-auditor-b | `left unread by every lens this round; the production surface was read instead` |
| `crates/busbar-unit-breaker/src/tests` | stale | 1 | 565 | 1923 | in_progress | opus-units-auditor-b | `left unread by every lens this round; the production surface was read instead` |
| `crates/busbar-unit-cost/src/tests` | stale | 2 | 565 | 1622 | in_progress | opus-units-auditor-b | `read for tautologies and shared-fixture races; none found in what was opened` |
| `crates/busbar-unit-cost/tests` | stale | 2 | 565 | 326 | findings (MEDIUM=1) | opus-units-auditor-b | `the allocation case measures the rate lookup only, so the function that prices a settled unit allocates outside anything the case can see` |
| `crates/busbar-unit-egress/src/tests` | stale | 2 | 565 | 3824 | in_progress | opus-units-auditor-b | `left unread by every lens this round; the production surface was read instead` |
| `crates/busbar-unit-egress/tests` | stale | 2 | 565 | 610 | in_progress | opus-units-auditor-b | `the cross-crate breaker adapter case was read and is a real agreement test, not one that passes by construction` |
| `crates/busbar-unit-ledger/src/tests` | stale | 2 | 565 | 2779 | in_progress | opus-units-auditor-b | `read for tautologies and shared-fixture races; none found in what was opened` |
| `crates/busbar-unit-trust/src/tests` | stale | 2 | 565 | 3509 | in_progress | opus-units-auditor-b | `sampled for tautologies and shared-fixture races; none found in what was opened` |
| `crates/busbar-unit-trust/tests` | open | 2 | 565 | 113 | in_progress | opus-units-auditor-b | `the weighted-allocation case was read and measures what it claims to` |
| `crates/busbar-unit-usage/src/tests` | open | 2 | 565 | 1115 | in_progress | opus-units-auditor-b | `read for tautologies and shared-fixture races; none found in what was opened` |
| `crates/busbar-unit-verbs/src/tests` | stale | 2 | 565 | 3622 | in_progress | opus-units-auditor-b | `read for tautologies and shared-fixture races; none found in what was opened` |
| `crates/busbar-unit-wal/src/tests` | stale | 2 | 565 | 2518 | in_progress | opus-units-auditor-b | `read for tautologies and shared-fixture races; none found in what was opened` |
| `crates/busbar/benches` | unaudited | - | - | 523 | unaudited | - | - |
| `crates/busbar/src/tests` | unaudited | - | - | 760 | unaudited | - | - |
| `crates/busbar/tests` | stale | 1 | 929 | 12168 | findings (HIGH=15, MEDIUM=38, LOW=13) | opus-test-auditor-root | `busbar-landq-state/gate/audits/tests-root.md` |
| `crates/hooks-ranking/src/tests` | unconfirmed | 1 | 663 | 390 | zero | opus-examples-auditor-a | `zero; read fully` |
| `crates/hooks-ranking/tests` | unaudited | - | - | 136 | unaudited | - | - |
| `crates/plugin-loader/src/tests` | unaudited | - | - | 8889 | unaudited | - | - |
| `crates/plugin-loader/tests` | unaudited | - | - | 175 | unaudited | - | - |
| `crates/plugin-pack/src/tests` | unconfirmed | 1 | 663 | 883 | zero (HIGH=0, MEDIUM=0, LOW=0) | opus-plugins-auditor-a | `tests lens (Sonnet) read all 4,592 test lines in full; zero findings IN the test modules (the one MEDIUM is in plugin-testkit/src production battery). Not retired: Sonnet, not opus` |
| `crates/plugin-sdk/src/tests` | stale | 1 | 663 | 1145 | zero (HIGH=0, MEDIUM=0, LOW=0) | opus-plugins-auditor-a | `tests lens (Sonnet) read all 4,592 test lines in full; zero findings IN the test modules (the one MEDIUM is in plugin-testkit/src production battery). Not retired: Sonnet, not opus` |
| `crates/plugin-sdk/tests` | unaudited | - | - | 402 | unaudited | - | - |
| `crates/plugin-sign/src/tests` | stale | 1 | 663 | 1205 | zero (HIGH=0, MEDIUM=0, LOW=0) | opus-plugins-auditor-a | `tests lens (Sonnet) read all 4,592 test lines in full; zero findings IN the test modules (the one MEDIUM is in plugin-testkit/src production battery). Not retired: Sonnet, not opus` |
| `crates/plugin-testkit/src/tests` | unconfirmed | 1 | 663 | 99 | zero (HIGH=0, MEDIUM=0, LOW=0) | opus-plugins-auditor-a | `tests lens (Sonnet) read all 4,592 test lines in full; zero findings IN the test modules (the one MEDIUM is in plugin-testkit/src production battery). Not retired: Sonnet, not opus` |
| `crates/secret-ref/src/tests` | unaudited | - | - | 399 | unaudited | - | - |
| `crates/secret-ref/tests` | unaudited | - | - | 105 | unaudited | - | - |
| `crates/store-example-plugin/src/tests` | unaudited | - | - | 1016 | unaudited | - | - |
| `crates/store-memory/src/tests` | stale | 1 | 663 | 929 | zero | opus-examples-auditor-a | `zero; read fully` |
| `crates/store-memory/tests` | unaudited | - | - | 509 | unaudited | - | - |
| `xtask/tests` | unaudited | - | - | 3562 | unaudited | - | - |

## instrument scopes (19)

| scope | status | round | age (commits) | LOC | result | auditor | report |
| --- | --- | ---: | ---: | ---: | --- | --- | --- |
| `.githooks` | unaudited | - | - | 16 | unaudited | - | - |
| `.github` | unaudited | - | - | 682 | unaudited | - | - |
| `.github/scripts` | unaudited | - | - | 58 | unaudited | - | - |
| `.github/workflows` | stale | 3 | 1033 | 16498 | findings (HIGH=4, MEDIUM=3, LOW=3) | opus-instr-p3 | `instrument fresh eyes with plants: false-green paths closed (fail-open guards, stale reports graded, zero-of-zero passes); remaining rows that pass against nothing named for a ruling` |
| `assets/readme` | unaudited | - | - | 3122 | unaudited | - | - |
| `bin` | unaudited | - | - | 46 | unaudited | - | - |
| `examples` | open | 1 | 663 | 996 | findings (MEDIUM=1, LOW=2) | opus-examples-auditor-a | `a router example's copy-paste config names a module that does not exist for the in-process shape it claims, when the shipped binary is actually an out-of-process socket server needing different config; its two reference sidecars read peer input with no size cap; and a usage script neither enables strict shell error handling nor checks its cloud CLI's exit status, so a failing query silently reads as empty` |
| `qa` | stale | 2 | 1033 | 41801 | findings (HIGH=2, MEDIUM=4, LOW=2, NIT=3) | opus-instr-p3-construction | `construction gate fresh eyes: eight rules wrote FAIL into a ledger nobody read, the only gating legacy-reach ratchet and two doubles rows were never owed (now RED at 95 vs 92), seven stale unsafe waivers each forgave a paid debt, two waiver families invisible to the stale-waiver rule, three more rules pass on a renamed subject; 45/45 rules planted` |
| `scripts` | stale | 5 | 1033 | 39003 | findings (HIGH=20, MEDIUM=14, LOW=1) | opus-instr-p3-scripts-mz | `scripts m-z fresh eyes: six gates guarded their scan root but not their subject, the queue lander only ever landed the first commit, a secret gate nothing runs, the PGO proof asserted a flag not the artifact, a prose comment satisfied the plugin registry check; 37 plants, every survivor closed with a red proof; 33 fixes` |
| `testing` | unaudited | - | - | 320 | unaudited | - | - |
| `testing/a2a-governance` | open | 3 | 1033 | 305 | findings (MEDIUM=1) | opus-instr-p3 | `instrument fresh eyes with plants: false-green paths closed (fail-open guards, stale reports graded, zero-of-zero passes); remaining rows that pass against nothing named for a ruling` |
| `testing/a2a-harness` | stale | 3 | 1033 | 11453 | findings (HIGH=3, MEDIUM=2) | opus-instr-p3 | `instrument fresh eyes with plants: false-green paths closed (fail-open guards, stale reports graded, zero-of-zero passes); remaining rows that pass against nothing named for a ruling` |
| `testing/a2a-supplement` | stale | 3 | 1033 | 4036 | findings (HIGH=1, MEDIUM=1) | opus-instr-p3 | `instrument fresh eyes with plants: false-green paths closed (fail-open guards, stale reports graded, zero-of-zero passes); remaining rows that pass against nothing named for a ruling` |
| `testing/a2a-tck` | stale | 3 | 1033 | 1921 | findings (HIGH=1, MEDIUM=2) | opus-instr-p3 | `instrument fresh eyes with plants: false-green paths closed (fail-open guards, stale reports graded, zero-of-zero passes); remaining rows that pass against nothing named for a ruling` |
| `testing/fleet-fixtures` | stale | 2 | 1033 | 2014 | findings (MEDIUM=1, LOW=1) | opus-instr-p3-fleet | `fleet fixtures fresh eyes: verdict survived twelve plants; the default ledger path is fixed and never truncated for ad-hoc runs; last-row-wins is documented` |
| `testing/llm-conformance` | stale | 3 | 1033 | 2942 | findings (HIGH=1, MEDIUM=2, LOW=2, NIT=3) | opus-instr-p3-conformance | `conformance rig fresh eyes: a drifted spec cache decided verdicts (a body missing a required member passed), gaps had no ceiling, outcome and status were never reconciled; 23 plants, 2 survivors fixed` |
| `testing/mcp-conformance` | stale | 3 | 1033 | 5848 | findings (HIGH=4, MEDIUM=3, LOW=2) | opus-instr-p3 | `instrument fresh eyes with plants: false-green paths closed (fail-open guards, stale reports graded, zero-of-zero passes); remaining rows that pass against nothing named for a ruling` |
| `testing/shadow-oracle` | stale | 7 | 742 | 61719 | findings (HIGH=11, MEDIUM=14, LOW=10) | opus-oracle-auditor-b | `audits/oracle-tool-b.md — PARTIAL, tool busbar-oracle v0.2.3: harness-skew guard is one editable field; effects.egress hardcoded [] on concurrent cells; a cell PASS is gated on a file existing, three drivers cannot go red` |
| `testing/streaming-conformance` | unaudited | - | - | 1690 | unaudited | - | - |

## Statuses

- `invalid` -- the register entry is not readable -- an unknown result or severity
- `open` -- findings recorded, no fix stamped
- `unaudited` -- never audited
- `stale` -- code changed since the audit
- `fixed` -- findings fixed, owes a confirming round
- `unconfirmed` -- one zero on this tree -- owes a second, independent zero
- `clean` -- two auditors read this tree and both found nothing
- `in_progress` -- a round is running against a recorded tree hash
