# THE MAP — 1.6.0

**What this file is.** The complete list of what stands between HEAD and 1.6.0 dev-green, and the
proof that the list is complete. Two numbers live here and nowhere else:

1. **MAP %** — how much of the corpus has been swept with an instrument proven able to return NO.
2. **X** — the count of findings. Green is `X = 0`.

When the map reaches 100% it is **LOCKED**: no new sweeps, only `X → 0`. Anything discovered after
the lock is a defect *in this map* and is recorded as such, not quietly appended.

---

## 0. WHAT BUSBAR 1.6.0 IS, IN ONE SENTENCE

> A customer can teach busbar a new protocol by dropping a file into a folder — and busbar's core
> does not know that protocol exists.

Everything else is a consequence: core is a thin engine naming zero plane types; every plugin obeys
the same two rules whatever its kind; money is exactly right to a bank auditor's standard; and the
customer sees no difference from 1.5.5 unless the owner signed off on a specific change.

The 23 groups in `scripts/verify-1.6.0-done.sh` are **how we check** that sentence is true. They are
not the goal. If someone splits a group tomorrow there are 24 and nothing about busbar changed.

---

## 1. THE DEFECT CLASS — "HALF HERE, HALF THERE"

Every finding in this release has had the same shape: **a broken pair.** One half exists, is
correct, and is tested. The other half is missing, stale, or different. The code looks right
wherever you open it; the bug is that the *other* copy is the one that runs.

| the pair | proven instance |
|---|---|
| definition ↔ call site | the rate clamp is at `busbar-kernel-ledger/src/cost/rate.rs:40-47`, absent at `busbar-kernel/src/cost.rs:138` — and `:138` is the enforcement path |
| capability ↔ construction | `signing_with` (`busbar-kernel-audit/src/record.rs:356`) has zero non-test callers; the audit chain ships unsigned |
| declaration ↔ wiring | `reachability` gate: 19 rows, incl. "a module of the composition root is reached by nothing" |
| old name ↔ new name | #18 renamed the plane to `streaming`; the gate still prints `(on disk: voice)` |
| writer ↔ reader | `qa/reachability-evidence.md` is named only inside three `format!` strings — no reader at all |
| unit A ↔ unit B | `/usage` speaks `spend_micros`; `/keys/{id}/usage` speaks `spend_cents`. 10,000× |
| removed ↔ still counted | `pure_auth`/`egress_auth` still spelled in 3 gate sites after #3 collapsed them to `auth` — so the auth kind is scanned as zero crates |
| gate scope ↔ tree | `qa/audit-ledger.json` carries 24 scope paths that do not exist; the gate is green over all of them |
| doc ↔ doc | spec rule (1) at `:65` vs DECISION #30 "SUPERSEDED" at `:354`; spec says 22 done-oracle groups, the script declares 23 |
| branch ↔ trunk | the NAT64 `64:ff9b::/96` SSRF fix, written on a branch, lost in the W2/W4 fold |

This is why "read every file" is the wrong frame and this is the right one: the map is not a tour of
the corpus, it is an **enumeration of broken pairs**, and each pair-type has a command that finds all
of them.

---

## 2. THE TWO LAWS OF MEASUREMENT

Everything in this file obeys both. They are not style; they are why the earlier maps were wrong.

> **An instrument that cannot produce a NO is not a check.**
> Zero is the passing answer to every ban. A gate that scans no files passes. A grep with a typo
> finds nothing and looks clean. Before any count is believed, hold one known instance out of the
> instrument's reach and confirm it reports the absence. 11+ proven instances of this class.

> **A capability that is never constructed is a capability that does not ship.**
> Its tests prove the code works, not that it runs. **A test suite cannot detect this class**, because
> the test constructs the capability itself. Only a trace from the composition root can.

Two corollaries earned the hard way in this repo:

- **Never `head`/`tail` a result whose total you are about to report.** A truncated grep hid a struct
  definition and nearly produced a false "this type does not exist". A gate truncates offenders in
  *path order*, so an 8-item sample is structurally incapable of showing a distribution — an 8-item
  read was reported as "7 of 8"; the truth over all 113 was 36.
- **A silent tool failure and a real zero must never be the same output.** `git diff
  --pathspec-from-file` exited 129 with empty stdout, which read as "identical to HEAD" for all 95
  branches. `set -o pipefail`, and check exit codes separately from output.

**No published COUNT IN in this project has ever reproduced at its own pin.** S2b's 33 was 25. S2c's
44 was 42. S3's 242 was never enumerable. S4's 2,444 was two rows high. S10's 15 is now 5. Re-derive
every number; never inherit one.

---

## 3. THE 22 SWEEPS — MAP PROGRESS

A sweep is **DONE** when it has a number *and* a falsification proof. Not when an agent says it
looked.

| # | sweep | scope | status | findings |
|---|---|---|---|---|
| 1 | S2b oracle-coverage | reconcile vs LEDGER | **DONE** | ❌ UNRECONCILED — **7** |
| 2 | S2c duplication-census | recovered from `b873e599c^` | **DONE** | ✅ RECONCILES — 0 |
| 3 | S3 commit residue | 307 commits, bounded | **DONE** | aggregate UNRECONCILABLE; sample 71% — **12** |
| 4 | S4 qa/*.toml ratchets | ceilings, waivers, held reds | **DONE** | ✅ RECONCILES — **12** uncovered |
| 5 | S9 banned words + evasions | incl. synonym hunt | **DONE** | ❌ UNRECONCILED — **4** |
| 6 | S2d/S2e/S2f | the 3 landed docs | **DONE** | ❌ ALL UNRECONCILED — **36** |
| 7 | seam 1 — definition ↔ call site | 1,841 files | **DONE** | **2** |
| 8 | seam 2 — never constructed | `crates/` | **DONE** | **3** + all 19 confirmed |
| 9 | seam 3 — declared ↔ unwired | composition root | **DONE** | **7** (+1 refuted) |
| 10 | seam 4 — half-applied rename | whole tree | **DONE** | **5** (+3 landed clean) |
| 11 | seam 5 — writer ↔ reader orphans | qa/ 35, testing/ 292, inventory 8 | **DONE** | **3** (2 fixed, 2 false alarms) |
| 12 | seam 6 — two units, one quantity | **money** | **DONE** | **3** + control confirmed |
| 13 | seam 7 — removed ↔ still counted | consts, asserts, floors | **DONE** | **4** (+9 refuted) |
| 14 | seam 8 — gate scope ↔ tree | 88 walks, 44 fast-tier gates | **DONE + FIXED** (`c29dedb1c`) | **5** |
| 15 | seam 9 — doc ↔ doc, doc ↔ code | **87** design docs + spec | **SUBSTANTIALLY DONE** (7 of 11 batches) | **8** more |
| 16 | seam 10 — branch ↔ trunk (novel) | 1,386 novel paths | **PARTIAL — 169 swept, ~1,070 left** | **1 FIXED (auth) + 5 MERGE** |
| 17 | branch ↔ trunk (renamed) | **2,262** derived; money bucket FULL | **PARTIAL** | **1 money + 5 park** |
| 18 | trunk — kernel family | `crates/busbar-kernel*` | IN-FLIGHT | — |
| 19 | trunk — plane family | 607 files, 313k LOC | **DONE** | **2** + a clean money bill |
| 20 | trunk — root + plugins | `crates/busbar`, loader, sdk | IN-FLIGHT | — |
| 21 | trunk — admin | `crates/busbar-core-admin` | **DONE** | 58 files classified |
| 22 | collation + lock | fold all of the above into X | PENDING | — |
| 23 | **decision audit** | every X row cited to its governing decision row (1–85) | **DONE** | **X recounted: 79 firm** |

**MAP = 15.5/23 (67%).** **X = 136 live (137 raised, 1 struck).** (§6 doc consolidation raised X-136, X-137.)

### Why the branch corpus is 2 sweeps and not 816

816 branches × N files is the wrong work unit. The right one is the distinct `(path, blob)` pair:
**46,828** pairs not in HEAD, spanning only **4,039 distinct paths** — **1,386 genuinely novel**
(basename appears nowhere in HEAD) and **2,653 renamed/moved/evolved**. If `x.rs` is byte-identical
across 100 branches, it is read once.

Three methods failed before this one. Patch-id is useless — the rename waves moved every path. Only
**3 of 819** branches are ancestors of HEAD, so `git log trunk..branch` is meaningless for the rest.
**Symbols survive renames; paths do not** — that is the whole method.

Prior bucketing was EMPTY 14 / HARVESTED 133 / SURVIVOR 669. **669 is an upper bound, not a
measurement** — one slice showed a 15% false-SURVIVOR rate from ripgrep leftmost-first shadowing
(1,445 false absences) before the instrument was rebuilt.

---

## 3.1 FINDINGS AS THEY LAND — this is X

### Sweep 1 · S2b `1.6.0-oracle-coverage.md` — ❌ UNRECONCILED, 7 findings

Commit `bb83912d5` is titled *"ledger(S2b/S2c): both sources reconcile"*. **Only one of the two
did.** S2c's items really did become LEDGER rows, one for one. S2b's did not: the LEDGER's own
housekeeping still lists `1.6.0-oracle-coverage` under **"LANDED, NOT YET FOLDED"**, and zero LEDGER
rows cite it. The commit *named* the seven open items rather than giving anyone a row that owns them
— which is a restatement of the gap, not a closure of it.

Counts: **section count did NOT reproduce (33 → 25)**; table-row count (86) reproduced exactly. Same
pattern in S2c (44 → 42, 264 exact) — in both documents the row extraction was sound and the heading
regex was wrong.

| X | finding | where |
|---|---|---|
| X-1 | admin: 403/500/rate-limit unmodelled; **26 verb.rs ops** with no oracle cell | oracle-coverage §1.3 |
| X-2 | **14 unserved dialect×op pairs**, streaming shapes uncovered | §1.4 |
| X-3 | **zero oracle cells on 3 of 5 planes** | §1.6 |
| X-4 | auth-through-plugin, OAuth AS, **13 net-guard variants** uncovered | §1.7 |
| X-5 | `BEHAVIOUR.md` — **30 of 41 sections UNMAPPED** | §1.8 |
| X-6 | two money reads carrying `resolves: false` | §3.1 |
| X-7 | **14 named money behaviours with no cell at all** | §3.4 |

X-6 and X-7 are money. They are PARKED, not fixed — a billed byte is never self-approved (#10/#59).

Two of §3.4's sub-findings coincide with existing rows **M1** (the `cost.rs:138` rate clamp) and
**M3** — but those rows were reached independently and cite nothing from this source, so the auditor
declined to credit them as COUNT OUT. That is the right call: a row that happens to overlap is not
evidence the fold happened.

### Sweep 2 · S2c `1.6.0-duplication-census.md` — ✅ RECONCILES, Δ = 0

27 ranked items → 27 LEDGER rows (`S3`, `S10`, `S11`, `S12`, `M1` + `ENG1`–`ENG22`), verified one
for one by file:line, not by assertion. Four further rows (`ENG23`–`ENG26`) were folded beyond the
required 27 — the fold was thorough rather than minimal. The source is correctly listed under
**"FOLDED AND DELETED"**.

### Sweep 5 · S9 banned words — ❌ UNRECONCILED, 4 findings

**The count is not a gate and never can be.** 93 at PIN → 260 → **334 today**. It moves *up* as
work is done, because house policy strikes superseded text rather than deleting it (scoring one
fixed deferral twice) and because the audit documents quote the ban while auditing it
(`1.6.0-map-proof.md` alone: 129 occurrences). Ledger row **G56** already named this — *"the
banned-word count is not monotone in the thing it measures, so it cannot be a gate"* — and this
sweep confirmed it independently rather than citing it.

**The ban is evaded by vocabulary, exactly as it was the first time.** Searching the four literal
words finds nothing; searching *"follow-up" / "future work" / "Post-1.6.0"* finds real owed work.
Worse — three of them sit under **checkboxes already ticked `[x]`**. Marked done in one half, undone
in the other. The same broken pair as everything else in §1.

| X | finding | where |
|---|---|---|
| X-8 | **A10 ticked `[x]`** — 9 named boot-mutation oracle rows still need "a follow-up script" (7 need a durable-store precondition, 2 need reload/rollback). No ledger row. | `1.6.0-TRACKER.md:18` |
| X-9 | **F6 ticked `[x]`** — PB-71's 56-claim README/CHANGELOG audit, self-described as *"real future work, named not papered over."* No ledger row. | `1.6.0-TRACKER.md:148` |
| X-10 | **`## P. Post-1.6.0` → P1**, plane/transport plugin LOADING. A 2026-09-06 decision predating the ban, never revisited against it or against #39 twelve days later. Named only as a caveat inside ENG30; no row of its own. **Owner ruling owed: does P1 survive #39?** | `1.6.0-TRACKER.md:213`, `:100` |
| X-11 | **ENG5 cites a path that does not exist** — `crates/busbar-plugin/src/plane_host/mod.rs:14`. The real scaffold is `crates/busbar-kernel/src/plane_host/mod.rs:13`. The work is covered; the citation is not. | `1.6.0-LEDGER.md` ENG5 |

`1.6.0-TRACKER.md` is the source of three of the four. The READ-FIRST hook already declares its
lines stale — it is now also proven to hide owed work behind done-marks. **It folds into this map
and is deleted**, but not before X-8/X-9/X-10 are carried out of it.

Correctly judged INNOCENT and named so the count stays auditable: the ban's own enforcement code and
fixtures (`deferral-words.py`, `qa_gate_dispatch.rs`, `changelog.rs`); the two-phase hook pipeline's
"phase-2", which is a built, running stage and not a deferral; `not now+u64::MAX` in a store test,
where `now` is the timestamp fn; and `1.6.0-proof-dashboard.md`'s Phase 2, explicitly marked
non-blocking — the opposite of hiding a deferral. One stale proposal
(`1.6.0-waist-rebuild-plan.md`) describes `busbar-core`, a crate deleted at `673ecdaaa`.

**Instrument note:** without excluding `.claude/worktrees`, `find -iname "1.6.0-*.md"` returns **418**
files instead of 42 — fifteen stale nested checkouts each carrying a full `docs/design`. That is the
precise mistake that produced a wrong finding earlier in this project; the auditor reproduced it
deliberately and discarded it.

### Sweep 6 · S2d/S2e/S2f — ❌ ALL THREE UNRECONCILED, 36 findings

**The structural finding: landing a document did not turn its findings into master-list rows.** All
three files exist, are tracked, and are clean — so §11 row 8's claim that they are *"ABSENT"* is
false and is struck. But its bottom-line judgement stands, now proven rather than assumed: the map
is not complete on account of these three. The verdict changes from "absent" to "landed but
unreconciled."

The ledger has its own bookkeeping for exactly this (`## THE FOLD`, lines 750–826) — and **that
bookkeeping is itself wrong**. It records done-readout as *"PARTIALLY FOLDED … the document
self-declares 'Run in progress', 21 of its 23 group rows read PENDING."* The committed file has
**zero** PENDING rows; it is a completed run. The ledger is citing a superseded draft, and row G9
carries the stale number with it (**79 hits** where the landed document says **81**).

| source | IN | OUT | Δ | verdict |
|---|---|---|---|---|
| **S2d** done-readout | 14 owed items | 4 topical (all via *other* audits, none attributed) + 2 stale | **8 clean gaps + 2 stale rows** | ❌ |
| **S2e** instrument-audit | 10 owed items | 2, both incidental | **8 of 10 absent — 80%** | ❌ worst of the three |
| **S2f** gate-sweep | 70 distinct red rows | 59 clean + 2 needing no row | **2 true gaps + 3 unrecorded new + 7 stale verdicts** | ⚠️ 91% topical, still not Δ=0 |

**S2e is the one that should sting.** A 937-line, internally self-reconciled instrument audit exists
and the master worklist does not know it exists. The ledger's own words for it: *"LANDED, NOT YET
FOLDED."*

**S2f's seven stale verdicts are the inverse defect** — the ledger asserts `OPEN` on rows
(`GS5` hold-escapes, `GS15` no-deferral ×3, `GS22` ship-ready:ceiling-slack) that S2f's own re-run
at `a7cceac3f` found **GREEN**. A worklist that carries fixed work as open is as wrong as one that
carries open work as fixed; it just wastes effort instead of shipping a defect.

### The 6 never-measured groups — a hole nobody rowed, now with the exact command each needs

Zero ledger presence under any substring tried. Neither done-readout's 14-item queue nor
gate-sweep's 70 rows contains "go run these" as an owed action. **This is a hole inside the map that
the source documents named and never rowed** — 6 findings, X-42…X-47.

| group | command that measures it |
|---|---|
| 2 PLANE-PURITY | `cargo xtask gate plane-purity --selftest && … && … plane-purity-strict` |
| 3 KIND-ISOLATION | `cargo xtask gate kind-isolation-ship --selftest && cargo xtask gate kind-isolation-ship` |
| 13 EQUALITY | `cargo test -p busbar --features root-* --test capability_equality` + `capability-equality-summary.py --root-legs` |
| **14 PARITY** | **blocked** — `target/release/busbar` is a stale symlink into another agent's `/tmp` checkout (G46). Fix, quiesce, then `oracle record` → `oracle replay` |
| 19 KERNEL | `cargo test -p busbar-kernel && cargo test -p busbar-contract` (leg 3, attempt-identity, already passes 2/2) |
| 21 TELLER-STEPS | `cargo xtask teller-steps --root-legs && rigs-ledger.sh --bin … --check` — second half needs PARITY's fix first |

PARITY is the most important check we have and it is blocked by a **symlink pointing at another
agent's temp directory**. Nothing about the product; everything about the harness.

### Sweep 3 · S3 commit residue — aggregate UNRECONCILABLE (proven), sample 71% reconciled, 12 findings

**The 242 cannot be enumerated by any command, and that is now proven rather than asserted.** All
five class labels (`OWED-WORK`, `SEPARATE-FINDING`, `KNOWN-RED-ON-PURPOSE`, `EXEMPTION-GRANTED`,
`OWNER-RULING`) return **zero literal hits** across all 307 bodies — the per-class tally was a human
read, not a command's output. A best-effort keyword classifier catches about a third of the implied
density, and **two falsification plants both slipped through it**: one synthetic, and one real
corpus sentence — *"voice still hardcodes `UpstreamIdx(0)`… bills every turn on the wrong provider's
lane"* — which is unambiguous owed work by the map's own definition and trips no keyword. **Residue
is a semantic category, not a lexical one.** No regex will ever enumerate it.

**The printed command was also not a stable measurement.** `--since` with no upper bound returns
**415** today, not 307. Bounded to the pin (`git log --since=… a197ad2f5`) it reproduces exactly.
An open-ended range is not a pin.

**Why the delta looked catastrophic — it is dateable.** `docs/design/1.6.0-LEDGER.md` **did not
exist** at the pin: `git show a197ad2f5:docs/design/1.6.0-LEDGER.md` → *fatal: path exists on disk,
but not in 'a197ad2f5'*. It was created at `3d822e503`, **two hours after the pin, inside the very
commit range S3 was analysing**. "COUNT OUT ≤ 45" was measured against a document that did not yet
exist. It now carries 375+ ID-tagged rows, and the 108 post-pin commits have been visibly draining
S3 into it. §5 was correct *for the document that existed at that moment* — it just cannot be cited
now.

**Checkable sample: 59 sha-named entries → 40 OUT, 1 partial, 1 leans-out, 5 mistagged, 12 new.**
The 5 mistagged are an **over-count**, not an under-count — commits that cite an existing locked
decision as justification for completed work, or whose "defect" was fixed by that same commit.

| X | finding | sha |
|---|---|---|
| X-48 | **CHANGELOG advertises rate-card-history as shipped**; the endpoint is not wired to the dated history. *"It cannot ship as it stands."* | `ea94ce933` |
| ~~X-49~~ | ~~float-usage fix landed for Cohere only~~ **STRUCK — DISPROVED BY EXPERIMENT.** All six readers go through one seam (`read_count_u64`/`billed_count`, `busbar-llm-codec/src/usage_count.rs`); no live `.as_u64().unwrap_or(0)` remains on any usage field. Proven by *mutation*: neutering the seam fails **26 tests across all six dialects** with the exact under-billing symptom — anthropic `0` vs `27`, gemini `0` vs `832`, openai_chat `0` vs `800`. Landed by `834dc1836`, `812370e09`, `3c321a31e`, `3c533603f`. | struck |
| X-50 | **`UpstreamIdx(0)` hardcoded** — a gemini-live session declared after an openai one bills every turn on the wrong provider's lane | `7ca14571b` |
| X-51 | **every TLS private-key read in production is unaudited.** `AccessJournal`/`AccessPurpose` exist and are unit-tested; `transport_key_token` has **zero production callers**. Contradicts the owner ruling that plugins may not touch secrets *because the kernel audits them*. | `6d3bda8a3` |
| X-52 | compiled-in and dropped-in reach **different metrics recorders** — breaks #11's compiled-in == dropped-in equivalence for export counters | `1059d3c36` |
| X-53 | `qa/denylist-allow.toml` concedes one sentence **six times**, four load-bearing — the only reason `denylist` reports 2 hits and not the real, larger count | `c6f69b6d4` |
| X-54 | **the oracle recorder fabricated PASS rows** — a port collision recorded a foreign process's HTTP 429s as boot successes, and a regex artifact invented ~200 divergences | `f2efe6390` |
| X-55 | **oracle-green is structurally silent on plane money** — mcp 912 cells / **0** touch money, a2a 468 / **0**, llm 149/14. No 1.5.5 golden can exist for a post-1.5.5 plane. | `f40b96bb4` |
| X-56 | no export plugin has ever been published; the oracle corpus holds no load cell, so #85's widened-window claim would ship on assertion | `03791e495` |
| X-57 | **the CI-runner reaper was never scheduled** — it ran only when invoked by hand, which is how a 52-minute / ~63-core-hour runaway survived a 20-minute kill rule | `f48aeb1eb` |
| X-58 | plane crates carry **9–41 live money references each** where #43/#87 require none; `UnitKey::new(0)` was fixed at `units_a2a.rs:1019` and **is still live at `units_mcp.rs:1481`** | `1ddc2b16a` |
| X-59 | the billing-fix bytes for X-49 landed inside an unrelated commit (subject: *"Encode::ArenaExhausted → ScratchExhausted"*) because a shared-tree `git add` swept them up — a permanent mislabelled audit-trail entry for a real money change | `f48aeb1eb` |

**X-49 and X-58 are seam 1 exactly** — a fix applied to one copy and not its twin, found by a
completely independent method. That is the frame in §1 confirming itself.

**X-54 is the gravest instrument finding in the release so far.** The oracle — the check that is
never waived — recorded another process's HTTP 429s as its own boot successes. A green it produced
was not a green.

X-48, X-49, X-50, X-58, X-59 are money. **PARKED, not fixed** (#10/#59).

### Sweep 9 · seam 3, declared ↔ unwired — 7 findings, 1 refutation

**THE HEADLINE: the reachability gate rewards the least-wired plane.**

**The reachability gate rewards the least-wired plane.** `xtask/src/gates/reachability.rs:1109,1148`:
when a roster plane's `root/units_*.rs` file is simply *absent*, both `unit-path` and `root-reach`
score `Ok(...)` — a clean PASS, not a red, not even a tracked row. This is live: `units_decision.rs`
does not exist, so **decision passes 2 of 3 axes for free**, scoring strictly better than mcp, a2a
and streaming whose modules exist but are unreached. None of the gate's 25 selftest cases exercises
it. The auditor did not need to plant anything — the tree already contained a live instance.

| X | finding | evidence |
|---|---|---|
| X-63 | **the reachability gate's vacuous-pass branch** — a missing unit file is scored PASS; decision is the least-wired plane and scores best | `reachability.rs:1109,1148` |
| X-64 | **`busbar-plane-decision` (2,644 lines) is not a dependency of the `busbar` binary at all.** Zero references in `crates/busbar/src/`. The 5th plane (#48) does not exist for any customer. | crate vs `crates/busbar/Cargo.toml` |
| X-65 | **the `decisions:` config section is not in the grammar.** `config_sections()` folds only registered `PlaneDecl`s; zero `"decisions"` strings in `busbar-kernel/src/`. An operator who writes it gets a rejection. | direct consequence of X-64 |
| X-66 | **#18 violated in the serving path**: `register_planes()` installs `&busbar_voice::PLANE_DECL` — **the banned name**. `busbar_plane_streaming` appears only in `plane_claims()` (the OpenAPI/claims surface) and in the never-bound `root-voice` scaffold. **Two crates back two facets of one plane in one composition root, and customers are served by the legacy one.** | `main.rs` `register_planes` vs `root/registry.rs` |
| X-67 | `plugins.yaml` + `plugin-registry-check.sh:192` admit 4 of 7 kinds. **DOWNGRADED by owner, 2026-09-22: really old code.** Created 2026-08-01, `kind:` line untouched since 2026-08-05 — it predates the 7-kind model. It registers out-of-tree plugin *repos* (`repo:`, `version_line:`, `release_gate:`) for release coverage, not the in-binary kind taxonomy. Update the enum or retire the file; **not an architecture blocker.** | LOW-MED |
| X-68 | **#39's export kind is compiled into the kernel, not plugin-routed** — `busbar-kernel/src/export/{prometheus,webhook,file}.rs`. **`otlp`, named in #39, has zero implementation anywhere.** | grep clean |
| X-69 | **the transport kind has no loader ABI at all.** `busbar-contract/src/transport/registry.rs` says so in its own doc: *"Transports are in-tree and never dynamically loaded, so there is no loader window to police."* Third-party transports are categorically impossible. | that doc comment |

**REFUTED — remove from the worklist:** `PlaneHostVtable::EMPTY` in `plane-example` is **not** a
defect. It appears only in a doc comment describing a *negative test* (hand the fixture a null
vtable, it must refuse — that is the fixture's purpose). `plane-example` is fully wired and is one
of only two fixtures proven to link both `rlib` and `cdylib` from one source. I have been carrying
this as a gap; it is not one.

The three unreached unit modules (mcp, a2a, streaming) have **no customer effect today** — the
pre-1.6.0 dispatch paths still serve. The hazard is the cutover: `qa/reachability.toml` records that
three money bugs (wrong-denomination accrual, hardcoded `rate_card_version: 0`, colliding
`UnitKey::new(0)`) shipped *into* the unreached a2a module precisely because nothing runs it.

### Sweep 12 · seam 6, money units — control confirmed, 3 further findings

The `spend_micros` / `spend_cents` 10,000× split was found independently — both derive from the same
`u128` nanos accumulator, divided by `NANOS_PER_CENT=10_000_000` and `NANOS_PER_MICRO=1_000`.
**Neither the ledger nor the rate card is at fault**: both are correct. It is a wire-contract defect
— two admin projections of one correctly-computed quantity, in two scales, under names that invite
direct addition.

| X | finding | evidence |
|---|---|---|
| X-60 | **the no-float-money gate cannot scan the file with the missing clamp.** `ROW_NO_FLOAT` is hardcoded to `LEDGER_SRC` + 4 named files; `busbar-kernel/src/cost.rs` and `busbar-kernel-budget/src/price.rs` are in neither scan set. **The gate cannot produce a RED for that file no matter what float code is added to it.** | `no_float_money.rs:93,117,130` |
| X-61 | the same rate conversion in two homes produces **opposite failure modes on one input**: `nano_rate` returns `0` (free) where `cost.rs:138` saturates to `u64::MAX` nanos/token. `busbar-kernel-budget/src/price.rs` had this exact drift, was fixed by delegating — `cost.rs` reimplements inline, so **the bug class recurred a second time**. | `rate.rs:40-45` vs `cost.rs:138-143` |
| X-62 | **#79's dated card history is honoured on `/usage` and on neither of the other two usage surfaces.** `derived_bucket_usage` (`governance/state.rs:1599-1633`) prices against the *current* card — zero `card_at`/`arrived_ms` references. The surfaces are **non-reconcilable even after unit conversion** whenever a forward-dated or signed back-dated card exists. | grep: zero hits |

**X-62 corrects a correction I made earlier.** I said #79's dated-history had a production feed and
closed the M6 row. True for `/usage`. False for `/groups/{name}/usage` and `/keys/{id}/usage`. The
row reopens, narrowed and named.

Checked and cleared — the sweep proved it can say NO: time units (`now()` seconds vs `now_ms()`
never mixed; the one crossing point multiplies correctly), the per-1k and per-mtok rate math
(dimensionally consistent against all four agreeing `NANOS_PER_CENT` declarations), and
`cost_per_mtok` as a routing signal that is never settled or billed.

All of X-60, X-61, X-62 are money. **PARKED. Not fixed.** (#10/#59)

### Sweep 10 · seam 4, half-applied rename — 5 findings, 3 renames confirmed fully landed

**The mandated feature name does not exist.** #18 requires crate `busbar-plane-streaming`, feature
`plane-streaming`. Grepping every `Cargo.toml`: **there is no `plane-streaming` feature anywhere.**
The only one that exists is `plane-voice` (`crates/busbar/Cargo.toml:203`,
`busbar-kernel/Cargo.toml:207`). The spec-mandated feature cannot be passed to cargo. Worse,
`root/registry.rs:462-463` gates the **new** `StreamingPlane` push behind the **old**
`#[cfg(feature = "plane-voice")]` — the new half is switched on by the old half's flag.

**A file asserts its own completion falsely.** `busbar-plane-streaming/src/register.rs`'s header
says *"S1 IS LIVE… `busbar-plane-voice` is DELETED."* `busbar-voice` is alive at 359 LOC, and
`busbar-plane-streaming`'s own `Cargo.toml` depends on `busbar-voice-codec`.

| X | finding | evidence |
|---|---|---|
| X-70 | **feature `plane-streaming` does not exist**; the new plane is gated behind the old plane's feature flag | `Cargo.toml` ×2, `registry.rs:462` |
| X-71 | `register.rs` **claims the voice crate is DELETED**; it is alive and depended upon | `register.rs` header |
| X-72 | **#34/#35 promised an enforcing gate for `busbar-api`'s retirement and it does not exist.** 18 live dependents; `full_gate.rs:203-204` builds it as a permanent package. **Nothing will ever go red over this crate shipping.** | `crates/api/Cargo.toml` |
| X-73 | no gate enforces "planes = exactly 5 crates" — the codec fold (#39) has thin adapters sitting atop full-size legacy crates, tracked only as acknowledged-red deps | `qa/construction.toml:834-843` |
| X-74 | **16 undocumented live neutrality leaks**, uncaptured by `qa/instance-noun-neutrality.toml` — including six fresh `streaming@crates/busbar/src/root/*`. The switchover is happening mid-flight and the ratchet has not caught up. | gate re-run |

**Correction to a number I have been quoting.** I said "instance-noun: 28 rows RED." The gate emits
**28 rows = 23 FAIL + 5 PASS**, and the FAIL counts sum to **739 (noun, file) leaks** — matching the
spec's own figure exactly, so that census is internally consistent and not stale.

**FULLY LANDED — stop worrying about these:** `busbar-caps` → `busbar-contract` (#37/#38): no live
crate, comment citations only. The `busbar-plane-mcp-host` anti-pattern (#39): never existed.
**`decisions`/jev is the only plane of five with no legacy dual** — single crate, and
`instance-noun-neutrality:jev` PASSES. The plane that is least wired is also the cleanest named.

Three gates — `reachability`, `instance-noun-neutrality`, `plane-delete-test.sh` — already print the
on-disk/locked mismatch themselves. **They are not the problem; they are the only reason we can see
it.** `qa/teller-steps.json` and `teller_steps.rs` are the opposite: zero occurrences of `streaming`
or `decisions` in either. That gate cannot attempt to notice the rename (already ledger row DOC15).

**Fixed in this pass:** `.github/workflows/plugin-ci.yml` carried an *uncommitted* comment regressed
to `crates/plugin-sign` — a path deleted and repointed long ago, the same debris defused from the
index earlier today. Reverted to `crates/plugin-loader/src/sign.rs` before it could be committed.

### Sweep 4 · S4 qa/*.toml ratchets — ✅ RECONCILES, 12 findings

**2,444 does not reproduce. The number is 2,442**, and the 2 are map-proof's own double-count of a
dict-held 2-entry list. Re-derived from a clean-room structural scan (every TOML `[[x]]` found by
shape, not by hardcoded name, so a rule under a new name cannot hide) and cross-checked against
**live gate output today**, not against the prior census.

**THE RESULT THAT MATTERS: 1,571 owed rows are 24 decisions.** Grouped by the single governing
register that one ruling would close, stopping only where each entry is an independently-ruled fact
about a different place in the tree. Every row lands in a named bucket — SATISFIED 122 / OWED 1,571
/ VACUOUS 620 / NOT-A-ROW 129.

**620 rows are VACUOUS** — they cannot fail. The `ports-only*.max_per_crate` family is eight
ceilings of `0` keyed on `busbar_core::`, a symbol that occurs **exactly once in the entire tree, in
a comment**. Zero is the passing answer to every ban.

**13 of the 24 decisions are ledger rows. 11 are not — 523 raw rows, named in full:**

| X | uncovered register | rows |
|---|---|---|
| X-75 | **`plane-purity-strict` DIALECT+KEY+SYMBOL — 460 live test-scope banned-word occurrences.** The single largest uncovered block in S4. R30 and BR-S8 cover only this file's *vacuous* halves. | 460 |
| X-76 | `legacy-reach.ceiling` — 17 permitted `busbar_llm::` root symbols | 17 |
| X-77 | `kind-isolation [[disagreement]]` — 13 live scanner-vs-scanner contradictions, none stale | 13 |
| X-78 | `kernel-seal-impls.known_sites` — 9 tracked-debt test-harness sites | 9 |
| X-79 | `kind-isolation [[question]]` — 6 open questions | 6 |
| X-80 | `seal-sites` — 6 reviewed capability-minting sites | 6 |
| X-81 | `one-teller-loop.max_legacy_sites` — 4 legacy `run_gauntlet` adapters | 4 |
| X-82 | `manifest-allowlist.reviewed_allowlist` — 3 retiring-crate exceptions | 3 |
| X-83 | `kind-isolation [[transitional]]` — 3 live, plus a gate-code hole: `to` is never checked for staleness | 3 |
| X-84 | `sealed-unit-traits.max_unsealed` — `busbar-kernel-budget::Admission` | 1 |
| X-85 | `segments.toml` "plugins" fallback — unreachable by construction | 1 |

**X-86 — a fourth extraction bug, and the funniest one yet.**
`[rules.no-test-doubles-in-production.known_sites]` is authored as a **table of named sub-tables**,
not a TOML array. `isinstance(v, list)` is False, so **every prior extraction — map-proof's and the
census's — silently skipped all 14 entries.** Five of them carry `verdict = "double"`: `NullShipper`,
`RecordingRows` ×3, and **`Pricer::flat(`** — live test doubles in the shipped binary, one of them a
pricer.

> **A `known_sites` register that cannot be seen by the tool looking for `known_sites`.**

That is the dominant defect class landing inside the instrument built to measure the dominant defect
class. Fourth extraction bug on this shelf, after the line-regex miscount and the phantom
double-count.

**Scope note the auditor raised unprompted:** `qa/unconstructed.toml` already exists in the working
tree — 19 `[[capability]]` rows, `cargo xtask gate unconstructed`, **uncommitted**. It encodes
exactly the never-constructed class. Correctly excluded from a PIN-disciplined count, but **S4's
population is not closed** and the next census must include it.

### Sweep 13 · seam 7, removed ↔ still counted — 4 findings, 9 refutations, and it caught this map

**It refuted its own positive control, which is the best sign a sweep can give.** `VERB_COUNT` is
**fixed at HEAD** — the assert now derives from live `.len()`s, all 92 verbs are real, and the
ledger/audit verbs route to real implementations at `units_admin/mod.rs:985,1000`, not a 404
catch-all. Closed today by `b1a448454` / `47f70d241`.

**Then it caught this file.** `1.6.0-MAP.md` still listed `VERB_COUNT` and "orientation says 78
decisions" as live defects. **Both are fixed** — the spec says **85** at lines 9, 13, 20 and 305,
and zero hits for "78" near "decision" anywhere in it. **The map whose job is to be the complete,
accurate list was itself carrying two dead rows within an hour of being written.** Corrected in this
commit; the §1 table now cites live instances only.

**Also refuted, and worth retiring for good:** `DONE_GROUPS_DECLARED=23` vs `grep -c` returning 24
is **not** an off-by-one — line 94 is `begin_group() {`, the function definition, with no trailing
space; the script's own guard uses space-terminated `awk`. 23 is correct and self-guarded.
`CONFIG_NOUN_FLOOR=17` reproduces live. 49 workspace crates matches. `openapi.json.gz` is
byte-identical to its source. Zero `#[deprecated]` anywhere.

| X | finding | evidence |
|---|---|---|
| X-87 | **the auth kind is never scanned.** `rules2.rs:307` still spells `pure_auth`/`egress_auth` after #3 collapsed them into `auth`. `kind_globs()` returns an **empty Vec** for an absent key rather than erroring, so both resolve to zero crates and `auth` is skipped. **A `busbar-kernel` dependency creeping into `auth-admin-tokens` or `auth-static-plugin` would never be caught.** Three sites carry it — and `construction.rs:242-252` builds the *completeness oracle* from the same wrong vocabulary, so **checker and checked share the bug**. | HIGH |
| X-88 | **the selftest cannot red the auth kind.** `selftest.rs:882-889` plants a bad dependency into one crate per kind as a positive control — and plants nothing for those two, because they resolve to zero crates. *"Can this instrument produce a NO"* is **untested for the auth kind**, and the selftest stays green while the kind is unguarded. | HIGH |
| X-89 | **`Secret<T>` (#54) was never constructed.** `grep -rn "Secret<" crates/` → **0 hits**. Only `Redacted<T>::expose_secret` exists, with **43 unsealed call sites**. Both CI invocations run `secret-hygiene-gate.sh` with **`SECRET_GATE_REPORT_ONLY=1`**, and `ship_ready.rs` has zero secret checks. A locked decision's capability absent, and its one gate wired never to fail. | HIGH |
| X-90 | **the spec is one group behind the oracle.** `BUSBAR-1.6.0.md:86,88,1178,1212` say **22** groups and a 6-GREEN/16-RED split. The script declares and self-verifies **23**. Anyone judging ship-readiness from the spec is using a stale denominator. Separately, `1.6.0-secret-hygiene.md` carries **11 citations to `crates/busbar-core/src/…`**, a crate #36 collapsed into `busbar-kernel`. | MED |

**X-87/X-88 are the live instance of the class my refuted control was supposed to illustrate.** The
verb table got fixed; the same disease moved one floor down into the gate that polices plugin
manifests, where it now silently exempts an entire plugin kind — and the selftest built to catch
exactly that cannot see it either.

### Sweep 15 partial · seam 9, doc ↔ code — first batch, 4 findings

*The parent agent was killed by rate limiting; its 11 forks outlived it and are reporting
independently. Batch 1 of 11 below. The sweep is re-dispatched with sub-spawning banned.*

**The release engine contradicts the decision that governs it.** #67 (`BUSBAR-1.6.0.md:404`,
**OWNER-LOCKED 2026-09-20**) reads: *"`predev` is the permanent WIP branch; `dev` is
release-train-write-only… the release train is the ONLY writer of `dev`."* Its own enforcement
column says *"branch protection enforces no direct dev writes."*

The live tooling does the opposite:

| X | finding | evidence |
|---|---|---|
| X-91 | **`scripts/pr-land.sh:52` hardcodes `base="dev"`** and arms auto-merge. Every landing puts WIP **directly on `dev`** — precisely what #67 forbids, destroying dev's always-promotable invariant. | `base="dev"; title=""; …` |
| X-92 | **`scripts/promote.sh:109` cannot express the promotion #67 requires.** Its allowed pairs are exactly `"dev qa"` and `"qa main"`; there is **no `predev dev` case**, and anything else exits 1. The roster the spec locks is unreachable by the tool that moves branches. | the `case "$FROM $TO"` block |
| X-93 | `voice-cross-dialect-mapping.md:3` calls it *"the busbar voice plane"* — #18's load-bearing distinction is that voice is a **dialect inside streaming**, never a plane. No `busbar-plane-voice` crate exists either. | #18 |
| X-94 | three docs cite crates deleted by #19/#37 — `plane4-seam-audit-B-session.md` (`busbar-core`, fat `busbar-substrate`), `1.6.0-waist-rebuild-plan.md` (describes a layout that no longer exists as "today"), `1.5.3-config-consolidation.md:44`. **Per #58 the waist-rebuild plan should have been deleted when superseded, not left standing.** | `ls crates/` |

**X-91/X-92 together are the branch-roster version of the same seam.** The decision says one thing,
the tooling implements another, and the spec's enforcement column asserts a branch protection that
would have to reject the tooling's own default. Three halves, no two agreeing.

**One doc did it right, and it is the pattern the others should copy:**
`docs/security/advisories/2026-09-10-admin-unit-scope-port-drift.md` carries its own *"Addendum —
where this code lives now"* table mapping every historical path to its current home. All three
current paths verified present. A document that survives a rename because it was written to.

### Sweep 15 · seam 9 batches 2, 3, 4, 8 of 11 — 10 more findings

**X-96 — CRITICAL, money. An advisory claims a landed fix that is not on this trunk.**
`docs/advisories/1.6.0/mysql-store-split-flush-request-count-loss.md` (ADV-1.6.0-2, a **billing /
rate-cap integrity** defect) carries a present-tense *"Fix (1.6.0)"* section citing
`crates/plugin-testkit/` and `assert_usage_survives_reopen_atomically`. I verified directly:
**`crates/plugin-testkit` does not exist**; the function's only occurrence in the entire repo is
**inside the advisory's own text**; and its cited commit `5e3c13044` is **not an ancestor of HEAD**
— it lives on two orphan branches. A release-signoff reader believes a money defect is fixed and
gated. On this trunk it is not.

**X-95 — the ABI ships an eighth plugin kind.** `crates/busbar-contract/src/plugin.rs` declares
`pub enum Kind { Plane, Transport, Auth, **EgressAuth**, Store, Secret, Hook, Export }` — eight
variants, verified by reading it. #3 locks **seven**, and says auth is ONE kind with inbound-verify
and outbound-sign as two *operations*. The gate's own `FORBIDDEN_KINDS` bans the string
`"egress-auth"` while the type system mints it. **This is the root cause of X-87** — `rules2.rs`
spells `egress_auth` because the enum still offers it.

**X-97 — a live cost hazard the spec says is gone.** `docs/ci/self-hosted-runners.md` describes an
EC2 fleet at **~$9.36/hr**, and I confirmed **all 8 `scripts/ci-runners-*.sh` are present and
executable**, including `ci-runners-up.sh`. #56 and Part 6 say the fleet is retired — *"zero
instances, zero volumes, zero NAT gateways, zero elastic IPs."* Nothing in the doc or the scripts
warns a reader. One command stands up infrastructure the owner tore down.

| X | finding |
|---|---|
| X-98 | **four more instances of the #67 branch-model seam** — `RELEASE.md` (the actual runbook a human executes), **`SECURITY.md:69`** (public, linked from GitHub's Security tab), `release-path-i5.md`, and `CI-HEALTH-2026-09-04.md:88` which TRACKER row I3 still marks `[x]` current. All say `dev`→`qa`→`main` and hand-push. |
| X-99 | **`billing-unified.md:172-225` puts money math on the hot request path** and persists computed prices — contradicting #71 (*"no rate lookup/multiply/currency math runs on the request path"*) and #77(3) (*"price is NEVER stored"*). |
| X-100 | **`rate-card-epochs.md:264-292` recommends the exact behaviour #79 names as the defect** — repricing history flat off the current card, presented as parity-required and final. It was superseded the same day and never deleted. An agent doing the #79 fix that meets this doc first will resist it. |
| X-101 | **decision `#87` is cited in three documents. The table runs 1–85**, verified contiguous with no gaps. |
| X-102 | **`ARCHITECTURE.md:1175` lists `admin` as a plane** (and omits `decisions` entirely) — and it is not only a doc error: `busbar-core-admin/src/admin_codec/meta.rs:68` really does `impl PlaneMeta for AdminPlane`, against #5. |
| X-103 | **~12 docs cite crates deleted by #19/#36/#37 with no staleness marker** — `D33-legacy-retirement`, `contract-notes`, `foundation-abi-config`, `feature-sets`, `llm-test-fixture-seam`, `rate-card-history`, `plane-decl-registry`, `CODE-ANALYSIS`, `1.6.0-contract-gaps` (~60 gap rows), `composition-root-plan`. **#58 says delete-don't-supersede.** |
| X-104 | **ADR-0005 documents a defect that is fixed.** It says unmodelled IR keys drop silently; `busbar-llm-codec/src/chat_handle.rs:284-298` logs every dropped key with a diagnostic banner and the comment *"the DEFECT was the silence, not the drop."* An engineer could cite the ADR to excuse a gap that no longer exists. |

**Two things that came back clean, and both matter.**

The original **"15 broken documentation references" are now 0 silent ones** — independently
re-walked; every one is either fixed or explicitly flagged in-line as absent/owed. And three
advisories are exemplary: the admin-unit-scope, plugin-ABI-discriminant and rate-limiter-clock
advisories each carry their own *"where this code lives now"* addendum, and **every path in them
still resolves.** That is the pattern the other twelve should copy.

**An independent confirmation worth noting:** batch 3 found `map-proof.md` §4.2 and
`oracle-coverage.md` §6.6 disagreeing about the same count — 33 vs 25 — with oracle-coverage
showing its arithmetic and map-proof not rebutting it. That is sweep 1's S2b finding, reached by a
different agent from a different direction.

---

## 3.20 🔴 X-159 — **ENABLING KEY AUTH GRANTED EVERY CALLER FULL ADMIN. FIXED** (`9e8dea58b`)

**The gravest finding of the session, and it came from a test that exists only on an abandoned
branch.** `busbar-core/src/auth/tests/keys_arm_view_tests.rs` described the misreport precisely —
*"operator configured `auth.chain: [keys]`, told `open: true`, front door is actually closed"* — and
was never ported. **The branch corpus paid for itself on this one file.**

Every link verified by me before touching anything:

1. `auth.chain: [keys]` → `keys_in_chain = true`, and **`chain` stays empty**. `keys` is
   engine-handled and installs no boxed module — HEAD's own test
   `test_keys_in_chain_sets_flag_not_module` asserts `chain_names().is_empty()` for exactly this.
2. `is_open()` (`auth/mod.rs:371`) returned **`self.chain.is_empty()`** → **`true`**.
3. `front_door_is_open()` (`root/kernel.rs:893`) forwards it. Its own doc reads *"no module and **no
   keys arm**"* — **it states the correct predicate in prose and did not implement it.**
4. **`admin_grant()` (`root/kernel.rs:916`): `if self.front_door_is_open() { return
   Some(VerbScope::Full); }`**

**An operator who turned authentication ON handed full administrative scope to every caller.**
Fail-open, in the worst possible direction, triggered by *enabling* a security control. And
`service_operations.rs:1561` reported `open: true` beside it — consistent with the grant, and the
opposite of what was configured.

**The correct predicate was 37 lines above the defect**, in the same file, where construction warns
`AUTH_CHAIN_OPEN_RELAY` on `chain.is_empty() && !keys_in_chain`. The accessor now says the same.

**Red before green, and the vacuity trap caught on the way through.** The new test failed on the
assertion quoted in its own message. After the fix, my first re-run printed `running 0 tests` —
**a filter that matched a different binary. I did not accept it as a pass**; re-run against `--lib`
explicitly: 1 test, 1 passed. The two credential-echo failures in the kernel suite **reproduce
identically with the fix reverted** and are not from this change.

## 3.21 R1 LANDED — `Kind` is seven (`211ecd799`)

`EgressAuth`, `EgressAuthKind` and `EgressAuthScheme` are deleted; `"egress-auth"` now survives only
in `FORBIDDEN_KINDS`, where it belongs. `Signer`/`SignFailed` correctly **kept** — outbound-sign is
an *operation* of the auth kind, exactly as you ruled.

**The root cause was fixed, not the symptom.** `Cfg::kind_globs` answered an undeclared kind key
with an empty `Vec`; it now returns `Result` and **refuses**, naming the key and listing the ten it
does declare. Every caller propagates. `informational_ids` is the single swallow and **fails
closed** — a refusal there drops the exemption, leaving rows gating.

| | before | after |
|---|---|---|
| `manifest-allowlist` rows | 12 | **14** |
| `auth-admin-tokens` / `auth-static-plugin` | **0 rows each** | 1 each, PASS |
| pre-existing RED rows | 11 | **the same 11, byte-identical** |

**Falsified three ways**, including re-introducing the stale spellings to confirm the gate now
*refuses* where it previously produced 12 silent green rows. Worth knowing: `source-denylist` and
`forbid-unsafe` read their kinds **from the TOML** and were scanning the auth crates correctly all
along — **the only stale list was the one hard-coded in Rust, and its oracle, and its self-test.**

### Five more MERGE candidates from the novel-path sweep, all security-adjacent

**SigV4** — a 500-year leap-calendar proof and a signature-collision-freedom proof, against HEAD's
8 basic tests · **`kernelseal_trust_tests.rs`** — rogue-cdylib rejection and
*self-attestation-cannot-grant-trust*, no equivalent anywhere · **`transport_conformance_tests.rs`**
— dropped-in vs compiled-in ABI parity, which is **#11(2)'s own witness** · **`secret_accessor_seal.rs`**
— the sealed-read and access-journal-origin tests for `KeyMaterial::bytes` · and
**`busbar-plane-streaming` is missing its `*_d2_billing_oracle.rs`** where voice and llm both have one.

**A third zsh trap, new:** `"$var:crates/…"` — a variable followed by `:c` parses as a **history
modifier**, silently swallowing the colon and turning `git show $sha:path` into a garbled revspec
that fails and reads as a clean zero. Reproduced deterministically; it had already produced one
false *"3-line stub"* (actually 90 lines) and one false *"0 references"*.

---

## 3.18 ⚠️ X-149 — **A MONEY OVERFLOW THAT ESCAPES EVERY BUDGET CAP** (verified by me)

**Two implementations of `reserved_nanos`. Same name, same signature, same fold. One guards, one
does not — and the unguarded one is the enforcement path.**

```rust
// busbar-kernel-budget/src/price.rs:113-117   — CORRECT
let amount = u128::from(n).saturating_mul(u128::from(self.reserved_rate(u)));
acc.saturating_add(amount)

// busbar-kernel/src/cost.rs:181-186           — UNGUARDED
acc + (n as u128) * (self.reserved_rate(u) as u128)
```

**Its own doc comment is true about the wrong operation.** It says *"a u64 count times a u64 nano
rate cannot overflow u128"* — correct for a **single product** (`u64::MAX²` < `u128::MAX`) — and
says nothing about the **sum of four**. Four maximal products is ≈2¹³⁰ against a u128 ceiling of
2¹²⁸. In release builds `+` on u128 **wraps silently**, so the reserved cost of a large unit map
collapses toward zero and **passes every budget cap on the way through.**

**The fix already exists twice over**: on `origin/integration/reland` and
`origin/verify/keep-ledger-tier`, and in HEAD's own sibling. **PARKED, not fixed** (#10/#59) —
cell, diff, root cause and recommendation are here, and the ruling is the owner's.

> **This is X-61 again with a different function.** A guard applied to one copy and not the copy
> that enforces. Third instance of the pattern tonight, and the second one where the *correct*
> version is sitting in the same repository.

**Also parked from the branch corpus** (all money, all KEEP-FOR-REVIEW): `budget_headroom_cents`
disagreeing with the actual admission check at the i64 saturation edge, where a branch already
**wrote up why**; `KeyBudgetView`'s read-time spend derivation (10 branches); the whole tariff
schedule subsystem — `TariffScope`/`ENTRY_CLASS`/`BOUND_CLASS` confirmed **absent from HEAD
entirely**, not renamed; and `fee_unpriced`, which distinguishes *"fee configured at zero"* from
*"a currency the card names no fee for"* — which is **#77(5)'s explicit-zero law** in miniature.

**And a real scare that came back clean:** `crates/busbar/src/net_guard.rs` looked alarming on
`origin/main` with 31 extra keyword hits. All four SSRF primitives — `is_cgnat_shared_v4`,
`is_unique_local_v6`, `is_link_local_v6`, `is_alternate_ipv4_encoding` — are **present word-for-word**
in HEAD's consolidated judge. Verified, not assumed.

**Count honesty:** the agent derived **2,262 renamed / 765 novel** against the briefed 2,653/1,386,
**could not reconcile it, and refused to force the match** — most likely force-pushes moving
branches during a multi-hour run. It reported what it measured.

## 3.19 THE 49-GATE AUDIT — two gates are pure theatre

| X | finding |
|---|---|
| X-150 | **`hot-path-perf` is vacuous, proven three ways.** (1) Comment out *both* real assertions → still **5/5 PASS**. (2) Raise the budget **1,000,000×** (1µs → 1s) → still PASS, **row label still reads "under 1µs"**. (3) **Nothing in CI or any script ever runs the bench at all** — its doc names the deleted crate `busbar-core`. It greps source text for literal substrings, comments included. |
| X-151 | **`hot-path-alloc` likewise.** Replace the entire **170-line allocator instrument with a 4-line comment stub** → still **3/3 PASS**, including the row asserting *"the instrument installs a counting `#[global_allocator]`."* |
| X-152 | **`construction --posture` exits 1 on HEAD right now, and `ci.yml:3481` runs exactly that with `PIPESTATUS[0]`. This is gating CI today.** 3 of 5 `CONSTRUCTION_STANDING_REDS` are stale — those rules are green now. |
| X-153 | **`ship-ready`'s excuse can never fire.** No entry named `"ship-ready"` exists in `REGISTRY_NOT_IN_CI`; the `?` on the missing lookup means `--all` **unconditionally counts it as a dev-line regression**, contradicting its own posture. Its four siblings all have entries. |
| X-154 | **Three `construction` selftests are unfalsifiable** — plant, and they stay GREEN: `token-sealed` (its fixture vocabulary `UnitToken::mint(` has **0 occurrences**; the real shapes are `Pass::mint(`/`Grant::<…>::mint(`, 99/39/40/20/48 hits), `forbid-unsafe-deny` (12 rows), and `legacy-reach` (2 of 3 prefixes name deleted crates). |
| X-155 | **`crates/busbar-kernel-scope` is covered by no gate scope at all** — checked against every `qa/*.toml`, every `qa/*.json`, every hardcoded Rust scope. It appears only in an auto-derived dependency census and the human-review register. **Zero per-push architectural rule watches its content, and it is under active development this session.** |
| X-156 | **`teller-steps` floors cells but never checks the plane roster.** Deleting an entire plane's cells still scores 5/5 green — and **`busbar-plane-streaming`, a real installed plane, has zero Teller cells today** with nothing flagging it. |
| X-157 | **`plane-transport-neutrality` has a live exploitable hole**, A/B proven: `rtp_port: u16` → RED, **`output_audio_rtp_port: u16` → GREEN**. Same banned noun, one spelling caught. |
| X-158 | **`config-schema`: 1 of 57 selftest cases is RED on HEAD**, its fixture naming the deleted crate `busbar-core-config` — and **`ci.yml:1294` gates on that selftest**. Its `additive-only` rule also diffs HEAD-vs-HEAD on `push`, and this repo promotes by push. |

**Sound and exemplary, so they need no further audit:** `kind-abi-lane` (reads its lane assignment
**live from the spec** rather than copying it beside the check), `kind-isolation` (derives its kind
roster live from `Cargo.toml` and **catches its own stale rows**), `plane-abi-neutrality` (plants a
fifth plane the ban list has never heard of), `duplex-ws-default-edge`, `no-tracked-ignored` (sound
**by construction** — it has no static scope to drift), and `plane-pricing-blindness`, the reference
implementation.

**Root cause of the audit-ledger blindness, found in source:** `xtask/src/audit_cmd.rs::check()`
derives `f.missing` tree→register only, and `stamped_hash` re-derives against
`git.files_at(<audited_at>)` — **the historical commit. Present-day existence is never asked.**

---

## 3.17 DOC FOLD — 11 deleted, 4 KEPT because the brief was wrong

**`docs/design/*.md`: 87 → 76.** Deleted outright, each re-verified to have **zero machine
references** immediately before removal, with a positive control: `SEAMS.md` · `rate-card-epochs.md`
· `D33-legacy-retirement.md` · `plane-decl-registry.md` · `CODE-ANALYSIS.md` ·
`1.6.0-composition-root-plan.md` · `1.6.0-waist-rebuild-plan.md` · `plane4-seam-audit-B-session.md`
· `1.5.3-config-consolidation.md` · `foundation-abi-config.md` · `llm-test-fixture-seam.md`.

### FOUR CORRECTIONS TO MY BRIEF — I was wrong on all four, and the hard rule caught it

| I said delete | Why it must stay |
|---|---|
| `1.6.0-TRACKER.md` "once X-8/9/10 are carried out" | **It can never be deleted as-is.** `xtask/src/gates/no_deferral.rs:310` does `cx.read(TRACKER)` and **fails the gate if the file is absent** — *"the tracker could not be read, so no waiver's expiry can be checked."* **Every waiver's expiry is checked against a tracker row.** Verified. |
| `rate-card-history.md` | **It is not stale — it is the authority.** `ARCHITECTURE.md` PB-103 cites it as the design behind landed #79; the citation flows into `qa/design-bindings.json:4925` and `units_admin/mod.rs:1047`. It is the *successor* that superseded `rate-card-epochs.md`. |
| `1.6.0-contract-gaps.md` | `ARCHITECTURE.md:1544` heads its decision register with it; CG-59/CG-60 are the live rationale for the denylist gate. |
| `contract-notes.md` | **Six rustdoc comments in `busbar-contract` point at it**, and its prose was moved *out* of those headers deliberately to satisfy the LOC ceiling. Only its framing is stale. |

**This is exactly why the rule is "prove nothing reads it, then delete" and not "it looks old."**

| X | finding |
|---|---|
| X-147 | **A SECOND doc instructing against a fix that has already LANDED — one I had not named.** `SEAMS.md` §3.4.4: *"Do not touch `derive_spend_micros_row` … switching `/usage` to the ledger is G3+C13 work, not a cleanup."* `ARCHITECTURE.md` PB-103 records that fix as **landed**. With `rate-card-epochs.md`, **two documents were telling an agent to revert shipped money behaviour.** That is the damage #58 exists to prevent, proven twice. |
| X-148 | **`PLANE_ALTERNATION` is a stale roster in live gate code.** `xtask/src/gates/plane_purity/scanner.rs:29` = `["llm","mcp","a2a","voice"]`. **No `busbar-plane-voice` crate exists**, and it omits **`busbar-plane-streaming` and `busbar-plane-decision`** — both real. **Plane-purity is not looking at two of the five planes.** Same class as X-87. Verified. |

### Gate results — and two were already red before this work

| gate | before | after |
|---|---|---|
| `design-bindings --strict` | **RED** (104 PASS / 1 FAIL `regen-clean`) | **RED — identical** |
| `inventory-coverage` | green (6) | **green (6)** |
| `changelog-register` | green (5) | **green (5)** |
| `public-hygiene-lint` | **RED, 90 hits** | RED, 100 hits — **none from the deletions** |

**`docs/design/` contributes 0 public-hygiene hits** (the lint carries an explicit `SKIP_DESIGN`),
so eleven deletions are invisible to it. The +10 came from other agents' concurrent commits to
`qa/construction.toml`, `qa/plane-pricing-blindness.toml` and an `xtask/fixtures/` file. The agent
introduced exactly one hit itself — a bare finding id tripping `[audit-finding-id]` — and removed it.

### The zsh trap caught a second agent, and the map stopped it

> *"My first 'zero references' check used `for f in $DEL` — zsh doesn't word-split unquoted
> parameters, so it silently checked one concatenated non-existent filename and returned 0 for
> everything. That is precisely the trap the MAP's Sweep 15 batch 7 documents. I caught it, re-ran
> with proper splitting plus a positive control, and only then deleted."*

**A false zero would have deleted eleven documents on the strength of a search that examined
nothing.** It was caught because a previous agent's failure was written down. **The map is now
teaching the agents.**

---

## 3.16 CITATION REPAIRS — **X-115 and X-116 are FIXED** (verified in HEAD)

**`DECISION #87` no longer appears anywhere in `xtask/`, `qa/` or `crates/`.** All 13 locations
repointed to **#43 + Part 7 §13(3)**, and the agent did the important thing **first**: it verified
those two genuinely cover what the gate enforces, rather than repointing to a plausible-looking row.
#43's 2026-09-22 addendum states it near-verbatim — *"a plane appends its counts UNCONDITIONALLY —
no branch, no knowledge of billing state… 'billing off' means the VIEW reads 0… never that the
plane skipped the ledger"* — and §13(3) carries the owner's own words, *"planes always ledger"* and
*"its a kernal default all planes run though. no tunring things on or off by plane."* The ruling was
real; only the number was fiction.

**The gate's behaviour is byte-identical**, proven by revert → run ×3 → reapply → run ×3 while
holding the rest of the tree constant: same 11 rows, same RED verdict, identical PASS/FAIL set. One
transient flip on an early run was traced to a **concurrent edit landing in `busbar-plane-llm`
mid-run**, not to the change — and it re-ran to confirm rather than accepting the anomaly.

**Two judgement calls it got right by refusing to edit:**
- `1.6.0-map-proof.md:357` **quotes an immutable commit verbatim.** Rewriting the number inside the
  quote would have **misquoted history**. It appended a bracketed gloss instead.
- `1.6.0-MAP.md` was left alone as the **append-only findings journal** — editing it would falsify
  the record of the defect's discovery.

**TRACKER D36/D37 renumbered to #4/#5**, each with one added sentence explaining the collision, and
nothing else in either row touched. `qa/construction.toml`'s own `D36` references were **correctly
left alone** — those cite the spec's genuinely-rejected proposal and do not collide.

| X | finding |
|---|---|
| X-146 | **Agents are sweeping each other's work with `git add -A`.** This agent's staged hunks were absorbed into roughly a dozen unrelated concurrent commits before it could land a dedicated one. All 13 files' fixes **are present and verified in HEAD — nothing was lost** — but they landed dispersed across the commit stream. It declined to rebase, correctly, since other agents are building on that tip. **Third instance tonight**, after the abandoned plants and the 13 swept index entries. |

> **RULE, effective now: an agent commits only the paths it touched. Never `git add -A` on a shared
> tree.** Seven sessions are live in this checkout; a blanket add is a silent merge of somebody
> else's half-finished work, under your commit message.

---

## 3.14 SWEEP 14 — **LANDED: an empty scope is now a refusal everywhere** (`c29dedb1c`)

This one did not just measure. **It fixed the class**, and the proof is that three gates that were
green went red on the first run afterwards — which is the instrument starting to work, not a
regression.

**The general rule is now in the shared walker.** `Ctx::list` is the one place all **88 scan sets**
resolve, and an empty resolution is now `WalkError::Empty`. The reason this mattered:

> **Only 33 of the 88 walks carried a floor. The other 55 returned an empty vec that reads
> identically to a clean tree.**

Opt-out is `WalkSpec::allow_empty()` — greppable, one written claim per use. And
**`XTASK_SCAN_AUDIT=1`** now traces every resolved scope with its file count.

| X | finding |
|---|---|
| X-141 | **55 of 88 walks could not tell an empty scope from a clean tree.** Fixed globally in `Ctx::list`. |
| X-142 | **`release-order` R0–R15 were sixteen false passes.** Before: **16 × PASS**. After: **16 × FAIL — `walk over [.github/actions] yielded ZERO files`.** The composite-action pin rule looped `["yml","yaml"]`; `.github/actions/cargo-home/action.yml` exists, **`yaml` matched nothing**, and that half of the scope read as clean. Fixed so each spelling may declare `allow_empty` but **the union must be non-empty**. Proven R14 `PASS → FAIL → PASS` by moving the manifest out and back. |
| X-143 | **Secret-hygiene armed = exit 1, 17 violations**, stable across two runs — `private_key` in `transport/trust.rs:33` and `jwt_bearer.rs:313`, `access_token` ×2, `secret`, `credentials`, `api_key` in `busbar-voice` ×2, `caller_token` ×7. **And the trap: the script's own default is `SECRET_GATE_REPORT_ONLY:-1`, so deleting the env var would NOT arm it.** Arming requires `=0` explicitly. |
| X-144 | **`no-float-money`'s selftest is degraded, and was before this work.** Every case uses whole-verdict `prove_red`/`prove_green`, and the gate is RED on HEAD — so its **17 red-expecting cases now pass trivially**. *A `prove_red` against an already-red gate proves nothing.* Fixing it means narrowing all 23 cases to their covered rows; the agent declined rather than make it *look* green. |
| X-145 | **13 gates read by named path rather than by walk, so they have no walk denominator at all** — outside the new rule by construction. |

### `no_float_money` can now see the bug it was written for

**Scan set 29 → 38 files.** Added the whole `busbar-kernel-budget` crate (floor 5 — it is a
dedicated money crate) and two *named* kernel files, `cost.rs` and `billing.rs`. Named rather than
walked deliberately: `busbar-kernel` is 353 files with **99 legitimate floats** — routing weights,
health scores, scrape histograms — and a whole-crate scan *"would be waived within a week."*

**Row `PASS` → `FAIL`, 5 findings, none waived**, including `cost.rs:138`. The gate now sees the
clamp drift directly:

```
cost.rs:138  (kernel)              cost/rate.rs:40  (ledger — the ONE exempt #44 boundary)
v.is_finite() && v > 0.0           v.is_finite() && v > 0.0 && v <= u64::MAX as f64
```

**Falsification on every change, tree restored clean each time:** 5 → 6 → 5 by planting a float in
the new whole-crate scope and in the new named file; deleting `billing.rs` makes the row fail
*"the money-float scan set did not fully resolve"* rather than silently scan less; emptying the
budget crate reports *"yielded 0 file(s), under its floor of 5."*

**`audit-ledger`: 6/6 PASS → RED**, naming all 24 phantom scope paths — several stamped
`result: findings, round 8` **over an empty set**. It deleted none. Selftest now 15 cases, 0 skipped.

**`reachability-evidence.md` has a reader**, read *both* directions: forward (every module a red row
cites is written up) and backward (every module written up still exists). Only the first expires on
its own, which is why both are checked. Currently PASS at 6 written / 5 cited.

### Two pieces of conduct worth recording

**It corrected its own method mid-report.** It first called `plane-pricing-blindness` blind to a
present-but-empty crate — then found it had **filtered the output to 2 rows**, re-ran over all of
them, and reported the gate **sound**. The same truncation error that has burned this project three
times, caught by the agent on itself.

**It caught a shared-index hazard.** Its first commit **swept in 13 files another session had left
staged**. It reset, committed only its own 7, and restored the other session's index entries
**byte-exact** (`git ls-files --stage` diff: EXACT MATCH). Seven other sessions are live in this
repo; the shared index is a standing hazard.

---

## 3.11 SWEEP 11 — **THE ANTI-ORPHAN GATE IS ITSELF AN ORPHAN**

| X | finding |
|---|---|
| X-138 | **`qa/unconstructed.toml` is committed, its gate is registered — and the gate never runs.** The repo's own self-test says it in terms: `full-gate --selftest` → `[FAILED] registered gate(s) absent from ci.yml: ["unconstructed"] — a gate nothing runs is a gate that cannot fail`. It is absent from `ci.yml`, `qa/segments.toml`, `qa/full-gate.toml`, **and** from the 10-entry `REGISTRY_NOT_IN_CI` excuse list. **The gate built to catch capabilities that are never constructed is itself never constructed.** Perfectly recursive, and found by running the instrument rather than reading it. |
| X-139 | **`qa/design-bindings-gaps.json` claims to be enforced and is not.** Its own text: *"Checked by the design-bindings gate… each have a case in `cargo xtask gate design-bindings --selftest`."* Repo-wide grep for the filename returns **exactly 2 hits, neither a parser**. It is vacuously green only because nothing produces a `gap` verdict yet — a landmine for the first real `[[gap]]` entry, which would be **silently unchecked**. `1.6.0-ratchet-census.md:449` compounds it by marking the file *"gate detects staleness? yes / GREEN."* |
| X-140 | **The `ports-only` ceiling family is vacuous, measured not inferred.** `grep -rn "busbar_core::" crates/busbar-{llm,mcp,a2a,voice}` → **0 hits** in production or test code. And **`busbar-core` is itself one of the 24 phantom audit-ledger paths** — the crate the ceiling guards against does not exist to be imported. Eight ceilings of zero, permanently satisfiable. |

### Two of my "proven instances" were already fixed — in the uncommitted working tree

**`qa/reachability-evidence.md` now has a reader.** `xtask/src/gates/reachability.rs:1024`
`evidence_row()` reads it both directions and is wired into `owed()` — **+127 uncommitted lines**.
Verified live: `gate reachability --report` → `PASS reachability:evidence`.

**The audit-ledger phantom-path detector now exists and correctly FAILS.** `gate audit-ledger
--report` → `FAIL audit-ledger:scope-paths-exist`, naming the identical 24 paths an independent
Python count found. **The detector is fixed; the data is still broken, which is the correct state
to be in.**

### Two were false alarms, and the agent said so rather than padding the count

**`method-coverage.missing` is legitimate** — git history shows a real queue worked down to zero
with a dated paper trail, last item landed 2026-08-30. **`field-coverage.missing` is doc drift, not
a bug**: `qa/field-coverage.status` was fully populated (405 carried + 9 waived) in the *same
commit* that created the empty `.missing`, so it was never a long queue. Only the module doc at
`crates/busbar/tests/field_coverage.rs:44-48` still narrates *"most of the 412 fields not yet
backed"*, which is false. Both test binaries pass under `--ignored`.

**And three apparent orphans were checked specifically so they would NOT be reported** —
`inventory-coverage.sh`, `loc-surface.py`, `release-order-lint.py` are each *self-documented*
retirements naming their deleting commit and successor gate. **Ruling out a false positive is work,
and it was done.**

**`secret-hygiene-gate.sh` sharpened:** it has **no armed call site anywhere** — not in CI, not in
`qa/segments.toml`, **not even in the release DONE-oracle** — and, unlike its siblings, **no
exemption ruling**. `plane-noun-gate.sh` and `plane-grep-gate.sh` are permanently exempted by an
explicit owner ruling at `verify-1.6.0-done.sh:57-60`; `plane-config-noun-gate.sh` is actually armed
at `:637`. Secret-hygiene is alone in being neither.

---

## 3.15 SWEEP 19 — **NOT ONE OF THE FIVE PLANE CRATES SERVES A BYTE**

This is the single most important architectural fact in the release, and it is not a defect in any
plane crate. **All five are complete.** Each implements the full 9-method `Plane` trait —
`decode_ingress`, `decode_response`, `authenticate`, `verify`, `approve`, `admit`, `route`, `meter`,
`audit` — each is tested at 1.5–2× its source in test LOC, and each documents itself honestly as
*"an ADAPTER… a few lines over a codec that already exists."* **The defect is that nothing dispatches
into any of them.**

| plane | plane-crate LOC | what actually serves the bytes | reachability |
|---|---|---|---|
| llm | 1,846 | **legacy `busbar_llm`** — `root::units_llm` binds the kernel `Units` trait onto `busbar_llm::unit::*` | `unit-path:llm PASS` — **but that pass is the legacy crate**, not `busbar-plane-llm` |
| mcp | 3,230 | **legacy `busbar_mcp`** (`main.rs:288`) | `unit-path:mcp FAIL — NO UNIT PATH AT ALL` |
| a2a | 3,402 | **legacy `busbar_a2a`** (`main.rs:293`) | `unit-path:a2a FAIL` — `A2aUnits` built only at its ctor and in tests |
| streaming | 2,932 | **legacy `busbar_voice`** (`main.rs:300`, WS via `voice_ws_arrivals()`) | `unit-path:streaming FAIL` — `VoiceUnit` at 49 sites, **none from `fn main()`** |
| decision | 1,077 | **nothing — and it has no legacy predecessor either** | registered nowhere; `qa/reachability.toml:61` self-declares *"DELIBERATELY UNWIRED"* |

**12,487 lines of finished plane-crate code, and every byte a customer sends is answered by the
crate it was written to replace.** The plane crates are constructed only as `::EMPTY` boot seals —
`main.rs:323` and `:474` — which the code itself documents as binding *"no listener,"* reading *"no
configuration,"* writing *"no line."* `crates/busbar/Cargo.toml`'s own `root-a2a` comment says it
plainly: ***"It diverts no byte: the serving path is still the one `register_planes` mounted."***

**`busbar-plane-decision` is the sharpest case**: the only plane that is a real implementation with
literally nowhere to plug in — no legacy predecessor to replace, and no wiring of any kind.

| X | finding |
|---|---|
| X-136 | **0 of 5 plane crates are on a serving path.** "Protocols as Plugins" is, today, five complete plugins that nothing loads. This subsumes X-64 and reframes X-66: the streaming case is not a naming slip, it is one instance of a five-for-five pattern. |
| X-137 | `busbar-plane-streaming/src/register.rs`'s `plane_for_root()` is exported, documented as *"the plane side of S1/S4 is complete"*, and has **zero callers anywhere** — `registry.rs` builds `StreamingPlane::EMPTY` inline instead. **TRASH.** |

### Two clean bills, both earned by exhaustive rather than sampled checking

**`priced_from_ms` is NOT a violation — strike the concern.** `busbar-llm/src/unit/meter.rs:490` is
a hardcoded zero on a non-authoritative evidence struct, and the code explains why in its own words:
*"A plane crate asking when a card started would be a plane reaching a rate seam (#43: plugins are
pricing-blind), so this leaves the field at its default and the durable row carries the instant."*
**The zero is the pricing-blindness law being obeyed, not broken.** It also lives in the legacy
crate, not a `busbar-plane-*` one.

**Zero money reads in any plane crate — #43/#71 are CLEAN there.** Two independent **full-content**
sweeps of every file under all five plane crates, not a sample: the broad vocabulary pass returns
251 lines, every one a doc comment disclaiming pricing, a `meter()` returning raw counts with no
arithmetic, or a test fixture. The narrow identifier pass —
`rate_card(_version)?|per_request_fee|priced_from_ms|PriceLocator|fn price\(|\.price\(` — returns
**zero matches**. `Budget`/`Pricer`/`RateCard` hits are all refusal-reason enum variants or a
scratch-bytes allocation cap.

---

## 3.2 SWEEP 23 — THE DECISION AUDIT, AND IT AUDITED THE MAP

**The owner asked for the map to be checked line by line against the locked decisions. It was — and
the first thing it found was that this file was overcounting its own findings.**

| | count |
|---|---|
| numbers **allocated** | 118 |
| findings **individually stated with evidence** | **84** |
| allocated but **never written out** | 34 — X-12…X-41, X-43…X-46 |
| **allocated to nothing at all** | **10 — X-105…X-114 name no finding anywhere in this file** |

**`X = 134` was the numbering's high-water mark, not a census.** Ten numbers were allocated to
findings that were never written — almost certainly lost with the rate-limited sweeps, which this
file records happening. **The honest figure is 79 firm findings** (84 stated, less the strikes
below). Everything downstream of the inflated number in earlier status lines was wrong, and this is
the correction.

### The strikes — 5 removed, 3 narrowed, 5 reclassified

| X | verdict | why |
|---|---|---|
| **X-49** | **STRUCK** | Refuted a **second** time, independently. All six dialects read through `usage_count::read_count_u64`; the surviving `.as_u64().unwrap_or(0)` is in two prose comments, and the bare `as_u64()` in `anthropic/reader.rs` are `max_tokens`/`top_k`/`budget_tokens` — request params, not counts. **Owner question B4 falls with it.** |
| **X-3**, **X-55** | **STRUCK** | Part 7 §12 rules the oracle's 1.5.5 bound *"the design, not a defect — the witness belongs in the rigs"*, and 12 gating rig legs have landed. |
| **X-67** | **STRUCK** | #32 already declares the plugin-roster manifest *"OWED, not shipped"*. It is #32 tooling debt, not a #3 taxonomy breach. The owner's "really old code" call was right. |
| **X-101** | **MERGED into X-115** | Same finding, lower resolution — and its count was wrong: **7 source files, 5 docs, 2 `qa/*.toml`**, not "three documents". |
| X-68, X-97, X-4 | **narrowed** | `otlp` is **misplaced**, not missing (Part 7 §18). #56 *sanctions* burst EC2, so the up-scripts are permitted — only the always-on doc violates. #2 assigns plugin self-testing to the plugin. |
| **X-116** | **RECLASSIFIED** | **The spec is not wrong about D36/D37.** It is a *label collision* between two documents' identifiers — remedy is a rename plus a hook amendment, not a ruling. **The work the TRACKER row protects is real and has now landed as X-95/R1.** |
| **X-118** | **DOWNGRADED — not an owner question** | #14's own authority order (Part 2 > Part 5) already resolves it: **#73 governs, sign-off IS required**, and the `:438`/`:960` "no owner Q" lines are simply wrong. Edit, don't ask. |
| **X-126** | **RECLASSIFIED** | #79 already ruled `rate_card_version: 0` a **reporting-provenance** defect, not a pricing one. Keep the landmine, drop the money framing. |

### Two owner questions are answerable from the spec and should not have been asked

**A1 and A2 are closed.** #30's own text says the rule-(1) shorthand *"is SUPERSEDED"* — it answers
A2 itself. The `CostHold` correction box at `:729-767` is dated later and cites file:line — it
governs `:812`. **I raised both as questions when the spec already resolved them.** My rulings were
right; asking was the error.

### THE LARGEST SPEC GAP — 30 findings rest on an obligation the spec never states

**X-12…X-41 — 23% of this map's findings — assert that a landed audit's findings must be folded
into a master worklist. Nothing in Parts 1–5 says that.** #58 governs whether a doc may *exist*.
**`1.6.0-LEDGER.md` and `1.6.0-TRACKER.md` are named nowhere in Part 2.** Either a fold-obligation
row is owed, or those 30 are process debt outside the release gate. They also cannot be audited
individually **because they were never written down.**

### ONE RULING CLOSES SIX FINDINGS

**The proposed Law E1 — *a gate that scans zero files is RED, not green*** — closes **X-63, X-77,
X-85, X-83's staleness hole, and the vacuous halves of X-86 and X-89** in a single stroke. It is
already implemented correctly in `plane_pricing_blindness.rs`. **Highest leverage item on the sheet.**

### WHERE THE MAP IS THIN — 30 of 85 decisions untouched

**#76 — lossless carry — is the worst.** The owner's own words: *"literally the product"*, *"an
un-looked-at field is a MAJOR violation, not an acceptable gap"*, *"307 fields un looked at is major
violation."* **Not one X row sweeps IR field coverage.** The map is thinnest on the decision the
owner calls the differentiator.

Also untouched and load-bearing: **#52** (admin↔config parity gate — X-65 proves a whole plane
section is missing from the grammar, which that gate should have caught) · **#62** (*"a mid-stream
cut is NOT a refund"* — a money decision with zero coverage in a release whose fourth plane is
streaming) · **#50/#51** (transport scheme selection, with X-69 saying third-party transports are
categorically impossible) · **#70** (loader refuses unsigned by default) · **#24**
(`amend_rate_history` — *"spec + build it"*, and no row says whether it exists) · **#60** (two
consecutive clean passes — a ship gate nothing checks).

### Confirmed landing in the working tree

**R1 is real and verified both ways.** `git show HEAD:…/plugin.rs` carries `EgressAuth` at line 28;
the working tree's enum is **the seven**, with the doc comment *"direction is a property of the leg
— (transport, auth, direction) — never a kind of its own."* `rules2.rs:314` now keys on `auth`.
**X-87, X-88 and X-95 are closing.**

And **X-102 is sharper than I wrote it**: the spec's roster claims *"CLOSED 2026-09-22 — the plane
entry face is DELETED"*, naming `admin_codec/codec.rs`. **The `impl PlaneMeta for AdminPlane` at
`admin_codec/meta.rs:68` survived that deletion.** The "CLOSED" is over-claimed.

---

## 4. THE SCOREBOARD — 7 of 23

From `1.6.0-done-readout.md`. **GREEN 7 · RED 10 · NEVER MEASURED 6.**

A group nobody ran is not green and not red. Six were never measured — including **PARITY**, the
comparison against the published 1.5.5 binary, which is the single most important check on the list
and is currently marked *"do not run as-is"*.

| | groups |
|---|---|
| **GREEN (7)** | CONFORMANCE · NO-DEFERRAL · CONFIG-NOUN · CHANGELOG · INVENTORY-COVERAGE · ISOMORPHISM · STORE-QA |
| **RED (10)** | BUILD · INSTANCE-NOUN · REACHABILITY · PLANE-DELETE · BYTE-IDENTITY · CONFIG-STABILITY · TEST · DESIGN · AUDIT-LEDGER · PUBLIC-HYGIENE |
| **NEVER MEASURED (6)** | PLANE-PURITY · KIND-ISOLATION · EQUALITY · **PARITY** · KERNEL · TELLER-STEPS |

Verified this session: `cargo check --workspace --all-targets --locked` exits **0**, zero errors.
BUILD is red on fmt/clippy/test, **not** on compilation. Earlier reports of `PlaneDecl::STUB` and a
missing `openapi_schemas` do not reproduce at HEAD.

---

## 5. RULES THAT BIND EVERY AGENT ON THIS MAP

- **Money is binary — right or wrong.** Bank-auditor standard: would it pass? If not it is wrong.
- **The ledger is the truth; money is a view on the ledger × the rate card.** Money is never wrong —
  the ledger or the rate card is wrong. Say which.
- **Never self-approve a billed byte (#10/#59).** PARK it: cell, diff, root cause, recommendation.
- **A row without EVIDENCE is not a row.**
- **A gap and a failure must never be the same output.**
- Never push `dev`, `qa` or `main`. Snapshot before any delete. No AI attribution on any commit.

---

*Sweeps 1–20 dispatched 2026-09-22. This file is the tracker; it is updated as each lands. When the
map locks, `1.6.0-map-proof.md` and the remaining scratch docs fold into it and are deleted.*


### Sweep 15 batch 7 — 8 findings, and the sharpest instrumentation of the night

**This agent caught its own false zeros before reporting them.** Its first two sweeps returned a
confident clean NO — because zsh does not word-split an unquoted `$FILES` and macOS `xargs -a` is
unsupported, so both **silently searched nothing**. It built a selftest requiring known-present
patterns to return >0, re-ran everything through `tr '\n' '\0' | xargs -0`, and retracted a
1,175-item false count that had mistaken MCP method names and git branch names for file paths.
Every NO it reports comes from an instrument proven able to produce a YES on the same run. **That is
the standard.**

| X | finding | verified |
|---|---|---|
| X-115 | **A live money gate enforces DECISION #87, which does not exist.** `plane_pricing_blindness.rs:3` — *"THE RULING THIS ENFORCES. DECISION #87 (owner-locked 2026-09-22)"* — plus `gates/mod.rs:146,2254`, `full_gate.rs:472,479`, its test, and four docs. **The table ends at 85.** The ruling is real but **unnumbered**: it lives in #43 and Part 7 §13(3). An agent told to cite a row and never re-litigate looks up #87, finds nothing, and either deletes a live RED-provable money gate as unfounded or reopens a settled money ruling. | ✅ by me |
| X-116 | **The spec tells every agent to ignore a line that names real blocking work.** See below — this one implicates the READ-FIRST hook itself. | ✅ by me |
| X-117 | **The gates disagree about how many planes exist**, and `reachability.rs` carries **both rosters** — #48's five at `:43`, the retired four-plus-voice at `:173`. `plane_purity/scanner.rs:29`, `plane_pricing_blindness.rs:94` and `kind_isolation.rs:373,425` all carry the four. `qa/teller-steps.json` adds `admin`, which #5 says is not a plane. | |
| X-118 | **#73's Pass/Grant rename map is both required and not required to have owner sign-off.** `:413` (OWNER-LOCKED) demands *"rename map owner-approved"*; `:438` and `:960` say *"Architect follow-ups (no owner Q)"*. It renames the money-write tokens `LedgerToken`/`ExitToken` and the security root `KernelSeal`, and `seal-witness` would then read green against an unapproved map. | |
| X-119 | **The fold arithmetic contradicts itself and its base is stale.** `:1188` says 57 → 34, *"twenty-two still fold"*; `:3551` says **23**. 57−34 = 23 — and **57 is stale, the tree has 49**, so 15 remain. `:1885` instructs *"do not re-derive these; cite the ledger"*, so the wrong number is quoted **by instruction**. | |
| X-120 | **Six deleted scripts are cited as live instructions**, including — worst — `scripts/changelog-register-check.sh` named inside **`testing/shadow-oracle/accepted-differences.json:12`, a live data file**: *"scripts/changelog-register-check.sh is the gate."* It does not exist. Also `plane-purity-lint.sh`, `plane-abi-neutrality.sh`, `release-order-lint.py` (one doc says it *now fails CI*, another says it *passes*), `full-gate.sh`, `enumerate-cells.py`. Each has a real xtask replacement. | |
| X-121 | **`crates/busbar-core-transport` is the exact form #37 bans** — *"no `busbar-core-<kind>` ever"*, and transport is one of the seven kinds. It carries a gate excuse at `kind_isolation.rs:289` that #37's text does not sanction. | ✅ by me |
| X-122 | **`CHANGELOG.md:23` — customer-facing — says "the live-voice plane."** It correctly calls OpenAI Realtime and Gemini Live *dialects*, then hangs them off a plane #18 says must not be named that. `README.md:365` says the model *"spans three planes"*; there are five. | |

#### X-116 in full — the trap is in the orientation everyone reads first

The spec says: *"**Rejected, do not resurrect:** D36… and D37… If a stale checklist reports these as
'todo', it is wrong — **ignore it**."* The READ-FIRST hook repeats it to every agent, every turn,
describing D36 as *"split each dialect into its own plugin / add a Dialect kind."*

**`1.6.0-TRACKER.md:123`'s D36 says the exact opposite of that.** It reads *"dialects are
plane-internal, **not** crates and **not** a kind"* — it **agrees** with #4 — and it carries:

> **MEASURED, and blocking**: the contract's closed `Kind` enum must carry exactly the seven kinds…
> (auth's outbound `decorate` is an operation, not a separate variant).

**That is X-95. That is the owner's ruling R1 tonight. That is the work a builder is implementing
right now.** The identifiers collided: the spec's D36/D37 name *rejected proposals*; the TRACKER's
D36/D37 name *tasks that enforce the correct rule*. Same label, opposite referent — and the
instruction to ignore them is the first thing every agent reads.

D37 is the same shape: it says admin and oauth2 are cleanliness crates and **not** a kind, agreeing
with #5, and carries owed gate rows for keeping a cleanliness crate unmetered.

**Nothing was lost — R1 was reached independently. But it was reached despite the instruction, not
because of it.** The fix is a rename, not a ruling: the TRACKER rows are about #4 and #5 and should
be numbered as such.

### Sweeps 7, 8, 16 — 6 findings, and the largest dead subsystem in the release

**X-123 — the entire egress walk/route engine has no caller.** `busbar-kernel-egress`'s
`EgressUnit::route()`, `walk::walk`, `RouteRequest`, `Capacity` and `Permit` are fully implemented
and fully tested. `ProductionUnits.egress` is built **once** (`root/kernel.rs:732`) and then
**never read** — zero `.egress` field accesses tree-wide. `RouteRequest { .. }` is constructed in
exactly one place: a test. Only `LlmUnit` and `GauntletKernelUnit` implement `RouteAwait`; **neither
`units_mcp.rs` nor `units_a2a.rs` implements it at all** — and `root-mcp`/`root-a2a` are in the
crate's **default** feature set. LLM, the plane that actually dials upstreams, uses a **separate
parallel implementation inside `busbar-llm`**.

That one unwired engine explains two findings that looked independent:

| X | finding |
|---|---|
| X-123a | **`Denylist` (`busbar-kernel-egress/src/trust/net.rs:1498`) has zero production constructions.** The cloud-metadata **SSRF** deny/allow set for MCP and A2A outbound dials never exists at runtime; the guard evaluates against an empty set. The structs that would carry it — `NetSeam`, `A2aBindings` — are themselves constructed only in their own test files. |
| X-123b | `Permit`/`Capacity` (`ports.rs:249`) — the pool-concurrency-slot abstraction has no production backing store; its only implementor is a test harness. |
| X-124 | **`hook_veto::Veto` is a complete security seat with zero callers.** `HookGate` has **zero production implementors**; `hook_veto::gate()` — the only consumer of `Veto` — has **zero callers anywhere, test or production**, outside its own tests. The "first veto wins" kill-switch is inert on every deployment. And `busbar-llm`'s separate `VetoSeat` is *also* uninstalled (self-documented as such). **Both veto mechanisms in the tree are dead on the Approve step.** |
| X-125 | `KeyMaterial` and `SecretValue` (`busbar-contract/src/kinds.rs`) have zero construction **and zero read-side calls**. No production code wraps a resolved secret in the one type designed to gate its exposure behind `KernelSeal`. |
| X-126 | **`rate_card_version: 0` hardcoded at `units_voice.rs:1846,1950`** while a2a, mcp and llm all resolve it through `card_at(…)`. A2A and MCP each carry a comment calling this exact defect already-fixed: *"a literal zero here was `HistorySeq::OPENING`… a false reading on every deployment that has ever changed a price."* Not an active mischarge — voice's root path is boot-sealed today — but **`root-voice` is default-on**, and the PR that flips the serving switch inherits the regression with zero lines touched. A landmine, not a fire. |
| X-127 | `float_to_secs()` (`egress_auth/mod.rs:99-107`) guards `is_finite() && >= 0.0` then casts to `u64` with **no upper bound**. A token endpoint returning `expires_in: 1e20` saturates to `u64::MAX` seconds — **an effectively eternal egress-auth token.** Same class as the rate clamp, single site rather than a diverged twin. |

**All 19 `qa/unconstructed.toml` rows independently confirmed**, each re-grepped rather than trusted.
Its misses are X-123, X-124 and X-125 — none of them named in the register.

**The file that measures this class almost produced a 6× over-report of itself.**
`xtask/src/gates/unconstructed.rs` quotes `Ledger::dual_writing(` and `.signing_with(` six times as
**string-literal test fixtures**. An agent counting hits rather than reading lines would have marked
a shipped capability dead. It read every line.

### Sweep 16 — the positive control PASSED, and the instrument was caught lying

**The NAT64 SSRF fix is PRESENT.** `busbar-kernel-egress/src/trust/net.rs` carries `embedded_ipv4()`
handling `64:ff9b::/96` (RFC 6052) and `64:ff9b:1::/48` (RFC 8215), called from
`ip_is_cloud_metadata()`, exercised by three test files. The fix that was lost was found and landed.

**But the inherited instrument's "found in HEAD" verdict was wrong 4 times in 12 (33%)** — because
its search never excluded `docs/design/*.md`, so **a documented-but-unfixed gap read as harvested**.
Audit prose citing a hole counted as the hole being filled. Every TRASH verdict in this sweep was
re-confirmed by finding the actual matching file in HEAD; anything not personally re-grepped was
marked KEEP-FOR-REVIEW instead.

| X | finding |
|---|---|
| X-128 | **Branch survivors worth merging**, verified against HEAD: `money_invariants_tests.rs` (the 7-term money identity — every import resolves against today's `busbar-kernel-ledger`, the production surface exists, the test was never ported) · `units_admin/crossed.rs` (tests the sixty-six money verbs against the composition root; target dir exists, file does not) · `the_secret_never_leaves_the_unit.rs` (asserts the egress-auth unit is the only holder of a clear-text credential). |

**Coverage, honestly:** 55 of a 129-path priority subset, drawn from 1,386. Money complete;
auth/secrets, egress and plugin-ABI partial; **~1,200 paths unswept.** The remainder is the map's
largest open edge.

### Gate falsification sweep + money verification — 3 findings, 1 finding struck

**X-49 IS STRUCK, and how it was struck is the point.** The agent did not grep and declare; it
**reverted the shared seam to the pre-fix `v.as_u64()` in an isolated worktree** and ran the suite.
Intact: 38 passed, 0 failed. Neutered: **26 failures, every one of the six dialects**, each showing
`left: 0` against the true count. That is a positive control on the *fix* rather than on the search.
**A finding disproved by experiment is worth more than a finding confirmed by grep.**

| X | finding |
|---|---|
| X-129 | **`plane-purity:backwards` is VACUOUS, and so are five of its siblings.** They grep for the crate name **`busbar_core`**, which ceased to exist when it was absorbed into `busbar_kernel`. **Proven by plant**: a real backwards reach spelled `busbar_kernel::profile::Stage` left both `plane-purity` and `plane-purity-strict` **fully green**, while the dead-name equivalent correctly went red. Also covers `plane-purity-strict`'s `category:BACKWARDS` and all four `test-reach:*` rows. The gate has been passing a ban it cannot see for as long as the crate has been gone. |
| X-130 | **`plane-purity --selftest` exits 1 on HEAD** — reproduced deterministically at `--jobs 1`, so it is a real defect and not load. The `scan-denominator` rule's message was reworded to *"…yielded ZERO files"* while the fixture still asserts on `"neutral_files=0"`. **`plane-purity` has no `REPORT_ONLY` posture to excuse it**, so any runner scoring the selftest tier is already red. Same disease as X-129 one layer up: a stale string literal outliving its subject — one fails loud, one fails silent, and **the loud one is the tree telling you about the quiet one.** |
| X-131 | **The rate-clamp fix is built, proven, and blocked on a row only the owner can write.** Measured on real code at config rate `1.0e18` micro-units/token: **kernel returns `18446744073709551615`, ledger returns `0`** — the same input, opposite failure modes. The existing kernel test covers only `f64::INFINITY`, which is exactly why the finite-but-huge case survived. The delegation fix works — RED→GREEN on both boundaries, `cargo check` exit 0, 1,847 kernel tests and all 11 money-equivalence tests pass. **But it needs a `busbar-kernel → busbar-kernel-ledger` manifest edge, and `kind-isolation:deps` demands a `[[dep]]` row in `qa/kind-isolation.toml` for every measured edge.** Authoring a verdict for a money-path edge is self-approval (#10/#59), so the agent refused. The gate is **already red at HEAD with 27 findings; this makes 28** — one more of a shape the baseline already contains. **One owner-authored row unblocks it.** Patch held at `/tmp/fix2-rate-nanos-delegation.patch`. |

**Gates proven SOUND by plant→red→revert→clean** — these can be trusted and need no further audit:
`release-order`, `service-images`, `workspace-deps`, `segregation`, `unconstructed`, plus
`plane-purity`'s PATH-INCLUDE/SYMBOL/TYPE/KEY/DIALECT rows and `plane-purity-strict`'s five
non-BACKWARDS categories.

**`reachability`'s decision rows confirmed vacuous by plant** — a 2-line stub at
`units_decision.rs` flipped both rows red and grew `root-module`'s undeclared list from 2 to 3,
proving the `None => Ok(...)` auto-pass rather than inferring it.

### The wiring attempt — nothing wired, and that is the correct outcome

A builder was sent to wire the four never-constructed capabilities. **It wired none of them**, and
its reasoning is better than the wiring would have been: *each is blocked on a missing **source**,
not a missing call.* It refused three separate wirings that would have **struck a capability's RED
status while leaving the capability inert** — which is the exact false-green
`xtask/src/gates/unconstructed.rs` exists to prevent.

| X | finding |
|---|---|
| X-132 | **The audit chain's READ side is already wired — only the key is missing.** `NodeAudit::keys()` (`units_admin/mod.rs:756-767`) builds an `AuditKeySet` for the admin read and returns an **empty set on every node**, because `record.public_key_hex()` is `None`. **`auth.signing_key` is not a substitute, on three independent grounds from the tree's own docs:** it is **fleet-shared** (*"every node resolves the SAME secret"*) so any node could forge any other's chain, destroying the one property an audit signature has; it is **rotated as a revocation mechanism**, and `AuditKeySet` requires keys be only ever *added* because *"a key removed makes every record it signed unverifiable, which is indistinguishable from forgery"*; and it is optional, so the chain would be signed or not as a side effect of an unrelated auth decision. A per-node `audit.signing_key` moves the **frozen config schema snapshot**, gated byte-for-byte. **Owner decision.** |
| X-133 | **`open_export` has no reachable argument.** `export.<n>.module` fails closed against a **const list** — `prometheus \| request-log-webhook \| request-log-file \| otlp` (`config/sections.rs:434-440`). `preflight.rs:110-112` says it outright: *"it just needed no registry, because the export vocabulary is a const list."* No config can name an export plugin, so the loader can never be called. Also no `App` slot for a `DynExport`, and the route table is built from built-ins alone. |
| X-134 | **`open_plane` is blocked by BOOT ORDER, structurally.** `register_planes()` runs at `main.rs:601` inside `fn main()`. Config — and therefore the plugin registry built from `plugins.dir` — loads at `main.rs:996`, **after**. And `install_planes` panics if called twice *or* *"after the plane list was first read"*, which `--validate` does immediately at `:598-600`. **There is no point in the process where the registry exists and the plane list is still writable.** The drop-in half of #11(2) for the plane kind is not a missing call — it is currently impossible. Tracker H6 owns the C-ABI→native `PlaneDecl` adapter; the spec sizes it at 2,513–4,148 LOC. |
| X-135 | **`qa/unconstructed.toml` carries no row for either veto seat.** Unconstructed *and* untracked by the register built to track exactly this. |

**X-124 sharpened — the real seam is the other one.** `busbar-llm`'s `VetoSeat` is **already
constructed and called on a production path** (`units_llm.rs:1327-1332`, threaded from the mount);
only the list is empty (`NATIVE_SEATS = &[]`). `hook_veto::gate` has **no call site at all**,
returns a third refusal vocabulary (`Approval::{Approved,Refused,Vetoed}`) that **no step signature
in the tree accepts**, and `HookGate::veto(&self)` **takes no arguments** — it is a pre-computed
verdict whose producer its own doc concedes *"lives in the plane host"* and does not exist.
**Recommendation: delete `busbar_kernel_scope::hook_veto`** as a duplicate spelling with no feed, no
consumer and no call site.

**And seating the real hooks is a money change.** `busbar-llm/src/unit/approve.rs:27-32` says it in
its own words: *"Seating them here would move a veto from after a charge to before one, which
changes what is billed; that is a behaviour change wearing a refactor's clothes."* **PARKED.**

---

# OWNER RULINGS RECEIVED

## R2 — `busbar-core-transport` violates #37 (2026-09-22, owner: "AGREED")

`#37` rules *"no `busbar-core-<kind>` ever."* **transport is one of the seven kinds**, so
`crates/busbar-core-transport` is precisely the banned form — and it carries a gate excuse at
`kind_isolation.rs:289` that #37's text does not sanction. **The crate is renamed and the excuse is
deleted.** The legitimate core tier is `busbar-core-admin`, `busbar-oauth2`, `busbar-substrate-values`.

## R1 — auth is ONE kind; direction is a TRANSPORT concern (2026-09-22, owner)

> *"auth and egress auth is ONE plugin — direction is a transport concern. this transport this auth
> this direction."*

**This closes C1 and unblocks X-87, X-88 and X-95 — one ruling, three findings.**

The declaration form is **(transport, auth, direction)**. Direction is a property of the *leg*, not
of the plugin: the same auth plugin verifies an arriving credential on an inbound leg and signs an
outbound request on an outbound leg. It is one implementation used two ways, exactly as #3 already
said and as the code did not.

**What this makes true, and the order to do it in:**

1. **`crates/busbar-contract/src/plugin.rs` — delete the `EgressAuth` variant.** `Kind` becomes the
   seven #3 locks: `Plane, Transport, Auth, Store, Secret, Hook, Export`. This is the root; every
   other item below is downstream of it and several stop being wrong the moment it lands.
2. **Direction moves to the declaration site** — a plane declares its needs per leg as
   `(transport, auth, direction)`. Direction never appears in a kind name, a crate name, or an ABI
   symbol.
3. **`xtask/src/gates/construction/rules2.rs:307`** — replace `"pure_auth"`/`"egress_auth"` with the
   single key `auth`. **Today both resolve to zero crates**, because `kind_globs()` returns an empty
   `Vec` for an absent key instead of erroring, so `auth-admin-tokens` and `auth-static-plugin` are
   scanned by nothing. A `busbar-kernel` dependency creeping into either would not be caught.
4. **`kind_globs()` must ERROR on an unknown key, not return empty.** This is the actual root cause
   and it will recur under a different name if only the spelling is fixed. Zero is the passing
   answer to every ban.
5. **`xtask/src/gates/construction.rs:242-252`** — the completeness oracle is built from the same
   stale vocabulary, so checker and checked share the bug. Same fix.
6. **`selftest.rs:882-889`** — the positive control plants a bad dependency into one crate per kind
   and plants **nothing** for those two, because they resolve to zero crates. Whether the instrument
   can produce a NO is untested for the one kind it exempts. Must plant into `auth`.
7. `FORBIDDEN_KINDS` in `kind_isolation/truths.rs:57-62` already bans the string `"egress-auth"` —
   correct by design, a detection list. **Leave it.** After (1) it is the only place the string
   should survive.

---

# OWNER QUESTIONS — for 2026-09-23, 09:00

Rulings I will not make myself. Each is blocking real work, each has the evidence beside it, and
none is a judgement call I am entitled to (#10/#59: never self-approve a billed byte; and the spec
is yours).

## A0. **THE HOOK TELLS EVERY AGENT TO IGNORE REAL BLOCKING WORK** — read this one first

`1.6.0-TRACKER.md`'s D36/D37 do **not** say what the orientation says they say. They state the
*correct* rule and carry measured, blocking work — including the seven-kind `Kind` enum you ruled on
tonight. The spec's "ignore it" and the READ-FIRST hook's summary both point at the wrong referent.
**Rename the TRACKER rows to #4/#5 and amend the hook**, or the next agent discards the same work
again. This is the only item here that actively causes harm every hour it stands.

## A. THE SPEC CONTRADICTS ITSELF — three live, all load-bearing

**A1. `CostHold` reserve/settle: shipped, or forbidden?** `BUSBAR-1.6.0.md:812-814` lists metering
reserve/settle under *"Scope discipline for 1.6.0 — DO NOT implement."* The correction box at
`:729-767`, same Part, says it **SHIPPED and the streaming plane is the production caller**. An
agent reading the discipline list rips out a live money path. Which governs?

**A2. Universal plugin rule (1) — live or superseded?** `:65` states *"each plugin has both ABIs."*
DECISION **#30** at `:354` says that shorthand is **"SUPERSEDED."** Both are live text. Every
plugin-kind conformance judgement depends on the answer.

**A3. Decision `#87` is cited in three documents. The table runs 1–85**, verified contiguous. Is
`#87` a real ruling that never landed as a row, or a typo for `#43`?

## A4. THREE CAPABILITIES BLOCKED ON A SOURCE ONLY YOU CAN AUTHORISE

**A4a — the audit chain's signing key.** The read side is wired and returns empty on every node.
`auth.signing_key` cannot be reused (fleet-shared, rotated-as-revocation, optional). A per-node
`audit.signing_key` `SecretRef` plus key-set persistence is the fix, and it **moves the frozen config
schema snapshot** — deliberately, with your signature.

**A4b — seat the migrated hooks at Approve, or leave `NATIVE_SEATS` empty and say so.** Seating them
moves a veto from *after* a charge to *before* one. That is a billed-byte change. Also: delete
`busbar_kernel_scope::hook_veto`? It has no feed, no consumer and no call site.

**A4c — open `export.<n>.module` to plugin-resolved names?** Today it is a closed const list, so
`open_export` can never be called. Opening it changes a config **error** surface — configs that are
refused today would boot. That needs a signature.

## B. MONEY — parked, never self-approved

**B1.** `/usage` speaks `spend_micros`; `/groups/{name}/usage` and `/keys/{id}/usage` speak
`spend_cents`. **10,000×** for any client aggregating across them. Neither ledger nor rate card is
wrong — it is a wire-contract split. Standardise on micros, or version the surfaces?

**B2.** #79's dated card history is honoured on `/usage` **only**. The other two price flat off the
current card, so the surfaces are **non-reconcilable even after B1 is fixed**. Route them through
`card_at(arrived_ms)`?

**B3.** `crates/busbar-kernel/src/cost.rs:138` lacks the magnitude clamp its twin at
`busbar-kernel-ledger/src/cost/rate.rs:40-47` has. Same config input → `0` (free) on one path,
`u64::MAX` nanos/token on the other. **And `no_float_money.rs` cannot scan that file** — it is
outside the gate's hardcoded scan set, so no float added there will ever go red.

**B4. ~~STRUCK~~** — disproved by mutation experiment; the fix is landed in all six readers. **Replaced by: the rate-clamp fix is built and proven but needs ONE `[[dep]]` row in `qa/kind-isolation.toml`** for the `busbar-kernel → busbar-kernel-ledger` edge. Writing a verdict for a money-path edge is self-approval, so no agent will author it. The gate is already red at 27; this makes 28. Patch at `/tmp/fix2-rate-nanos-delegation.patch`. **This is the single cheapest money fix on the list and you are the only one who can unblock it.**

**B5.** `docs/advisories/1.6.0/mysql-store-split-flush-request-count-loss.md` advertises a landed
fix for a billing/rate-cap defect. The crate it cites does not exist, the function occurs nowhere
but in the advisory, and its commit is **not an ancestor of HEAD**. Withdraw the claim, or land the
fix?

## C. ARCHITECTURE — one answer unblocks a class

**C2.** `busbar-core-admin` does `impl PlaneMeta for AdminPlane` against #5 (*admin is a cleanliness
crate, not a plane*). Remove the impl, or amend #5?

**C3.** **P1 "Post-1.6.0" — plane/transport plugin loading.** A 2026-09-06 decision predating the
ban, never revisited against it or against #39 twelve days later. In or out?

**C4.** `busbar-api` retirement (#34/#35) promised an enforcing gate. It does not exist, the crate
has 18 dependents, and `full_gate.rs` builds it as permanent. Build the gate, or amend the decision?

## D. RELEASE ENGINE

**D1.** #67 locks `predev` → `dev` train-only. **`pr-land.sh:52` defaults `base="dev"`** and
`promote.sh:109` has **no `predev dev` case at all** — the promotion the decision requires exits 1.
Four docs including public `SECURITY.md` teach the old model. Fix the tooling to #67, or amend #67?

**D2.** All **8 `ci-runners-*.sh` are present and executable**, and `docs/ci/self-hosted-runners.md`
documents the fleet at ~$9.36/hr. #56 says it is torn down to zero. Delete the scripts, or mark them
retired?

## E. INSTRUMENTS THAT CANNOT FAIL — policy, one ruling covers all

**E1.** Several gates are green because they scan nothing: 24 dead scope paths in
`qa/audit-ledger.json`; eight `ports-only` ceilings keyed on a symbol that appears once, in a
comment; `secret-hygiene-gate.sh` wired `SECRET_GATE_REPORT_ONLY=1` at both CI call sites.
**Proposed rule: a gate that scans zero files is RED, not green** — the pattern
`plane_pricing_blindness.rs` already implements. Adopt as a Law?

---

## APPENDIX — TWO OPERATIONAL RULES EARNED TONIGHT

**1. A falsification plant must be self-cleaning, or it must not touch the real tree.**
Two sweep agents planted a known violation into `crates/` to prove their gate could produce a RED —
correct practice, and exactly what they were told to do — then were killed by rate limiting in the
window between planting and removing. One plant (`planted_kernel_reach()` referencing the deleted
`busbar_core`) **would have broken the workspace build** for every other agent.

> **The rule: plant into a scratch copy, or wrap the plant in a trap that removes it on any exit.
> An instrument you prove by editing the shared tree is an instrument that can leave the tree
> broken when you die.**

Both plants reverted; `cargo check --workspace --all-targets --locked` exits 0, zero errors.
Nothing was tampered with — `qa/design-bindings.json` is byte-identical to HEAD, and the
`.audit-bak` beside it was cleaned up by its own author.

**2. Agents must not spawn agents.**
Eleven sweeps fanned out to ~76 forks on their own initiative. That drove machine load to 29 while
the reaper's counters read near-zero — it counts rustc, cargo, test binaries and the product, and
**`xtask` gate processes match none of those patterns**, so it reported an idle box while eleven
cores were busy. The fan-out then triggered sustained server-side 429s that killed a dozen agents
mid-flight, several of them holding findings they never got to report. One died having written
*"three well-corroborated, code-verified contradictions"* — and named none of them.

> **The rule: sequential work that finishes beats parallel work that dies.** Every re-dispatch now
> carries an explicit ban on sub-agents, a work-in-priority-order instruction, and a statement that
> partial coverage honestly reported is the preferred outcome.

The forks did outlive their parents and report independently, so the work was not wholly lost —
four doc-sweep batches landed that way and produced X-91 through X-104. That is luck, not design.

---

# 6. DOC CONSOLIDATION — RULE #58, "DELETE, DON'T SUPERSEDE"

*Executed 2026-09-22 against the full-repo snapshot `busbar-all-refs-2026-09-22-2051.bundle`
(2,393 refs). Everything named DELETED below is recoverable from it. Content was folded BEFORE
deletion, never after.*

**Why this section exists.** Twenty-odd design docs stood in `docs/design/` describing a tree that
no longer exists. Agents steer by whatever checklist they meet first, and two of these docs
actively instructed against a fix that has already LANDED (see X-136). This is the same broken
pair as everything else in §1: **doc ↔ code**, where the doc is the copy that gets read.

**The rule applied to every candidate:** a doc dies only when nothing machine-readable reads it —
no gate, script, workflow, rustdoc comment, or `qa/*` artifact. Where a doc held a decision,
finding or owed item the keep-set lacked, it was folded into a row here first.

## 6.1 The landscape that made these stale

`busbar-core`, `busbar-substrate`, `busbar-caps`, `busbar-api` and every `busbar-unit-*` crate
except `busbar-unit-transport-key` were deleted by #19/#36/#37. Each doc below inventories,
measures or plans against those crates and carries **no staleness marker**, so it reads as a live
description of the tree.

## 6.2 DELETED — and what was folded out first

| doc | held what the keep-set lacked | disposition |
|---|---|---|
| `SEAMS.md` (717 ln) | a ~170-trait inventory framed as LIVE, over `busbar-core`/`busbar-substrate`/13 `busbar-unit-*`. §3.4's "do not change before ship" list is **anti-#79** → **X-136**. §1.12b E5 (`open_export` has no non-test caller) is already a live row in `qa/unconstructed.toml:265`, whose citation was repointed here — and **X-133 supersedes it with the stronger cause** (the export vocabulary is a const list, so no config can even name an export plugin). | folded → DELETED |
| `rate-card-epochs.md` (325 ln) | §7 recommends `/usage` keep pricing flat off the CURRENT card — **the exact behaviour #79 names as the defect** → **X-136**. Superseded the same day by `rate-card-history.md`, never deleted. | folded → DELETED |
| `D33-legacy-retirement.md` (530 ln) | §5's measurement lesson, folded as **operational rule 3** below. Its §7 measurement is cited by `ARCHITECTURE.md:107`; the ceiling FIGURES it justifies are already inline in ARCHITECTURE, so only the derivation is lost, and the crates it measured no longer exist. | folded → DELETED |
| `plane-decl-registry.md` (286 ln) | 3 owner questions. Q1 (`busbar-core-config` vs `busbar-substrate::config`) and Q2 (`busbar-unit-egress` duplicated failover constants) are **MOOT — all three crates are deleted**. Q3 (the plane-crate literal) is still live → **X-137**. | folded → DELETED |
| `CODE-ANALYSIS.md` (115 ln) | a production-LOC inventory whose top five rows are all deleted crates (`busbar-core` 77,050). Its verdict ("not a trash-all; a plumbing tear-down with furnishing salvage") is the strategy that was then EXECUTED — it is history, not a plan. | DELETED outright |
| `1.6.0-composition-root-plan.md` (823 ln) | a plan for a composition root that has since been built; every §1.x path names a dead crate. Superseded by the landed root under `crates/busbar/src/root/`. | DELETED outright |
| `1.6.0-waist-rebuild-plan.md` (204 ln) | already recorded as stale in **X-94**; describes `crates/busbar-substrate/src/teller/` and `busbar-core` as "today". | DELETED outright |
| `plane4-seam-audit-B-session.md` (301 ln) | already recorded as stale in **X-94**; cites `busbar-core` and the fat `busbar-substrate`. | DELETED outright |
| `1.5.3-config-consolidation.md` (174 ln) | already recorded as stale in **X-94**; a shipped 1.5.3 design. | DELETED outright |
| `foundation-abi-config.md` (571 ln) | a DECISION-READY ABI/config design against `integration/transport-core`; its "one authored host surface → generated FFI" was never built and the 44-slot vtable it maps has since moved. No live decision the keep-set lacks. | DELETED outright |
| `llm-test-fixture-seam.md` (130 ln) | the `877 → 565 → 23 → 20` ladder counting `busbar_core::` in `busbar-llm`'s test code. The crate it counts is deleted, so the ladder's denominator is gone. | DELETED outright |

## 6.3 KEPT — each because something machine-readable reads it

These were on the stale list but **could not die under the hard rule**. This is the finding, not an
omission:

| doc | what reads it |
|---|---|
| `1.6.0-TRACKER.md` | **`xtask/src/gates/no_deferral.rs:60,310` — `const TRACKER` is `cx.read()` and the gate FAILS if it is absent** ("the tracker could not be read, so no waiver's expiry can be checked"). Every waiver's expiry is checked against a tracker row. Also `qa/teller-steps.json:3` (H2) and `crates/plugin-loader/src/plane.rs:35` (H6). Its D36/D37 mislabel — which made the READ-FIRST hook say "ignore" over live blocking work — **is already fixed in-file** at `:123,:125`, and X-8/X-9/X-10 were already carried into §3.1. **It cannot be deleted until `no_deferral` stops reading it.** |
| `rate-card-history.md` | `ARCHITECTURE.md` **PB-103** cites it §1.1/§2.3/§9 as the design behind the landed #79 behaviour; the citation flows into `qa/design-bindings.json:4925`; `crates/busbar/src/root/units_admin/mod.rs:1047` cites `:445-467` for the D38 sealed-key hardening. It is the SUCCESSOR that superseded `rate-card-epochs.md` and the correct authority for #79. |
| `1.6.0-contract-gaps.md` | `ARCHITECTURE.md:1544` heads its decision register with it; `xtask/Cargo.toml:9`, `xtask/src/denylist.rs:2,86` and `xtask/src/main.rs:2` cite CG-59/CG-60 as the **live rationale for the denylist gate**. |
| `contract-notes.md` | **six rustdoc comments** in `busbar-contract` point at it (`lib.rs:7`, `kinds.rs:3`, `plane.rs:4`, `bounded.rs:4`, `caps/hold.rs:8`, `caps/mod.rs:11`). Its prose was deliberately moved OUT of those `//!` headers to satisfy ARCHITECTURE's LOC ceiling — deleting it destroys rationale the ceiling forced out of the code. Only its framing is stale: the `busbar-caps` modules it describes now live at `crates/busbar-contract/src/caps/`, same filenames. |
| `docs/ci/feature-sets.md` | `.github/workflows/ci.yml:381,1804,2185` name it as "the measurement behind" the feature matrix. |
| `docs/ci/self-hosted-runners.md` | `scripts/ci-runners-lib.sh:42` cites its arithmetic. **FLAGGED, owner's call:** it documents a live EC2 fleet at ~$9.36/hr with 8 working `scripts/ci-runners-*.sh`, while #56 and Part 6 say the fleet is torn down to zero. The scripts were NOT touched — deleting release tooling is not an agent's decision. |

## 6.4 New findings raised by this sweep

| X | finding | evidence |
|---|---|---|
| X-147 | **Two docs instructed against a fix that has already LANDED.** `ARCHITECTURE.md` PB-103 records `GET /api/v1/admin/usage` **NOW RESOLVES THROUGH THE HISTORY** (#79's owed item, landed) — pricing each row at `card_at(row's own first instant)`, with `derive_spend_micros_row` demoted to a FALLBACK. Against that: `rate-card-epochs.md:§7.3` said "**A must nevertheless survive on `/usage`**, and does, unchanged", and `SEAMS.md:§3.4.4` said "**Do not touch `derive_spend_micros_row`** … switching it to the ledger is G3+C13 work, not a cleanup". An agent meeting either doc first would have REVERTED the landed fix. Both deleted. **This is the concrete damage rule #58 exists to prevent.** | `ARCHITECTURE.md` PB-103 (PASSES today) vs the two deleted docs |
| X-148 | **`PLANE_ALTERNATION` is a stale roster in live gate code.** `xtask/src/gates/plane_purity/scanner.rs:29` is `["llm", "mcp", "a2a", "voice"]` — but there is **no `busbar-plane-voice` crate** (#18 made voice a dialect inside `streaming`), and the roster **omits two live plane crates**, `busbar-plane-streaming` and `busbar-plane-decision`. The scanner drives three sites (`:249`, `:274`, `:530`), so plane-purity scans a roster that does not match the tree: a violation in `streaming` or `decision` is not looked for. Same class as X-87 (auth scanned as zero crates) and X-93. | `ls crates/busbar-plane-*` → a2a, decision, llm, mcp, streaming |

## 6.5 Operational rule 3, earned from `D33-legacy-retirement.md` §5

> **A raw `grep` for a crate path counts prose.**
> In a tree whose migration discipline is "relocate the item and leave a comment saying where it
> came from", the comments accumulate exactly where the migration has **succeeded** — so the naive
> reach counter reads HIGHEST precisely where the work is finished. Any legacy-reach number in this
> project must be taken with comments stripped, or it sends the next agent to clean a crate that is
> already clean.

This is the §2 law ("an instrument that cannot produce a NO is not a check") in its inverse form:
an instrument that cannot produce a ZERO is equally useless. It is the one durable thing D33 held.
