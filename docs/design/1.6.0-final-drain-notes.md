# 1.6.0 FINAL DRAIN — drive plane-purity BACKWARDS 42 → 0

Base ref: `aa5dc9c4` (metrics A' gate-fix). `--baseline` at base = **42 BACKWARDS** (TYPE 0, all other
categories 0). Every reach is a `busbar_core::` name in a NON-test busbar-llm source. Goal: repoint each
onto `busbar_substrate::` / `busbar_api::` or reach it through an `EngineHost` seam method, alloc-free,
so `--check` (never run here) can arm.

Green control at base (verified): oracles egress_differential 5 / crossproto_delivery_billing 3 /
on_exhausted 35 / pool_upstream_creds 2 / usage_tap 8 / signal_catalog 6; alloc-gate surgical
`translate_request_cross_protocol == 0`, whole-path `forward_with_pool` steady-state min = **84** (≤107).

## The 42, by `busbar_core::` submodule (from PLANE_PURITY_HITS_OUT)

| submodule | n | symbols | disposition |
|---|---|---|---|
| config | 10 | `PolicyOnError` (9: hooks 41/812/909/963/967, select 16/126, pipeline 1299/1527); `caller_in_hook_groups` (pipeline 2986) | PolicyOnError = **CLEAN identity repoint** (already `pub use`d from `busbar_substrate::config`); caller_in_hook_groups needs `GroupCfg` neutral or a seam |
| state | 8 | `App` (health 26, mod 40, native_ingress 19, alloc_gate 196), `UpstreamClients` (build_runtime 261/276, tables 11), `impl AppEngineExt for …App` (tables 339) | **App cascade** — App-coupled, drained by threading host/tables from the arrival boundary |
| plane_host | 7 | `engine_host_value(app)` (pipeline 843, usage 155, walk 437, native_ingress 55/212/243/511) | **App cascade** — the transitional carrier mint; drains once the host is threaded from the substrate arrival payload |
| ingress | 3 | `admission_door` (native_ingress 247), `arrival_host::ArrivalPayload` downcast (native_ingress 652/653) | **App cascade** — ArrivalPayload must become substrate-owned (`Arc<dyn EngineHost>`+gov+caller_token), single-compile TypeId |
| governance | 3 | `GovState` (response_body 13), `AdmitGrant` (response_body 40, native_ingress 443) | **App cascade** — engine carrier struct fields; core-coupled types, not relocatable → seam/view |
| store | 2 | `LaneRuntime` (pipeline 334, select 223) | trait in core `store`; keystone §4 says relocate trait to `busbar_substrate::store` + `host.lane_store()`. Core-coupled — verify |
| limits | 2 | `default_probe_interval_secs/timeout_secs` (health 138/143) | free fns over core `LIMITS` global; relocate or seam |
| egress_auth | 2 | `MetadataSsrfPolicy` (build_runtime 26), `CredentialProvider` trait (tables 36) | build_runtime is the cold lowering entry; types have `'a`/dyn — verify relocatability |
| telemetry | 1 | `translation` (wire 450) | pure `(from,to)` metric emit but over core metrics infra; wire is the **surgical alloc==0 path**, no host in scope |
| proxy | 1 | tap re-export `fire_stage_taps, gate_rejected, spawn_bounded_tap, GateRejected, StageShape` (hooks 983) | core proxy TAP machinery filling core `StageShape` — NOT the neutral vocab; core-coupled |
| cost | 1 | `CostModel` (response_body 16) | **App cascade** — carrier struct field |
| auth | 1 | `auth_failure_status_and_kind` (pipeline 2546) | pure `fn(proto)->(StatusCode,&str)` over `substrate::proto` + `substrate::proxy::KIND_*` → **relocatable to substrate** |
| admin | 1 | `admin::audit::AUDIT.record_by` (wire 511) | global `AUDIT` static; `host.audit_emit` exists but wire has no host (surgical path) |

## Proven patterns available (all ancestors of base)
- CONFIG-ENUMS `17773658`: PolicyOnError/ProviderAuth relocated to `busbar_substrate::config`, core re-exports
  by-identity. **PolicyOnError repoint is a no-op type-identity change.**
- telemetry host-seam `082ec5eb`: `EngineHost::telemetry_{translation,upstream_attempt,…}` already exist.
- ingress host-seam `de78f4c0`: `EngineHost::{finish_admitted,finish_rejected,pool_label,destination_guard}`.
- REGISTRY A–E: neutral proto/handlers/ingress-error shaper relocated to substrate.
- keystone `a0381801`/§: the alloc-free borrowed-host carrier (`engine_host_value` = 1 Arc::clone).

## Execution order (low risk → App cascade last)
1. **config PolicyOnError** (9) — identity repoint to `busbar_substrate::config::PolicyOnError`.
2. Relocatable singles: **auth** `auth_failure_status_and_kind` → substrate; then limits / egress_auth /
   telemetry / admin as each is confirmed relocatable-or-seamable without a core-dep cycle or alloc hit.
3. **caller_in_hook_groups** + `GroupCfg` (config).
4. **The App cascade** (state 8 + plane_host 7 + ingress 3 + governance 3 + cost 1 + store 2, plus proxy
   tap): thread `&dyn EngineHost` / `EngineTables<'_>` acquired ONCE per request at the native_ingress
   entry (alloc-free `engine_host_value(app)` = one Arc::clone), and pivot ArrivalPayload to a
   substrate-owned payload. AUTHORIZED single medium commit; hard backstop = alloc-gate (surgical==0,
   forward min ≤107). STOP+report if it regresses the alloc-gate or an oracle, or if ArrivalPayload
   cannot cross as a substrate-owned single-TypeId payload.

VERIFY EVERY COMMIT: build; 6 oracle suites byte-count; alloc-gate (surgical==0, forward min ≤107);
`--baseline` BACKWARDS strictly lower; `--selftest`; neutrality.sh; calllog; plane_integration.
