<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- Copyright (C) 2026 Busbar Inc and contributors -->

# 421 — `money-one-function-view` is declared unshipped and has shipped

**RESOLVED 2026-09-24 — owner ruling Q12/Q25c: strike the row. Struck with its KNOWN_UNSHIPPED id in one commit; no longer parked.**

PARKED by W1.1 (Phase 1, stream B), 2026-09-24. The register row's own `switch` says
"MONEY: PARKED-OWNER — an owner signs, not an agent", so no agent strikes it.

| | |
|---|---|
| **cell** | `cargo xtask gate unconstructed` → `unconstructed:stale-declaration` FAIL: "`money-one-function-view` is declared `unshipped` but NOW HAS 1 production construction site(s): crates/busbar-core-admin/src/v1/service.rs:270 `busbar_kernel_ledger::cost::LedgerEntry::new(lane, arrived_ms),`". The row is `qa/unconstructed.toml` `[[capability]] id = "money-one-function-view"`; the id is also on `KNOWN_UNSHIPPED` in `xtask/src/gates/unconstructed.rs`. |
| **diff** | Register: "called by nothing in production, because no production code ever builds a LedgerEntry to feed it". Tree: `derive_spend_micros_row_at_card` (service.rs) builds a `LedgerEntry` and prices it through `price_in_view` on `GET /api/v1/admin/usage`, since `0ff99df5e`. That commit's own red-before-green table: THE ONE FUNCTION = 39000 micro, GET /admin/usage = 39000 micro (so /admin/usage already reads the one function). /groups/{g}/usage and /keys/{id}/usage = 20000 micro, the budget gate = 20000, the hook seam = 24000. Those surfaces do not read it yet. No figure was moved by this slot. |
| **root cause** | The ledger and the ratecard are both correct. The REGISTER is out of date: `0ff99df5e` wired the production producer and did not strike the declaration, because the row's `switch` sends that strike to the owner. What remains open under it is the #42 divergence the service.rs doc states: where `price_in_view` REFUSES (an unpriced lane or class, or an out-of-range accumulator), the read falls back to the legacy projection at the same card, which is the 1.5.5 figure. Turning that served figure into a refusal is a customer-visible money change and belongs to the owner (#10, #59). |
| **recommendation** | The owner signs that `GET /admin/usage` pricing through `price_in_view`, with the legacy fallback on refusal, is the shipped behaviour. Then, in ONE commit, strike the `money-one-function-view` `[[capability]]` row from `qa/unconstructed.toml` and the same id from `KNOWN_UNSHIPPED` (`unconstructed:stale-declaration` goes green; the gate's own rule says the two lists shrink together). Keep the three surfaces still on the 20000/24000 arithmetic as the #42/#79 drain they already are. None of this belongs in the strike commit. |
