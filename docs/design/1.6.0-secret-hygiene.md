# 1.6.0 — secret hygiene: complete + enforce the `Redacted` / `SecretRef` vocabulary (secrets never enter logs, audits, or metrics)

**Owner intent (verbatim):** "secrets can never enter logs or audits. Secret ID yes, not THE
secret. Secrets in busbar core need to be on type [a secret type] not `String` to preserve this." The
vision's audit leg is "audited into a record you can read back later" — reading back must surface a
secret's **identity/reference**, never its plaintext. A competitor (LiteLLM) just shipped PII/PCI
masking across every egress path (spend logs, debug logs, logging-only responses) as *runtime*
masking; busbar makes it a **TYPE guarantee** — a value that is a secret is a different Rust type
from a value that is not, and that type structurally cannot reach a log/audit/metric sink. That type
already exists (`Redacted<T>`); this doc completes its adoption and enforces it.

## Vocabulary (ENFORCED — one name per concept, zero synonyms)

This is the naming law the gate and the migration enforce. Any deviation found in the audit is
listed as a **consistency violation** to fix (§1.4).

| concept | THE name | never |
|---|---|---|
| a secret **VALUE** held in memory | **`Redacted<T>`** (`busbar_api::Redacted`) — `Debug`/`Display` = `[REDACTED]`, value only via `.expose_secret()`, zeroize-on-drop | a bare `String`/`Vec<u8>`/`Option<String>`; a new `Secret<T>` value wrapper; any other synonym |
| a secret's safe-to-log **IDENTITY** (goes in logs/audits/metrics) | **`SecretRef`** (`busbar_secret_ref::SecretRef`) or a plain non-secret id string (`key_id`, `AccessKeyId`) | the value; a `Redacted` (never log the value even redacted-by-accident) |
| resolving identity → value | **`SecretResolver`** / the `SecretResolve` seam (`SecretRef` → `Redacted<T>`) | — |

**The `Secret*` prefix is reserved for the REFERENCE / RESOLVER / CONFIG machinery** — `SecretRef`,
`SecretResolver`/`SecretResolve`, `SecretError`/`SecretErrorKind`, `SecretModule`, `SecretForm` — and
is **never** used to name a value wrapper. There must be **no competing `Secret<T>` value type**; the
one secret-value type is `Redacted<T>`.

**Status.** This is a design + audit doc only. No code changed. A live refactor is touching
`busbar-core`/`busbar-llm` concurrently (the "everything is a plugin" pivot, see
[`1.6.0-technical-changes.md`](1.6.0-technical-changes.md)); the implementation plan in Part 4 is
sequenced to land **after** that pivot's `api_key_plaintext` DTO lands.

**Key finding up front — THE TYPE ALREADY EXISTS; do not build a new one.** busbar already ships the
secret type this task asks for, in two halves:

- **the VALUE wrapper — `busbar_api::Redacted<T: Zeroize>`** (`crates/api/src/redacted.rs`): a
  working redacting/zeroizing/**non-serializing** secret wrapper (tested in
  `crates/api/src/tests/redacted_tests.rs`), already threaded through most in-memory secrets (state
  `api_key`, oauth `client_secret`, minted bearer, MCP subject/upstream tokens, the auth-plugin
  submitted credential — see §1.1);
- **the IDENTITY / "secret id" — `SecretRef`** (`crates/secret-ref/src/lib.rs`): the non-secret
  stable reference (`env:VAR`, `file:/path`, `module '<name>'`) that IS safe to log and audit — the
  "read it back later" half of the owner's ask;
- resolved through **`SecretResolver`** / the `SecretResolve` seam (`crates/api/src/secret.rs`,
  `busbar-core/src/config/secret.rs`); `zeroize` is already a workspace dep (`Cargo.toml:273`).

Adoption is **partial**. The work is therefore **not to invent a type** — it is to (1) **complete
adoption**: close the remaining bare-`String`/`Vec<u8>` secret holes the audit below enumerates,
(2) **enforce it**: add a gate so the tree cannot regress to a bare-`String` secret, and (3) confirm
`Redacted` + `SecretRef` are sufficient **as-is** and recommend at most minor additions (Part 2).
The freshest hole — the in-flight `api_key_plaintext: String` on the substrate build-input DTO —
does not exist in-tree yet and is the first conversion target once it lands.

---

## Part 1 — AUDIT: every secret-bearing value + every leak sink

### 1.0 What already protects a secret today (existing mechanisms)

| mechanism | where | what it guarantees |
|---|---|---|
| `Redacted<T: Zeroize>` | `crates/api/src/redacted.rs` | `Debug`/`Display` print `[REDACTED]`; **not** `Serialize`/`Deserialize`; zeroize-on-drop; constant-time `Eq` (via `constant_time_eq`); value reachable only via `.expose_secret()` |
| host credential **mint** with `Zeroizing<Vec<u8>>` | `crates/busbar-core/src/plane_host/creds.rs:46` | resolved provider plaintext lives host-side only, keyed by an opaque `u64` `resolved_ref`; the secret **never crosses the plane seam** (FLAG-4 confinement); zeroized on drop |
| `SecretRef` (identity, **not** value) | `crates/secret-ref/src/lib.rs:53` | config secrets are a **reference** `{ module, settings }`, never an inline literal; its `Deserialize` even rejects a bare string *without echoing it* (`visit_str`, line 144) |
| manual redacting `Debug` | `CredentialSecret` (`crates/api/src/store.rs:543`), a2a `Lease` (`crates/busbar-a2a/src/a2a/creds.rs:316`) | `{:?}` prints `<redacted; present>` instead of the value |
| `SecretForm` storage discipline | `crates/api/src/store.rs:439` | a credential kind must explicitly declare `Recoverable`/`Digest`/`None`; `CredentialMeta` (the listing type) has **no secret field to leak** |
| bounded metric label space | `crates/busbar-core/src/metrics.rs:30-42` | every label is a finite operator-controlled enumeration; free-form / secret values "MUST NOT appear as metric labels" (documented contract) |

`secrecy`/`zeroize`: `zeroize` **is** a workspace dep (`Cargo.toml:273`), used by `Redacted` and by
`plane_host::creds`. The `secrecy` crate is **not** used anywhere. `Redacted` is the home-grown
equivalent.

### 1.1 Secret-bearing values and their current type

Ordered **most dangerous first** (dangerous = a bare secret reachable by a `derive`d `Debug`/`Serialize`).

| # | value | file:line | type today | risk |
|---|---|---|---|---|
| **A** | **self-serve issued key** — the *full busbar token shown to the caller* | `crates/busbar-core/src/auth/self_keys.rs:33` (`IssuedKey.secret`) | **bare `String` on a `#[derive(Debug,…)]` struct** (`self_keys.rs:31`) | **HIGH — active leak vector.** `{:?}`/`tracing` of an `IssuedKey` prints a live bearer token verbatim. Must become `Redacted<String>`. |
| **B** | Google service-account **private key** + minted **access token** | `crates/busbar-core/src/egress_auth/jwt_bearer.rs:299` (`ServiceAccount.private_key`), `:310` (`TokenResponse.access_token`) | bare `String`, `#[derive(serde::Deserialize)]` | MED — inbound-only derive (no `Debug`/`Serialize`), but held bare in memory, not zeroized; JWT **signing_key** class. |
| **C** | OAuth client-credentials minted **access token** | `crates/busbar-core/src/egress_auth/oauth_client_credentials.rs:175` (`TokenResponse.access_token`) | bare `String`, `Deserialize` | MED — same shape as B; the cached bearer that authenticates every egress hop. |
| **D** | rotated bearer **token** | `crates/busbar-core/src/governance/mod.rs:614` (`RotatedCredential.token`) | bare `String`, no `Debug` derive | MED — minted signed token held bare; one `derive(Debug)` addition away from A. |
| **E** | AWS **SigV4 secret access key** (recoverable) | `crates/api/src/store.rs:527` (`CredentialSecret.secret`) | bare `String`; **manual** redacting `Debug` (`:543`); **`Serialize` IS derived** (load-bearing — crosses the store plugin ABI as JSON) | MED — Debug-safe, but the value serializes by design; the *value* leaving the process is intended (store persistence), so this stays serde but should be typed to make the boundary explicit. |
| **F** | a2a delegation-credential **secret** | `crates/busbar-a2a/src/a2a/creds.rs:320` (`Lease.secret`) | bare `String`; **manual** redacting `Debug` (`:316`); but `#[derive(PartialEq, Eq)]` on the struct (`:315`) | MED — Debug-safe, but the derived `Eq` compares `secret` **byte-by-byte (timing side-channel)**; `Redacted`'s constant-time `Eq` would close it. |
| **G** | mint-API response **token** + SigV4 secret | `crates/busbar-core/src/admin/v1/contract/schema.rs:66` (`token`), `CreatedKeyView` (`:50`, `#[derive(Serialize)]`) | bare `String`, `Serialize` | LOW-by-intent — this is the *once-shown* wire response; `Serialize` is the whole point. Type it `Secret` with an **id-only** `Serialize` would break the API, so this is an **allowlisted intentional egress**, documented, not converted. |
| **H** | static-auth plugin configured **token** | `crates/auth-static-plugin/src/lib.rs:34` | bare `String` | LOW — separate plugin crate, deserialized config; convert opportunistically. |
| **I** | self-signing **key material** | `crates/busbar-core/src/auth/self_keys.rs` region / `governance/signing.rs` | signing key bytes | audit: JWT/token **signing_key** — verify it is not on a `derive(Debug)` path (governance signer). |

**Already-wrapped (correct today), for completeness:**

- lane provider `api_key` → `Redacted<String>` (`crates/busbar-core/src/state.rs:27`; built at
  `appbuild.rs:695`; exposed at the egress seam `proxy/engine/walk.rs:553`, `health.rs:302`).
- OAuth confidential-client `client_secret` → `Redacted<String>`
  (`egress_auth/oauth_client_credentials.rs:23`, `auth/token.rs:73`).
- minted egress **bearer** → `Redacted<String>` (`egress_auth/bearer_token.rs:37`).
- admin token → `Redacted<String>` (`appbuild.rs:1137`, `preflight.rs:449`).
- MCP RFC-8693 **subject_token** / **upstream credential** / static bearer → `Redacted<String>`
  (`crates/busbar-mcp/src/mcp/client/egress.rs:61,82,109,111,388,400`; `upstream.rs:386`).
- auth-plugin **submitted** login credential → `Vec<(String, Redacted<String>)>`
  (`crates/api/src/auth.rs:166`); crosses the one plaintext wire boundary via `.expose_secret()`
  at `crates/busbar-plugin/src/cold/auth.rs:420`.

### 1.2 CRITICAL — the in-flight `api_key_plaintext` path

The pivot introduces `api_key_plaintext: String` on the substrate build-input DTO
(`crates/busbar-substrate/src/plane_host/build_input.rs`, `LlmBuildInput`/`LlmLaneInput`). **It is
not in the tree yet** (`grep -rn api_key_plaintext crates/` → 0 hits;
`crates/busbar-substrate/src/plane_host/build_input.rs` does not exist). When it lands, resolved
plaintext provider keys will flow **core → plane** through it:

```
config SecretRef  →  SecretResolver.resolve_string()  →  LlmBuildInput.api_key_plaintext: String
   →  build_runtime lowering  →  upstream auth header (Authorization / x-api-key / SigV4)
```

This is a **new** bare-`String` secret on a DTO that (per the pivot) will be `Debug`/serde-touched
across the build seam — the single most important field the secret-hygiene work must convert, because
it is the freshest and it crosses a crate boundary. Note this **partially contradicts** the existing
FLAG-4 confinement (`plane_host/creds.rs` was built specifically so the plane holds an opaque
`credential_ref` and the *host* owns plaintext); if the pivot really moves plaintext across into the
plane's build input, converting the field to `Redacted<String>` is the *minimum*, but the **preferred**
fix is to carry a **`SecretRef`** (id) on the DTO and resolve plane-side via `SecretResolve`, like the
mint path already does — so no bare secret ever exists on the DTO. **Flag for the pivot author**
(Part 4, step 2).

### 1.3 Leak SINKS — can a secret reach each?

| sink | mechanism | can a secret reach it? |
|---|---|---|
| **(a) `derive(Debug)`** | any struct transitively holding a secret | **YES via A** (`IssuedKey`, derives Debug, bare `secret: String`). NO for B–F (no Debug, or manual redacting Debug). This is the one active hole. |
| **(b) `tracing`/`log`/`eprintln!`/`println!`** | formatting a secret-holding struct or field | Reachable **only** through (a): a `tracing::info!(?issued_key)` or `{issued_key:?}` prints A's token. Every `Redacted` field is safe here by construction. ~~No direct `println!(secret)` found in production.~~ **THAT SENTENCE WAS TRUE AND USELESS — see sink (f).** |
| **(c) AUDIT record path** | `AuditRecord` (`crates/api/src/store.rs:816`); `PlaneAuditLog` (`plane/auditlog.rs:266`); `calllog.rs`; plane-record `body` bytes (`store.rs:886+`) | `AuditRecord` fields are metadata only (action/resource/outcome/principal/hash) — **no secret field** (`store.rs:814` states this). RISK is the **opaque `PlaneRecord.body`** (caller-serialized bytes): a plane could `serde_json::to_vec` a struct that embeds a secret. Prevented **structurally** if the secret is `Secret`/`Redacted` (neither implements `Serialize`, so a body that embeds one won't compile). |
| **(d) metric labels** | `counter!`/`gauge!`/`histogram!` in `metrics.rs` | **NO** — labels are a finite operator-controlled enumeration by documented contract (`metrics.rs:30-42`); no secret-typed value is passed as a label today. Gate keeps it so. |
| **(f) A MESSAGE THAT REACHES A CALLER** — `format!`/`write!`/`panic!` enclosed by an `Err(...)`/`map_err`/`ok_or_else`/`errors.push(...)` | interpolating a secret-bearing value into the TEXT of an error that is RETURNED, not logged | **YES, AND IT HAPPENED TWICE** (49d781bd1). This row did not exist when the audit was written, which is why row (b)'s `println!(secret)` sentence was a true statement about the wrong question: the audit looked at TYPES, DERIVES and SINKS and never at INTERPOLATION. `parse_proxy` rendered the raw `$HTTPS_PROXY` (`user:password@`) with `{v:?}` into a refusal that becomes a boot panic; `pem_to_pkcs8_der` interpolated `base64::DecodeError`'s Display — which names a byte OF THE KEY BODY — into a string `config/validate` returns to a **read-scope** caller. Both are now covered by the scanner's **Check 3** (Part 5). |
| **(e) `serde::Serialize` on a secret-holder** | hits wire/disk/log | **YES by design** for E (`CredentialSecret`, store ABI) and G (`CreatedKeyView`, once-shown mint response). Both are **intended** plaintext egresses, not accidents. Everywhere else, `Redacted`'s absence of `Serialize` prevents it. |

**Summary of real exposure:** exactly one *accidental* live vector (**A**, `IssuedKey` derives
Debug over a bare token), a cluster of bare-but-not-yet-logged holds (**B, C, D**), two
Debug-safe-but-not-typed values (**E, F** — F has a timing-`Eq` bug), two intentional serde egresses
(**E, G**), and one incoming offender (**§1.2 `api_key_plaintext`**).

### 1.4 Vocabulary consistency violations (against the enforced naming law)

Two classes: (i) **bare-`String` secret VALUES** — every §1.1 item A–I where the value is a bare
`String`/`Vec<u8>`/`Option<String>` instead of `Redacted<T>` (that whole table IS the value-side
violation list; fixed by Part 4). (ii) **`Secret*`-prefix used for a value, or a value-wrapper
synonym** — the naming-law violations proper:

- **No competing `Secret<T>` value type exists today** — good; the one value type is `Redacted<T>`.
  (An earlier draft of *this* doc proposed a `Secret<T>` wrapper + `SecretId`; that proposal is
  **withdrawn** to keep `Redacted<T>` the sole value name.)
- **`CredentialSecret`** (`crates/api/src/store.rs:523`) — a **type name using the `Secret` suffix
  for a value-bearing record** (it carries `secret: String`). Tension with the law, but this is an
  established **store-plugin-ABI** record (serde is load-bearing) and the `Secret` here reads as
  "credential + its secret material", not as an in-memory value wrapper. **Verdict: allowed
  exception**, documented — do *not* rename (ABI-breaking); the gate allowlists it (§Part 3). Its
  in-engine `.plaintext()` output should still be wrapped in `Redacted` post-lookup.
- **`SecretForm`** (`store.rs:439`), **`SecretModule`/`SecretError`/`SecretResolve`**
  (`crates/api/src/secret.rs`) — all **reference/resolver/config machinery**, correctly using the
  `Secret*` prefix per the law. No violation.
- **field named `secret: String`** on `Mint` (`plane_host/creds.rs:46`, wrapped in `Zeroizing` —
  acceptable value protection), `Lease` (`a2a/creds.rs:320`), `IssuedKey` (`self_keys.rs:33`),
  `CredentialSecret` (`store.rs:527`) — these are *field* names, not type synonyms; the law targets
  the value *type*, so a field literally named `secret` is fine **provided its type is
  `Redacted<T>`/`Zeroizing`** (or an allowlisted serde exception). Today `Lease.secret` and
  `IssuedKey.secret` are bare `String` → value-side violations (class i), fixed in Part 4.

**No synonym for the `Redacted` value wrapper was found** (no `Sensitive`, `Masked`, `Protected`,
`SecretString`, etc. naming an in-memory value type). The vocabulary is clean on the type-name axis;
the debt is purely (i) incomplete adoption.

---

## Part 2 — the type is `Redacted<T>` + `SecretRef`: sufficiency assessment (do NOT build a new type)

**Recommendation: keep `Redacted<T>` + `SecretRef` exactly as they are.** They already satisfy every
property the owner named. Do **not** introduce a new `Secret`/`SecretString` type and do **not** adopt
the `secrecy` crate — both would be a lateral rewrite that re-touches every already-correct call site
(§1.1) for zero new guarantee, and `secrecy` would still need a bolt-on for the identity half that
`SecretRef` already provides. The only additions this doc recommends are **minor and optional**
(§2.3). Below is the property-by-property check.

### 2.1 Does `Redacted<T>` deliver the required properties? — YES

| required property | delivered by `Redacted<T>`? | evidence |
|---|---|---|
| (a) `Debug`/`Display` redact — never the value | **YES** — both print `"[REDACTED]"` | `redacted.rs:59-70` |
| (b) NOT `Serialize`/`Deserialize` (can't hit audit/wire/disk) | **YES** — deliberately implements neither; comment calls it structural | `redacted.rs:15-22` |
| (c) value reachable ONLY via a greppable `.expose_secret()` | **YES** — the sole accessor, documented as the audit point | `redacted.rs:46-48` |
| (e) `zeroize`-on-drop | **YES** — `T: Zeroize`, `Drop` overwrites | `redacted.rs:72-76` |
| (f) `Clone`/`Eq` sane — constant-time `Eq` | **YES** — `Eq` routes through `constant_time_eq`, avoiding prefix-timing | `redacted.rs:92-98` |
| ABI safety — must not serialize into cold JSON **or** hot `repr(C)` lane | **YES structurally** — no `Serialize` ⇒ cold lane won't compile a struct embedding it; not `repr(C)`/not POD ⇒ hot lane rejects it | — |
| home crate shared by all crates incl. single-compiled substrate | **YES** — lives in leaf `busbar-api`, re-exported `busbar_api::Redacted` (`lib.rs:45`); depended on by core/substrate/llm/mcp/a2a/plugin. **Keep it there — do not move to `busbar-core`.** | `crates/api/src/lib.rs:45` |
| no-panic / edge-validator posture | **YES** — no `unwrap`/panic; `expose_secret` is an infallible borrow | — |

### 2.2 Does the "Secret ID yes, not THE secret" / read-back-later half exist? — YES, via `SecretRef`

Property (d) — "carries or pairs with a non-secret STABLE IDENTITY that IS safe to log and audit" —
is already met by **`SecretRef`** (`crates/secret-ref/src/lib.rs:53`). It holds only
`{ module, settings }` (no secret material — stated at `lib.rs:34`), derives `Debug`/`Serialize`
safely, and exposes `.describe()` → `env:ANTHROPIC_API_KEY` / `file:/run/secrets/x` /
`module '<name>'` (`lib.rs:112`). **This is the exact "secret id" the owner means:** the audit/read-back
path records the `SecretRef` (or its `.describe()`), never the resolved value.

The one structural nuance: `Redacted<T>` and `SecretRef` are **separate** values today — the wrapper
does not itself carry its ref. In practice that is fine, because the code paths that hold a
`Redacted` already hold the corresponding config `SecretRef`/`key_id`/`AccessKeyId` right next to it
(the lane, the credential row, the virtual key). The audit-write pattern is therefore a **convention**
— "log the `SecretRef`, never the `Redacted`" — not a type guarantee that the two travel together.

### 2.3 The only gaps + the minor, optional additions recommended

1. **Adoption is incomplete** (the real gap — Part 1 §1.1 items A–I). This is a *migration* problem,
   not a *type* problem. Fixed by Part 4.
2. **No compile-time pin that `Redacted` stays non-serde.** Someone could add
   `#[derive(Serialize)]`-adjacent glue in a future edit. Recommend a **compile-fence test** in
   `busbar-api` asserting `Redacted<String>: !Serialize`/`!Deserialize` (Part 3, Check 3). Minor,
   additive.
3. **The id/value pairing is a convention, not enforced.** *Optional, only if the owner wants the
   pairing to be structural:* add a thin helper that binds them at the audit boundary — **not** a new
   wrapper type:

   ```rust
   // Optional convenience in busbar-api — NOT a replacement for Redacted. Pairs a held secret with
   // the SAFE reference to record when auditing/logging "which secret was used", so a call site
   // physically cannot log the value where it means to log the id.
   pub struct Held<T: Zeroize> { pub reference: SecretRef, secret: Redacted<T> }
   impl<T: Zeroize> Held<T> {
       pub fn new(reference: SecretRef, secret: Redacted<T>) -> Self { Self { reference, secret } }
       pub fn expose_secret(&self) -> &T { self.secret.expose_secret() }
       /// Safe to log/audit — the identity, never the value.
       pub fn reference(&self) -> &SecretRef { &self.reference }
   }
   // Debug derives safely: `reference` is non-secret, `secret` prints `[REDACTED]`.
   ```

   **RULING (2026-09-22): `Held` is NOT BUILT, and that is not a deferral — it is a design refusal, which is
   a different thing and must not be written in the banned vocabulary.** The word *defer* is struck. `Held`
   adds **no protection `Redacted<T>` does not already give**: it composes `SecretRef + Redacted<T>` and its
   `expose_secret` is just as unsealed as the one it wraps. Its only value is making the id/value pairing
   structural at an audit boundary, and **no call site in the tree asks for that today**. Building an 8-line
   type with zero callers is precisely what acceptance gate §11a’s *"no 0-caller ABI"* and v5’s *"implement
   only what the planes use"* rule forbid. If a concrete audit call site appears, it is 8 lines and one
   afternoon — it is not held back by anything. The convention + the gate (Part 3) close the practical risk.

   > ### ⚠ AND WHILE RULING ON `Held`, A MUCH LARGER CONFLICT SURFACED IN THIS DOCUMENT.
   >
   > §1’s Vocabulary table (see “never” column) bans *“a new `Secret<T>` value wrapper”* and states
   > *“there must be **no competing `Secret<T>` value type**; the one secret-value type is `Redacted<T>`”*.
   > **DECISIONS #54 is OWNER-LOCKED (2026-09-20) and mandates exactly that type:** *“a first-class
   > `Secret<T>` (not merely a `Redacted` wrapper + gate): the TYPE itself enforces the safety … raw value
   > access is a single explicit sealed method (KernelSeal, #40)”*. This document and the lock cannot both
   > stand. Measured 2026-09-22: `grep -rn 'Secret<' crates --include='*.rs'` → **0 hits**; `Redacted<T>`
   > (`crates/busbar-contract/src/redacted.rs:35`, 149 lines, named by 49 files) has no seal on
   > `expose_secret` (`:46`), which has **43 call sites**. Separately, **#53’s witness is unimplemented**:
   > `scripts/secret-hygiene-gate.sh` runs REPORT-ONLY in both places that invoke it
   > (`.github/workflows/ci.yml:1450`, `qa/segments.toml:371`) and `xtask/src/gates/ship_ready.rs` has no
   > secret row at all, so *“a bare/unwrapped secret fails the ship gate”* is not true today.
   >
   > That is a locked release blocker with no row. It is now **TRACKER S1/S2/S3** and is owner/architect
   > work, not an edit this document may make to itself. Found by the banned-word adjudication,
   > `docs/design/1.6.0-map-proof.md` §A. For
   **minted/stored** secrets (bearer, sigv4, issued key) whose identity is a `key_id`/`AccessKeyId`
   rather than a config `SecretRef`, the same helper can take a plain reference string; again, only if
   a real call site needs the pairing enforced.

### 2.4 What legitimately stays serde / bare `String` (not `Redacted`)

- `CredentialSecret.secret` (§1.1 E) — must serialize across the **store plugin ABI**. Keep as-is
  (manual redacting `Debug` at `store.rs:543` + `Serialize`); after a store lookup, wrap the
  `.plaintext()` output (`store.rs:538`) in `Redacted` for in-engine handling so live memory holds a
  `Redacted`, not a bare `&str`.
- `CreatedKeyView.token` / SigV4 fields (§1.1 G) — the **once-shown mint response**; `Serialize` is
  the whole purpose. Allowlisted intentional egress (Part 3).
- the auth **wire** boundary `CompleteLoginRequest.submitted` — the documented single plaintext
  crossing (`crates/busbar-plugin/src/cold/auth.rs`), filled from `Redacted` via `.expose_secret()`
  (`:420`). Stays a plain `String` on the wire type by design.

---

## Part 3 — the GATE: `scripts/secret-hygiene-gate.sh`

Modeled on `scripts/plane-grep-gate.sh` (substring scan, comment/doc/test stripping, path-scoped
allowlist). Conservative by design — false positives are silenced by the allowlist, never by
weakening the rule. **THREE SCANNER CHECKS** (Check 1 and Check 2 below, Check 3 in Part 5) plus a
Rust compile fence, each failing with a file:line and a remedy.

**Check 1 — no bare secret field.** For each known-secret identifier
(`api_key`, `api_key_plaintext`, `client_secret`, `private_key`, `signing_key`, `access_token`,
`subject_token`, `bearer`, `password`, `credential_secret`, `secret_access_key`, and a bare field
literally named `secret`/`token` on a struct whose name matches `*Key*|*Cred*|*Token*|*Secret*`),
in production `.rs` under `crates/`, flag a declaration of the form
`<ident>: String | &str | Vec<u8>` that is **not** wrapped in `Redacted<…>` / `Zeroizing<…>`. Remedy:
wrap the value in **`Redacted<T>`** (the one value type); if it must appear in a log/audit, carry a
**`SecretRef`**/id instead. This check also bans any *new* `Secret<T>`-named value wrapper (the naming
law: `Redacted` is the only value type). Allowlist entries (token × path × reason):
- `crates/api/src/store.rs` `CredentialSecret.secret` — store-ABI serde egress, manual redacting Debug.
- `crates/busbar-core/src/admin/v1/contract/schema.rs` `CreatedKeyView`/`token` — once-shown mint API.
- the auth wire boundary `CompleteLoginRequest.submitted` — documented single plaintext crossing.

**Check 2 — no secret at a log/audit/metric site.** Fail if `.expose_secret()` appears on the same
statement (a bounded window, e.g. same line or within the macro's paren span) as any of:
`tracing::` / `log::` / `println!` / `eprintln!` / `dbg!` / `panic!` / `counter!` / `gauge!` /
`histogram!` / `metrics_emit` / `journal_append` / an `AuditRecord`/`PlaneAuditLog` constructor.
Also fail on a `{:?}`/`{}`/`?ident`/`%ident` of a *known-secret-typed local* inside a `tracing`/`log`
macro. This is the "`.expose_secret()` used at a log/audit/metric site" rule.

**THE COMPILE FENCE — `Redacted` never gains serde.** *(Called "Check 3" when this was written. It is NOT a scanner check — it is a Rust test, it lives in a crate, and it runs under `cargo test`, not under `secret-hygiene-gate.sh`. The scanner had exactly two checks and now has three, so the number is reassigned to the scanner's third — Part 5 — and the fence is named for what it is. Nothing was deleted; it ships today at `crates/busbar-contract/src/tests/redacted_no_serde.rs`, moved there by the #84 `busbar-api` → `busbar-contract` fold.)* A compile-fence test
(`crates/busbar-contract/src/tests/redacted_no_serde.rs`) that asserts `Redacted<String>` is **not** `Serialize`
and **not** `Deserialize` — e.g. a `trybuild` "must-not-compile" case, or a negative trait probe:

```rust
// A blanket helper impl'd for all T; a competing `impl<T: Serialize> NotSerialize` would make this
// ambiguous ONLY if Redacted were Serialize — the coherence conflict is the assertion.
trait NotSerialize {}
impl<T> NotSerialize for T {}
fn _assert_not_serialize(s: &Redacted<String>) { fn is<T: NotSerialize>(_: &T) {} is(s); }
// Plus a trybuild `compile_fail` fixture: `serde_json::to_string(&redacted)` MUST fail to compile.
```

Wire `secret-hygiene-gate.sh` into `scripts/full-gate.sh` / CI alongside the other grep gates; ship it
**warn-only for one commit**, then **flip to blocking** in the same commit that finishes the last
conversion (Part 4, step 7), so the tree is green when the gate arms.

---

## Part 4 — implementation plan (sequenced AFTER the in-flight pivot lands)

Green-per-commit; each step names its files (from Part 1) and the oracle that proves no behaviour
change. **Money-path oracles must stay byte-identical** — the plaintext still reaches the upstream
header via `.expose_secret()`; only the *type* changes. Steps that change what an **audit record**
contains (value/nothing → id) are **intended** changes needing a golden update — called out
explicitly and kept distinct from money-path goldens, which must NOT move.

1. **No new type — add only the enforcement fence.** `Redacted<T>` and `SecretRef` already exist and
   stay as-is. The one additive change here is the **compile-fence test** in `busbar-api` pinning
   `Redacted<String>: !Serialize`/`!Deserialize` (Part 3, Check 3). Oracle: `redacted_tests.rs`
   unchanged + the new fence compiles/asserts. Purely additive, no behaviour change. *(An earlier
   plan to add a `Secret<T>` value type is withdrawn — the value type is `Redacted<T>`, one name.)*

2. **Convert the freshest offender FIRST — `api_key_plaintext`.** *Gated on the pivot landing.* When
   `crates/busbar-substrate/src/plane_host/build_input.rs` introduces
   `LlmBuildInput.api_key_plaintext: String`:
   - **PREFERRED: don't carry plaintext across the DTO at all.** Carry a **`SecretRef`** (the
     identity) on `LlmBuildInput` instead and let the plane resolve it to `Redacted<String>` via the
     `SecretResolve` seam (`crates/api/src/secret.rs:181`) plane-side, at the moment it builds the
     upstream header. This keeps the FLAG-4 confinement the `plane_host/creds.rs` mint already
     establishes (plane holds a *ref*, host/plane-local resolution owns plaintext) and means no
     bare secret ever exists on the DTO. This is the recommended shape — flag it to the pivot author.
   - **FALLBACK (only if resolution cannot move plane-side): `Redacted<String>`, never bare
     `String`.** The `build_runtime` lowering then `.expose_secret()`s at the upstream-header build
     only, and records the lane's `SecretRef` for audit.
   Oracle: the Bedrock/Anthropic/OpenAI money-path egress-header golden — **byte-identical**.

3. **Convert config secret-resolution outputs.** `SecretResolve::resolve_string`
   (`crates/api/src/secret.rs:188`) and `resolve_builtin_string` (`:193`) currently return bare
   `String`; have the *engine-side* resolver hand back `Redacted<String>` at the point of resolution
   (carrying the originating `SecretRef` for the audit-write path) so plaintext is `Redacted` from
   birth, not wrapped a few lines later. Keep the low-level `resolve_builtin` byte function as-is for
   the TLS/PEM raw-bytes path (wrap in `Redacted<Vec<u8>>` at the caller). Oracle:
   `config/tests/secret_tests.rs`, `resolver_tests.rs` — fail-closed behaviour byte-identical (error
   strings never echo values, already true).

4. **Convert the egress credential holders (B, C, D, F) to `Redacted<T>`.**
   - `jwt_bearer.rs` `ServiceAccount.private_key` (`:299`) + `TokenResponse.access_token` (`:310`) →
     `Redacted<String>`; `read_credential`/signer use `.expose_secret()`.
   - `oauth_client_credentials.rs` `TokenResponse.access_token` (`:175`) → `Redacted<String>`.
   - `governance/mod.rs` `RotatedCredential.token` (`:614`) → `Redacted<String>`; audit/response logs
     the key id (`SecretRef`/`key_id`), never the token.
   - a2a `Lease.secret` (`creds.rs:320`) → `Redacted<String>` — this also **fixes the timing-`Eq`
     bug** (the derived byte-wise `Eq` at `:315` becomes `Redacted`'s constant-time `Eq`). Oracle:
     `egress_auth/tests`, `a2a` creds tests — header output byte-identical; the a2a `Eq`-change is a
     *fix*, assert with a new constant-time test, not a golden move.

5. **Convert `IssuedKey` (A) — the active leak — to `Redacted<T>`.** `self_keys.rs:33`
   `secret: String` → `Redacted<String>`; the struct keeps `#[derive(Debug)]` safely because
   `Redacted`'s `Debug` prints `[REDACTED]`, and any log records the `key_id` (safe id) instead.
   Oracle: a **new** test asserting `format!("{issued_key:?}")` no longer contains the token
   (red before, green after) — this is the headline fix.

6. **`UpstreamCreds` / credential-provider + SigV4 (E).** Where the store hands back
   `CredentialSecret`, wrap `.plaintext()` (`store.rs:538`) into `Redacted<String>` for the in-engine
   SigV4 signing context so `crates/busbar-substrate/src/sigv4.rs::sign_v4(secret: &str, …)`
   (`:165`) is fed via `.expose_secret()` at the signer call only. Leave `CredentialSecret` itself
   serde (store-ABI, allowlisted). Oracle: the SigV4 known-answer test
   (`busbar-substrate/src/sigv4.rs` tests, AWS worked example) — byte-identical signature.

7. **Audit/calllog stores the reference, not the value + arm the gate.** Sweep the
   audit/calllog/provenance path (`crates/busbar-core/src/calllog.rs`, `plane/auditlog.rs`,
   `plane_host/journal.rs`) to confirm no secret reaches a `body`; where any credential is referenced,
   record its **`SecretRef`** / non-secret id (e.g. `env:ANTHROPIC_API_KEY`, `cred:sigv4:0`) so "read
   it back later" surfaces the identity, never the value. Then land `scripts/secret-hygiene-gate.sh`
   (+ Check-3 compile-fence) and **flip it blocking**. **Intended golden change:** any audit/calllog
   golden that previously logged *nothing* (or, in the pre-fix state, risked a value) now logs a
   `SecretRef`/id — update those goldens as a deliberate, reviewed change, and note in the commit that
   they are **audit** goldens, explicitly **not** money-path goldens (which stay frozen).

**Byte-identity call-outs.** Money-path (upstream header / SigV4 signature) goldens: **frozen** — the
plaintext still egresses identically via `.expose_secret()`. Audit/calllog goldens: **intended to
change** from nothing/value → `SecretRef`/id (steps 5 and 7). a2a `Eq` change (step 4): a
**behavioural fix** (timing side-channel closed), covered by a new test, not a golden.
</content>
</invoke>

---

## Part 5 — CHECK 3: a secret must not be INTERPOLATED INTO A MESSAGE A CALLER RECEIVES

**Governing decision: `docs/design/BUSBAR-1.6.0.md:383` (#53, OWNER-LOCKED 2026-09-20)** — *"The
secret-hygiene scan is a hard release blocker … the scan for in-the-clear secrets (bare-`String`
holds etc.) moves from report-only to BLOCKING."* Check 3 is part of that scan; §5.6 rules on the
flag.

### 5.1 Why this check exists — two leaks shipped with the instrument watching

Commit `49d781bd1` fixed two committed disclosure sites. Both were the same shape, and **neither was
in range of Check 1 or Check 2, structurally**:

| site | what leaked | why it reached a human |
|---|---|---|
| `crates/busbar-kernel/src/egress/engine/mod.rs` `tunnel::parse_proxy` (3 refusal arms) | the RAW `$HTTPS_PROXY`/`$HTTP_PROXY`/`$ALL_PROXY` value via `{v:?}` — these carry `user:password@` (RFC 3986 §3.2.1) in every deployment that authenticates its proxy | the refusal is not swallowed: `resolve_config` → `install_proxy_tunnel_if_configured`, whose `Err` the llm engine's runtime build turns into a **boot panic with no `catch_unwind`**. stderr, crash report, CI log. |
| `crates/busbar-kernel/src/egress_auth/jwt_bearer.rs` `pem_to_pkcs8_der` | `base64::DecodeError`'s own `Display`, which names a byte **of the key body** — `InvalidLastSymbol` prints a *valid* base64 character, i.e. **six bits of the operator's RSA private key** | `validate_credential` → `config_validate` → `errors.push(…)` → the `errors` array admin `POST /api/v1/admin/config/validate` returns to a **READ-SCOPE** caller. **A privilege boundary, not log hygiene.** |

**The gate was blind to the whole class, and its own self-test said so out loud:**

1. **Check 1 scans struct FIELD DECLARATIONS.** Both sites are a fn *parameter* (`parse_proxy(v: &str)`)
   or a third-party error's `Display`. The gate's GREEN selftest case asserted, in as many words,
   that a fn param `api_key: String` **must NOT be flagged**. That is *true of Check 1* — a parameter
   has no derived `Debug`/`Serialize` to leak through — and it was *read by the whole file* as "not a
   violation." **That case encoded the blind spot.** It is not deleted (the statement it makes about
   Check 1 is correct and worth keeping); it is now **PAIRED** with `c1_green_param_is_c3_red`, which
   takes the identical source text and proves Check 3 sees it.
2. **Check 2 needs `.expose_secret()` on the same statement as a SINK**, and `format!` was not in
   `SINKS` at all. A secret interpolated into a message that is **returned** rather than **logged**
   had no rule anywhere in the file.
3. **CI runs it `SECRET_GATE_REPORT_ONLY=1`** (`.github/workflows/ci.yml:1460-1466`), so even its
   in-class findings blocked nothing.

§1.3 of this document said *"No direct `println!(secret)` found in production."* True, and about the
wrong question. Row (f) of that table now exists.

### 5.2 The rule

> A value whose **binding, source type, or declared subject** is secret-bearing must not be
> interpolated into a `format!` / `write!` / `panic!` / `expect` that is **enclosed by** an
> `Err(…)` / `map_err` / `ok_or_else` / `errors.push(…)` / diagnostic call.

**The remedy is redaction AT THE FORMATTING SITE, never deleting the diagnostic.** An error that no
longer says which setting was wrong is a worse error than one that says too much. Both real fixes
keep the failing variable, the failing field and the reason — only the value goes, and the check is
proven to tell the fix from the leak (§5.4).

Three rules, one class. Each has its own RED fixture **and its own GREEN negative control**, because
a rule that flags everything satisfies a red arm exactly as well as a correct one:

| rule | evidence it uses | what it is for |
|---|---|---|
| **A `secret-named-binding`** | the value RENDERS UNDER a secret name — Check 1's tail rule (`f == N \|\| f ends with _N`) applied to the **last path segment**, because that is what reaches the string (`cred.meta.public_id` renders `public_id`, not `cred`) | the ordinary case: `Err(format!("… {api_key}"))` |
| **B `decoder-error-on-secret-input`** | a decoder error's `Display` interpolated **where the decoder's INPUT is a secret**, evidenced by the decoded binding's NAME (`hex_seed`) or by the message's own SUBJECT (`private_key`) | the `jwt_bearer` shape and LEDGER **S21**'s `pack.rs` companion. `base64::DecodeError` prints the byte, the offset AND the decoded bits; `hex::FromHexError::InvalidHexCharacter { c, index }` prints the character; serde prints `invalid type: string "…"` |
| **C `secret-subject-parameter`** | the message NAMES a secret subject **right where** it interpolates the enclosing fn's OWN PARAMETER (word-boundary match, within 56 chars) | the `parse_proxy` shape — **the only rule that could have caught it.** In that function *nothing* is secret-named: the param is `v`, the local is `url`, the error is `e`. The only evidence the value is a credential is the sentence the author wrote about it. |

`proxy` is a subject word on **measured** grounds: `HTTP_PROXY`/`HTTPS_PROXY`/`ALL_PROXY` are RFC 3986
URLs with a userinfo component, which is the canonical place a corporate proxy password lives.

**Exemptions are rules, not an allowlist** (`ALLOWLIST_C3` ships **empty**, and that is a measurement
— see §5.5): a capture whose source text contains a redactor (`redact`/`mask`/`SecretRef`/
`describe()` — `SecretRef::describe()` renders `env:VAR`/`file:/path`/`none`, never a value); the
safe-to-log IDENTITY nouns, tail-matched (`host`, `port`, `id`, `location`, `reference`, …) which are
*what the remedy asks you to print instead*; and ALL-CAPS consts, which name an env var rather than
hold its contents.

### 5.3 The instrument's own integrity is a HARD failure, never report-only

Same posture `check_allowlist_paths` already takes: *a debt count is a thing to burn down; a scanner
that did not finish is a thing that is lying.* Independent of `SECRET_GATE_REPORT_ONLY`:

- **a non-zero scanner exit code.** Check 3's `awk` bailed out mid-tree on its first run —
  `illegal byte sequence` on an em-dash under a UTF-8 locale — and a bail is a **partial scan that
  still prints a number**. Fixed with `LC_ALL=C` in the scanner; armed as a hard fail regardless.
- **`PARSE-WARN` — the lexer lost string state.** Rust string literals span lines, so `instr` is
  file-scoped; a literal the lexer mis-opens swallows the rest of the file and returns a **silent
  zero**. The first cut did exactly this on `br#"…"#` (the `b` disqualified the raw-string rule, the
  inner `"` flipped the state, every later finding became fiction). Proven both ways in `--selftest`:
  byte/raw strings parse clean *and a leak planted after them is still seen*; a genuinely
  unterminated literal is **reported**, not scanned as zero.

### 5.4 FALSIFICATION — what was actually run

**RETRO-PROOF against the real pre-fix bytes**, through the *shipped* gate (`--scan3`, a mode that
exists so this is never re-argued from a private copy of the rules):

```
git show 49d781bd1^:crates/busbar-kernel/src/egress/engine/mod.rs      > $PRE/egress_engine_mod.rs
git show 49d781bd1^:crates/busbar-kernel/src/egress_auth/jwt_bearer.rs > $PRE/jwt_bearer.rs
scripts/secret-hygiene-gate.sh --scan3 $PRE/egress_engine_mod.rs $PRE/jwt_bearer.rs      # rc=0

secret-subject-parameter        egress_engine_mod.rs:746  [v]   format!("proxy env value {v:?} is not a valid URL: {e}")
secret-subject-parameter        egress_engine_mod.rs:749  [v]   Err(format!("proxy env value {v:?} uses scheme {:?}: …"))
secret-subject-parameter        egress_engine_mod.rs:756  [v]   ok_or_else(|| format!("proxy env value {v:?} has no host"))
decoder-error-on-secret-input   jwt_bearer.rs:88   [e]   serde_json::from_str(&sa_json)   -> "service-account JSON is invalid: {e}"
decoder-error-on-secret-input   jwt_bearer.rs:222  [e]   serde_json::from_str(&body)      -> "token response JSON invalid: {e}"
decoder-error-on-secret-input   jwt_bearer.rs:301  [e]   base64 .decode(body)             -> "service-account private_key base64 is invalid: {e}"
```

**LEAK #1: 3 of 3 arms named. LEAK #2: named.** Against the same two files at HEAD, the fixed sites
report **0** — the check tells the fix from the leak.

**PLANT, in the real tree** (orphan `.rs` under `crates/`, referenced by no `mod` so cargo never
compiles it, created and removed under `trap`), carrying one leak arm and one **negative-control**
arm of identical shape:

```
planted   →  Check 3: 9   (secret-named-binding [client_secret] + secret-subject-parameter [provider])
             SECRET_GATE_REPORT_ONLY=0 exit = 1   (it would block)
removed   →  Check 3: 7   (plant file gone, no git residue)
```

The negative-control arm — `Err(format!("upstream '{base_url}' did not answer after {attempts} …"))`,
the same macro, the same `Err`, a non-secret value — was **not** flagged. Without that arm, a rule
that flags every interpolation would have passed the red arm.

`--selftest` is ALL GREEN with both real leaks as **permanent verbatim fixtures**, the S21 `pack.rs`
shape, the rule-A plant, both fixes as GREEN, an eight-shape negative control, and the lexer proven
RED and GREEN. `cargo check --workspace --all-targets --locked` exits **0**. The scan is
byte-identical across repeated runs.

### 5.5 What Check 3 finds on this tree — **7**, every one read against the source

`ALLOWLIST_C3` is empty because none of these is a false positive.

| # | site | decoder / shape | disposition |
|---|---|---|---|
| 1 | `crates/busbar-kernel/src/egress_auth/jwt_bearer.rs:88` | `serde_json::from_str(&sa_json)` — the SERVICE-ACCOUNT JSON, which *contains* `private_key` | **PARK (auth path)** |
| 2 | `crates/busbar-kernel/src/egress_auth/jwt_bearer.rs:222` | `serde_json::from_str(&body)` — the token-endpoint response, which *contains* the access token | **PARK (auth path)** |
| 3 | `crates/busbar-kernel/src/egress_auth/oauth_client_credentials.rs:165` | same shape, same body | **PARK (auth path)** |
| 4 | `crates/busbar-kernel/src/preflight.rs:908` | `serde_json::from_str::<Map>(settings)` — a `kind: secret` plugin's open-time settings, which is where a vault token lives | **PARK (auth path)** |
| 5 | `crates/busbar-voice/src/topology/minter_https.rs:143` | `serde_json::from_slice(&raw)` — the `ClientSecretResponse` body, i.e. the `ek_` client secret | **PARK (auth path)** |
| 6 | `crates/plugin-sdk/src/pack.rs:537` | `hex::decode(hex_seed.trim())` — `$BUSBAR_SIGN_KEY`, a 32-byte ed25519 seed; `InvalidHexCharacter { c, index }` prints a **nibble of the signing key** | **PARK — this is LEDGER S21's named companion, already `PARKED-OWNER`.** `crates/busbar-kernel-audit/src/sign.rs:35` **already documents it** ("The packaging CLI's own `$BUSBAR_SIGN_KEY` reader does exactly that today and is a separate known defect") — so the tree has carried a written acknowledgement of an unfixed leak. Check 3 is the first instrument that *counts* it. |
| 7 | `crates/secret-example-plugin/src/lib.rs:62` | `serde_json::from_str(cfg)` — `ExampleSecretConfig` **is** the secret map; its values are the secrets | PARK (example plugin, same class) |

`crates/busbar-kernel-audit/src/sign.rs:35` itself is **CLEAN** and is the right pattern: that module
hand-rolls its own hex decoder precisely so `KeyError` "carries no part of the input it rejected —
not the text, not a character, not an offset." It is the sibling LEDGER S21 cites, not a second leak.

**ADJACENT, NOT FLAGGED, WORTH A NAME:** `oauth_client_credentials.rs:160` returns
`format!("token endpoint returned {status}: {}", body.chars().take(200)…)` — a **deliberate,
documented** 200-char cap on a token-endpoint response body, for human diagnosis. Check 3 does not
flag it (the body is not a decoder input and the message names no subject near the capture). On a
non-2xx that body is normally an OAuth error document; it is recorded here because it is the nearest
thing to this class that the check waves through by construction.

### 5.6 `SECRET_GATE_REPORT_ONLY` — the verdict

**A gate whose findings block nothing is an advisory, not a gate,** and #53 says so:
`BUSBAR-1.6.0.md:383` moves this scan to **BLOCKING** before 1.6.0 cuts.

**Arming it today (`SECRET_GATE_REPORT_ONLY=0`) reds CI on 24 pre-existing findings — measured, not
estimated:**

- **Check 1: 17** — the Phase-2 bare-secret holds (`caller_token` ×6, `api_key` ×3, `access_token` ×2,
  `private_key` ×2, `secret` ×2, `credentials`, `token`). Listed in full by `--report`. These are the
  holds Part 4 step 7 converts; §1.3/§2.4 and `BUSBAR-1.6.0.md:2985-3020` already rule them **type
  debt, not a vulnerability** (every one was checked for an actual egress and has none).
- **Check 2: 0.**
- **Check 3: 7** — §5.5, all PARKED.

**The flag is deliberately LEFT AT `1` and NOT flipped in either direction.** Flipping it now would
red a shared branch on 24 findings whose owners have not ruled, none of which this change introduced,
and the only way to make it green today is to loosen a rule — which is the move this file exists to
prevent. **What IS armed now, because it is green today and costs nothing to arm:** scanner exit
code, `PARSE-WARN`, and allowlist staleness all hard-fail **regardless** of the flag (§5.3).

**The condition for arming, stated so it can be checked rather than remembered:** Check 3's 7 are
PARKED for owner ruling; Check 1's 17 are Part 4 step 7's conversion list. **When both reach zero the
flip is one env var in `.github/workflows/ci.yml:1466` and no code edit** — the same one-flag flip
this file has always promised.

### 5.7 What Check 3 still CANNOT see — named, not waved at

- **The SHAPE blind spot is untouched.** `BUSBAR-1.6.0.md:2999-3010` already proves it for Check 1:
  the real AWS secret travels as the 4th element of an unnamed tuple into a `json!` body, and *a
  tuple element has no name to spell*. Check 3 inherits this exactly — it reasons about **names and
  sentences**, so a secret in an unnamed position, behind an alias, or returned by a method whose
  name says nothing (`cfg.get(3)`) is invisible. **A green source-scanning gate is evidence about
  SPELLINGS, not about BEHAVIOUR.**
- **A credential-bearing URL bound to a `*_url`/`*_uri` name is waived by construction.** RFC 3986
  permits userinfo in any URL, so `format!("… {token_url} …")` *could* carry `user:pass@`. Those
  names are in `C3_IDENTITY` because `token_url`/`token_uri` are endpoints this tree names dozens of
  times and the measured alternative fired on every one. **This is the one exemption that trades a
  real hole for signal, and it is written down here rather than buried in a needle list.**
- **A third-party error type this file has not enumerated.** `C3_DECODERS` is a list. A new dependency
  whose `Display` echoes its input is invisible until somebody adds it — the same "every new spelling
  is another needle, forever" that #53's own commentary names. The structural answer is a type the
  compiler recognises, not a wider vocabulary.
- **Cross-function flow.** `let msg = format!("{secret}"); Err(msg)` in two statements is two windows;
  rule A still catches the capture, but a secret laundered through a helper (`fn describe(x) -> String`)
  and *then* interpolated is out of range. Check 3 is a statement-window scanner, not dataflow.
- **A `format!` that builds a VALUE is deliberately out of scope** and must stay so — that is
  `format!("Bearer {}", key.expose_secret())`, a header injection, which Check 2 already proves GREEN.
  The discriminator is whether an err/diagnostic call **encloses** the macro. A leak written into a
  value that is *later* logged is Check 2's question, not this one.
