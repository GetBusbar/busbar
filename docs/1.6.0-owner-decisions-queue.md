# Busbar 1.6.0 — owner decisions queue (morning agenda)

Everything awaiting your call, gathered while you were out. Nothing here was auto-decided. Ranked by weight.

## TL;DR (read this first)

## RESOLVED by owner (2026-09-18)
- **B2 transport-kind: RULED — transport IS A PLUGIN** (not TCB). #3/#11/#30/#39 STAND unamended. A new plane brings its new transport; both are plugins (drop-in or compiled-in, both ABIs). The ABI-review's transport=TCB recommendation is REJECTED; the async-trait/cdylib concern is an implementation problem to solve, not a reclassification. Core-dissolution wave proceeds with transport-as-plugin (transport crates are plugins → 1 repo each, ABI-only).
- **Fleet never-idle: BUILDING** — owner chose auto up/down. PR #120 (`infra/fleet-auto-scale`) ships INERT (report-only; IAM role `busbar-ci-fleet-controller` created least-privilege; nothing mutates until `FLEET_CONTROL_ENFORCE=enforce`). Latchkey rejected for money-path CI (its self-healing can silently turn red→green, corrupting the oracle verdict).
- **kind-isolation:vocab carve-out: HELD** on `gate-fix/kind-isolation-vocab` pending owner ratify (touches a LOCKED gate; revertible).

**What shipped (all on branches/PRs, nothing forced onto dev/qa/main):**
- **5 PRs open, all green in-scope**: #115 codeaudit hardening (12 fixes), #116 plugin-infra (loader HIGH OOB + MED TOCTOU), #117 #38 fold (contract-transport→contract, +private_key Debug redaction), #118 admin-lifecycle test relocation (busbar-llm→busbar-admin, coverage preserved+proven), and **#119 the COMBINED RC** = all four merged in order + 2 test-path fixups, validated (`build --workspace` OK, `clippy --workspace --all-targets -D warnings` CLEAN, targeted tests pass).
- **3 fresh survivor audits**: busbar-contract (post-fold, clean), plane-streaming (voice; CRITICAL first-frame confirmed carried in #115, HIGH lane/idx mis-attribution refined→queued), busbar-llm test fully diagnosed+relocated.
- **2 base-line gate fixes** (mechanical, pushed to the feature line): `audit_ledger` register sync; feature-sets red cleared via #118.
- **Merge order dry-run VERIFIED**: the 4 PRs merge with zero conflicts; the #40 seal and the fold survive together (A1 seal-clobber worry retired).

**3 gates only YOU can clear (everything else is done):**
1. **A0 — money oracle is blocked** by a differ replay-selftest that fails on a stale `SUPERVISOR_MARKERS` path in the external oracle tool (not a divergence; not caused by any fix). Until cleared, NO line gets a money-parity verdict. → fix the oracle tool's expected path (`crates/busbar-admin/src/restart.rs`) or restore the marker where it looks.
2. **A — dev landing**: feature line is 2301 commits ahead of origin/dev. Decide how 1.6.0 becomes dev (promote the RC #119, not bare base — base lacks all the hardening).
3. **ABI rulings + rename/repo-split wave** (B/A2 below): unblock the paused rename wave (ruling B2 transport=TCB + 8 rename items) and the batched contract security cluster (A2).

## 0. Open PRs to review + merge order (built while you were out)
All three are open against the 1.6.0 feature line (`busbar-1.6.0-config-neutral-half-parse-byte-safe`), NOT dev — each exists to run the **full-tier CI money oracle** (which does not run on `integration/**` pushes) and to give you a clean reviewable diff. None merged.
- **PR #115 — codeaudit hardening (12 fixes)** — head `integration/codeaudit-hardening`. Merge FIRST.
- **PR #116 — plugin-infra hardening** (loader HIGH OOB + MED TOCTOU) — head `codeaudit-fix/plugin-infra-restructured`. Independent of #115/#117 (touches only plugin-loader) — merge any order.
- **PR #117 — #38 fold contract-transport→busbar-contract** — head `fold/contract-transport-into-contract`. Merge LAST of the three: it edits `busbar-contract` + the 7 transport crates' deps, so it will conflict with #115 (which has the contract secret-seal + transport bodies). Recommend: merge #115, then rebase #117 on the result, then merge. Carries 2 flagged items (feature_invariance/ci.yml scoping; 2 MED in transport/surface.rs moved-verbatim-not-fixed).
- **Base-line fix already landed** (mechanical, not a decision): `qa/audit-ledger.json` `ledger sync` registered owed audit scopes for `busbar-plane-{a2a,mcp}-host` (commit `8fa4e94eb`) — the `audit_ledger` gate was RED on the base for these two crates, unrelated to any fix branch. This is the gate's own printed remediation and only marks them *owed* (not audited). NOTE: per #39 these `-host` crates fold away anyway, which will remove these scopes again.
- **PR #119 — COMBINED RC** (`integration/1.6.0-rc` @ `652938328`) = #115+#117+#116+#118 merged in order + 2 test-path repoints. A single promotable candidate. **Validated locally: `cargo build --workspace` OK, `clippy --workspace --all-targets -D warnings` CLEAN, grpc 44/sse 26 tests pass, KernelSeal doctests pass.** Merged tree confirmed to carry BOTH the #40 seal and the fold. Use this to promote once the two gates (oracle A0, dev reconciliation) clear; review granularly via #115/#116/#117/#118.
  - The 2 repoints (grpc `battery.rs:1897`, sse `tests/mod.rs:591`: `busbar_contract_transport::wire::`→`busbar_contract::transport::wire::`) are merge-interaction artifacts — hardening added those TEST lines, the fold couldn't repoint them (they weren't on its base). Only appear when #115 and #117 combine; fixed in the RC merge (test-only, no logic).
- **Merge order depends on item A** below (these PRs target the feature line; the feature line → dev is the real reconciliation call).

## A0. BLOCKER — the money oracle cannot render a verdict on the 1.6.0 line (found during the PR #115 run)
Ran the full-tier CI money oracle via PR #115. **It did NOT flag any money divergence from the 12 hardening fixes.** It fails *earlier*, at its own **differ replay selftest**: every differ-capability case PASSes, but one selftest proof-case is **SKIPPED** and the strict selftest counts a skip as red →
`replay selftest: RED (1)` → `The differ can see a diff (replay selftest)` step exits 1 → the real 1.5.5 parity comparison never runs.
- Skipped case: *"record.sh vs SUPERVISOR_MARKERS: the Rust source naming them is not in this tree, so the one case that would notice record.sh no longer unsetting INVOCATION_ID cannot run."*
- BUT `SUPERVISOR_MARKERS` / `INVOCATION_ID` **do** exist in-tree at `crates/busbar-admin/src/restart.rs` (+ its tests). The restructure moved supervisor/restart logic into `busbar-admin`; the oracle tool's path/pattern detection (lives in the external `BUSBAR_ORACLE_TOOL_DIR`, not this repo) is stale and no longer finds it. So this is a **tooling-vs-restructure mismatch, not a product regression, and not caused by any fix branch** (PR #115 touches none of: restart.rs, record.sh, or the oracle tool).
- Impact: **no line (integration/fold/plugin-infra) can pass the parity gate until this is resolved** — it will fail identically on #116/#117. This gates "ready for qa."
- Also note: pushing the base branch itself does NOT run CI (`ci.yml` fires only on `integration/**`, dev, qa, main, PRs), so there's no base-only run to A/B against — attribution is by inspection (changed files are disjoint from the failing path).
- NOT fixed unattended (money-path tooling; needs your context on the oracle tool version / the expected marker path). **Decision needed:** update the oracle tool's SUPERVISOR_MARKERS source-path to `crates/busbar-admin/src/restart.rs`, or restore the marker where the tool expects it.
- Separately, the 1.6.0 base fails many architecture/process gates in CI (construction, design-bindings, docs-hygiene, feature-sets, gate-mutants) — expected for a mid-restructure line, but all must green before qa.

## A1. Merge-order — DRY-RUN VERIFIED CLEAN (the seal-clobber worry does NOT materialize)
I test-merged all 4 PRs in order in a scratch worktree (no push): **#115 → #117 → #116 → #118 merge with ZERO conflicts at every step** (final scratch tip `c97bb8470`). Crucially, the #117 fold **does not touch `kinds.rs`** (it only moves the transport files into `transport/`), so it cannot clobber #115's seal. Verified in the merged tree: `expose(&self, _seal: &dyn KernelSeal)` and `bytes(&self, _seal: &dyn KernelSeal)` are BOTH present (seal intact) AND the fold applied (transport/ module present, `busbar-contract-transport` deleted). So the earlier "naive merge could revert the seal" concern is retired — seal and fold live on disjoint files. Recommended merge order still #115 → #117 → #116 → #118 (order-independent for conflicts; this order keeps the diffs cleanest). Build/test validation of the combined stack: DONE — `cargo build --workspace` OK, `clippy --workspace --all-targets -D warnings` CLEAN, grpc 44/sse 26 pass. One merge-interaction fixup needed (2 test-path repoints, see PR #119) — test-only. Combined RC is PR #119. NOTE: the money oracle (A0) still cannot validate the combined money-path until the A0 tooling block is cleared — do not treat a green build as money-parity proof.

## A2. busbar-contract audit (post-fold) — new items for the ABI queue (section B)
Folded transport ABI audited clean; no behavior-preserving fix warranted. Items below are all ABI-signature or observable-behavior changes → your ruling, batched:
- **Secret-`PartialEq` cluster (constant-time):** `SecretValue` (`kinds.rs:416`), `KeyMaterial` (`kinds.rs:100`), `Credential` (`kinds.rs:125`), `SecretOnce.nonce` (`dest.rs:362`) all `#[derive(PartialEq,Eq)]` over secret bytes → timing-variable secret compares. Removing the derive is an ABI change. Recommend: hand-roll constant-time `PartialEq` (or drop it) across the cluster in one pass.
- **`AuthDecoration` Debug leak** (`dest.rs:401`) — derives `Debug` over `envelope_fields`/`body_signature` raw bytes. Lower risk here (secrets cross as `SecretSlot` placeholders substituted later in the egress-auth operation, not inside the decoration) but the derive stands. Redacting Debug is safe-ish but changes the type's Debug output → your call.
- **`address_of` precision (one ruling, 3 findings):** #5 `Target{path,method}` vs `Service{service,method}` share tuple-space → false `DuplicateAddress` at boot (`transport/surface.rs:367`); #6 `Document` binding with empty `mounts` boots then 404s every request (`:418`); #9 (LOW) method compared case-INsensitively at serve (`:455`) but case-sensitively at the boot dup-check (`:369/:427`) → a case-only-different duplicate row boots yet is silently unreachable. All change boot-accept/request-matching → flag. Recommend normalizing method case + unifying the tuple space in `address_of`, once ruled.
- **Correction to section B:** `HostCtx` does NOT exist in `busbar-contract` (it lives elsewhere post-fold) — the "HostCtx generation-counter UAF guard" ABI-review item is misattributed to this crate; re-scope it to wherever `HostCtx` now lives before actioning.
- **KernelSeal forgeable** — reconfirmed (`plugin.rs:189` doctest forges a `VerifiedDestination`); still gated on the channel-1 trust-model ruling (B1).

## A0c. CI runner fleet is SATURATED (linux CI validation of the RC is pending capacity)
The RC (#119) CI sat `queued` for 2h20m+ across three monitor windows with ZERO throughput — this is the known "fleet asleep / no awake box has a free proof slot" condition (landq/latchkey), not transient saturation. It needs WAKING (owner/infra action); it will not self-recover. I stopped watching after 3 futile windows. Contributing load: 5 full-tier PRs (#115–#119) + re-triggers, each ~12 jobs. Since the oracle (A0) blocks ALL of them from passing regardless, these runs cannot succeed until A0 is fixed — so the full-tier CI churn is currently low-value. Options for you: (a) fix A0 first, then let ONE line (the RC #119) run; (b) close the 4 component PRs' CI in favor of the RC to cut load (keep the PRs for review, they don't need to each run full-tier); (c) raise fleet capacity. The RC's build/clippy(--all-targets)/tests are already GREEN locally (macOS); linux parity is the only thing pending, and it's gated on runner availability, not code. I did NOT cancel any runs (left the choice to you).

## A0b. CI red attribution (so you don't re-derive it) — NO regression from any fix branch
The PR #115 CI reds break down as: (1) `fmt · clippy · build · test` — failed ONLY on the pre-existing `audit_ledger` 2-test red (fixed on base by `8fa4e94eb`; the run predated it). NOT a hang, NOT a compile/assert failure from the 12 fixes; the `hook-test-plugin` panics in that log are the fixture's intentional `catch_unwind` test. I merged base into the integration head (`a90475c59`) so the next CI run is representative. (2) `shadow oracle` — the A0 differ-selftest block above (not a divergence). (3) construction gate / design-bindings / docs-hygiene / feature-sets / gate-mutants — **pre-existing base gates**, red because the 1.6.0 line is mid-restructure; all must green before qa but none are caused by the fix/fold branches (changed files are disjoint). Net: the fixes and the fold do not regress CI; the line's red is base-state + the A0 tooling block.
  - feature-sets specifics: one failing test — `busbar-llm engine::forward_pool_integration_tests::test_governance_admin_api` (`:1788-1898`; 584 passed / 1 failed). DIAGNOSED: **deterministic (6/6)**, a STALE mis-wired duplicate — the admin `/api/v1/admin/keys*` routes mount only via the admin seam, which busbar-llm's test binary can't install (busbar-admin is not, and per #37 must not be, a dep of a plane) → valid-token POST 404. The admin surface itself is FINE. **Correction to the initial "safe to delete" read:** this test asserts UNIQUE money/security coverage NOT present in busbar-admin's tests (exact 201; 1.5.0 `bbk_` token returned once; `generation_hash`/legacy-secret absent; full create→list→usage→delete lifecycle incl. 204; minted-key goes live + pool-ACL applied). So a blind delete loses coverage. **RESOLVED — PR #118** (`fix/relocate-admin-lifecycle-test-to-busbar-admin` @ `8a0a007d8`): ported the lifecycle test into busbar-admin (proven green, 262/0, every assertion preserved), removed the busbar-llm duplicate + `auth-admin-tokens` stub + ci.yml row (feature grep-confirmed gone). busbar-llm 584/0. No product code changed. Merge to clear the feature-sets red. Confirmed unique coverage (busbar-admin previously had only auth-carrier + mint-success, not the full bbk_/generation_hash/CRUD/minted-key-ACL lifecycle).

## A. Git / dev landing (read first)
- **`origin/dev` has diverged from the 1.6.0 line.** The feature branch `busbar-1.6.0-config-neutral-half-parse-byte-safe` is **2301 commits ahead** of `origin/dev`, and `origin/dev` has **1 commit** the feature line lacks. So "push fixes to dev" is not a small delta — it's a full reconciliation. I did NOT force a 2301-commit merge onto shared `origin/dev` unattended. All hardening is consolidated on a green integration line (see status doc); **decide how the 1.6.0 line becomes `dev`** (likely: reset/ff `dev` to the 1.6.0 line after cherry-picking dev's 1 commit — but that's your call).

## B. ABI design review — verdict HYBRID (doc: `docs/design/1.6.0-ABI-design-review.md`, branch `abi-design-review`)
Keep the ABI shape (shared spine + per-kind faces + HOT/COLD by heat) — **no swap**, it's the right design and a blank sheet reproduced it. Fold in fixes. Rulings needed:
1. **Trust model (channel-1):** is a *signed* cdylib trusted on the plugin→busbar channel, or zero-trust? This decides whether **KernelSeal must be made unforgeable** (today it's forgeable — a doctest forges a `VerifiedDestination`, which undercuts #40). My #40 accessor-seal (shipped, `contract-secret-seal`) closes the accidental path; the *malicious* path needs an unforgeable seal, which hinges on this ruling.
2. **transport = TCB, not a dropped-in plugin?** Its async `&dyn Transport` can't cross a cdylib; it's in-tree/trusted. Honest lane table becomes HOT=plane, COLD=5 (store/secret/auth/hook/export), TCB=transport. Amends #3/#11/#30/#39.
3. **Timing** of the 3 big ABI reconciliations (plugin-sdk/loader dep busbar-api→busbar-contract; collapse the 3 coexisting plane representations; real `kind-abi-lane` perf gate) relative to the cut.
Cheap fixes the review flagged (safe to do once you rule): real hot-lane perf gate (#30's `kind-abi-lane` gate doesn't exist), `HostCtx` generation counter (UAF guard), KernelSeal unforgeability.

## C. Parity STOP — 2 divergences for one-at-a-time sign-off (doc: `docs/design/PARITY-DIVERGENCE-PACKETS.md`)
- **#3 NEUT-U-auth `operator_pub`** — MINOR/additive (D38 feature); sign off or prepass-lift.
- **#4 BOOT-135 wording** — MINOR; dependency-owned archive-EOF text; sign off (BOOT-141 twin).
- (#1/#2 PutConfigSettings restart-defer = drift/laziness → being fixed at root, not sign-off; #5 boot-lines = harness flake → normalizer, not a divergence.)

## D. Smaller queued items
- **unit-audit redaction MEDs** — `What::destination` / `Access::fields` written unredacted onto the erasure-exempt audit chain. Fixing changes *recorded content* = money-path divergence → needs sign-off. (The 2 HIGH chain-break swallows are already fixed, byte-safe: `codeaudit-fix/unit-audit`.)
- **plane-streaming lane + upstream-index mis-attribution** (HIGH, money path) — REFINED by fresh audit: `plane.rs:413-423`, the already-paired `SessionUpstream` branch hard-codes `UpstreamIdx(0)` AND `lane = upstreams().first().lane` regardless of the dialed upstream. So in a >1-upstream deployment (e.g. `[openai@laneA, gemini@laneB]`), EVERY duplex turn after the session pairs meters/ceilings against upstream 0's lane+index — a Gemini turn settles against laneA/idx0. The fresh-session branch just below (`:426-432`) is correct (`default_upstream(arriving).lane`); only the paired path diverges. Fix = carry the dialed upstream's real index+lane into the fact. Changes metering → your sign-off, not fixed.
- **plane-streaming CRITICAL Unit-0 first-frame — status/branch reconciliation.** The fix (empty/mis-shaped first egress frame: a `session.update`-open ships an EMPTY frame, an audio-open ships RAW PCM instead of a dialect frame → voice/instructions/tools/turn_detection never apply) is CARRIED IN PR #115 (`0c196cc`) but was NEVER on the base tip. A fresh audit re-derived it onto `codeaudit-fix/plane-streaming-basetip` (`67716e06a`) — **redundant with #115, no PR opened**. Action: when #115 merges the fix lands on base; retire BOTH `codeaudit-fix/plane-streaming` (stale, `0c196ccba`) and `codeaudit-fix/plane-streaming-basetip` (`67716e06a`).
- **transport-tcp listener-prune** (OPEN) — `self.listeners` can't be pruned; the `Transport` trait has no `unlisten` op. Needs an ABI addition to `busbar-contract`.

## Status: what shipped while you were out
Converged + pushed as `codeaudit-fix/<crate>` (fresh-audit 0, tests+clippy+fmt green each): secret-ref, transport-{http,ws,stdio,tcp,tls,sse,grpc}, store-memory (plane_token_live now live), plane-streaming (CRITICAL first-frame), unit-audit (2 HIGH chain-break), contract #40 accessor-seal. Turnstile armed (`1b9407d`, 559 tests, A9/A10 + opened_at). Ledger: `docs/1.6.0-codeaudit-findings-ledger.md`.
