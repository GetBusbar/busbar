# BUSBAR 1.6.0 — STATUS

**As-of:** 2026-09-17 00:52 PDT (heartbeat 3) · **Tip:** local `integration/oracle-phase0` @ `bab4462fa` · **Phase:** batch2 staged; additive switch-over prep COMPLETE — deliberately HOLDING the swarm for owner (only money-adjacent + CI-linux work remains).

**Heartbeat 3 (00:52 PDT):** verified state — 0 agents of mine running, 0 completions to integrate, branch inventory intact (d31 `b3698d1ee`, r4-0 `491ac46fe`, batch2 `5355d90f9`, loc-repin `d4e21aa26`, grep-gate `98c15c10e`), no worktree collisions. **Swarm deliberately NOT re-saturated to 20:** safe-additive pool is exhausted; the only remaining work is money-adjacent (ingress swap / one-book / D-cleanup — DECISIONS #10 deny-and-fix), owner-gated (mint #2, gemini #3), CI-linux-gated (mid-stream golden), or LOCKED-to-cutover-cleanup (#17 root-* collapse / #18 streaming rename — not standalone). Padding with any of these unattended would risk the money-path byte-identity the owner holds sacred (§UNATTENDED SAFETY RULE: quality + byte-identity outrank raw agent count). Holding clean for the owner's 8am one-motion; heartbeat cron continues.

**ETA:** Target **Sat 2026-09-20** · Worst-case floor **Wed 2026-10-08 17:00 PDT** · **Δ: confidence GUARDED (down from "rising")** — honest recalibration: the landed wins (Commit-C verified, 4 gates, 12 findings, docs) are the *easier* portion; the **switch-over cutover is the dominant remaining pole and is large** — today only the ADMIN plane is on the kernel loop; llm/mcp/a2a/streaming still ride legacy ingress. 9/20 is tight; **10/8 floor firm**.

**Status:** 9/20 TIGHT (not glib-on-track) — gated on the switch-over landing cleanly.

**Heartbeat 2 (00:13→00:40 PDT) — DONE, all byte-safe additive prep:** (a) batch2 +1 loc-ceiling re-pin `d4e21aa26` (→ ff-ready, both loc-ceilings green); (b) R4-0 engine-symbol relocations already materialized on tip (dormant, delete-nothing, shipped path byte-unchanged); (c) D31 producers built — new dormant `A2aNode` + `dormant_a2a_node()` on branch `switchover/d31-chain-builder` @ `b3698d1ee` (unmounted, tests 4/4). **D31 producer set now COMPLETE:** admin/streaming/llm already had nodes; a2a now built; **mcp keeps its own `Mount` composition model — it has no `A2aNode`-shaped gap, so building an `McpNode` would be a step-2 re-architecture, correctly NOT forced.** Safe additive switch-over prep is now EXHAUSTED — everything remaining is money-adjacent (ingress swap / one-book) or CI-linux-gated (mid-stream golden). **HOLDING** per unattended safety rule; heartbeat cron continues.

**Branch inventory for owner's 8am one-motion:** ff `integration/oracle-phase0` → batch2 `5355d90f9`; loc re-pin `d4e21aa26`; grep-gate fix `98c15c10e`; D31 producers `b3698d1ee` (dormant, safe to carry). Push origin (classifier-blocked for me → owner) → CI-linux parity oracle proves the construction pick's money bytes.

## How much is left (big rocks)
| Rock | State |
|------|-------|
| Money-path **Commit C** | VERIFIED byte-identical on tip; D-cleanup only |
| **Switch-over cutover** — THE DOMINANT POLE | Today ONLY admin is on the kernel loop; llm/mcp/a2a/streaming ride legacy ingress. **Additive prep DONE & byte-safe:** R4-0 already on tip; D31 producers complete (A2aNode built dormant; admin/streaming/llm already had nodes; mcp keeps its own `Mount` model — no gap). **Remaining = all money-adjacent / CI-gated, HELD:** per-plane ingress swap (step 2), llm one-book money collapse (step 5), legacy-loop delete (step 6), and the **mid-stream upstream-error golden which must be recorded on the linux oracle** (hard predecessor to the llm cutover — so the linux/fleet box is ON the cutover critical path, not post-cutover). Unblocks the 4 RED oracle groups (PARITY/EQUALITY-root/ISOMORPHISM/TELLER-STEPS) |
| **Batch integration** (17 picks) | **batch2 `5355d90f9` STAGED & VERIFIED** — build+release binary green; design-bindings, plane-purity, kind-isolation(all but registry/matrix) GREEN. HELD for owner integration: (a) construction pick money-bytes need CI-linux oracle (host skew here); (b) diverged from local STATUS commits (trivial, disjoint) → merge; (c) origin push owner-gated (#4). Construction still RED: ports-only-floor (endgame-gated) + hold-discipline (switch-over-gated) + 1 trivial loc re-pin |
| Gates: plane-purity / construction(7) / kind-isolation:deps | GREEN (on batch) |
| Gates: kind-isolation `registry`/`matrix` + AUDIT-LEDGER | RED → **owner decision #2** (one admin/oauth2 mint unblocks both) |
| grep-gate stale allowlist row | **FIXED** (green, batch-integrate) |
| 14 HIGH audit findings | **12 fixed**; 1 STOP → owner #3; (1 xtask ledger-sync HIGH also in batch) |
| Doc reconciliation (114 docs → canon) | DONE; integrates via batch2 (with its coupled gate fixes) |
| Fleet oracle closure (~379 cells + rigs + store-persist) | SEQUENCED after cutover (running pre-cutover would re-run) |
| Cutover cleanup: `root-*` collapse + streaming rename (#17/#18) | QUEUED (part of switch-over) |
| Release staging (version+CHANGELOG ✓, I5 promote) | promote NOT STARTED |

## Done since last
grep-gate stale allowlist row fixed (`98c15c10e`); audit-ledger regen correctly parked → consolidated into decision #2 (one mint clears kind-isolation + audit-ledger); batch1 integrated + diagnosed (15/16 picks clean, 2 regressions isolated to the doc-reconcile pick's missing coupled updates); batch2 finalize agent launched; stray working-tree files restored (clean); STATUS decisions #2/#4 recorded.

## Gotchas / risks
- **Lesson (batch1):** a normative-taxonomy DOC change must land *coupled* with its gate reader + ledger regen, or it regresses gates — batch2 does exactly that.
- Origin push of canon is **classifier-blocked** → agents keep landing on origin's stale `eeddd6815` (no DECISIONS.md) and self-reset; I keep them on the local canon tip. Owner push needed (decision #4).
- The construction pick edits money-adjacent llm/egress → batch2 must prove shadow-oracle byte-identity before any fast-forward (not yet cleared).

## Blocked on owner
Not hard-blocked on parallel work. Decision #2 gates two gates; #1/#3 gate streaming money semantics; #4 (origin push) unblocks agent base-consistency.

## Questions queued for owner (numbered — why + recommendation)
1. **CostHold for streaming.** Why: `duplex-plane-and-realtime.md` mandates mid-session `cost_reserve`/`cost_settle` (hard-stop a live stream on budget); taxonomy demotes it to a non-1.6.0 extension point — conflict, streaming is in 1.6.0. **Rec:** ship streaming with end-of-turn metering (byte-identical Meter step); mid-session hard-stop stays an extension point.
2. **admin/oauth2 mint (classification + ledger cells).** Why: the two newly-extracted crates are absent from the kind + audit ledgers, so ONE mint unblocks BOTH kind-isolation `registry`/`matrix` AND AUDIT-LEDGER. **Rec:** classify legacy/core-family (drained post-1.6.0 with the busbar-core retirement arc — not a cut blocker) + advance the merge-base once with their cells (preserves findings, no open→clean). Need your nod on the merge-base advance.
3. **gemini `UsageIdentityNote` (write-only).** Why: codec computes a provider token shortfall nothing reads; changing the billed figure = a registered money change. **Rec:** wire a consumer in busbar-llm surfacing it as a `RefusalReason::MeterDisputed` fact; do NOT change the billed number.
4. **Integrate + push (one motion).** Why: batch2 (`5355d90f9`) is staged/verified and agents keep landing on origin's stale `eeddd6815` (no DECISIONS.md); origin push is classifier-blocked for me. **Rec (your 8am one-motion):** merge/ff local `integration/oracle-phase0` → `5355d90f9` (disjoint from my STATUS commits), do the trivial +1 loc-ceiling re-pin, `git push origin integration/oracle-phase0`, and let the **CI-linux job run the parity oracle** to prove the construction pick's money bytes (the only piece unprovable on this darwin/arm host). If CI shows any billed-byte divergence it returns to you as approve/deny (DECISIONS #10). Or grant me the Bash push rule to do the push half.

Standing (not re-asked): deny-and-fix on byte moves = DECISIONS #10; integration-push authority granted (but classifier-gated); never qa/main; fleet = spot-only/zero-idle, brought up once a green candidate exists post-cutover.

---
Format is fixed; diff this file between checks to see progress. Author: Matthew <dev3@getbusbar.com>.
