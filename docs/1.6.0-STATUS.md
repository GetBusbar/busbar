# BUSBAR 1.6.0 — STATUS

**As-of:** 2026-09-17 07:10 PDT (heartbeat 10 — owner due ~8am) · **Tip:** local `integration/oracle-phase0` @ `02efeaac6` · **Phase:** COMPLETE HANDOFF — both DONE gates sized, money packet done, 12 decisions queued; verifying tip builds green as owner's base.

**Heartbeat 10 (07:10 PDT):** 0 agents running, nothing to integrate. Owner imminent → held off new analysis. Final check DONE: **`cargo build --workspace` on the tip = exit 0, GREEN** — the owner's 8am integration base (incl. plane-purity `12cd42c3b`) compiles clean. Handoff is complete and the base is verified-good; holding.

**Heartbeat 9 (05:40→06:10 PDT):** 2 read-only money-brief lanes (per-finding cell/defect/root-cause/byte-impact/rec for the 10 money-path audit blockers — the DECISIONS #10 guardrail's required prep). **Billing-correctness cluster (5) back:** 1 already-fixed on tip (`busbar-llm` sink/hold `bea098858` — flag: don't re-open, so real-blocker count ~36 not 37), 2 byte-safe refactors (`unit-admission` dup-cents single-sourcing; `unit-cost` String→MeterClassId zero-alloc — both need CI-linux shadow-oracle confirm, NOT self-asserted), 3 need owner-signoff (`llm-codec` usage-shortfall [=#3]; `plane-mcp` empty-answer-billed-complete + 22-refusal-collapse; `busbar-llm` PB-27 non-SSE refund). I did NOT self-implement even the byte-safe ones — money-path crates need oracle-proof unavailable on darwin; queued as owner-approve→I-implement→CI-linux-prove (batch2 discipline). **Lifecycle/ledger cluster (5) back — with GOOD NEWS: the ledger is STALE.** Full 10-money-path triage (verified): **~3 already FIXED on tip** (llm sink/hold `bea098858`; a2a redeem/ts `cee321af3`; busbar-a2a unmetered/URL-leak/pushback — all ancestors of tip, ledger just not re-synced), **4 byte-safe** (unit-admission cents, unit-cost alloc, unit-ledger retire-wiring, store-example replay), **5 owner-signoff** (llm-codec #3; plane-mcp empty-answer+refusal-collapse; busbar-llm PB-27 refund; plane-a2a unit-never-ends; root node_book data_dir durability), **1 test-only** (root late-accrual coverage). **Implication: the "100 open / 37 real" counts are inflated by already-fixed work `ledger --check` still shows open — a `ledger sync` (owner/audit-sacred) would drop the true count materially.**

**Heartbeat 7 (04:35 PDT):** 0 agents running, nothing to integrate. Confirmed busbar-voice drain NOT safe unattended (billing modules + composition root + 9 frozen wires + needs linux oracle + cutover-sequenced). Launched 1 read-only lane sizing AUDIT-LEDGER's 100 open HIGH/MED findings (real-blocker vs stale vs post-retirement-arc) — same "size it for the owner" move as the drain, for the 2nd DONE gate. Last read-only sizing lane; everything else is #10/owner/money-gated.

**Heartbeat 6 (03:35→03:45 PDT):** 2 read-only drain lanes done + I verified the headlines myself. **busbar-core = 64,639 non-test LOC (stale plan said 43,722), ~55–85 SERIALIZED agent-days, 3 destination crates absent, cost.rs undrained** → sizes owner #10 decisively (option A = ~2 months out). **busbar-voice = ~4–6 agent-days mechanical collapse, 9 frozen wires, no ABI port.** Firm recommendation now on #10: **Option B** (busbar-core deletion = post-1.6.0 arc; ship on byte-identical engine), keep busbar-voice in. No other safe lane exists — the drain + 9 kind-iso-ship rows + switch-over are money-adjacent or #10-gated.

**Recon verdicts (verified):** (1) **dual-ABI #11 — cut-ABI MET:** repr(C) `PlaneDecl`/`PlaneHostVtable` fully declared + layout-golden-pinned; token-auth cdylib is the working proof. Plane→repr(C) wiring is "ADDITIVE and UNUSED" = conformance follow-through, not a cut blocker. **De-risks #11.** (2) **streaming-rename #18 — clean rename + 3 frozen wires:** crate/feature renames are mechanical, but `SessionConfig.voice`, `audit_kind:"voice_session"`, `LaneId("voice")` are user-observable/persisted and must stay frozen (#9); also a real 3-crate→1 collapse per #6. Held cutover-cleanup intel; may warrant a wire-freeze caveat on #18 (owner ratify). (3) **kind-iso-ship/ledger — see ⚠ TOP RISK + corrected big-rocks.**

**Heartbeat 5 (02:35 PDT):** collected all HB4 investigators. **a2a TEST reclassified → owner #9 (not a self-fix):** the fix touches the crate dep graph the admin-extraction (`cb2ba5df3`) deliberately kept one-directional — registering the admin seam in a2a/mcp test binaries needs a `busbar-core` test_support helper OR a dev-dep on `busbar-admin` (reverse-edge risk). Owner-scoped. Launched 3 read-only recon lanes (byte-safe). Swarm NOT padded to 20 — every genuinely-safe high-signal lane is running; remaining work is money/owner/audit-sacred and padding it would breach the §UNATTENDED SAFETY RULE quality bar.

**ETA:** Target **Sat 2026-09-20** · Worst-case floor **Wed 2026-10-08 17:00 PDT** (NOT moved unilaterally — owner-gated per DECISIONS #16; but now flagged AT RISK — see below) · **Δ: 9/20 still NOT achievable; 10/8 back to PLAUSIBLE under option B (partial re-brighten)** — why: deeper triage shows the AUDIT-LEDGER gate is SMALLER than HB7 feared — its "37/100" count is inflated by already-fixed findings the stale ledger still shows open (verified ~3 of 10 money-path already fixed on tip), and the real money work is ~4 byte-safe fixes (CI-linux-proof) + 5 owner money-decisions + 1 test-add, not 37 fresh bugs. So under option B (drain = post-1.6.0 arc), the remaining real work is: the switch-over cutover (money-adjacent, CI-linux-gated) + a `ledger sync` + ~4 byte-safe money fixes + 5 owner sign-offs + test/tooling cleanup — **tractable toward 10/8**, though CI-linux-gated pace + per-money-finding owner serialization keep it out of the 3 days to 9/20. Option A (drain in-cut) is still ~2 months. Date stays owner-gated (#16); recommend B + a realistic reset off this smaller backlog.

## ⚠ TOP RISK — surfaced the hour it was known (per 1.6.0-PLAN.md); now SIZED
**The DONE oracle demands busbar-core/busbar-voice deletion the PLAN said was out-of-cut.** `verify-1.6.0-done.sh` → KIND-ISOLATION runs `kind-isolation-ship` (0 exemptions, "has to reach zero"); `legacy-drain` requires `busbar-core` (→plane-admin, →unit-*) and `busbar-voice` (→plane-voice) GONE at ship. PLAN says both (a) DONE = verify exit 0 AND (b) busbar-core deletion NOT a cut blocker. **Drain now sized (verified):**
- **busbar-core: ~64,639 non-test LOC** (the plan's 43,722 is stale by ~48%; crate has GROWN). ~3/4 still has nowhere to land: the 3 owner-ruled destination crates (`busbar-core-config` ~11.5k, `busbar-core-hooks` 3k, `busbar-control-oauth2`) **do not exist** (0 progress in 9 days). `cost.rs` money resolver (`CostModel::resolve_parts`) **undrained, no replacement anywhere**. Money one-book dual-booking (D33 §7.12) is the highest risk. **Estimate: ~55–85 agent-days, SERIALIZED (not parallelizable past the money pole).**
- **busbar-voice: ~7,583 LOC, MECHANICAL 3-crate→1 collapse, ~4–6 agent-days, 9 frozen wires, NO repr(C) re-architecture** (already on the same substrate registry ABI as mcp/a2a). Tractable; it's really part of the #18 streaming rename (already 1.6.0 scope).
**Consequence:** under option A (core deletion in-cut) 9/20 is impossible and 10/8 blows too — honest date ~2 MONTHS out. Under B (core deletion = post-1.6.0 arc) 1.6.0 ships on the byte-identical engine. I did NOT move the date or weaken the gate — owner #10 rules it (recommendation: **B**, see below).

## Heartbeat 4 — what happened
Course-corrected from pure-hold to the one plan-endorsed safe lane (1.6.0-PLAN.md critical-path #3: run §11 audits continuously NOW). Launched 6 read-only audit lanes (Explore = cannot edit/commit). **Landed a real gate win:** plane-purity was RED on the bare tip (`prepass.rs` named the `mcp` plane type+key — DECISIONS #1/#7 violation); the fix existed only on an orphan branch. Cherry-picked it (`12cd42c3b`, byte-invariant rename — `McpEndpointSection`→neutral `EndpointSection` alias, `"mcp"`→`ENDPOINT_SECTION_KEY`), **verified plane-purity 9/9 GREEN + core builds + 464 config tests pass + no new construction RED**. Money-path bytes untouched (pure alias, no value/logic change).

## Rolling §11 architecture-audit results (my verification, not the finders' word)
| Scope (DECISIONS row) | Score | Verdict |
|---|---|---|
| Core thinness (#1) | 8/10 | Architecture real — no plane types/dialects/backward-reach in production core. Only gap was plane-purity RED (now FIXED). |
| Broker law / coupling-down (#8) | 4/10 | Core names 6 concrete plugin crates by path (2 in `default`); a2a/mcp name `store-memory`+loader directly. **All tracked debt in `qa/kind-isolation.toml` = the busbar-core retirement arc (D16/D33) — explicitly NOT a cut blocker per 1.6.0-PLAN.md.** The 5 `busbar-plane-*` adapters are exemplary/clean. |
| Transport seam (#7) | 6/10 | llm + HTTP legs of mcp/a2a correctly delegate to shared `net_guard`. 2 real gaps: mcp spawns own child-process breaker (`stdio.rs`); a2a hand-rolls DNS+runtime. **source-denylist NOT armable yet.** |
| Kind-isolation (#3/4/5) | RED×3 | `registry` (=admin/oauth2 mint, owner #2), **`matrix`** (needs ratchet/dead-cell cleanup — NOT the mint), **`deps`** (=the broker-law core-names-plugins debt). Stale **D36/D37 language still LIVE** in ARCHITECTURE.md, PLUGIN-TREE.md + the gate's own `KINDS` table (touched 09-08/09/10, pre-cancellation) — DECISIONS #14 resurrection hazard. |
| Teller-steps / single-audit (#1+§5) | 3/10 | **single-audit-step NOT met:** llm Audit never touches the shared AuditChain, admin uses a third chain type, a2a/voice each duplicate the seal. Gate passes 5/5 but doesn't check emission uniqueness. **Correction: the substrate teller loop is NOT orphaned — it is LIVE, carrying a2a/mcp/voice** (kernel loop carries llm+admin). The "orphaned loop / delete it" design-doc line is stale. |
| Done-oracle 21-group map | DONE | **9 green / 9 host-local-red / 3 box-gated** (plane-purity flipped green post-fix). See below. |

## Done-oracle: TRUE DONE distance (21 groups, measured on tip)
**GREEN (9):** PLANE-PURITY (just fixed), CONFIG-STABILITY, CONFORMANCE, CONFIG-NOUN, EQUALITY, DESIGN, CHANGELOG, INVENTORY-COVERAGE, ISOMORPHISM.
**Host-local RED (9)** — investigator verdicts (I verified each):
- **NO-DEFERRAL** → root-caused: **mechanical +1 off-by-one** in `scripts/no-deferral.waivers` (an earlier edit inserted 1 comment line above `mod stub`; waivers never regenerated). All 44 are `/hot/` rows = the gate's *permanent* built-in exemption (`strict-done` already PASSES). Verified +1 resync (44 rows, `/tmp/waivers.new`) turns it fully GREEN. **Apply BLOCKED by auto-mode classifier (Audit-Tampering guard on the waiver file) → owner #6.** Separately, TRACKER H5's text uses banned phrase "out of 1.6.0 scope" (DECISIONS #15) → doc reconcile to #11 "conformance follow-through" language (owner #5).
- **TEST** (a2a adminverbs 404×4) → **test-setup-gap, NOT a product regression:** the busbar-admin extraction's `AdminMountSeam` is never registered by a2a/mcp `test-support`; production calls `busbar_admin::install()` in `main.rs` and is unaffected. Fix = register the seam in the test harness (test-only, byte-safe).
- **PUBLIC-HYGIENE** (9 hits/11 rules) → **7 trivial-safe** (docs/ci ×4, xtask comments ×2, keep-proof.yml wrapped-citation false-positive ×1), **2 careful** (shadow-oracle `accepted-differences.json` load-bearing JSON; a money-adjacent billing-advisory). NOTE: this STATUS file itself trips `commit-hash-citation` (it cites SHAs) — either exclude `docs/1.6.0-STATUS.md` from the lint or accept it's an internal ops doc (owner call).
- **Harness-drift (verified by me):** 3 benign stale-callsite/count re-pins in `verify-1.6.0-done.sh` → **owner #8** (I proved each replacement runs green here; I won't self-edit the DONE-oracle definition unattended): KERNEL `attempt_identity` count `1`→`2` (a legit 2nd test); TELLER-STEPS `rigs-ledger.sh`→`./bin/oracle rigs-ledger` (script externalized); STORE-QA `service-images-check.sh`→`cargo xtask gate service-images` (ported, gate 8/8 green). **BYTE-IDENTITY is NOT benign:** its `-p` list is stale (matches 1 not 15 — vacuous) AND underneath a **real customer-visible golden divergence** — comment-wipe `e24a9b359` reworded an admin openapi.json description without regenerating the golden → **owner #7 (deny-and-fix default).**
- *Owner-gated:* KIND-ISOLATION (registry/matrix/shape/test-deps/testkit), AUDIT-LEDGER (stale scopes + open HIGH/MED).
**Box-gated / unrun (3):** BUILD (full-gate not run; `cargo build --workspace` rc=0), PLANE-DELETE (`--all` not run), PARITY (oracle record/replay = CI/linux only).
D38 `amend_rate_history` LANDED — `POST /api/v1/admin/ledger/amend-rate-history`, `full` + irreducible, posture-gated like every money-governance verb; its effect appends a signed, back-dated `Author::Amend` entry to the dated rate-card history (`RootHistory::amend`), out-ranking the corrected entry and rewriting nothing. Wire decode, request shape and apply-half written; operator signature is presence-checked (a full operator-key-registry verification seam remains a follow-up). Unit tests green (happy path + scope/validation/no-history refusals + existing rate/ledger reads byte-unchanged); `admin.ops`/`ledger` oracle cells still to record (CI/linux-only). One reviewed ceiling ratchet: `qa/construction.toml` `verbs_ceiling` 1428→1431 (a new closed-table verb costs three surface lines), surfaced by `ceiling-rose` for sign-off.

## How much is left (big rocks)
| Rock | State |
|------|-------|
| Money-path **Commit C** | VERIFIED byte-identical on tip; money-path D-cleanup only (held, DECISIONS #10 deny-and-fix) |
| **Switch-over cutover** — DOMINANT POLE | kernel loop carries llm+admin; **substrate loop (LIVE, not orphaned) carries a2a/mcp/voice**. Additive prep DONE (R4-0; D31 `A2aNode` `b3698d1ee`). Remaining money-adjacent/CI-gated, HELD: ingress consolidation, **single-audit-step (§5) — a2a/voice/admin/llm onto ONE owned Audit emission** (teller audit at 3/10), one-book (step 5), mid-stream golden (linux oracle). Unblocks 4 RED oracle groups |
| **plane-purity gate** | **GREEN on tip** (fixed `12cd42c3b`) ✅ |
| **Batch integration** (17 picks) | batch2 `5355d90f9` staged; loc-repin `d4e21aa26`; grep-gate `98c15c10e`; D31 `b3698d1ee`. HELD for owner one-motion (push classifier-blocked for me) |
| **KIND-ISOLATION-SHIP** (DONE gate, 0 exemptions) | **RED on 9 rows** — mint (owner #2) clears only `registry` fully + ~27 of `matrix`. Distinct fixes: `deps` (86 ship-edges = core-retirement debt), `test-deps` (30), `faces` (4), `shape` (23), `testkit` (31), `control-path` (4 — `busbar-plane-admin` codec names `egress`/runs `route`+`meter`), **`legacy-drain` = DELETE busbar-core + busbar-voice (owner #10 ⚠).** FAR more than "one mint" |
| **AUDIT-LEDGER** (DONE gate) — 2nd gap, **#10-independent, but SMALLER than it looked** | RED, 100 open (verified) — but **the count is INFLATED: `ledger --check` shows already-fixed findings as open** (verified ~3 of the 10 money-path are fixed on tip). A `ledger sync` (owner/audit-sacred) drops the true count. Of the 10 money-path: ~3 already-fixed, 4 byte-safe (CI-linux-proof), 5 owner-signoff, 1 test-only. Real work = the byte-safe fixes + the 5 owner money-decisions, NOT 37 fresh bugs |
| **repr(C) plugin ABI (#11)** | **cut-ABI MET** ✅ (declared + golden-pinned + token-auth cdylib proof; plane wiring = follow-through) |
| Stale D36/D37 in ARCHITECTURE.md/PLUGIN-TREE.md/`KINDS` table | RED (DECISIONS #14 hazard) → **queued: coupled six-kind reconcile** (doc + gate `KINDS` table + truths reader together, per batch1 lesson) — SAFE, next lane |
| Transport seam (#7): mcp stdio breaker + a2a DNS/runtime | 2 gaps → source-denylist not armable until closed (plane-internal work) |
| 14 HIGH findings | 12 fixed; 1 → owner #3; 1 in batch |
| Fleet oracle closure (~379 cells + rigs) | SEQUENCED after cutover |
| Cutover cleanup: `root-*` collapse (#17) + streaming rename (#18) | QUEUED — LOCKED to cutover cleanup, not standalone |
| Release staging (version+CHANGELOG ✓, promote) | promote NOT STARTED |

## Done since last
Landed plane-purity RED→GREEN (`12cd42c3b`, byte-invariant, verified). Ran 6 read-only §11 audits + verified each finding myself. Corrected two STATUS errors: plane-purity was RED on tip (not green-on-batch); the substrate teller loop is LIVE (not orphaned). Consolidated the true kind-isolation breakdown (3 sub-checks, distinct causes).

## Gotchas / risks
- **My prior "plane-purity green on batch" was wrong** — it was RED on the bare tip; fixed now. Lesson: verify gates on the actual tip, not on an assumed-integrated branch.
- Single-audit-step (§5) is a genuine design gap (3/10), folds into switch-over — not a quick fix.
- Stale D36/D37 taxonomy language persists in 3 places incl. the gate's own KINDS table → must reconcile COUPLED (doc+table+reader) or it regresses the gate (batch1 lesson).
- Origin push classifier-blocked → agents land on stale origin base; owner push needed (#4).
- Broker-law 4/10 and transport 6/10 are real but map to the accepted core-retirement arc / plane-internal work — NOT 9/20 cut blockers.

## Blocked on owner
Not hard-blocked on the safe read-only + coupled-doc lanes. Money-adjacent / mint-gated / CI-linux-gated work still needs the 8am one-motion + the 4 decisions.

## Questions queued for owner (why + recommendation)
1. **CostHold for streaming.** Why: `duplex-plane-and-realtime.md` mandates mid-session `cost_reserve`/`cost_settle`; taxonomy demotes it to an extension point — conflict. **Rec:** ship streaming with end-of-turn metering (byte-identical Meter step); mid-session hard-stop = extension point.
2. **admin/oauth2 mint** (much smaller lever than I'd said). Why: the mint clears kind-isolation `registry` fully + ~27 of `matrix`, and 4/5 audit-ledger missing scopes — but NOT the other 7 kind-iso-ship rows nor the 100 open audit findings (see #10 + big-rocks). Still worth doing. **Rec:** classify legacy/core-family + advance merge-base once with their cells. Need your nod on the merge-base advance. (Note: this is necessary-not-sufficient; #10 dominates.)
3. **gemini `UsageIdentityNote` (write-only; brief ready).** Why: `gemini/mod.rs:1774` detects when Google's `totalTokenCount` exceeds busbar's decoded buckets, threads the shortfall through the fold, and NOTHING reads it — unit billed on the smaller decoded total; struct doc says attribution-only. **Rec: alert-only** — wire a consumer that emits an audit/alert, don't change the billed number (byte-safe, I do it post-approval + CI-linux-prove). Byte-changing alternative = bill on reported total (money moves, breaking CHANGELOG) only if you want the gap closed in-bill.
4. **Integrate + push (one motion, your 8am).** Why: batch2 staged/verified; tip has advanced (plane-purity `12cd42c3b` + STATUS commits) so this is now a MERGE, not a ff — **VERIFIED conflict-free (`git merge-tree` HEAD×`5355d90f9` exit 0, zero conflicts).** Push classifier-blocked for me. **Rec:** merge `integration/oracle-phase0` (now `335bd848f`) ← batch2 `5355d90f9`, carry loc-repin `d4e21aa26` + grep-gate `98c15c10e` + D31 `b3698d1ee`, `git push origin integration/oracle-phase0`, let **CI-linux run the parity oracle** (proves the construction pick + config rename bytes). Any billed-byte divergence returns approve/deny (DECISIONS #10). Or grant the Bash push rule and I'll do the push half.

5. **TRACKER H5 wording (banned phrase).** Why: H5 (`1.6.0-TRACKER.md:167`) labels the 44 hot-lane ABI stub slots "Out of 1.6.0 scope" — a DECISIONS #15 banned phrase — yet the `no-deferral:strict-done` gate *permanently exempts* `/hot/` and DECISIONS #11 calls the hot loader a "conformance follow-through, NOT a cut blocker." Substance is already locked-consistent; only the wording violates #15. **Rec:** reconcile H5's text to cite DECISIONS #11 (permanent conformance follow-through) instead of the banned phrase — doc-hygiene, no scope change. Confirm you agree the hot stubs stay a gate-sanctioned exemption (not 1.6.0 work).
6. **Apply the verified NO-DEFERRAL waiver resync (classifier-blocked).** Why: the fix is a mechanical +1 line-number resync of 44 `/hot/` rows in `scripts/no-deferral.waivers` (verified: 44 rows, non-hot untouched; preserved at `/tmp/waivers.new`), which turns the NO-DEFERRAL done-oracle group GREEN. I was blocked applying it (auto-mode Audit-Tampering guard on the waiver file) and did NOT force it. **Rec:** you apply it (`awk` one-liner in STATUS history / the /tmp file), or grant a Bash rule for `scripts/no-deferral.waivers` so I can. Pure gate-bookkeeping, zero shipped-code/oracle/money impact.

7. **Admin openapi.json golden divergence (customer-visible).** Why: `openapi_json_matches_committed_file` genuinely fails — comment-wipe `e24a9b359` de-vendored a description ("Bedrock-SDK / MinIO/S3-compatible" → generic "a client") that feeds the published admin openapi.json, but the committed golden was never regenerated. UNSIGNED customer-visible byte change (prose-only, MINOR). **Rec (deny-and-fix default, #10):** restore the original doc-comment wording so the golden stays byte-identical to 1.5.5 — UNLESS you intended to de-vendor the published schema text in 1.6.0, in which case sign off and I regen (`UPDATE_OPENAPI=1`). Your call: keep byte-identical, or ship the reword.
8. **Apply 3 verified DONE-oracle re-pins (harness drift).** Why: `verify-1.6.0-done.sh` has stale callsites/counts falsely redding 3 groups; I verified each replacement runs green here but did NOT self-edit the DONE-oracle definition unattended (audit-sacred — its strictness just caught #7). **Rec:** apply — L676 `filtered_cargo_test 1`→`2`; L710-717 `bash testing/shadow-oracle/rigs-ledger.sh`→`./bin/oracle rigs-ledger` (swap the `-f` guard to `bin/oracle`); L753-758 `scripts/service-images-check.sh`→`cargo xtask gate service-images`; L427 add `-p busbar-admin` (de-vacuifies BYTE-IDENTITY so #7 is visible). Greens KERNEL, TELLER-STEPS, STORE-QA. Or grant a Bash rule for `scripts/verify-1.6.0-done.sh`. **ALSO run `ledger sync`/`ledger fixed`** to retire the already-fixed findings the stale ledger still shows open (verified: llm sink/hold `bea098858`, a2a redeem/ts `cee321af3`, busbar-a2a metered/URL/pushback, + unit-verbs/unit-wal from HB4) → drops the "100 open / 37 real" to its true lower count (audit-sacred, owner-run).

9. **a2a/mcp adminverbs test-seam (dep-graph decision).** Why: `cb2ba5df3` extracted `busbar-admin` and kept the dep graph one-directional; a2a/mcp `test-support` binaries can't link `busbar-admin` (reverse edge), so `/api/v1/admin/*` is unmounted in-test → the 4 a2a (+3 mcp) adminverbs tests 404. Production is fine (`main.rs` calls `busbar_admin::install()`). Not byte-trivial — it touches the graph the extraction deliberately shaped. **Rec:** add a `busbar-core` `test_support`-only helper that registers a minimal admin-mount seam for these plane test binaries (no new production/dep edge) — I can implement once you approve the approach; option B (dev-dep `busbar-admin` in a2a/mcp) risks the cycle, not recommended.

10. **⚠ TOP DECISION — is busbar-core deletion IN 1.6.0? (busbar-voice can stay in cheaply)** Why: the DONE oracle's `kind-isolation-ship:legacy-drain` requires busbar-core + busbar-voice GONE at ship; PLAN says busbar-core deletion is NOT a cut blocker. Now SIZED (verified): **busbar-core = ~64.6k LOC / ~55–85 SERIALIZED agent-days / 3 destination crates don't exist / cost.rs undrained** → under A, 9/20 impossible and 10/8 blows (honest date ~2 months). **busbar-voice = ~4–6 agent-days mechanical collapse** (part of #18, already 1.6.0 scope). **Rec: OPTION B for busbar-core** — ship 1.6.0 on the byte-identical engine; busbar-core deletion is the post-1.6.0 retirement arc. Justification: (i) DECISIONS #12 = 1.6.0 is 1.5.5 + MCP + A2A + STREAMING — all three planes EXIST as crates; core *deletion* was never a user-facing deliverable; (ii) the repr(C) cut-blocker (#11) is MET; (iii) prior architect rec + PLAN both already say "ship on byte-identical engine, retirement separate." This is NOT banned "defer" — core deletion isn't a 1.6.0 feature, it's cleanup. **Mechanism (owner-signed, not silent):** you approve extending the `legacy-drain` exemption for busbar-core through the ship sha + I draft the DECISIONS row + reconcile the PLAN's DONE definition (kind-isolation-ship's legacy-drain scoped to the retirement arc; the other 8 rows still bind). Keep busbar-voice IN (4–6 days). If instead you pick A, I re-plan the date ~2 months out and throw max agents at the serialized drain. I did NOT move the date or touch the gate unilaterally.

Standing (not re-asked): deny-and-fix on byte moves (DECISIONS #10); never qa/main; fleet spot-only/zero-idle post-cutover; heartbeat cron continues. Audit/waiver-ledger + DONE-oracle-definition edits wait for owner (classifier-guarded / audit-sacred).

---
Format is fixed; diff this file between checks to see progress. Author: Matthew <dev3@getbusbar.com>.
