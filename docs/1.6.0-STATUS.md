# BUSBAR 1.6.0 — STATUS

**As-of:** 2026-09-16 22:31 PDT (heartbeat 1) · **Tip:** `integration/oracle-phase0` @ `08f5d4f59` · **Phase:** batch integration in flight + bounded cleanups.

**ETA:** Target **Sat 2026-09-20** · Worst-case floor **Wed 2026-10-08 17:00 PDT** · **Δ: HELD** — why: integration pass launched, no new gate result back yet since the swarm-complete refresh.

**Status:** ON TRACK for 9/20.

**Heartbeat 1 (22:31 PDT) launched:** (a) integration agent — cherry-pick the 9 green worktree branches onto `integration/oracle-phase0-batch1`, re-run gates + store-free oracle, report SHA to fast-forward; (b) audit-ledger regen (drifted); (c) grep-gate stale-allowlist-row removal. **Pool = 3** — right-sized to the safe/available parallel work: the deep money-adjacent cutover (ingress-swap, one-book) is held for careful serial execution + owner decisions, and blind-scaling to 20 would be make-work or unsafe (quality > count, per PLAN unattended-safety rule). Fleet oracle box **sequenced after the cutover lands** — running it on a pre-cutover tip would just need re-running (correctness, not idle-avoidance).

## How much is left (big rocks)
| Rock | State |
|------|-------|
| Money-path **Commit C** | **VERIFIED byte-identical** on tip (0 unaccepted oracle divergence); only D-cleanup remains |
| **Switch-over cutover** (one-book collapse + mount mcp/a2a/streaming on kernel loop + audit-chain→owned step) | QUEUED — design ratified; deep/serial/money-adjacent; unblocks the 4 RED oracle groups (PARITY, EQUALITY-root, ISOMORPHISM, TELLER-STEPS) + `hold-discipline` |
| Gate: design-bindings | **GREEN** |
| Gate: plane-purity + neutral-no-dialect | **GREEN** |
| Gate: construction (7 target rows) | **GREEN**; `hold-discipline` + `ports-only-tests:busbar-llm` red (gated on switch-over / composition-root endgame) |
| Gate: kind-isolation | `deps`/`test-deps` + 7 rows **GREEN**; `registry`/`matrix` RED → owner decision #2 |
| busbar-llm ABI purity (H1/C7/C9) | test-refs 27→21; floor to 14 gated on composition-root endgame |
| 14 open HIGH audit findings | **12 fixed** (xtask 5, planes/codec 7 — incl. real bugs: a2a terminal-detection, a2a/mcp refusal codes, breaker no-trip, voice tool-call); 1 STOP → owner decision #3 |
| Fleet oracle closure (~379 cells + mcp/a2a rigs + 3 store-persist) | PENDING — green candidate binary now exists; heartbeat brings up a spot box |
| Cutover cleanup: `root-*` collapse + streaming rename + owner's default line (#17/#18) | QUEUED (part of switch-over) |
| audit-ledger regen (drifted at `9f26b93b0`) | TODO — blocks AUDIT-LEDGER gate |
| Release staging (version bump ✓, CHANGELOG ✓, I5 promote) | version+changelog DONE; promote NOT STARTED |
| Doc reconciliation (all 116 docs → canon) | **DONE**, grep-clean |

## Done since last
Full initial swarm landed (greens on per-agent worktree branches, batch-integrate): design-bindings green; plane-purity+neutral-no-dialect green; construction 7 rows green (real consolidation, not ceiling-hacks); kind-isolation deps/test-deps green; 12/14 HIGH findings fixed; **Commit-C verified byte-identical**; both design plans ratified; doc reconciliation grep-clean; version bump (14 crates)+CHANGELOG; DECISIONS #17 (one-feature-per-plane) + #18 (streaming≠voice) locked + hook updated.

## Gotchas / risks
- Landed greens are on isolated worktree branches based on `16319d64e`; tip is now `cbcfa2216` → cherry-pick/rebase needed at batch integration (heartbeat owns it).
- `audit-ledger.json` drifted (generated at an older HEAD) → AUDIT-LEDGER gate red until regen at the frozen ship SHA.
- Real remaining reds all converge on the switch-over cutover + owner decision #2; the cutover is deep/money-adjacent → careful serial execution, not blind swarm.

## Blocked on owner
Not hard-blocked on the parallel work; decision #2 gates kind-isolation full-green, #1/#3 gate streaming money semantics.

## Questions queued for owner (numbered — why + recommendation)
1. **CostHold for streaming.** Why: `duplex-plane-and-realtime.md` mandates mid-session `cost_reserve`/`cost_settle` (hard-stop a live stream on budget) but the taxonomy demotes CostHold to a non-1.6.0 extension point — conflict, and streaming is in 1.6.0. **Rec:** ship streaming with end-of-turn metering (byte-identical Meter step); mid-session hard-stop stays an extension point. Say so to pull it into scope.
2. **admin/oauth2 mint (classification + ledger cells).** Why: the two newly-extracted crates carry ~3.5k cross-kind vocab refs and are absent from the audit + kind ledgers, so this ONE decision blocks BOTH the kind-isolation `registry`/`matrix` gate AND the AUDIT-LEDGER gate (regen agent confirmed the only fix mints `busbar-admin/src`+`busbar-oauth2/src` scopes). **Rec:** classify as legacy/core-family (compiled-in, drained post-1.6.0 with the busbar-core retirement arc — not a cut blocker) + advance the merge-base once with their audit + kind cells (`cargo xtask ledger sync --write` for audit; kind cells for matrix). The merge preserves findings (no open→clean). Need your nod on the merge-base advance (won't move it autonomously).
4. **Push canon to origin/integration.** My 9 doc/planning commits (DECISIONS/PLAN/STATUS, incl. canon) are LOCAL-only; agents keep landing on origin's stale `eeddd6815` (no DECISIONS.md) and self-reset. A `git push origin integration/oracle-phase0` (FF, doc-only) fixes it but the auto-mode classifier blocks it as out-of-place publication. **Rec:** you push it (or grant the Bash push rule); meanwhile I keep agents on the local canon-bearing tip.
3. **gemini `UsageIdentityNote` (write-only).** Why: codec computes a provider token shortfall nothing reads; changing the billed figure = a registered money change (needs sign-off). **Rec:** wire a consumer in busbar-llm that surfaces it as a `RefusalReason::MeterDisputed` fact (post lower evidence + flag dispute), do NOT change the billed number. Confirm.

Standing (not re-asked): deny-and-fix on byte moves = DECISIONS #10; integration-push authority granted; never qa/main; fleet = spot-only/zero-idle, brought up autonomously once a green candidate exists.

---
Format is fixed; diff this file between checks to see progress. Author: Matthew <dev3@getbusbar.com>.
