# BUSBAR 1.6.0 — STATUS

**As-of:** 2026-09-16 21:13 PDT · **Tip:** `integration/oracle-phase0` @ `16319d64e` · **Phase:** swarm launched (day 0), first results pending

**ETA:** Target **Sat 2026-09-20** · Worst-case floor **Wed 2026-10-08 17:00 PDT** · **Δ since last: HELD** — why: swarm just launched under `go`; no agent results back yet to move either number.

**Status:** ON TRACK (day 0). 9/20 is the push target; 10/8 is the commitment that won't slip silently.

## How much is left (big rocks)
| Rock | State |
|------|-------|
| Money-path **Commit C** (decompose atomic 26k-LOC move → oracle-green sub-steps, then execute) | IN DESIGN (decomposition agent running; execution gated on ratified plan) |
| **Composition-root switch-over** D30→D31→D32 (unblocks PARITY, EQUALITY-root, ISOMORPHISM, TELLER-STEPS) + audit-chain relocation | IN DESIGN (plan agent running) |
| **Frozen-SHA audit** (audit-ledger clean + two §11 audits ≥9/10 + J3-to-dry) | NOT STARTED (waits for a frozen tip) |
| 5 RED gates (kind-isolation, plane-purity, design-bindings regen, construction ceilings/pick/pricing) | IN PROGRESS (4 fixer agents) |
| busbar-llm ABI purity (H1/C7/C9 + neutral fixture seam) | IN PROGRESS |
| 14 open HIGH audit findings | IN PROGRESS (2 batch agents: xtask 5, planes/codec/breaker 7) |
| Fleet oracle closure (~379 golden cells + mcp/a2a rigs + 3 store-persist) | BLOCKED on a green candidate binary (needs ≥1 spot box) |
| Doc reconciliation (all design docs → DECISIONS.md/VISION/LOCKED) | IN PROGRESS (finisher agent) |
| Release staging (version bump, CHANGELOG [1.6.0], I5) | NOT STARTED |

**DONE-oracle:** ~14/21 groups green · 7 red (4 collapse to the switch-over root).

## Done since last
- Committed permanent, never-compacted `DECISIONS.md` (16 locked decisions + enforcing gates) + `1.6.0-PLAN.md` (`16319d64e`); wired both into the every-turn READ-FIRST hook.
- Reconciled TRACKER/PLUGIN-TREE/ARCHITECTURE to DECISIONS (dialects-not-a-kind, admin/oauth-not-plugins). Marketing audit: no contradictions; 1 design fix owed (audit-chain home) folded into switch-over.
- Launched 10-agent swarm (2 design, 8 isolated fixers), ramping to 20+ as worktrees free.

## Gotchas / risks
- Money-path atomic move + frozen re-audit are **serial poles** — agent count can't collapse them; that's the whole 9/20-vs-10/8 gap.
- Isolated worktrees must integrate cleanly onto `oracle-phase0`; I own the merge and watch for collisions (never force-remove a locked worktree).
- Any oracle RED on a money-path sub-move = deny-and-fix by default (DECISIONS #10); I won't self-approve a moved billed byte.

## Blocked on owner
- None blocking right now.

## Questions queued for owner (answer at 8am to unblock)
1. **Fleet box for oracle closure.** Why: ~379 golden cells + mcp/a2a rigs + 3 store-persist need ≥1 box with stores up; can't close locally, and it gates 4+ oracle groups. **Recommendation:** authorize me to bring up spot boxes autonomously under the standing spot-only/zero-idle rule (terminate on close) so I don't stall waiting for you — I'll proceed on that basis unless you say otherwise.

_(Standing, not re-asked: deny-and-fix on byte moves = locked (DECISIONS #10); integration-push authority = granted; never qa/main.)_

---
Format is fixed; diff this file between checks to see progress. Author: Matthew <dev3@getbusbar.com>.
