//! busbar-core — the engine library (placeholder).
//!
//! This crate exists AHEAD of the file move so that every `-p busbar -p busbar-core` cargo
//! selector in scripts/ and .github/ resolves and is proven non-vacuous on the un-moved tree.
//! The move commit fills it; see busbarAI-private/design/1.6.0-core-split-plan.md.
