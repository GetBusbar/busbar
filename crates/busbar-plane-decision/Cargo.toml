# busbar-plane-decision — THE DECISION PLANE (jev), as an ADAPTER and nothing else.
#
# WHAT THIS CRATE IS. One implementation of the plugin contract's plane kind for the "decision"
# protocol surface, dialect `jev` — a byte-identity passthrough to api.typesafe.ai's two operations:
# POST /v1/systemone and GET /v1/models. The signed design (DECISION #39, jev v5 sign-off) rules
# this the ONE crate for the whole plane: unlike busbar-plane-mcp/busbar-plane-a2a, which are pure
# adapters over a separate "impure host" crate (busbar-mcp/busbar-a2a) that carries the config
# section, the admin wiring and the registry declaration, jev is deliberately a SINGLE crate that
# holds the pure plane AND its own typed config section.
#
# `busbar_kernel::config::providers::ModelCfg`, reused VERBATIM for `decisions.models.<name>`, moved
# to `busbar_contract::config::ModelCfg` (DECISIONS #40/#38) and no longer requires this manifest to
# name the kernel. This manifest STILL names `busbar-kernel`, and that remains a DECISIONS #40
# dep-wall violation — an OWNER-KNOWN, OPEN one, not fixed by this pass: `src/registry.rs`'s
# crate-local `PlaneDecl`/`BuildCtx` constants (declared for a FUTURE root registration, not wired
# yet) still name `busbar_kernel::plane::registry::{PlaneDecl, BuildCtx}`, and those two types'
# OWN fields transitively name kernel-internal wiring — `plane_host::PlaneSlots`,
# `plane_routes::PlaneRouteSpec` (which itself names `axum`), `admin_verbs::AdminRouteSpec`,
# `plane::config::{PlaneCfg, PlaneEndpointCfg}`, `plane::store::PlaneStore`,
# `plane_host::{EngineTablesView, ContainerGateSink, EngineHost}`, `api::NamedDefView`, and
# `config::providers::{ProviderDef, ProviderDeploy, ProviderCfg}` — none of which is contract/ABI
# vocabulary under DECISIONS #38 (capability traits, wire types, neutral vocab). Moving `PlaneDecl`/
# `BuildCtx` into the leaf `busbar-contract` crate would have to drag all of that kernel-internal
# routing/admin/store surface with them, which `busbar-contract`'s own manifest forbids (it may name
# no kernel crate). So this dependency stays, reported rather than silently carried forward; see
# `src/registry.rs`'s own module doc.
#
# WHAT THIS CRATE IS NOT. There is no governance here, no breaker, no hook seat, no approval
# decision and no metering arithmetic. Those are units, and a unit is on the other side of the
# kernel from a plane. The metering method returns LOCATORS — where the numbers are — and never a
# number the ledger would post. The routing method returns a plan, never a connection. jev holds no
# durable state of its own (it is a stateless request/response passthrough, provider-scoped), so it
# declares no record schemas.
[package]
name = "busbar-plane-decision"
version = "1.6.0"
publish = false            # internal workspace crate; never goes to crates.io
edition = "2021"
# One pinned number, set to what CI verifies — the same rationale every other member states.
rust-version = "1.97"
description = "The decision plane for busbar: the plugin contract's plane kind implemented over the jev dialect (api.typesafe.ai byte-identity passthrough)."
license = "Apache-2.0"

[lib]
name = "busbar_plane_decision"
path = "src/lib.rs"

# NO FEATURES, ON PURPOSE. A plane compiled two ways is two planes, and the kernel would have no way
# to tell which one it registered. The contract crate holds itself to the same rule and asserts it.

[dependencies]
# THE CONTRACT. The one busbar crate a plugin manifest may name without argument. Also where
# `ModelCfg` (the model-serving config shape `decisions.models.<name>` reuses verbatim, uniform with
# `pools.models.<name>`) now lives — `busbar_contract::config::ModelCfg` — since DECISIONS #40/#38
# moved it out of the kernel.
busbar-contract = { path = "../busbar-contract" }
# NOT for `ModelCfg` any more (that is `busbar-contract`'s now) — ONLY for the crate-local
# `PlaneDecl`/`BuildCtx`-shaped constants in `src/registry.rs`, declared for a FUTURE root
# registration that gates on config-model Stage 1/2. This is a KNOWN, OPEN DECISIONS #40 dep-wall
# violation this pass could not close: see the header comment above and `src/registry.rs`'s own
# module doc for the full STOP report of what `PlaneDecl`/`BuildCtx` would drag into `busbar-contract`
# if moved there wholesale.
busbar-kernel = { path = "../busbar-kernel", default-features = false }
# THE RESERVED `upstream_credentials` SHAPE. `PoolCfg` (busbar-kernel) names the same enum for
# its own per-section override; naming it here keeps `decisions:` on the identical reserved-key
# grammar rather than a look-alike type.
busbar-api = { path = "../api" }
# The object notation the jev dialect speaks. Byte-identity passthrough of the body is the norm;
# this crate parses ONLY the handful of declared pointers (never `state`/`answers`) to build facts.
serde_json = { workspace = true }
serde = { workspace = true }
