# What this hand-back asks keep-proof / prove-latchkey to prove.
#
# families — the two families the OR-PIN-1 measurement is about: the three standing-red rows this
#            hand-back is measuring (neutrality|routes|admin-openapi-paths, admin.ops|GetOpenapiJson,
#            admin.ops|DeleteOverlaySection) all live inside `neutrality` and `admin.ops`. No product
#            code changed on this branch — only testing/shadow-oracle/oracle.pin (the tool pin) — so
#            no other family is in scope.
# tests    — empty: this branch is a data-only oracle-pin move, no Rust changed.
families = '^(neutrality|admin\.ops)([|.]|$)'
tests = []
