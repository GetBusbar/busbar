THE SCAN-FLOOR FIXTURE: a binary crate that holds Rust but no composition root. The gate must
report that it could not find its subject — never nineteen quietly passing rows about a `main.rs`
it never read.
