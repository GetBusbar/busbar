# THE CONFORMANCE SUITE REGISTRY — the ONE hand-authored input, and it holds NO status.
#
# DERIVED-never-typed (scripts/release-gate/expected-ids.sh:11-14): this file lists WHICH suites
# exist and their claim TYPE (`tier`) — the ceiling a suite may ever claim. It does NOT record
# whether a suite passed; that is read from the per-suite verdict artifacts (conformance/verdicts/)
# and reconciled into conformance/manifest.json by `cargo xtask gate conformance-sync --write`.
#
# `tier` is a registry constant and is the CEILING (CONFORMANCE-MATRIX-RULING.md):
#   certified        a third-party body ISSUED a cert (requires cert fields in the verdict)
#   conformant       passes an official/authoritative suite
#   spec-conformant  validated against a published spec; no vendor cert program exists
#   capable          implements the mechanism; not externally validated
#   evidence-only    busbar supplies audit EVIDENCE; never busbar's own cert
#
# A green suite may render only AT OR BELOW its ceiling. Adding a suite is one block here plus the
# suite's own verdict artifact — nothing on the README is hand-edited (design §7).

# ── GREEN in 1.6.0 (verdict artifacts present) ───────────────────────────────────────────────────

[[suite]]
id = "a2a"
standard = "Agent2Agent (A2A) Protocol"
plan = "official a2aproject/a2a-tck + a2a-harness + a2a-go reference peer"
tier = "conformant"
label = "A2A"
verdict = "conformance-verdict-a2a"
public = "true"

[[suite]]
id = "llm-anthropic"
standard = "Anthropic Messages API"
plan = "spec battery vs pinned vendor OpenAPI"
tier = "spec-conformant"
label = "LLM Anthropic"
verdict = "conformance-verdict-llm-anthropic"
public = "true"

[[suite]]
id = "llm-bedrock"
standard = "Amazon Bedrock Runtime API"
plan = "spec battery vs pinned botocore models"
tier = "spec-conformant"
label = "LLM Bedrock"
verdict = "conformance-verdict-llm-bedrock"
public = "true"

[[suite]]
id = "llm-cohere"
standard = "Cohere Chat API"
plan = "spec battery vs pinned vendor OpenAPI"
tier = "spec-conformant"
label = "LLM Cohere"
verdict = "conformance-verdict-llm-cohere"
public = "true"

[[suite]]
id = "llm-gemini"
standard = "Google Gemini API"
plan = "spec battery vs pinned discovery document"
tier = "spec-conformant"
label = "LLM Gemini"
verdict = "conformance-verdict-llm-gemini"
public = "true"

[[suite]]
id = "llm-openai"
standard = "OpenAI Chat Completions API"
plan = "spec battery vs pinned vendor OpenAPI"
tier = "spec-conformant"
label = "LLM OpenAI"
verdict = "conformance-verdict-llm-openai"
public = "true"

[[suite]]
id = "llm-responses"
standard = "OpenAI Responses API"
plan = "spec battery vs pinned vendor OpenAPI"
tier = "spec-conformant"
label = "LLM Responses"
verdict = "conformance-verdict-llm-responses"
public = "true"

[[suite]]
id = "mcp"
standard = "Model Context Protocol"
plan = "official modelcontextprotocol TCK + in-house battery"
tier = "conformant"
label = "MCP"
verdict = "conformance-verdict-mcp"
public = "true"

[[suite]]
id = "voice-gemini-live"
standard = "Google Gemini Live API"
plan = "spec battery + replay"
tier = "spec-conformant"
label = "Voice Gemini Live"
verdict = "conformance-verdict-voice-gemini-live"
public = "true"

[[suite]]
id = "voice-openai-realtime"
standard = "OpenAI Realtime API"
plan = "spec battery + replay"
tier = "spec-conformant"
label = "Voice OpenAI Realtime"
verdict = "conformance-verdict-voice-openai-realtime"
public = "true"

# ── PLANNED for 1.6.0 (no verdict yet → not-run → renders NOTHING) ────────────────────────────────

[[suite]]
id = "fapi2"
standard = "FAPI 2.0 Security Profile"
plan = "OIDF FAPI2 conformance suite (self-hosted)"
tier = "conformant"
label = "FAPI2"
verdict = "conformance-verdict-fapi2"
public = "true"
not_run_reason = "planned 1.6.0 — PAR/DPoP/client-assertion wiring in flight; paid OIDF listing is owner-owned, the code is built to pass the suite (MATRIX-RULING owner ruling 2026-09-19)"

[[suite]]
id = "h2"
standard = "HTTP/2 (RFC 9113)"
plan = "h2spec against busbar-transport-http"
tier = "conformant"
label = "HTTP/2"
verdict = "conformance-verdict-h2"
public = "true"
not_run_reason = "planned 1.6.0 — h2spec leg not yet green (MATRIX-RULING B-tier report card)"

[[suite]]
id = "jev"
standard = "jev decision plane"
plan = "2 typesafe SDK peers exercising /v1/systemone + /v1/models"
tier = "conformant"
label = "jev"
verdict = "conformance-verdict-jev"
public = "true"
not_run_reason = "planned 1.6.0 — jev wiring stage in flight (MATRIX-RULING §jev)"

[[suite]]
id = "oidf-oauth2"
standard = "OpenID Foundation OAuth2 conformance suite"
plan = "self-hosted OIDF OAuth2 plan (openid=plain_oauth)"
tier = "conformant"
label = "OIDF OAuth2"
verdict = "conformance-verdict-oidf-oauth2"
public = "true"
not_run_reason = "planned 1.6.0 — self-hosted OIDF leg not yet green; claim is 'passes the suite', NEVER 'certified' (MATRIX-RULING owner ruling 2026-09-19)"

[[suite]]
id = "slsa-verifier"
standard = "SLSA provenance (v1.0)"
plan = "slsa-verifier over the release attestations"
tier = "conformant"
label = "SLSA"
verdict = "conformance-verdict-slsa-verifier"
public = "true"
not_run_reason = "planned 1.6.0 — slsa-verifier leg not yet wired as an arm-or-red verdict"

[[suite]]
id = "tls"
standard = "TLS (rustls, no OpenSSL)"
plan = "testssl.sh grade against a loopback subject"
tier = "conformant"
label = "TLS"
verdict = "conformance-verdict-tls"
public = "true"
not_run_reason = "planned 1.6.0 — testssl leg not yet green (MATRIX-RULING B-tier report card)"

[[suite]]
id = "ws"
standard = "WebSocket (RFC 6455)"
plan = "Autobahn|Testsuite against busbar-transport-ws"
tier = "conformant"
label = "WebSocket"
verdict = "conformance-verdict-ws"
public = "true"
not_run_reason = "planned 1.6.0 — Autobahn leg not yet green; currently zero coverage (MATRIX-RULING)"
