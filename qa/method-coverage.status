# METHOD COVERAGE STATUS -- the only place a cell of qa/method-inventory.json may be declared
# anything other than MISSING.
#
# Owner's ruling, 2026-08-12: "i want a customer to be able to do anything mcp says it can do and
# anything a2a says it can do. so if thats calling 20 methods we need to support it. how internally
# though doesnt matter. i want full alphabet coverage not just A-J. I want A-Z"
#
# THREE STATES PER CELL, and there is no fourth:
#
#   implemented                          the cell works, and an instrument was watched saying so
#   waived <YYYY-MM-DD> <reason>         deliberately absent, with a reason a customer could read
#   (absent from this file)              MISSING -- and MISSING FAILS THE BUILD
#
# FORMAT, following crates/busbar/src/config/config-schema.waivers, which is the house waiver
# format and is this file's precedent:
#
#   - One entry per line:  <exact-cell-id> = implemented
#                          <exact-cell-id> = waived <YYYY-MM-DD> <reason>
#   - EXACT cell ids only. No globs. `*` or `?` is a hard error -- a wildcard waiver is how a
#     whole transport disappears in one line.
#   - A reason is MANDATORY on a waiver. `= waived 2026-08-12` with no text is a hard error.
#   - A STALE entry (a cell id that is not in qa/method-inventory.json) is a hard error, so a
#     method that the SDKs renamed cannot keep an entry that no longer excuses anything.
#   - Blank lines and `#` comments are ignored.
#
# WHY A FILE AND NOT A CONSTANT IN THE TEST: a waiver here is a line in the PR diff, sitting next
# to the obligation it excuses, dated, with the reasoning attached. That is the whole point.
#
# WHAT COUNTS AS A LEGITIMATE WAIVER. The PUSH-DELIVER precedent (goal D16): PUSH-DELIVER-001/002/
# 003 stay red on purpose because the TCK's webhook receiver defaults to localhost and busbar's
# SSRF guard correctly refuses it. A security control winning over a conformance score is a
# legitimate waiver. "We ran out of time" is ALSO a legitimate waiver -- but only if it is written
# down here, dated, with the customer-visible consequence spelled out.
#
# WHY THIS FILE IS EMPTY OF `implemented` LINES TODAY, and that is not an oversight.
# `crates/busbar/src/mcp/` and `crates/busbar/src/a2a/` are being deleted whole (goal D10) and
# rebuilt cell by cell on the Protocol x Operation x Transport axes. Claiming a cell here on the
# strength of code that is scheduled for deletion would be claiming coverage the release does not
# have. Every cell is therefore MISSING, the full list is pinned in qa/method-coverage.missing,
# and THAT LIST IS THE WORK QUEUE FOR THE REST OF 1.6.0. A cell is claimed here when its leg has
# been watched green on an instrument -- read the output, not the tick.

# ---------------------------------------------------------------------------
# implemented
# ---------------------------------------------------------------------------

# MCP CLIENT DIRECTION, STDIO TRANSPORT. An authenticated inbound `tools/call` on
# busbar's own front door reaches a SUPERVISED CHILD PROCESS: spawn, newline-delimited
# JSON-RPC on its stdin/stdout, capped backoff on a crash and a five-crashes-in-a-window
# quarantine. Watched green on the instrument, not inferred:
#   mcp::upstream::stdio_dispatch_tests::a_tools_call_reaches_a_child_process_and_returns_its_result
#   mcp::upstream::stdio_dispatch_tests::a_crash_looping_child_backs_off_and_is_quarantined_through_the_dispatch_path
# The second one goes RED if the supervisor is bypassed, which is what makes the first
# one a claim about a transport rather than about a fork.
mcp|stdio|client|client|tools/call = implemented

# MCP CLIENT DIRECTION, STDIO TRANSPORT, `tools/list`. The drift re-check asks a CHILD PROCESS for
# its tool list over the same vtable the call rides, so an operator's approved digests are compared
# against what the child is actually serving. Watched green on the instrument:
#   mcp::upstream::stdio_dispatch_tests::a_refresh_re_observes_a_child_process_tool_list
# It is claimed rather than left in the queue because the alternative was worse than absent: the
# refresh used to send through the HTTP transport unconditionally, which on a stdio registration
# would have POSTed to an empty string and DEMOTED a healthy server for failing a contact busbar
# addressed to nowhere.
mcp|stdio|client|client|tools/list = implemented

# ---------------------------------------------------------------------------
# waived
# ---------------------------------------------------------------------------

# THE STDIO SERVER DIRECTION, ANSWERED RATHER THAN LEFT SILENT. The coverage matrix
# implies the cell and nobody had ruled on it; this is the ruling. The reason is
# repeated on every line because the format takes exact ids and no globs -- a
# wildcard waiver is how a whole transport disappears in one line.
mcp|stdio|server|client|completion/complete = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|client|initialize = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|client|logging/setLevel = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|client|notifications/cancelled = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|client|notifications/elicitation/response = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|client|notifications/initialized = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|client|notifications/progress = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|client|notifications/roots/list_changed = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|client|ping = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|client|prompts/get = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|client|prompts/list = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|client|resources/list = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|client|resources/read = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|client|resources/subscribe = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|client|resources/templates/list = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|client|resources/unsubscribe = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|client|server/discover = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|client|subscriptions/listen = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|client|tasks/cancel = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|client|tasks/get = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|client|tasks/update = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|client|tools/call = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|client|tools/list = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|server|elicitation/create = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|server|notifications/cancelled = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|server|notifications/message = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|server|notifications/progress = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|server|notifications/prompts/list_changed = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|server|notifications/resources/list_changed = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|server|notifications/resources/updated = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|server|notifications/subscriptions/acknowledged = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|server|notifications/tasks = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|server|notifications/tools/list_changed = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|server|ping = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|server|roots/list = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
mcp|stdio|server|server|sampling/createMessage = waived 2026-08-12 busbar is not itself launched as a child process. The stdio SERVER direction means an agent spawns `busbar` and speaks MCP on its stdin -- one gateway process per client, with no listener, no TLS, no admin plane and no shared store. Every control busbar exists to apply is scoped to a long-lived multi-tenant listener: virtual keys, budgets, the audience validation that is the confused-deputy defence, the per-call log chain, the trust lifecycle. None of them means anything inside one client's own child process, and a busbar that served MCP there would be a governance gateway with the governance switched off. The OUTBOUND stdio direction is implemented instead, which is the one a gateway is for: busbar fronts local stdio MCP servers on behalf of many callers. Reversible -- it is a product decision, not a technical block.
